---
title: 2026-09-18
date: 2026-09-18
tags: daily
---

# 2026-09-18

## 今日重点

> 从 09-17 的 Daily 带过来（09-17 当天 50+ 条工作日志，全部实质内容，无归档；昨晚 17:10 / 21:10 两批 push 已把当时"大量未提交"清零）。

- [ ] luka **真机业务验证**：真机链路已打通，业务主流程（登录 / 划卡 / 聊天）还没在真机上回归
- [ ] **真机复测近期改动批**：Discover 追加式分页 + 2/3 预取 + 菊花 / 性能收尾 / 首帧窗口化 / 胶囊动画；亲密度主页三段动作；Quick Pick（`dcaa9837` 已 push）；聊天键盘跟随（`087d00e3`）
- [ ] Chats 两条待真机确认：白卡底部不被标签栏遮、爪印分割线居中
- [ ] `fix/luka-code-review` 合回 `feature/luka-ios`：merge → 解冲突（预期 `app/_layout.tsx`、`app/dev/_layout.tsx`、`dev/index.tsx`、`lib/dev-gate.ts`）
- [ ] Luka 提审阻断闭环（产品/外部）：客服邮箱占位（建议 `support@lukasoc.com`）、FAQ story/stories 模板残留、Live chat「~2 min」与实现不符
- [ ] edit profile 剩余屏 `photos`；Nearby 列表模式缺仓鼠；未决设计：photos 单/多图、interests 平铺/分组、AI tip 去留
- [ ] template-migrations 落后 26 条（0009 → 0035，支付与 review-mode 实现）
- [ ] 远端已合并分支清理（`feat/luka-edit-profile` 等）

## 工作日志

## 工作日志 — 10:19 整理 09-17 + 汇总本周周报（9.11–9.17）

- **做了什么**：读 9.11–9.17 六天 Daily（170+ 条工作日志）与两仓库 git 提交，按五条工作线汇总成周报草稿（Luka 主线 / Figma 资产 / sitin-rn 基建 / sitin-next / 其他），并把 09-17 未完成待办带到今天。
- **为什么**：用户要准备本周周报；同时按每日规则补做前一天整理。
- **结果**：周报草稿产出；09-17 的坑当晚已沉淀（`expo-missing-native-module-guard`、`rn-overlapping-views-touch-swallow` 等），无垃圾归档。
- **关键命令 / diff**：`rg` 提取各日标题 + 今日重点；`git log --all --author` 统计提交（sitin-rn 全分支 271 条 / sitin-next 6 条）。

## 工作日志 — 10:22 周报写入飞书 wiki「张峥」部分（表格整体替换）

- **做了什么**：把 9.11–9.17 周报写进飞书 wiki `O35wwq2D7ihoqskw7w1cZGdjnih` 张峥部分——「本周工作」bitable 旧 23 条记录删除，新增 5 条父项 + 25 条子项（Luka 提审 + UI 重做 / Figma 资产 / sitin-rn 基建 / sitin-next / 其他）；「风险与阻塞」「下周计划」两块同步替换为新内容（用户确认）。
- **为什么**：用户要求把新周报替换掉原来表格中的内容。
- **结果**：文档 revision 148 → 150；读回验证表内 count=30，列表 3 + 4 条正确。
- **关键命令**：`lark-cli base +record-batch-create` → `+record-delete --json ... --yes`；`lark-cli docs +update --command block_delete` + 两次 `block_insert_after`。
- **踩坑**：无（profile 为张峥，token needs_refresh 不影响读写）。

## 工作日志 — 10:32 拉取 feature/luka-ios（+6 commits）并修 expo-camera 解析报错

- **做了什么**：`git fetch` + `git pull --ff-only` 把 sitin-rn `feature/luka-ios` 从 `dcaa9837` 快进到 `d681de27`（PR #520，6 commits / 183 files）；随后 iPad 模拟器 Metro 报 `Unable to resolve module expo-camera`（`apps/luka/src/app/ar.tsx`）——根因是拉入的新代码声明了 `expo-camera: catalog:`（→ 57.0.4）但没跑 `pnpm install`，install 装机后已解决 JS 解析。
- **为什么**：同步 caobo 的 AR 陪伴场 / Chats·Discover 玩法层 / Personal Team 真机链路；并处理用户截图里的红屏。
- **结果**：`expo-camera@57.0.4` 已装入；但 `expo-camera`、`apps/luka/modules/arkit-play`（Expo 本地模块）、`patches/react-native@0.86.0.patch` 都是原生面，已有 dev client 二进制里没有 → 必须 `pnpm luka:ios` 重出开发包（必要时先 `pnpm luka:prebuild`）。
- **关键命令 / diff**：`git pull --ff-only origin feature/luka-ios`；`pnpm install`（+73 −98，husky prepare OK）。
- **沉淀**：Metro `Unable to resolve module X` 的第一反应是「依赖没装完」，但 X 若带原生面，装完 JS 依赖只是第一步——旧 dev client 仍需重编。与 `expo-missing-native-module-guard` 同属「基包没跟上」主题，已在其中记过「明确要求重出基包」，不另开文件。

## 工作日志 — 11:15 luka dev client 重出：修 pod install 的 ExpoModulesCore 版本冲突

- **做了什么**：用户跑 `pnpm luka:ios` 在 `pod install --repo-update` 失败。手动复现拿到真错误：`CocoaPods could not find compatible versions for pod "ExpoModulesCore"`——`Podfile.lock` 快照里的版本与 `Pods/Local Podspecs` 不一致（本次拉代码后 `expo-modules-core` 升到 57.0.18）。先按提示 `pod update ExpoModulesCore`，下一环坏在 `ExpoModulesWorklets`（同包）；删 `Pods/Local Podspecs` 重装仍被 lock 拦；最终 `pod update ExpoModulesCore ExpoModulesWorklets ExpoModulesWorkletsAdapter --no-repo-update` 一次解决。
- **为什么**：拉入的新代码（PR #520）钉了 `expo-modules-core` 版本并新增 `expo-camera`，旧 `ios/` 的 pod 缓存全部过期。
- **结果**：`Podfile.lock` 已含 `ExpoCamera (57.0.4)` / `ExpoModulesCore (57.0.18)`，`Pods/ExpoCamera` 已安装，本地模块 `ArkitPlay` 已接入（podspec 4 处引用）；可重跑 `pnpm luka:ios`。
- **关键命令**：`pod install`（复现）→ `pod update ExpoModulesCore ExpoModulesWorklets ExpoModulesWorkletsAdapter --no-repo-update`。
- **踩坑**：① `pod install` 报「版本不兼容」且指向 path pod 时，逐个 `pod update <名字>` 会连环报下一环，直接把同包相关 pod 一起 update 或删 `Pods/Local Podspecs`+重装；② 本机 shell 当前 Node 是 v22（`.nvmrc` 要 24），跑 luka 脚本前先 `nvm use 24`。

## 工作日志 — 11:54 Luka Discover 玩法层回退 + 划卡毛玻璃刮刮乐与右下小崽

- **做了什么**：
  1. 按用户要求把 Discover 页整体回退到 `69137bb1` 之前（`dcaa9837`）：只 restore 4 个接线文件 —— `(tabs)/discover.tsx`（品牌名 + 心页头）、`discover-nearby/nearby-view.tsx`（DeckHero + 照片卡 DiscoveryDeck + 瀑布流列表）、`discover-nearby/discover-view-toggle.tsx`（白壳开关）、`discover-feed/feed-fab.tsx`（FAB 顶上旧小崽）；房间背板 / 居中 wordmark / 票卡玩法的组件与素材保留不删，只是不再挂载。
  2. 还原后的划卡右下角加同款小崽视频 `ParkPassHamster`（`right: cardInset + 2`、`bottom: tabBarClearance + BELOW_CARD_HEIGHT − 16`；`BELOW_CARD_HEIGHT` 改为导出）。压在卡上必须 `pointerEvents="none"`。
  3. 新增 `components/discovery/frosted-scratch.tsx`：参考 `ParkPassScratch` 的网格挖洞 / peel 逻辑（复用 `lib/discover-play.ts`），外观从镭射箔换成 SVG 毛玻璃 —— 半透明白底 0.52 + 斜向高光 + 4pt `Pattern` 颗粒 + 一圈亮边，`<G mask>` 挖洞；接到 `CandidateCard`（传 `scratchActive` 才挂）与 `DiscoveryDeck`（每人一个 gesture ref + `CardStack.waitForGesture` 仲裁，只有当前卡可刮）。
- **为什么**：用户不要 9/18 上午 PR #520（`69137bb1`）的 Discover 玩法层，要回旧版页面，但把票角那只小崽与刮刮乐以「旧版照片卡 + 毛玻璃」的形式保留在划卡上。
- **结果**：模拟器逐屏验证 —— Nearby 旧卡 + 毛玻璃 + 右下小崽 ✓、Feed FAB 旧小崽 ✓、注入拖拽刮开后 mask 洞与 20% 阈值 peel ✓、刮层与上滑仲裁正常（向下拖未误触划卡）。`luka:typecheck` / biome 全过；`luka:test` 6 files / 8 tests 失败，与改动前基线**完全一致**（HEAD 已有失败）。文档同步 `docs/discover-play.md` + README 现状段。未提交（规范模式）。
- **关键命令 / diff**：`git restore --source=dcaa9837 --worktree -- <4 files>`；`xcrun simctl openurl booted "luka://discover?tab=nearby"`；JXA CGEvent 拖拽。
- **沉淀**：[[ios-simulator-keyboard-and-taps]] 追加 §8（JXA CGEvent 拖拽注入，免编译）。
- **后续调整（12:11）**：按用户要求把玻璃底从 0.52 加浓到 0.85（照片只隐约透出，白色文字基本融进玻璃），`docs/discover-play.md` 同步；模拟器热更已确认。

## 工作日志 — 12:18 刮层从毛玻璃改高斯模糊 + 基础信息带上层底色

- **做了什么**：
  1. 按用户要求把刮层材质从「SVG 磨砂玻璃」换成**真高斯模糊**：`frosted-scratch.tsx` 重写 —— 用 react-native-svg 15.15 的 `<Filter><FeGaussianBlur stdDeviation={16}>` 对**同一张照片**（新增 `photo` prop）做模糊，`<Mask>` 白底 + 黑洞挖开，洞处透出底层 `expo-image` 的清晰照片；阈值 peel 逻辑不变。白底/高光/颗粒那一套（0.85→0.95 的白玻璃）全部删除。
  2. 基础信息露出来：新增 `candidate-card-info.tsx`（`photo` / `glass` 两变体）——`glass` 变体把距离药丸 + 兴趣/名字/职业带挪到模糊层**上层**，信息带加 `DeckColor.surface` 纸色底、名字换 `DeckColor.display` 墨色。`CandidateCard` 有照片才挂刮层，无照片回旧版。
  3. 文案/文档同步：`docs/discover-play.md`、README、`candidate-card.tsx` / `discovery-deck.tsx` 注释里的「毛玻璃/95%」措辞清掉。
- **为什么**：用户先要「毛玻璃再浓到 85% → 95%」，随后改口「别用毛玻璃了，用高斯模糊」——白纱+颗粒的假玻璃换成对照片本身的真模糊，同时要求基础信息不被糊掉、加底色。
- **结果**：模拟器验证 —— 模糊渲染 ✓、mask 洞与模糊严丝合缝对齐（临时初始洞截图确认后已还原）✓、拖拽刮开 20% 阈值整层 peel ✓、信息带与右下小崽都在 ✓。`luka:typecheck` / biome 全过；`discover-play` 两个测试文件 16 passed。
- **关键命令**：`xcrun simctl` terminate/launch + `luka://discover?tab=nearby` + JXA 拖拽（同 §8）。
- **坑 / 备注**：react-native-svg 的 `FeGaussianBlur` 在 iOS Fabric 生效；模糊层与底层照片必须同源同裁切（SVG `slice` ↔ expo-image `cover`），否则刮开的洞会与人脸错位。

## 工作日志 — 12:25 刮的笔刷收细

- **做了什么**：`frosted-scratch.tsx` 加两个本地常量 `SCRATCH_BRUSH = 1`（原票卡版默认 2，5×5 邻域）与 `HOLE_SCALE = 0.7`（洞圆半径 ×0.7），`markParkScratch` 与 `parkScratchHole` 的调用改传/乘它们；不动共享 `lib/discover-play.ts`（票卡版与测试零影响）。
- **为什么**：用户反馈「滑动的笔的粗细再细一点」——原一笔下去是一大片清晰。
- **结果**：模拟器短拖验证轨迹约为原来一半宽、连续无断珠；typecheck / biome 全过；`luka:test` 与基线一致（6 files / 8 tests，均为 HEAD 已有失败）。

## 工作日志 — 12:35 刮层不再锁死划卡：上滑与刮同时识别

- **做了什么**：用户问「现在不刮开不让划吗」——确认票卡版继承来的 `waitForGesture`（刮层优先）确实让未刮开的卡划不动。`CardStack` 新增 `simultaneousGestureRef`（与既有 `waitForGesture` 互斥，默认都不传则行为不变），`DiscoveryDeck` 从 `waitForGesture` 切到它；`candidate-card.tsx` 注释同步。
- **为什么**：回退版的划卡本来就随时可划，刮刮乐是附加效果，不该把划卡锁死；用户既没选「保持先刮后划」，按「不阻塞划卡」的合理默认推进（改回来只需换回一个 prop）。
- **结果**：模拟器注入验证 —— 未刮开上滑 60pt，卡片跟手上移（截图 1）、松手回弹不消耗额度（截图 2），同时轨迹上留下刮痕（两手势并行）；typecheck / biome 全过；`luka:test` 与基线一致（6 files / 8 tests 均 HEAD 已有）。

## 工作日志 — 12:55 划卡分区：未刮开只认信息带上滑，刮开后整卡可划

- **做了什么**：给两个手势加"起点分区"。
  1. `CardStack` 新增 `swipeBottomBand`（`SharedValue<number>`）：`onTouchesDown` 里 `touch.y < cardH - band` 就 `state.fail()`（用 `event.allTouches[0].y`，`GestureTouchEvent` 没有顶层 y）。
  2. `FrostedScratch` 加 `swipeBottomBand` / `onClear`：起点落在信息带（`touch.y > h - band`）就 fail，把触摸让给划卡；peel 时 `onClear` 通知外部。
  3. `CandidateCardInfo` 的 `glass` 变体 `onLayout` 上报信息带实测高度；`CandidateCard` 透传三个 props（`scratchBand` / `scratchOnClear` / `onInfoLayout`）；`DiscoveryDeck` 持有 `swipeBand`：info 高度（未刮开）→ `FULL_CARD_SWIPE_BAND = 1e6`（揭开后），换人时先归 0 等新卡量完；只认正面槽的测量与回调。
- **为什么**：用户要求「未刮开时只有信息栏能上滑划卡，刮开后整卡可划」。
- **结果**：模拟器标定后逐场景验证（日志 `[swipe-band]` 已删）—— 未刮开照片区上滑：卡不动、只刮 ✓；未刮开信息带上滑：卡跟手回弹 ✓；刮开后照片区上滑：卡跟手 ✓。typecheck / biome 全过；测试与基线一致。
- **坑 / 备注**：Simulator 窗口有缩放（实测 1 Mac pt ≈ 1.15 手势 pt），别拿截图 px 反推坐标 —— 用日志标定（两次不同 MacY 的 `touch.y` 求斜率与零点）。`state.fail()` 在 `onTouchesDown` 里对 simultaneous 手势有效，且能救回"起点不在分区内"的触摸给刮层/划卡。

## 工作日志 — 13:45 刮层模糊换原生 backdrop blur，修「边缘覆盖不全」

- **做什么**：用户反馈模糊在卡片边缘没盖全（露出清晰照片）。根因：SVG `FeGaussianBlur` 在画布边缘没有邻居像素可采样，把透明混进边缘（`edgeMode` 在 react-native-svg 里明确未实现）。`frosted-scratch.tsx` 渲染层重写 —— 模糊改用 `expo-blur` 的 `BlurView`（intensity 90、`systemUltraThinMaterialLight`，原生 backdrop 模糊、边缘不衰减；Android 无稳定真模糊，退化成半透明白纱，理由同 `HeaderBlurFade`）；刮开的洞改成「清晰照片副本 + 反向 mask（黑底白洞）」叠回模糊之上；peel 仍是整层 opacity 动画。
- **为什么**：边缘渗色会让整张卡看起来没盖住，且用户明确要高斯模糊（真模糊，不是白纱）。
- **结果**：模拟器验证 —— 卡片四边模糊完整无渗色；水平刮一条，洞处清晰图与周围模糊严丝合缝（同一 cover 裁切）；信息带/药丸/右下小崽不受影响。typecheck / biome 全过；测试与基线一致。
- **坑**：`SvgImage` 的 `slice` 与 expo-image 的 `cover` 裁切要一致，洞与底层才对得上；`BlurView` 必须铺在卡片内、信息层另压在它上面。

## 工作日志 — 13:52 Luka Chats 小窝：一页 8 张、滑动 + 按钮翻页、拆掉摇杆、未读角标转红

- **做了什么**：
  1. 一页 4 → 8 张 Polaroid（左右各 4 张）。`lib/chats-play.ts` 的 `NEST_VISITOR_SLOTS` 改成 `side/row/x/rotate` 结构、`NEST_PAGE_SIZE = 8`；新增 `nestVisitorRowLayout(height)`：按房间实测高度算 4 行行距、统一缩放（下限 0.7），末行落在 86% 不压 See all / 翻页键 —— 固定百分比在矮屏会盖住名字。
  2. 翻页：`NestPlayfield` 持有 `pageIndex`（从 `index.tsx` 收进来），横向 `Gesture.Pan`（`activeOffsetX ±18`、`failOffsetY ±24`，只认明确的横向拖拽）卡片按 `NEST_SWIPE_FOLLOW` 阻尼跟手，松手按距离/速度判翻页；新增 `NestPageControls`（左下 `‹ 1/5 ›`，两端禁用不循环，页码从顶部药丸挪进来）。
  3. 删掉摇杆三件套 `nest-move-pad.tsx` / `nest-dpad-arm.tsx` / `nest-move-pad-glyph.tsx`；`nest-polaroid.tsx` 未读角标 `Colors.caramel` → `Colors.badge`（红），并让 `pageTurnToken` 在非首次会话也能重播入场（共享值初值同时改 0，避免翻页闪一帧）。
  4. 分析 `position` 由「页内序号」改为全局序号（`safePage * 8 + index`）。
- **为什么**：用户要求「一页放 8 个，左右滑动翻页，下方加左右翻页按钮，不要摇杆」；追加「新消息角标要是红色的」。
- **结果**：`pnpm luka:typecheck` 过；`chats-play.test.ts` 13 passed（页 8、8 槽位、四行布局、两端夹取）；biome 过（新增 `nest-page-controls.tsx` 无圆角/投影违规）。全量 `luka:test` 6 files / 8 tests 失败与基线一致（均为工作区已有的 discover/onboarding 等失败，与本次无关）。**模拟器实拍已验证**：临时假数据 13 条 → 第 1/2 页各 8 张（左右 4+4、名字全可见、三颗红色角标）、第 2/2 页 5 张且 `›` 置灰、左下 `‹ 1/2 ›` 与 See all 同高不重叠；验证用的三处 TEMP 补丁（`_layout.tsx` profileComplete、mock 池、`useState(1)`）已全部还原（`git diff` 确认 `_layout.tsx` 无 diff，全仓 grep 无 TEMP）。
- **关键命令**：`pnpm luka:typecheck`、`pnpm --filter luka exec vitest run tests/lib/chats-play.test.ts`、`pnpm exec biome check apps/luka/src/components/chats-play ...`
- **坑 / 备注**：`NestPlayfield` 现在接收整池（不再收分页后的切片），分页与页码自持，`index.tsx` 只剩 `sortNestConversations`。行布局用 onLayout 实测前不渲染卡片层，避免 scale/位置闪一下。本机 CGEvent 点按在本会话宿主无辅助功能权限（`osascript` 报 -25211，鼠标事件静默丢弃），模拟器上验证不了「滑动手势」运行态；静态布局用临时强制 `profileComplete + mock 池 + 初始 pageIndex` 绕过了 onboarding 守卫，未依赖点按。

## 工作日志 — 14:16 Luka Chats 卡片行距：加 NEST_VISITOR_ROW_GAP

- **做了什么**：用户截图反馈「每个头像卡片上下保持一定距离」。根因：`nestVisitorRowLayout` 原来让 4 行正好铺满 band（step == 卡高），卡与卡零间距，旋转后角落几乎相碰。改为先扣 `NEST_VISITOR_ROW_GAP = 14`（`(rows-1)*gap`），再算统一缩放 `scale = (band - gaps) / (rows * cardH)`（下限 0.7）；实测 iPhone 17（房间 ~609pt）下 scale ≈ 0.81，行距恒为 14pt（±8° 旋转在上下各吃掉约 4pt，视觉约 5–10pt）。
- **为什么**：挤在一起是公式的必然结果，不是随机效果；间距优先，卡按需缩小。
- **结果**：模拟器 6 张 mock 实拍 —— 三行之间都能看到地板缝、无重叠、See all / 翻页键不压卡；测试补了一条「相邻行距 = 卡高×scale + gap」；`luka:typecheck`、`chats-play.test.ts` 13 passed、biome 全过，TEMP 验证补丁已还原。已按用户「push」提交 `20063751 feat(luka): Chats 小窝每页 8 张，滑动/按钮翻页，拆掉摇杆`（11 files, +394 −355）并推送 `feature/luka-ios`（`d681de27b..20063751a`）；工作区里 discover / intro / pet 的并行未提交改动未动。
- **坑 / 备注**：行布局现在是「先留缝、再缩卡」；若以后嫌卡小，优先调小 `NEST_VISITOR_ROW_GAP` 或放宽 `NEST_VISITOR_BOTTOM_RATIO`，不要退回零间距公式。

## 工作日志 — 14:15 Luka 开场片换成 open2.mp4（720×1280 / 1.36 MB）

- **做了什么**：注册成功后（`/intro-video`）与 `/how-it-works` 共用的开场片从 `assets/intro/luka.mp4` 换成 `assets/pet/open2.mp4`。
  1. 用户丢来的母版是 1080×1920 / 10-bit HEVC / 13.0 MB / 带音轨（仓鼠在跑轮里的整屏合成片）——直接入包违反素材规矩（Android 无硬解 + 包体）。试压 320 / 720 / 1080 三档（H.264 8-bit、crf 24、无音轨，365 KB / 1.36 MB / 2.68 MB）后用户拍板 **720×1280**；用 `pnpm compress-mov` 原地替换，母版备份到仓库外临时目录（SHA-256 `2d3dee35…`）。
  2. `components/intro/intro-video-preload.ts` 的 `INTRO_VIDEO_SOURCE` 改 require `@/assets/pet/open2.mp4`；`intro-film.tsx` 注释同步。
  3. `components/pet/pet-video-source.ts` 的 dev 落盘缓存后缀改为跟源文件走（`.mp4` / `.mov`），不再把 mp4 存成 `.mov` 交给播放器。
  4. 文档：`pet/README.md` 新增「开场片」小节（母版 / 成品规格、三档对比、转码命令）；`intro/README.md` 去掉 luka.mp4 供 how-it-works 的说法、交叉引用 open2，并修正入库 luka.mp4 的实际规格；`assets/README.md` 表格与规矩 1 的数字同步。
- **为什么**：用户要求「把 onboarding 之后这个视频替换为 apps/luka/src/assets/pet/open2.mp4 这个」。
- **结果**：成品 H.264 High / yuv420p / 720×1280 / 24fps / 8.04s / 无音轨 / 1.36 MB；biome 3 文件 clean、`luka:typecheck` 0、onboarding design 测试「预加载接线」通过；全量 `luka:test` 6 files / 8 tests 失败与基线一致（把本次 3 个源文件 stash 掉复跑，同样是 6 files / 8 tests，均为工作区已有的 discover / 守卫 / 圆角失败）。
- **关键命令**：`pnpm compress-mov <母版> <out> --size 720x1280 -an -q 24`、`pnpm luka:typecheck`、`pnpm luka:test`。
- **坑 / 备注**：母版不入库（备份在 `/var/folders/.../opencode/open2-check/open2.master.mp4`）。运行态起播未验证（要跑 Metro + 模拟器，用户手上的 dev 包可 `luka://intro-video` 深链看）；`assets/pet/open2.mp4` 目前仍是未跟踪文件，未提交。

## 工作日志 — 14:25 push Discover（da7f4ea8）

- **做了什么**：把今日 Discover 全部改动提交为 `da7f4ea8`（11 文件：回退 4 个接线 + 新增 `frosted-scratch.tsx` / `candidate-card-info.tsx` + `CandidateCard` / `DiscoveryDeck` / `CardStack` + README/docs）并 push 到 `origin/feature/luka-ios`。工作区里并行会话的 intro/pet/login/_layout 等在途改动**未**混入本次提交（按文件逐个 stage）。
- **验证**：pre-commit biome（9 文件）OK；pre-push boundaries + luka typecheck 全过（9.1s）。
- **备注**：分支 design 测试仍有 8 个失败（`protected-routes` / `onboarding-flow` 那条与 PR #520 删守卫有关，见今日排查记录），push 不受影响，但 PR 门禁会红。

## 工作日志 — 14:25 修登录页（视频版样式）+ 复原 PR #520 带回的合并回归

- **背景**：用户截图报「左上角按钮没了 / 字体颜色看不清」。排查发现 PR #520（`69137bb1`）把主线 09-17 的登录页 hero 换回全屏视频、`_layout.tsx` 退回旧版（丢 29 条路由守卫声明、`/intro-video` 声明、widget 同步、dev host），只是忘了同步图标 tint 与文字色。用户拍板：**登录页保留视频版**（Figma 17 · Welcome · video），其它回归一并修。
- **登录页（`app/login.tsx` + `components/login/*`）**：
  1. `welcome-widget-guide-button` 去掉 `tintColor`（它会把白圆片 + 墨格刷成实心棕点），补注释「不要再 tint」；`welcome-sign-in-link` 改回 `Colors.white`，`welcome-legal-links` 改回 `rgba(255,255,255,0.72)`，注释同步视频口径。
  2. 恢复 09-17 审核修复：iOS 换回 Apple 官方 `AppleAuthenticationButton`（WHITE / SIGN_IN / 圆角 29）+ Apple 失败 `setError`（分支把它退回了自绘胶囊 + 静默失败）。
- **`_layout.tsx`**：整体取合并前 main（`dcaa9837`）的版本，再补回分支新增的 `ar` 全屏路由。恢复内容：exhaustive 守卫清单（chat/compose/settings/edit-profile/quick-pick…）、`/intro-video` 的 `authed && !pendingDeletion` 守卫、`how-it-works` 声明、`account-deletion` 的 `pendingDeletion || authed`、`startWidgetSync/stopWidgetSync/clearWidgetSnapshot`、`LoveRingPreviewHost`（dev gate）。
- **测试**：`guest-path.test.ts` 两处期望对齐现状 —— 邮箱登录改判 `executeEmail(normalizedEmail, password)`、开场片跳过键改判共用组件 `components/intro/intro-film.tsx`（并保留 intro-video 复用断言）；Apple 断言改为官方组件。
- **SIWA 中文按钮**：官方按钮由系统渲染，`expo-apple-authentication` 的 plugin 默认写 `CFBundleAllowMixedLocalizations = true`，app 又没声明支持语言 → 跟随设备语言出中文。已在 `app.config.ts` 显式 `CFBundleAllowMixedLocalizations: false` + `CFBundleLocalizations: ["en"]` + `CFBundleDevelopmentRegion: "en"`（该 plugin 用 `?? true`，显式 false 可覆盖），并写进 `docs/app-store-review-findings.md`。**原生改动需 `pnpm luka:ios` 重出基包才生效**。
- **结果**：biome clean、`luka:typecheck` 0；`luka:test` 从 **6 文件 / 8 失败** 降到 **3 文件 / 4 失败**（shape-language 3 + candidate-card 1，均为并行会话 Discover WIP；`good-review` 那条是 expo `ImportMetaRegistry` 的收集错误，也与本次无关）。**未验证**：真机/基包上的视频登录页截图与 SIWA 英文（要重出包）。
- **关键命令**：`git show dcaa9837:apps/luka/src/app/_layout.tsx > apps/luka/src/app/_layout.tsx`、`pnpm luka:typecheck`、`pnpm luka:test`。
- **坑 / 备注**：`Stack.Protected` 只移除它声明的屏；漏声明 = 该路由在游客态仍可深链（`protected-routes` 测试就是钉这个）。合并分支前先跑一遍 design 测试，能挡住这类整文件回退。
- **提交 / push**：用户要求 push，拆成三笔：`a75cc738` 开场片换 open2.mp4、`5415a88d` 登录页视频版 + 官方 Apple 按钮 + SIWA 英文钉死、`61d7f34a` 复原 `_layout` 守卫等回归；已推 `feature/luka-ios`（pre-push：boundaries OK + luka typecheck 过）。

## 工作日志 — 14:33 Quick Pick 开局说明收进一屏：小崽 252 → 200

- **做了什么**：用户截图反馈 `Start picking` 被挤出屏幕，要求「仓鼠图片小一点，一屏展示全部」。`apps/luka/src/components/quick-pick/quick-pick-intro.tsx` 的 `HERO_WIDTH` 252 → 200（高 336 → 267，省 69pt；溢出量实测约 55pt），注释改写为「为什么是 200」。
- **为什么**：除小崽外的固定内容（标题 36 + 说明 3 行 63 + 段间距 32 + 清单卡 214 + 按钮区 86 + 底部 32）在 iPhone 17 上约 475pt，加页头与安全区后 336 的插画必然溢出；矮屏仍由 ScrollView 兜底。
- **结果**：模拟器 iPhone 17 实拍验证 —— 小崽、标题、三行说明、四条承诺、`Start picking` 全在一屏内且按钮完整（深链 `luka://quick-pick?source=debug` + `simctl screenshot`，截图在 `/var/folders/.../opencode/luka-qp/`）。biome clean、`luka:typecheck` 0。
- **关键命令 / diff**：`xcrun simctl openurl booted "luka://quick-pick?source=debug"`（Metro 8081 热更免重编）；diff 一处常量 + 注释。
- **未提交**（规范模式）。

## 工作日志 — 14:49 Chats 小窝：房间底图铺满整屏（页头/标签栏悬浮其上）

- **做了什么**：用户截图要求「这页背景图做成全屏的」。把 `NestPlayfield` 的舞台从「页头到标签栏」改成**整个 tab 场景**：
  1. `(tabs)/index.tsx`：`SafeAreaView` 拆成两条分支——loading 保持原样；正常态改成 `View flex-1` 里直接铺 `NestPlayfield`，页头 + `ConnectionBanner` 放进 `absolute inset-x-0 top-0` 的 `SafeAreaView` 悬浮层（兼触摸屏障，点标题空白不会落到墙上/门上），call 卡仍在 `bottom: tabBarClearance + 8`。
  2. `NestPlayfield` 新增 `topInset`（= `insets.top + SCREEN_HEADER_HEIGHT`）/ `bottomInset`（= `tabBarClearance`）：卡片带用 `nestVisitorRowLayout(stage.height − topInset − bottomInset)` 再用 `topInset` 把行头顶回页头下沿（卡片落点与改前逐像素一致）；See all / 翻页键 `bottom = bottomInset + (call 118 / 18)`；hint 气泡同样按 content 高度定位。
  3. `ScreenHeader` 导出 `SCREEN_HEADER_HEIGHT = 54`（h-[54px] 改 style），页头高度只有一处事实源。
  4. 小崽 `top-[29%] → 37%`（整屏后地毯中心在 ~65%，脚要落回地毯中心）；墙的点击热区 `top-[6%] → 14%`（否则被悬浮页头盖住）。
- **为什么**：用户要底图全屏；房间图与叠层必须共享同一容器（都用百分比），错开容器会让门/信堆与照片错位，所以只能整屏舞台 + 让位 insets，而不是把图单独提到 root。
- **结果**：iPhone 17 模拟器实拍（`luka:///` 深链 + `simctl screenshot`）——房间铺到状态栏与标签栏后面、页头/爪印悬浮可读、小崽站在地毯上、卡片与 See all 位置与改前一致；tab 栏毛玻璃透出地板（并行会话的 tab-bar 改动）。`luka:typecheck` / biome 全过；`luka:test` 3 files / 4 failed，与基线一致（shape-language 3 + candidate-card 1 + good-review 收集错误，都是并行会话的在途文件，offender 列表里没有本次文件）。
- **文档**：`docs/chats-play.md` 页面结构图 + 舞台说明、`assets/chats-play/README.md` 底图说明同步（含「9:16 图铺 19.5:9 会纵向拉伸 ~20%，要消掉得换更高比例底图」）。
- **未提交**（规范模式）。

## 工作日志 — 14:40 Luka 底栏改毛玻璃渐变（上浅下深）

- **做了什么**：`apps/luka/src/components/tabs/tab-bar.tsx` 底栏从「实心奶油底 + 6% 墨色顶线」改成毛玻璃渐变：
  1. iOS 用 6 层 `BlurView` 按高度比例叠（opacity 0.1/0.2/0.34/0.52/0.72/0.92，intensity 10）—— 上清下糊，顶边不出现硬模糊线（与 `BottomBlurFade` / `HeaderBlurFade` 同一条办法）。
  2. 上面压四段奶油渐变：`surface` 0.45 → `background` 0.72 → 0.92 → `cream` 1（locations 0/0.45/0.78/1），上端白雾比页面浅、底边比页面深一档。
  3. Android 无真模糊（Dimezis 掉帧），退化成只有渐变、每档更实（0.65/0.85/0.96/1）。
  4. 删掉 `borderTopWidth/Color` 与实心 `backgroundColor`；栏高与让位不变（`useReportBottomBar` 照旧）。
- **为什么**：用户「底栏做成毛玻璃渐变吧 上浅下深过渡自然」。
- **结果**：booted iPhone 17 模拟器实拍 + 像素采样验证 —— 顶边无硬线，木纹/白卡在栏顶被模糊 + 白雾，向下自然化进 cream；`luka:typecheck` / biome 过；`luka:test` 仍 3 文件 / 4 失败（把我的文件 stash 掉复跑一致，均为工作区已有失败）。
- **关键命令 / diff**：`xcrun simctl io booted screenshot` + `magick -format "%[pixel:p{x,y}]"` 竖直采样对账；滚动验证用 CGEvent 拖拽（§8）。
- **沉淀**：[[ios-simulator-keyboard-and-taps]] 追加 §9（像素采样验分层视觉、渐变同色端点不可见的坑）。
- **未提交**（规范模式）。

## 工作日志 — 14:45 luka 退出确认页按 Delete account 新稿重画

- **做了什么**：用户给两张图（当前 `See you soon?` 页 + `Close the nest?` 新稿）要求「这页重画成图 2 风格，并用图 2 的仓鼠」。`apps/luka/src/app/settings/logout.tsx` 整屏重写：
  1. 白底 👋 emoji + 直角块 → 奶油底（`bg-background`）、页头 `RoundBackButton` + 左对齐 `Sign out` 标题。
  2. 内容收进白卡（圆角 20 + 发丝线 `Colors.hairline`）；`hamster-peek-worried.png` 居中从卡片上沿探头，图片底边落卡沿下 5pt（沿用 `EmptyHero` 的台沿算法）。
  3. `we'll keep your place` 从直角淡块 chip 改成蜜色胶囊（`Colors.honey` + `Colors.honeyInk`）。
  4. 主按钮 58pt 整圆实心棕（`Button variant="coral"`），次动作仍是文字按钮；文案原样保留，未加图 2 的 bullet。
  `tests/design/shape-language.test.ts` 的 `MIGRATED` 登记 `app/settings/logout.tsx`。
- **为什么**：与 09-17 那批新稿（Settings hub / Manage Subscription / Delete account）统一视觉语言；旧版是这一族里最后几屏旧语言之一。
- **结果**：iPhone 17 模拟器深链 `luka://settings/logout` 截图核对两轮（第一版 `mt-7` 仓鼠盖住页头，改 `mt-[96px]` 后正常）；biome clean、`luka:typecheck` 0；`luka:test` 仍 3 文件 / 4 失败，均为工作区既有失败（同 14:40 那条）。
- **关键命令**：`xcrun simctl openurl booted "luka://settings/logout"` + `xcrun simctl io booted screenshot`；`pnpm luka:typecheck`、`pnpm exec biome check`。
- **沉淀**：[[design-mock-measure-and-sample]] 追加「探头角色绝对定位要让位」。
- **提交 / push（用户中途要求）**：`d664347aa feat(luka): 退出确认页按新稿重画（奶油底 + 白卡 + 探头仓鼠）`，已推 `feature/luka-ios`（pre-push：boundaries OK + luka typecheck 6.4s 过）。刻意未带上工作区里未提交的 `quick-pick-intro.tsx`、`tab-bar.tsx`。

## 工作日志 — 14:59 Luka 底栏：Chats 页不透明（毛玻璃例外）+ 修 activeRouteName 取错

- **做了什么**：用户「chat 页的时候就 不要毛玻璃了」。
  1. `tab-bar.tsx` 新增 `GLASS_FILL_OPAQUE`（全不透明的 background→cream，保留上浅下深），Chats tab 上 `canBlur = false`、不挂 6 层 `BlurView`；其余 tab 照旧毛玻璃。判断条件 `activeRouteName === "index"`。
  2. 首版条件恒 false：`useSegments()` 在 index 路由只返回 `["(tabs)"]`（index 不占段），`segments[1]` 是 `undefined`。改成 `onTabsRoot && segments[1] ? segments[1] : state.routes[state.index]?.name`；用临时栏上渲染 `{name} | {segments}` 截图读出证据后删除。
- **为什么**：房间是整屏场景（并行会话 14:49 刚把底图铺满整屏），底栏透出地板/地毯显脏；用户只要求 chat 页例外。
- **结果**：Chats 页栏像素 `#FDF9EE → #F6F1E7`、无房间内容透出；Discover 页仍是玻璃（顶 `254,252,246` 白雾 → 底 cream）；五个 tab 聚焦高亮正常；biome / `luka:typecheck` 过、设计测试无新增失败（仍 3 文件 4 条既有失败）。
- **关键命令**：`xcrun simctl io booted screenshot` + `magick -format "%[pixel:p{x,y}]"`；深链 `luka:///` 与 `luka://discover?tab=nearby` 对照两分支。
- **沉淀**：新建 [[expo-router-use-segments-index]]（`useSegments()` 的 index 缺段坑）。
- **提交 / push（用户要求）**：只 stage `tab-bar.tsx` 一个文件（并行会话的 chats-play / quick-pick / screen-header 在途改动未带上），`33a48d4c3 feat(luka): 底栏改毛玻璃渐变，Chats 页保持不透明` 已推 `feature/luka-ios`（pre-push：boundaries OK + luka typecheck 6.7s）。

## 工作日志 — 15:03 Luka 法务链接换成正式 carrd 页面

- **做了什么**：用户给两个正式地址，要求「luka 所有涉及这些的地方都改」。`apps/luka/src/lib/legal.ts` 的 `LEGAL.privacy` → `https://luka-privacypolicy.carrd.co`、`LEGAL.terms` → `https://luka-termsofservices.carrd.co`；文件头注释从「下面全部是占位值」改为「派生时仍须逐条替换（carrd 按包各托管一份）」。App 内 settings / paywall / credits / welcome / ai-insights 等入口全部经 `LEGAL` 取值，无需逐屏改。
- **为什么**：提审阻断项 1.5 / 5.1.1(i) / 3.1.2(c)，原先指向 `sites.google.com/view/luka-*` 占位页。
- **结果**：全仓 `sites.google.com/view/luka` 已清零；同 PR 更新 `apps/luka/docs/app-store-review-fixes.md`（法务页已闭环，客服邮箱仍占位）。biome clean、`pnpm luka:typecheck` 过。
- **待办**：App Store Connect 提交元数据里的法务链接要同步改成这两个 carrd 地址（逐字符一致）；客服邮箱 `support@luka.example.com` 仍是占位。
- **提交 / push（用户要求）**：只 stage `legal.ts` + `app-store-review-fixes.md`（并行会话在途改动未带），`ef187cfb5 fix(luka): 法务与隐私链接换成正式 carrd 页面` 已推 `feature/luka-ios`（pre-push：boundaries OK + luka typecheck 6.8s）。

## 工作日志 — 15:05 Luka Chats 小窝：待机 stay-all、新消息播一次收消息短片、两段小崽大小对齐

- **做了什么**：用户要求「没有新消息时播 `stay-all.mov`；有新消息进来播当前收消息短片，一条播一次、不循环；播的过程中来新消息不重播不打断；只有 stay 状态才起播」，随后追加「保证两个视频里的仓鼠大小一致」。
  1. 新状态机 `lib/chats-play.ts` 的 `NestMessagePlayback` / `stepNestMessagePlayback` / `finishNestMessagePlayback`：待机（`playing=false`）才起播，播放中的信号只记账不重播不打断不排队，页面不可见（切走/后台）只记账不补播，同 token 不重复触发。
  2. `components/chats-play/nest-hamster.tsx` 重写：收消息层（320×436，`loop=false`，不播时透明）+ 待机层（iOS `PET_MY_FEED_SOURCE`=stay-all 循环；Android 退 `PET_IDLE_FALLBACK` 静态图），两层常驻挂载、`withTiming` 160ms 交叉淡入；`playToEnd` 回待机；减少动态效果不播视频（待机停首帧，新消息 seek 到 3.5s 抱信封定格静置 2.6s）；聚焦/前后台沿用 `PetAnimation` 策略。
  3. 信号：`NestPlayfield` 复用已有未读快照比对产出的 `unreadPulseToken` 传给 `NestHamster`（一波只播一次）。
  4. 大小对齐：用 `measure-pet-alpha` 量出逐帧 alpha 包围盒中位 √面积 `stay-all` 284 vs 收消息片 315–358（起跳 358 / 定格 350），待机层放大 `IDLE_SCALE=1.25`、底边对齐消息框底边（脚同一条地毯线）；`frameWidth()` 保证两组比例同源。
  5. 单测 2 条（待机起播/播放中忽略/播完可重播/同 token 不重复/不可见不补播）。
- **为什么**：原来两段素材只有收消息短片在无限循环，页面平时也在播「收信冲刺」，没有待机与「来消息」的区分。
- **结果**：`luka:typecheck` 0、biome clean、`chats-play.test.ts` 15 passed；全量 `luka:test` 3 文件 / 4 失败与改动前基线一致（并行会话的 shape-language / candidate-card）。模拟器（iPhone 17，Metro 热更）实拍验证：待机站立、临时注入 6s 间隔信号「播一次→回 stay」、1s 间隔信号不打断不重播（4 张连续截图走完整段动作）；大小对比截图接近（不同姿势用 √面积对齐）。临时 `setInterval` 补丁已还原（已 grep 确认）。
- **关键命令 / diff**：`/tmp/measure-frames.swift`（临时量指定帧 alpha bbox）、`xcrun simctl io booted screenshot`、`git diff` 逐文件；改动 6 文件：`nest-hamster.tsx`（重写）、`nest-playfield.tsx`（1 行）、`lib/chats-play.ts`、`tests/lib/chats-play.test.ts`、`docs/chats-play.md`、`assets/chats-play/README.md`。**未提交**（规范模式）。
- **坑 / 备注**：`ffmpeg` 看不到 HEVC alpha，量大小必须走 AVFoundation（`scripts/measure-pet-alpha.swift` 的思路）；热更时并行会话未提交的 `tab-bar.tsx` 曾崩一次红屏（`debugName`，与本改动无关），`simctl terminate + launch` 后恢复；待机层放大后 Android 静态兜底同框也变大，但 `hamster-standing.png` 自带大透明边，仍比 iOS 视频小。

- **提交 / push（用户要求）**：`f0ca27f04 feat(luka): Chats 小窝小崽待机站定，新消息播一次收信短片`（6 文件，+280 −40），已推 `feature/luka-ios`（pre-push：boundaries OK + luka typecheck 6.3s）。并行会话在途改动（`(tabs)/index.tsx`、`nest-wall-frame.tsx`、`quick-pick-intro.tsx`、`screen-header.tsx`）未带上；同文件的并行 hunk（`nest-playfield` 的 topInset/bottomInset、两份文档的整屏舞台段）用 `git apply --cached` 只暂存本会话的行；`nest-hamster.tsx` 因整体重写，其中并行会话的 `top-[37%]`（脚落地毯中心）随文件一并入库（本改动验证所依赖的落点）。

## 工作日志 — 15:16 清空工作区：并行会话的 Chats 整屏舞台 + Quick Pick 两笔提交并 push

- **做了什么**：用户要求把剩余未提交改动也提交 push。按功能拆两笔：`ee1f52ab4 feat(luka): Chats 房间铺满整屏，页头与连接横幅改悬浮层`（6 文件：`(tabs)/index.tsx` 重构、`nest-playfield` insets、`nest-wall-frame` 下移 14%、`screen-header` 导出 `SCREEN_HEADER_HEIGHT`、两份文档）；`fe94f1b10 fix(luka): Quick Pick 小崽插画收到 200，一屏放下 Start picking`（1 文件）。已推 `feature/luka-ios`（`f0ca27f04..fe94f1b10`，pre-push boundaries + luka typecheck 6.2s 过），`git status` 干净、无未跟踪文件。
- **为什么**：这批原是并行会话在途改动，用户裁决由本会话一并入库。
- **结果**：会话开始时工作区 7 个未提交文件清零；提交前确认无 TEMP / console.log 残留，biome 5 文件 clean。

## 工作日志 — 15:52 luka 出 preview IPA（测试后端）并上传蒲公英

- **做了什么**：用户要「线上的代码、测试接口」的 preview 包并发蒲公英。跑 `scripts/build-ipa.sh luka test preview`（Release 构建 + `api-dev.lukasoc.com` + Ad Hoc；Node 24，`IOS_TEAM_ID=39CFYH6W55`，buildNumber 10000），产物 `apps/luka/ios/build/export/Luka.ipa`（88.8 MB，`com.lukasoc.luka.devx`，1.0.0 (10000)）；用 `scripts/ci/pgyer-upload.mjs --install-type 1` 上传 → **https://www.pgyer.com/luka-ios**（公开、无密码，curl 200 已验）。
- **过程（两次失败）**：
  1. 首次 archive 挂：`Unable to log in with account richardzhang1999@163.com (error -1200)`，自动签名退化到不带 App Groups/Push/SIWA 的通配 profile。根因是 Apple 开发者域名瞬时不可达（Clash 规则 `apple.com` 走 DIRECT）；复测恢复后重试即通 → 沉淀 [[xcode-apple-login-error-1200-network]]。
  2. 二次挂：`com.lukasoc.luka.dev` 被 Personal Team 占用，公司 Team 注册不了（[[ios-appid-personal-team-conflict]] 老坑）。按 09-16 文档把**生成物** pbxproj 的三个 Bundle ID 改成 `.devx`（替换 6 处，备份 `/tmp/luka-project.pbxproj.bak.*`，不入库），复用工程重跑 → **ARCHIVE + EXPORT SUCCEEDED**；Xcode 自动新建 `iOS Team Ad Hoc Provisioning Profile: com.lukasoc.luka.devx`（2027-08-30 到期）。
- **结果**：蒲公英链接已出；Ad Hoc 描述文件里只有 **1 台注册设备**，其他人要装需先把 UDID 注册进公司 Team 再重出包。
- **关键命令**：`printf 'n\n' | IOS_TEAM_ID=39CFYH6W55 scripts/build-ipa.sh luka test preview "" 10000`；`PGYER_API_KEY=*** node scripts/ci/pgyer-upload.mjs --file apps/luka/ios/build/export/Luka.ipa --type ios --install-type 1`。
- **备注**：`apps/luka/ios/` 现为本地改过的 `.devx` + team 39CFYH6W55；下次 `expo prebuild --clean` 还原成 `.dev`，公司 Team 签名会再撞同一冲突（根治仍未做：等 09-21 个人 profile 过期后试回收 App ID）。TIMPush 证书 luka 各 profile 均未配置，本次包收不到离线推送（脚本已警告）。

## 工作日志 — 16:14 luka 素材瘦身第一批：图片转 WebP + 图标字体按需 + 清未用字体

- **做了什么**（规范模式，未提交；工作区另有并行会话在途改动）：
  1. **图片转 WebP**：luka 随包 26 张 PNG/JPG（mascot / quick-pick / pet `room-bg` / widget `wake-plate` / chats-play 房间图）用 `cwebp -q 85 -alpha_q 90 -metadata none` 转 WebP，57 处 import 与注释同步、删除源文件；`icon.png` 的运行时用途（chats 列表 / chat 官方头像）换成 512 的 `icon-avatar.webp`，`icon.png` 只留给 `app.config.ts` 的桌面图标。RN 原生 `Image` 渲染的 4 处（widget jpg ×3、coin png）保持原格式。
  2. **图标字体按需 import**：64 个文件的 barrel `@expo/vector-icons` 全部改子路径（Ionicons 65 / Feather 2 / MCI 1 / Entypo 1）。
  3. **清未用字体**：`_layout` 移除 Archivo ×2、Fraunces（全仓 0 引用）与 Caveat（0 使用面）的加载；字体总数从 31 个 5.2 MB → 14 个 3.2 MB（残余 3 个 FA6 是 `chat-bubble.tsx` 直用的，属既有选择）。
- **验证**：`luka:typecheck` 0；`luka:test` 3 文件 / 4 失败与基线完全一致（802 passed）；`npx expo export` 资产桶 44.6 → 29 MB、WebP 27 个共 2.0 MB、剩余 PNG/JPG 0.44 MB；iOS 模拟器（dev 包 + Metro 热更）逐屏截图 pet / quick-pick / chats / settings/logout 全部正常（WebP 走 expo-image 的 WebPCoder）。
- **预估**：IPA 88.8 MB → ~74 MB（图片约 -14 MB、字体约 -0.9 MB）；要不要重出包 / 重传蒲公英待用户发话。
- **文档**：`src/assets/README.md` 新增第 4 条规矩（WebP 规范 + 例外清单 + cwebp 命令）；`screen-header.tsx` 里过时的 Archivo 注释改 Nunito。
- **沉淀**：新增 [[rn-webp-rendering-paths]]（渲染路径对照 / 分类排查法 / 质量验收）。
- **注意**：工作区混有并行会话 16:07 的 `chats-play.ts` / 测试改动（删 `NEST_CALL_RESERVE`），本批未触碰；`apps/luka/.heyhru-ios-team`（构建脚本产物）未被 gitignore，未处理。

## 工作日志 — 16:14 Luka Chats：video match 卡搬到页头下并缩小；底部卡片带不再为它让位

- **现象**（用户发 iPhone X 实机截图）：有 videoMatch 时，第 8 张访客卡（左下 Crystal / 右下 Nicole 下面那两张）被 See all / 翻页键压住，露出的底边又被底部 call 卡盖住。根因：`NestPlayfield` 的 `controlsBottom = bottomInset + NEST_CALL_RESERVE(118)` 只抬高控件，卡片带仍按「页头 → 标签栏」自适应用 23%–86% 排（`contentHeight` / `nestVisitorRowLayout`），两者互不知情。手算 iPhone X：控件顶 y≈541、卡片底 y≈628（叠 ~87pt），高屏也叠 ~60pt。
- **拍板**（question 两轮）：① 先定卡片整体上移 + call 卡变小；② 用户改主意——「放到最上方 页头下，并且视频卡要更小更窄」；③ 追加「出现动画幅度小一点，轻微抖动就行」。
- **改动**（5 文件，规范模式未提交）：
  - `app/(tabs)/index.tsx`：call 卡从底部 `absolute left-3 right-3 + bottom: tabBarClearance+8` 搬进悬浮 `SafeAreaView`（`ScreenHeader` → `ConnectionBanner` 之后），`mx-6 mt-2`；进入/退出 `SlideInDown/FadeOutDown` → `FadeInDown.springify().damping(16).stiffness(180)` / `FadeOutUp`（FadeInDown 自带 ~25pt 位移，弹簧轻微过冲）。
  - `components/chat/video-match-card.tsx`：min-h 96→72、头像 56→48、pad `px-3.5 py-3.5`→`px-3 py-2.5`、名字 16→15、副文案两行→一行 12px、call 按钮 h-12→h-10 字号 14。
  - `components/chats-play/nest-playfield.tsx`：`controlsBottom` 恒定 `bottomInset + CONTROLS_GAP(18)`，不再随 `videoMatchReady` 变（`videoMatchReady` 只剩门 glowing/onPress 用）；删 `NEST_CALL_RESERVE`。
  - `lib/chats-play.ts` + `tests/lib/chats-play.test.ts`：删常量与断言。
  - `docs/chats-play.md`：结构图与说明同步。
- **验证**：biome 5 文件 0；`pnpm --filter luka typecheck` 0；`tests/lib/chats-play.test.ts` 14 passed。第一版圆角写了 `rounded-[10px]/[13px]` 被 shape-language 拦下，已改成 12/14/20 允许档。
- **坑**：`biome check --write` 会顺手把 `import { Ionicons } from "@expo/vector-icons"` 改成深层入口（仓库 65 个文件已是深层写法），本次已还原 barrel，属仓库在途迁移不顺手带。全量 `luka:test` 另有 4 失败均为并行会话在途/环境（good-review 的 expo `ImportMetaRegistry`、candidate-card-fields 的 `nearby-list-row.tsx` 被改名、shape-language 的 discover-play `rounded-[8px]` 与 bubble-* 内联圆角）。
- **待确认**：call 卡在页头下会盖住房间上部墙框（Team）与它的可点区（视频卡消失即恢复）；动画观感待实机。

## 工作日志 — 17:04 Luka Quick Pick：结算页列表区滚动、按钮置底

- **现象**（用户 iPhone X 尺寸模拟器截图）：10 枚 pick 时列表超出屏幕，整页不能滚，后面几行与底部按钮都看不到。
- **拍板**（用户原话）：「列表区域滚动 按钮置底」。
- **改动**：`components/quick-pick/quick-pick-settle-panel.tsx` —— 外层 `View flex-1 px-5` 内改为「标题固定 → `ScrollView flex-1`（列表/空态 + Say hi to all）→ 底部按钮组固定」；删 `flex-1` 占位；底部按钮组加 `paddingBottom: 24 + insets.bottom`（原按钮在屏幕外，没暴露 home indicator 问题）。
- **验证**：biome 0 / `luka:typecheck` 0 / `quick-pick.test.ts` + `quick-pick-analytics.test.ts` 18 passed。
- **状态**：规范模式，未提交。

## 工作日志 — 17:07 Xcode 26 模拟器构建修复：SwiftUICore stub（rn-trtc 0.13.1）

- **现象**：模拟器构建挂 `cannot link directly with 'SwiftUICore'`（新建 iPhone X 等价设备后暴露）；真机构建此前正常。
- **根因与弯路**（详版沉淀 [[xcode26-swiftuicore-sim-link]]）：Swift 5 模式 `import SwiftUI` strong autolink SwiftUICore；旧 `-weak_framework`（Xcode 16 方案）在 Xcode 26 失效且对 NSE/HomeWidget 反向引爆（加载受限 tbd）；lazy 无效；全局 disable-autolink 修不了预编译产物还连锁缺符号。
- **解法**：`rn-trtc/app.plugin.js` 的 post_install 改为「拷真实 `SwiftUICore.tbd`、删 `allowable-clients` 段 → 写 `ios/SwiftUICoreStub/`（生成物）→ `FRAMEWORK_SEARCH_PATHS[sdk=iphonesimulator*]`（App + 扩展）→ 清遗留 weak flag」。本地在 luka 上等价手工验证：`xcodebuild -workspace Luka.xcworkspace -scheme Luka ... build` → **BUILD SUCCEEDED**（App + HomeWidget + NSE）。
- **改动文件**：`packages/rn-trtc/app.plugin.js`（重写该段）、`CHANGELOG.md`（0.13.1）、`package.json`（0.13.0→0.13.1）、`README.md`、`apps/iris/docs/05-native-capabilities.md` §1.1.a、`docs/react-native-guide.md` 插件表。
- **验证**：biome 0 / `pnpm boundaries` OK（41 包面）/ luka typecheck 0。
- **教训**：清理实验文件时 `rm -rf packages/rn-trtc/ios` 差点删掉包自己的原生源码（`CallRechargePresenter.podspec` / `.swift`），立刻 `git restore` 恢复 —— **删目录前先确认它不是包的原生源码目录**。
- **状态**：规范模式，未提交；luka 的生成工程（Podfile/pbxproj）已是手改版，新机器或 `prebuild --clean` 后由 plugin 自动注入。

## 工作日志 — 17:23 Luka iOS 模拟器：SwiftUICore 链接失败定位 + 安装后原生崩溃排查（未闭环）

- **背景**：用户要求「启动 iPhone X 模拟器 + Metro」。模拟器（`iPhone X (11 Pro eq)`，iOS 26.5 runtime）已 boot，Metro 复用 8081 上的旧 luka 进程（PID 43394）；把 app 装进模拟器后用户手跑 `pnpm luka:ios --device 'iPhone X (11 Pro eq)'` 报 `xcodebuild exited with error code 65`。
- **构建失败定位**：报错是 `Linking Luka/HomeWidget` 时缺 `associated type descriptor for SwiftUI.View.Body`（SwiftUICore 私有符号）。根因 = 生成工程当时用的是「空导出 stub tbd」遮蔽真实 SwiftUICore；**并行会话 17:00–17:07 已改用「拷真实 SDK tbd、只删 `allowable-clients` 段」的方案并写进 `packages/rn-trtc/app.plugin.js`（未提交，mtime 17:07）**，17:02 构建已通过（`HomeWidget.appex` 产出正常）。这条不是我的修复。
- **新阻塞（运行时）**：app 安装后 dev launcher 正常，一旦加载 RN app 就原生崩溃，签名统一为 `ExpoFabricViewObjC registerNativeViews` → `RawPropsParser::prepare<ExpoViewProps>` → `React Props::Props()` EXC_BAD_ACCESS/SIGSEGV。crash 报告 `~/Library/Logs/DiagnosticReports/Luka-2026-09-18-17*.ips`（17:05/17:08/17:18 多次，含清空 DerivedData 全量重建后 17:18:55 的一次）。
- **已排除**：① JS/Metro（崩在原生组件注册，早于 JS 执行）；② 构建产物陈旧（清了两个 Luka DerivedData 目录全量重建，仍复现）；③ SwiftUICore stub 只影响链接期，不影响运行时；④ 预编译模块无 `Testing.framework` 之类脏链接（otool 扫过）。
- **线索**：仓库有既存记录「预编译 Expo 模块 / RN core 引发的运行时崩溃」，确定性规避是 `EXPO_USE_PRECOMPILED_MODULES=0`（所有 `ios:device` 脚本都用，见 `apps/iris/docs/05-native-capabilities.md` §1.1.f、`30-Resources/troubleshooting/rn-prebuilt-core-dyld.md`）；模拟器构建目前走预编译（`React-Core-prebuilt` + `ExpoModulesCore.xcframework`）。**待用户拍板**：是否 `EXPO_USE_PRECOMPILED_MODULES=0 pod install` + 清 DerivedData 重建（会改共享 `ios/Pods` 状态，且需与并行会话协调）。
- **关键命令**：`xcodebuild -workspace Luka.xcworkspace -scheme Luka -configuration Debug -destination 'platform=iOS Simulator,id=7DE771A0-…' build`；`xcrun simctl install/launch`；`xcrun simctl spawn … log show --predicate 'process == "Luka"'`。
- **备注**：只改了模拟器内 app 的 UserDefaults（把 `http://localhost:8081` 写进 dev launcher 的 recent apps + `EXDevLauncherTryToLaunchLastBundle=true`，用于自动连 Metro），未动仓库任何文件。

## 工作日志 — 17:36 Luka 模拟器运行时崩溃根治：源码编译 Expo 模块（预编译模块组合崩溃）

- **承接 17:23 条目**（构建已通、安装后原生崩溃）。用户拍板后按仓库既有确定性路线修复：
  1. `cd apps/luka/ios && USE_CCACHE=1 EXPO_USE_PRECOMPILED_MODULES=0 LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 pod install` —— Podfile 里 `enabled?` 要求 `EXPO_USE_PRECOMPILED_MODULES=1` 且 `RCT_USE_PREBUILT_RNCORE=1`，置 0 后 Expo 模块全部源码编译（RN core 仍为 prebuilt，符合 `ios:device` 的既有配方）。
  2. 清 `~/Library/Developer/Xcode/DerivedData/Luka-*`，`USE_CCACHE=1 … xcodebuild -workspace Luka.xcworkspace -scheme Luka -configuration Debug -destination 'platform=iOS Simulator,id=7DE771A0-…' build` → **BUILD SUCCEEDED**（ccache 命中，约 4 分钟）。
  3. `simctl install/launch` → **不再崩溃**；Metro 8081 连上，JS 日志出现 `luka_login_page_show`，登录页（Sign in with Apple）正常显示。
- **验证点**：`Pods/Local Podspecs/ExpoModulesCore.podspec.json` 从 `vendored_frameworks` 变回 `source_files: ios/**/*…`；`Pods-Luka-frameworks.sh` 只剩 `ExpoModulesJSI`（本就 xcframework）embed。
- **结论**：模拟器崩溃根因 = **预编译 Expo 模块（ExpoModulesCore 等 xcframework）+ prebuilt React core** 组合在该机器上注册 Expo view 时 `Props::Props()` 段错误；`EXPO_USE_PRECOMPILED_MODULES=0` 与 `apps/iris/docs/05-native-capabilities.md` §1.1.f / [[rn-prebuilt-core-dyld]] 同源的确定性规避。当前修复只落在本地生成目录 `ios/Pods`（gitignore）：**将来不带该 env 重跑 `pod install` / `prebuild --clean` 会退回预编译并可能复崩**；是否固化到 luka 的 Podfile.properties.json / 脚本待用户决定。
- **备注**：并行会话已把 SwiftUICore 方案定稿进 `packages/rn-trtc/app.plugin.js`（拷真实 SDK tbd、删 `allowable-clients`，未提交）。

## 工作日志 — 17:44 Luka Chats：卡片带上移 24pt；videoMatch mock 看完已撤

- 用户（模拟器实拍）「头像区域可以再往上一点」：`lib/chats-play.ts` 的 `NEST_VISITOR_TOP_RATIO` 0.23→0.19、`NEST_VISITOR_BOTTOM_RATIO` 0.86→0.82（等量 -0.04，band 高度不变 → 卡片大小与行距不动，整带在 iPhone X 尺寸上上移 ≈24pt）；`chats-play.test.ts` 里两个字面量同步。
- 用户要求 mock videoMatch 看新位置/尺寸：`app/(tabs)/index.tsx` 临时 `MOCK_VIDEO_MATCH_ON`（state 初值 + `loadVideoMatch` 早退，id 用 "0" 防真实拨号），**看完后应要求已完全撤除**（grep 无残留）。
- 验证：biome 0 / `luka:typecheck` 0 / `chats-play.test.ts` 14 passed。
- 状态：规范模式，未提交。
