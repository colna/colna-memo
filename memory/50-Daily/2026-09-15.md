---
title: 2026-09-15
date: 2026-09-15
tags: daily
---

# 2026-09-15

## 今日重点

> 从 09-14 的 Daily 带过来。

- [ ] **公司 Team 生效后**：把 `apps/luka/app.config.ts` 的 `appleTeamId` 从 `PD2XGB735Y`（Personal Team，已提交 `56a821ba`）换回真实 ID —— 推送 / 小组件 / Apple 登录随之恢复
- [ ] luka **真机业务验证**：真机链路 09-14 已打通（`pnpm luka:start` + 手机开 App），但业务主流程（登录 / 划卡 / 聊天）还没在真机上回归
- [ ] edit profile 剩余屏：`photos`（mockup `82`）
- [ ] Nearby **列表模式没有仓鼠**（w3 稿上有）—— 要加说一声
- [ ] 远端 `feat/luka-edit-profile` 分支未删，要不要清
- [ ] `heard.mov`（1.5 MB）自 Daily Bonus 弹层换素材后没人引用，去留待定
- [ ] 未决设计点：photos 单图换头像 vs 多图相册；interests 平铺 vs 分组；AI tip 留不留

## 工作日志

## 工作日志 — 10:20 前一天（09-14）整理

- **沉淀**：1 条新增 —— [rn-scroll-perf-and-blur-header](../30-Resources/troubleshooting/rn-scroll-perf-and-blur-header.md)（`onScroll` 每帧 setState 的卡顿根因与修法、BlurView 页头三个坑、滑窗口的闩别放盯数据的 effect、页头浮起时子页让位的「别让两次」）。真机调试与小组件白屏两条 09-14 当天已各自沉淀，复查无遗漏。
- **带进今日**：7 条待办（appleTeamId 还原 / 真机业务验证 / photos 屏 / Nearby 列表仓鼠 / 远端分支清理 / heard.mov 去留 / 三个设计未决点），已写进上方「今日重点」。
- **项目进展**：记进 [luka-rn-app](../10-Projects/luka-rn-app.md)（已完成表补 09-14 的五项：真机调试打通、小组件白屏修复、页头毛玻璃 + 瀑布流卡顿修复、Nearby / Feed 瀑布流、Edit profile 六子页）。
- **归档**：无 —— 09-14 的工作日志都是实质内容，原文保留。

## 工作日志 — 10:18 push 所有未提交代码（10 个提交进 `feature/luka-ios`）

用户「push 所有未提交代码」。本地当时**领先远程 9 个提交**（含 09-14 各会话的工作与真机调试的 `appleTeamId`），工作区另有 50 个文件的未提交改动。

- **提交前验证**：`pnpm lint`（5072 文件，0 error / 19 条既存 warning）、`pnpm typecheck`（全仓通过）、`pnpm --filter luka test`（117 文件 / 657 通过）。
- **新提交**：`d34b92bf feat(luka): Discover / Nearby 改瀑布流，视觉统一到蜜色新稿`（50 文件 / +509 −936 量级）—— 内容为 09-14 其他会话的 21:12 / 21:26 两项工作 + 配套视觉统一：
  - Discover / Nearby 从 `FlatList` 换成 `MasonryList`（新增 `masonry-list` / `feed-masonry-card` / `nearby-masonry-card` / `nearby-masonry` / `nearby-masonry.ts`），页头浮起 + `HeaderBlurFade`；
  - sparks、credits、onboarding、edit-profile 等从蓝图蓝统一到蜜色系（`#FDD496` / `#684431` / `#F5EADA`），edit-profile 空值行的星标单独上红；
  - Debug Tools 加 PAYMENTS 分组（credits 三屏 + 三个 paywall 真路由入口）；pet 文案与 deck 气泡素材重出。
- **推送**：`git push` 一次带上 10 个提交（`56a821ba..d34b92bf`），pre-push 的 boundaries + typecheck 实跑通过。**注意**：`56a821ba chore(luka): 真机调试临时用 Personal Team ID` 也随之进了远程 `feature/luka-ios`，还原待办见「今日重点」第一条。
- **Love Ring 小崽调整（已含在 `1ebfc405` 里）**：09-14 傍晚把恋爱铃横幅右上角那只 `point-down.mov` 小崽从 120 收到 88（与发帖 FAB 同档）、下压量 10 → 35（大部分在抵消素材透明边）、方框中心对准绿色接听键（`PET_RIGHT = 2`）、改为上层渲染让爪子可见地搭在接听键上沿（`pointerEvents="none"` 保点击）。这几处调整被另一会话的提交打包，未单独成笔。

## 工作日志 — 10:25 Luka：Plus Pass 小崽挪到 CTA 上 + CTA 置底

用户贴 paywall 截图：「左下坐着的仓鼠应该坐在按钮上」→「坐在中间 而不是靠左」→「按钮可以往下一点 置底」。三条连着来的，同一处。

- **小崽挪位**（`apps/luka/src/app/paywall/index.tsx`）：`hamster-sit` 之前挂在「CHOOSE YOUR PASS」那个容器上（`right: 2, top: -104`），实际飘在权益格 06 那一格上、离 CTA 很远 —— 稿子（`20260914-pay-pass.png`）里它是坐在按钮上的。现在 `bottom: CTA_HEIGHT - PET_SIT_DEPTH`（50 − 8 = 42，屁股压在按钮上沿 8pt）、水平居中（`left: "50%"` + `marginLeft: -48`）；新增 `CTA_HEIGHT = 50` / `PET_SIT_DEPTH = 8` 两个常量。`DtcPayArea`（三方支付）与直连内购两个分支各包一层 `relative` 容器 —— 小崽必须相对**按钮**定位，相对外层的话三方支付那支底下还多一行内购按钮，位置会漂。
- **试过靠左**（`left: 14`）：小崽的头会探到「CHOOSE YOUR PASS」那一行、压住左对齐的标题；截图确认后改居中（用户随后也确认要居中）。
- **CTA 置底**：`ScrollView` 的 `contentContainerClassName` 加 `grow`，CTA 那组加 `mt-auto` —— 内容短时「选套餐 + 按钮 + 页脚」整块沉到屏底（稿子如此），内容超一屏时照常从头排、跟着滚，不会叠在内容上。
- **验证**：模拟器截图 `/tmp/v-paywall3.png`（小崽居中踩在按钮上沿）、`/tmp/v-paywall4.png`（CTA 置底）；typecheck 0 / test 657 / lint 0 error（19 条既存 warning）。
- **未提交**：这两笔晚于 10:18 那次「push 所有未提交代码」（`d34b92bf`），还在工作区。
