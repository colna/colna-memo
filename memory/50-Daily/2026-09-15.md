---
title: 2026-09-15
date: 2026-09-15
tags: daily
---

# 2026-09-15

## 今日重点

> 从 09-14 的 Daily 带过来。

- [x] **公司 Team 生效后**：`appleTeamId` 换回 `39CFYH6W55`（AI FANTASY）+ `usesAppleSignIn` 恢复 + entitlements 复原（11:15 完成，改动待提交）
- [ ] luka **真机业务验证**：真机链路 09-14 已打通（`pnpm luka:start` + 手机开 App），但业务主流程（登录 / 划卡 / 聊天）还没在真机上回归
- [ ] edit profile 剩余屏：`photos`（mockup `82`）
- [ ] Nearby **列表模式没有仓鼠**（w3 稿上有）—— 要加说一声
- [ ] 远端 `feat/luka-edit-profile` 分支未删，要不要清
- [ ] `heard.mov`（1.5 MB）自 Daily Bonus 弹层换素材后没人引用，去留待定
- [ ] 未决设计点：photos 单图换头像 vs 多图相册；interests 平铺 vs 分组；AI tip 留不留

## 工作日志

## 工作日志 — 10:20 前一天（09-14）整理

- **沉淀**：1 条新增 —— [rn-scroll-perf-and-blur-header](../30-Resources/troubleshooting/rn-scroll-perf-and-blur-header.md)（`onScroll` 每帧 setState 的卡顿根因与修法、BlurView 页头三个坑、滑窗口的闩别放盯数据的 effect、页头浮起时子页让位的「别让两次」）。真机调试与小组件白屏两条 09-14 当天已各自沉淀，复查无遗漏。
- **带进今日**：7 条待办（appleTeamId 还原 / 真机业务验证 / photos 屏 / Nearby 列表仓鼠 / 远端分支清理 / heard.mov 去留 / 三个设计未决点），已写进上方「今日重点」。
- **项目进展**：记进 [luka-rn-app](../10-Projects/luka-rn-app.md)（已完成表补 09-14 的五项：真机调试打通、小组件白屏修复、页头毛玻璃 + 瀑布流卡顿修复、Nearby / Feed 瀑布流、Edit profile 六子页）。
- **归档**：无 —— 09-14 的工作日志都是实质内容，原文保留。

## 工作日志 — 10:18 push 所有未提交代码（10 个提交进 `feature/luka-ios`）

用户「push 所有未提交代码」。本地当时**领先远程 9 个提交**（含 09-14 各会话的工作与真机调试的 `appleTeamId`），工作区另有 50 个文件的未提交改动。

- **提交前验证**：`pnpm lint`（5072 文件，0 error / 19 条既存 warning）、`pnpm typecheck`（全仓通过）、`pnpm --filter luka test`（117 文件 / 657 通过）。
- **新提交**：`d34b92bf feat(luka): Discover / Nearby 改瀑布流，视觉统一到蜜色新稿`（50 文件 / +509 −936 量级）—— 内容为 09-14 其他会话的 21:12 / 21:26 两项工作 + 配套视觉统一：
  - Discover / Nearby 从 `FlatList` 换成 `MasonryList`（新增 `masonry-list` / `feed-masonry-card` / `nearby-masonry-card` / `nearby-masonry` / `nearby-masonry.ts`），页头浮起 + `HeaderBlurFade`；
  - sparks、credits、onboarding、edit-profile 等从蓝图蓝统一到蜜色系（`#FDD496` / `#684431` / `#F5EADA`），edit-profile 空值行的星标单独上红；
  - Debug Tools 加 PAYMENTS 分组（credits 三屏 + 三个 paywall 真路由入口）；pet 文案与 deck 气泡素材重出。
- **推送**：`git push` 一次带上 10 个提交（`56a821ba..d34b92bf`），pre-push 的 boundaries + typecheck 实跑通过。**注意**：`56a821ba chore(luka): 真机调试临时用 Personal Team ID` 也随之进了远程 `feature/luka-ios`，还原待办见「今日重点」第一条。
- **Love Ring 小崽调整（已含在 `1ebfc405` 里）**：09-14 傍晚把恋爱铃横幅右上角那只 `point-down.mov` 小崽从 120 收到 88（与发帖 FAB 同档）、下压量 10 → 35（大部分在抵消素材透明边）、方框中心对准绿色接听键（`PET_RIGHT = 2`）、改为上层渲染让爪子可见地搭在接听键上沿（`pointerEvents="none"` 保点击）。这几处调整被另一会话的提交打包，未单独成笔。

## 工作日志 — 11:15 公司 Team（AI FANTASY）生效：Team ID 换回 + entitlements 复原

用户加入公司 Team 后（Xcode 显示 AI FANTASY, INC / Admin），恢复 luka 的正常签名配置。

- **证书**：Xcode → AI FANTASY, INC → Manage Certificates 创建 Apple Development（`峥 张 (QG6KT5G52K)`，**OU = `39CFYH6W55`** —— 正是仓库已知的开发 Team，iris / lumi / koda 同款）。期间 Team 详情页的「Certificates, Identifiers, & Profiles」报红叉，同意 Apple 开发者协议后转绿。
- **配置**：`apps/luka/app.config.ts` —— `appleTeamId` 由 `PD2XGB735Y` 换回 `39CFYH6W55`；`usesAppleSignIn: true` 取消注释（`login.tsx:53` 的 Apple 登录是活跃功能，之前因免费号不支持而临时关闭）。
- **prebuild**：`npx expo prebuild -p ios --clean` → entitlements 全部复原（`Luka.entitlements`：`aps-environment` / `applesignin` / `application-groups`；`HomeWidget.entitlements`：`application-groups`）。
- **模拟器重建**：`npx expo run:ios --no-bundler`（不动后台 Metro）→ Build Succeeded，`com.lukasoc.luka.dev` 在公司 Team 下自动注册 App ID + 描述文件，App 重装并连上 Metro（登录态保留）。
- **真机验证入口**：`pnpm --filter luka ios:device`（公司 Team 签名后可验推送 / 小组件 / Apple 登录）。
- **未提交**：`app.config.ts` 的还原改动在工作区（规范模式，等用户发话）。

## 工作日志 — 11:50 拉取 blueprint 分支（merge 67 提交 + 共享包契约适配 + push）

用户要求把 `origin/feature/blueprint` 拉到 `feature/luka-ios`。本地缓存停在 09-12，先 fetch 最新 —— SSH 被 Clash TUN 挡（`198.18.0.30` 两次卡死），改**一次性 HTTPS URL** 成功（HTTPS 一直通），拉到 `ad0713f1`（今天 10:52）。

- **merge**：`origin/feature/blueprint`（67 提交 / 331 文件）—— packages/ 的修复（支付 / IAP / rn-net / tim-push / 埋点）、CI 脚本与门禁、blueprint 自身的 0010–0035 迁移记录。确认了 merge 范围不含 `apps/luka/` 与 `packages/rn-home-widget/`（另一会话的工作区改动不受影响）后才动手。两处冲突：
  - `docs/review-mode.md`：采用 blueprint 的「邮箱由后端下发」新设计（旧版各包邮箱表被取代，两处冲突同向处理）；
  - `package.json`：两边命令都保留（`luka:*` + `blueprint:archive-ios`）—— 解冲突时 `blueprint:pre-submit` 写重复了一次，pre-commit 的 biome 抓到（`noDuplicateObjectKeys`），修后过。
- **merge 后的适配**（`3f009a21`，8 文件）：
  - `rn-net` 新增必填身份字段（`packageName` / `appVersion` / `appBuild` / `appDisplayName`）→ luka 的 `client.ts` 与 `settings.ts` 两处调用补齐（取 `expo-application` 运行时真值，与 intro / blueprint 同款接线）；
  - mock 层补 `EmailLoginRequest` / `GetSubscriptionManageUrlRequest` 两个 handler（共享包新引用被 coverage 测试扫出）；
  - `analytics-contracts` / `dtc-checkout-routing` / `third-party-subscriptions` 三个测试跟上新契约字段（`pay_value` / `current_coins` / `provider`）—— 前两个直接同步 blueprint 版本，dtc 那个只补字段（因为 luka 的支付**实现**还没迁移）；
  - `settings.test.ts` 加 `expo-application` mock（Node 环境加载不了原生模块；blueprint 的逐测试 mock 模式）。
- **验证**：`pnpm install`（merge 带来 `expo-web-browser` 等）；全仓 typecheck 通过；luka 测试 **118/119 文件**（剩下 1 个 `widget-sync.test.ts` 是**另一会话的进行中工作**、未提交，不归本轮）；pre-push 的 boundaries + typecheck 实跑通过。
- **push**：`f2aab69a..3f009a21`（HTTPS 一次性 URL）。
- **重要发现**：luka 的 `.template-rename.json` 的 `appliedMigration` 仍停在 **0009**，blueprint 已到 **0035** —— **26 条迁移记录（0010–0035）待逐条处理**。其中支付（0015 收银台锁 / 0027 支付失败原话透传 / 0030 支付回跳不是屏）与 review-mode 新设计（0024 / 0026）的**实现**尚未跟上；本轮只做了 merge 必需的契约适配。
- **沉淀**：[clash-tun-blocks-github-ssh](../30-Resources/troubleshooting/clash-tun-blocks-github-ssh.md)（curl 通、git 挂的判据；一次性 HTTPS URL 的 refspec 写法；别改 remote）。

## 工作日志 — 10:25 Luka：Plus Pass 小崽挪到 CTA 上 + CTA 置底

用户贴 paywall 截图：「左下坐着的仓鼠应该坐在按钮上」→「坐在中间 而不是靠左」→「按钮可以往下一点 置底」。三条连着来的，同一处。

- **小崽挪位**（`apps/luka/src/app/paywall/index.tsx`）：`hamster-sit` 之前挂在「CHOOSE YOUR PASS」那个容器上（`right: 2, top: -104`），实际飘在权益格 06 那一格上、离 CTA 很远 —— 稿子（`20260914-pay-pass.png`）里它是坐在按钮上的。现在 `bottom: CTA_HEIGHT - PET_SIT_DEPTH`（50 − 8 = 42，屁股压在按钮上沿 8pt）、水平居中（`left: "50%"` + `marginLeft: -48`）；新增 `CTA_HEIGHT = 50` / `PET_SIT_DEPTH = 8` 两个常量。`DtcPayArea`（三方支付）与直连内购两个分支各包一层 `relative` 容器 —— 小崽必须相对**按钮**定位，相对外层的话三方支付那支底下还多一行内购按钮，位置会漂。
- **试过靠左**（`left: 14`）：小崽的头会探到「CHOOSE YOUR PASS」那一行、压住左对齐的标题；截图确认后改居中（用户随后也确认要居中）。
- **CTA 置底**：`ScrollView` 的 `contentContainerClassName` 加 `grow`，CTA 那组加 `mt-auto` —— 内容短时「选套餐 + 按钮 + 页脚」整块沉到屏底（稿子如此），内容超一屏时照常从头排、跟着滚，不会叠在内容上。
- **验证**：模拟器截图 `/tmp/v-paywall3.png`（小崽居中踩在按钮上沿）、`/tmp/v-paywall4.png`（CTA 置底）；typecheck 0 / test 657 / lint 0 error（19 条既存 warning）。
- **未提交**：这两笔晚于 10:18 那次「push 所有未提交代码」（`d34b92bf`），还在工作区。
- **追加（10:35）**：用户红框指出「CHOOSE YOUR PASS」应在权益格正下方 —— 拆开那组：标题留在流里（格子下 24pt），`mt-auto` 只挂「套餐 + 按钮」那一组，空出来的高度留在标题与套餐之间。截图 `/tmp/v-paywall5.png`；typecheck 0 / test 657 / lint 0 error。
- **推送（10:40）**：用户说 push，提交 `f2aab69a feat(luka): Plus Pass 小崽改坐 CTA 上沿，套餐与按钮组置底`（1 文件 / +67 −31），已推 `feature/luka-ios`（`d34b92bfc..f2aab69aa`）；pre-commit 的 biome 与 pre-push 的 boundaries + typecheck 实跑通过。

## 工作日志 — 10:45 Luka 桌面组件：仓鼠状态系统方案 + 两张状态板出图

用户发仓鼠三视图 + 一张小组件参考截图（Emma / Say hi），要「参考多邻国逻辑」的组件 UI 方案：仓鼠按每天互动状态变心情，组件展示连胜 / 进度制造监督感。

- **调研**：读了 `luka-rn-app` 项目笔记 + `rn-home-widget` README（现组件只循环帧图、不读数据）+ `apps/luka/docs/widget-guide.md`（组件引导五步）。现有帧图是暗色夜景风，与三视图的暖奶油风完全不同 —— 本方案按暖色重做。
- **方案（五态）**：监督（白天 0 进度，探头盯人）/ 加油（进行中，开心）/ 惬意（完成，瘫坐眯眼）/ 焦虑（21:00+ 未完成，惊恐 + 汗滴 + 橙底）/ 庆祝（里程碑，未出图）。组件元素：火苗 + 连胜数 + 进度条 + 一句催促文案；Small 2×2 / Medium 4×2 两档。
- **出图**（`image2-gen`，`gpt-image-2`，1536×1024，4 张参考图 = 三视图 + pet-sit / pet-happy / pet-worried 姿势参考）：
  - `20260915-widget-states-board.png`（Medium 四态 2×2）
  - `20260915-widget-states-small.png`（Small 四态 2×2）
  - 两张的 prompt 也存到同目录 `.prompt.txt`；文字全部正确、姿势对得上参考。
- **坑**：`--out` 传相对文件名会落到 **cwd**（不是默认归档目录），已 mv 回 `Luka- 物料/opencode-image2/`；下次传绝对路径或目录。
- **网关 403**：`GROUP_NOT_ALLOWED`（地域拦截，首页 200 / API 403）—— `open -a "Clash Verge"` 后即通，已沉淀进 [muskapi-image2-relay](../30-Resources/troubleshooting/muskapi-image2-relay.md)。
- **顺手发现（未修）**：用户贴了 `GO_BACK was not handled` 警告 —— 全仓仍有 ~74 处裸 `router.back()`，其中 `paywall/index.tsx:225`（Plus Pass 的 ✕）、edit-profile 六子页、contact/*、post/chat 等深链可达屏在冷启动时按钮会失效。是否全扫接 `useGoBack` 待用户拍板。

## 工作日志 — 11:19 figma-write-bridge：开白四步（27–30）按代码重写分层复刻 spec

用户要求把 Meta 开白流程第 6–9 步（`identify` / `looking-for` / `drink` / `smoke`）按**代码**重建为
Figma 节点 spec，写 `screens/27-identify.mjs` / `28-looking-for.mjs` / `29-drink.mjs` /
`30-smoke.mjs`；不改 `lib/`、`plugin/`、`bridge.mjs` 与 sitin-rn。

- **产物**：四个模块 `node -e import(...).build()` 全过（各 5 个顶层子节点：bg / Status bar /
  Header / Identity options / CTA），另跑 walker 校验节点字段齐全，并把每个 `characters`
  逐条溯源回 tsx 源码（唯一例外见下）。
- **与既有 27–30 的关系**：仓库里已有 `27-review-identify.mjs` 等（09-14 16:36 按 image2 生成图
  画的）——选项文案是 Woman/Man/Non-binary、副标全按 1 行，与现在的代码不符（代码是
  "I'm a woman"…）。新四文件按代码重写；旧文件与 `group-7-review.mjs` 未动（用户未要求）。
- **排版基线**：返回行 56 / eyebrow 150 / 标题 170 / 副标 214，内容起点 = 副标底 + 30
  （`mt-[30px]`）。折行用 CoreText 量真字体（Nunito）定：identify 与 looking-for 的副标都是
  2 行 → 行起点 288 / 328；drink、smoke 1 行 → 266。**looking-for 的标题 30px ExtraBold 量出
  372.9pt > 342 可用宽，真机折两行** → 标题 82 高、副标 254、五行选项 328..712（离贴底 CTA
  只剩 16pt，仍在画布内）。
- **选项行**：按 `identity-option-row.tsx` 真实样式 —— 选中行 2pt `caramelLine` 描边 + 28pt 焦糖
  实心圆 + check、文字 `caramelDeep`；未选 1.5pt `hairline` + 26pt 空心圆 `radioRing`。第一项
  选中（CTA 因此为可用态）。进度条 `stepProgress(step, total=9)` → `06 / 09`…`09 / 09`；
  smoke 的 CTA 是 `Meet your matches`（代码 `ctaLabel`）。
- **验证**：四屏 build + 结构 dump（9 段进度 + 计数器文案、选中/未选行的描边与圆、radio
  x=294/296）；字符溯源仅 `What are you looking\nfor?` 的换行符不在源码里（按量测折的）。
- **沉淀**：`design-mock-measure-and-sample` 追加「没有截图时用 CoreText 量折行」一节（含校准
  方法与三组实测宽度）。

## 工作日志 — 11:12 Luka：Chat 页按新风格出 3 稿（带小崽）

用户贴了两张图 —— 一张 chat 列表风格稿（白卡 + 头像/名字/预览/时间/未读红点，首行 Poo 高亮带仓鼠头像和粉心）+ 一张仓鼠三视图角色卡 —— 要求「chat 页按这个风格重新出一个 ui 图，带着仓鼠宠物元素」。

- **做法**（image2，三图喂入、逐张声明角色）：底图用模拟器当前 Chats 页截图（保住 Luka 自己的 chrome：头像 + Chats + 铃铛 + 底部 tab 栏）；风格稿只取列表样式（一张大白卡、行结构、首行蜜色高亮、红点未读）；角色卡只取小崽长相（可可棕 + 奶油白、粉鼻子、圆眼、小爪）。prompt 里写死「NEVER copy its screen chrome / watermark / Chinese text」，避免把参考图的版式与「微信 0950」水印抄进来。
- **产物**（`~/Pictures/Luka- 物料/opencode-image2/`，1024x1536）：
  - **v1** `20260915-111148-*.png`：小崽从白卡上沿探头（头 + 两只爪压在上沿），Poo 行 = 小崽头像 + 「Come play with me」+ 粉心；
  - **v2** `20260915-111114-*.png`：Poo 行做成加高的引导卡（坐姿小崽 + 对话气泡 + 粉心），白卡下沿再探一只（tab 栏上方）；
  - **v3** `20260915-111031-*.png`：小崽坐在卡片右上角挥手（铃铛下方），行分隔线上点缀小爪印。
- **注意**：生成图文字不可控（这轮 Poo / Come play with me 拼对了）；v2 气泡文案与行内预览重复；v3 的爪印是装饰细节，实现时可去掉。三稿的 chrome 与列表结构都能直接照做。

## 工作日志 — 11:20 Luka 桌面组件：改成数据驱动的四态 + 实现（rn-home-widget 0.4.0）

用户反馈「触发条件要改，我们没有连胜机制，按项目里真实存在的机制设计，然后实现」+ 给了 4 张去背图（`Luka- 物料/widght1-4.jpeg`）。

- **状态机（基于真实机制）**：`happy`＝有未读（notifications store 的 totalBadge）；`worried`＝距上次宠物互动 ≥ 24h；`content`＝今天互动过；`waiting`＝兜底。**状态由组件自己按本地时钟推导**（快照里只有 `unreadCount` / `lastInteractionAt` 两个原始字段）—— 否则「它想你了」只能在用户回来之后才出现，失去召回意义。
- **素材坑**：4 张 jpeg **无 alpha、背景是纯黑**（导出丢了透明通道）。写了洪水填充脚本（只从「边框上确实是黑的」像素起种子，避免填进小崽身上）+ 连通域去斑点，出透明 PNG 存 `apps/luka/widgets/states/`。规格写在那个目录的 README 里（换 2x 更好，命名不变）。
- **包**：`@heyhru/rn-home-widget` 0.3.0 → 0.4.0，新增 **states 模式**（给 `states` 选项才启用，frames 模式一行未动）。iOS 生成 SwiftUI（读 App Group 快照、推导状态、`.after(nextTransition)` 排下一个转变点）；Android 生成 RemoteViews 布局 + provider 读 SharedPreferences（`updatePeriodMillis` 30 分钟重推）。颜色/文案/key/深链全部 app 注入，包里不带视觉。测试 46 条（含两端推导共享同一阈值的守门）。
- **App 侧**：`lib/widget-snapshot.ts`（严格解析：版本精确比对、坏字段整份作废、负计数夹 0）+ `services/widget-sync.ts`（写容器免费每次写、请求重绘按 15 分钟窗口节流 + face 比对）+ `_layout.tsx` 在宠物 hydrate/孵化后 start、登出时 stop+clear。深链 `luka://pet` 走 expo-router 自带解析（已核对 `extractPathFromURL`：host 会进 path）。
- **门禁**：typecheck / lint（19 条既存 warning）/ boundaries 全绿；luka 685 tests 通过；`expo prebuild -p ios` 实跑，产物 `ios/HomeWidget/HomeWidget.swift` + 4 个 state imageset 正确；生成的 Swift 过 `swiftc -parse`。
- **顺手改**：`daysLabel` 从 `app/pet.tsx` 提到 `lib/pet-copy.ts`（组件与宠物页共用同一句「Together for N days」）；`stores/pet.ts` 的 `lastInteraction` 改为导出。
- **待办**：引导页三张 `widget-*.jpg` 预览还是旧暗色款；`widgets/frames/` 已无人引用；素材换 2x 透明 PNG 更清晰。

## 工作日志 — 11:21 figma-write-bridge：写 42/43 两屏（Daily Bonus 弹层 / 恋爱铃横幅）分层复刻 spec

接 09-14 那条线继续，仍然只写模块文件，不改 `lib/`、`plugin/`、`bridge.mjs` 与 sitin-rn 仓库。这两屏**没有截图**，全按代码还原（`heard.png` / `point-down.png` 从视频帧抽的图已在 `images/app/pet/`）。

- **产物**：`screens/42-daily-bonus.mjs`（`42 · Daily Bonus 弹层`，10 个顶层子节点）与 `screens/43-love-ring.mjs`（`43 · 恋爱铃横幅`，9 个）；`node -e import(...).build()` 两屏过，另写 walker dump 逐层核坐标。
- **42 几何**：外层 `px-8` 把 `max-w-[330px]` 压到 **326**（不是 330），卡片 326×400 @(32,222)（垂直居中）；高度 = pt-8 + 小崽 140 + mt-4 + 标题 36 + mt-1 + 副标 20 + mt-4 + 蜜色胶囊 36 + mt-5 + CTA 56 + pb-6。小崽 140（旧的 32 屏写的是 150，已过期）。文案取 `app/dev/index.tsx` 的预览假数据（`reward=2` / `streakDay=1` / `title="Streak x 1 🎉"`，代码注释说与真签到发 2 枚一致）。底图 import 21 的 children 摊平，再叠 `Colors.scrim` + 弹层。
- **43 几何**：胶囊 366×80 @(12,54)（`insets.top` 取 54）、头像 52 @(26,68)、蓝角标 24 @(22,98)、两颗 52 圆键 @x 256/318、探头小崽 88 @(288,1)（`bottom:100%` + `marginBottom:-35` → 压进胶囊 35、头顶出屏幕）。小崽排在胶囊之后（代码 `zIndex:1`），向下的爪子才压在接听键上沿。decline / video 两枚 SVG 用 `lib/svgicon.mjs` 的 `iconSpec(appAssetPath(...))` 直接读 sitin-rn 真素材转矢量（同 02 屏做法，不改 lib）。
- **发现的代码问题**：`reward-modal.tsx` 里 `<SparkEmblem size={16} />` 没传 color，默认 `#FDD496` 与蜜色胶囊底**同色 → 看不见**（`spark-emblem.tsx` 的默认色本来是给奶油底用的）。spec 里按胶囊自己的墨色 `honeyInk` 画，并在文件注释里标了。
- **假设**：① 43 的邀请人是服务端数据，取 bridge 里 18/20 屏已用的 mock 身份（Macy + `mock/avatars/person-04.jpg`）；② 42 底图沿用 21，于是卡片副标题仍是截图里的 "Streak Bonus"（当前代码是 `hintText`，兜底 "Come back tomorrow for more" / "Your coins are waiting"），且底卡是**待领态**（Check in）而弹层是**已领态** —— 两处要不要按代码改由 21 决定。
- **顺手**：今日重点里「`heard.mov` 自弹层换素材后没人引用」与代码不符 —— `assets/pet/index.ios.ts` 的 `PET_HEARD_SOURCE` 正引用它，`reward-modal.tsx` 在用它（2026-09-14 17:06 的 `c562c89e` 就是换成它），那条待办可以划掉。

## 工作日志 — 11:23 figma-write-bridge：写 35–41 七屏（Settings + 六个 edit-profile 子页）分层复刻 spec

同一个批次里我负责 7 屏（其余编号由并行会话写），只写模块文件：`35-settings` / `36-edit-bio` /
`37-edit-education` / `38-edit-height` / `39-edit-interests` / `40-edit-location` /
`41-edit-occupation`。没有截图，全按 `0133747f`（六个子页重做）+ `13578314`（Settings 粉底）后的代码
还原；不改 `lib/`、`plugin/`、`bridge.mjs`、sitin-rn，七个模块各自独立（不依赖 09-14 的
`_edit-subpage.mjs`）。

- **验证**：七屏 `node -e import(...).build()` 全过（顶层子节点 9/10/8/6/9/10/8），另跑 walker dump
  逐层核绝对坐标；图片 URL 与文件都核对过（`pet/tilt-head.png`、`pet/like.png`、`mock/avatars/self.jpg`）。
- **外壳基线**：沿用 24–26 那条 —— 裸 `‹` y59、eyebrow 111、标题 134；卡片顶 = 标题底 + 20
  （height 例外：返回行下沿 91 + `mt-16` = 155；interests 再加 `mt-8` = 216）。
- **贴底 CTA 按代码算，不再统一 728**：bio/education/occupation/location/height 的底部留白是
  `max(insets.bottom,16)+6` 或直接 40 → 胶囊 **y746**；interests 是 `+20`（54）→ **y732**。旧 33–38
  全用 728，是 `_edit-subpage.mjs` 的旧基线。
- **仓鼠改用视频帧**：`pet/tilt-head.png`（五个子页）/ `pet/like.png`（Height 常态，拖动时才是 clap），
  不再用 Android 兜底的 `hamster-lean.png`。帧是 320 画布、底部有透明边（tilt-head 30px、like 48px），
  按帧自身 alpha 定位，让前爪落在卡沿下 10pt（`PAW_OVERLAP`），而不是套代码里那组实测常量（tilt-head
  那组与抽出的帧差 7px）。
- **Height 标尺按真实组件**：`width` 342 → `floor(342/50.5)=6` 格 → 盒宽 **303**（不是 342），居中
  于卡片；选中 73in 居中，可见 7 个标签（两端各半格被圆角盒裁掉，与真机一致）。读数 `6′1″` +
  `≈ 185 cm`（`formatHeight` 用的是 prime 字符）。
- **Interests 数据取仓库自己的 mock**：目录 = `mock/handlers/profile.ts` 的 `INTEREST_TABS`（Outdoors /
  Food & drink / …），chip 宽按 Nunito 14 SemiBold 逐条实测写死并预先分行（第 4 组 MAKING THINGS 只露标题，
  卡沿裁掉，和 `fill` 卡片一致）。状态取**空态**（`0 / 10`、无勾），与 hub 24 上那行 `add yours ✦` 对齐；
  AiTip 在滚动内容末尾、视口外，未画。
- **Location 状态**：沿用 hub 24 的 `Carpentersville, IL` → ZIP `60110`（`searchCityesWithCode` 只命中
  1 条 → `1 match` + 选中行实心星），Save 可用。
- **编号冲突（未动）**：仓库里 09-14 那批 `31-settings.mjs` / `33-edit-height.mjs` … `38-edit-bio.mjs`
  覆盖同样的屏，`group-8` / `group-9` 仍 import 它们；新 35–41 没接进任何 group（和 27–30 那次一样，
  等用户定夺）。

## 工作日志 — 11:25 figma-write-bridge：18 / 19 两屏按瀑布流重画（Discover · Nearby / Feed）

接 09-14 那条线继续，只改这两个模块文件（`lib/`、`plugin/`、`bridge.mjs`、sitin-rn 一律没碰）。

- **背景**：代码 `d34b92bf`（Discover / Nearby 改瀑布流、视觉统一到蜜色新稿）之后，18 屏
  （原划卡大卡）与 19 屏（原大白卡列表）画的都是旧版式。重画成 `MasonryList` 的两列瀑布流。
- **18 · Nearby**：列表模式（`DeckColor.background` #FBF3E6 铺 54 以下），20 的左右边距、
  169 列宽、12 列缝；窗口 10 人里画屏幕内 6 张（Elena/Priya/Sofia/Mia/Naomi/Harper），
  高度按 `masonryAspect(id)` 散列（188/169/235/256/211），标签取 interests 前两条、名字 19/24
  压底部渐变（六段纯色叠），右上 32pt 招呼钮；页头浮起 + Nearby 选中 + `DiscoverViewToggle`
  74×44（代码实测宽，稿子是 66）选中 list。旧划卡层（仓鼠 + 气泡 + Say hi/Skip/♥）整块删掉。
- **19 · Feed**：信任横幅 170..204（34pt 浅绿胶囊），第一排 216、第二排 537，封面 3:4 = 231、
  卡高 309（`FEED_TEXT_BLOCK` 78）；作者行 = 头像 20 + 名字 + 心 + 赞数（右对齐），封面右上
  30pt 招呼钮；页头 Feed 选中、无开关；FAB 56 琥珀圆 + `point-down.png`（zIndex 更高，画在
  按钮之后）。
- **验证**：两屏 `build()` 过（children 6 / 7）、group-5 总装 import 过；walker dump 逐层核
  坐标；所有 IMAGE 引用的素材在 `images/app/` 都在。没改别的文件。
- **假设**（都写进文件注释或本条）：① Nearby 远端默认是 deck（`NEARBY_DEFAULT_VIEW = 'deck'`），
  我按任务要求画瀑布流列表 —— **deck 模式现在没有 spec 覆盖**，要的话得单开一屏；② 窗口 10 人
  而 mock 首页只返 8 人（`loadFeed` limit 8），按「预取后」的 10 人画（后 4 张本来也在折叠线
  下）；③ Feed 封面统一 3:4 —— mock 没有 `imageRatios`，代码就是回退 3/4（骨架屏里那套错落比例
  要真后端数据才出现）；④ 正文按 `numberOfLines={2}` 预截断；⑤ `HeaderBlurFade` 不画（静态帧里
  没有内容滚到页头底下，模糊/底色渐变不可见）。
- **沉淀**：`design-mock-measure-and-sample` 追加两节 —— `numberOfLines` 截断的词边界规则 +
  用 TextKit（`byWordWrapping`，别用 `byTruncatingTail`）复现折行；`flex-wrap` 临界值必须按
  真字宽量（Naomi 的 chip 行 146.9 > 145，差 1.9pt 也换行）。

## 工作日志 — 11:28 figma-write-bridge：21 / 22 / 24 三屏按最新代码更新分层复刻 spec

接 09-14 那条线继续，只改这三个模块文件（`lib/`、`plugin/`、`bridge.mjs`、其它 screens、sitin-rn 一律没碰）。

- **21 · Get more**：Edit profile 入口从 14px 棕字改成 **96×32 蜜色胶囊**（`6365771c`：honey 底 +
  honeyInk 字，x274 y327.5，右缘 370 = `px-5`）；名字行那颗心从 x240 挪到 **228**（代码是 `ml-2`
  接在 age 后，整组居中在 195 —— 旧稿偏右 11pt，用真机截图核对）；Daily bonus 副标题
  "Streak Bonus" 改成代码兜底 **"Your coins are waiting"**（`hintText` 为空 + 未签到；旧稿那句是
  服务端数据，42 屏 import 本屏 children 当底图，会跟着更新）。
- **22 · My feeds**：小崽素材 `heard.png` → **`stay-all.png`**（`3b1737ea` 换回），两颗心按素材并集
  盒重算到耳朵右上（(66,460) 10pt / (78,451) 13pt），气泡/尾巴/文案整行移到 452..491；顶部补回
  **完整头部**（头像 + 名字 + 胶囊 + MeTabs），列表内容整体下移 276（小崽行 427、composer 511、
  第一张白卡 583，下一张 941 在折叠线下）。
- **24 · Edit profile**：空值行尾的 ✦ 单独上 **alertDot 红**（`d34b92bf`：`add yours` 可可色 + 星红，
  拆成两个 text 节点，右缘 350）；顶栏注释标上 `95111aa3` 钉住不滚（顶部状态下几何不变）。
- **验证**：三屏 `build()` 过（children 8 / 8 / 7），42 屏（import 21 底图）仍过；walker dump 逐层核
  绝对坐标；引用的 5 个素材文件都在。真机（模拟器 iPhone 17，`luka:///(tabs)/me`）截图核了胶囊
  右缘/宽与名字行居中。
- **发现（代码问题，未改）**：Me 页头部是**整块、不裁剪**地随滚动上移，任何滚动位置都会有头部元素
  压在顶栏/状态栏上 —— 全收起时名字行停在 **y=1..36**、分段控件落在 **56..102**（盖住导航栏）。旧 spec
  那种「分段钉在顶栏下」的状态在新代码里不存在，所以 22 按**滚动顶部**画；要不要修（容器加
  `overflow-hidden` 或 clamp 减掉顶部留白）由用户定。已沉淀进 `rn-scroll-perf-and-blur-header` 第五节。
- **假设**：① 22 的头部高按旧稿 325（8 + 200 + 16 + 名字行 35 + 20 + 46），列表内容 y 以此为准；
  ② 名字行沿用旧稿 mock 身份（AA, 28）+ `self.jpg`；③ 24 顶栏钉住在顶部状态无视觉差，只更新注释；
  ④ 21 菜单卡仍只画两行（旧稿如此，第三行 "More information" 在 844 画布上会被标签栏压住一半）。

## 工作日志 — 11:30 figma-write-bridge：写 31–34 四屏（Sparks store / Credits recharge / Sparks ledger / Plus pass）

同一个并行批次里我负责支付面四屏，只写模块文件：`screens/31-sparks-store.mjs` / `32-credits-recharge.mjs` /
`33-history.mjs` / `34-plus-pass.mjs`；不改 `lib/`、`plugin/`、`bridge.mjs` 与 sitin-rn（旧编号
`31-settings.mjs` / `32-daily-bonus-modal.mjs` / `33-edit-height.mjs` / `34-edit-location.mjs` 未动，
本批并行会话正在重编号：Settings→35、Daily Bonus→42、edit 子页→38–41）。

- **验证**：四屏 `node -e import(...).build()` 全过（顶层子节点 5 / 4 / 4 / 12），另跑 walker dump
  逐层核绝对坐标与出界；所有 IMAGE 引用的素材在 `images/app/` 都在。
- **没有截图 → 用 CoreText 量真字体**（`swift` + 注册仓库里的 Nunito TTF，`CTFramesetter` 取折行）：
  ① 34 的权益格 01 是 **3 行**（"750/1,500/3,500" 115 + "Coins instantly" 101.1 + "added" 42.7，
  文字宽 137），其余五格 2 行 → 行高 121/101/101；② 31 的法务两行断在 "…non-refundable" /
  "and expire per our Terms."；③ 32 的副标题断在 "…Balance:" / "12."；④ 34 的 lead/尾在折行处
  跨行（"video call|every day"、"post|instantly"），按实测宽度拆成同行的两个 text 节点。
- **31 · Sparks store**：CreditsHeader（X 方 32 + History）→ 余额卡（SparkEmblem 27×36 用
  `SparkEmblem` 原 path 发 VECTOR，同 42 屏做法）→ OR PICK A PACK → PackGrid（hero=850 的
  BEST VALUE + 两张小卡 + 三条全宽行）。**ScrollView 视口用带 `clipsContent` 的 FRAME 画**（128..668，
  高 540）—— PayArea 高 176 钉在屏底，第 3 条全宽行（1800）本来就在折叠线下，不裁会叠到 CTA 上。
  默认选中第一张（`useState(0)`）→ 20 那张小卡是选中态。DTC 支（Continue + "+10% Sparks" 角标 +
  Continue with IAP）与内购支二选一，取 DTC（`paymentBonus` 默认 0.1；代码注释把三方当主推）。
- **32 · Credits recharge**：`paddingTop` 顶行（‹ + Restore）→ SPARKS RUNNING LOW / Almost out of
  sparks → 倒计时卡（小崽从卡上沿探出，right -2 / top -104）→ 6 行档位（中间档 400 选中 + BEST DEAL）
  → CTA。CoinEmblem 的 `assets/coin.png` 桥里没有 → 用 rim/face 两圆 + `¢` 近似（注释标了）。
- **33 · Sparks ledger**：白底屏（不是奶油底）。HistoryHeader = ‹ 方块 + 余额胶囊（SparksCoinGlyph
  用 ELLIPSE 描边圆 + 组件原 path 的 C 弧）+ "Sparks history"；列表取 `data/credits.ts` 的
  COIN_HISTORY，按连续日期分四组（TODAY 2 行 / YESTERDAY / MON / SUN），正负色与 8pt 圆点按代码。
- **34 · Plus pass**：**代码在 390×844 上装不下** —— 内容约 869pt（header 90 + 权益格 347 + 套餐 206 +
  CTA 50 + 页脚 16 + 各 gap 96 + pb 32）> 视口 746pt，`grow` + `mt-auto` 只在内容短时才「沉底」。
  按任务里「套餐与按钮组置底」的要求：底部那组钉在屏底（CHOOSE YOUR PASS 464 / 套餐 504–710 /
  CTA 722–772 / 页脚 796），权益格裁在第二行下沿（462）—— 即真机滚到底的构图，**05/06 两格被裁掉**
  （注释里写明了）。小崽 `stay-all` 按 `bottom: 50 − 8` 坐在 CTA 上沿、水平居中。
- **假设**：① 首充加成数值（+10 / +30 / +120 / +600）按 `data/credits.ts` 里 `bonus` 文案的量级取
  （fixture 没有 `firstBonus`，后端才有）；② 34 的档位是后端下发，周期取 `planTermLabel()` 的
  "1 week / 1 month / 12 months"，价格除年费 $59.99（共享包测试里唯一真实值）外都是估的；
  ③ 仓鼠素材：`hamster-happy` → `pet/like.png`、`hamster-ask` → `pet/tilt-head.png`、
  `hamster-sit` → `pet/stay-all.png`（桥里没有那三张静态图）；④ 34 取非 DTC 支（单一 CTA）——
  `paywall/index.tsx` 本来也没用 `ContinueCta`，任务里那句是泛指 payment 组件；⑤ 32 的 CTA 走
  DTC 支（主 CTA + Continue with IAP）。

## 工作日志 — 11:45 Luka：Chats 页按新稿重做（白卡列表 + 首行小崽/招呼合并 + 页头右上小崽）

用户从三稿里选了 v3（`20260915-111031-*.png`），两处修正 + 一个问题确认后要求「直接照这版实现到代码」：
① 首行 Poo → **Luka**，并且就是**包装后的 Hellos You Sent 栏**（问过：选「小崽行与 Hellos You Sent 合成一行」）；② 右上角仓鼠用 **luka.mov**（即 `point-down.mov`）。

- **首行合并**（新 `components/chat/pet-hellos-row.tsx`）：小崽头像（`hamster-sit.png` 装在白圆角方框里）+ 名字 `DEFAULT_PET_NAME`（= Luka）+ 副标题「All the hi's you've sent」+ 右侧粉心（`DeckColor.blush`）。**两个入口**：整行 → `/waves-you-sent`，头像/粉心 → `/pet`（内层两个 Pressable 自己吃触摸）。
- **右上小崽**（新 `components/pet/pet-perch-corner.tsx`）：`PetVisual` + `PET_POINT_DOWN_SOURCE`，边长 72，趴在卡片上沿。`PET_OVERHANG = 38` 是**逐帧量出来的**：`point-down.mov` 的可见内容在画布上半（静止帧只到 50~52%，伸展帧到 66%），所以「抵掉留白 + 露 2dp」= 72×50% + 2。触摸框只做露出来那一截（`height: PET_SIZE - PET_OVERHANG`）—— 画布压在卡片里的透明区不挡首行手势（RN 不裁溢出子视图，但触摸不送到父边界外）。
- **列表装进一张白卡**（`app/(tabs)/index.tsx`）：`mx-4 flex-1 overflow-hidden rounded-[28px] bg-surface`，`marginBottom: insets.bottom + 8`（iOS 26 的 `bottom` 已含系统标签栏）—— 列表自己不再替标签栏留位（`paddingTop 10 / paddingBottom 12`，去掉 `progressViewOffset` 与 `PET_PERCH_BAND_HEIGHT`）。小崽排在卡片之后渲染（后渲染压得住）。
- **会话行**：未读角标从棕色直角小块改成**红色整圆**（`Colors.badge`，稿子是红点）。
- **文案**：`copy/interactions.ts` 加 `GREETING_SENT_SUBTITLE`；`copy/pet.ts` 删 `PET_CHATS_TEASER`（气泡没了，名字底下那行让给招呼入口）、加 `PET_PERCH_LABEL`。
- **删**：`components/chat/waves-you-sent-card.tsx` + `assets/chat/hellos-sent-{icon,bubble,chevron}.svg`（旧招呼行）、`components/pet/pet-perch-banner.tsx`（旧横幅，改完后全仓无引用）；同步改 `my-feed-pet` / `pet-animation.ios` 的注释与 `assets/pet/README.md` 三处提法。`shape-language.test.ts` 的 MIGRATED 加 `app/(tabs)/index.tsx`、`chat/(conversation-card|pet-hellos-row)`、`pet/pet-perch-corner`。
- **验证**：模拟器截图（`/tmp/v-chats-final.png`）—— 白卡、首行蜜色高亮 + 小崽头像 + Luka + 副标题 + 粉心、右上小崽趴在卡片上沿、Luka Team 行红色圆角标都对上稿子；`pnpm lint` 0 error（1 条既存 warning）。
- **注意（不是本次改动）**：工作区现在被另一会话的 `1f448908 Merge origin/feature/blueprint into feature/luka-ios` 拖红了 —— typecheck 报 `client.ts` / `settings.ts` 缺 `CreatePbClientOptions` 的 `packageName/appVersion/appBuild`，17 条测试挂（mock 缺 `EmailLoginRequest` / `GetSubscriptionManageUrlRequest` 处理器 + analytics contracts），Metro 还有一个 `TransformError: src/global.css`。那批文件（`app.config.ts` / `_layout.tsx` / `pet.tsx` / `stores/pet.ts` / `rn-home-widget`）都不是我的，没碰。
- **小崽落点的一个观感**：它的爪子是奶油白，压在白卡上会融进卡里 —— 实际读起来是「从卡片上沿探头」（`point-down` 这只本来就是探头姿势），与稿子「爪子搭在卡沿上」略有差别，要更明显的话把 `PET_OVERHANG` 再调小几 pt 即可。

## 工作日志 — 11:50 把「最近开发的」补进 Figma（section 7–10 + 刷新 5/6）

**需求**：截图之后新增/大改的屏补进 `poo` 页。范围（用户确认）：新增 15 屏 + 刷新视觉变了的旧屏。

**新增（17 屏，4 个 section）**
- `· 7 开白四步`（27–30）：`onboarding/identify|looking-for|drink|smoke`，进度条是长流程 **06/09…09/09**（`lib/onboarding-flow.ts` 的 LONG_FLOW），不是短流程的 5 步。
- `· 8 Sparks / Plus Pass`（31–34）：`credits/index|recharge|history`、`paywall/index`。
- `· 9 Settings / Edit profile 子页`（35–41）：`settings/index` + `edit-profile/{bio,education,height,interests,location,occupation}`（提交 `0133747f` 的「探头小崽 + 浮起卡片」新稿）。
- `· 10 弹层`（42–43）：Daily Bonus 弹层（`ui/reward-modal.tsx` 重做版，heard 小崽）+ 恋爱铃横幅（`call/love-ring-banner.tsx`，point-down 小崽）。

**刷新（8 屏，重推 section 5/6）**
- 18/19 → `d34b92bf` 的**瀑布流（masonry）**版式（`components/masonry/masonry-list.tsx`），旧的划卡层整块删掉。
- 21/22 → Me 页头部整块随滚动收起 + Edit profile 改**蜜色胶囊**；22 小崽换回 `stay-all.mov`、副标题改成代码兜底的 `Your coins are waiting`。
- 24 → hub 顶栏钉住不滚 + 空值行尾 `✦` 单独上 `alertDot` 红。
- 顺手修 39 的兴趣 chip 文字盒（原 `w-44` 太窄，"Hiking" 被折成两行 → 改左 16/右 16）。

**与并行会话的冲突处理**：昨晚（09-14 19:15）另一个会话已推了 `· 7 开白四步` / `· 8 Settings 与 Daily Bonus` / `· 9 Edit profile 子页`（按 image2 生成图画的，非代码）。本次按用户「根据代码分层」的要求**重建**：删掉旧的 8/9，7 同名覆盖，并把开白四步/设置/子页全部换成代码版；旧 spec 文件（`27-review-*.mjs`、`31-settings.mjs`、`33-38-*.mjs`、`group-8-settings.mjs`、`group-9-edit-subpages.mjs`）保留未删。

**结果**：`poo` 页 12 个 section —— 2 个旧 Poo + **10 个「分层复刻」共 43 帧**（26 + 17），全部可编辑节点树；无游离节点。

**踩的坑**：Figma REST API 中途整体不可用（`curl https://api.figma.com` 返回 000，代理节点问题），但**本地插件桥不受影响**，写入照常 —— 只是没法回读渲染图核对，等网络恢复补的。

## 工作日志 — 12:10 组件在模拟器上验证 + 修掉冷启动误报「0 未读」的坑

- **构建**：`expo prebuild -p ios --no-install` + `pod install` + `expo run:ios --no-bundler`（约 40 min，含一次网络失败重试）。产物正确：`ios/HomeWidget/HomeWidget.swift`（states 版）+ 4 个 `state-*.imageset` + Xcode target 的 Resources 阶段；生成的 Swift 过 `swiftc -parse`。
- **踩坑（已沉淀）**：pod install 那次 Maven 检查失败 → RN core 静默回落源码编译，与预编译 Expo 模块混搭 → app 装上一开就 dyld 崩（`@rpath/React.framework/React`）。**不是 widget 改动引起的**。关掉 Clash 后重跑 `pod install` 即恢复预编译 core，重建后正常启动。详见 [rn-prebuilt-core-dyld](../30-Resources/troubleshooting/rn-prebuilt-core-dyld.md)。
- **真机链路验证**（模拟器 + 真实 app 数据）：组件在桌面上依次显示 `happy`（「18 / 14 new updates」+ 开心姿态）与 `waiting`（「Come play with me」+ 中性姿态），名字与「Together for 1 day」正确 —— 说明「app 发布快照 → App Group → 组件读 → 自己推导状态 → 渲染图文」整条链路通。
- **发现并修掉一个真 bug**：冷启动时未读还是 0 就发布了快照，随后真值到达却被 15 分钟重绘节流挡住 —— 组件会长时间显示「Come play with me」而 tab 上有 18 条未读。修法：`startWidgetSync` 的**首次发布等到未读来源至少报过一次**（任何 setBadges，哪怕是 0；期间组件保留上一份快照，宁旧不假）。加了回归测试。
- **门禁**：lint 1 条既存 warning / boundaries OK / typecheck OK / luka 687 tests（+30）/ rn-home-widget 46 tests。
- **未验证**：`worried` / `content` 两态没有在真机/模拟器上肉眼看到（需要「一天没互动」或「今天互动过且无未读」的真实状态）；逻辑与素材映射由单测与生成源码守门。

## 工作日志 — 13:50 Luka：Chats 首行头像换成稿子上那只（粉底圆 + 挥手小崽）

用户指出实机与稿子对不上：我这版是**白底圆角方框 + 抱爪坐着**（`hamster-sit.png`），稿子上是**粉底圆 + 挥手**那只，并给了素材位置 `~/Pictures/Luka- 物料/chat.png`。

- **素材**：`chat.png`（782×808，自带透明底）拷成 `apps/luka/src/assets/mascot/hamster-chat.png`（不叫 wave —— 目录里已有一只 `hamster-waving.png`，避免撞名）。
- **改法**（`components/chat/pet-hellos-row.tsx`）：头像从 `rounded-[16px]` 白底方框改成 `rounded-full` + `Colors.peach`（#F2B5BC，全库离稿子那颗 #F8C1B2 最近的一档）**粉底圆**；图用 `contentFit="cover"` 填满整圆（小崽身子被圆裁掉一圈，和会话行那些照片头像同一种形状）。删掉不再用的 `AVATAR_RADIUS`。
- **验证**：模拟器截图（`/tmp/v-chats-avatar3.png`）—— 粉底圆里那只挥手小崽与稿子一致；`pnpm lint` 0 error（1 条既存 warning）。

## 工作日志 — 14:15 Luka：首行小崽与右上小崽再调（粉底圆改成两层、两只都放大）

用户看完实机两处反馈：「仓鼠要在粉色底色外面」「这个仓鼠可以大一点」（附图：首行头像特写 + 右上小崽那张）。

- **首行头像改成两层**（`components/chat/pet-hellos-row.tsx`）：粉底圆（`DISC`，`rounded-full`）画在下面、小崽（`PET`，`contentFit="contain"`）画在上面且**不裁**（去掉 `overflow-hidden`）—— 圆只是底色，耳朵/爪子/身子都探在圆外。`PET` 62 > 头像槽 54（小崽也探出槽一点），`DISC` 52（比脸大、比整只小）。中间试过 42 / 38：圆几乎被小崽整个盖住，只从爪子边露几条，等于没有底色。
- **右上小崽放大**：`PET_SIZE` 72 → 88；`PET_OVERHANG` 从写死的 38 改成 `Math.round(PET_SIZE * 0.5 + 2)`，换尺寸时不用再手算。
- **验证**：模拟器截图 `/tmp/v-chats-bigger3.png`（粉底圆在头后露出来、小崽探出圆外；右上那只明显大了一圈）；`pnpm lint` 0 error。
- **追加（14:25）**：用户再调首行头像 —— 「这个里面的仓鼠应该再小一点，略大于粉色底色就行」，并贴了稿子的对照图（圆里那只）。改法：小崽 62 → **56**（只比粉底圆 52 大 4pt）；粉底圆**上移 6pt**（`DISC_RISE`）—— 圆心落到脸上、圆顶探到头顶之上，粉色才会在头上露出来。`chat.png` 的头顶贴着画布上沿、没有自带留白，所以这一步只能靠挪圆来做。截图 `/tmp/v-pet-final.png`；这轮门禁全绿：typecheck 0 / test 119 文件 687 通过 / lint 0 error（另一会话的 blueprint 合并破的红已修好）。

## 工作日志 — 14:22 Luka：全局 toast 重做（修白对白 + 底部浮出 + 避让底栏）

用户贴 Debug Tools 截图问「中间的白色框是什么」—— 是全局 toast 发送成功提示，但文字
`text-background`（#FDF9EE 奶油）压在 `Colors.panel`（#FFFFFF）上，几乎不可见。用户点单
「要修，重新设计一个全局 toast」；确认视觉取可可墨色胶囊、范围只改 Luka（blueprint 同款
白对白另说）。

- **根因**：模板 blueprint 的 toast 是白便签（`bg-panel` + `text-background`），它自己
  panel 与 background 同为 #FFFFFF；Luka 派生后 background 换成奶油，panel 仍白 → 白对近白。
  已沉淀 [derived-app-token-pair-contrast](../30-Resources/troubleshooting/derived-app-token-pair-contrast.md)
  （含「沉睡接缝」一节：`stores/bottom-bar` 一直只写不读）。
- **改动**（6 文件，未提交）：
  - `src/lib/toast-duration.ts`（新）：停留时长按字数 2400ms 起、45ms/字、封顶 5600ms；
  - `src/stores/toast.ts`：加 `seq`（同一句重发也让宿主重播动效/重播报）；计时改用 `toastDuration`；
  - `src/components/ui/toast.tsx`：重写为底部浮出的可可胶囊（`bg-foreground` + `text-background`、
    `rounded-full`、无投影，只留 `TOAST_LAYER` 的 elevation 压层）；读 `useBottomBarStore` 的
    `tallestBottomBar`，自动避开标签栏（97pt）/ 聊天输入栏 / 结算 CTA；reanimated 淡入上滑
    （180ms）、淡出 140ms、重复 toast 先落到 40% 再升起；iOS `announceForAccessibility` +
    Android `accessibilityLiveRegion`；`numberOfLines={4}` 上限；
  - `tests/design/shape-language.test.ts`：把 `components/ui/toast.tsx` 挪进 MIGRATED 白名单（按约定）；
  - `tests/lib/toast-duration.test.ts`（4 条）+ `tests/stores/toast.test.ts`（4 条，假时钟）。
- **验证**：`pnpm lint` 0 error（1 条既存 warning）/ `pnpm luka:typecheck` 0 / `pnpm --filter luka test`
  121 文件 695 通过。模拟器截图：无底栏屏（dev）短/长文案各一张（`/tmp/toast-short.png` 两行换行、
  `/tmp/toast-final.png` 单行胶囊）；有标签栏屏（Chats）确认浮在标签栏之上
  （`/tmp/toast-burst-5.png`）。触发方式是临时在 `dev/index.tsx` 与 `(tabs)/index.tsx` 挂
  `TEMP-VERIFY` 计时 toast，截完已删（`rg TEMP-VERIFY` 无残留）。
- **未提交**：工作区另有其他会话的 chats / widget / pet 改动（30+ 文件），本次只动上面 6 个；
  commit 需用户点头且只 `git add` 这 6 个文件（Luka 在 `feature/luka-ios`）。
- **追加（14:45）**：用户自己把常量调成 `PET 50 / DISC 55 / DISC_RISE 0`（改成圆包住小崽），并要求「加一个参数可以调整小崽左右」。加了 `PET_SHIFT_X`（正数向右、负数向左、0 = 槽里居中），用 `transform: [{ translateX }]` 而不是 `margin` —— `align-items: center` 居中的是边距盒，加 margin 只会被折半。顺手把 `PET` / `DISC` 的注释从「断言大小关系」改成「谁包住谁由两者的关系决定」，值再变注释也不会说谎。验证：临时设 10 截图确认生效（`/tmp/v-shift-test.png`）后已还原 0；lint 0 error。

## 工作日志 — 14:35 Luka：Video match 那条重设计出 3 稿

用户贴了当前 video match 条（旧语言：3px 直角 + 棕色块）说「这个条要重新设计一下」；问过之后选了「先出 2-3 版稿再实现」。

- **做法**（image2，两图喂入）：底图用用户那张卡片截图（保住内容与左右结构：圆头像 / 名字 + video searching / Taking one sealed hello. / call）；风格参考用当前 Chats 页截图（只取白卡 + 蜜色 + 大圆角那套语言）；v3 额外喂三视图取小崽长相。
- **产物**（`~/Pictures/Luka- 物料/opencode-image2/`，1536x1024）：
  - **v1** `20260915-142815-*.png`：紧凑一行（头像 48 + 蜂蜜色 rounded-full 状态胶囊 + 可可色圆 pill「call」），行坐在浅蜜色圆角块上；带整屏语境。
  - **v2** `20260915-142836-*.png`：整块卡（大圆头像 + 宽「call」胶囊横跨底部）；只有卡本身、没有屏语境。头像比稿子大不少，实现时按 56 收。
  - **v3** `20260915-142802-*.png`：同 v1 的排布 + 状态胶囊带小圆点；小崽趴在卡上沿 —— 但模型把它放到了**列表卡**上沿（就是现在右上角小崽那个位置），不是在条上，等于重复，选它的话小崽那部分可以不要。
- **注意**：生成图文字不可控（这轮 Mila / video searching / Taking one sealed hello. / call 都拼对了）；三稿都保留原信息结构，只换了形状与配色语言。

## 工作日志 — 14:33 Luka：Hellos You Sent 页重设计（视觉 + 信息结构）

用户贴了模拟器截图（`waves-you-sent`，旧语言：3px 直角便签 + 平铺行 + 点不动的行）说「这个页面要重新设计一下」。按默认规范模式先问了范围与参考，答「视觉 + 信息结构都重做 / 按 Luka 现有视觉自由发挥」，于是按 2026-09-15 的 Chats 稿那套（奶油底 + 白卡 + 蜜色 + 小崽）整屏重做。

- **信息结构**：顶栏改大标题（32）+ 白色圆返回键；顶部加 hero 白卡（`hamster-peek` 小崽探出头 + 「N hellos waiting」+ 原有那句 Chat unlocks…）；列表按 **TODAY / YESTERDAY / EARLIER** 分组（复用 Notifications 的日界）；每行右侧加 `Sent` 回执胶囊（蜜底 + 焦糖深字 + check）；空态换 `EmptyHero`（无图版，同 People 两页）+ Find people nearby；页脚那句隐私说明保留。
- **顺手修掉的真问题**：旧版每行 `onPress={() => {}}` —— 行点不动。现在 `openUserProfile(router, peerId, "chat")` 打开对方主页（挡自己/空 id + 带 source 埋点）。
- **改动**（8 文件，未提交）：
  - `src/lib/day-groups.ts`（新）：通用 `groupByDay<T>`，notifications-group 改为委托它（一份日界，不再两处）；
  - `src/lib/chat-list.ts`：加 `pendingWaveSections`（待回应招呼按最后外发消息分天，`now` 可注入便于测试）；
  - `src/components/chat/waves-hero.tsx`（新）/ 删 `waves-explainer.tsx`；`wave-order-row.tsx` 重写（48 头像 + 在线点 + Sent 胶囊）；
  - `src/app/waves-you-sent.tsx` 重写（SectionList 装在一张 20px 白卡里）；
  - `tests/design/shape-language.test.ts` 白名单 + `design/theme.js` 迁移注记改成「以白名单为唯一事实源」。
- **验证**：`pnpm --filter luka test` 121 文件 697 通过（新增 2 条分组单测）；`pnpm luka:typecheck` 0；`pnpm --filter luka lint` 0 error（1 条既存 warning，`use-swipe-actions.ts`）；`pnpm boundaries` OK。模拟器实机截图：`luka://waves-you-sent` 深链直达，5 条数据分两组，见 `/tmp/luka-waves-redesign.png`。
- **边角发现（未动）**：`pnpm --filter luka tokens:check` 在 HEAD 就是 stale —— `design/theme.js` 的 `callBannerBadge` 已是 `#684431`，而入库的生成物 `src/theme/colors.ts` 还是 `#0A84FF`（且 generator 现在不输出注释，那行注释是手留的）。我只改了 theme.js 的注释、没跑 tokens 覆盖它；要不要把这处漂移单独收掉等用户决定。
- **未提交**：工作区仍是多会话共用（chats / widget / pet 30+ 文件的脏改动在），本次只动上面 8 个；分支 `feature/luka-ios`，commit 待用户点头。

## 工作日志 — 14:45 Luka：video match 条按稿重做 + Hellos 行常驻置顶

用户从三稿里选了 v3（`20260915-142802-*.png`），并要求「All the hi's you've sent 如果存在要持续置顶」。

- **video match 条重做**（`components/chat/video-match-card.tsx`）：从「3px 直角 + 两块实心棕」改成与会话行同构的一行 —— 头像 48；名字 + `rounded-full` 的「video searching」状态胶囊（`Colors.tagBg` 底、可可字、一颗鲑鱼粉小圆点 `DeckColor.blush`，照稿子取的 #EFB3A7）；副标题；「call」改成可可色圆 pill（h-40，图标 + 字）。整行点击 = call，胶囊/按钮不再单独吃手势，并补了 `accessibilityRole/Label`。
- **接线**（`app/(tabs)/index.tsx`）：`ListFooterComponent` 去掉 `px-3 pt-4` 那层缩进 —— 头像要和上面那些会话行对齐。
- **置顶**（`lib/chat-list.ts`）：`buildChatsListItems` 不再把 waves 行按时间混排，改成**存在就排第一**（其余会话照旧按时间排）；`tests/lib/chat-list.test.ts` 的期望顺序与用例名同步更新。
- **守卫**：`shape-language.test.ts` 的 MIGRATED 加上 `chat/video-match-card`。
- **验证**：模拟器截图 `/tmp/v-vm-check.png` —— 首行是小崽/招呼那行（压在新的「22」会话之上）；video match 行（Mila）头像、胶囊、小圆点、call pill 都照稿子；lint 0 error / typecheck 0 / test 121 文件 702 通过。
