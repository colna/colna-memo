---
title: 2026-07-01
date: 2026-07-01
tags: daily
---

# 2026-07-01

## 今日重点

- [ ] shangtang-sdk-demo 项目:license 到期前(2026-07-26)找许佳续期
- [ ] shangtang-sdk-demo:验证 `npm install` + `npm run dev` 是否跑通,9 类 SDK 是否正常加载
- [ ] app-pwa 商汤美颜集成:等产品拉齐 P0 效果清单 + 商汤商务续期/OSS 商谈,再决定立项(见 [tech-proposal-pwa-integration](../10-Projects/shangtang-sdk-demo/tech-proposal-pwa-integration.md))
- [ ] 【follow-up 2026-06-30】sitin-next 后端 `app-social-proxy-server` 里指向 src 不存在脚本的死映射清理(单独开 PR)

## 工作日志

### 11:00 — shangtang-sdk-demo:clone + license 分发 + 项目笔记落库

- **做了什么**:
  1. `git clone git@github-colna:presence-io/shangtang-sdk-demo.git` 到工作区(SSH 走 `github-colna` 别名;直连 `git@github.com` 会被 pubkey 拒)
  2. 把根目录 `WebAR.lic`(2774 字节)复制到 `public/sdk/{earring,effects,glasses,hair,makeup,nail,ring,shoes,watch}/` 全 9 个 SDK 子目录 —— 商汤邮件明确要求每个 SDK 目录都需要 license
  3. 摸清项目结构 → 写项目笔记 [shangtang-sdk-demo/README](../10-Projects/shangtang-sdk-demo/README.md)
- **为什么**:用户要开始基于商汤 SenseMARS WebEffects 2.3 做 H5 试戴/美妆/特效集成 demo(参考 https://tryon.softsugar.com/),许佳邮件发来 SDK 交付 + license
- **结果**:
  - 项目已本地就绪(749M),9 个 SDK 目录 lic 齐
  - 项目笔记落 `10-Projects/`(有截止日期 = license 2026-07-26)
  - `npm install / npm run dev` 未验证
- **关键命令**:
  - `git clone git@github-colna:presence-io/shangtang-sdk-demo.git`(首次 2 分钟超时是仓库大 749M,后台重跑 exit 0)
  - `for d in public/sdk/*/; do cp WebAR.lic "$d"; done`
- **沉淀**:
  - 本工作区 clone git 项目一律走 `github-colna` SSH 别名,不要用 `git@github.com`(pubkey 拒);已在 `.agent-memory/memory.md` 有说明
  - 商汤 SenseMARS SDK 加载机制:tryon(watch/shoes/glasses/ring)走 `st-ar-tryon.js`,makeup(makeup/hair/nail)走 `st-ar-makeup.js` 且显式 `checkLicenseFromPath(sdkPath)` 校验,effects 走 `st-ar-effects.js`;dev 走本地 `public/sdk/*`,test/prod 走阿里 OSS 路径(license 已在 OSS 上,本地不用管) —— 完整细节见项目 README

### 14:30 — sitin-next feature/pwa:商汤美颜集成技术方案

- **做了什么**:
  1. `sitin-next` 切到 `feature/pwa` 分支(远端已合入 `pwa ota升级 #460` + 2 个 login/guildReport 更新)
  2. 深入 `packages/app-pwa` 视频通话链路(`services/webCallManager.tsx`、`hooks/useCameraStream.ts`),读 TRTC v5 官方 type 定义
  3. 出方案:[tech-proposal-pwa-integration](../10-Projects/shangtang-sdk-demo/tech-proposal-pwa-integration.md)
- **为什么**:用户要评估能否把商汤 SenseMARS WebEffects SDK 接进 heyhru PWA 视频通话
- **核心结论**:**技术可行**。TRTC v5 `LocalVideoConfig.option.videoTrack` 原生接受自定义 `MediaStreamTrack`,商汤 SDK 输出 canvas 走 `captureStream()` 即可上行。已有 `_trtc` 底层实例暴露(`getTRTCCloudInstance()._trtc`),不用改 TUICallEngine
- **推荐方案**:自定义 videoTrack 路径,只接 4 类 SDK(effects + makeup + hair + nail),舍弃 tryon 系(视频通话不需要);降级链兜底(商汤 → BasicBeauty → 裸流);P0(MVP)8-10 人天
- **关键约束**(sitin-next CLAUDE.md 硬规则):
  - Modifications 前必须列 diff + 拿用户确认,不能直接改代码
  - Doc-first:任何 code change 必须先更新文档
  - English only for code/comments/UI(本次方案是给用户的调研,落在 colna-memo 中文可以;真进 sitin-next 仓库要翻译成英文)
- **开放问题**(待用户/产品决策):
  1. 商汤商务:现在的 license 是 softsugar 客户的,直接用有法律风险
  2. OSS:demo 用的 OSS 是 softsugar 的,咱们要谈自己的路径
  3. 腾讯 BasicBeauty vs 商汤:如果只做基础美颜,免费的更省
  4. iOS 兼容底线(iOS 14 不支持 canvas.captureStream)
- **沉淀**:
  - TRTC v5 自定义视频源 API:`trtc.startLocalVideo({ view, publish: true, option: { videoTrack, profile }})` + `updateLocalVideo` 热切换
  - TRTC v5 内置美颜插件:`BasicBeauty`(免费,3 项)、`Beauty`(付费,细分 6 项 + Effect[] 滤镜)。位于 `plugins/video-effect/`
  - TUICallEngine v3 底层可以拿到原生 `trtc` 实例:`this.engine.getTRTCCloudInstance()._trtc`

### 15:00 — 商汤美颜方案:补充「对方端能看到美颜」验证章节

- **做了什么**:在 [tech-proposal-pwa-integration](../10-Projects/shangtang-sdk-demo/tech-proposal-pwa-integration.md) 最顶部加「零、核心业务价值验证」章节,把"对方能看到美颜"作为方案的**核心业务价值**显式落定,附端到端数据流图 + CSS 滤镜 vs 上行流处理对比表
- **为什么**:用户确认业务价值时的关键问题 —— 主播美颜后**对方是不是能看到**。答案 = 能,因为方案是改上行 `MediaStreamTrack` 本身(不是本地 CSS 滤镜)。这个点是方案的核心 ROI 依据,值得在方案顶部显式落地
- **结果**:方案文档更新,提出 3 个附加要点(对方零负担 / 对方 App 不用改 / 主播自拍所见即对方所见)+ 2 个边界(对方美颜要单独接、音频不在美颜范畴)
- **沉淀**:
  - **"美颜对方能看到"的判定原则**:凡是"改 MediaStreamTrack + 塞给推流 API"的方案,对方必然看得到;凡是"CSS filter / 只改本地 video element"的方案,对方必然看不到。这是产品沟通的关键边界
  - TRTC v5 官方教程 `20-advanced-customized-capture-rendering` 就是干这个的官方推荐路径

### 15:30 — sitin4.0 前端:把 OSS presigned 直传抽成基础能力

- **做了什么**:在 `sitin-next` 分支 `personal/zz/sitin4`,把「获取 OSS 上传凭证 + 直传」从 `chatModerationApi.ts` 抽出,新建独立基础模块。改动 3 文件(`packages/app-pwa/`):
  1. 新建 `src/http/ossUploadApi.ts` —— `FileType` 枚举 + `getUploadCredential`(拿 presigned PUT URL)+ `putToOss`(直传)+ 便捷组合 `uploadToOss({file,fileType,fileExt,contentType})`(签名+PUT 一步,返回含 `cdnUrl` 的 cred)。去掉了原来没用到的 `userId` 入参。
  2. `src/http/chatModerationApi.ts` —— 删掉 `FileType`/上传 interface/`getUploadCredential`/`putToOss` 及 `GetChatUploadCredential*` proto import,只留审核(`ViolationCategory`/`AuditResult`/`auditText`/`auditVoice`/`auditImage`)。
  3. `src/hooks/useChatModeration.ts` —— voice/image 分支两步(getCredential+putToOss)改为一句 `uploadToOss(...)`;`FileType` 改从 `ossUploadApi` 导入。
- **为什么**:用户要求把 presigned 直传做成可复用基础能力,和聊天审核解耦(相册、帖子图片等以后可直接 `import { uploadToOss, FileType } from "@/http/ossUploadApi"`)。
- **结果**:ESLint 3 文件 0 报错。proto 依赖仍是同一个 `chat_api`(后端补齐 `chat_api.proto` + `proto:gen` 后生效)。已 commit 到 `personal/zz/sitin4`。
- **沉淀**:仓库现有上传(`profileApi.uploadAlbumPhoto`)是文件字节走后端,不是 presigned 直传;OSS presigned 直传是 sitin4.0 新引入的模式,基础入口现为 `ossUploadApi.uploadToOss`。

### 昨日(2026-06-30)整理

- **保留**:
  - sitin-next PR #471 上线闭环记录(admin-sp 分支已合入 release/test-admin 并上线),原文保留在 06-30 Daily
  - lark-cli 多应用切换 + `cli_a948f5747e3b9ccc` 应用坑已归 [feishu-release-request-format](../30-Resources/feishu-release-request-format.md),不再复搬
- **复制到今日重点**:后端死映射清理 follow-up
- **归档**:无临时垃圾

### 13:55 — 商汤美颜方案:补充「下行美颜(对方无感的主播侧美颜)」章节 + SDK API 一手核验

- **做了什么**:在 [tech-proposal-pwa-integration](../10-Projects/shangtang-sdk-demo/tech-proposal-pwa-integration.md) 追加「八、下行美颜」章节:场景定义(下行 vs 上行)、SDK 一手证据、端到端数据流、工程实现、产品分级建议(P0 只上行 / P1 环境增强 / P2 才考虑改脸)、未核验点标注
- **为什么**:用户下一个业务问题 —— 能不能给主播看到的对方加美颜、对方无感?先核验技术断言,再给产品建议。上行方案文档已成型,下行是其延续章节,不新建独立笔记,归位到同一份方案
- **结果**:
  - 技术断言已核验:`trtc.getVideoTrack({ userId })` 支持返回远端 `MediaStreamTrack`,证据是本仓库已装的 `sitin-next/node_modules/trtc-sdk-v5/index.d.ts:2216`,SDK 版本 `5.15.3-beta.9`
  - 产品建议:P0 只做上行;P1 追加下行「环境增强档」(bokeh 背景虚化 + 亮度补正 + 降噪,不改脸);P2 才考虑「改脸档」(精度、预期落差、双向 GPU 翻倍多重风险)
  - 工程侧:复用 `beautySdkManager` 单例,+2-3 人天集成
  - 未核验项显式标注:①商汤 effects 具体清单 ②480p 下行商汤关键点精度下降幅度
- **关键命令 / diff**:
  - 核验方式(不靠记忆/搜索):`grep -n "getVideoTrack" sitin-next/node_modules/trtc-sdk-v5/index.d.ts` → 定位到 line 2216 注释和 line 2235 签名
- **沉淀**:
  - **"下行美颜"判定原则**:凡是"用 `getVideoTrack({userId})` 拿远端 track + 本地 canvas 处理 + UI 渲染 canvas"的方案,**对方一定无感**(因为对方那边播的是他自己的原相机,不消费主播这边的处理结果);对方能感知的必须是"影响上行/下行推流本身"的动作
  - **产品分档原则(改脸 vs 环境增强)**:"改人脸"和"改环境"要分档看,前者有预期落差风险(线下/其他场景暴露真实长相),后者(bokeh/亮度/降噪)对主播体验有帮助且无落差,ROI 明确更高
  - **技术断言核验方法优先级**:项目已装 SDK 源码/类型定义 > 官方文档 > 网络搜索 > 记忆。本仓库同时装了 `trtc-sdk-v5 5.15.3-beta.9`,类型注释精细到 API 参数含义,直接读比搜索靠谱

### 14:05 — 商汤美颜方案 HTML 可视化版本(light 风格 + Mermaid 彩色流程图 + 粒子动效)

- **做了什么**:把 [tech-proposal-pwa-integration](../10-Projects/shangtang-sdk-demo/tech-proposal-pwa-integration.md) 完整方案(§0 业务价值 → §8 下一步,含新增的下行美颜章节)写成自包含 HTML 文档,产物落在 [`_attachments/shangtang-beauty-pwa-tech-proposal.html`](../_attachments/shangtang-beauty-pwa-tech-proposal.html) 并同步到 metabot outputs 供用户在飞书查看
- **为什么**:方案文档是给决策层和 PM 看的,md 阅读体验不够直观;流程图、路径对比、产品分级建议这些结构化内容用可视化 HTML 呈现更好沟通,也方便对外分享
- **结果**:
  - 单文件 HTML,53KB,包含 5 张彩色 Mermaid 流程图(上行链路、下行链路、降级链、业务价值数据流等),按语义分色(粉=对方视角、蓝=TRTC、紫=服务器、黄=SDK、绿=canvas 处理)
  - light 风格:白底 + 紫/粉/蓝渐变强调 + 玻璃拟态卡片 + JetBrains Mono 代码块
  - 动效:粒子系统(canvas + 鼠标斥力 + 粒子间连线)、渐变文字流动、Section IntersectionObserver 淡入、ToC 滚动高亮、顶部渐变进度条、卡片悬停放大 + 渐变描边
  - 依赖极少:mermaid via CDN + Google Fonts,其余纯手写 CSS/JS
- **关键命令 / diff**:
  - `Write` `colna-memo/memory/_attachments/shangtang-beauty-pwa-tech-proposal.html`(不入索引,`_` 前缀)
  - `cp` 到 `$OUTPUTS_DIR` 给 metabot 桥自动发回飞书
  - 主 md 文档头部加一行「可视化版本」链接指向 HTML
- **沉淀**:
  - **技术方案文档「双版本」模式**:结构化文本(md,团队协作源)+ 可视化 HTML(决策沟通/分享)。md 永远是源,HTML 是派生产物,不要在 HTML 里做实质编辑
  - **Mermaid classDef 着色**:同一份 flowchart 用 classDef 按角色分色比按流程步骤分色更有意义(数据流里区分「谁的东西」比区分「第几步」更帮助读者)。示例:粉=用户视角、蓝=通信层、紫=服务器、黄=处理器、绿=数据结果
  - **粒子动效落地要点**:①用 `devicePixelRatio` 采样避免高分屏模糊;②粒子间连线加距离衰减 alpha 才优雅;③鼠标斥力 + 阻尼 + 保底速度避免粒子聚集或全停;④浅底色下用彩色粒子(紫粉蓝青)+ shadowBlur 才亮而不刺

### 14:20 — 商汤美颜实现指南(v2 聚焦版)+ SDK 抽 @heyhru/web-plugin-sensetime-beauty

- **做了什么**:按用户要求把方案 HTML 收窄成实现指南 v2 —— 砍掉方案对比 / 上下文 / 工作量 / 开放问题,只留 5 章:能力范围 · 上行接入 · 下行接入 · Package 设计 · Package 实现 + 上层调用示例。产物落在 [`_attachments/shangtang-beauty-implementation-guide.html`](../_attachments/shangtang-beauty-implementation-guide.html) 并同步 metabot outputs
- **为什么**:v1 HTML 有太多"为什么这么选"的讨论,用户明确只要"正确方案的实现"。同时把商汤 SDK 抽成独立 package 让上层 app 只依赖标准接口,不 import TRTC / 不打包 SDK 资源
- **结果**:
  - 能力范围明确圈定:只启用商汤 `effects.baseSkinCare`(磨皮/美白/红润 3 项)+ `effects.microPlastic`(瘦脸/大眼/下巴/额头/鼻梁/嘴形 6 项),其余(滤镜/贴纸/美妆/染发/美甲/试戴)一律不加载 → SDK 只加载 effects 单包 ≈ 3-5 MB
  - Package 命名:**`@heyhru/web-plugin-sensetime-beauty`**(遵循 sitin-next `web-plugin-*` = 前端第三方库封装规范,参考 `web-plugin-codemirror`)
  - Package 边界:只接受/返回标准 W3C 类型(`MediaStream` / `MediaStreamTrack` / `HTMLCanvasElement`),**不 import TRTC**,把 TRTC 集成留给调用方 → 未来其他 app(非 TRTC)也能用
  - SDK 静态资源不打包:package 只暴露 `sdkBaseUrl` / `licenseUrl` 参数,调用方自己 host(便于测试/生产走各自 CDN,license 也不跟包分发)
  - 公共 API:`createBeauty(options)` → `process(stream)` / `processRemote({userId, track})` / `setParams(params, channel?)` / `stopRemote(userId)` / `dispose()`,`channel` 支持 `'local' | { userId }` 分通道调参
- **关键 diff / 命令**:
  - `Write` `_attachments/shangtang-beauty-implementation-guide.html`(v2,58KB)
  - `cp` 到 `$OUTPUTS_DIR` 让 metabot 桥发回飞书
- **沉淀**:
  - **sitin-next 里第三方 SDK 封装归 `web-plugin-*`**,包名 `@heyhru/web-plugin-<vendor>-<capability>`,不用 `web-ext-*`(那个留给需要账号/token 的外部服务集成,如 sentry)
  - **SDK 静态资源不进 npm 包**:让调用方 host(`public/sdk/`),package 只暴露 URL 参数。避免包体积、license 分发、CDN 灵活性三个问题
  - **package 层不 import 上下游 SDK**:输入/输出用标准 W3C Media API 类型,把 TRTC 集成留给调用方。这样 package 可以复用给非 TRTC 场景(WebRTC/getDisplayMedia/文件回放),也便于单测
  - **只启用两项能力的三重收益**:①包体积小 3-5×;②精度集中在关键点密度足够的效果上(避开滤镜/贴纸对 480p 精度的敏感度);③参数面板简单,PM 好讲、用户好用

