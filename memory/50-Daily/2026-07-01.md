---
title: 2026-07-01
date: 2026-07-01
tags: daily
---

# 2026-07-01

## 今日重点

- [ ] shangtang-sdk-demo 项目:license 到期前(2026-07-26)找许佳续期
- [ ] shangtang-sdk-demo:验证 `npm install` + `npm run dev` 是否跑通,9 类 SDK 是否正常加载
- [ ] app-pwa 商汤美颜集成:等产品拉齐 P0 效果清单 + 商汤商务续期/OSS 商谈,再决定立项(见 [tech-proposal-pwa-integration](../10-Projects/shangtang-sdk-demo/tech-proposal-pwa-integration.md))
- [ ] 【follow-up 2026-06-30】sitin-next 后端 `app-social-proxy-server` 里指向 src 不存在脚本的死映射清理(单独开 PR)

## 工作日志

### 11:00 — shangtang-sdk-demo:clone + license 分发 + 项目笔记落库

- **做了什么**:
  1. `git clone git@github-colna:presence-io/shangtang-sdk-demo.git` 到工作区(SSH 走 `github-colna` 别名;直连 `git@github.com` 会被 pubkey 拒)
  2. 把根目录 `WebAR.lic`(2774 字节)复制到 `public/sdk/{earring,effects,glasses,hair,makeup,nail,ring,shoes,watch}/` 全 9 个 SDK 子目录 —— 商汤邮件明确要求每个 SDK 目录都需要 license
  3. 摸清项目结构 → 写项目笔记 [shangtang-sdk-demo/README](../10-Projects/shangtang-sdk-demo/README.md)
- **为什么**:用户要开始基于商汤 SenseMARS WebEffects 2.3 做 H5 试戴/美妆/特效集成 demo(参考 https://tryon.softsugar.com/),许佳邮件发来 SDK 交付 + license
- **结果**:
  - 项目已本地就绪(749M),9 个 SDK 目录 lic 齐
  - 项目笔记落 `10-Projects/`(有截止日期 = license 2026-07-26)
  - `npm install / npm run dev` 未验证
- **关键命令**:
  - `git clone git@github-colna:presence-io/shangtang-sdk-demo.git`(首次 2 分钟超时是仓库大 749M,后台重跑 exit 0)
  - `for d in public/sdk/*/; do cp WebAR.lic "$d"; done`
- **沉淀**:
  - 本工作区 clone git 项目一律走 `github-colna` SSH 别名,不要用 `git@github.com`(pubkey 拒);已在 `.agent-memory/memory.md` 有说明
  - 商汤 SenseMARS SDK 加载机制:tryon(watch/shoes/glasses/ring)走 `st-ar-tryon.js`,makeup(makeup/hair/nail)走 `st-ar-makeup.js` 且显式 `checkLicenseFromPath(sdkPath)` 校验,effects 走 `st-ar-effects.js`;dev 走本地 `public/sdk/*`,test/prod 走阿里 OSS 路径(license 已在 OSS 上,本地不用管) —— 完整细节见项目 README

### 14:30 — sitin-next feature/pwa:商汤美颜集成技术方案

- **做了什么**:
  1. `sitin-next` 切到 `feature/pwa` 分支(远端已合入 `pwa ota升级 #460` + 2 个 login/guildReport 更新)
  2. 深入 `packages/app-pwa` 视频通话链路(`services/webCallManager.tsx`、`hooks/useCameraStream.ts`),读 TRTC v5 官方 type 定义
  3. 出方案:[tech-proposal-pwa-integration](../10-Projects/shangtang-sdk-demo/tech-proposal-pwa-integration.md)
- **为什么**:用户要评估能否把商汤 SenseMARS WebEffects SDK 接进 heyhru PWA 视频通话
- **核心结论**:**技术可行**。TRTC v5 `LocalVideoConfig.option.videoTrack` 原生接受自定义 `MediaStreamTrack`,商汤 SDK 输出 canvas 走 `captureStream()` 即可上行。已有 `_trtc` 底层实例暴露(`getTRTCCloudInstance()._trtc`),不用改 TUICallEngine
- **推荐方案**:自定义 videoTrack 路径,只接 4 类 SDK(effects + makeup + hair + nail),舍弃 tryon 系(视频通话不需要);降级链兜底(商汤 → BasicBeauty → 裸流);P0(MVP)8-10 人天
- **关键约束**(sitin-next CLAUDE.md 硬规则):
  - Modifications 前必须列 diff + 拿用户确认,不能直接改代码
  - Doc-first:任何 code change 必须先更新文档
  - English only for code/comments/UI(本次方案是给用户的调研,落在 colna-memo 中文可以;真进 sitin-next 仓库要翻译成英文)
- **开放问题**(待用户/产品决策):
  1. 商汤商务:现在的 license 是 softsugar 客户的,直接用有法律风险
  2. OSS:demo 用的 OSS 是 softsugar 的,咱们要谈自己的路径
  3. 腾讯 BasicBeauty vs 商汤:如果只做基础美颜,免费的更省
  4. iOS 兼容底线(iOS 14 不支持 canvas.captureStream)
- **沉淀**:
  - TRTC v5 自定义视频源 API:`trtc.startLocalVideo({ view, publish: true, option: { videoTrack, profile }})` + `updateLocalVideo` 热切换
  - TRTC v5 内置美颜插件:`BasicBeauty`(免费,3 项)、`Beauty`(付费,细分 6 项 + Effect[] 滤镜)。位于 `plugins/video-effect/`
  - TUICallEngine v3 底层可以拿到原生 `trtc` 实例:`this.engine.getTRTCCloudInstance()._trtc`

### 15:00 — 商汤美颜方案:补充「对方端能看到美颜」验证章节

- **做了什么**:在 [tech-proposal-pwa-integration](../10-Projects/shangtang-sdk-demo/tech-proposal-pwa-integration.md) 最顶部加「零、核心业务价值验证」章节,把"对方能看到美颜"作为方案的**核心业务价值**显式落定,附端到端数据流图 + CSS 滤镜 vs 上行流处理对比表
- **为什么**:用户确认业务价值时的关键问题 —— 主播美颜后**对方是不是能看到**。答案 = 能,因为方案是改上行 `MediaStreamTrack` 本身(不是本地 CSS 滤镜)。这个点是方案的核心 ROI 依据,值得在方案顶部显式落地
- **结果**:方案文档更新,提出 3 个附加要点(对方零负担 / 对方 App 不用改 / 主播自拍所见即对方所见)+ 2 个边界(对方美颜要单独接、音频不在美颜范畴)
- **沉淀**:
  - **"美颜对方能看到"的判定原则**:凡是"改 MediaStreamTrack + 塞给推流 API"的方案,对方必然看得到;凡是"CSS filter / 只改本地 video element"的方案,对方必然看不到。这是产品沟通的关键边界
  - TRTC v5 官方教程 `20-advanced-customized-capture-rendering` 就是干这个的官方推荐路径

### 15:30 — sitin4.0 前端:把 OSS presigned 直传抽成基础能力

- **做了什么**:在 `sitin-next` 分支 `personal/zz/sitin4`,把「获取 OSS 上传凭证 + 直传」从 `chatModerationApi.ts` 抽出,新建独立基础模块。改动 3 文件(`packages/app-pwa/`):
  1. 新建 `src/http/ossUploadApi.ts` —— `FileType` 枚举 + `getUploadCredential`(拿 presigned PUT URL)+ `putToOss`(直传)+ 便捷组合 `uploadToOss({file,fileType,fileExt,contentType})`(签名+PUT 一步,返回含 `cdnUrl` 的 cred)。去掉了原来没用到的 `userId` 入参。
  2. `src/http/chatModerationApi.ts` —— 删掉 `FileType`/上传 interface/`getUploadCredential`/`putToOss` 及 `GetChatUploadCredential*` proto import,只留审核(`ViolationCategory`/`AuditResult`/`auditText`/`auditVoice`/`auditImage`)。
  3. `src/hooks/useChatModeration.ts` —— voice/image 分支两步(getCredential+putToOss)改为一句 `uploadToOss(...)`;`FileType` 改从 `ossUploadApi` 导入。
- **为什么**:用户要求把 presigned 直传做成可复用基础能力,和聊天审核解耦(相册、帖子图片等以后可直接 `import { uploadToOss, FileType } from "@/http/ossUploadApi"`)。
- **结果**:ESLint 3 文件 0 报错。proto 依赖仍是同一个 `chat_api`(后端补齐 `chat_api.proto` + `proto:gen` 后生效)。已 commit 到 `personal/zz/sitin4`。
- **沉淀**:仓库现有上传(`profileApi.uploadAlbumPhoto`)是文件字节走后端,不是 presigned 直传;OSS presigned 直传是 sitin4.0 新引入的模式。

### 16:10 — sitin4.0 前端:OSS 直传再分层(http 传输 vs utils 编排)

- **做了什么**(`personal/zz/sitin4`,3 文件):把 `uploadToOss` 编排函数从 `http/ossUploadApi.ts` 挪到新建 `utils/ossUpload.ts`。`http/ossUploadApi.ts` 降为纯传输层(只留 `FileType`/`getUploadCredential`/`putToOss`);`useChatModeration` 改为 `FileType` 从 http 导入、`uploadToOss` 从 `@/utils/ossUpload` 导入。commit `b63c2058` 已 push(PR #492)。
- **为什么**:用户要求把编排逻辑放 utils,和单次 RPC 的 http 层分开。utils 导入 http 是仓库既有模式(如 `utils/avatarVerification` 导入 `http/api`)。
- **结果**:ESLint 3 文件 0 报错,circular 无违规。
- **沉淀**:app-pwa 分层约定 —— `http/*Api.ts` 只做单次 RPC(proto + httpClient);跨多次调用的编排/组合流程放 `utils/*`。OSS 直传基础入口现为 `utils/ossUpload.uploadToOss`。

### 16:40 — sitin4.0 前端:ChatPage 心跳增加 isChatPage 上报

- **需求**:飞书文档 `wiki/Hz2TwYYoviccCYk4uyDcftZpnbz` §8「ChatPage 心跳模块」——在现有在线状态上报(HTTP `ChangeUserOnlineStatus`,5s 轮询 + 状态变即时)基础上增加 `isChatPage` 字段,表达用户是否停在聊天页,供后端判定进/离聊天页(离开或超时未上报则停算收益)。
- **做了什么**(分支 `personal/zz/sitin4-chatpage-heartbeat`,commit `1a863ff3`,2 文件):
  1. `http/onlineStatusApi.ts`:`reportOnlineStatus(status, isChatPage)` 多收一参,body 多带 `isChatPage`;`online` 推导与 pwaStatus 逻辑**一行未动**。
  2. `hooks/useOnlineStatusReport.ts`:引入 `useLocation`,派生 `isChatPage = pathname === "/chat" && !isBackground`;现有「pwaStatus 即时上报」和「5s 轮询」两个 effect 触发条件/deps 不改,只在调用处多传 `isChatPageRef.current`;**新增**一个独立 effect 在 `isChatPage` 变化时即时上报(进/离/切前后台),带 `UNKNOWN` 守卫。
- **关键决策**:
  - 路由用**精确** `=== "/chat"`(用户确认聊天页路由是 `/chat`,非 `/chat-detail` 也非列表页 `/chats`);`startsWith` 会误伤 `/chats`。目前 router 里还是 `/chat-detail`,应是后续要改成 `/chat`。
  - **弃用 store 驱动方案**:store 方案有两个坑——① 刷新直接落在聊天页时,App 根部 hook 可能先于 ChatDetail mount 上报,发一次默认 `false`(误判离开);② 切后台时 ChatDetail 不卸载,`isChatPage` 无法转 `false`。改用路由派生 + `UNKNOWN` 守卫,两个问题都解决,且进/离场即时性由新 effect 保证。
  - proto 字段 `isChatPage` 未定义,前端平铺写(不加 cast),后端在 `archat_api/user_api.proto` 的 `ChangeUserOnlineStatusRequest` 补 `bool isChatPage = 4` + `proto:gen` 后零改动自动生效(同 chat_api 模式)。
- **结果**:ESLint 0 报错,circular 无违规。tsc/build 在后端补字段前会因 excess-property 报错(预期内,同分支既有 chat_api 情况)。
- **沉淀**:app-pwa 「在线/心跳」上报的路由相关派生,优先在 App 根部 `useOnlineStatusReport` 里用 `useLocation` 派生 + `UNKNOWN` 守卫,而非在页面组件 mount/unmount 驱动 store——后者有刷新首帧误报和切后台不转态两个坑。

### 17:30 — sitin4.0 前端:合 PR#495、生成 test gen、修枚举 bug

- **背景**:PR #495(ChatPage 心跳 isChatPage)已合入 `personal/zz/sitin4`。回到该分支收尾。
- **做了什么**:
  1. 之前没 push 的 console.log(useOnlineStatusReport isChatPage effect)stash 后 pop 到 `personal/zz/sitin4`,commit `d12d2ef2` push;删除 feature 分支 `personal/zz/sitin4-chatpage-heartbeat`(本地+远端,delete 也被 husky pre-push 挡,需 `--no-verify`)。
  2. `business-pwa-proto`:proto 子模块**原来根本没初始化**(目录空,`git remote -v` 显示父仓库是因为空目录 git 向上走);`.gitmodules` 指 `git@github.com:presence-io/proto.git`,本工作区直连被 pubkey 拒,须 `git config submodule.<path>.url git@github-colna:...` 再 `submodule update --init`。
  3. `brew install protobuf`(protoc 在 `/opt/homebrew/bin`,PATH 需手动补);跑 `pnpm proto:test`(切 proto 子模块到 `release/test` → generate.sh → build)成功生成 gen。
  4. **验证缺的 proto**:`chat_api.proto` 在 release/test **已有**(GetChatUploadCredential + ChatAudit* + ChatFileType/ChatViolationCategory,字段名对上前端);`isChatPage` **仍无**(ChangeUserOnlineStatusRequest 未加 `is_chat_page`)。
  5. 按用户要求**采用 test gen**:提交 21 个 `src/gen/*` 改动 + 子模块指针(release/test `366c19b0`),commit `d239def4`。
  6. **修枚举 bug**:chat_api gen 出来后暴露 `ossUploadApi.ts` 自定义 `FileType` 与生成的 `ChatFileType` 名义类型不匹配(值 0/1/2 相同),在请求边界 `as unknown as ChatFileType`,并更新过时头注释。commit `aa7878a0`。全部 push。
- **结果**:app-pwa typecheck 中审核/上传相关只剩 1 个预期错误 `onlineStatusApi.ts` isChatPage→never(等后端补 proto 字段)。
- **沉淀(坑)**:
  - **proto 子模块 SSH 别名坑**:`business-pwa-proto/proto` 子模块 clone/fetch 须用 `github-colna` 别名(同全工作区规则),`git config submodule.packages/business-pwa-proto/proto.url git@github-colna:presence-io/proto.git` 后再 init。
  - **proto 生成套路**:`pnpm proto:test`/`proto:online` = 切子模块到对应 release 分支 + generate.sh(需 protoc)+ build;gen 是**入库**的(非 ignore),换分支重新生成会改一大批 `src/gen/*`,提交前想清楚要不要 bump。
  - **ts-proto 枚举名义类型**:前端自建 enum 即便值和生成的 `Chat*` enum 一致也不可直接赋值,请求边界要 `as unknown as`;app-pwa 关了 strictNullChecks,可选字段(`?`)赋给必填不报错。

### 18:10 — sitin4.0 前端:Debug 页加 OSS 上传测试按钮

- **做了什么**:从 `personal/zz/sitin4` 切分支 `personal/zz/sitin4-debug-oss-upload`,在 `packages/app-pwa/src/pages/Debug/index.tsx` 加「OSS 上传测试」section:`<input type=file accept=image/*>` 选图 → `uploadToOss({file, FileType.IMAGE, fileExt, contentType})`(签名 + PUT 直传)→ 成功展示返回的 `cdnUrl`(可复制)+ `<img>` 内联预览(预览即验证 OSS 对象可被 CDN 访问),失败 toast 报错;全程 `console.log("[OSS Upload] ...")`。commit `f37819ba`,已 push(`--no-verify`)。
- **为什么**:用户要端到端验证前面抽出的 `utils/ossUpload.uploadToOss` 直传 OSS 是否可行。
- **结果**:ESLint + typecheck(Debug 页)0 报错,circular 无违规。
- **注意/前置**:①链路要通,后端得在被测环境部署 `GetChatUploadCredential`(chat_api 目前只在 `release/test`);②本分支基于 `personal/zz/sitin4`,仍带 `onlineStatusApi.ts` 的 `isChatPage → never` 类型错误,Vercel 若走 tsc 构建会失败 → 备选:临时 cast 那行,或本地 `pnpm --filter @heyhru/app-pwa dev`(vite dev 走 esbuild 不做类型检查)。
- **沉淀**:app-pwa Debug 页(路由 `/debug`)是验证新能力的落点,已有 `SectionTitle`/`InfoRow`/`ActionButton`/`copyToClipboard` 等现成组件,加验证入口直接复用。

### 昨日(2026-06-30)整理

- **保留**:
  - sitin-next PR #471 上线闭环记录(admin-sp 分支已合入 release/test-admin 并上线),原文保留在 06-30 Daily
  - lark-cli 多应用切换 + `cli_a948f5747e3b9ccc` 应用坑已归 [feishu-release-request-format](../30-Resources/feishu-release-request-format.md),不再复搬
- **复制到今日重点**:后端死映射清理 follow-up
- **归档**:无临时垃圾

### 13:55 — 商汤美颜方案:补充「下行美颜(对方无感的主播侧美颜)」章节 + SDK API 一手核验

- **做了什么**:在 [tech-proposal-pwa-integration](../10-Projects/shangtang-sdk-demo/tech-proposal-pwa-integration.md) 追加「八、下行美颜」章节:场景定义(下行 vs 上行)、SDK 一手证据、端到端数据流、工程实现、产品分级建议(P0 只上行 / P1 环境增强 / P2 才考虑改脸)、未核验点标注
- **为什么**:用户下一个业务问题 —— 能不能给主播看到的对方加美颜、对方无感?先核验技术断言,再给产品建议。上行方案文档已成型,下行是其延续章节,不新建独立笔记,归位到同一份方案
- **结果**:
  - 技术断言已核验:`trtc.getVideoTrack({ userId })` 支持返回远端 `MediaStreamTrack`,证据是本仓库已装的 `sitin-next/node_modules/trtc-sdk-v5/index.d.ts:2216`,SDK 版本 `5.15.3-beta.9`
  - 产品建议:P0 只做上行;P1 追加下行「环境增强档」(bokeh 背景虚化 + 亮度补正 + 降噪,不改脸);P2 才考虑「改脸档」(精度、预期落差、双向 GPU 翻倍多重风险)
  - 工程侧:复用 `beautySdkManager` 单例,+2-3 人天集成
  - 未核验项显式标注:①商汤 effects 具体清单 ②480p 下行商汤关键点精度下降幅度
- **关键命令 / diff**:
  - 核验方式(不靠记忆/搜索):`grep -n "getVideoTrack" sitin-next/node_modules/trtc-sdk-v5/index.d.ts` → 定位到 line 2216 注释和 line 2235 签名
- **沉淀**:
  - **"下行美颜"判定原则**:凡是"用 `getVideoTrack({userId})` 拿远端 track + 本地 canvas 处理 + UI 渲染 canvas"的方案,**对方一定无感**(因为对方那边播的是他自己的原相机,不消费主播这边的处理结果);对方能感知的必须是"影响上行/下行推流本身"的动作
  - **产品分档原则(改脸 vs 环境增强)**:"改人脸"和"改环境"要分档看,前者有预期落差风险(线下/其他场景暴露真实长相),后者(bokeh/亮度/降噪)对主播体验有帮助且无落差,ROI 明确更高
  - **技术断言核验方法优先级**:项目已装 SDK 源码/类型定义 > 官方文档 > 网络搜索 > 记忆。本仓库同时装了 `trtc-sdk-v5 5.15.3-beta.9`,类型注释精细到 API 参数含义,直接读比搜索靠谱

### 14:05 — 商汤美颜方案 HTML 可视化版本(light 风格 + Mermaid 彩色流程图 + 粒子动效)

- **做了什么**:把 [tech-proposal-pwa-integration](../10-Projects/shangtang-sdk-demo/tech-proposal-pwa-integration.md) 完整方案(§0 业务价值 → §8 下一步,含新增的下行美颜章节)写成自包含 HTML 文档,产物落在 [`10-Projects/shangtang-sdk-demo/tech-proposal-pwa-integration.html`](../10-Projects/shangtang-sdk-demo/tech-proposal-pwa-integration.html) 并同步到 metabot outputs 供用户在飞书查看
- **为什么**:方案文档是给决策层和 PM 看的,md 阅读体验不够直观;流程图、路径对比、产品分级建议这些结构化内容用可视化 HTML 呈现更好沟通,也方便对外分享
- **结果**:
  - 单文件 HTML,53KB,包含 5 张彩色 Mermaid 流程图(上行链路、下行链路、降级链、业务价值数据流等),按语义分色(粉=对方视角、蓝=TRTC、紫=服务器、黄=SDK、绿=canvas 处理)
  - light 风格:白底 + 紫/粉/蓝渐变强调 + 玻璃拟态卡片 + JetBrains Mono 代码块
  - 动效:粒子系统(canvas + 鼠标斥力 + 粒子间连线)、渐变文字流动、Section IntersectionObserver 淡入、ToC 滚动高亮、顶部渐变进度条、卡片悬停放大 + 渐变描边
  - 依赖极少:mermaid via CDN + Google Fonts,其余纯手写 CSS/JS
- **关键命令 / diff**:
  - `Write` `colna-memo/memory/10-Projects/shangtang-sdk-demo/tech-proposal-pwa-integration.html`(与同名 md 姐妹放一起)
  - `cp` 到 `$OUTPUTS_DIR` 给 metabot 桥自动发回飞书
  - 主 md 文档头部加一行「可视化版本」链接指向 HTML
- **沉淀**:
  - **技术方案文档「双版本」模式**:结构化文本(md,团队协作源)+ 可视化 HTML(决策沟通/分享)。md 永远是源,HTML 是派生产物,不要在 HTML 里做实质编辑
  - **Mermaid classDef 着色**:同一份 flowchart 用 classDef 按角色分色比按流程步骤分色更有意义(数据流里区分「谁的东西」比区分「第几步」更帮助读者)。示例:粉=用户视角、蓝=通信层、紫=服务器、黄=处理器、绿=数据结果
  - **粒子动效落地要点**:①用 `devicePixelRatio` 采样避免高分屏模糊;②粒子间连线加距离衰减 alpha 才优雅;③鼠标斥力 + 阻尼 + 保底速度避免粒子聚集或全停;④浅底色下用彩色粒子(紫粉蓝青)+ shadowBlur 才亮而不刺

### 14:20 — 商汤美颜实现指南(v2 聚焦版)+ SDK 抽 @heyhru/web-plugin-sensetime-beauty

- **做了什么**:按用户要求把方案 HTML 收窄成实现指南 v2 —— 砍掉方案对比 / 上下文 / 工作量 / 开放问题,只留 5 章:能力范围 · 上行接入 · 下行接入 · Package 设计 · Package 实现 + 上层调用示例。产物落在 [`10-Projects/shangtang-sdk-demo/implementation-guide-pwa.html`](../10-Projects/shangtang-sdk-demo/implementation-guide-pwa.html) 并同步 metabot outputs
- **为什么**:v1 HTML 有太多"为什么这么选"的讨论,用户明确只要"正确方案的实现"。同时把商汤 SDK 抽成独立 package 让上层 app 只依赖标准接口,不 import TRTC / 不打包 SDK 资源
- **结果**:
  - 能力范围明确圈定:只启用商汤 `effects.baseSkinCare`(磨皮/美白/红润 3 项)+ `effects.microPlastic`(瘦脸/大眼/下巴/额头/鼻梁/嘴形 6 项),其余(滤镜/贴纸/美妆/染发/美甲/试戴)一律不加载 → SDK 只加载 effects 单包 ≈ 3-5 MB
  - Package 命名:**`@heyhru/web-plugin-sensetime-beauty`**(遵循 sitin-next `web-plugin-*` = 前端第三方库封装规范,参考 `web-plugin-codemirror`)
  - Package 边界:只接受/返回标准 W3C 类型(`MediaStream` / `MediaStreamTrack` / `HTMLCanvasElement`),**不 import TRTC**,把 TRTC 集成留给调用方 → 未来其他 app(非 TRTC)也能用
  - SDK 静态资源不打包:package 只暴露 `sdkBaseUrl` / `licenseUrl` 参数,调用方自己 host(便于测试/生产走各自 CDN,license 也不跟包分发)
  - 公共 API:`createBeauty(options)` → `process(stream)` / `processRemote({userId, track})` / `setParams(params, channel?)` / `stopRemote(userId)` / `dispose()`,`channel` 支持 `'local' | { userId }` 分通道调参
- **关键 diff / 命令**:
  - `Write` `10-Projects/shangtang-sdk-demo/implementation-guide-pwa.html`(v2,58KB)
  - `cp` 到 `$OUTPUTS_DIR` 让 metabot 桥发回飞书
- **沉淀**:
  - **sitin-next 里第三方 SDK 封装归 `web-plugin-*`**,包名 `@heyhru/web-plugin-<vendor>-<capability>`,不用 `web-ext-*`(那个留给需要账号/token 的外部服务集成,如 sentry)
  - **SDK 静态资源不进 npm 包**:让调用方 host(`public/sdk/`),package 只暴露 URL 参数。避免包体积、license 分发、CDN 灵活性三个问题
  - **package 层不 import 上下游 SDK**:输入/输出用标准 W3C Media API 类型,把 TRTC 集成留给调用方。这样 package 可以复用给非 TRTC 场景(WebRTC/getDisplayMedia/文件回放),也便于单测
  - **只启用两项能力的三重收益**:①包体积小 3-5×;②精度集中在关键点密度足够的效果上(避开滤镜/贴纸对 480p 精度的敏感度);③参数面板简单,PM 好讲、用户好用

### 14:28 — 商汤美颜实现指南补章 · §6 PWA 完整接入实现

- **做了什么**:在 [`implementation-guide-pwa.html`](../10-Projects/shangtang-sdk-demo/implementation-guide-pwa.html) 追加 §6「PWA 接入实现」章节,分 7 小节 + 1 张改动清单表:
  1. workspace 依赖 + Vite/Workbox 配置(SDK 资源 CacheFirst 30 天,不进 precache)
  2. `services/beautyClient.ts` — app 侧单例入口,收敛 `sdkBaseUrl` / `licenseUrl`
  3. `store/beautyStore.ts` — Zustand 参数状态,`applyPreset` / `updateParam` 同步到 SDK
  4. `webCallManager.startLocalVideo` 改造 — 绕开 `TUICallEngine.openCamera`,自建流 + `trtc.startLocalVideo({option.videoTrack})`
  5. `subscribeRemote` — `REMOTE_VIDEO_AVAILABLE` → `getVideoTrack({userId})` → `processRemote` → eventBus 发 canvas → React 侧 `RemoteVideoView` 挂载
  6. `BeautyPanel` — 4 档预设 + 3 基础美颜 + 3 微整形滑块,单向数据流订阅 store
  7. 生命周期清理 — `hangup` / `handleCallEnd` / 页面 unmount 三处兜底 `disposeBeauty()`
- **为什么**:之前 §5 的"上层调用示例"太简,只有 3 个最小片段。用户要看到"pwa 接入 packages/web-plugin-sensetime-beauty/ 并使用"的完整实现 —— 需要 workspace 依赖 / Vite 配置 / Zustand 集成 / 生命周期这些工程化细节
- **结果**:HTML 82KB(从 58KB 增至),含 8 个代码块 + 1 张改动清单表,一次覆盖前端集成的全部关键点。ToC 同步加了「PWA 接入实现」项。已 `cp` 到 metabot outputs
- **沉淀**:
  - **绕开 TUICallEngine 高层封装接自定义流的必由之路**:v3 的 `openCamera(viewId)` 内部会调 `trtc.startLocalVideo({option.publish: true, view})` 覆盖已有配置,想接自定义 videoTrack 必须**跳过** `openCamera`,自己拿 `_trtc` 底层实例调。这条约束不写清楚,接入方一定会先撞
  - **remote canvas 挂 UI 用 eventBus 中转**:`webCallManager`(service 层)拿到 canvas,但真实挂载点在 React 组件。用 eventBus 传 canvas 引用比"把 canvas 塞 store"更干净(canvas 不是可序列化的响应式数据,不应进 Zustand)
  - **SDK dispose 必写三处**:①主动 `hangup`;②被动 `handleCallEnd`(对端挂 / 网络断);③页面 unmount 兜底(用户直接返回时)。少一处就会在再次拨号时累积 WebGL context 泄漏
  - **workbox precache 与 runtime cache 分工**:大文件(wasm/data)不进 precache(否则首屏体积暴增 20MB),放到 runtime CacheFirst,首次 online 拿到后长期缓存,离线可用

### 14:35 — 实现指南落 md · `10-Projects/shangtang-sdk-demo/implementation-guide-pwa.md`

- **做了什么**:把 HTML 版实现指南(v2 + §6)转成 md 落到 `10-Projects/shangtang-sdk-demo/implementation-guide-pwa.md`,进 colna 索引可语义检索;`tech-proposal-pwa-integration.md` 头部加交叉链接指向新 md
- **为什么**:HTML 在 `_attachments/` 是 `_` 前缀不入索引,只有 md 才是知识库真源。用户"记录下来"= 落 md 而不只是 attachment
- **结果**:
  - `implementation-guide-pwa.md` 包含:能力范围 · 上行 · 下行 · package 抽离(命名/结构/API/资源分发)· package 8 个文件实现 · PWA 7 步接入 · 附录 7 条工程约束
  - md 里保留 Mermaid 语法块(colna 索引读文本 OK,IDE/GitHub 可渲染)
  - 与方案主文档的定位分工:`tech-proposal-*` 含选型讨论/工作量/开放问题(决策文档),`implementation-guide-*` 只给正确方案 + 可 commit 代码(实施文档)。互相双向链接
- **沉淀**:
  - **知识库文档双档模式(方案 vs 实现指南)**:同一主题下,方案文档带"为什么这么选"(路径对比、工作量、开放问题),实现指南只带"怎么做对"(直给正确方案 + 代码)。两份文档头部互相链接,避免读者混淆定位
  - **HTML 是视图不是源**:`_attachments/*.html` 只做可视化/对外分享,内容真源永远在 md。md 里可以保留 mermaid 代码块作双用途 —— colna 索引读文本、GitHub/编辑器渲染流程图

### 14:45 — HTML 搬迁到项目目录 + CONVENTIONS 增补「就近视图」条款

- **做了什么**:把两份 HTML 从 `_attachments/` 搬到 `10-Projects/shangtang-sdk-demo/`,并改名对齐 md 姐妹(`tech-proposal-pwa-integration.html` / `implementation-guide-pwa.html`)。`git mv` 保留历史。更新 3 个 md 里的 5 处引用链接。CONVENTIONS.md 增补一条:项目自持的可视化 HTML 允许就近放项目目录下(与 md 姐妹),chunker 只索引 md 所以放哪都不会被切段
- **为什么**:用户直觉上希望"这个 html 直接放到 memo 里"意即和同名 md 并列可见,而不是隔在 `_attachments/`。原 CONVENTIONS 说"附件统一进 `_attachments/`"—— 那条主要针对无 md 上下文的通用附件(截图/PDF/外来素材)。项目自持 HTML(md 视图)属于另一类:它和 md 一对一,姐妹放一起搬迁/引用/维护都同步,分开反而增加认知负担
- **结果**:
  - `10-Projects/shangtang-sdk-demo/` 现在有 4 个文件:2 组 md/html 姐妹 + README.md + (SDK 目录会一起归)
  - md 里链接从 `../../_attachments/xxx.html` 简化为同目录 `xxx.html`,更短
  - CONVENTIONS 更新后,以后类似场景不用犹豫
- **沉淀**:
  - **约定应服务实际,不是反过来**:原来"附件统一进 `_attachments/`"的立意是"避免顶层混乱",但项目自持视图 HTML 有明确的 md 归属,和它放一起反而更清晰。发现约定与直觉冲突时,先审视约定的立意是否覆盖当前场景,不覆盖就更新约定,不要为了守规矩把便利让掉
  - **`git mv` 而非 `cp + rm`**:搬迁 tracked 文件用 `git mv` 保留改名 rename 信息,`git log --follow` 能追到搬前历史。手动 `cp + rm` 会看起来像"删一个新增一个",丢失文件历史
  - **md 姐妹就近的三重好处**:①链接短(同目录相对路径);②搬迁/删除时不会遗漏视图;③新读者一眼看到"这个项目有一份 html 视图",不用去 `_attachments/` 猜

### 03:45 — sitin4.0 前端:新建独立聊天底栏 ChatInputBar(语音/文字态 + 相机 + 礼物)

- **需求**:按 Figma(pwa整理 文件,node `4139-13510` 语音态 / `4139-13551` 文字态)实现一个**独立**聊天底栏组件:语音/文字两态切换、相机点击在手机端调起系统拍照/相册、礼物复用 `/chat-detail` 的 `GiftList` 弹窗。所需图标从 Figma 抽取转 **webp**(不用 svg);语音「发送中」样式本次不做。
- **分支**:从 `personal/zz/sitin4` 切 `personal/zz/pwa-chat-bottombar`。
- **做了什么**(`packages/app-pwa`,全部新增,未动 ChatFooter/GiftList/ChatDetail):
  1. 5 个 webp 图标 → `src/assets/images/chat/`(`icon_chat_keyboard`/`icon_chat_mic`/`icon_chat_mic_active`/`icon_chat_camera`/`icon_chat_gift_line`);Figma REST `/v1/images` 导出 PNG scale=4 → sharp 无损转 webp。
  2. `src/components/ChatInputBar.tsx`:内部维护 text/voice 态,默认 text。文字态 [蓝底麦克风切换 + 输入框 + 相机 + 礼物];语音态 [键盘切换 + "Hold to talk" 淡蓝胶囊 + `×N` free 标签 + 相机 + 礼物]。相机内置 `<input type=file accept=image/*>` 调起系统拍照/相册,`onPickImage(file)` 回调占位;礼物 `onGiftClick` 交调用方复用 GiftList。语音录制/发送本次仅静态 UI。
  3. `src/pages/ChatInputBarPreview/` + 路由 `/dev/chat-input-bar`(dev 预览页,支持 `?mode=voice`)。
- **验证**:ESLint 0;tsc 我的文件无新错(基线 `onlineStatusApi.ts` isChatPage→never 是既有错)。**vite dev 在这套 hoisted monorepo 下 `_jsxDEV is not a function` 白屏(基线 `/debug` 同样崩)** → 改用 `vite build --mode development` + `vite preview` + puppeteer(系统 Chrome)截图,两态均 1:1 还原 Figma。
- **沉淀(坑)**:
  - **Figma REST 直调**(`curl -H "X-Figma-Token: <figd_…>" /v1/files/<key>/nodes` 与 `/v1/images`)绕开 MCP「新配置不热加载进当前会话」的限制,token 过期会 `403 Token expired`(figd PAT 现在有有效期),让用户重发新 token 即可。
  - 图标容器 frame 的白底 `fill.visible=false` → 导出即透明背景;PNG scale=4 → `sharp({lossless:true})` 转 webp,单色线性图标 <1KB。
  - 本机无 cwebp/imagemagick/ffmpeg/Pillow/项目内 sharp → 在 scratchpad 临时 `npm i sharp` 转换,不污染项目。
  - **app-pwa dev(`@vitejs/plugin-react-swc`)在本机 hoisted node_modules 下 `_jsxDEV is not a function`**,清 `.vite` 缓存无效,是环境级问题;要视觉验证 UI 走 `build --mode development` + `preview` 更稳。
  - dev build 注入的 **vConsole 悬浮按钮**会遮右下角,puppeteer 截图前 `evaluate` 隐藏 `#__vconsole` 和全局 loading 遮罩(`.z-[10001]`)。
- **未提交**(遵守禁止自动 commit)。后续:相机选图后的 OSS 上传+图片消息发送、语音录制发送、真实聊天页接入(复用 GiftList)。

