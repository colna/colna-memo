---
title: 2026-09-13
date: 2026-09-13
tags: daily
---

# 2026-09-13

## 今日重点

> 从 09-11 的 Daily 带过来（09-12 无记录）。

- [ ] Edit profile › Name 那屏（探头仓鼠）待提交 —— 规范模式不自动 commit，等用户给提交形状
- [ ] 其余编辑子页按新语言重做：`height`(HeightRuler) / `occupation` / `bio`(UnderlinedInput) / `interests`(chip 待定)；`age` 已迁完但未提交
- [ ] hub（mockup `77`）与 `photos`（mockup `82`）两屏
- [ ] 未决：photos 单图 vs 多图；interests 平铺 vs 分组；hub 那颗 Save 的逻辑；AI tip 留不留
- [ ] 收尾：`.gitignore` 已加 `.fastembed_cache/`（未提交）、根目录 `300`（蒲公英 cookie 垃圾）待删或忽略
- [ ] 最后 PR 回 `feature/luka-ios`（**开 PR / 合 PR / 删分支前都要先问**）

## 工作日志

## 工作日志 — 23:45 Edit profile › Name 探头仓鼠：换素材 + 按稿子修正尺寸（apps/luka，未提交）

用户点单：Name 屏的仓鼠换成「探头」那只（`hamster-peek.png`），对齐 mockup `78 · Edit name`（Figma `932:1038`）。

- **做了什么**
  - 新增素材 `apps/luka/src/assets/mascot/hamster-peek.png`（640² 正方形画布，**故意不裁透明边** —— 左边那条约 19% 的透明带就是「探头」的几何本身），`assets/README.md` 记一行。
  - `OnboardingScaffold` 加 opt-in 的 `mascotPeek`：命中就给 `marginLeft: -MASCOT_PEEK_OFFSET`，注册五步零影响（纯 props 分支）。
  - `MascotAside` 文档补一条：这版素材是唯一例外，落点和别的姿态不通用。
  - **尺寸修正**：`MASCOT_SIZE` 160 → **214**，`MASCOT_PEEK_OFFSET` 52 → **62**。
- **为什么改尺寸**：稿子那张是**扁平图片 mockup**（Figma 里 `children: []`、唯一 fill 是 `IMAGE`），量不到 fontSize/padding，只能按比例反推。选同一屏内可比的量 —— 仓鼠画到的高度 ÷ 屏幕高：稿 **152/619 = 24.6%**，原来的实现只有 **18.0%**（小三分之一）。改后 `214 × 0.983 / 874 = 24.0%` ✓。
  - 落点跟着重算：`24(px-6) + 0.189 × 214 − 2 = 62`（0.189 = 素材里那条身体竖边 121/640）。
- **结果 / 验证**：模拟器（iPhone 17 / iOS 26.5，深链 `luka://edit-profile/name`）截屏与稿子并排比对 —— 仓鼠占屏高度、探头裁切、台词两行、值 + 下划线 + 计数、Save 位置全部合上。
  - 门禁：`biome check` 干净、`pnpm luka:typecheck` = 0、`pnpm luka:test` **115 文件 / 635 通过**。
- **关键命令**

  ```sh
  /tmp/rowscan.sh /tmp/peek-214.png 0 850 640 800 '#FDF9EE' 6 'hamster'   # 按行投影量 bbox
  magick figma2/932-1038.png -crop 286x619+53+113 +repage -resize 480x cmp-mock.png
  magick peek-214.png -resize 480x cmp-render.png
  magick cmp-mock.png cmp-render.png +append cmp-214.png                  # 左稿右实现
  ```

- **沉淀**：已补进 [design-mock-measure-and-sample](../30-Resources/troubleshooting/design-mock-measure-and-sample.md) —— 「扁平 mockup 只按同一屏内的同类量比比例」+「换 `mascotSize` 必须重算探头落点」。
- **顺带**：`age.tsx` 上一轮已迁到新外壳（`hamster-pointing`，不探头），本轮未再动；`git status` 里它与 Name 一起等提交。
