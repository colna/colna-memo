---
title: 2026-07-29
date: 2026-07-29
tags: daily
---

# 2026-07-29

## 今日重点

- [ ] 探针自拍超时:后端补 `taskType==PROBE` 分支(清 probe、不暂停绿灯收益)+ `handleOutboundMessage` 识别 `replyKind:probe` 走 `handleProbeComplete("completed")`——需给对应 owner 出改动说明

## 工作日志 — 15:10 sitin-next 探针自拍「总是超时」根因定位 + 前端修复

- **做了什么**:排查 sitin-next【消息金】自拍探针「正常完成却总被后端记超时、且只给了文字消息奖励」。跨前后端(`sitin-server/chat-service-*` + `sitin-next` app-pwa)通读链路,拿到一份 dev vconsole 完整复现日志定位根因,并在 `feature/sitin4.0.1`(拉进 `sitin-next2`)修复前端。
- **为什么**:探针 7419 从没被清成 completed → 后端统计恒为超时;钱走了消息任务 7418(`reply_type=probe_image`)。
- **根因(前端)**:`PhotoTaskDrawer.done()` / `VoiceTaskDrawer.send()` 里 `onSent?.(...)` 之后**无条件同步调 `onClose()`**,而父层 `onClose=dismissPhoto=dismiss(report=true)` → 每次成功发送都**多打一发 `pwa_chat_task_abandoned` + `reportTimeout(PROBE, level=GREEN)`**。日志实锤:audit passed → `task_abandoned 7419` + `ReportTimeout{level:1,taskType:2}` → 之后自拍才真正上传发出。
- **根因(后端,未改·另一个仓)**:① `reportTimeout` grpc 完全忽略 `taskType`,对 PROBE 也 `setEarningsPaused`+发假 `TASK_TIMEOUT_YELLOW`,且不碰 probe 实体;② `handleOutboundMessage` 不读 `cloudCustomData.replyKind`,只清 pending 消息任务并付其奖励;③ `completeProbeDone` grpc 全工作区零调用方 → probe 永不 completed。
- **结果**:改 4 文件——`done()/send()` 改 `const ok = await onSent?.(...); if (ok !== false) onClose();`(useLockFn 防连点);`ProbeTaskDrawers` 加模块级 `sentProbes` 集,`handlePhotoSent/handleVoiceSent` 成功打标并 `return ok`、不再自己关抽屉,`dismiss` 的 report 块加 `!sentProbes.has(id)` 守卫使「已发送」的 onClose 静默关(不放弃/不上报);`index.tsx` 的 `onSendImage/onSendVoice` 补 `return ok`。语义:发送成功静默关、失败保持打开可重试。**未 commit/未 push**(待用户批准)。
- **关键命令 / diff**:`sitin-next2` 切 `feature/sitin4.0.1`(HEAD 22dd9b77);`pnpm install`(19s)→ `pnpm --filter "@heyhru/app-pwa^..." build` → `tsc --noEmit` **0 err**、`eslint` 改动文件 **0 err**。app-pwa 无测试设施(无 vitest/RTL),暂靠 dev 预览 + 分支自带 console.log 验证(改后成功发送不再打印 `report_abandoned`)。
- **沉淀**:根因+修法已单独写到 `30-Resources/troubleshooting/sitin-next-probe-selfie-timeout.md`;后端契约见既有 `sitin4.0-anomaly-probe-backend`。
- **PR**:分支 `personal/zz/pwa-probe-send-close-await`(base `feature/sitin4.0.1`),commit `6406f634`,**PR #758** https://github.com/presence-io/sitin-next/pull/758。pre-commit 全量 lint + circular 通过;pre-push 全量 test 被无关包 `app-minerva-server`(全新 clone 缺 `generated/prisma`)拖挂,经用户逐次授权 `--no-verify` 推送(符合工作区例外)。gh 身份已切 colna。

## 工作日志 — 17:1x sitin-next Partnership Ads 任务「显示 + 跳出」

- **做了什么**:按飞书 RFC《Partnership Ads 自动化投放 v1.0》PWA 侧方案,实现 task_id=2000 广告任务四步闭环的**前两步**(任务显示 + 点击跳 IG)。从 `feature/pwa` 切分支 `feature/pwa-ads`。
- **为什么**:后端把 2000 作为白名单可见 common task 下发,PWA 要渲染任务卡(Go)并点击经端能力跳 Instagram 共创审批页。
- **结果(6 文件)**:`task.ts` 加 `TaskId.PartnershipAds=2000`;`taskRegistry.ts` 登记(tracking `partnership_ads`/`rewardModal:true`);`bridge.ts` 新增端能力 `openDeepLink(deepLink,fallbackUrl)`(跳 `instagram://branded_content_ad`,invokeBridge 约定,无独立 callbackId);`useTask.handleTaskAction` 加 `case PartnershipAds`→openDeepLink,端不支持/H5 时 `window.open` 兜底;新建 `PartnershipAdsCard`(复用 CashoutTaskCard 的 Go 按钮,tasks 含 2000 且未完成才渲染)挂 `Home/index.tsx`。
- **关键点/坑**:①`getServerTasks` **不过滤 taskId**→后端白名单返回 2000 即进 tasks map、卡片自动出;②首页任务列表其实分「stage 固定(CashoutTaskList)/category 派生(DailyTaskList)/onboarding」几套,common task 不会自动进这些列表,所以**新建独立卡**最干净;③commitlint `subject-case` 禁大写起头,subject 用中文起头才过;④UI 文案用英文(app-pwa 面向英文用户)、注释中文,与现有惯例一致。
- **验证**:`pnpm --filter "@heyhru/app-pwa^..." build` 后 `tsc` 0 err;6 文件 eslint 0 err;pre-commit 全量 lint+circular 过。pre-push 全量 test/build 被**无关包** `business-minerva-users`(`users.service.ts:950` 既有 TS 错误)拖挂,授权 `--no-verify` 推。
- **范围/残留**:仅「显示+跳出」。RFC 三~五步(回前台 `checkPartnershipStatus`/Received 态/`finishUserCommonTask` 发奖)本次不做,已在 case 留挂接注释、`rewardModal:true` 就位。依赖:客户端提供 `openDeepLink` 端能力、后端对白名单下发 2000。
- **PR**:分支 `feature/pwa-ads`(base `feature/pwa`),commit `c32131d7`,**PR #760** https://github.com/presence-io/sitin-next/pull/760。
- **追加(commit `039c3aeb`)RFC 三~五步(跳回查询→Received→完成发奖)**:新增 `stores/partnershipAdsStore.ts`(phase/awaitingReturn/lastCheckAt,persist localStorage,抗套壳 WebView 重载)+ `hooks/usePartnershipAds.ts`(Go 置一次性 awaitingReturn 跳 IG;回前台消费一次 / Received 点击 → `finishTask(2000,{silentError:true})`,30s 节流;挂载补查覆盖跨页返回)+ 重写 `PartnershipAdsCard`(相位化按钮 Go/转圈/Received)。**关键判断**:后端章节里 `finishUserCommonTask{2000}` 已把「查 Meta 批准 + 完成发奖」合并(code 1 已批准发奖 / 1000 未批准 / 3 非法),所以**不引 section 6 那个「待定」的 `checkPartnershipStatus`**,直接用 `finishTask` 兼做查+完成(silentError 压掉 1000 的错误 toast)。状态两层:服务端 TaskStatus 权威(finish→卡自隐)、客户端 phase 仅 UI。取舍:可见性监听按首页作用域(该任务只在首页),未做全局挂载。tsc/eslint 0,pre-commit lint+circular 过,pre-push 同前无关包拖挂 `--no-verify`。
- **追加(commit `9ae8f9b2`)**:文案/金额定为「后端优先 + 前端兜底」。坑:`task.title` 来自 `getServerTasks` 的 `getTaskTitle(taskId, serverTask.title)`——2000 未登记时走 default `title || getTaskTrackingName()`,后端不给 title 会退成埋点 slug `partnership_ads`(丑)。修法:`getTaskTitle` 加 `case PartnershipAds: return title || "Approve the Instagram partnership"`;`PartnershipAdsCard` 金额 `Number(task.reward) || 10`、拼进标题显示 `{title} $X.00`。之前卡里写的 FALLBACK_TITLE 是死代码(getTaskTitle 已保证非空),已移除。

## 工作日志 — 18:2x Android 键盘上报补漏,修 PWA 输入栏「偶高偶低」

- **做了什么**:排查 PWA 聊天输入栏软键盘弹起时偶尔偏低(被盖)偶尔偏高(留缝)。定位到根因是 **Android 键盘高度上报不完整**:开合动画有 start/progress/end,但**键盘已弹出后联想条/工具栏那行出现/消失(无开合动画)改变 ime inset 时不上报**,前端停在旧高度。从 `GraceChat-Earn-Android` 的 `haven/release` 切分支 `p/zz/keyboard-ime-height` 修复。
- **为什么**:APK 里键盘是覆盖 WebView(原生不 resize、visualViewport 不动),前端唯一信号就是原生上报;上报缺了「非动画期的 ime 变化」这一类,前端 `keyboardLift.ts` 那套「历史最大值+15px gap」反推就必然偶高偶低。
- **结果(2 文件)**:`KeyboardHeightProvider.kt`——`KeyboardChangedListener` 加 `onKeyboardImeInsetChanged`、Provider 加 `notifyKeyboardImeInsetChanged`(刷 mRealKeyboardHeight);`TranslateDeferringInsetsAnimationCallback.onApplyWindowInsets` 在 `!animationRunning` 时按真实 ime inset 去重(`mLastReportedImeBottom`)补报一次。`PWAWebViewFragment.kt`——实现新回调,映射成非动画期 `progress` 上报。
- **效果/边界**:直接修好「被盖(偏低)」方向;「留缝(偏高)」还卡在**前端** `keyboardLift.ts` 的历史最大值缓存(键盘变矮时仍取旧大值不缩)——需配套把「历史最大值+固定 15px gap」去掉、直接采信原生真实高度,两端一起上才根治。前端那半在 sitin-next `feature/sitin4.0.1`,待做。
- **验证**:仅代码级审查,**未编译/未真机**(本地无 Android 构建环境),需客户端出包真机 QA(重点:带联想条输入法下打字时输入栏是否始终贴键盘上沿)。
- **PR**:分支 `p/zz/keyboard-ime-height`(base `haven/release`),commit `9b2ac1f2`,**PR #91** https://github.com/presence-io/GraceChat-Earn-Android/pull/91。gh/git 身份 colna。
- **沉淀**:APK 键盘抬升机制(覆盖式、visualViewport 不动、只能靠原生逐帧上报 → 前端 `--kb-height` + transform),以及本次「非动画期 ime 变化漏报」的坑,可整理进 troubleshooting(待补)。
- **配套前端 PR #773**(分支 `personal/zz/pwa-kb-lift-native-height`,base `feature/sitin4.0.1`,commit `ad69d688`)https://github.com/presence-io/sitin-next/pull/773 —— 与 Android PR #91 配对上线。`keyboardLift.ts`:phased 路径去掉「历史最大值兜底 `max(本次,lastKbFullHeight)`」(只增不减 → 键盘变矮不缩、留缝的根源)+ 去掉 end 差值门槛 `KB_END_CORRECTION_MIN`,直接采信原生上报真实高度;`KEYBOARD_TOP_GAP` 留作视觉微调(真机可调/改0);legacy(API30-)路径不动。tsc/eslint 0(注:切分支后 proto dist 陈旧会误报 chatApi.ts:269,`pnpm --filter "@heyhru/app-pwa^..." build` 重建即消)。**方案不"绝对准确"**:依赖原生 ime inset 可靠、有 ~1px 取整、悬浮/分屏键盘等边界不准、gap 需真机调——两端都要真机 QA。

## 工作日志 — 21:xx dev/photo-task 补 reportTimeout(PROBE) 日志

- **做了什么**:对比 `/dev/photo-task` 与 Chat 探真流程接口后,给 dev 页按 Chat 一模一样时机把 `reportTimeout(PROBE)` 打成日志(只日志不真调)。
- **对比结论(值得记)**:①**审核半段一样**——dev(real 模式)和 Chat 都用同一个 `useProbeTaskVerify`(`uploadToOss` + `auditImage/auditVoice`);②**发送 + 超时上报 dev 原来都没有**——dev 用裸 `PhotoTaskDrawer`、onSent/onClose 只打日志,Chat 用 `ProbeTaskDrawers` 才接 `sendImage/Voice`(TIM)和 `reportTimeout(PROBE)`。
- **改动**:`pages/dev/PhotoTaskPreview/index.tsx` 加 `sentRef`/`reportedRef` + `dismiss(report)` 复刻 ProbeTaskDrawers 规则——X关/折叠/倒计时=放弃→打 `[reportTimeout]`;成功发送(onSent 置 sentRef)→随后 onClose 静默不打;每次打开重置、每 probe 只打一次。voice-task 未做(用户只要 photo)。
- **PR**:分支 `personal/zz/pwa-dev-phototask-reporttimeout-log`,base `feature/sitin4.0.1`,commit `89bf9ee1`,**PR #777**。tsc/eslint 0;pre-push 无关包 `--no-verify`。

## PR #775 merge 记录(四要素)— 键盘输入栏

- **做了什么**:PWA 聊天输入栏键盘弹起被盖/露缝的最终修复,已 merge(merge commit `792e0277`)进 `feature/sitin4.0.1`。方案=保留历史最大值兜底 + 过抬偏置 + 白色裙边;终值 `KEYBOARD_TOP_GAP=0`(8px 白带、余量 0,即输入条自带 pb-2)。
- **遇到什么问题**:①中途误判方向(以为兜底是遮羞布,做了 #773 去兜底 + #91 Android 补报),真机日志推翻——原生 resume 欠报一行工具栏(351→320)、inset 落定不变,兜底恰是正确补偿;②白带宽度反复调(16→10→14→10→8px)才定。
- **怎么解决**:挂 `[kbLift]` 诊断日志抓真实 payload 定死机制;回退到 stock 兜底,改用「过抬+白色裙边」稳健兜底(不追像素精度);#773、#91 关闭。白带 = 过抬 + 输入条 8px pb-2,`KEYBOARD_TOP_GAP` 每 -1 = 白带/余量各 +1px。
- **残留**:`KEYBOARD_TOP_GAP=0` 余量 0、完全靠兜底估准,**真机 QA 若某机型偶发被盖需往负调**。
- **清理**:相关分支(overlift-skirt / debuglog / lift-native-height / Android keyboard-ime-height)已全删;#773、#91 已关。
- **沉淀**:可复用经验 + 元教训(重踩 §13 坑一、动 keyboardLift 前先查笔记)已整理进 `30-Resources/troubleshooting/mobile-keyboard-and-viewport.md` §14。**下次动键盘先读 §8/§12/§13/§14。**

## 键盘输入栏被盖/露缝 — 定论与最终方案(20:xx 收尾)

- **真机日志定论**(诊断分支 `personal/zz/pwa-kb-debuglog` + `[kbLift]` 日志):原生上报键盘高度**首开 351、切后台回前台后掉到 320**(差 31px=一行 IME 工具栏),且 `ime()` inset 落定后**不再变化**。→ 是**原生 resume 欠报**,不是键盘真变矮。
- **推翻我中途的误判**:stock 的「历史最大值兜底 `lastKbFullHeight`」正是对这个欠报的**正确补偿**(记住 351→一直 336);我先前 #773 去掉它反而是 resume 变矮的**元凶**;#91(补报 ime 变化)因 inset 卡死无变化可报、**对本 bug 无效**。→ **#773、#91 已关闭。**
- **最终方案 PR #775**(分支 `personal/zz/pwa-kb-overlift-skirt`,base `feature/sitin4.0.1`,commit `0fe64cb3`)https://github.com/presence-io/sitin-next/pull/775:基于 stock**保留兜底**,再加两招稳健兜底——①`keyboardLift` 过抬偏置 `KEYBOARD_TOP_GAP 15→-8`(负值=多抬,宁可高不被盖);②`ChatInputBar` 白色裙边(`absolute top-full`,高度绑 `var(--kb-height)`,收起为 0 不遮 TabBar),把过抬/欠报的缝用白色盖住。
- **坑/沉淀**:APK 覆盖式键盘 `visualViewport` 不动、只能靠原生上报,而原生 resume 欠报且 inset 卡死 → **追像素精度不可行,改用「偏高兜底+白色填缝」**。别再像我这次凭推断改方向,resume/时序类先挂 `[kbLift]` 日志抓真实 payload 再动。tsc/eslint 0;pre-push 无关包 `--no-verify`。**待真机 QA**(过抬量 `-8` 可调)。诊断日志分支不进任何 PR。

## PR #758 merge 记录(四要素)— 16:0x

- **做了什么**:修 app-pwa 自拍/语音探针「成功发送却误报放弃/超时」。`done()/send()` 改 `await onSent` 成功才 `onClose`;`ProbeTaskDrawers` 加 `sentProbes` 让已发送的 onClose 静默关。已 merge(merge commit `3c40e362`)进 `feature/sitin4.0.1`。
- **遇到什么问题**:①根因难定——跨前后端,靠一份 dev vconsole 复现日志才锁定是 `done()` 里 `onSent` 后无条件 `onClose(report=true)` 双触发;②`sitin-next2` 是全新 clone,tsc 报一堆 `Cannot find module '@heyhru/...'`(依赖没 build)、pre-push 全量 test 被无关包 minerva-server 缺 prisma 拖挂。
- **怎么解决**:①`pnpm --filter "@heyhru/app-pwa^..." build` 构建 workspace 依赖后 tsc 干净;②经用户授权 `--no-verify` 绕过无关包 test;③给测试出了 7 条验证用例(核心 2 + 回归 5),判定标准=成功发送前端无 `report_abandoned`/`onDismiss reportTimeout`、Loki 无 `ReportTimeout taskType:TASK_TYPE_PROBE`。
- **残留**:**后端(另一个仓 sitin-server)未修**——`reportTimeout` 仍忽略 `taskType`、`handleOutboundMessage` 不认 `replyKind:probe`、`completeProbeDone` 无调用方,所以后端侧「探针完成/超时」口径要等后端 PR 才彻底修好;前端本 PR 只消除「成功却误报」噪声。另有相邻风险(相机期间不暂停倒计时→长时间拍照会真超时),可另开分支加 pause/resume。
