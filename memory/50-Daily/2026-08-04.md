---
title: 2026-08-04
date: 2026-08-04
tags: [daily]
---

# 2026-08-04

## 工作日志 — sitin-next PR #845 去掉误带的 proto submodule 改动
- **背景**:PR #845(`personal/zz/ci-ai-review-stream` → main,colna 提,CI 流式 code-review)误把 `packages/business-pwa-proto/proto` submodule 指针从 `dad3cb70f` bump 到 `e95a3a0d9`,需去掉只留 yml 改动。
- **定位**:该 submodule(指向 presence-io/proto,branch release/online)误 bump 只在 tip commit `b40b3b741`「feat: ai code review」里;父 commit `c7dc5e089` proto 是对的。main 与 merge-base 均为 `dad3cb70f`。
- **改法**:本地工作树当时在另一分支 `ig-to-professional-account` 且有未提交的脏 submodule,故用独立 `git worktree` 操作,避免打扰。worktree 里 `git update-index --cacheinfo 160000,dad3cb70f...,packages/business-pwa-proto/proto` 还原指针 → `commit --amend --no-edit` 保留 yml → `push --force-with-lease=...:b40b3b741` 推回。
- **结果**:新 tip `3aa73d6bd`,PR 改动只剩 `.github/workflows/ai-code-review.yml`(40+/4-),`gh pr diff` proto 出现 0 次。worktree/临时分支已清理,原分支脏改动未动。
- **坑+经验**:①本地 `origin/<branch>` remote-tracking ref 可能滞后于 GitHub(该分支被 force-push 过),核对 PR diff 前先 `git fetch origin <branch>` 再 `git ls-tree`,否则会误判「没有 proto 改动」。②改 submodule 指针无需 checkout 子模块内容,`update-index --cacheinfo 160000,<sha>,<path>` 直接改索引即可。③脏工作树上动别的分支,优先 worktree 隔离。

## 工作日志 — 分析「无人脸日志」定位截图违规识别问题
- **任务**:客户端反馈 PWA 视频通话「截图违规识别→后端返回『无人』类型→弹窗」在无人时不触发,让我据一份端上日志(RTF,~900KB)判断是否属实。
- **做法**:`textutil` 把 RTF 转 txt(6499 行);grep 分离出两条检测链路;把日志里嵌的 base64 截图解码成 JPEG 查看内容。
- **结论**:属实,且问题在**后端截图检测**。同一时刻本地人脸检测 `SVFaceDetector` 判无人(positives=0/faceRate=0/faceLost×3),但截图违规识别(JS `useVideoScreenshotViolation`→图普/TUPU)对截图返回 `{code:1,isViolation:false,detectResult:""}`,没回「无人」→ 不弹窗。解出的截图是**对着天花板拍的**,确实无人。
- **根因**:截图违规识别走内容安全模型,只认色情/NSFW/涉政,「无人/空镜」对它是干净内容 → 天生识别不出「无人」。修法见 troubleshooting。
- **沉淀**:`30-Resources/troubleshooting/pwa-video-screenshot-violation-no-person.md`(含两条链路对照、根因、修法、base64 截断坑、待后端确认项)。
- **坑**:日志里 base64 标称 51248 实际只写进 ~32749 → 解出图底部约 30% 缺失;补 padding + `sips` 重编码可正常打开。图已发用户。

## 工作日志 — HH:xx 整理 memo(zhangzheng bot 会话)
- 本次会话(跨天)工作日志误全部追加到 `50-Daily/2026-08-03.md`(38 段);08-04 已有别的会话的日志,不动。历史归档保留,不迁移。
- **沉淀复用经验**到 `30-Resources/troubleshooting/ai-code-review-triage.md`:新增「CI 基础设施:中转 524 / streaming / reasoning_effort」+「proto submodule 合并/更新」两节(本次最有价值的坑:stream 和 reasoning_effort 必须一起改,gpt-5.6-sol 默认推理就重、只有 low 过)。
- **项目笔记** `10-Projects/sitin4.1-spv2-pwa.md`:补齐 08-03/04 全部推送记录(731bc2bfc…bfe22d104)+ 「CR 已修/待办」+ FE-2.2 卡后端现状。
- **新增 reference** `30-Resources/ai-agent-learning.md`:学做 agent 路线 + 李博杰《AI Agents in Depth》教材 + 三产品对比。


<!-- 以下 14 段为 zhangzheng bot 本次会话工作,原误记于 2026-08-03,08-04 整理时迁回 -->

## 工作日志 — 21:xx sitin4.1 新增 taskId 137 Snapchat 授权一次性任务

- **需求**:一次性任务增加 taskId 137「Authorize snapchat 💲0.5」,参考 IG 的 110(BindInsAccount)。
- **改了什么**(sitin-next2/packages/app-pwa):
  - `types/task.ts`:TaskId 枚举加 `BindSnapchatAccount = 137`。
  - `types/taskRegistry.ts`:加注册条目 `{ tracking: "snapchat_authorize", rewardModal: true }`——完成弹奖励弹窗;**故意不设 v4Onboarding 字段**,因用户约定「Welcome Checklist 不用动」,不把 137 注入 v4 欢迎清单。
  - `hooks/useTask.ts`:getTaskTitle 加 `case BindSnapchatAccount → "Authorize Snapchat"`;handleTaskAction 加 `case BindSnapchatAccount → showInsModal("media_task_from_tasklist", { platform: "snapchat" })`(点任务弹 Snapchat 登录弹窗)。
- **没动**:cashout 阶梯(`stageTaskIdsMap` StageTwo / `STATIC_TASK_MAP`)属金额结算结构,非「一次性任务」范围,未加 137(避免改动结算逻辑)。
- **决策/坑**:「参考 110」与「Welcome Checklist 不用动」冲突 → 取后者(更具体的硬约束),只镜像 110 的功能行为(登录弹窗 + rewardModal),不镜像 checklist 成员身份。奖励金额 0.5 由后端任务数据下发,不写在 registry。
- 验证:app-pwa `tsc -b` 干净、`eslint` 改动文件 0 error。commit subject 记得小写(commitlint)。

## 工作日志 — 22:xx sitin4.1 修复 Snapchat 授权后登录弹窗关不掉 + 137 任务不完成

- **现象**(用户抓的 vconsole 日志):snapchat 授权成功(`setPlatformAuth: snapchat true`)后,登录弹窗(showInsModal)不收回,且 137 任务不完成、$0.5 奖励不弹。
- **根因**:`showInsModal` 的「成功→完成→关闭」整条生命周期只绑定 legacy `insState`(按 FE-0.2 约定**只镜像 instagram**)。snapchat 授权只写 `authByPlatform.snapchat`,不碰 insState → `insState` effect 永不触发 → `handleComplete` 不跑 → `onClose()` 不调 + `finishTask(110)` 也不跑。日志无 `pwa_earning_ins_task_page_two_click`(step2)佐证。
- **修法**(`components/showInsModal.tsx`,tsc/eslint 全过):
  1. 授权态按平台取:`authed = platform==="instagram" ? insState : authByPlatform[platform]`。
  2. 用「弹窗存续期间 false→true」的 `justAuthed`(靠 `authedAtOpenRef`)驱动成功视图 + `handleComplete`,替代直接读 insState —— 防 snapchat(授权态持久、无兜底、不重置)已授权时重开弹窗立即自动完成+关闭的死循环。
  3. `handleComplete` 按平台选任务:snapchat→`BindSnapchatAccount(137)`,其余→`110`;`isTaskFinished/finishTask/奖励埋点` 都用它。137 已在 `rewardModalTaskIds` → 自动弹 $0.5 奖励弹窗。
  4. **重置分支**:IG 靠 `userInfo.insId` 兜底,完成后 `setInsState(false)` 让下次开弹窗回登录态;snapchat 的 `authByPlatform.snapchat` 是唯一授权信号、无兜底,**不重置**(否则被误判未授权)。
  5. 埋点 payload 全部加 `platform` 字段(事件名沿用 IG 历史口径,便于区分社媒);成功页 logo/文案改 `cfg.logo` / `cfg.successDesc`(平台化)。
- **坑/教训**:IG 完成后能安全 `setInsState(false)` 是因为真正的持久授权信号是 `userInfo.insId`(见 `tryStartInsRobot` 的 `!!userInfo?.insId || insState`);其他社媒没有这个分离,授权态与「弹窗成功触发器」是同一个字段,不能沿用 IG 的重置套路 → 改用 session 级 `justAuthed` 解耦。
- **follow-up**:成功页 showcase 图仍是 IG 的 `bgInsAuth`(无 SC 素材,暂留);埋点事件名仍是 IG 名(只加了 platform 字段,彻底重命名需与数据侧对齐);137 完成依赖后端返回该任务+奖励。

## 工作日志 — 22:xx sitin4.1 Chat 空状态放行:授权任一社媒即可进入

- **需求**:Chat「Earn to chat」拦截页只要授权了 IG 或 Snapchat 任一个就该放行(现只看 IG,授权 snapchat 仍被拦)。
- **根因**:`App.tsx` 计算 `authBlock` 只用 `userInfo.insId` + `isInsLoggedIn`(纯 IG 口径),不看 `authByPlatform`。
- **改法**(`App.tsx`):新增 `scAuthed = authByPlatform.snapchat === true`;判定改为 `igUsable(=hasBoundIG && isInsLoggedIn) || scAuthed → null(进聊天)`;都没连过→`ig-unbound`;连过但都不可用(如 IG 登录丢失)→`ig-login-lost`。**保留 IG 原口径不回归**,只额外放行非 IG 授权(这类平台暂无小崽概念,授权即可用)。
- **安全性核对**:`TabBar` 只把 authBlock 当真假用;`AIPersonaPlaceholder` 把 ig-unbound/ig-login-lost 渲染成同一 EarnToChatState → 收敛子状态无副作用。
- 验证 tsc/eslint 全过。

## 工作日志 — 22:xx sitin4.1 三组 snapchat 改动 push(personal/zz/sitin4.1)

- push 3 commit(身份 colna,pre-commit lint/circular + pre-push build 全过):
  - `5bf52bcb4` feat(app-pwa): add snapchat authorize one-time task (id 137)
  - `293ad1e98` fix(app-pwa): close & complete social login modal per platform
  - `8507a8f22` feat(app-pwa): allow chat access when any social authorized
- 均在 PR #824 分支上;涉及文件:task.ts / taskRegistry.ts / useTask.ts / showInsModal.tsx / App.tsx。

## 工作日志 — 23:xx sitin4.1 PR#824 合并 main(submodule 冲突处理)

- 需求:PR#824 分支 personal/zz/sitin4.1 合并最新 main(落后 72 提交)。
- **唯一冲突 = proto submodule** `business-pwa-proto/proto`:base 87aeb62 / ours 19259794(release/test 08-03,我为 cardType/platform_online regen 的)/ theirs dad3cb70(main 07-24)。
  - `git merge-base --is-ancestor` 验证:**ours 包含 main 的 proto**(超集,更新)→ submodule 保留 ours(工作区本就指在 19259794,`git add` 记录)。
- **src/gen 是生成代码**,3-way auto-merge 不可靠 → `git checkout HEAD -- src/gen` 整体取 ours(超集),再 `pnpm --filter business-pwa-proto build` 重建 dist。
- 验证:tsc app-pwa 干净、circular ✓、pre-push build 49 tasks ✓;三处 snapchat 改动全在。merge commit b9674ae22 已 push。
- **坑/教训**:合 main 遇 proto submodule 冲突时,先用 `git merge-base --is-ancestor A B` 判断谁是谁的祖先——谁包含对方就取谁;生成代码目录别信 auto-merge,直接 checkout 超集那侧再 rebuild dist。

## 工作日志 — 23:xx sitin-next2 AI Code Review 524 排查 + 改流式(PR #845)

- **现象**:AI Code Review workflow 切到 xingsuancode `gpt-5.6-sol`(#829)后,PR 审查报 524(`{"error":"openai_error","type":"bad_response_status_code"}`)。
- **排查**(用真实 PR diff 复刻同请求):
  - 524 是中转上游 **Cloudflare idle 超时,实测 ~125s**(非 100s);中转边缘是 nginx,只透传状态码。
  - `gpt-5.6-sol` 默认推理就重:`reasoning_effort` max/medium/**默认** 均 125.5s → 524;仅 `low` 通过(63s)。`gpt-5.4` 不带推理 30s 通过。
  - **流式**:`stream:true` 下推理阶段每 ~15s 持续下发 keep-alive 块(正文仍 0),不断重置 idle 计时器 → 活过 234s 不 524。**根治超时**。
- **改法**(`.github/workflows/ai-code-review.yml`,基于 main 单独开分支 `personal/zz/ci-ai-review-stream`):`stream:true` + SSE 逐块解析累积 `delta.content` + 心跳 console.log(进 Actions 日志)+ `reasoning_effort` max→low。YAML/`node --check` 均过。→ PR #845。
- **坑/教训**:① LLM 中转 524 = 上游 CDN idle 超时,流式是通用解(推理模型思考阶段也吐 keep-alive);② 推理模型「默认档」可能就很重,别以为不写 reasoning_effort 就轻;③ github-script 里 fetch 流式要 `response.body.getReader()` 手动解析 SSE,`response.json()` 只适用非流式;④ commitlint subject 不能大写开头(「AI …」被拒,改中文开头)。

## 工作日志 — 深夜 撤销 PR#824 误合的 main

- **问题**:之前按「pull main」把 origin/main 合进 PR#824 的 head,但 **PR base 是 feat/sitin4.1(非 main)**,main 超前 base 72 提交/152 文件全进了 diff → 文件从 44 炸到 194,AI review 只审首页 30 个全是 minerva 无关代码。
- **修复**:`git reset --hard 8507a8f22`(合并前)+ `git push --force-with-lease origin personal/zz/sitin4.1`,撤销 merge commit b9674ae22。核对 PR#824 → 44 文件,全 app-pwa/business-pwa-proto。
- **教训**:给 PR 合基线要合它的 **base 分支**,不是无脑合 main;base≠main 时先 `gh api .../pulls/<n> --jq .base.ref` 看清。已写进项目笔记。

## 工作日志 — 交换弹窗按 Figma 改多社媒「Social Requests」

- **需求**:按 Figma 7268:969 把交换请求弹窗(InsExchangeModal)从 IG-only 改成多社媒。
- **改了什么**(sitin-next2/packages/app-pwa):
  - `components/InsExchangeModal.tsx`:标题 Instagram Requests → **Social Requests**;新增 `RequestRowAvatar`——外圈按平台着色(IG 复用 bg_ins_border_circle.svg 渐变圈 / Snapchat #FFFC00 黄圈)+ 右下角平台 logo 角标(ic_logo_ins_rect / ic_logo_snapchat),平台用 `cardTypeToPlatform(msg.cardType)` 判定;行间加 divider(#EEEEF1);底部按钮统一「Accept to earn · $总额」(去掉 length>1 的 "Accept all to earn" 变体,按设计图)。
  - `pages/dev/Spv2ModalsPreview`:加 Snapchat mock(cardType=2),预览「IG+SC 两行 / 只 IG / 只 SC」。
- **数据前提**:平台角标依赖 `msg.cardType`(InsExchangePendingMessage 已有该字段,FE-3.4 加);但 useInsTaskInit 映射时后端订单没透出 cardType → 现在实际都落 instagram(默认),等后端 listUserInsExchangeOrder 订单带 cardType 才会真正区分。UI 已就绪。
- 验证:tsc -b 干净、eslint 改动文件 0 error。临时 figma 图 scratch-figma 已删。

## 工作日志 — AI Review「正文 0 字」根因 + 修复(sitin4.1 分支)

- **现象**:personal/zz/sitin4.1 上有人推的 731bc2bfc「feat: ai code review」上了流式,但 `reasoning_effort:"max"`(注释写着 low、值忘改)。AI Review 跑满 711s、`正文 0 字`。
- **根因**:stream 撑住不 524,但 `max` 让 gpt-5.6-sol 全程只推理(reasoning token 不算 content),流结束时正文为空 → 0 字。**印证"光流式不够,必须 reasoning_effort=low"**。
- **修复**:该分支 workflow `reasoning_effort` max→low(commit 3da1760d2,连同交换弹窗改动一起 push)。新 run:**117s / 正文 7160 字 / 审 26 文件**,正常出评审。
- **额外收获**:这次 AI Review 审出我 SP V2 真问题(记为 follow-up):① showInsModal 第2参 onClose→opts 签名不兼容旧调用;② App.tsx 用 authByPlatform.snapchat 放行,过期(abnormal)仍放行;③ bridge Snapchat 失败 fallback 到 IG;④ startSocialProxyRobotWebView 返回值被忽略仍设 loggedIn=true;⑤ InsExchangeBubble 用 IG isInsLoggedIn 判 Snapchat;⑥ /dev/spv2 没 DEV 门禁;⑦ 核心迁移逻辑缺测试。
- **教训**:改 AI Review 模型/参数,stream 和 reasoning_effort 要**一起**改;只改注释不改值会得到"不报错但 0 字"的假成功,比 524 更隐蔽。

## 工作日志 — 按 AI Review 修 SP V2 多社媒状态一致性(推荐批 5 处)

- 依据 PR#824 AI Review 的 Important findings,修了「真且成本可控」的 5 处(sitin-next2/app-pwa):
  - `stores/insStore.ts`:新增统一 selector `selectPlatformUsable(s,platform)`——IG→isInsLoggedIn;其它→授权&&非异常。消除各页面自定义"可用"口径(AI 架构建议)。
  - `App.tsx`(#2):Chat 放行 scAuthed→`scUsable=selectPlatformUsable(s,"snapchat")`,排除过期(abnormal)态;保留 scAuthed 仅用于 unbound/login-lost 区分。
  - `InsExchangeBubble.tsx`(#5):点击登录判据从写死 isInsLoggedIn → 按气泡 cardType 的平台 selectPlatformUsable,不再用 IG 态误判 snapchat。
  - `useInsTaskInit.ts`(#4):startRobot 接住 startSocialProxyRobotWebView 返回值,失败 setPlatformLoggedIn(false)+return,不再无条件标已登录。
  - `utils/bridge.ts`(#3):两个 SocialProxy 方法 legacy 降级前加 `if(platform!=="instagram") return false`,snapchat 不再 fallback 到 IG。
- **决策**:selectPlatformUsable 对 snapchat 用「授权&&非异常」不要求 loggedIn(snapchat 无 robot,loggedIn 恒 false;要求它会永久 block)。robot 闭环接通后再纳入。
- **未动**(另排期):#6 重连抽屉、#7 /dev 路由门禁(仓库既有惯例,要统一改)、#8 测试;Minor 2(as CardType 故意保留)。
- 验证:tsc -b 干净、eslint 5 文件 0 error。

## 工作日志 — push 多社媒状态一致性修复(bce6f89a1)

- commit `bce6f89a1` fix(app-pwa): 统一多社媒可用口径,推到 personal/zz/sitin4.1(PR#824)。5 文件:App/useInsTaskInit/InsExchangeBubble/insStore/bridge。身份 colna,hook 全过。

## 工作日志 — snapchat 授权 finish 流程对齐 IG

- **背景**:handleInsAuthResult 里 snapchat(else 分支)只写授权态,缺账号保存 + 拉小崽(原注释"待 FE-3.x")。用户要求 snapchat 流程和 IG 保持一致。
- **改法**(sitin-next2/app-pwa):
  - `useUser.ts`:`bindInstagram` → `bindSocial(platform, params)` —— saveContactCard 按平台 cardType 存(IG=CARD_TYPE_IG/SC=CARD_TYPE_SNAPCHAT,proto CardType,值 1/2)+ refreshUserInfo。
  - `useInsBridgeCallbacks.ts`:`finishInstagramBind` → `finishSocialBind(platform,...)`(账号保存+setPlatformAuth+权限+风控+`tryStartInsRobot(platform)`);handleInsAuthResult 删 if/else 统一走。
- **关键**:saveContactCard 早就支持 `cardType?`(默认 CARD_TYPE_IG),bindInstagram 内部本就是它 → snapchat 账号保存直接可用。snapchat 小崽拉起靠 tryStartInsRobot(platform),配合前面 #3/#4(不 fallback IG、失败不误标登录)优雅降级。
- **仍依赖**:snapchat robot 真跑起来要 APK/social-proxy-server 支持(RFC §8);前端已对齐,就绪即生效。
- 验证:tsc/eslint 全过。

## 工作日志 — 更新 proto 到 release/test b23ea527(78c6de3b6)

- proto submodule e95a3a0d9→b23ea527(最新 release/test,落后 7 提交:PWA swipe getPwaMatchFeeds/pwaSwipeCard、直播开关播、anomaly/report-reply 7022/7023)。`bash scripts/generate.sh` 重生成 gen(protoc 35.1),`pnpm --filter business-pwa-proto build` dist,app-pwa tsc 干净。commit 78c6de3b6 push 到 personal/zz/sitin4.1。
- **仍未解锁 FE-2.2**:InsExchangeOrderInfo 仍无 cardType、TaskType 仍只有 UNKNOWN/PENDING/PROBE/VIDEO_TIPS。后端那两个前置没进 release/test。

## 工作日志 — 结合 CE PR#104 + AI CR 修前端 3 处

- 摸清 contact-exchange-service PR#104(multi-platform IG+SC):platform_online map、checkBlurredCardCondition 返 cardType、createBlurredCardOrder(cardType)、saveCard/getCard 按 cardType + OSS 头像转存、selectBestPlatform。**建单名片后端按 cardType 权威取**。
- 据此 + CR 改了 3 处(sitin-next2/app-pwa,tsc/eslint 过):
  - **#2** showInsModal:删多余 setInsState(false)——成功视图已由 justAuthed 驱动,它会连带清 authByPlatform.instagram→IG 掉线重连抽屉失效。修好 live IG。
  - **#5** useInsTaskInit 发卡:TIM payload 账号/头像改 getContactCard({cardType}) 按平台取女方名片,不再写死 IG userInfo.insId(snapchat 卡曾带 IG 账号)。
  - **#4** handleAcceptExchange:检查批量所有涉及平台登录态(不只 messages[0]),未就绪各拉小崽+return,修混合批量用错平台。
  - 顺带 getContactCard 参数 CardType→number(消除本地/proto 名义不匹配)。
- **未改**(留 follow-up):#3 bindSocial 失败继续=旧 IG best-effort 设计(改反更糟);#1 签名(无旧调用)、#6 /dev 门禁(既有惯例)、#7 CardType 彻底统一、Minor、测试。
