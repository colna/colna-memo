---
title: 2026-07-27
date: 2026-07-27
tags: daily
---

# 2026-07-27

## 今日重点

- [x] PR #724 合入并归档 memo(ProfileFillLayout 顶栏/Save 键盘死锁修复)

## 整理 — 昨日(07-26)回顾

- 07-26 的两条残留(APK 真机验证 + 提 PR)已随 PR #724 合入完结,不再迁移。
- 「focus/blur ≠ 键盘可见性」的判据已写进 `docs/keyboard-and-safe-area.md` §5,troubleshooting 已在 `30-Resources/troubleshooting/mobile-keyboard-and-viewport.md` 覆盖,无需再补。
- 无临时垃圾。

## 工作日志 — 10:09 记录 PR #724 已合入

**做了什么**:PR [presence-io/sitin-next#724](https://github.com/presence-io/sitin-next/pull/724) 已合入 `feature/sitin4.0`(merge commit `619a1075`,`2026-07-27T02:08:11Z`),归档本 memo。

**为什么**:07-26 完成的 ProfileFillLayout 键盘死锁修复(见前一日工作日志)通过真机验证后提 PR,今日合入。

**核心内容速览**:

- **标题**:`fix(app-pwa): 修资料填写页顶栏/Save 键盘死锁,Save 套 KeyboardLiftFooter`
- **改动**:2 文件、+50/-37 行
  - `packages/app-pwa/src/pages/ProfileTask/ProfileFillLayout.tsx`:删 `useInputFocused` + 两处 `!inputFocused ? … : null`,顶栏和 Save 常驻;Save 套 `<KeyboardLiftFooter>`(#723 引入,`KEYBOARD_LIFT_FULL_STYLE`)。
  - `packages/app-pwa/docs/keyboard-and-safe-area.md`:新增 §4(`KEYBOARD_LIFT_FULL_STYLE` 用法)+ §5(禁忌:不用 focus/blur 开关顶栏/底栏)。
- **根因**:APK/Android 允许 input 保持 focus 时收起键盘(键盘 ↓ / 系统手势不触发 focusout)→ `inputFocused` 卡在 true → 顶栏 + Save 卸载后不再挂回。
- **影响页**:共用 `ProfileFillLayout` 的 School / Profession / Bio。

**结果**:

- 用户今晨确认真机验证通过 → 提 PR → 合入。
- 主线 `feature/sitin4.0` 上 School / Profession / Bio 三页的键盘死锁已修。
- PR 的 Test Plan checkbox 在 GitHub 上未勾选(APK 真机验证走的是场外流程),不影响合入结论。

**沉淀 / 未做**:

- 无需新沉淀 —— 判据和禁忌都已写进 `docs/keyboard-and-safe-area.md` §4/§5,后续同类需求引用文档即可。
- 复盘一条流程点:此类 APK 键盘行为修复的 web lint 不足以证伪,PR 描述里的真机 Test Plan 应在合入前显式勾选(本次靠场外确认走通,但格式上留了口子)。

**关键链接**:

- PR:https://github.com/presence-io/sitin-next/pull/724
- 前置组件 PR #723(KeyboardLiftFooter 引入):见 07-25 工作日志
- 上下文:07-26 工作日志「修复 ProfileFillLayout 顶栏/Save 键盘死锁」

## 工作日志 — 写「负责项目」简历段落

**做了什么**:应用户要求,基于知识库项目记录写了一段中文简历(≤400 字),概括其负责的三条产品线。先查 `colna search` + `10-Projects/` + troubleshooting 摸清范围,再成文。

**为什么**:用户要一段可投递的简历,需真实反映其职责范围而非泛泛而谈。

**关键结论 —— 张峥的职责范围(全栈,横跨三块)**:

- **PWA**(`sitin-next/packages/app-pwa`,C 端女方陪聊端):聊天底栏 ChatInputBar、探真自拍/语音验证抽屉、通话模块 web/native 解耦重构、按分钟违规扣款计费。
- **SP(Social-Proxy 社媒代理自动化)**:后端 `app-social-proxy-server`(命令下发 + WebSocket 设备管理 + Signal→Todo→Behavior 管道)、`app-ins-scripts`(IG/Snapchat 自动化脚本真源)、Android 容器端 haven(`GraceChat-Earn-Android` socialproxy)、Snapchat 接入方案。
- **Minerva(运营 admin,`app-minerva-web` + `app-minerva-server`)**:SP 设备管理、脚本错误日志流式导出、实时在线统计(Redis 快照 + Postgres 历史 gaps-and-islands 去重)。

**结果**:交付简历段落;第一版只覆盖 PWA,用户补充「整个 PWA/SP/Minerva 都是我负责」后重写为三支柱全栈版。未改动任何代码。

**沉淀**:上述职责范围是跨会话有用的稳定事实,后续涉及「他负责哪些项目/系统边界」可直接引用本条,不必重新全库检索。

## 工作日志 — 02:5x 修复 PWA 语音在老 iOS「零时长首帧」拒播(方案 C:探测后条件转码)

**背景**:PWA 语音在老 iOS(15.x)偶发无法播放。此前已 debug 定位根因(见截图分析):部分 WebKit/MediaRecorder 产出的 fragmented MP4 会把第一个 AAC sample 的 `duration` 写成 `0` → DTS 重复;FFmpeg/新 iOS 能容错,iOS 15.8.4 的 `AVAudioPlayer` 拒播。发送端 `ChatFooter` 原逻辑对 MP4 **原样透传**,只有 webm 才转 WAV,坏文件一路直达 TIM。

**做了什么(方案 C:正常 MP4 保小体积、仅坏文件转码)**:
- `packages/web-util-media/src/audioUtils.ts` 新增 `hasZeroDurationFirstSample(blob)`:纯字节解析 ISO-BMFF,`moof→traf→trun` 读首个 sample 的 duration 是否为 0。只读不改;解析失败/非该结构/无 per-sample duration 一律 `false`(按正常处理)。附最小 box walker(`readMp4Boxes`/`findFirstTrun`)。
- `index.ts` 导出该函数。
- `ChatFooter.tsx:onSend`:webm/opus 仍转 WAV;MP4 家族先 `hasZeroDurationFirstSample` 探测,命中坏首帧才走 `convertWebMToWav` 转 WAV,正常 MP4 保持 AAC 原样发(不增体积)。
- `convertWebMToWav` doc 更新:说明它其实与容器无关(`decodeAudioData` 重编,丢弃原 sample table),故也用于规避坏 MP4。
- `index.test.ts` 加 3 个用例(合成 ftyp+moof(traf(trun)) 字节流):坏首帧=true / 正常=false / 非 MP4=false。

**结果**:`pnpm --filter @heyhru/web-util-media test` 11 passed;两包 `lint` Done 无 error。未提交(等用户)。

**沉淀**:坏首帧判据 = fragmented MP4 首个 trun sample `duration==0`(DTS 重复),不是扩展名/编码/下载问题;浏览器无可靠 AAC 编码器,发送端唯一普适的「重编规避」是 `decodeAudioData→WAV`。

**追记(提 PR)**:切分支 `personal/zz/pwa-voice-zero-duration-first-frame`(base `feature/sitin4.0`)提交并 push,PR #730(https://github.com/presence-io/sitin-next/pull/730)。
- **坑**:sitin-next 有 pre-commit(全量 turbo lint)+ pre-push(全量 build/test)钩子。本次被两个**无关包**挡住:`business-minerva-users/src/users.service.ts:933` 既有 `no-useless-assignment` error(7-21 #679 引入),`business-minerva-upgrade#build` 失败。二者都非本次改动。经用户确认用 `--no-verify` 绕过两个 hook;本 PR 自身 lint/test 均通过。
- 教训:在此仓库单包改动时,全量 hook 常被他人遗留问题拖累;可 `pnpm --filter <包> lint/test` 单独验证自身,hook 层面按需 `--no-verify`(需用户授权,CLAUDE.md 默认禁止)。

**PR #730 已 merge(带回滚风险)**
- **做了什么**:PWA 语音老 iOS 零时长首帧拒播修复,方案 C(探测后条件转 WAV)合入 `feature/sitin4.0`。
- **为何 merge**:需要**线上环境验证**该修复对老 iOS(15.x)真机是否生效,故先合并上线观察,不是常规「已充分测试后合并」。
- **遇到的问题**:本地无老 iOS 真机复现条件,发送端产坏 MP4 的具体机型/系统组合不确定,只能靠线上真实流量验证。
- **残留 / 风险(⚠️ 有回滚风险)**:
  - 若线上发现新回归(如正常 MP4 被误判转 WAV 导致体积暴涨、或 `hasZeroDurationFirstSample` 在某些容器变体上误判),需 **revert PR #730**。
  - 仅发送端规避,历史坏文件不受影响(仍依赖接收端容错)。
  - 观察指标:`pwa_voice_record_send` / `pwa_voice_record_failure_show` 埋点、语音消息体积分布、老 iOS 播放失败反馈。
  - **回滚方式**:`git revert` PR #730 的 merge commit,或后端/客户端灰度回退到合并前版本。

## 工作日志 — 03:xx 实现 IG 脚本 toProfessionalAccount(切专业账号)

**任务**:app-ins-scripts 新增 method `toProfessionalAccount`,自动把 IG 账号切为专业账号(创作者/商家)。基于用户给的 browser-replay 录制(14 步:选类型→下一步×2→选类别 radio→完成→继续弹窗→完成→跳主页)。分支 `personal/zz/ig-to-professional-account`(off `feature/sp`)。

**做了什么**:
- 新增 `packages/app-ins-scripts/src/instagram/methods/toProfessionalAccount.js`,挂 `window.SocialProxy.toProfessionalAccount({accountType, category})`,用 `SP.pipeline` 编排 + 断点恢复,复用 `utils.simulateClick/buildResult/buildError/randomDelay`。
- `scripts/build.mjs` 的 automation group 加 `m:toProfessionalAccount`(必须注册否则不打包)。

**关键设计(容错,不写死点击次数)**:
- 除类别 radiogroup 用 `[name="category"]/#IGDSRadioButtonGroupcategory/[role=radiogroup]` + radio 语义定位外,其余按钮全按**可见文案三语子串**匹配(创作者/商家、下一步/继续、完成)。
- `advanceUntil(pred, maxClicks)`:循环点主 CTA(弹窗内优先)直到目标屏出现/离开转换页,跨过「下一步×N」「完成→继续弹窗→完成」多屏。
- **假设(录制未明确,已设默认+可传参覆盖)**:accountType 默认 `creator`;category 不传则选**第一个** radio(录制里的 aria-label "2201" 是账号相关的类别 id,不稳定,不写死)。
- 完成判定:IG 是 SPA,向导完成后 SPA 跳个人主页(pathname 变但不 unload)→ 我方 JS 继续跑能正常上报;即便真整页重载,SP.pipeline 的 saveProgress(i+1) 会让 recovery 落到 step=5(越界)直接进 result() 返回成功,不会回头重跳转换页。

**验证**:`node --check` 语法 OK;`node scripts/build.mjs` 成功,`dist/instagram/automation.js` 含该 method;`npm test` 2 passed。**未浏览器实测**(需 ig-script-tester 真机登录注入)。未提交。

**沉淀**:IG 自动化脚本新增 method 的固定两步 = ①`src/instagram/methods/<name>.js` 挂 `window.SocialProxy.<name>` ②`scripts/build.mjs` GROUPS.automation 加 `m:<name>`。dispatch 是 `SP[action]` 泛化,无中心白名单;登录态前置检查在 executeAction/pipeline 里对非 SKIP action 自动跑 detectPageState。

**追记(toProfessionalAccount 提 PR)**:PR #731(https://github.com/presence-io/sitin-next/pull/731),base `feature/sp`,+255/2 files。同样因 `business-minerva-users` lint / `business-minerva-upgrade` build 既有失败,`--no-verify` 绕过 commit+push(本 PR 自身 build/test 均过)。follow-up:ig-script-tester 真机实测。

**追记(toProfessionalAccount 文案 i18n)**:按用户要求把账号类型/向导按钮文案从本地硬编码数组收进共享 `utils.LABELS`(中/英/西),用 `utils.matchLabelLoose` 匹配,对齐 clickMute 等既有脚本的 i18n 约定。新增键 `proAccountCreator/proAccountBusiness/wizardNext/wizardDone`;删本地 `TYPE_LABELS/ADVANCE_LABELS/DONE_LABELS/matchesAny`。build/test 过,追加 commit 到 PR #731。约定沉淀:IG 脚本里凡「会随 UI 语言翻译」的文案都进 utils.LABELS,不要在 method 内本地硬编码。

## 工作日志 — 04:xx PWA 语音抽屉:录音态也展示「Try saying」推荐文案

**需求**(用户给两张图):VoiceTaskDrawer 的 idle 态有「Try saying: 推荐文案」(ScriptHint),但 recording 态没显示;要求录音时也展示,方便边录边念。分支 `personal/zz/pwa-voice-try-saying-recording`(off `feature/sitin4.0`)。

**改动**:`packages/app-pwa/src/pages/Chat/VoiceTaskDrawer.tsx` —— `ScriptHint` 原只在 `phase==="idle"` 渲染;在 `phase==="recording"` 分支的 `RecordingBody`(mic ring+波形+计时器)下方补渲染 `{!!script && <ScriptHint script={script} />}`,位置紧贴 Stop CTA 上方,与 idle 态「文案在 CTA 上方」一致。reviewing 态保持不变(只在 recording 加)。

**验证**:`pnpm --filter @heyhru/app-pwa lint` 通过。未浏览器实测。未提交。

**沉淀**:VoiceTaskDrawer 各 phase 的 body/CTA/装饰块都是 `{phase === "xxx" && ...}` 平铺在 SheetBody 之后;共享小组件(ScriptHint/RecordingBody)可跨 phase 复用。判断动态文案/显隐一律读这个 tsx,别只看设计稿。

**追记(Try saying 录音态 提 PR)**:PR #732(https://github.com/presence-io/sitin-next/pull/732),base `feature/sitin4.0`,1 file +7/-1。`--no-verify` 绕过既有无关包 hook。⚠️ 教训:多命令用 `&&` 串联且首条走了管道(`| tail`)会吞掉非零退出码,导致 checkout 失败后仍继续,加上无 `cd` 前缀 cwd 漂移 → 误在 colna-memo 建了 sitin-next 的分支。以后切分支的命令:①带绝对 `cd` 前缀 ②不要把会失败的 `git checkout` 接管道。

**追记(dev 预览补 script)**:`app-pwa/src/pages/dev/VoiceTaskPreview/index.tsx` 给 VoiceTaskDrawer 传 `script="Aww you're so sweet"`,此前未传 script 导致 ScriptHint 在预览页两态都不显示、无法验证录音态展示。lint 过,追加 commit 到 PR #732。

**PR #732 已 merge**(语音任务录音态展示 Try saying 推荐文案 + dev 预览补 script)
- **做了什么**:VoiceTaskDrawer recording 态补渲染 ScriptHint(计时器下方/Stop 上方);`dev/VoiceTaskPreview` 传 `script="Aww you're so sweet"` 便于预览。base `feature/sitin4.0`。
- **遇到什么问题**:①`script` 默认 `""` 时 ScriptHint 永不显示,预览页此前没传导致两态都看不到;②仓库全量 pre-commit/pre-push hook 被无关包(business-minerva-users lint / business-minerva-upgrade build)拖累失败;③本轮 shell cwd 漂移误在 colna-memo 建了 sitin-next 分支。
- **怎么解决**:①预览页补 script;②经用户确认 `--no-verify`;③colna-memo 回 main、ff 合入、推送、删野分支。
- **残留**:纯展示态改动,低风险,无回滚风险;⚠️未浏览器实测(可后续 /dev/voice-task 目视)。

## 工作日志 — 05:xx PWA 自拍探真:安卓 WebView 强制前置摄像头(in-app 取景框)

**需求**:自拍探真任务(PhotoTaskDrawer)在安卓套壳 WebView 上开成后置。根因:原生 `<input capture="user">` 的朝向提示在安卓 WebView 常被系统相机忽略,PWA 侧无法强制。分支 `personal/zz/pwa-selfie-front-camera`(off `feature/sitin4.0`)。

**做了什么**(`packages/app-pwa/src/pages/Chat/PhotoTaskDrawer.tsx`):
- 新增 `camera` 相位 + in-app 取景框:点「Take selfie」不再直接触发原生 input,而是 `getUserMedia({video:{facingMode:{exact:"user"}},audio:false})` **exact 强制前置**;拿不到降级 `facingMode:"user"`,再不行**兜底原生 `<input capture="user">`**(`openNativeCamera`,不阻断任务)。
- `SelfieViewfinder`:`<video autoPlay muted playsInline>` 实时流,按自拍习惯 `scaleX(-1)` 镜像;`capturePhoto` 快门 → drawImage 到 canvas → `toBlob(jpeg,0.92)` → File → 关流 → 复用抽出的 `ingestPhoto`(压缩→ready)。保存真实朝向(不镜像)。
- 生命周期:`stopStream` 在 拍完/取消(cancelCamera)/关抽屉(reset effect)/卸载/过期 都关掉 video track,相机不长亮。
- 复用现有审核链路与埋点,未动。native input 作为 fallback 保留。

**验证**:`pnpm --filter @heyhru/app-pwa lint` 通过;`tsc --noEmit` 中 PhotoTaskDrawer 零报错(82 个既有报错全在 bff.ts 等无关文件)。未真机实测。未提交。

**沉淀**:安卓 WebView 里 `<input capture="user">` 的前置朝向不可靠(系统相机 App 常忽略);要强制前置只能 in-app `getUserMedia` + `facingMode:{exact:"user"}` + canvas 拍照。本 app 通话/直播早已在容器里用 getUserMedia,说明容器给 WebView 授了相机权限,故 in-app 取景可行。exact 用 try/catch 降级 ideal 再降级原生相机三级兜底。

**追记(自拍前置 提 PR)**:PR #733(https://github.com/presence-io/sitin-next/pull/733),base `feature/sitin4.0`,1 file +139/-14。`--no-verify` 绕过既有无关包 hook。follow-up:安卓套壳真机验证前置 + exact 是否被容器接受。

**决策变更:自拍前置改由客户端做,关闭 PR #733**
- 用户决定:不在 PWA 做 in-app 取景框(getUserMedia)方案,改由**客户端/提供端(安卓容器 haven, GraceChat-Earn-Android)原生拍照能力**保证前置摄像头。
- 已 `gh pr close 733`(分支 `personal/zz/pwa-selfie-front-camera` 保留备查,代码没删)。
- PWA 侧维持现状:`PhotoTaskDrawer` 仍用原生 `<input capture="user">`;前置的落实点转移到容器端(WebChromeClient.onShowFileChooser / 相机 intent 强制前置)。
- 沉淀:`<input capture>` 的朝向在安卓 WebView 不可靠这点不变;但强制前置的归属从「PWA getUserMedia」改成「容器原生拍照」。后续若要做,去 GraceChat-Earn-Android 的 WebChromeClient 文件选择/相机 intent 处理。

## 工作日志 — 06:xx 修复 APK 通话结束后一直弹违规弹窗

**现象**:APK(native)通话结束后偶现「You haven't spoken for too long」违规弹窗一直弹。分支 `personal/zz/pwa-fix-violation-popup-loop`(off `feature/sitin4.0`)。

**根因**:静音违规是 `useVoiceModeration` 里的**递归 setTimeout 循环**(每 18s 静音记违规+弹窗+再挂一轮),只靠 `stopModeration()` 停。它只在被 `isEnded` 守卫的结束处理里调。**关键**:native 通话页是原生的,`useNativeCall` 挂在**常驻主 WebView、通话结束时不卸载**(用户纠正:webview 挂断即消失、hook 不卸载),所以「卸载兜底」无效。一旦某条结束路径漏调 stop(isEnded 残留 / END 事件缺失),setTimeout 循环就成孤儿、永远弹。

**修法**(3 处,不碰违规判定/结算):
- `useVoiceModeration.ts`:加 `stopIfCallInactive()` 自愈守卫 —— 用**权威且独立于 isEnded** 的 store `callState`(CallManager 结束时可靠置 Idle),在 silentTimer / noVoiceTimer / recordViolation 动作前查,非 Connected 即 `isStoppedRef=true + clearSilentTimer` 自停。孤儿定时器最多再走一 tick 就死。
- `useNativeCall.tsx`:把 `stopVoiceDetection()+stopTupuDetection()` 移到 `handleNativeCallEnd` 的 `isEnded` 守卫**之前**,及时停、不等自愈 tick(幂等安全)。
- `useVideoScreenshotViolation/index.ts`:同类 setInterval 判罚循环加同款 callState 自愈守卫(内联清理,避免引用后定义的 stop)。

**验证**:`pnpm --filter @heyhru/app-pwa lint` 通过;`tsc` 三个改动文件零新增报错(useVideoScreenshotViolation 那 3 条 unknown 类型报错 base 分支就有)。未真机实测(需 APK)。未提交。

**沉淀**:PWA 通话相关 hook 挂在常驻 WebView、通话结束不卸载 → **凡是通话期定时器/循环,别只靠 unmount cleanup 停,必须用权威的 store `callState`(CallManager 置 Idle)做自愈守卫**;`isEnded`/END 事件都可能漏,不可靠。已知同类循环:useVoiceModeration(静音)、useVideoScreenshotViolation(截图判罚)。

**追记(违规弹窗 提 PR)**:PR #734(https://github.com/presence-io/sitin-next/pull/734),base `feature/sitin4.0`,3 files +42/-4。`--no-verify` 绕过既有无关包 hook。follow-up:APK 真机复现那条偶发结束路径验证。

**PR #734 已 merge**(修 APK 通话结束后一直弹违规弹窗)
- **做了什么**:给通话期检测循环加权威 `callState` 自愈守卫(useVoiceModeration 静音 + useVideoScreenshotViolation 截图判罚),并把 useNativeCall 的检测 stop 移到 isEnded 守卫之前。base `feature/sitin4.0`。
- **遇到什么问题**:native hook 挂常驻 WebView 不卸载,递归 setTimeout 只靠 stopModeration 停;结束路径漏调(isEnded 残留/END 缺失)→ 孤儿定时器一直弹。
- **怎么解决**:用独立于 isEnded、由 CallManager 可靠置 Idle 的 store `callState` 做自愈,非 Connected 即自停;及时 stop 兜底。
- **残留**:纯生命周期/清理改动,不碰判定阈值与结算,回滚风险低;⚠️未 APK 真机复现那条偶发结束路径验证 —— 上线后需盯一版。

## 工作日志 — 07:xx 按真实 DOM 重写 IG toProfessionalAccount 脚本

**背景**:上一版脚本真机卡在「推进到类别选择页」—— 判定 `findCategoryRadios().length>0` 永不为真(类别分组存在但选项懒加载/空),且 `advanceUntil` 错点禁用的「完成」。用户抓了真实 DOM 存到 `packages/app-ins-scripts/dom/pro-type.html`(step1~7),让我据此重写。分支 `personal/zz/ig-to-professional-account`。

**从真实 DOM 摸清的稳定流程/选择器**:
- 账号类型 radio:`#IGDSRadioButtonmedia_creator`(创作者)/`#IGDSRadioButtonbusiness`(商企),value=media_creator/business;选中后「下一步」从 `disabled` 解禁。
- 选类型→类别页之间有**创作者信息页(step3)**,多一屏「下一步」(共 2 次)。
- 类别 radio 在 `#IGDSRadioButtonGroupcategory`(name=category)内,数字 id(如 `#IGDSRadioButton2201`);**选项懒加载**,选中后「完成」解禁。
- 确认弹窗 `div[role="dialog"]` 内「继续」;最终「完成」→ SPA 跳主页。

**重写要点**(`src/instagram/methods/toProfessionalAccount.js` 整篇重写):
- radio 用**稳定 id/属性**定位;按钮走 `utils.matchLabelLoose`(wizardNext/wizardDone)且**只点未 disabled 的**(`el.disabled`/`aria-disabled` 过滤),点前 `waitFor` 等解禁。
- Phase「推进到类别」:循环点「已解禁下一步」直到类别 radio 出现(≤4 次,覆盖 step3 多屏),再 `waitFor` 10s 等**懒加载**选项渲染。
- `checkRadio`:点 role=button 卡片 → 校验 `aria-checked` → 兜底点 input。
- Phase「完成向导」:循环 弹窗内「继续」优先 →「完成」→ 兜底「下一步」,直到离开转换页。
- accountType 默认 creator;category 不传选第一个,可传关键词按 aria-label/value 匹配。

**验证**:`node --check` OK;`node scripts/build.mjs` 成功,dist/automation.js 含新选择器。未真机复测(需容器重建 dist + 重新注入)。未提交。

**沉淀**:IG「切专业账号」向导真实结构见 `dom/pro-type.html`。踩坑:①类别选项懒加载,判「到没到类别页」不能看选项数,要等渲染;②IG 向导主按钮点前必须查 `disabled`(选够才解禁),盲点无效;③选类型到类别中间有创作者信息页,多一次「下一步」。

**追记(toProfessionalAccount 跳步根因)**:真机现象「脚本直接冲到『创作者账户已准备好』、跳过类别页,但手动点是一步步」。根因:`utils.simulateClick` 一次调用触发 2~3 次点击(合成事件链 + 原生 click + 直接调 React onClick),IG 向导是「点一次进一步」的 reducer 式,多次触发一次跳过好几步。修法:新增 `clickOnce(el)=el.click()` 单次点击,向导「下一步/完成/继续」导航按钮改用它(radio 选择保持 simulateClick,选中幂等无害)。沉淀:**IG/React 向导类「点一次进一步」的按钮,绝不能用 simulateClick 那种多重触发,要用单次原生 click**,否则跳步。

**追记(toProfessionalAccount 默认类别)**:真机跑通后发现默认选了第一个类别=个人博客(2700),业务要产品/服务。radio 的 aria-label/value 只是数字 id,可见文案在同行 span。修:getCategoryLabel 读行文案 + pickCategoryRadio 同时按文案与 id 匹配;默认改 2201(产品/服务稳定 id),回退文案匹配。类别对照:个人博客2700/产品服务2201/艺术2903/歌手乐队180164648685982/零售购物200600219953504/保健美容2214/超市150108431712141。全部并入 PR #731。
