---
title: 2026-09-22
date: 2026-09-22
tags: daily
---

# 2026-09-22

## 今日重点

- [ ] **T0048**（likes/visitors 与爪印数量不一致）：A（新鲜度，`bb37c2ad3`）+ **B1（徽标与名单同源，`71a075ddd`）** 均已推送 —— 待真机验证（**需重打 IPA**）；飞书单子未动
- [ ] **T0052**（重登后资料丢）：等真机日志，`grep '\[profile\]'`（dev/preview 包 Export Log）→ 定后端修投影还是客户端合并读模型
- [ ] **T0061**（订阅赠币 +0）：给「SP后端内部群」的对账请求已拟好，等用户确认后发
- [ ] **真机验收**（昨天一批修复，均未实机走查）：T0057 键盘退出/T0054 进屏不弹键盘/T0053 地址保存/T0055 去动作栏/T0056 全帖列表；Sent 卡修复 `a759a0692`；引导第 4 步 `35608d542`；通知分段徽标定位 `84a5b1107`（**需重打 IPA**）
- [ ] **luka moments See all 常显** `a4d0f3f9a`（已推）：实机看 3 张照片的资料页有 See all、纯文字帖也能进全帖列表
- [ ] 根目录未跟踪的 `-` 文件（devicectl UDID 误产物）删不删，待用户定夺

## 工作日志

### 10:40 · sitin-rn：把 origin 从 GitHub SSH 改为 GitLab HTTPS

- **做了什么**：`git remote set-url origin https://gitlab.lumosolo.com/app-rn/sitin-rn`（原 `git@github.com:presence-io/sitin-rn.git`）；并在**仓库本地** config 加两条 rewrite 兜底，保证任何 SSH 写法都强制落回 HTTPS：`url."https://gitlab.lumosolo.com/".insteadOf = git@gitlab.lumosolo.com:` 与 `ssh://git@gitlab.lumosolo.com/`。
- **为什么**：用户要求仓库换到 GitLab，且**只准用 HTTPS 推拉**（不走 SSH）。
- **结果**：`git remote -v` 已指向 GitLab；`GIT_TERMINAL_PROMPT=0 git ls-remote origin` 报 `could not read Username` —— 远端是私有 GitLab，**本机还没有该站的凭据**。全局 `credential.helper = osxkeychain` 已在，第一次交互式 pull/push 输入用户名 + Personal Access Token 即会被钥匙串记住，之后免输。**未做**：没有嵌入 token，也没有改动任何全局 config（改动全在 `.git/config`，只影响本仓库）。
- **关键命令**：`git remote set-url origin https://…`；`git config --local --add url."https://gitlab.lumosolo.com/".insteadOf "git@gitlab.lumosolo.com:"`（两条 insteadOf 需用 `--add`，直接 `git config k v` 会覆盖前一条）
- **待办 / 提醒**：
  - 各 worktree 共用同一 `.git/config`，remote 变更对 `fix/luka-code-review` 等 worktree 一起生效；本地 `origin/feature/luka-ios` 等 remote-tracking ref 仍是 GitHub 那份，下次 `git fetch` 会连新站重取。
  - `apps/*` 的 CI（`.github/workflows`）在 GitHub 侧，迁 GitLab 后需要另配（未处理）。
  - 仓库文档 `docs/native-dev-onboarding.md` 仍写着 `git clone git@github.com:presence-io/sitin-rn.git`，与本次迁移不一致 —— 用户没要求改，且「是否全队迁 GitLab」未定，未擅自动。

### 10:40 · colna-memo：整理 2026-09-21 Daily（首次任务）

- 前一天 527 行、共 40 余条工作日志，全部已闭环或已在日志内标注去向；本轮**无新增沉淀、无归档**（可复用经验 09-21 当天已逐条写进 `30-Resources/troubleshooting/`：`luka-logout-fatal-account-stranded` / `luka-profile-fields-lost-on-relogin` / `subscription-coins-zero` / `react-native-svg-ios-mask-crash` / `uniwind-dev-stale-css-new-class` / `sitin-rn-local-ipa-verification` / `react-native-stale-measure` / `expo-router-leaf-branch-route-static-check` / `colna-sync` 等）。
- 复制 5 条未完成待办到今日「今日重点」（T0048 / T0052 / T0061 / 真机验收批 / 根目录 `-` 处置）。
- 项目进展同步：`10-Projects/luka-rn-app.md` 补「仓库已迁 GitLab HTTPS」与「域名/商店身份改动已提交」两处。

### 10:50 · sitin-rn：T0048 修法 A —— 爪印计数补两处「立刻刷新」

- **背景**：T0048 视频逐帧取证（09-21 晚）显示页头爪印静帧 = 3，而进通知页那次 `useIntoYouCounts()` 刷新后 store 变成 `likeUnread=0 / visitorUnread=1` —— 即**爪印停留在旧数**。app 级只有 30s 轮询，`useInterval` 在 `enabled/active` 重新为真时不会立即触发，回前台/切回 Chats 都不刷。用户选 A（补新鲜度），B（口径统一）暂不做。
- **改动**：
  - `apps/luka/src/hooks/use-into-you-badge.ts`：AppState 监听里，`s === "active"` 时立即 `refresh()`（用 `enabledRef` 读登录态，监听只订阅一次）。
  - `apps/luka/src/app/(tabs)/index.tsx`：新增一个 `useFocusEffect`，focus 到 Chats 时 `refresh()`（带 `userId` 守卫，登出态不刷）。
  - `apps/luka/docs/notifications.md`《计数新鲜度》补「回前台 / 切回 Chats 各刷一次」与 T0048 由来。
- **验证**：`pnpm --filter luka typecheck` 0；biome 2 文件干净；`tests/lib/notification-badges.test.ts` 4 passed。**未实机**（dev client fast refresh 或重打 IPA 可看）。
- **未提交**：按规范模式等用户确认；注意仓库 origin 今天已被并行会话改成 GitLab HTTPS（`https://gitlab.lumosolo.com/app-rn/sitin-rn`），本机还没该站凭据，push 前先确认。

- **10:55 提交 / push 受阻**：`595f37ea0` `fix(luka): 爪印未读计数回前台/进 Chats 立即刷新（T0048）`（3 files，+33/−3）已落地本地；`GIT_TERMINAL_PROMPT=0 git push` 报 `could not read Username for 'https://gitlab.lumosolo.com'`，`git credential fill` 也无凭据 —— **需要用户在本机终端跑一次 `git push`**（输入 GitLab 用户名 + PAT，osxkeychain 记下），之后才能推。未用嵌入 token 的写法（并行会话迁移时明确留了这条）。

### 10:56 · sitin-rn：查「资料页地址显示 0, California」——接口数据问题，非展示

- **做了什么**：定位展示链（截图是 luka 资料页，标题 `{NAME}'S PROFILE` 唯一命中 `apps/luka/src/app/profile/[id].tsx:200`）→ `buildLocation()` 只是 `[city, province||region]` 原样 join，客户端无任何写入 `0` 的路径。为坐实，用 esbuild 打包 `PbClient` + 共享包源码，FastLogin 新设备号直连 `api-dev.lukasap.com`：扫 ~2000 个 feed 女用户、对 400 个逐个对比 `GetUserProfilePageInfo` 与 feed/`GetUserBasicInfosV2` 的 `geoLocation`。
- **结果**：**三接口 400/400 完全一致**（资料页不可能与卡片不同）；后端把 `"City, State"` 整串存在 `region`（city/province 常空）。样本里已见同类脏值 `" Ohio"` / `"South African"` / `"Strand, Georgia"`，`0` 是同一来源的城市位占位；本次 2000 样本没扫到 `0`，说明是少数账号。**结论：数据问题，不是展示 bug**。
- **沉淀**：`30-Resources/troubleshooting/luka-profile-address-zero-city.md`（含「无 token 直连 dev 后端扫码」手法：esbuild alias 共享包 + `PbClient` 无 RN 依赖 + FastLogin skipAuth）。
- **未做**：前端兜底（过滤纯数字城市）需改 iris/luka/koda/lumi 四份 `buildLocation`，等用户定夺；定位具体账号需 uid 或设备 session.log。
- 关键命令：见 troubleshooting 笔记；脚本在 `/tmp/loc-check/check*.ts`（临时，未入库）。

### 11:30 · sitin-rn：luka 地址 0 占位段不显示（用户确认口径：地址里的 0 段不显示）

- **做什么**：用户拍板「0 的话就不显示」。改 `apps/luka/src/lib/candidate-mapper.ts` 的 `buildLocation()`：新增 `displaySegments()`，把 city / province / region / zip 里**全 0 的段**（`region = "0, California"` 或 `city = "0"`）当空值丢掉再拼接；`province` 清完为空时回落到 `region`（否则 `province = "0"` 会把真正的州挡掉）。判据用 `/^0+$/` 而非「纯数字」——ZIP 也是纯数字，第一版用 `^\d+$` 直接把 ZIP 兜底打断了（测试抓到）。
- **测试**：`apps/luka/tests/lib/candidate-mapper.test.ts` 加 5 条回归（0 城市保州 / 0, California / 0 province 不遮 region / 只剩占位时回落 ZIP / 全占位时整条空）。26 passed；`pnpm --filter luka typecheck` 0；biome 干净。
- **文档**：`apps/luka/docs/location.md` 约定加第 4 条（含「全 0 不是纯数字」的判据说明）。
- **未做**：iris/intro/blueprint 各有一份同款 `buildLocation`，未同步改（等用户定夺）；未 commit（规范模式）。

- **11:35 提交并 push**：`2e21ccf58` `fix(luka): 地址不显示后端写入的 0 占位段`（3 files，+50/−2）→ `feature/luka-ios`（GitLab HTTPS 凭据已可用）。pre-push：boundaries OK、luka typecheck 通过（9.1s）。工作区另有并行会话未提交改动（T0088 自己账号地址 `geoLocationLabel` 一套 4 文件），**未纳入本次提交**。

### 10:56 · sitin-rn：luka 资料页 moments 的 See all 按「有没有帖子」常显

- **需求**：用户发实机截图（Ellie Murphy's profile，正好 3 张 moments），红框处缺 See all —— 从 feed 进的资料页要能进全帖列表。根因：`ProfileMoments` 只在 `images.length > 3` 时渲染 See all，正好三张时没有入口；纯文字帖（无图）更是整块不渲染、全帖列表不可达。
- **改动**（用户拍板「只要有帖子就常显」）：
  - 新增 `apps/luka/src/lib/profile-moments.ts`：`profileMomentsLayout(postCount, photoCount)` → `{ visible, strip }` —— 有帖子就出「标题 + See all」；有照片才画三格照片列；一条帖子都没有整块隐藏。
  - `components/profile/profile-moments.tsx`：接 `postCount`，See all 去掉 `>3` 条件常显，照片列按 `strip` 条件渲染。
  - `app/profile/[id].tsx`：传 `posts.length`；`docs/discover-play.md` Feed 章节补一条。
- **验证**：luka typecheck 0 / biome 5 文件干净 / 新增 `tests/lib/profile-moments.test.ts` 5 passed。全量 1019 tests 中 7 failed 与基线完全一致（stash 掉本次改动复跑确认：good-review、me（expo-modules-core 在 node_modules 里的类型剥离报错）、use-swipe-actions 3、candidate-card-fields、shape-language 3，均既存）。
- **提交 / 推送**：`f62b32ce8`（5 files，+107/−43）。当时 push 受阻（GitLab 无凭据；VS Code 的 askpass 弹窗不出来，表现为「执行了没反应」—— 进程其实卡在等 `Username for 'https://gitlab.lumosolo.com'`）。
- **11:17 已拉取并推送**：用户把凭据（GitLab `zhangzheng` + PAT，`read_repository`/`write_repository`）存进 osxkeychain 后，`git pull --rebase origin feature/luka-ios` 并入远端 5 个提交（无冲突）→ `git push` 成功（pre-push：boundaries 41 包 + luka typecheck 通过）。rebase 后哈希：T0048 `595f37ea0`→`bb37c2ad3`、本次 `f62b32ce8`→`a4d0f3f9a`。
- **坑（沉淀）**：VS Code 终端里 git 要凭据时走 VS Code askpass 弹窗，弹窗没出来就完全「没反应」（终端既不提示也不报错）——`ps` 能看到 `Code Helper (Plugin) … askpass-main.js Username for '…'` 在等；换 macOS「终端」App 跑即可看到终端提示，或先在「终端」用 `git credential-osxkeychain store` 存一次。
- **顺带发现（未修）**：`tests/hooks/use-swipe-actions.test.ts` 的 `@/services/swipe-limit` mock 缺 `swipeLimitReachedOutcome`，3 条超限去重测试在 HEAD 上已红，非本次引入。

### 11:33 · sitin-rn：T0088 登出重登后 Location 变回「add yours」——自我地址补 region 兜底

- **来源**：飞书 buglist T0088（P1，未开始，指派张峥；T0052 关闭 2 秒后由测试赵延杰单开）。
- **根因（已实证）**：后端把用户提交的地址整串存进 `geoLocation.region`（`"Dallas, Texas"`），`city` / `province` 留空。用 esbuild 打包 `PbClient` 直连 `api-dev.lukasap.com` 探针复现：FastLogin 新设备 → `UploadUserInfo`（Dallas/Texas/75231）→ `GetUserBasicInfosV2`、`EditUserLocation`、**重登 FastLogin** 回来都只有 `region` + `zipCode`（`login#2 derived location label = ""`）。客户端自我地址五处只拼 `[city, province]`，重登后快照重建 → 空 → Location 行「add yours」；登出前能看到只是因为 `editLocation` 的乐观写把 `"Dallas, Texas"` 留在本地快照里。
- **修法**：`apps/luka/src/lib/location.ts` 新增 `geoLocationLabel(geo)`（`province → region → ZIP`，丢全 0 占位段），五处统一：`services/auth.ts` `toUserSnapshot`（登录快照）、`services/me.ts` `selfFromProfile`（Me 读模型）、`stores/me-overview.ts` `locationFromSnapshot`（缓存合并）、`services/user.ts` `editLocation`（乐观写）、`services/complete-onboarding.ts`（注册落库快照）。`lib/candidate-mapper.ts` 的 `buildLocation` 收敛为调用同一实现（并行会话 T0087 的 `displaySegments` 去重，行为不变）。
- **测试**：`tests/lib/location.test.ts` +5（region 兜底 / ZIP 兜底 / 0 占位）、`tests/services/me.test.ts` +1（region-only 读模型回归）、`tests/stores/account-state.test.ts` +1（快照合并）。相关 5 文件 62 passed；`pnpm luka:typecheck` 我的文件 0 错（剩 2 条在并行会话在飞的 `candidate-card-info/discovery-deck`）；biome 干净；全量 `1035 passed / 7 failed`，stash 复跑确认 7 条失败与基线完全一致（design×4、use-swipe-actions×3、good-review）。
- **顺带修复**：`tests/services/me.test.ts` 自 `0296c01d1`（T0052 字段自查）起在该环境 load 失败 —— `services/me` → `@/lib/dev-gate` → `@/services/config` → `expo-constants` → `expo-modules-core`，Node 无法 strip `node_modules` 里的 TS；加 `vi.mock("@/lib/dev-gate")` 后套件恢复。**验证改前/改后都失败，不是本次引入**。
- **文档**：`apps/luka/docs/location.md` 约定新增第 5 条 + 后端现状补「提交地址只存在 region」。
- **未提交**（规范模式）；T0088 状态未动，等验证后更新。探针脚本 `/var/folders/.../T/opencode/t0088-probe.ts`（临时，未入库）。
- **11:37 提交并 push（用户要求）**：`868535448` `fix(luka): 重登后 Location 不再变回 add yours，自我地址补 region 兜底（T0088）`（11 files，+154/−48）→ `origin/feature/luka-ios`（`2e21ccf58..868535448`）。`git commit --only` 只带本次 11 个文件，并行会话在飞的 discovery 组件改动未混入；pre-commit biome、pre-push boundaries（41 包）+ luka typecheck（5.5s）全过。T0088 飞书状态未动，等用户定夺。

### 11:39 · sitin-rn：luka 划卡信息带改毛玻璃底、并压到卡角小崽之上

- **需求**：用户发实机截图（Megan 的划卡），卡角小崽压着「名字 / 职业 · 城市」那一行，正文尾巴被吃掉一截（连省略号都看不见）。要求：**文字加毛玻璃底色，改在小崽上面**。
- **根因**：信息带（`CandidateCardInfo`）原本嵌在卡片子树里，而小崽是 `nearby-view` 里卡片**之上**的浮层（`zIndex: 10`）。RN 里 zIndex 只在兄弟之间比较，卡片的子树跨不出来 —— 小崽必然压住信息带。
- **改动（6 files，+195/−107）**：
  - `components/discovery/card-stack.tsx`：新增 `renderChrome(index, frontStyle)` —— 卡框大小的一层，渲染在两张槽之上（`zIndex: 2` > 正面槽的 1）；`frontStyle` 传的是正面卡的位移动画。
  - `components/discovery/discovery-deck.tsx`：`renderChrome` 里先画小崽（**不套** `frontStyle`：屏级装饰、不跟卡移动）、再画信息带（套 `frontStyle`：拖动跟手、划走一起飞出）。小崽落点从 `cardInset + 2 / tabBarClearance + BELOW_CARD_HEIGHT - 16` 换成卡框内偏移 `right: 2 / bottom: -16`（等值，且不再受屏宽/标签栏换算漂移）。
  - `components/discovery/candidate-card.tsx`：卡片不再渲染信息带（只留照片 + 渐变 + 刮层 + 调试角标），`onInfoLayout` 随之移到 chrome。
  - `components/discovery/candidate-card-info.tsx`：`glass` 档的底从 `DeckColor.surface` 纸色改成**毛玻璃** —— iOS `BlurView`(intensity 60, tint light) + 72% 纸色雾面；Android 只有 90% 雾面（与底栏/聊天输入栏同一条「Android 不开 Dimezis」取舍）。它不再吃卡片的 `overflow: hidden`，所以底角自己给了 24 圆角 + 裁剪。
  - `docs/discover-play.md` 现状段同步。
- **验证**：`pnpm luka:typecheck` 0（并行会话行里那 2 条报错已随本次编辑消失）；biome 干净；全量 `1035 passed / 7 failed`，与 `git stash` 基线逐条一致（design×4、use-swipe-actions×3、good-review）。**未实机**：模拟器（iPhone X）停在 onboarding 且本机没有 Simulator.app（只有 simctl），没法点进去看划卡。
- **已知取舍**：小崽画布 130pt、可见约 110pt，信息带约 138pt → 小崽整只落在毛玻璃后面，玻璃里只剩一层模糊剪影。若用户希望它探出头，把 `HAMSTER_BOTTOM` 往上挪 ~40pt 即可（一个常量）。
- **提交 / 推送**：`6d8baefec` `fix(luka): 划卡信息带改毛玻璃底，并压到卡角小崽之上`（6 files，+195/−107）→ `origin/feature/luka-ios`（`868535448..6d8baefec`）；pre-commit biome 5 文件、pre-push boundaries（41 包）+ luka typecheck（5.2s）全过。并行会话的 location 改动已由它自己提交，未混入。
- **可复用经验** → `30-Resources/troubleshooting/rn-cross-subtree-z-order.md`。

### 13:50 · sitin-rn：luka 划卡卡角小崽撤掉（信息带保留毛玻璃底）

- **用户反馈**：「去掉这个仓鼠吧」—— 上一轮把信息带搬到小崽之上后，用户直接决定**撤掉卡角那只小崽**（`ParkPassHamster`），不再用层级去让。
- **改动**：`git checkout 6d8baefec~1 -- <6 files>` 把上一轮的 `renderChrome` 接缝整段回退（`card-stack.tsx` 的 `renderChrome`/`CHROME_Z`、`discovery-deck.tsx` 的小崽与 `HAMSTER_*`、`candidate-card.tsx` 的信息带迁出、`nearby-view.tsx` 的注释），只保留 `candidate-card-info.tsx` 的**毛玻璃底**（iOS `BlurView` 60 + 72% 纸色雾面 / Android 90% 雾面）与 `docs/discover-play.md` 的注记。信息带回到卡片子树里，`onInfoLayout` 也回到卡片 → 拖动/划走不再有「下一张暂时没有信息带」的过渡帧。净 diff 只有 2 个文件（+69/−42）。
- **验证**：luka typecheck 0；biome 干净；全量 `1035 passed / 7 failed`（与基线逐条一致）。仍未实机（本机无 Simulator.app）。
- **提交 / 推送**：`643bc78bc` `fix(luka): 撤掉划卡卡角的小崽，信息带改毛玻璃底`（6 files，+74/−135，含回退）→ `origin/feature/luka-ios`（`6d8baefec..643bc78bc`）；pre-push 全过。
- **留档**：`ParkPassHamster` 组件与素材（`park-hamster-scratch.mov/webm`）**留在仓库里**，票卡玩法（`NearbyTicketPlayfield`）将来重挂时还能用 —— 与 discover-play 那一批「不再挂载但保留」的组件同样的处置。
- **⚠️ 14:0x 打脸修正**：上面那次「回退到 `6d8baefec~1`」**把小崽又装了回去** —— 小崽原来就挂在那份改动之前的代码里（`nearby-view` 的 deck 分支），回退等于还原它。用户回「还是在」后查 `grep ParkPassHamster` 才发现。补 `6a31e4b4f`（1 file，+5/−28）才真正删掉：`nearby-view` 去小崽浮层 + `DECK_HAMSTER_HEIGHT` + 只为定位它才用的 `cardInset` / `screenWidth`。**教训**：用 `git checkout <rev> -- <files>` 做「部分回退」时，先确认目标 rev 里没有你要删掉的东西 —— 回退不是「删除」。

### 14:25 · sitin-rn：luka 划卡简介区域回到原纸色（毛玻璃整段回退）

- **用户反馈**：「简介区域的颜色也恢复吧」—— 小崽撤掉之后，毛玻璃底没有存在的理由了，要求回到原来的纸色底。
- **改动**：`git checkout 868535448 -- apps/luka/src/components/discovery/candidate-card-info.tsx` 整体回到改动前（`BlurView` + 雾面参数 + 内层 padding 拆分一并去掉），`docs/discover-play.md` 现状段改成「小崽撤掉；毛玻璃试过一版、看过实机后**也回到纸色**」。`ab5d9804a`（2 files，+43/−70）。
- **本轮净效果**：对照改动前（`868535448`）只剩下 **2 个文件** —— `nearby-view.tsx` 少掉卡角小崽浮层（+ `DECK_HAMSTER_HEIGHT` / `cardInset` 两个只为它存在的量），加上这条文档注记。划卡其余部分与改动前逐字一致。
- **验证**：luka typecheck 0；biome 干净；全量 `1041 passed / 7 failed`（总数因并行会话新增 fallback-avatar 测试多 6 条；7 条失败与基线一致）。仍未实机。
- **提交 / 推送**：`ab5d9804a` → `origin/feature/luka-ios`（`6a31e4b4f..ab5d9804a`）。（`git pull --rebase` 因并行会话在工作区里有未提交改动而拒绝，远端没有新提交，push 直接成功。）

### 14:00 · sitin-rn：出 luka test/preview IPA（含今天全部前端改动）

- **做了什么**：用户「打一个ipa包」→ 先核 `HEAD = origin/feature/luka-ios = 6a31e4b4f`（今天 13:53 最后一条，工作区干净）、`a759a0692..HEAD` 里原生相关只有 `package.json` 脚本文案与 `app.config.ts` 注释/运行时 `appName` → 复用 `ios/` 不 clean，打 test + preview（Ad Hoc）。命令同昨天：`export PATH="$HOME/.nvm/versions/node/v24.21.0/bin:$PATH"; printf 'n\n' | IOS_TEAM_ID=39CFYH6W55 scripts/build-ipa.sh luka test preview "" 10001`。
- **结果**：`apps/luka/ios/build/export/Luka.ipa` 71 MB（74,046,215 bytes），`com.presence.gracechat` 1.0.0 (10000)，Ad Hoc；`extra.apiBaseUrl=https://api-dev.lukasap.com`、`appName=luka`、`dataSource=live`；产物 grep `See all moments` ✓（10:53 moments 那条）、`deckSkipTargetFromCurrent` ✓。日志 `apps/luka/build-logs/build-20260922-135518.log`。**未上传蒲公英**。
- **注意**：buildNumber 参数仍无效（luka 是 `app.config.ts:15` 常量 10000），与前两版同号；核验/坑见 `30-Resources/troubleshooting/sitin-rn-local-ipa-verification.md`。

### 14:11 · sitin-rn：Me 页头像立即用随包 CAI 兜底占位（注册未上传头像场景）

- **用户反馈（09-22）**：「【注册】未上传头像，在 you are all set 页应给一个兜底的动漫头像；me 页面给了动漫头像，但是出现的太慢」。两件事拆开：① all set 页兜底 **09-21 已实现**（`df9b16d6c`，本次只把映射抽成公共模块，行为不变）；② Me 页「慢」= 头像 `avatarUrl` 是后端分配的**远程 CAI 图**，首次下载要时间，`Avatar` 在加载完成前显示首字母。
- **改动（8 files，未提交）**：
  - 新 `apps/luka/src/lib/fallback-avatar.ts`：`FALLBACK_AVATAR`（按 `Gender` 字面量）+ `fallbackAvatarForProtoGender`（proto 枚举，session snapshot 用）；素材仍是 `assets/onboarding/cai-avatar-{male,female}.webp`，nonbinary 按 female 兜，未知性别不兜（宁可不给也不给错）。
  - `components/ui/avatar.tsx`：新增 `placeholderSource` —— 主图未加载（或最终失败）时画本地兜底图，**替代首字母**；与 `source`（替换主图）语义区分。
  - `components/me/profile-portrait.tsx`：加 `fallbackSource`，接到 `Avatar` 的 `placeholderSource`。
  - `app/(tabs)/me.tsx`：`useAuthStore.userInfo.gender`（proto 值）→ 兜底图传给 `ProfilePortrait`。
  - `app/onboarding/summary.tsx`：删本地 `FALLBACK_AVATAR` 定义，改用共享模块。
  - `assets/README.md` 目录表 `onboarding/` 行补 Me 页；测试：新 `tests/lib/fallback-avatar.test.ts`（3 条）+ `tests/design/onboarding-fallback-avatar.test.ts` 加 Me 页守卫 3 条。
- **验证**：`pnpm --filter luka typecheck` 0；biome 7 files 干净；新测试 11 passed；全量 `1041 passed / 7 failed`（既有红：good-review、shape-language ×3、candidate-card-fields、use-swipe-actions ×3，与基线逐条一致）；Metro bundle（`.expo/.virtual-metro-entry.bundle`，Metro cwd = `apps/luka`）200 且含 `fallbackAvatarForProtoGender` ×3 / `placeholderSource` ×4。
- **待确认**：用户若在**新包**上仍说 all set 页没有兜底头像，需查他装的是不是旧包（09-21 14:00 之后的 IPA 才含 `df9b16d6c`）；dev 包 reload 即取到本轮改动。**未实机**（本机无 Simulator.app，视觉留真机走查）。
- **14:20 提交并推送（用户要求）**：`78c8e8bc7` `fix(luka): Me 页头像加载前先显示随包 CAI 兜底，不再退回首字母`（8 files，+125/−22）→ `origin/feature/luka-ios`（`ab5d9804a..78c8e8bc7`，fast-forward；`ab5d9804a` 是并行会话的「划卡信息带回到原来的纸色底」）。pre-commit biome 7 files、pre-push boundaries（41 包）+ luka typecheck（6.0s）全过。`-` 未跟踪文件未动。

### 14:50 · sitin-rn：luka 选择性别页在 SE 上重叠、不可滚（内容区补 ScrollView）

- **用户反馈（附图）**：SE 机型注册第 3 步 gender「How do you identify?」内容重叠、无法滑动 —— 第三行 `I'm non-binary` 被贴底 Continue 胶囊盖住一半。
- **根因**：`OnboardingScaffold` 的中间区是 `min-h-0 flex-1` 的**普通 `View`**，`flex-1` 只分高度、**不裁剪也不滚动**。这一屏内容（仓鼠 `mt-6`+112 + 三行固定 64pt + `mt-[34px]`）在 375×667 上约 394pt，而中间区只有约 374pt → 溢出 20pt 画到 CTA 底下。其它选项屏（looking-for / faith / drink / smoke / who-you-meet）本来就带 ScrollView，只有 gender 用裸 `View`。
- **改动（2 files）**：
  - `apps/luka/src/app/onboarding/gender.tsx`：裸 `View className="mt-[34px] gap-4"` → `ScrollView className="mt-[34px] min-h-0 flex-1" contentContainerClassName="gap-4 pb-4" showsVerticalScrollIndicator={false}`（与其余选项屏同构）；注释写明「固定行高 + 小屏溢出」的原因，不再只留 Figma 坐标那句。
  - `apps/luka/tests/design/onboarding-flow.test.ts`：新 describe「选项屏的滚动区」1 条 —— 断言 `IdentityOptionRow` 落在 `ScrollView` 内（判结构不判正文）。
- **验证**：`pnpm luka:typecheck` 0；biome 2 files 干净；`onboarding-flow.test.ts` 12 passed；全量 `1042 passed / 7 failed` —— 7 条为**基线既有红**（good-review、shape-language ×3、candidate-card-fields、use-swipe-actions ×3），已 `git stash` 后单跑那 4 个文件逐条复核确认与本次无关。**未实机**（本机无 Simulator.app / 无 SE 真机，视觉留真机走查）。
- **沉淀** → `30-Resources/troubleshooting/rn-pinned-cta-overlap-small-screen.md`（贴底 CTA + 固定高度内容的小屏溢出这一类；含尺寸自查法与最小机型判据）。
- **提交 / 推送（用户要求）**：`84eb5b3d9` `fix(luka): 性别选项列表改可滚动，SE 上不再被 Continue 压住`（2 files，+24/−4）→ `origin/feature/luka-ios`（`78c8e8bc7..84eb5b3d9`，GitLab HTTPS）。pre-commit biome 2 files、pre-push boundaries（41 包）+ luka typecheck（6.6s）全过。

### 16:08 · sitin-rn：Luka 照片编辑页 redesign —— image2 出三版方向对比

- **需求**：用户（飞书发实机截图）要求用 `image2-gen` 重新设计 luka 照片编辑页。定位到实现 `apps/luka/src/app/edit-profile/photos.tsx` + `components/edit-profile/photo-well.tsx`（Figma 122:646 / 656 / 657）。
- **图源复原**：用户贴的图不在文件系统（飞书缓存路径已失效），按 `troubleshooting/opencode-desktop-image-extraction.md` 从 `~/.local/share/opencode/opencode.db` 的 `part` 表按 `message_id` 精确解出 base64 → `/tmp/image2-refs/avatar-edit-original.png`（924×2000）。
- **设计口径**（读代码取真值）：KEEP = 暖奶油 `#FDF9EE` + 墨棕 `#3A2317` + Nunito 圆体 + 全部英文文案逐字 + 同一插画；三方向 = A 照片即主角（照片近全宽、改照片收进照片本体）、B 手帐拍立得（相框/胶带/手写 this is me/印章/撕纸 tip）、C 极简留白（细线、描边 pill、无装饰）。三版都去掉顶右蓝齿轮（dev client 悬浮按钮，非设计元素）。
- **产出**：3 张 1024×1536 → `/Users/colna/Pictures/Luka- 物料/opencode-image2/photos-edit-{A-hero,B-journal,C-minimal}.png`；prompt 在 `/tmp/image2-refs/prompt-photos-{A-hero,B-journal,C-minimal}.txt`。三版均通过验收（文案逐字正确、齿轮已去）；A / C 顶部返回箭头 `‹` 丢失（mockup 层面的小差异，落地时按现有实现保留）。
- **坑**：三条图生图**并行**发 → 1 条成功、2 条 `RemoteDisconnected`；改串行后 183s / 277s 正常。沉淀 → `30-Resources/troubleshooting/muskapi-image2-relay.md` 新增「不要并发」节。
- **状态**：未改任何代码；等用户选定方向后再落 RN 实现。

### 16:25 · sitin-rn：Luka 照片编辑页 A 版落地（照片通栏 + 手写体 + 圆胶囊）

- **需求**：用户从三版 image2 稿里选定 A 版（照片即主角），要求落到 RN，且「两个入口都改」。
- **实际入口只有一个**：`PhotoWell` 在 luka 只有一个消费方（`app/edit-profile/photos.tsx`）；代码里反复提到的「home avatar-nudge sheet」是**过期注释**（luka 的 nudge 是 store + 卡，不嵌照片位），本次把 `photo-well.tsx` / `photos.tsx` 的相关注释一起改对了。
- **改动（5 files ±，未提交）**：
  - `components/edit-profile/photo-well.tsx` 重写：200×240 固定块 → 通栏竖幅（`aspectRatio 1/1.2`，5:6 裁切不变）、`rounded-[22px]`（与注册流 `PhotoDropzone` 同档）、「Change photo」从照片下的文字链变成压右下角的白圆胶囊（空态同控件换成「Add photo」）、「this is me ✦」搬进照片左下角换 **Caveat 手写体**（-2°）。触控从 gorhom `TouchableOpacity` 换回 RN `Pressable`（没有 sheet 了）。
  - `app/edit-profile/photos.tsx`：内容区改 `ScrollView`（通栏后在 SE 上会溢出，见 troubleshooting 那条的「第二个实例」），Save 钉在滚动区外，并换成 `ContinueButton` 可可棕胶囊（与 Location / Bio 同一颗，替掉模板的 `floating-gradient` 3px 按钮）。
  - `app/_layout.tsx` + `theme/fonts.ts`：**重新加载 Caveat**（`_layout` 原注释写着「no screen uses it today；re-add when one does」）。
  - `tests/design/shape-language.test.ts`：把这两个文件加进 MIGRATED 进度条。
  - 新增 `tests/design/edit-profile-photos.test.ts`：守通栏 22px、pill 双文案、手写体与字体加载成对出现、Save 在 ScrollView 之外，4 条。
- **照抄稿子 vs 按本仓库语言改**：稿子照片下的「软投影」**没有落**（Luka 形状语言无投影，`shape-language.test.ts` 有守门）；叠字改为照片下沿压一层暖墨渐隐（用户会选任意底图，浅色图上奶油色手写体会消失）。
- **验证**：`biome check` 6 files 干净；`tsc` 我这几个文件 0 错（仓库唯一 typecheck 错在并行会话未完成的 `tests/lib/chat-list.test.ts`，不是本次）；全量 `1052 passed / 7 failed` —— 与今天基线逐条一致（shape-language ×3、candidate-card-fields、use-swipe-actions ×3）。Metro bundle 200 且含 `Caveat_400Regular` / `this is me`。**未实机视觉复核**（本机无 Simulator.app）。
- **待用户裁决**：`AiTip` 的 chip 仍是 3px 直角（共享组件，bio / occupation / education / interests / location 都在用，动它会连带改 5 个已迁移子页），与新屏的圆角不搭 —— 要不要单开一笔把它迁到 12px。

### 16:27 · sitin-rn：luka 房间访客卡「有时不出现」——快照快速判定 + 失败分类重跑

- **来源**：用户报「chat 页会出现只有右上角徽标、头像卡片没出来」+ 实机截图（Android 360×780，924×2000 缩放 2.565 px/pt）。
- **先排除截图的干扰**：那个悬空的「42」不是访客卡徽标，是 **Waves（42 条待回应招呼）入口的徽标** —— 位置与 `NestLetterStack` photoRoom 热区右上角公式（`left 1% + 22%×0.94`、`top 46% + 12%×4%`）在 x/y 两轴都吻合到 0.5pt；底图里那堆信封在 51–55%，所以徽标比信封高约 45pt，看着像孤儿徽标。判据：访客卡徽标有 2pt 奶油描边，实测那一圈是沙发橙（无描边）。房间两张卡（Jessica/Emma）都在。
- **根因（代码）**：房间卡片 = `conversationReplyStates` 为 `replied` 的会话（`docs/chats-play.md` 锁着的规则），而结论**只**来自 `classifyConversationReply` 的 per-conversation TIM 历史扫描（4 并发、失败只 `console.warn`，要等下次会话列表推送才重试）→ 冷启动/切回 Chats 时状态还空着，房间只剩页头徽标 / See all / Waves，卡片一张不出；请求失败的会话可能长期缺卡。
- **改法（用户选 A：快速判定 + 失败重试）**：
  - `packages/rn-tim/src/index.ts`：`TimConversationVM` 加 `lastMessageInbound`（`flow === "in"` 且 `!isSilentMessage` 且非通话记录 —— 与 `classifyConversationReply` 同一套判据，不会比分出更多）。
  - `apps/luka/src/lib/chat-list.ts`：新 `withSnapshotEstablishedStates` —— 用列表自带的两个**充分**条件（`unreadCount > 0` 或 `lastMessageInbound`）直接判 `replied`；只补不降、无变动返回原引用（`applyConversations` 每次推送可无条件调用，不空转重渲）。
  - `apps/luka/src/stores/chat.ts`：`applyConversations`（会话列表唯一入口）内应用；新增 `retryUnknownReplyClassifications` 动作（只跑没有结论的，`status !== ready` 时不动作）。
  - `apps/luka/src/app/(tabs)/index.tsx`：focus 到 Chats 时调用重试。
- **验证**：`pnpm typecheck`（全仓 7 workspace）exit 0、0 条 TS 错；`pnpm boundaries` OK（41 包）；`pnpm --filter luka test` **1056 passed / 7 failed**，7 条与基线逐条一致（good-review 套件级 + shape-language×3 + candidate-card-fields + use-swipe-actions×3）；`@heyhru/rn-tim` 5 文件 58 passed。新增 10 条：`tests/lib/chat-list.test.ts` +6、新 `tests/stores/chat-reply-states.test.ts` 4 条（快照判定不触发历史扫描 / `pending` 不被误清 / 失败分类重跑 / 未 ready 不动作）、rn-tim `conversation-preview.test.ts` +6（撤回、静默信令、通话记录都不算）。
- **文档**：`apps/luka/docs/chats-play.md` 数据规则补「`replied` 的两条来源」+ 测试清单。
- **未做 / 待定**：未实机（iPhone X 模拟器被并行会话占着；改动是数据层，出卡链路已被单测锁住）；**未 commit**（规范模式，等用户确认）。工作区另有并行会话未提交改动（头像编辑页一套 + `shape-language.test.ts`），未纳入。

- **16:37 提交 / rebase / push（用户要求）**：`git commit --only` 只带本次 8 个文件 → `55b83d33a`；`git pull --rebase --autostash` 并入远端 3 个提交（`feature/luka-ios` 上刚从 `personal/caobo/luka-ios-develop` 合入的「联系方式交换与通话流程」+ 并行会话的照片编辑页），冲突只有 `packages/rn-tim/tests/conversation-preview.test.ts`（两边都在文件末尾追加 describe）—— 保留双方，rebase 后哈希 `8431ac212`；autostash 恢复后并行会话的 3 个在改文件 sha256 与 rebase 前逐字节一致（已比对）。rebase 后复验：`pnpm --filter luka typecheck` 0、luka 全量 **1102 passed / 7 failed**（仍是那 7 条基线红：good-review 套件 + shape-language×3 + candidate-card-fields + use-swipe-actions×3，rebase 无新增）、`@heyhru/rn-tim` 20 passed。**push 时远端已在 `8431ac212`**（并行会话先推了同一分支）→ 最终 `origin/feature/luka-ios = 5a72f69fe`（含我的 `8431ac212` 与并行会话的 `c48e12259` / `5a72f69fe`），本地与远端 0/0。**注意**：这条分支被多个会话共用，tip 上混着别人的提交。


### 16:36 · sitin-rn：luka 编辑资料 Bio / Education（+ Interests）标题被仓鼠遮挡修复

- **来源**：用户实机截图 —— `edit-profile/bio` 的「What makes you, you?」与 `education` 的「Where did you study?」结尾被探头仓鼠盖住。
- **根因**：与 09-21 Location 同一处几何 —— `PeekingHamster` 可见部分伸到卡片上沿之上约 82pt，而标题下沿到卡片顶只有 `pb-5`（20pt），仓鼠绝对定位不参与布局，标题尾部正好落在它那一列。
- **改动（3 文件，各一处 className + 注释）**：`bio.tsx` 的输入框容器 `relative mx-5` → `mx-5 mt-16`；`education.tsx` `<EditorCard className="mt-16">`；`interests.tsx` 同类风险一并收口 `mt-8` → `mt-16`（离线合成证明它的「?」也被压住）。`mt` 必须加在容器上（不能 padding，仓鼠锚 padding box 顶）。
- **验证**：`biome check` 3 文件干净；`pnpm --filter luka exec tsc --noEmit` 0；`luka` 全量 `1056 passed / 7 failed`，7 条与 16:27 基线逐条一致。**未上模拟器**（本机 dev 包停在 onboarding，无 session，深链被守卫挡住）—— 改用真字体 + 视频抽帧的 4× 离线合成，逐屏对比 M=0/64 确认修后无遮挡（方法见 [[design-mock-measure-and-sample]] 的「探头角色绝对定位」节，本次已把那族的量法补进去）。
- **提交 / 推送**：`5a72f69fe` `fix(luka): 编辑资料 Bio / Education / Interests 标题让开探头仓鼠`（3 files，只 stage 这三份），已推 `feature/luka-ios`（`8431ac212..5a72f69fe`）；pre-push（boundaries + luka typecheck 5.8s）通过。

- **16:35 提交并推送（用户要求）**：`c48e12259` `feat(luka): 照片编辑页按 A 版稿改版——照片通栏、Change photo 收进照片角、Save 换可可棕胶囊`（6 files，+155/−94）→ `origin/feature/luka-ios`（`b414c5cf9..8431ac212`）。pre-commit biome 6 files、pre-push（boundaries + 受影响 workspace typecheck，含 `packages/rn-tim`）全过。
  - **并发插曲**：首次 push 被拒（远端已被并行会话推进 3 个提交）；随后并行会话在同一个工作区执行 `git pull --rebase`，**把我这一笔一起 rebase 了**（本地 SHA 从 `4f9f64a4c` 变成 `c48e12259`），并在 `packages/rn-tim/tests/conversation-preview.test.ts` 冲突过一次后自行解完。push 时栈上带着并行会话的 `8431ac212`（房间访客卡）一并上去 —— 同一分支，不能只推自己那一笔。
  - 只 stage 了本次 6 个文件；工作区里并行会话未提交的改动（`bio/education/interests.tsx`、`chat-list`、`chat` store、`rn-tim` 等）原样保留未动。

### 16:45 · sitin-rn：T0048 复核 —— 截图的差值是口径差，不是陈旧（A 修不了）

- **来源**：用户转发 T0048（飞书 buglist base `MbjobIs4Qax91fsXfUWc9Z4WnKd` / table `tblywHQVyR7KIGC9`，record `recvvPE7rfIeRO`）+ 截图。记录态：09-22 11:07 赵延杰附 png 并打回 → 11:20 张峥标「已解决待验证」→ **16:26 赵延杰又打回「未开始」（无新附件，仅状态变更）**。
- **附件比对**：下载 record 的 png `KEDfbagnbo9ALcxvE2wcb3pKnHd` = 用户发的这张。**设备时钟慢 15h**（09-21 视频帧 05:57 ↔ 实际上传 20:58），png 状态栏 20:06 ≈ 09-22 11:06 实拍 —— 即附上那一刻；当时 A（`bb37c2ad3`，10:55 才落地、11:17 才 push 成功）还没进包。视频帧另证：图里徽标定位已修（`84a5b1107` 之后）。
- **结论（口径差，非陈旧）**：
  - Activity 徽标 6 = 服务端 `totalUnreadLikedUserCount`，**含** `filterOutMatched` 从列表隐掉的「已在聊」的人 → 会大于列表行数（截图 3 行）；
  - Visitors 徽标 9 = `GetNewVisitorCount(visitorTime)` 服务端计数，列表是全量 `GetVisitorList` → 两数不同源（09-21 视频里还反着差过：徽标 1、列表 2 个红点）。
  - 两处都是 09-21 记录的「已知残差 / 两套来源」，A（新鲜度）只解决爪印停旧数，解决不了同一时刻的差值。
- **待用户裁决**：B1 徽标与列表同源（likes 扣隐掉的人、visitors 弃 `GetNewVisitorCount` 改本地算，paw 刷新变重）；B2 Activity 列表不再隐人（推翻 09-21 裁决）；B3 维持现状、回测试说明。另需确认赵延杰 16:26 测的是不是今天 13:55 的 IPA（含 A）。
- 核实手法：`lark-cli base +record-search/+record-history-list/+record-download-attachment`（字段名先 `+field-list`）。

### 16:48 · figma-write-bridge：Luka contact 模块四屏分层复刻（64 · Inbox / 65 · Exchange waiting / 66 · Exchange revealed / 67 · Link opened）

- **需求**：续作指令 —— 用同一套 DSL 把 `apps/luka/src/app/contact/` 四屏按代码重建成 Figma 节点树（不是贴图），画布 390×844、只用 `lib/` 原语；不动 `lib/`、`plugin/`、`screens/` 其它文件，也不动 sitin-rn 仓库。
- **产出（4 个新文件，均在 `~/WORK/figma-write-bridge/screens/`）**：
  - `64-inbox.mjs`：ScreenHeader（‹ 32/36 + 30/36「Chats」）+ 四条 ThreadRow（Avatar 52 + 名字 16 + `score>0` 才画的 BondChip + 末条 13.5 + 右列 relTime / 9pt 未读点）。数据取 `mock/fixtures/chats.ts` 的四条线程（与 44 · Chats 同一批 peer，时间 38m/1d/2d/6d），Bond 分 86/240/12/0。
  - `65-exchange-waiting.mjs`：ExchangeScreenHeader（48 行）+ 两枚 84 色盘头像（`mr-[-16px] z-10`，You 在上）+ 标题 / 副标题 + TIME LEFT TO REPLY 倒计时卡（1h 57m）+ YOU SHARED 白卡（Instagram / Snapchat 两张）+ 「Send Macy a nudge」。
  - `66-exchange-revealed.mjs`：NEW CONNECTION 胶囊 + 「Macy left you a link」+ 白卡（80 头像 + 28 蓝解锁徽标 + @macy.moves 的 RevealedContactCard）+ 贴底「Keep chatting on Luka」胶囊（742..794，safe-area 34 + pb-4）。
  - `67-link-opened.mjs`：LINK OPENED 胶囊 + 标题 + 340×300 头像块（蓝底 #1D4ED8 + 右上 ✓ 32 徽标 + 左下名字）+ 白卡（Instagram 40 图标 / @macy.moves + Copy 描边 / Open 实心）+ 结尾语（CLOSING_LINES[0]）+ Back to the chat ›。
- **关键做法（沿 troubleshooting 的既有结论）**：
  - Instagram 图标没有 `lib/` 原语，直接内联 `social-icon.tsx` 的 16pt 源路径：描边两条合成一个 VECTOR、实心点另起一个 VECTOR；**每个 VECTOR 的声明尺寸取该路径自己的几何 bbox、`viewBoxSize` 同值**（见 `figma-write-access.md` 2026-09-15「VECTOR 的 bbox / resize 语义」），否则 5.7 单位的镜片圆会被 `resize()` 撑满。66/67 各带一份局部 helper（不改 lib）。
  - 坐标层级坑自查：用临时 walker 打绝对坐标，抓到 64 的 header 子节点误用绝对 y（写在 group 里被叠加了 54）与 66 里 peerCard 的 `rowY` 混用屏坐标 —— 已改成组内局部坐标。
- **验证**：四屏 `node -e "import('./screens/<f>.mjs').then(m=>{const s=m.build();console.log(m.label,'children',s.children.length)})"` 全过（7 / 9 / 5 / 9 children）；另跑自写 walker 逐节点核绝对坐标无越界、无负数。**未推送 Figma**（本轮没要求，bridge `:3055` 当时未起）；**未建 group 文件**（按要求不改 `screens/` 其它文件，等后续统一挂进 13/16 那类 group）。
- **假设（已在返回里列出）**：① 会话数据用 fixtures 的四位 peer（真人数据来自 TIM，本地无种子）；② 65 的「YOU SHARED」两张卡与 66/67 的 handle（@macy.moves、@alex.*）是自造 mock（接口无 fixture），用户 Alex 取自 `fixtures/me.ts`；③ 66 的卡是全宽（代码里 card 不在 `items-center` 包裹内，`w-full` 行成立）；④ Macy 头像沿用 20 · Other profile 的 person-04；⑤ 67 的可选 `subtitle` 参数按注释「omitted rather than invented」不画。

### 16:53 · figma-write-bridge：Luka 启动/开白四屏分层复刻（60 · Splash / 61 · Who you meet / 62 · Faith / 63 · Intro video）

- **需求**：同一套 DSL 续作 —— 按代码把 `apps/luka/src/` 四屏重建成 Figma 节点树，390×844 绝对定位、只用 `lib/` 原语；不动 `lib/`、`plugin/`、`bridge.mjs`、`screens/` 其它文件，也不动 sitin-rn 仓库。
- **产出（4 个新文件，`~/WORK/figma-write-bridge/screens/`）**：
  - `60-splash.mjs`：mascot 244×434（Figma 291:907）+ 68/85 字标「Luka」+ 15/21「Small friend, Big Happiness」，整列 548 高居中 → 顶部 y=148；文案出自 `copy/brand.ts`。
  - `61-who-you-meet.mjs`：scaffold 外壳（‹ + 06/10 + YOUR CIRCLE + 两行标题 + 两行副标）+ 3 张 `layout="portrait"` 的 ChoiceCard（106×156，y=326）；「Women」选中态（honey 底 + caramelLine 边 + caramel 圆 + 白 ✓）。
  - `62-faith.mjs`：步骤 10/10、CTA「Make it mine」；7 张 `layout="compact"` 卡（165×100，两列 + 第 7 张 342 全宽）y=300；卡组总高 448 > 视口 428，套一层裁剪 FRAME（`Choice deck · Scroll viewport`，300..728，末卡在 CTA 上沿裁断 = app 首帧态）。
  - `63-intro-video.mjs`：夜空底 `#0B1020` + `wake-plate.jpg` 底板 + 视频占位帧 + light 状态栏 + 「AUTO PLAYING」药丸（honey 6pt rec 点，**wake-rec.svg 是 honey 不是红**）+ 40pt 关闭键 + 两行白字（26/32、14/20，块底 730 = 34 home indicator + pb-20）。
- **关键做法 / 坑**：
  - **文案折行先用真字体量**：临时 swift + CoreText 量 `node_modules/@expo-google-fonts/nunito` 的 ttf ——「Who would you like to meet?」410.3pt、「Do you practice a religion?」369.7pt 都 > 342，两屏标题都必须两行（钉 `\n`，h=82）；副标 462.5pt → 2 行、271.6pt → 1 行；全部卡面文案在卡内宽（74 / 133）单行。
  - 两张卡片的度量自 `choice-card.tsx`：portrait minHeight 156（内容只 101，地板值生效）、compact 恰 100（17+34+12+20+17）；圆牌 34、内边距 16；编号是 Space Mono 10 / ls 0.8，选中换 `checkmark`。
  - Ionicons（female/male/people/compass/heart/sparkles/moon/star/leaf/ellipsis-horizontal-outline）不在 `prims` 的 FEATHER/IONICONS 映射里 → 屏内直接 `import { iconSpec, ioniconsPath } from '../lib/svgicon.mjs'` 包一个局部 `ion()`（不改 lib）。
  - **63 的视频占位是抽帧**：`ffmpeg -ss 7.5 -i src/assets/pet/open2.mp4`（720×1280、8s）取「仓鼠在跑轮里立起看镜头」那帧 → 新素材 `images/app/pet/open2-frame.png`（bridge `:3055` 按需读盘，插件侧 fetch 即可用）；没用 `luka-welcome.png`（那是 luka.mp4 的海报，01-sign-in 在用）。
- **验证**：四屏 build() 全过（children 3/5/5/6）；另跑临时 walker 打绝对坐标，四屏无越界/负坐标、无与 CTA 意外叠压（62 的重叠由裁剪视口收纳）。
- **假设（已在返回里列出）**：① 进度条按**当前** `LONG_FLOW` 画 06/10 与 10/10 —— 既有 28/29/30 写的 /9 是 09-20 改流程前（identify 删、faith 增）的旧值；② 两屏都画「首项选中 + CTA 可点」态（store 初值为 null，未选中 + 禁用 CTA 是另一可达帧），沿用 27/28/29/30 的取样惯例；③ ChoiceCard 未选中卡的 ±0.9° 倾斜 DSL 表达不了（无 rotation），画正值；④ 未建 group 文件（按要求不改 `screens/` 其它文件）。

### 16:58 · figma-write-bridge：Luka 动态/帖子四屏分层复刻（68 · Match reveal / 69 · Activity / 70 · Post detail / 71 · Profile posts）

- **需求**：同一套 DSL 续作 —— 按代码把四屏重建成 Figma 节点树（不是贴图），390×844 绝对定位、只用 `lib/` 原语；不动 `lib/`、`plugin/`、`bridge.mjs`、`screens/` 其它文件，也不动 sitin-rn 仓库。
- **产出（4 个新文件，`~/WORK/figma-write-bridge/screens/`）**：
  - `68-match-reveal.mjs`：`REVEAL_GRADIENT` 三段同色（#EFF3FD，画平色）；居中组 = IT'S A READ（11/15 ls4）→ ✦（26/32）→ 两张 138×160 竖幅卡（自己那张 left-4 top-4 一颗 30 主色实心心）→ 两行 28/34 标题 + 两行 16/22 副题；贴底白底「Message Sofia」56 + 「Keep reading」44。居中区间 54..686（CTA 块 124），内容 391 → 顶在 174.5。
  - `69-activity.mjs`：白底；页头 40 白返回方 + 28/34「Activity」+ 12pt「RECENT VISITORS」；7 行访客（52 锁定头像 + 未读红点 ×2 + 「Checked out your profile [N times]」+ 相对时间 + 右端 Unlock 胶囊），行高 85/76；贴底 358×52 实心「See Who Visited You」（y=738 = 屏底 34 安全区 + 20）。
  - `70-post-detail.mjs`：PostDetailHeader（返回 26 + 32 头像 + 17 名字 + 20×4 MoreDots）→ 全宽照片 390×261.7（sofia-climb 1280×859）→ 21/28.6 标题两行（单行 body 经 `splitPostCaption` 整段当 lead，无 15pt 正文）→ 贴底点赞栏（y=754，h-14 + 34 安全区，描边心 + 127）。
  - `71-profile-posts.mjs`：ScreenHeader（‹ 32/36 + 30/36「Sofia's posts」）+ 两卡 `ProfilePostCard`（38 头像 + 15 名 + `· 14h`/`· 6d` + 15/21 正文 + 单图 326 宽/圆角 16 + 发丝线 + 描边心 127/174）；第二卡下沿 950 被 844 画布裁断。
- **关键做法 / 坑**：
  - **折行全部用真字体实测**：写了个临时 `swift + CoreText` 小工具注册 `node_modules/@expo-google-fonts/nunito/*.ttf` 量宽 + 贪心折行（同时量了 15px→20.46、12px→16.37、13px→17.73、21px→28.64 等默认行高）。结论：70 的 21pt 标题在 358 宽折 355.2/354.9；71 的 15pt 正文在 326 宽折 320.0/158.4、304.0/203.7；69 的 15pt 文案在 209 宽（= 390 − 40 − 52 − 12 − 77 胶囊）把「Checked out your profile 3 times」折成 180.5/37.4 —— 带次数的行因此比 52 头像高，行高 85 而非 76。
  - **坐标层级自查抓到 2 处真错**：临时 walker 打绝对坐标时发现 70 的 header 子节点误用绝对 y（写在 group 里被 54 叠加）、71 的列表卡 y 用了屏坐标（组在 y=144 处再叠加一次）。已改成组内局部坐标 —— 与 64/66 那次是同一个「坑」。
  - **SafeAreaView 下 absolute 的底边**：69 的贴底 CTA 是 SafeAreaView（padding 34）的 absolute 子节点，RN 0.86 的新 Yoga 默认把 padding 算进定位 → bottom-5 落在 810−20=790（不是 824）；70 的点赞栏不吃 SafeAreaView 而是 `paddingBottom: useBottomInset()` → 754 起。
- **验证**：四屏 `node -e "import('./screens/<f>.mjs').then(m=>{const s=m.build();console.log(m.label,'children',s.children.length)})"` 全过（5/6/6/4 children）；walker 逐节点核 x/y/w/h 无 NaN、无越界；另核 10 个图片引用路径在 `images/app/` 下全部存在。**未推送 Figma**（本轮没要求，bridge `:3055` 未起）；**未建 group 文件**（按要求不改 `screens/` 其它文件）。
- **假设（已在返回里列出）**：① 人物取 fixtures（自己 = `MOCK_SELF` 的 Alex + self.jpg；对方 = Sofia/person-04），**不沿用 20 屏的「Macy」**（那名字不在代码里，48/45/43 用 person-04 时 18/47 已按 fixtures 叫 Sofia）；② 68 的揭晓对象取 Sofia（与她在 47 的 liker、71 的帖子一致），实机取决于路由/stash；③ 69 的 7 位访客、次数与 2/7/20/31/52/77/96h → 2h…4d 全部逐字来自 `focus.ts`，前两位 = `GetNewVisitorCount` 的 2 个新访客（红点）；④ 71 的 `timeAgo` 取设计读法（14h/6d），mock 的 BASE 是固定时间戳、实机 `formatTimeAgo` 会算出更大的天数；⑤ 70 的照片比例用真图（1280×859）而代码 fallback 4/3 只在加载前生效；⑥ 锁定头像没有模糊原语，按 48 屏口径用「panelWash 圆盘 + 55% 照片」近似；⑦ 70 的 `MoreDots` / 两屏的 back chevron 分别以 feather 的 more-horizontal / chevron-left 顶替（Entypo/Ionicons 不在 `prims` 映射里）。

### 16:52 · figma-write-bridge：Luka 付费/账户四屏分层复刻（72 · Who liked you / 73 · Manage subscription / 74 · Deletion in progress / 75 · Delete account）

- **需求**：同一套 DSL 续作 —— 按代码把 `apps/luka/src/` 四屏（`app/paywall/likes.tsx` + `components/payment/`、`app/manage-subscription.tsx`、`app/account-deletion.tsx`、`app/settings/delete.tsx`）重建成 Figma 节点树，390×844 绝对定位、只用 `lib/` 原语；不动 `lib/`、`plugin/`、`bridge.mjs`、`screens/` 其它文件，也不动 sitin-rn 仓库。
- **产出（4 个新文件，`~/WORK/figma-write-bridge/screens/`）**：
  - `72-paywall-likes.mjs`：h-14 页头（40 奶油圆返回）+ 蜜色 `${count} LIKED YOU` 胶囊 + 白面板五张**方**照片（`LikesTeaser`，55% 不透明度近似 blur + 18pt 锁徽）+ tiltHead 探头小崽（`right -6`）→「See who likes you」30/36 + 两行副标 → 白卡三条 `LIKES_FALLBACK` 权益 → 会员三档（中间 POPULAR，价格同 34 屏）+ 自动续费说明 + 「Unlock 5 likes」+ 页脚。
  - `73-manage-subscription.mjs`：h-14 居中页头 + 白卡（52 深色 apple 图标 / `Luka Premium` / `Yearly plan · $5.00 / month` / 蜜色 ACTIVE）+ `my-5` 发丝线两列 NEXT BILLING / BILLED + `right -4` 探头小崽 + `mt-auto` 描边圆胶囊 Cancel Subscription + 15/22 说明（底部 34+24）。
  - `74-account-deletion.mjs`：居中 220 倒计时环（蜜色 ELLIPSE 轨道 + 焦糖进度弧 + 64pt「5」+ 13/17 两行副标）+ 抱心小崽压环底（marginTop -36）→ 15/21 说明 → 白卡两行 ReassureRow（40 蜜色圆图标 + 发丝线）→ 钉底 Keep my account + Continue deleting ›。
  - `75-settings-delete.mjs`：页头（圆返回 + 18/22「Delete account」）+ 告别信白卡（`hamster-peek-worried` 120×86.4 底边压卡 28、26/32 标题、三行说明、蜜色 `7 days to change your mind`、三条后果）→「Write DELETE to sign this note」+ 未确认态输入框 + 提示 → 禁用态 Delete my account + Keep my nest。
- **关键做法 / 坑**：
  - **四屏的折行/宽度都用真字体量过**：临时 swift + CoreText 脚本（`/var/folders/.../opencode/measure-text.swift`）读 luka 出包里的 Nunito ttf —— 73 的账单说明 638.4pt 在 334 里 3 行、74 的两行 body 504/454 在 266 里各 2 行、`days until the nest closes` 147.2 > 140 必折 2 行；`5 LIKED YOU`（12pt Bold +1.2 字距）86.5 → 胶囊 114；`Continue deleting` 121.5 → 行内 122 宽。
  - **进度弧沿 50 屏记录的两个坑**：Figma 不认 SVG `A` 弧 → 走 `lib/patharc.mjs` 的 `convertArcs`；VECTOR 的 bbox 不含描边、`resize()` 按几何 bbox 缩放 → `ARC_BOX` 用「两端点 + 区间内每个 90° 极值」算出的几何 bbox（10.56, 8, 201.44×204）做声明尺寸、`viewBoxSize` 同值让描边 scale=1，否则 257° 的弧会被撑满整环。
  - **72 的取景**：内容 831pt > 视口 734pt（844−110）；取 offset 62（不是 0 也不是最大 97）—— 小崽刚完整露在页头下沿、CTA 全在视口内，折线之下只剩页脚行盒的 3pt（字形完整）与 `pb-8`，避免出现「按钮被切一半」的帧。
- **验证**：四屏 `node -e "import('./screens/<f>.mjs').then(m=>{const s=m.build();console.log(m.label,'children',s.children.length)})"` 全过（4 / 8 / 9 / 12 children）；另跑临时 walker（92 / 37 / 36 / 36 节点）核绝对坐标：73/74/75 零越界，72 只有页脚那 5 个文字行盒超出帧底 2.8pt（会被根裁剪，字形完整）。**未推送 Figma**（未要求）；**未建 group 文件**（按要求不改 `screens/` 其它文件）。
- **假设（已在返回里列出）**：① 72 只画 `showThirdPay === false` 的单一 CTA 分支（与 34 屏同一取舍），五张照片用 mock avatars（`useLikedList` 无本地 fixture）；② 73 取有订阅年档 $59.99 / 到期 2026-12-12（`formatPrice` → "Yearly plan · $5.00 / month"、`formatDate` → "Dec 12, 2026"），卡片名走 `BRAND` 兜底那支；③ 74 取 `remaining = 5 天`（production 7 天）、days=5、progress=5/7；④ 75 是 `confirm === ""` 的初始禁用态，操作栏画键盘收起的静息态；⑤ 三处 34/24/16 之类底部 inset 按画布 34 home indicator 计。

### 17:00 · sitin-rn：T0048 修法 B1 —— 徽标改数名单里的行（用户裁决）

- **背景**：用户选 B1（徽标与列表同源），替换服务端汇总口径 —— 服务端 likes 计数含被 `filterOutMatched` 隐掉的人、visitors 计数是另一套来源，截图 6 vs 3 行就是这么来的。
- **改动（luka，9 src + 2 测试，未提交）**：
  - 新 `lib/into-you-counts.ts`：`unreadLikerCount`（`isUnread` 且未被 matched 隐掉）、`newVisitorCount`（`lastVisitTime > 基线`）。
  - `stores/into-you-unread.ts`：`refresh()` 改拉 `GetMatchLikedList` 第一页 + `GetVisitorList` 全量，按上面两条数；新增 `holdLists()` 租约与 `setLikeUnread` / `setVisitorUnread` / `setVisitorTotal`（屏上名单回报）；移除 `refreshVisitorTotal`。
  - `app/notifications.tsx`：挂 `useIntoYouListSync`（10s + 回前台拉名单：likes `syncNew`、visitors `sync`，并持租约让 app 级 30s 轮询让位）；屏上把行上的未读数推回 store；离开 Activity 额外 `liked.markAllRead()`（否则本地 fetch 时的 `isUnread` 会让下一次 `syncNew` 复活已读的人）。
  - `hooks/use-liked-list.ts` 加 `markAllRead`；`hooks/use-async-data.ts` 加 `sync`（静默重拉：不动 loading/error，也不 bump `runRef`，避免把在飞的 `reload` 的 `loading` 卡住）；`use-visitor-list` 暴露 `sync`。
  - 删 `hooks/use-into-you-counts.ts`（职责被名单自己取代），barrel 换 `useIntoYouListSync`。
  - `docs/notifications.md`《徽标》《计数新鲜度》《Activity 隐人》同步；记了已知边界：屏幕外 poll 只数第一页（>20 条未读时离开屏幕会被压回第一页口径，再进屏由名单报回准值）。
- **验证**：`pnpm --filter luka exec tsc --noEmit` 0；`biome check apps/luka/src` 干净；新/改测试 14 passed（`tests/lib/into-you-counts.test.ts` 6 + `tests/stores/into-you-unread.test.ts` 8）；全量 `1110 passed / 7 failed`，7 条与基线逐条一致（good-review、shape-language×3、candidate-card-fields、use-swipe-actions×3）；`pnpm boundaries` OK。
- **未提交**（规范模式）—— 等用户确认；工作区含并行会话未提交改动，提交时用 `git commit --only` 只带本批文件。**未实机**（无 Simulator.app，视觉/时序留真机走查）。

### 17:00 · figma-write-bridge：Luka 设置/AR/玩法七屏分层复刻（76 · Sign out / 77 · AI insights / 78 · AR / 79 · How it works / 80 · Quick pick / 81 · Edit photos / 82 · Official team）

- **需求**：同一套 DSL 续作 —— 按代码把七屏重建成 Figma 节点树（不是贴图），390×844 绝对定位、只用 `lib/` 原语；不动 `lib/`、`plugin/`、`bridge.mjs`、`screens/` 其它文件，也不动 sitin-rn 仓库。
- **产出（7 个新文件，`~/WORK/figma-write-bridge/screens/`，children 6/11/9/6/6/8/9）**：
  - `76-logout.mjs`：`logout.tsx` —— h-14 页头（40 `tagBg` 圆返回 + 22/30「Sign out」）+ 白卡（`pt-9` 36 + 27/34「See you soon?」+ 两行 15/22 + 蜜色 `we'll keep your place` 胶囊 180×41）+ 148×90 的 `hamster-peek-worried` 从卡上沿探头（本地 y −85）+ 贴底 58 胶囊与 52 行「Stay signed in」（底部 34）。
  - `77-ai-insights.mjs`：`ai-insights.tsx` —— 无标题 ScreenHeader（裸 `‹` 32/36）+ 28/36 标题 + 六行 13.5/20 引导段 + 白卡开关行（15pt 两行 + 12.5/18 三行 + 52×32 primary Toggle，按已开启画）+ `THE DETAIL` 段名 + 三段披露（11pt 编号 + 右侧细线 + 13.5/20 正文）+ `WHO RECEIVES IT` / 第三方 AI / 页脚（都在 844 外，ScrollView 可滚，仍画进图层）。
  - `78-ar.mjs`：`ar.tsx` + 12 个 `components/ar/*`、`lib/ar-play.ts`、`copy/ar.ts` —— playing · world 相位：奶油房间 = `[honey, creamCard, caramel]` 两段渐隐近似 + 小崽 `actorScreenPoint` 82² @(154, 389.4) + 顶栏（40 关闭 / 居中 159×34 说明胶囊 "Luka is in the room." / 80×40 缩放键）+ 底部手柄（136 摇杆 + 148 宽动作簇：44/44/44 表情 + 78 Jump + 44 Hide + 64 快门）。
  - `79-how-it-works.mjs`：`how-it-works.tsx` → `IntroFilm` —— `#0B1020` 底 + `wake-plate.jpg` `cover`（844 见方居中，x −227）+ light 状态栏 + `AUTO PLAYING` 119×28 胶囊（honey rec 点 + 描边）+ 40 关闭键 + 底部 26/32、14/20 两行白字（块底 730）。视频层不画（画的是它下面那张底板）。
  - `80-quick-pick.mjs`：`quick-pick.tsx` + 6 个 `components/quick-pick/*` + `lib/quick-pick.ts` —— **round 相位第一轮**：页头（返回 + 22「Quick Pick」+ FREE 胶囊 + 2/5 计分板）+ 轮次抬头（ROUND 1 OF 2 / 20s / 两枚进度点 / 满格倒计时条）+ **按代码重放洞位几何**（把 `rollDice` 写死成一组骰子，`separate`/`pushApart`/三遍扫描原样搬，scale 0.9408）+ 6 个洞（2 个有人：白环头像 + `hamster-face` 占位 + 名字小方卡）+ picks 托盘（两枚叠放头像）+ `Next round`。
  - `81-edit-photos.mjs`：`edit-profile/photos.tsx` + `photo-well` / `section-eyebrow` / `ai-tip` / `continue-button` —— 返回行 + `✦  PHOTOS` + 两行 24/30 标题 + 通栏照片位 350×420（圆角 22、底渐隐 88 用四条带近似、手写体 `this is me ✦`、右下 `Change photo` 140×35 白胶囊）+ AI tip + 禁用态 `Save` 58 胶囊。
  - `82-team.mjs`：`team.tsx` → `official-channel-screen.tsx` + 页头/页脚/卡片三组件 —— h-60 页头（40 `panelWash` 圆返回 + 38 字母圆「I」+ 17 ExtraBold「Luka Team」+ 15 勾 + 12 `Official · read-only`）+ `Today` 胶囊 + 三张白卡（16/20 标题 + 14.5/20 正文，实测 2/3/3 行）+ 归署 + 锁住的输入条 358×48（lock 13 + 13.5/16 文案）。
- **关键做法 / 坑**：
  - **折行全部按真字体量**：这次改用 Node + `fontkit` 读 `@expo-google-fonts/nunito/*.ttf`（临时目录 `npm i fontkit --cache $PWD/npmcache`），省掉上次的 swift/CoreText 脚本；顺手量了一批「只要一行还是折」的临界值（`Let Luka read my published photos` 244.9 > 列宽 240 → 两行）。方法与坑已补进 `30-Resources/troubleshooting/design-mock-measure-and-sample.md`。
  - **80 的洞位不是手摆的**：`QuickPickBoard` 每轮掷骰子再按三条规则（同列不同左 / 同行不同高 / 任意两洞 ≥10pt）推开，所以把骰子固定成一组写死的值、把那段几何**原样重放**，洞位就是机器真会摆出来的形状；同时按 `transform: scale(0.9408) + origin top center` 把盒内坐标折成绝对值（Figma 侧没有 transform）。
  - **素材缺口三处都用占位并在文件头写明**：`quick-pick/{mole-frame,hole,paw-stamp}.webp` 不在桥的素材里 → 洞口用暖墨椭圆、地鼠用白环头像 + `mascot/hamster-face.png`、爪印借同形的 `chat/paw-bg.svg`；AR 的相机画面本来就没有素材，直接画 `ArCameraStage` 自己那条兜底渐变。
  - **DSL 缺的能力两处显式声明**：`this is me ✦` 的 −2° 旋转、ChoiceCard 那类 ±0.9° 倾斜都画不了（插件无 rotation），81 按 0° 画并写进注释。
  - 自查沿用了 walker（打绝对坐标）—— 这次抓到 2 处「组内坐标写成屏坐标」：76 的胶囊 x 多减了一次 `PAD`、81 的 Save 少了 `pt-4` 16（落在 716，应为 732），均已修。
- **验证**：七屏 `node -e "import('./screens/<f>.mjs').then(m=>{const s=m.build();console.log(m.label,'children',s.children.length)})"` 全过（6/11/9/6/6/8/9）；walker 逐屏核绝对坐标，无越界（77 的 833 以下、71 类滚动内容按既有口径保留）。**未推送 Figma**（未要求，bridge `:3055` 未起）；**未建 group 文件**（按要求不改 `screens/` 其它文件）。
- **假设（已在返回里列出）**：① 78 取 playing · world · 2D 精灵那一帧（唯一同时有缩放键、精灵与整个手柄的相位），状态栏用桥里统一的深色（代码在非 permission 相位设 light 是给相机画面准备的）；② 80 取 round 第一轮：5 人 / 6 洞 / 已拍中 2 人（托盘）/ 冒头 2 人，`upIds` 归谁与洞位在真机上随机，本帧把 Elena / Harper 放在最分得开的两个洞位；③ 81 的 Save 是**禁用态**（`disabled={!picked}` —— 本次会话没挑新图，照片位显示的是账号已有头像），且 `photos-card.tsx` 属于 Edit profile hub（24 屏）不在这一屏；④ 76 的陪伴小崽素材是 `hamster-peek-worried`（148×90）、⑤ 82 页头字母圆的字是代码里写死的「I」。

- **17:05 提交并 push（用户要求）**：`71a075ddd` `fix(luka): 通知徽标改数名单里的行，与服务端汇总口径脱钩（T0048）`（13 files，+400/−163）→ `origin/feature/luka-ios`（`5a72f69fe..71a075ddd`，GitLab HTTPS）。pre-commit biome 11 files、pre-push boundaries（41 包）+ typecheck apps/luka（5.4s）全过。并行会话的改动已由它自己提交，未混入。**验证留真机：需重打 IPA（含 A+B1）**。

### 17:09 · sitin-rn：luka 通知两段「上滑超一屏 / 下拉刷新」未订阅拉起订阅（未提交）

- **需求**：用户「luka【likes&visitors】likes&visitors 上滑超一屏，和下拉刷新，未订阅的需要拉起订阅，已订阅的不被影响」。
- **口径与实现（按字面 + 07:05 已有的全量浏览裁决）**：闸门只管「拉起订阅」这一下，不改翻页/加载口径 —— 未订阅用户滚过一个视口高度推对应订阅页，返回后仍可继续滑完已加载内容（模糊行）；已订阅用户滚动、下拉、翻页一切如旧。**待用户确认**：是否要顺势收紧「未订阅不再继续翻页/reveal」（当前保持 09-21 全量浏览口径，只加手势触发）。
- **改动（3 源文件 + 2 测试 + 1 文档，未提交）**：
  - 新 `src/lib/notification-scroll-gate.ts`：`stepScrollGate(state,{locked,scrollY,viewportHeight})` 纯函数 —— 未订阅且 `scrollY > viewportHeight` 触发一次（`raised` 闩），**滚回第一屏内才复位**（否则从订阅页返回时列表停在越界位置，滚动事件会把订阅页无限叠栈）；首帧视口高度 0 不判定。
  - `app/notifications.tsx`：两段各一个 ref + `onActivityScroll` / `onVisitorScroll`（FlatList 加 `scrollEventThrottle={16}`，MasonryList 传 `onScroll`）；Visitors 下拉刷新改为未订阅 → `/paywall/visitor?source=list_scroll`（此前所有人都真重拉）；新增 source 常量 `VISITORS_SCROLL_SOURCE = "list_scroll"`（与 `list_item_click` / `unlock_btn` 区分）。
  - `components/masonry/masonry-list.tsx`：新增可选 `onScroll` 原样转发（内部曝光/2-3 触发线不变）。
  - 测试：`tests/lib/notification-scroll-gate.test.ts`（5 条：越界触发 / 一屏内不触发 / 一次越界只推一次且回顶复位 / 已订阅从不触发 / 视口 0 不判定）；`tests/design/notifications-scroll-gate.test.ts`（3 条接线守卫：两段都挂闸门、Visitors 下拉锁定分支、MasonryList 转发）。
  - 文档：`docs/notifications.md`《列表手势》增订 2026-09-22（含「闸门不改翻页口径」与 source 说明）+《验证》手点路径。
- **验证**：新测试 8 passed；`pnpm luka:typecheck` 0；`biome check` 5 文件干净；全量 `pnpm --filter luka test` = **1118 passed / 7 failed**，7 条为基线既有（good-review 文件级、use-swipe-actions ×3、candidate-card-fields ×1、shape-language ×3，与本次零交集）。**未实机**（本机无 Simulator.app）。
- **坑**：开工读文件时 `notifications.tsx` 正被并行会话改（457 → 492 行，`71a075ddd` 徽标改数行），首版 grep 结果作废 —— 后续都先重读再 edit；改动落在并行提交之上，未夹带对方内容。
- **17:11 提交并 push（用户要求）**：`b6f9c93c2` `feat(luka): likes/visitors 上滑超一屏与下拉刷新，未订阅拉起订阅`（6 files，+205/−5）→ `origin/feature/luka-ios`（`71a075ddd..b6f9c93c2`，GitLab HTTPS）；pre-commit biome 5 files、pre-push boundaries（41 包）+ typecheck apps/luka（7.3s）全过。只 stage 本次 6 个文件，根目录 `-` 未扫入。**验证留真机**（需重打 IPA 或 dev 包 reload）。

## 工作日志 — 17:20 补全 `apps/luka/src/app` 全部 route 到 Figma（section 16–20，23 屏）

**需求**：用户要求「按 app 中所有 route 页面补全 figma」。

**对齐**：`apps/luka/src/app` 共 **72 个 route 文件**（排除 `_layout`）。逐条比对已画的 57 帧，缺口 **24** 个；其中
- `onboarding/index.tsx` 只是 redirect、`render null`（代码注释说只为盖住 1–2 帧窗口底色），**不出屏**；
- `dev/call-spike.tsx` 是调试工具，**不出屏**。
→ 实补 **23 屏**。

**新增 5 个 section**
- `· 16 启动与引导`（60 Splash / 61 Who you meet / 62 Faith / 63 Intro video）
- `· 17 交换 / 收件箱`（64 Inbox / 65 Exchange waiting / 66 Exchange revealed / 67 Link opened）
- `· 18 互动与详情`（68 Match reveal / 69 Activity / 70 Post detail / 71 Profile posts）
- `· 19 付费 / 账号 / 设置`（72 Who liked you / 73 Manage subscription / 74 Deletion in progress / 75 Delete account）
- `· 20 其他页面`（76 Sign out / 77 AI insights / 78 AR / 79 How it works / 80 Quick pick / 81 Edit photos / 82 Official team）

**顺带修正**：`lib/onboarding-flow.ts` 已改成 **10 步长流程**（`identify` 移出、`who-you-meet` 顶第 06 步、`faith` 第 10 步）—— 27–30 的进度条从 `06/9…09/9` 改成 `06/10…09/10`，27 加了「已不在流程里」的注释。

**踩的坑（桥侧）**：子代理把 SVG 的 `d` 原样内联进 spec（没走 `svgicon`），渲染时报
`Failed to convert path. Invalid command at H5.60013C3.68554` —— 两处：
1. 命令字母与数字之间缺空格（`H5.6C3.68`）；
2. 我的规范化正则 `([a-zA-Z])(?=[-+0-9.])` 会把**指数记号切断**（`2.9e-16` → `e -16`）。
修法：`normalizePath` 排除 `e/E`，并在 `lib/render.mjs` 里对所有 VECTOR 的 paths 统一规范化（这样不用重启插件）；残缺路径（含 NaN）直接回退原文不推给 Figma。

**结果**：`poo` 页 **20 个「分层复刻」section、80 帧**，页面无游离节点（清掉 10 个）。`apps/luka/src/app` 的 72 个 route 至此全覆盖（70 出屏 + 2 个无 UI）。

### 17:25 · sitin-rn：T0121 非会员下拉 Activity 卡在刷新状态

- **来源**：飞书 buglist T0121（P1，赵延杰 17:12 建，record `recvvWgl1gsbNl`，附视频 `飞书20260922-170702.qt`）。**视频附件其实是聊天页（Bond unlocked），疑似附错**；但代码上复现路径明确。
- **根因**：非会员下拉 Activity 时 `onRefreshLiked` 只 `openLikedPaywall(...)` 就 return，`refreshing` 从未变 true。iOS `RCTRefreshControl.m` 的 `setRefreshing:` 只在 `_currentRefreshingState != refreshing` 时走 begin/endRefreshing；JS 不发 true 就永远不会 `endRefreshing` → 手势自己拉起来的 spinner 永久停在列表上（T0121）。
- **修法**（`apps/luka/src/app/notifications.tsx`，2 处）：屏幕加 `paywallPull` 状态；锁定分支先 `setPaywallPull(true)` 再进付费墙，effect 里立刻收；`refreshing={liked.refreshing || paywallPull}`。
- **验证**：`biome check` 干净、`tsc --noEmit` 0；全量 `1118 passed / 7 failed`（7 条与基线逐条一致）。**未实机**（无 Simulator.app）。
- **未提交**（规范模式）—— 等用户确认；docs/notifications.md《列表手势》已同步这条原生行为。

- **17:35 扩到两段并提交 / push（用户要求）**：提交前发现并行会话 `b6f9c93c2`（17:10）把 **Visitors 非会员下拉也改成进订阅页**（`onRefreshVisitors` → `openVisitorPaywall`），与 T0121 同一个坑，故把修法扩到两段：`paywallPull` 存被拉的段名（`"activity" | "visitors" | null`），两段 `refreshing` 各取 `xxx.refreshing || paywallPull === "<段>"`，effect 里立刻收；docs 同步两段。commit `addd24c70`（2 files，+21/−2）→ `origin/feature/luka-ios`（`b6f9c93c2..addd24c70`）；pre-commit biome、pre-push luka typecheck 5.5s 全过。全量 `1118 passed / 7 failed`（基线 7 条）。**验证留真机**：非会员在两段各下拉一次 → 进订阅页 → 返回后 spinner 不再卡住。

- **17:45 用户补图确认复现路径**：截图（17:20，锁定态 Visitors 段：模糊瓦片 + 锁角标 + See Who Visited You CTA）顶部 RefreshControl spinner 卡住 —— 即「非会员下拉 → 进订阅页」那条链（`b6f9c93c2` 引入的行为，17:20 的图早于 `addd24c70` 的 17:35）。结论：T0121 的准确页面是 **Visitors**（标题写的 like 页是笼统指代）；`addd24c70` 已覆盖。验证仍待 dev client reload 或重打 IPA。

- **17:30 T0121 第二轮：用户反馈「从 premium 返回后还是卡」**（同一张 17:20 图 + 明确路径：未订阅 → 下拉 → 进 premium → 返回 → 卡在 loading）。核对 RN 端后判断：JS 的两条 `refreshing` 来源都不可能悬挂（`paywallPull` 的 effect 必清、`useLikedList/useVisitorList.refresh` 的 `finally` 必收），所以卡住的是**原生控件状态** —— 手势自己拉起的 spinner，在「页面被订阅页盖住 / 手势被翻页打断」的窗口里丢了 `endRefreshing`（JS 的 true→false 或 RN 的补偿命令都可能没落到这个控件上）。
  - 加兜底（`apps/luka/src/app/notifications.tsx`）：`pullEpoch` + `useFocusEffect`，**回屏时把两个 RefreshControl 重挂一次**（`key` 变 → 旧控件整个卸载，原生侧连 `contentInset` 一起收掉，不依赖那次时序）。docs/notifications.md《列表手势》同步两层机制。
  - 验证：biome/tsc 干净；全量 `1118 passed / 7 failed`（基线 7 条）。**未提交**：dev 包 reload 即可拿到，等用户在真机确认后再推。

### 17:35 · sitin-rn：luka 通知未读清零时机改回「进段即清」（两段 tab 数字徽标）

- **需求**：用户「【likes&visitors】红点规则，在当前页面就消失，现在是离开此页面才消失」。追问两轮后用户拍板：**时机 = 进段/进页面立刻清；范围 = 只改两段 tab 数字徽标**（行内/卡片红点不在本次范围）。
- **历史**：进段即清本是 blueprint/intro 的写法（`if (tab === "activity") clearLike()`），luka 2026-09-21 因「tab 徽标一闪就没」改成离开才清（c9cd2a1c2 引入 `clearSegment(previous)`）；本次按用户口径推翻，改回 `clearSegment(tab)`（进屏落哪段清哪段、切段清新段）。
- **时序坑（本次真正的技术点）**：Activity 未读是服务端逐行下发的 `isUnread`，且该屏的徽标按 B1 由**名单行数**回报（`setLikeUnread(likedUnread)`）。进屏那一刻名单还在路上 → 进段清零打在空名单上 → 第一页落地后未读行又把徽标顶起来（变成「闪一下又回来」）。修法：加一条 `tab === "activity"` 的兜底 effect，名单里再出现未读行就继续 `markAllRead()` + `clearLike()`（本地行标志与服务端 `MarkMatchLikedRead` 一起落）。Visitors 无此问题（`markVisitorsSeen()` 拨的是本地 baseline，名单再晚回来也只算 0）。
  - 行内红点不受影响：`focus-unread` 的 observe 声明在兜底 effect 之前，先把 id 收进 latch；卡片红点则随 baseline 一起清（与徽标同源，B1 口径，用户未反对）。
- **改动（1 源文件 + 1 测试 + 1 文档，未提交）**：`app/notifications.tsx`（`clearSegment(tab)` + Activity 兜底 effect + 注释）；新 `tests/design/notifications-read-timing.test.ts`（2 条：进段即清且不再有 `prevTabRef`/`clearSegment(previous)`；Activity 落地后继续清）；`docs/notifications.md` 三处口径改写（《Activity 段》《分段徽标与默认段》《徽标》尾部）。
- **验证**：`pnpm luka:typecheck` 0；biome 2 文件干净；新测试 2 passed；全量 `1120 passed / 7 failed`（7 条与基线逐条一致）。**未实机**（本机无 Simulator.app）。
- **注意**：工作区里并行会话还有 T0121 第二轮的未提交改动（同两个文件：RefreshControl `pullEpoch` 重挂），本次改动落在不同区域；提交时需按 hunk 只暂存本笔。
- **17:45 提交并 push（用户要求）**：`9e86478ea` `fix(luka): 通知 tab 徽标改回进段即清，不再等离开该段`（3 files，+76/−25）→ `origin/feature/luka-ios`（远程 `addd24c70..9e86478ea`，同批带上并行会话的 `71665bf07` T0121 第二轮）。
  - **踩坑（新）**：pre-push 的 boundaries 抓 `apps/luka/src/app/notifications.tsx` 里我注释写的「对齐 blueprint」= **另一个 app 的 identity leak**（boundaries 扫源码文本，app 名不能出现在另一个 app 里）。改成「与参考实现一致」后过。**注意 docs/*.md 不扫**（luka 文档里 blueprint 字样遍地都是，只有源码受限）。教训：在 app 源码注释里提参考包一律用「参考实现/蓝图」，别写英文包名。
  - 本地未推送提交因 hook 被挡 → 直接 `--amend` 收干净（仓库惯例：push 过才不能 amend）。pre-commit biome 1 file、pre-push boundaries + typecheck apps/luka（5.4s）全过。

- **17:35 提交后 push 被并行会话挡住**：用户确认 dev 包 reload 后不再卡，要求 push。提交 `71665bf07`（2 files，+27/−4）已落地本地，但 `git push` 的 pre-push（boundaries）报 2 条身份泄漏：`apps/luka/src/app/notifications.tsx` 里并行会话**未提交**的注释「对齐 blueprint」—— 我的提交版本里没有这个词（已核 `git show 71665bf07:… | rg -i blueprint` 为空）。对方还在同文件继续写（16:4x 起改「进入段就清未读」，并新增 `tests/design/notifications-read-timing.test.ts`），也会被同一道拦。已请用户裁决：等对方落库后重推 / 授权 stash 后推 / 让对把词改成「apps/blueprint」或「参考实现」。

### 17:50 · sitin-rn：luka Plus Pass 订阅页按图二改一屏（权益列表 + 三格套餐）

- **需求**：用户贴两张图（图一 = luka `paywall/index` 现状：01–05 编号权益格 + 「CHOOSE YOUR PASS」整行套餐；图二 = 同 app 访客付费页）——「权益改为图二的列表形式，选择权益的按钮改为图二的形式，一屏展示，列表可滚动」。三问确认：① 固定页 + 权益列表内部滚动（非整页 ScrollView）；② 权益文案保持现有 5 条（赠币行仍读后端 `subCoins`），只换呈现；③ 删掉改后无引用的 `feature-grid.tsx` / `plan-rows.tsx`。
- **改动（1 源文件 + 2 删除，未提交）**：
  - `apps/luka/src/app/paywall/index.tsx`：`ScrollView` 整页壳 → 固定 flex 列；`FeatureGrid`（编号格）→ 白卡 + `BenefitCheckRow`（焦糖圆勾 + 发丝线，与 likes/visitor 同款）；`PlanRows`（整行）→ `PlanTiles`（三格平铺，选中档挂 POPULAR）；`PassFeature{lead,rest}` → 纯字符串数组 `PLUS_BENEFITS` + `coinBenefit(plans)`；权益卡 `min-h-0 flex-1 overflow-hidden rounded-[20px]` + 内部 `ScrollView contentContainerClassName="grow justify-center px-4 py-1.5"`（内容矮时居中、超时卡内滚）；根容器加 `paddingBottom: Math.max(insets.bottom, 12)`、去掉 `pb-8`。仓鼠（`hamsterHappy` 探在卡上沿 + `hamsterSit` 坐在 CTA 上沿）、`CHOOSE YOUR PASS` 行、自动续订说明、三方/内购分支、埋点全部照旧。
  - 删除 `components/paywall/feature-grid.tsx`、`components/paywall/plan-rows.tsx`（`rg` 确认 luka 内零引用）。
- **验证**：biome 干净；`pnpm --filter luka typecheck` 0；`pnpm --filter luka test` **1120 passed / 7 failed** —— 失败集（candidate-card-fields×1 / shape-language×3 / use-swipe-actions×3 / good-review 收集错误）与基线逐条一致，非本次引入。**未实机**（paywall 在登录守卫后）。
- **与并行会话的对齐**：写完后发现另一会话正在同批把 `likes.tsx` / `visitor.tsx` 做同一改造（固定页 + 权益卡内滚 + `grow justify-center` + 底部安全区），已把自己的实现对齐其写法（含卡内滚动条用系统指示器、根容器 paddingBottom），三页形态一致；`peeking-hamster.tsx` 的 `size` prop 是对方为 likes 页加的，本次未用（本页仓鼠仍是静态 `hamsterHappy`，与截图一致）。
- **遗留**：`tests/design/shape-language.test.ts` 的 MIGRATED 白名单里没有 `app/paywall/*` 与 `components/paywall/*`，该测试本就因此红（改动前 `rounded-[16px]` 已在榜上）；本次没动测试文件（并行会话也在改这批页，避免抢同一个文件），是否把 paywall 三页 + 零件加进 MIGRATED 待用户定。

### 17:49 · sitin-rn：luka 通知页空态/错误态换成 Luka 整屏空态（未提交）

- **需求**：用户贴通知页 Visitors 段错误态截图（灰字「Couldn't load your visitors.」+ `rounded-[3px]` 描边 Try again），要求「这个 empty 改成 luka 风格」。
- **改法（`app/notifications.tsx`）**：三支占位换 `EmptyHero`（`components/xuanyu/empty-hero`）+ `hamster-peek-worried` + `BACKEND_UNREACHABLE`，错误态给 `Try again` 可可胶囊、各走 `liked.reload()` / `visitors.reload()`；Visitors 空态去掉「RECENT VISITORS」段标题（整屏空态自己就是一段）。与 `settings/blocklist` 的出错态同口径（2026-09-16）。
- **页头小崽让位**：新增 `showHeaderPet`（按当前段判断 `liked/visitors` 是否落在自带小崽的占位上）→ `NotificationsHeader pet={showHeaderPet}`，避免同一只崽两个版本同屏（与 Calls 2026-09-17 同规则）。Activity 的空态仍是 `NotificationsEmpty`（蜜色圆盘 + 铃铛），未动。
- **文档 + 测试**：`docs/notifications.md` 新增《空态与错误态》一节；新 `tests/design/notifications-empty-hero.test.ts`（4 条源文本断言：三支 EmptyHero、CTA 各自 reload、旧灰字/直角按钮不再出现、页头小崽让位）。
- **验证**：biome 2 文件干净；`pnpm --filter luka exec tsc --noEmit` 0；`pnpm boundaries` OK；`pnpm --filter luka test` **1124 passed / 7 failed** —— 失败集（candidate-card-fields×1 / shape-language×3 / use-swipe-actions×3 / good-review 收集错误）与基线逐条一致。**未实机、未提交**（规范模式，等用户确认；工作区另有并行会话的 paywall 未提交改动，未触碰）。
- **待确认**：页头小崽让位是为避免两只仓鼠同屏而做的推断（用户截图里页头那只仍在），用户若想保留页头那只，删 `showHeaderPet` 一处即可。

### 17:58 · sitin-rn：T0126 注册定位页「Use my current location」点了没反应 —— 反馈被数字键盘盖住

- **来源**：飞书 buglist T0126（P2，指派张峥，未开始；录屏 `飞书20260922-174510.qt`，6s，iPhone 数字键盘全程弹起）。
- **复现 / 根因（视频逐帧 + 几何实证）**：luka 注册第 5 步 ZIP 页，权限已拒后点按钮只看到「Finding you…」闪 ~0.2s 再弹回，无任何提示。`onboarding-location-picker.tsx` 的拒绝分支走 `toast(...)`，而 luka 的贴底 `ToastHost` 只避让 `bottom-bar`（tab / composer），**不感知键盘** —— ZIP 输入框 autoFocus，数字键盘顶约 550pt，而 toast 落在距底 ~50pt，被完全盖住，所以测试读作「无反应」。
- **修法（用户拍板：Alert + Toast 键盘感知，只改 luka）**：
  - `app/onboarding/location.tsx`：把进屏时的权限 Alert 抽成 `showPermissionAlert`（`useCallback`），picker 经新增必填 prop `onPermissionDenied` 复用它 —— 点击被拒后弹同一条 Alert（Open Settings → 置 `retryAfterSettings` → 从设置返回自动重试）。
  - `components/onboarding/onboarding-location-picker.tsx`：拒绝分支改调 `onPermissionDenied()`，不再 toast（定位失败分支的 toast 保留）。
  - `components/ui/toast.tsx`：`ToastHost` 读 `useKeyboardState`；键盘弹起时 `clearance = 键盘高 + 已上报底部栏高 + 16pt`（聊天输入条骑在键盘上，只取 max 会把提示叠进栏里），收起时走原来的「底部栏 / 安全区取大 + 16pt」。
- **测试 / 验证**：新 `tests/design/onboarding-location-permission.test.ts` 4 条源文本断言（picker 不再 toast 权限文案、屏幕两条路收进同一 Alert、toast 两个 clearance 分支）；`pnpm luka:typecheck` 0；biome 4 文件干净；`pnpm --filter luka test` **1128 passed / 7 failed**，7 条与基线逐条一致（shape-language×3、candidate-card-fields×1、use-swipe-actions×3）。**未实机**（本机无 Simulator.app）。
- **文档**：`apps/luka/docs/location.md` 新增《权限被拒之后（T0126）》；`apps/luka/docs/keyboard-and-insets.md` 新增 §6《贴底 toast 要避让键盘》。
- **未提交**（规范模式，等用户确认；工作区另有并行会话的 paywall / notifications 改动，未触碰）。
- **已知留白**：`components/location/location-picker.tsx`（编辑资料定位页）权限被拒仍是 toast（现在能看见了），没配 Alert —— T0126 只覆盖注册页，是否统一待定夺。
