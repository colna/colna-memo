---
title: 2026-09-22
date: 2026-09-22
tags: daily
---

# 2026-09-22

## 今日重点

- [ ] **T0048**（likes/visitors 与爪印数量不一致）：A（新鲜度）已实现并提交 `595f37ea0`（3 files），**push 卡在 GitLab 凭据**（origin 已迁 `https://gitlab.lumosolo.com/app-rn/sitin-rn`，本机无该站凭据）—— 待用户本机交互式 push 一次 + 实机验证；B（口径统一）暂不做
- [ ] **T0052**（重登后资料丢）：等真机日志，`grep '\[profile\]'`（dev/preview 包 Export Log）→ 定后端修投影还是客户端合并读模型
- [ ] **T0061**（订阅赠币 +0）：给「SP后端内部群」的对账请求已拟好，等用户确认后发
- [ ] **真机验收**（昨天一批修复，均未实机走查）：T0057 键盘退出/T0054 进屏不弹键盘/T0053 地址保存/T0055 去动作栏/T0056 全帖列表；Sent 卡修复 `a759a0692`；引导第 4 步 `35608d542`；通知分段徽标定位 `84a5b1107`（**需重打 IPA**）
- [ ] 根目录未跟踪的 `-` 文件（devicectl UDID 误产物）删不删，待用户定夺

## 工作日志

### 10:40 · sitin-rn：把 origin 从 GitHub SSH 改为 GitLab HTTPS

- **做了什么**：`git remote set-url origin https://gitlab.lumosolo.com/app-rn/sitin-rn`（原 `git@github.com:presence-io/sitin-rn.git`）；并在**仓库本地** config 加两条 rewrite 兜底，保证任何 SSH 写法都强制落回 HTTPS：`url."https://gitlab.lumosolo.com/".insteadOf = git@gitlab.lumosolo.com:` 与 `ssh://git@gitlab.lumosolo.com/`。
- **为什么**：用户要求仓库换到 GitLab，且**只准用 HTTPS 推拉**（不走 SSH）。
- **结果**：`git remote -v` 已指向 GitLab；`GIT_TERMINAL_PROMPT=0 git ls-remote origin` 报 `could not read Username` —— 远端是私有 GitLab，**本机还没有该站的凭据**。全局 `credential.helper = osxkeychain` 已在，第一次交互式 pull/push 输入用户名 + Personal Access Token 即会被钥匙串记住，之后免输。**未做**：没有嵌入 token，也没有改动任何全局 config（改动全在 `.git/config`，只影响本仓库）。
- **关键命令**：`git remote set-url origin https://…`；`git config --local --add url."https://gitlab.lumosolo.com/".insteadOf "git@gitlab.lumosolo.com:"`（两条 insteadOf 需用 `--add`，直接 `git config k v` 会覆盖前一条）
- **待办 / 提醒**：
  - 各 worktree 共用同一 `.git/config`，remote 变更对 `fix/luka-code-review` 等 worktree 一起生效；本地 `origin/feature/luka-ios` 等 remote-tracking ref 仍是 GitHub 那份，下次 `git fetch` 会连新站重取。
  - `apps/*` 的 CI（`.github/workflows`）在 GitHub 侧，迁 GitLab 后需要另配（未处理）。
  - 仓库文档 `docs/native-dev-onboarding.md` 仍写着 `git clone git@github.com:presence-io/sitin-rn.git`，与本次迁移不一致 —— 用户没要求改，且「是否全队迁 GitLab」未定，未擅自动。

### 10:40 · colna-memo：整理 2026-09-21 Daily（首次任务）

- 前一天 527 行、共 40 余条工作日志，全部已闭环或已在日志内标注去向；本轮**无新增沉淀、无归档**（可复用经验 09-21 当天已逐条写进 `30-Resources/troubleshooting/`：`luka-logout-fatal-account-stranded` / `luka-profile-fields-lost-on-relogin` / `subscription-coins-zero` / `react-native-svg-ios-mask-crash` / `uniwind-dev-stale-css-new-class` / `sitin-rn-local-ipa-verification` / `react-native-stale-measure` / `expo-router-leaf-branch-route-static-check` / `colna-sync` 等）。
- 复制 5 条未完成待办到今日「今日重点」（T0048 / T0052 / T0061 / 真机验收批 / 根目录 `-` 处置）。
- 项目进展同步：`10-Projects/luka-rn-app.md` 补「仓库已迁 GitLab HTTPS」与「域名/商店身份改动已提交」两处。

### 10:50 · sitin-rn：T0048 修法 A —— 爪印计数补两处「立刻刷新」

- **背景**：T0048 视频逐帧取证（09-21 晚）显示页头爪印静帧 = 3，而进通知页那次 `useIntoYouCounts()` 刷新后 store 变成 `likeUnread=0 / visitorUnread=1` —— 即**爪印停留在旧数**。app 级只有 30s 轮询，`useInterval` 在 `enabled/active` 重新为真时不会立即触发，回前台/切回 Chats 都不刷。用户选 A（补新鲜度），B（口径统一）暂不做。
- **改动**：
  - `apps/luka/src/hooks/use-into-you-badge.ts`：AppState 监听里，`s === "active"` 时立即 `refresh()`（用 `enabledRef` 读登录态，监听只订阅一次）。
  - `apps/luka/src/app/(tabs)/index.tsx`：新增一个 `useFocusEffect`，focus 到 Chats 时 `refresh()`（带 `userId` 守卫，登出态不刷）。
  - `apps/luka/docs/notifications.md`《计数新鲜度》补「回前台 / 切回 Chats 各刷一次」与 T0048 由来。
- **验证**：`pnpm --filter luka typecheck` 0；biome 2 文件干净；`tests/lib/notification-badges.test.ts` 4 passed。**未实机**（dev client fast refresh 或重打 IPA 可看）。
- **未提交**：按规范模式等用户确认；注意仓库 origin 今天已被并行会话改成 GitLab HTTPS（`https://gitlab.lumosolo.com/app-rn/sitin-rn`），本机还没该站凭据，push 前先确认。

- **10:55 提交 / push 受阻**：`595f37ea0` `fix(luka): 爪印未读计数回前台/进 Chats 立即刷新（T0048）`（3 files，+33/−3）已落地本地；`GIT_TERMINAL_PROMPT=0 git push` 报 `could not read Username for 'https://gitlab.lumosolo.com'`，`git credential fill` 也无凭据 —— **需要用户在本机终端跑一次 `git push`**（输入 GitLab 用户名 + PAT，osxkeychain 记下），之后才能推。未用嵌入 token 的写法（并行会话迁移时明确留了这条）。
