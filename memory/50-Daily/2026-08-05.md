---
title: 2026-08-05
date: 2026-08-05
tags: daily
---

# 2026-08-05

## 今日重点

- [ ] 跟进 sitin4.1 SP V2 的 FE-2.2 后端字段与剩余 CR/测试缺口。
- [ ] 确认 Snapchat taskId 137 的后端任务/奖励，以及成功页素材和埋点命名 follow-up。
- [ ] 安排 GraceChat-Earn-Android 真机回归，并确认生产脚本 manifest 注册。

## 工作日志

### 14:55 修复 IG 脚本 CLICK_FAILED / getChatMessages 进不去聊天

**背景**:分析线上 script-errors 导出(40994 条,时间 7/30–8/5),逐层剔除伪错误/业务态后剩 7495 条真错误,CLICK_FAILED 占 32.9%(2463 条,全部 `getChatMessages`)排第一。

**根因**:`sitin-next` 的 `packages/app-ins-scripts` 收件箱有两套独立会话扫描逻辑不一致——
- `scanInboxThreads`:pagelet 容器 `[data-pagelet="IGDInboxThreadListScrollableAreaPagelet"]` 锚定,稳定,能找到未读线程;
- `clickChatThread`:却用 `div[style]` + `opacity==='1'` + `父元素恰好 1 子节点` 的脆弱启发式重扫,IG 虚拟列表行处于过渡态/结构微调时提取 0 候选(`candidates:[]`),`getChatMessages` 遂报 `CLICK_FAILED "无法进入聊天"`。数据佐证:`selector_found:false`、p50 仅 81ms(纯 DOM 查询非超时)。

**修法**(分支 `personal/zz/sp-snapchat`,IG 侧不参与 lint):
- `src/instagram/actions/scanInboxThreads.js`:私有 `findThreadItems()` 暴露为 `SP.findInboxThreadItems`,作为收件箱会话项唯一真源。
- `src/instagram/actions/clickChatThread.js`:改调 `SP.findInboxThreadItems()`,删掉 opacity/parent 启发式;名称精确匹配 + index 语义不变。
- `docs/API.md`:同步 `clickChatThread` 的 `index` 说明。

**验证**:`node scripts/build.mjs` 三个 IG bundle 编译通过,产物确认统一(automation.js:2254 挂载 / 2370 调用),旧启发式已从 clickChatThread 清除。未 commit(规范模式,待用户确认)。

**遗留**:`"Instagram User"`(93 条)重名——多个已注销账号同名,精确匹配命中第一个可能点错会话,建议后续用 thread 唯一标识兜底。

### 15:09 检查 2026-08-04 Daily

- **做了什么**:检查前一天 Daily 是否存在,并按每天首次任务规则初始化今天的 Daily。
- **为什么**:今天首次处理实质任务,需要先完成昨日整理检查再同步知识库。
- **结果**:`2026-08-04.md` 不存在,无需分类;沉淀 0 条、结转 0 条、归档 0 段。
- **关键命令 / diff**:`env TZ=Asia/Shanghai date`;检查 `memory/50-Daily/2026-08-04.md` 与 `2026-08-05.md`;新建本文件。
- **沉淀**:无。

### 15:35 整理 2026-08-04 Daily

- **做了什么**:远端更新拉取后重新检查昨日 Daily;确认原先缺失的 `2026-08-04.md` 已从远端同步到本地,保留其已完成日志,提取 3 类明确 follow-up 到今日重点。
- **为什么**:每日首次任务规则要求先整理前一天内容;首次检查发生在远端 rebase 前,需要在真源更新后补做。
- **结果**:未移动已闭环日志,未发现需要归档的临时垃圾;可复用经验和项目进展已有对应 troubleshooting / project 笔记,不重复写入。
- **关键命令 / diff**:`git pull --rebase --autostash`;`rg -n '^##|follow-up|未做|未改' memory/50-Daily/2026-08-04.md`;更新本文件的“今日重点”。
- **沉淀**:跨环境 Daily 先以远端真源为底再追加,避免在同一文件的不同尾部产生分叉冲突。

## 工作日志 — 16:01 完成 colna Memo 同步与 768 维索引恢复

- **做了什么**:解决 `pull --rebase --autostash` 恢复源码时的 3 处冲突,无损合并远端 E5Base/768、`embed_text`、zvec 512 写入上限与本地 16 块串行、写入/索引完整性守卫;把 state v2 升到 v3并加旧 v2 回归测试。涉及本地工作树 `README.md`、`src/{chunker,embedder,gitsync,main,mcp,state,store}.rs`,按规范模式保留未提交。
- **为什么**:直接选任一冲突侧都会丢失远端模型升级或本地索引恢复修复;旧 v2 state 也无法识别 384→768 维迁移,可能误用旧索引。
- **结果**:`cargo fmt --check`、Clippy、debug/release build 全过,19 tests passed;通过 `hf-mirror.com` 补齐 E5Base 缓存后全量重建 117 files / 1466 chunks,state v3 的文件数与 chunk 数一致;混合搜索正常。Memo 提交 `560ddc3` 已 push,当时 `HEAD == origin/main`。
- **Git 身份**:同步后发现全局 `user.name=COLNA`;不重写已推送历史,已在本仓库设为 `colna <richardzhang1999@163.com>`,后续 Memo 提交按统一身份生成。
- **关键命令 / diff**:`cargo test --all-targets -- --test-threads=1`;`cargo clippy --all-targets -- -D warnings`;`cargo build && cargo build --release`;`HF_ENDPOINT=https://hf-mirror.com COLNA_PROFILE=release ./colna sync`;`./colna search 'colna sync 索引维度' --topk 3`。
- **沉淀**:更新 `30-Resources/troubleshooting/colna-sync.md` 的 E5 模型升级 state 迁移规则;embedding 模型或输入契约变化时必须 bump state 版本并全量重建。

### 03:55 新建 social-post 社媒帖子采集平台（Phase 0~5 全量实现）

- **做了什么**:在新仓库 `git@github.com:colna/social-post`(原空仓)从 0 搭出可扩展社媒采集平台。pnpm monorepo:
  - `apps/server` NestJS + Prisma(Platform/Account/Post,连 Neon):platform/account/crawler/post 模块,抓取编排=调爬虫→回填账户画像→按 `platformKey_shortcode` upsert 去重→分页。11 jest。
  - `services/crawler` FastAPI + Scrapling(独立部署,Python):IG `web_profile_info` API 解析器(封面 display_url / 完整文案 edge_media_to_caption / 类型 image·video·reel·carousel / 点赞评论 / taken_at),平台注册表可插拔,Bearer 鉴权,scrapling **延迟 import**(无 scrapling 也能跑测)。14 pytest。子代理并行产出。
  - `apps/web` umi4 + antd6 + tailwind(关 preflight 避免与 antd 冲突):平台切换 + 账户增删选 + 画像卡片 + 帖子表格(封面缩略图/文案展开/分页)+ 抓取按钮。2 vitest。
  - 部署:server Vercel serverless(api/index.ts + rewrite + migrate deploy + prisma binaryTargets rhel-openssl-3.0.x + 初始 migration)、web Vercel SPA、crawler Dockerfile($PORT)+ render.yaml。docs/05-部署.md。
- **为什么**:用户需求——指定平台+账户抓帖子(封面+完整文案)统一表格展示,平台可扩展,最终部署 Vercel。参考 sitin-next `app-ins-scripts/getProfilePostImage.js` 的 IG 抓取口径。
- **结果**:三端测试全绿 **27 passed**(server 11 + web 2 + crawler 14),各端 type-check/lint/build 通过。5 个 commit 已推 main:36ab34f→d8fd05e→e831f15→c63b539→f369b01→e239b4d。
- **关键决策/坑**:
  - 架构岔路:Scrapling 是 Python+浏览器跑不了 Vercel Serverless → 拆成独立容器服务,NestJS 通过 HTTP 调它(用户拍板)。DB 用 Neon。
  - **INS 不靠浏览器 DOM**:直接打 `www.instagram.com/api/v1/users/web_profile_info/?username=X` + header `x-ig-app-id: 936619743392459` + 登录 cookie,一次拿画像+帖子+封面+文案。比滚动 DOM 稳且省。
  - 本机全局 `NODE_ENV=production` → vitest 里 React 加载 production 构建、`act` 不可用;vitest.config 里 `env:{NODE_ENV:'test'}` 修复。
  - umi tsconfig 要 `extends ./src/.umi/tsconfig.json` 才认 `umi` 的 Outlet 等导出;tailwindcss 插件需项目根 `tailwind.css` 入口;esbuild helper 冲突加 `esbuildMinifyIIFE:true`。
  - 无库也能出初始 migration:`prisma migrate diff --from-empty --to-schema-datamodel schema.prisma --script`。
- **残留**:全链路联调需用户给 Neon `DATABASE_URL` + `IG_COOKIE`(运营 IG 登录 cookie);三个托管项目需用户实际创建并填环境变量(按 docs/05-部署.md)。

### 04:55 social-post 前端重做为 antd Pro 管理系统 + 定位 405

- **做了什么**:按用户要求把 `apps/web` 从原生 antd 拼装重做成 **ProLayout + Pro Components** 管理系统:ProLayout 外壳(左侧平台菜单)、账户 ProTable(新增账户 ModalForm + 行操作 抓取/帖子/删除)、帖子放 Drawer(ProDescriptions 画像 + 帖子 ProTable)。后端 `/api/platforms` 改为返回全部平台(含禁用),seed 加 `facebook`(enabled=false),前端置灰显示"敬请期待"。抽 `utils/format.ts` + 4 vitest。
- **关键决策(硬冲突)**:`@ant-design/pro-components@2.8.10` peer 只支持 antd `^4||^5`,**不支持 antd v6**。用户要用 pro 组件 → 把 antd 从 v6 **降到 v5(5.29.3)**、icons 降 v5。强行 v6 会让 ProLayout/ProTable 运行时崩。已写进技术文档。
- **405 根因**:用户截图请求打到 `https://social-post-web-two.vercel.app/api/accounts`(前端自己的域名),被 SPA 的 index.html 静态兜底接住,POST 静态文件→405。根因:web 没配后端地址,`UMI_APP_API_BASE` 默认 `/api` 变成相对路径打自己。**修法**:在 web 的 Vercel project 设 `UMI_APP_API_BASE=https://<server域名>/api` 后重部署 web。
- **验证**:装 puppeteer-core 到 scratchpad,静态服务 dist + 请求拦截 mock API,headless Chrome 渲染截图:**零运行时错误**,标题/Facebook 置灰/账户表都正常。
- **结果**:server 11 + web 4 测试全绿,type-check/lint/build 过。commit 74d2dba 已推。
- **残留**:① 用户需在 web Vercel 配 `UMI_APP_API_BASE` 修 405;② server 需已部署且跑过 migrate+seed 平台菜单才有数据;③ 全链路真实抓取仍待 Neon `DATABASE_URL` + `IG_COOKIE`。
