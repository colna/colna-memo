---
title: 2026-09-10
date: 2026-09-10
tags: daily
---

# 2026-09-10

## 今日重点

- [ ] Luka 划卡页四条运行时改动实机验：首卡曝光上报 / 飞出途中乱点会不会丢卡 / 拖到快划走时才震 / 卡片顶部能不能点
- [ ] Luka「Global」tab 定方案 —— 设计稿画 `Nearby / Global`，代码是 `Nearby / Feed`，`useDiscoveryFeed` 没有 range 参数、后端也没有 global 概念
- [ ] Luka 底部标签栏 IA 未定：设计稿 Discover/Nearby/Match/Messages/Profile vs 已合并 #458 的 Chats/Calls/People/Discover/Me
- [ ] koda buglist R00218/R00219/R00220（订阅 / 访客订阅 / like+访客数据）——已定位，等后端确认 koda 租户商品与数据
- [ ] koda 端到端 EAS 真机验：审核双号登录（`review@` / `metareview@kodasoc.com`，同密码）、恋爱铃三子系统（需后端推 S2C）、图片发送
- [ ] `apps/koda/tests/config.test.ts` pre-existing 失败（`appName` 实为 `koda_android`，断言写的 `koda`）+ BytePlus appId/appName 仍是 lumi 串包值
- [ ] Poo 注册流程待确认三项：身高字段去留 / Android 登录按钮几颗 / 开屏视频与开屏图先后
- [ ] 是否把 muskapi 接成 image-gen 的第三条链路（BASE 可配置 + `--transparent` + `--quality`）—— 已问用户，未擅自改

## 工作日志

## 工作日志 — sitin-rn PR #465 merge（Luka 划卡页按 Figma 重做）

合并提交 `8c96b1a2` → `feature/luka-ios`，19 个提交。按四要素记：

**做了什么**

- 划卡容器从纵向分页滚动 `CardScroll` 换成堆叠式 `CardStack`（一屏一张、只能上滑）
- 卡片按 Figma 428:874/886 重做：照片拼贴、Common Ground 粉卡、Interests 标签、Say hi/✕/♡ 三个出口
- `DeckColor` 独立于全局 theme 的暖色板

**遇到什么问题 / 怎么解决**（这次踩的坑，都值得记）

1. **换卡闪一下**。两处叠加：① `key={候选人 id}` 让每换一张整棵子树卸载重建，**明明已经在背面加载好的照片被扔掉重来**，那一两帧画的是 `loadingColor` 占位色；② `translateY.value = 0` 写在 `advance` 里，而 **shared value 的写入比 React commit 早一帧生效** —— 那一帧 index 还是旧的、transform 已归零，刚飞出去的卡原地满不透明画了一帧。
   → 改成**两个槽轮流当正面**（`index % 2`），槽的 key 稳定，背面槽转正时演的还是同一个人、同一棵子树；归零改成归**刚转到背面**那个槽的零，放进 `useEffect([index])`。
2. **槽复用把 `greeting` / `favourite` 留给了下一个人**（1 的直接回归）。`greeting` 置 true 后代码里没有任何地方置回 false，于是「对 A 打过招呼后，轮到这个槽的每个人 Say hi 都点不动、卡片也点不开」。
   → 给 `CandidateCard` **按人加 key**。槽只在转到背面时才换人，所以只有被完全盖住的那张重建，正面那张照旧复用、照片不重解码 —— 修状态泄漏又不把闪动带回来。
3. **标签一行放不下切出半颗**。`flex-row`（不换行）+ `overflow: hidden`。设计稿帮不上忙：它的示例数据全是短词（Nature/Food/Art/Dogs/Fitness 正好排满），从没碰到过溢出。
   → 加 `flex-wrap` 但保留固定行高 + 裁剪：放不下的整颗掉到第二行，而第二行完全在裁剪框外 → 要么完整、要么不出现，卡片总高不变。
4. **重写容器时把旧容器攒下的守卫一起丢了**（`/code-review` 抓出来的，10 条全属实）：首卡从不上报展示、游标不夹范围、飞出途中第二次触摸吞掉这次划走、手势每次渲染重建、`remove()` 丢了上报、震动阈值沿用旧容器的 `0.05`（新容器进度已归一化，划走线是 `1`，等于手一动就震）。

**残留**

- **这批改动全部未在模拟器上验证** —— 本机没有 booted 的模拟器，`apps/luka/ios` 是 CNG 生成的不在库里。typecheck / 598 单测 / Biome 都过，但这些改的是运行时行为，静态检查证明不了。
- 「Global」tab 放什么没定（见今日重点）
- `card-scroll.tsx` 现在只剩 `CardScrollHandle` 类型和 `SCROLL_THRESHOLD` 被引用，组件本体已无人用
- `apps/luka` 有 5 条 pre-existing lint warning（blueprint / intro 也有同样的），未清

**顺带**

- #462（UI 生成 prompt 的纯文档 PR）关掉了：一次性 prompt，产出已落进代码，留在仓库只会和实现分叉
- #467（资料页改版）因 `shape-language.test.ts` 的 `MIGRATED` 白名单与 #465 同时改而冲突，已合基线取并集解掉，现在 CLEAN

## 工作日志 — 01:00 profile/[id] 页 UI 出图 + HTML demo 还原

- **做了什么**：
  1. 用 `image-gen` 按用户给的参考图出 profile 详情页 iPhone 17 mockup 两版（A 照片卡+信息卡分离 / B 姓名压在照片渐变上），用户选了 A。
  2. 新建 `/Users/max/Dev2/zhangzheng/profile-page-demo/profile-id.html`（单文件 262KB，素材 base64 内联，零外链零依赖），照 A 版稿 1:1 还原样式。
- **为什么**：用户要「按图 + PRD 写 HTML demo，样式完全还原」。
- **结果**：首屏与稿一致（标签 3+1 换行、bio 两行、Say hi/Message 宽度比）。含骨架屏、滚动折叠导航、action sheet、图片灯箱、toast、CTA 状态机（none → sent → replied）。演示开关 `?id= / ?state= / ?presence= / ?nosk=1 / ?long=1`。
- **PRD 缺口（未闭环）**：飞书 token 失效（`auth list` 是何家希且 `needs_refresh`），拉不到「Poo 技术向PRD」wiki `N848wkTFRij6ruko6tQck0gVnhe`，PRD 内容没结合进去。字段口径暂按 `sitin-rn/apps/intro/src/app/profile/[id].tsx` 的 Candidate 写死在 `PROFILES` 常量里，PRD 一到直接替换该常量即可。
- **踩的三个坑**：
  1. **从 mockup 上裁 hero 素材会把 UI 一起裁进去** —— 图里照片角上本来就画着返回/更多按钮和小头像，CSS 再叠一层就出现「双按钮」重影。修法：用 `--ref` 让 gpt-image-2 重出一张纯照片（prompt 里逐项列 NO phone frame / NO buttons / NO circular overlay）。
  2. **`*{box-sizing:border-box}` 下机身宽度要含边框** —— `.device{width:402px;padding:11px}` 实际屏幕只有 380，导致卡片窄 22px、标签排不下 3 个。应写 `width:424px; height:896px`（402×874pt + 11px×2）。
  3. **AI 出的 mockup 屏幕比例比真机长**（那张图折算 402×932 vs iPhone 17 的 402×874），照抄元素尺寸会溢出约 58px。必须压 hero 高度（`aspect-ratio:374/272`）才塞得进真机一屏。
- **验收**：headless Chrome 截图逐轮比对（`--headless=new --run-all-compositor-stages-before-draw`），并用 `--dump-dom` + `document.title` 回读 `getBoundingClientRect()` 量宽度定位标签换行问题。
- **关键命令**：

  ```bash
  # 量元素实际宽度（headless 里没法开 DevTools 时）
  chrome --headless=new --virtual-time-budget=800 --dump-dom "file://...?nosk=1" | grep -o '<title>[^<]*</title>'
  ```

## 工作日志 — 01:10 profile/[id] demo 加入 Luka 宠物元素

- **做了什么**：在 `profile-page-demo/profile-id.html` 加两处小崽露出，素材取自 `apps/luka/src/assets/mascot/`（`hamster-aside` / `hamster-heart`，按 alpha 裁紧后 base64 内联）。
  1. **Common Ground 条**：兴趣标签下方一条粉色（`#FBE5E0`，deck-theme 的 `commonGround`），小崽从条后探出上半身，说一句共同点 `You both shoot film — say that.`，可点。
  2. **Say hi 里的小崽**：把原来的 👋 换成抱爱心的小崽；点击时它弹跳 + 一颗 ❤️ 从按钮飞出淡出，按钮转 `Sent ✓`。
  3. `?pet=0` 一键关掉两处（Say hi 自动回到 👋），方便和上一版对比。
- **为什么这样放**：09-09 已经删过「鸡蛋横幅 `PetPerchBanner`」和「宠物孵化页」，说明**纯装饰的宠物露出会被砍**。所以这两处都挂在真实功能上——共同点条承载 deck 上那张 Common Ground 粉卡的内容，Say hi 是这一页的主动作。没有 `commonGround` 数据时整条不渲染。
- **坑：「探出」要靠层级而不是位移**。子元素永远画在父元素 background 之上，所以小崽放进条里只会「站在条上」。改法：条的底色用 `::before` 画（`z-index:1`），小崽 `z-index:0`、文字 `z-index:2`，小崽下半身才会被底色盖住。父元素加 `isolation:isolate` 免得 z-index 泄到外面。
- **代价**：首屏多了约 100px，`Her moments` 从「完整可见」变成被底部按钮截掉一半，页面开始需要滚动（折叠导航因此才真正生效）。要保住首屏原样，可以把条挪到 moments 之后。
- **验收**：headless 截图逐轮比对，`?pet=0` 与上一版一致；点击态截到了 ❤️ 飞出 + `Sent ✓`。

## 工作日志 — 01:30 出 Luka 探头+气泡透明底 PNG（品红幕抠像）

- **做了什么**：按用户给的两张图（图1=划卡页里「小崽探出卡沿 + 右侧气泡」的构图，图2=吉祥物角色卡三视图）出一套透明底 PNG。产物在 OUTPUTS_DIR：`luka-peek-bubble@1468/@880`（合成）、`luka-peek@881/@440`（纯仓鼠）、`luka-bubble-only@587`（纯气泡）。
- **两步出图**：① `--ref 图1 --ref 图2` 出构图；② 再拿 ①的结果 + 角色卡做第二轮 edits，用「KEEP EXACTLY AS IS / WHAT MUST VISIBLY CHANGE」把毛色从姜黄改成角色卡的可可棕背 + 焦糖颊 + 大面积奶油白胸腹。**只给正面描述模型不会改色**，必须逐条写「现在哪里错、应该是什么样」。
- **关键坑一：白气泡不能用白底抠**。`cutout_bg.py` 从画布四边 flood fill，白气泡和白底同色且连通到边缘，会被整个打穿。改用 **品红幕 `#FF00FF`**（暖棕仓鼠 + 奶油白 + 粉鼻子 + 白气泡都不含品红），prompt 里写死 `PURE FLAT MAGENTA #FF00FF, absolutely uniform`。
- **关键坑二：毛发边缘 flood fill 根本进不去**。毛丝之间的幕布被前景包住、连不到画布边缘，于是留下一圈**不透明的品红像素**——叠深色底上是刺眼的紫描边，`cutout_bg.py --bg 255,0,255` 解决不了（实测 345069 个边缘像素全部偏品红，做完 unpremultiply 仍剩 12735）。
- **解法：真正的 chroma key，不走 flood fill**。判据 `spill = min(R,B) - G`：品红 = +255，棕毛 −40、奶油白 −11、粉鼻子 −9、白气泡 −5，**对这只仓鼠是唯一的**。`alpha = 1 - clamp(spill/110)`，再按 `C = a·F + (1-a)·BG` 反解去色。结果：可见像素 593012，仍偏品红 **0**，三种底色（深棕/奶油/蓝）复检干净。
- **验收方式**：把结果分别叠到 `#1A1410` / `#FBF3E6` / `#4682C8` 三个底上看边缘，不看棋盘格预览。
- **待定**：要不要把这几张收进 `apps/luka/src/assets/mascot/`；要不要把 chroma key 脚本加进 `image-gen` skill（现在只有 flood fill 那条路）。

## 工作日志 — 01:40 Luka 面部斑纹返工（三色毛 + 额头白毛条）

- **返工原因**：用户指出上一版面部不对——**额头缺了那道奶油白纵向毛条**，整个额头是一片棕，读起来像「一张扁平棕色面具」。
- **用户给的规范**（已按此出图）：`#6A4632` 可可棕（头顶/耳外/爪/背上沿）、`#D49960` 焦糖橙（腮帮/体侧/背中段）、`#F8F1E6` 奶油白（额中竖条/嘴套/下巴/前胸腹）、`#F7B4B4` 蜜桃粉（鼻头/耳内）；要求自然不对称、非镜像斑纹、色块边缘柔和渐变、毛发互相穿插。
- **产出**：A / B 两版，各三档尺寸（`a|b-luka-peek-bubble@1467/880`、`a|b-luka-peek@879/440`）+ 共用的 `luka-bubble-only@588`。两版抠像残留品红均为 **0**。B 版斑纹更不对称、过渡更软。
- **提示词经验（有效）**：
  1. 缺失的元素要**单独点名并标成最重要**——`THE BLAZE — the single most important missing element`，只混在色号列表里模型会漏。
  2. 「自然不对称」必须写成**否定项**：`NO symmetry, NO mirrored patches, NO decal or sticker look`，再补一句具体怎么不对称（毛条略偏中线、一侧颊斑更高更宽）。
  3. 「柔和渐变」同理要写 `NO hard edges, NO crisp outlines, NO flat vector shapes between colour zones`，并说明机制：两色的单根毛互相穿插。
- **坑：飞书 downloads 临时目录会被清空**。`/var/folders/.../metabot-downloads-max/` 里用户发的角色卡三视图在几分钟后就没了（`参考图不存在`），第二轮少了一张 ref。**参考图拿到手第一件事就 cp 到 scratchpad**，别直接引用 downloads 路径做多轮出图。

## 工作日志 — sitin-rn PR #467 merge（Luka 他人资料页整屏重做）

合并提交 `f1ed5d20` → `feature/luka-ios`。按四要素记：

**做了什么**

按新 UI 稿整屏重排：大图从通屏出血改成留白圆角卡（只圆上两角，下沿被白卡压住）、
姓名/事实/自述/兴趣收进一张白卡、兴趣标签改成 emoji + 原样文字的胶囊、「RECENT POST」
小行换成「<name>'s moments」一排三张照片、大图卡左下角趴一只 `stay.mov` 仓鼠、
页码挪到顶部居中。新增 `interestEmoji`（后端 `interests` 只有字符串，没有图标字段），
删掉 7 个被取代的旧组件。

**遇到什么问题 / 怎么解决**

1. **两轮 `/code-review` 抓到的重要问题，全是我自己上一轮改动引入的回归。**
   划卡那次是「按槽复用」把 `greeting`/`favourite` 泄漏给下一个人；这次是把返回/更多
   两颗键从「屏幕钉住」挪进了滚动的大图卡 —— 而「更多」里的举报和拉黑**没有第二个
   入口**，滚下去就点不到了。**改结构时「顺手把 A 挪到 B」最容易掉这种坑。**
2. **`interestEmoji` 的匹配规则错了两轮。** 子串匹配 → `"Competitive spreadsheet
   racing"` 拿到 📚（sp-**read**-sheet）；改成词前缀 → `tea` 仍吃掉 `Team sports`、
   `car` 吃 `Cardio/Career`、`art` 吃 `Artificial`。最终：三四个字母只认整词（允许一个
   复数 s），5 个字母起才按前缀，`running` 这类形态逐个列表。两次都是**测试先发现的**。
3. **用精确字符串替换改代码，被 Biome 的重排坑了两次。** 一次漏改了小标题（报告说改了、
   实际没有），一次当场被 assert 拦住。**每一处替换都要加 assert。**
4. **合并后基线过不了 lint。** 我推完之后有一个 `feat: profile id mov` 直接改了坐标，
   `style={{ bottom: PET_BOTTOM,left:16 }}` 缺空格，`biome check` 报 error，而且
   `className="absolute left-2"` 和 style 里的 `left: 16` 同时定义了 left。已开
   `fix/luka-hero-pet-position` 修掉（只改写法与注释，渲染结果不变），**PR 待提**。

**残留**

- 这条线上四五轮改动**全部没在模拟器上验证过** —— 本机没有 booted 的模拟器，
  `apps/luka/ios` 是 CNG 生成的不在库里。
- `stay.mov` 的透明通道没法本地校验（ffprobe 的软件 HEVC 解码器会丢 alpha 辅助层，
  README 自己记着这条）。实机若是黑底方块就是素材没带 alpha。
- 「Online now」没做：后端没有他人在线状态字段。
- 「Global」tab 放什么仍未定。
- `tilt-head.mov` 已入库但无人引用。

**顺带**：用户授权「后续 Luka 的需求都自动 push」，已写进 bot 记忆
（开 PR / 合 PR / 删分支仍要问）。

## 工作日志 — sitin-rn PR #473 merge（Luka 划卡页大图模式）

合并提交 `afd867ff` → `feature/luka-ios`，9 个提交。按四要素记：

**做了什么**

划卡页大图模式整屏重做：卡片从「照片拼贴 + 一叠信息块」改成**整张卡就是一张照片**，
文字压在底部渐变上（`candidate-card` 288 行 → 148 行）；Say hi / ✕ / ♥ **搬出卡片**
（在卡里会跟着卡飞走）；仓鼠换成 `tilt-head.mov` + 素材气泡；页头 `Luka ♥ + 标语`
换成 `Discover`。删掉四个随之失效的组件。

**遇到什么问题 / 怎么解决**

1. **`/code-review` 抓到 6 条，最要紧的是给假反馈**：每日 like 上限用完后点 Say hi，
   头像照样飞进 Chats 标签（这段动画的唯一语义就是「招呼发出去了」），同时 toast 说
   超限 —— 因为 `setFlight` 写在 `onSayHi` 前面。**修法不是调换两行，而是让
   `useSwipeActions.sayHi` 返回「是否被受理」**，门控条件以后再加一条，调用方不用改。
2. **`onSayHi` 被关在 `measureInWindow` 回调里** → 视图没布局/已 detach 时回调不触发，
   主 CTA 点了完全没反应、连 toast 都没有。同仓库 `nearby-list-row` 早就是反过来写的。
3. **写死的尺寸在小屏上失效**：`SCRIM_HEIGHT = 260` 在 iPhone SE 上等于卡高，渐变从上
   盖到下 —— 正是那行注释自己说的毛病。改成随卡高走。
4. **`PetAnimation` 卸载时崩**（`NotFoundException: Unable to find the native shared
   object`）：`useVideoPlayer` 内部的 `useReleasingSharedObject` 先声明、cleanup 先跑，
   于是「先 release 后 pause」。**一直存在的潜在 bug**，因为唯一的使用者挂在不会卸载的
   tab 屏上；新加的两处（划卡页切列表、资料页返回）才把它炸出来。
5. **共用工作区的分支意外**：改 review 那 6 条时，工作区被从 `feat/luka-deck-big-photo`
   切到了用户新建的 `feat/luka-nearby-list-redesign`，提交落错分支，**而且切换过程中
   `use-swipe-actions.ts` 的改动丢了**（提交里只有 2 个文件），靠 push 前的 typecheck
   才发现。→ **提交前先 `git branch --show-current` 确认**。
6. **用精确字符串改代码被 Biome 的重排坑了第三次**（三元被折成单行）。每处替换都加
   assert，这次当场拦住了。

**残留**

- 这一屏**全程没在模拟器上验证过**（本机没有 booted 的模拟器，`ios/` 是 CNG 生成的）。
  用户逐轮发截图，靠他实机反馈迭代。
- `discoveryCardLayout` / `discovery-card-fit.ts` 已无生产调用方，只剩自己的测试养着 ——
  僵尸代码，未清。
- `lib/discovery-prefetch.ts` 与 `discovery-deck-skeleton.tsx` 的注释还在描述旧的拼贴布局。
- 「Global」tab 放什么仍未定。

**顺带**：`feat/luka-feed-list`（Feed 列表重做 + 发帖按钮上的仓鼠）已合基线，无冲突。

## 工作日志 — 03:10 划卡列表模式按新稿重排（分支 feat/luka-nearby-list-redesign）

- **做了什么**：`apps/luka/src/components/discover-nearby/nearby-list-row.tsx` 按选定的 UI 稿（B 版）重排为固定三行：名字独占一行 + 最右描边心 / 职业·距离·城市合成一行 / 引语（单行截断）+ `Say hi` 胶囊。commit `e4be82aa`，已 push。
- **修的是线上截图里的四个毛病**：① `Say hi` 和名字在同一条 `justify-between` 里分宽度，长名字被挤断、后面那颗心被胶囊圆角切掉；② 心是实心的，会被读成「已收藏」（其实还没接状态）；③ 职业和位置各占一行；④ **字段缺失的人整张卡矮一截**，五张高矮不齐像渲染坏了。
- **关键取舍**：事实行写死 `FACTS_HEIGHT = 18`、有没有内容都占着；引语只给一行并截断，第三行由必然存在的胶囊撑高 —— 行高与内容解耦，列表才扫得动。稿子上的斜体引语**没做**：自定义字体没有 italic 字重文件，Android 合成斜体不可靠，改用 `textMuted` 拉层次。
- **验收**：`pnpm typecheck` 通过、`pnpm lint` 0 error（余下 5 个 warning 是 `measureInWindow` 未用参数，pre-existing，`people-row.tsx` 同款）、`pnpm test` 605 passed / 110 files。只改了 1 个文件。

### ⚠️ 坑：同一个 worktree 上有两个 Claude 会话并行操作

切分支时撞上了另一个会话正在做「划卡页 code review 的 6 条」，连环踩了三次：

1. 我 `git stash push` 拿走了 `use-swipe-actions.ts` 的未提交改动 —— 那其实是**它**工作区的一部分，它随后的提交因此缺文件（`discovery-deck.tsx` 调用 `sayHi` 要 boolean，hook 还是 void，typecheck 会挂）。
2. 我 `git checkout -b` 之后，**它的 commit 落到了我刚建的分支上**（reflog `HEAD@{6}: commit` 时 HEAD 正指着我的新分支）。
3. 我的 `git rebase` 又把它那个提交重写成了 `caca1de1` 挂在我分支上。

**怎么发现的**：提交的 Date 是「刚刚」而我没提交过任何东西 —— 时间戳对不上就该停下查 `git reflog`，别继续 rebase。

**怎么收的**：`git stash pop` 还回去（它已自行补上，pop 后无净变化）→ `git branch -f` 把我的分支重置到**已推送的** `origin/...` 基线，甩掉误落的提交 → `git worktree add /Users/max/Dev2/zhangzheng/.luka-list-wt` 开独立工作树，之后全程在里面干活。

**下次的规矩**：多会话同仓库，先 `git worktree add` 再动手；新 worktree 要单独 `pnpm install`（不继承 node_modules）；**绝不用裸 `git stash` / `git stash pop`**（stash 栈是全仓库共享的）。

## 工作日志 — 03:20 划卡列表 PR #480

- **PR**：https://github.com/presence-io/sitin-rn/pull/480 —— `feat/luka-nearby-list-redesign` → **`feature/luka-ios`**，作者 colna，1 个文件 +75/−23。
- **开 PR 前踩的坑：base 选错会把别人的活一起带上。**
  - 我的分支原本基于 `feat/luka-deck-big-photo`，开 PR 时那个分支**在远端已被删除**（并行会话已合并），`git fetch origin <它>` 直接 `couldn't find remote ref`。
  - `origin/main` 上**根本没有 `apps/luka`** —— Luka 是独立开发线，base 不能选 main。
  - 真正的集成分支是 **`origin/feature/luka-ios`**（它上面的提交都带 `(#473)` 这种 PR 号）。而我的分支相对它**领先 11 个提交**，其中 10 个是并行会话尚未合并的工作。
  - 解法：`git rebase --onto origin/feature/luka-ios cba0c88a` 把我那**一个**提交摘出来，再 `git push --force-with-lease`。事前先 `git diff --stat origin/feature/luka-ios cba0c88a -- <我改的文件>` 确认为空（两边该文件一致 → 不会冲突）。
  - rebase 后**重跑了一遍** typecheck / lint / test（605 passed），再推。
- **判断 base 的通用办法**：`git log --oneline -1 origin/<候选分支>`，提交信息里带 `(#NNN)` 的那个就是大家往里合的集成分支；再用 `git rev-list --count origin/<base>..HEAD` 看自己领先几个 —— 超过自己实际提交数，就说明基线选错了，要先 `rebase --onto`。

## 工作日志 — 03:35 列表模式真机走查两处修正（PR #480 第二个提交）

- **用户反馈**：真机截图对比 UI 稿，「按钮 padding 和整体背景色与 UI 不一致」。
- **底色（根因）**：`nearby-view.tsx` 的列表容器用的是全局 `Colors.surface` = **`#FFFFFF`**，而卡片视图用的是 `DeckColor.background` = **`#FBF3E6`**。两者是同一个页面的两种形态（右上角开关切换），底色不一致 → 切换时背景闪白；更要命的是列表里的卡是 `DeckColor.surface`（`#FFFDF8`），**压在纯白上几乎看不出边界**，卡与卡之间那道本该透出页面底色的缝也没了。`nearby-list-skeleton.tsx` 同一处问题，一并改。
- **「Say hi」**：稿子上胶囊高度约为头像的三成，真机是 `44pt` 对 `76pt` 头像 = 六成，成了整行最重的元素，把同行引语挤到只剩几个字。收到 `34pt`，`px-5 → px-3.5`、图标 `17 → 14`、字号 `15 → 13.5`；补 `hitSlop {top:6,bottom:6,left:8,right:8}` 把可点高度还原到 46pt。
- **量稿子尺寸的方法（重要）**：AI 出的 UI 稿**内部比例不自洽**——同一张图里头像折算下来是 74.6pt（≈ 代码的 76，对得上），但名字字号折算只有 15.6pt（代码是 19），胶囊折算 21pt（点都点不动）。所以**不能按图上像素等比换算**，要挑一个两边已经对齐的元素当基准（这次是头像），再用「目标元素 ÷ 基准元素」的**比值**去定尺寸。
- **验收**：typecheck ✅ / lint 0 error ✅ / test 605 passed ✅。commit `43d0435e`，已 push，PR #480 现在 2 个提交 4 个文件 +99/−31。
- **遗留（未动，超出本次反馈范围）**：`nearby-list-skeleton.tsx` 的占位块还是 `borderRadius: 3` 的方头像和方块，和现在的圆头像 / 圆角 20 卡片对不上（模板遗留）。

## 工作日志 — 03:50 列表行右侧与头像等高（PR #480 第三个提交）

- **用户反馈**：截图圈出第三行（引语 + Say hi），要求「这一部分矮一点，文字和按钮区域的高度要和头像高度一致」。
- **根因**：右侧三行是**自然高度堆出来的**（名字 26 + mt 6 + 事实 18 + mt 8 + 按钮 34 ≈ 92pt），而头像只有 76pt —— 卡片被右侧撑高，头像底下空出一截。
- **改法**：给右侧容器写死 `height: AVATAR`，行间距交给 `justify-between` 分。这样「和头像一致」是**结构保证的**，不是靠调 margin 凑的；整张卡的高度从此只由头像一个数决定。
- **代价：三行高度变成一笔互相咬合的账**，必须加起来放得下：

      名字 22（写死 lineHeight）+ 事实 16（原 18）+ 按钮 28（原 34）= 66
      头像 76 → 余 10pt，justify-between 分成两道行间距（各 5pt）

  卡片高度 124pt → 108pt。胶囊收到 28pt 后 `hitSlop` 上下 6 → 9，可点高度仍是 46pt。
- **写死 lineHeight 的原因**：交给字体默认行高的话，名字行是「大约 26」，`justify-between` 就分不匀 —— 要按固定总高分配，每一行占多少都得是确定的数。
- **注释里留了账**：两个文件都写明「改任何一个数之前先把这笔账重算一遍」，跨文件的耦合光靠命名藏不住。
- **验收**：typecheck ✅ / lint 0 error ✅ / test 605 passed ✅。commit `32e5cdfd`，已 push。

## PR merge — #480 划卡列表模式改版（feature/luka-ios，merge `5546b6d0`）

**做了什么** —— 3 个提交，4 个文件 +115/−32：

1. `b8db5613` 列表行右侧从「名字与按钮同行」重排为固定三行（名字 + 最右描边心 / 职业·距离·城市合一行 / 引语 + `Say hi`）。
2. `43d0435e` 列表容器底色 `Colors.surface`(#FFFFFF) → `DeckColor.background`(#FBF3E6)，与卡片视图一致；骨架屏一并改；`Say hi` 胶囊收紧。
3. `32e5cdfd` 右侧容器写死 `height: AVATAR` + `justify-between`，整块与头像等高，卡片 124pt → 108pt。

**遇到什么问题**

- **同一个 worktree 上有另一个 Claude 会话并行操作**：我 `git stash` 拿走了它的未提交改动、它的 commit 落到了我刚建的分支上、我的 `rebase` 又把那个提交搬进了我的历史。
- **开 PR 时基线分支已被删除**：`feat/luka-deck-big-photo` 在远端没了，`origin/main` 上又根本没有 `apps/luka`（Luka 是独立线），我的分支相对真正的集成分支领先 11 个提交（10 个是别人的）。
- **rebase 后强推，把已经拉过这个分支的人搞出冲突**（用户反馈「拉取为什么有冲突」）。
- **照 AI 出的 UI 稿定尺寸定错了**：稿子内部比例不自洽，按像素等比换算会得出「按钮 21pt 高」这种不能用的结论。

**怎么解决**

- 并行冲突：`git stash pop` 还回去 → `git branch -f` 重置到已推送基线甩掉误落的提交 → `git worktree add /Users/max/Dev2/zhangzheng/.luka-list-wt` 全程隔离。
- base：`git rebase --onto origin/feature/luka-ios <基点>` 把自己那一个提交摘出来，`--force-with-lease` 推；rebase 后**重跑一遍**验证再推。
- 强推冲突：给对方 `git reset --hard origin/<分支>`（先确认本地没有自己的提交），否则 `git rebase --onto`。
- 定尺寸：挑一个两边已对齐的元素当基准（头像 74.6pt ≈ 代码 76），用**比值**而不是像素等比。

**残留**

- `cba0c88a`「划卡页 code review 的 6 条」等 **10 个提交现在不在任何远端分支上**（那条线的分支被删了又没合进 `feature/luka-ios`）。不是有意丢弃的话需要有人捞回来，本地 reflog 还找得到。
- 列表行那颗心仍是**装饰**，没接收藏状态（点了没反应）。要接得给 `NearbyListRow` 加 `favourite`/`onLike` 并从 `nearby-view` 传下来。
- `useNearbySayHi` 有「超限时照播飞头像动画、给出假的『已发送』反馈」的问题（划卡组 `useSwipeActions` 已修，列表这条路没跟改）。
- `nearby-list-skeleton.tsx` 的占位块还是 `borderRadius: 3` 的方头像方块，与圆头像 / 圆角 20 卡片对不上（模板遗留）。
- 基线上 `pnpm-lock.yaml` 与 `package.json` 不同步（缺 `@expo-google-fonts/caveat`），install 会自动补，我每次都还原掉没混进提交。

## 工作日志 — sitin-rn PR #478 / #481 merge（Luka Feed 列表 + 资料页毛玻璃）

`aca60d02`、`7c27643c` → `feature/luka-ios`。两个 PR 一起记：

**做了什么**

- **#478 Feed 列表**：每条帖子从「串起来的行 + 发丝线」改成**独立白卡**；照片区从横向
  轮播改成三宫格；顶部隐私提示改成浅绿胶囊；FAB 改整圆并在上方放一只 `luka.mov`。
- **#481 资料页**：底部加**上清下糊的毛玻璃渐变**；仓鼠从被裁掉半只改成压在白卡上。

**遇到什么问题 / 怎么解决**（这两轮真正学到的）

1. **渐变模糊做不出来的原因不是渐变没调好。** `BlurView` 的 `intensity` 是整块统一的。
   叠层能做出「越往下越糊」，但**最上面那层的顶边是一条硬切的模糊线**，而底色渐变
   救不了它 —— **渐变淡的是颜色，不是模糊**。修法是给每个 `BlurView` 套 `opacity`
   （= 模糊画面与原画面按比例混合 = 部分模糊），顶层压到 0.12，硬边消失。
   要更平滑该**加层**，不该调大 `intensity`（后者让台阶更明显）。
2. **`zIndex` 只在同级之间排序，子元素抬不过父级的兄弟。** 资料页仓鼠是被
   `overflow: hidden` **裁掉**的，不是被盖住的 —— 调多大都没用。得把它挪出裁剪框，
   并作为「后渲染的兄弟」自然压上去，一个 `zIndex` 都不用写。
3. **卡片换底色，坐在它上面的东西也得跟着换。** Feed 卡片改白底后，白底的认证徽标和
   「Sent ✓」胶囊**整个消失**（只剩浮着的字）。这类问题 typecheck / lint / 测试全绿。
4. **素材的透明边让所有「压进去多少」的数都推算不出来。** 划卡页仓鼠、FAB 仓鼠、资料页
   仓鼠，三处都因为这个反复调（16→35、10→45/46、22→17）。**注释里必须写明「只能实机
   对，换素材要重对」**，否则下一个人会以为它能从边距推出来。
5. **共用工作区又踩了两次**：`cd apps/luka` 在已经处于该目录时失败 → `&&` 链断掉 →
   编辑根本没执行（靠 grep 复核才发现）；以及推送时撞上用户在同一分支的提交。
   → **改完必须 grep 复核落地，`git add` 前先看分支。**

**残留**

- 全程没在模拟器上验证过；靠用户逐轮发截图迭代（硬边、高度、被裁，都是截图上先发现的）。
- Android 上 `expo-blur` 没有真模糊（要开 `experimentalBlurMethod`，掉帧），退化成只有
  底色渐变。要不要开未定。
- `discoveryCardLayout` / `discovery-card-fit.ts` 仍是僵尸代码。
- 「Global」tab 放什么仍未定。
- 远端还挂着若干已合并/已关闭的 luka 分支未清：`feat/luka-nearby-list-redesign`、
  `feat/luka-profile-screen`、`feat/luka-tab-icons`、`chore/luka-assets`、
  `docs/luka-ui-prompts`、`personal/zz/luka-login`、`personal/zz/luka-signup-flow`。
