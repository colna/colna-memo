---
title: 2026-07-15
date: 2026-07-15
tags: [daily]
---

# 2026-07-15

## 今日重点
- [ ] APK 键盘输入框「直接到位」版真机验(新分支 `personal/zz/pwa-kb-snap-height`)

## 工作日志 — APK 键盘 PR #608 收尾 + merge feature + 合并

- **清理调试**:①`bridge.ts` 移除页面日志浮层(`kbDebugLog`/`kbDebugEl`/`dbgPrev*`)+ focusin/focusout 探针(commit `8222e943f`);②`pages/Home/index.tsx` 删本分支加的临时 debug 按钮(黑色「debug」→ `/dev/chat-input-bar`,注释写着 Remove before ship)+ 无用 `useNavigate`(commit `15eede2ff`)。
- **PR commit 混入他人提交之谜**:PR #608 里除 colna 还显示 surmon-china / cjkunwang 的 commit。根因=**本分支落后 feature 9 个 commit**,GitHub PR 的 commit 列表按 merge-base..head 算,把之前 merge feature(`a5c8f2579`)带入、但在当前 feature 主线外的别人 commit 也列了进来。**不是有人推我分支**,三点 diff `feature...HEAD` 净改动仅 5 个键盘文件。
- **diff 姿势坑(重要)**:`git diff A..B`(两点)= 两端点直接差异,会把「B 落后 A 的部分」显示成反向差异 → 误以为分支有无关改动。看分支真正净改动要用**三点** `git diff A...B`(相对 merge-base),与 GitHub PR 口径一致。
- **merge 最新 feature**:`git merge origin/feature/sitin4.0` 进本分支,**无冲突**(ort 自动合并,feature 领先 9 commit 含 #610 WAV),merge commit `db8cc9952`。push 后 PR #608 commit 列表变干净(全 colna 36 个),别人的 commit 归位到 base 侧消失。tsc 0 / lint 0。
- **结果**:PR [#608](https://github.com/presence-io/sitin-next/pull/608) 已 **merged** 进 feature/sitin4.0(`9fb2616e0`,06:18Z)。APK 键盘 workaround(逐帧跟手 + settle 补位版)落地。

## 工作日志 — 键盘输入框「直接到位」版(新分支 pwa-kb-snap-height)

- **背景**:上版「跟手 + 停 180ms 补位」用户嫌补位不够直接,要「直接到位」。从最新 feature 切新分支 `personal/zz/pwa-kb-snap-height`。
- **方案**=floor 到历史最大高度(位置永远对)+ 给输入框上抬**加 CSS 过渡**,一步平滑滑到位,去掉逐帧跟手 + settle 补位。
- **改动**(未 commit):①`bridge.ts` handler 简化为 `show ? Math.max(0, lastKbFullHeight - KEYBOARD_TOP_GAP) : 0` 一步设终值,删 `kbSettleTimer`;②三处输入区 transform 过渡 `transform 0.04s linear` → `transform 0.2s cubic-bezier(0.2,0,0,1)`(`Chat/index.tsx` / `TutorialChat/index.tsx` / `ChatInputBarPreview/index.tsx`,`--kb-height` 消费点)。
- **权衡**:二次弹出已有历史高度 → 一来就滑到正确位;首次弹出无历史值,随 native 累积,若那次少报会略偏低、之后即修正。tsc 0 / lint 0,**待真机验观感**。
- **消费点定位**:`--kb-height` 由三处 `translate3d(0, calc(-1 * max(0px, var(--kb-height) - var(--nav-height))), 0)` 消费;`bridge.ts` 只负责设 `--kb-height`/`--nav-height`。

## 工作日志 — 语音 webm→WAV(PR #610,已合并)

- 详见 [[../50-Daily/2026-07-14]]。PR #610(`personal/zz/pwa-voice-webm-to-wav`)已 merge:非 AAC 语音发送前 `convertWebMToWav` 转 WAV;review 修复(fallback MIME 按 ext 兜底、保留正则 m4a 匹配 audio/x-m4a);加发送格式日志 `[voice] send: recorded=.. -> sent=..`。真机实测:iOS/新 WebView 录 `audio/mp4;codecs=mp4a.40.2` 直发(不转码),符合预期。

## 工作日志 — 键盘上抬纯 CSS 驱动版(PR #615)

- **接续**:上版「snap 到历史高度 + 0.2s CSS 过渡」(commit `e482ac120`)真机反馈**下落又不跟手**。根因:native 收起时先逐帧发 `show=true` 且高度骤降(非立刻 `show=false`),snap 逻辑 hold 在完整高度直到最后 `show=false` 才落 → 下落滞后。
- **改法(用户要「上升下落都改 CSS、不再依赖 native 逐帧回调」)**:handler 每状态只写一次 `--kb-height`(开→`lastKbFullHeight-GAP`、关→0),升降全交 CSS transition;新增**收起检测**——`show=true` 但 `height < kbPrevHeight - KB_RETRACT_DELTA(60)` 判定键盘在收 → 目标设 0 及时下滑。阈值 60 高于「回前台少报工具栏 ~30px」跌幅,回前台不误判成收起。commit `0b065b8ff`。
- **调试设施**:按用户要求先把 debug 加回本分支(`kbDebugLog` 浮层 + 首页 debug 按钮),日志加了 `ret=`/`-> target` 便于调阈值。
- **PR [#615](https://github.com/presence-io/sitin-next/pull/615)**(`personal/zz/pwa-kb-snap-height` → feature/sitin4.0)。**PR 描述已标注含临时调试,验证/调好 KB_RETRACT_DELTA 后单独 commit 移除再合并**。tsc0/lint0,真机观感待验。
- **消费点**:`Chat`/`TutorialChat`/`ChatInputBarPreview` 三处 transform 过渡 `0.04s linear`→`0.2s cubic-bezier(0.2,0,0,1)`。

## 工作日志 — 键盘上抬:纯 CSS 驱动定稿 + 重构出 keyboardLift 模块(PR #615)

- **多轮真机迭代(靠页面日志浮层)最终定稿逻辑**(全在 `app-pwa`):
  - **上升**:`--kb-height` 一次性写目标(不逐帧追 native),CSS transform `0.2s cubic-bezier(0.2,0,0,1)` 缓动;完整高度**持久化 localStorage**,冷启动首次即一步到位(否则逐帧累积会被 CSS 拖慢)。
  - **下降(跟手)**:native 收起是逐帧 `show=true` 高度平滑降到 0、末段抖动(h 0/1/2),最后才 `show=false`。用**收起锁存**:高度从近满峰值回落 > `KB_RETRACT_DELTA(45)` 即 latch `closing`→target 0,**保持到 show=false**,末段抖动不再翻转(修「落到底又弹一次」)。
  - **缓存防污染**:按屏幕方向分 key(`kb_full_height_px_portrait/_landscape`,旋转时切换+重置)、只缓存落在 viewport `20%~70%` 的高度(挡表情面板/异常值/回前台中间值)。
- **重构**(commit `8ac4cc0ef`):把全部键盘逻辑从 `utils/bridge.ts` 抽到新 `utils/keyboardLift.ts`,只暴露 `handleNativeKeyboardChanged(data)` + `clearCachedKbHeight()`;`bridge.ts` 回归纯端能力挂载(handler 一行转发)。**删掉冗余** `visibilitychange` 回前台兜底 + `readActiveInputKbHeight`(native 回前台会重发 notify,handler 已覆盖)。
- **消费点**:`Chat`/`TutorialChat`/`ChatInputBarPreview` 三处输入区 `translate3d(0, calc(-1*max(0px, var(--kb-height)-var(--nav-height))), 0)` + `transition transform 0.2s`。
- **测试页辅助**:`/dev/chat-input-bar` 加「清除键盘高度缓存」按钮(测冷启动)、mock nav 接 `useKeyboardOpen` web 端键盘弹起隐藏(对齐真实 `KeyboardAwareTabBar`,web 浏览器会 resize 视口顶起 flow nav)。
- **PR [#615](https://github.com/presence-io/sitin-next/pull/615)** 仍含临时调试浮层+首页 debug 按钮(PR 描述已标注,验证 OK 后单独 commit 移除再合并)。tsc0/lint0。

## 工作日志 — 键盘上抬收尾:清理调试 + 一致性抽取(PR #615 定稿)

- **各 inputbar 一致性**:Chat / TutorialChat / `/dev/chat-input-bar` 三处输入区原各自内联相同 lift transform,抽成 `keyboardLift.ts` 导出的单一常量 **`KEYBOARD_LIFT_STYLE`**,三处引用,效果永不漂移(commit `47b722541`)。
- **收尾清理**(commit `fe63629c8`):删掉全部临时调试——`keyboardLift.ts` 页面日志浮层(`kbDebugLog`/`kbDebugEl`/`dbgPrevWrote`)+ 各处调用、`clearCachedKbHeight` helper、测试页「清除缓存」按钮、首页 debug 入口按钮 + `useNavigate`。**保留**测试页 mock nav 的 web 键盘隐藏(真修复非调试)。
- **PR [#615](https://github.com/presence-io/sitin-next/pull/615)** 描述已更新为干净版(移除「含临时调试」警告段)。tsc0/lint0。**待合并**。
- **最终文件结构**:`utils/keyboardLift.ts` = 键盘上抬全部逻辑(按方向缓存+sanity+收起锁存+rAF 写 --kb-height+`KEYBOARD_LIFT_STYLE`),对外仅 `handleNativeKeyboardChanged(data)` + `KEYBOARD_LIFT_STYLE`;`bridge.ts` 纯端能力挂载。
