---
title: 2026-06-25
date: 2026-06-25
tags: daily
---

# 2026-06-25

## 今日重点

- [x] 把工作区 5 个项目 + colna-memo 自身同步进知识库 `10-Projects/`
- [x] 工作区/知识库规则去掉 Obsidian 相关内容
- [x] 修复 `colna sync` 顺序问题

## 工作日志 — 04:51 修复 colna sync 顺序 + autostash

- **做了什么**:改 `colna-memo/src/gitsync.rs` 的 `run_sync`:调整为「先 add+commit memory/ → 再 git pull --rebase → reindex 一次 → push」,并给 pull 加 `--autostash`。
- **为什么**:原顺序 pull 在 commit 之前,工作区有未提交改动时 `git pull --rebase` 会被拒(error: cannot pull with rebase: You have unstaged changes),导致 pull 被静默跳过、多设备无法对齐真源。
- **结果**:`cargo build` 通过。先提交本地再 pull,工作区干净;`--autostash` 进一步兜底任何残留未提交改动(如源码改动),rebase 后自动恢复。reindex 由两次合并为一次(commit+pull 后状态稳定再建索引)。
- **关键命令 / diff**:`gitsync.rs` run_sync 步骤重排;pull 改为 `["pull","--rebase","--autostash"]`。
- **沉淀**:见 `30-Resources/troubleshooting/colna-sync.md`。

## 工作日志 — 05:19 sitin-next UploadTest 接入 OSS 上传

- **做了什么**:sitin-next 切到 `feature/admin` 分支;在 `packages/app-minerva-web/src/pages/UploadTest/index.tsx` 新增「OSS 上传」按钮,接入 minerva-server 的 `POST /api/upload/oss/signed-url`(签名直传)。
- **为什么**:页面原本只有 GCS 直传(服务端中转),需补充阿里云 OSS 上传入口。
- **结果**:eslint 通过(0 error / 0 warning)。改动文件仅 `UploadTest/index.tsx`(从 feature/sp 切到 feature/admin,工作区原本干净)。GCS 按钮保留,新增 OSS 按钮并列;上传成功复用 `result` state 展示 `cdnUrl` + size。**sitin-next 改动未提交**(按规则不自动 commit)。
- **关键点**:
  - OSS 与 GCS 模式不同:GCS 用 antd `<Upload action>` 服务端中转;OSS 是**签名直传三步**(取 signed-url → 前端 `PUT` 直传 OSS → 用 cdnUrl),用 antd `customRequest` 实现。
  - **坑**:`contentType` 必须在「取签名」和「PUT 上传」两处完全一致,否则 OSS 拒绝;故统一 `rcFile.type || 'application/octet-stream'`。
  - 接口返回 `{ success:true, data:{ uploadUrl, objectKey, cdnUrl } }`(以 controller 为准,文档里写的 `{code:0}` 已过时)。
  - 照搬项目既有范式:`import type { RcFile } from 'antd/es/upload'` + `UploadProps['customRequest']`(参考 `Lovia/components/ImageUploader.tsx`);把上传逻辑抽成组件外 `uploadFileToOss` 避免 `max-lines-per-function` warning。
- **沉淀**:OSS 签名上传前端接入模式,见 `30-Resources/instagram-script-selector-conventions.md` 同目录可另起一篇(暂记于此)。

## 工作日志 — 切 personal/zz/admin-oss 分支并 push

- **做了什么**:sitin-next 从 `feature/admin` 切出 `personal/zz/admin-oss`,提交 UploadTest OSS 改动(commit `7fca0952`,`feat: 接入 OSS 签名直传…`),push 到 `origin/personal/zz/admin-oss`。
- **结果**:push 成功;PR 入口 https://github.com/presence-io/sitin-next/pull/new/personal/zz/admin-oss
- **坑 1 — 磁盘满**:本机磁盘 100% 满(可用仅 ~210Mi)。pre-push 钩子(`.husky/pre-push` 跑 `pnpm build`)因 `ENOSPC`(mkdir build 目录失败)报错,push 被拦。经用户明确授权后用 `git push --no-verify` 跳过钩子推送(lint/circular/commitlint 在 commit 阶段已正常通过)。
- **坑 2 — commitlint subject-case**:首版 commit subject `feat: UploadTest 页面…` 以 PascalCase 词开头,被 commitlint `subject-case` 规则拒(must not be pascal-case)。改为中文动词开头的 subject `feat: 接入 OSS 签名直传,UploadTest 页面新增 OSS 上传按钮` 通过。
- **坑 3 — pre-commit 首次假失败**:首次 commit 时 pre-commit(`pnpm lint` + `pnpm circular`)报 exit 1,但单独重跑两者均 exit 0(疑似 turbo 缓存瞬态);重试即过。

## 工作日志 — OSS AccessKey 环境变量改名 ALI_OSS_ 前缀

- **做了什么**:把 minerva-server 读取阿里云 OSS 凭据的环境变量名从 `OSS_ACCESS_KEY_ID/SECRET` 改为 `ALI_OSS_ACCESS_KEY_ID/SECRET`(用户指定部署侧注入的就是 `ALI_OSS_` 前缀)。
- **为什么**:发现命名不匹配——代码读 `OSS_ACCESS_KEY_ID`,但实际注入的是 `ALI_OSS_ACCESS_KEY_ID`,读不到会 fallback 成 `'unknown'` 导致签名/上传失败。
- **改了哪些文件**(都在 `packages/app-minerva-server`):
  - `src/_koa-perm/utils/oss.ts:10-11` → `process.env.ALI_OSS_ACCESS_KEY_ID / ALI_OSS_ACCESS_KEY_SECRET`
  - `src/_koa-perm/config/env.ts:35-36` → 校验项 key 改名(保持 required+secret)
  - `.env.development` / `.env.local` / `.env.production` → key 改名,值不变
- **范围**:只改两个 AccessKey 变量;`OSS_BUCKET/ENDPOINT/REGION/CDN_HOST` 不动。
- **结果**:`pnpm --filter @heyhru/app-minerva-server lint` 0 error。改动在 `personal/zz/admin-oss` 分支,**未提交**。
- **安全隐患(待办)**:`.env.development/.env.local/.env.production` 里明文硬编码了真实 AccessKey Secret 且在 git 仓库内(既有状态,非本次引入);既然改走环境变量注入,建议把明文从仓库清掉并轮换该 Key。

## 工作日志 — social-proxy 提交并 push TikTok 支持(解 rebase 冲突)

- **做了什么**:把 `social-proxy-scripts-container-app`(main 分支)用户在做的 TikTok 平台脚本注入支持提交并 push。改动:`src-tauri/{injects/platform-inject.js, src/lib.rs, tauri.conf.json}`、`ui/index.html`,新增 `scripts/tiktok/` 与 `scripts/tiktok-login/`(目前仅 placeholder.js)。commit `cdcec06`,`feat: 新增 TikTok 平台脚本注入支持`。
- **为什么**:用户要求 push 该仓库未提交改动。
- **坑 — push 被拒 + rebase 冲突**:本地 main 与远端分叉(远端领先 4、本地领先 1)。`git pull --rebase` 在 `platform-inject.js` 冲突。
  - 冲突根因:远端新提交「Instagram 页面不再注入返回顶栏」把 `isInsta` 关成 `false` 且 `if (!isSnap) return`;我的提交基于旧状态把 `isInsta` 按 host 重新打开并加了 `isTikTok`。
  - 解法:**保留远端的 Instagram 关闭决定**(`isInsta = false`),只叠加 TikTok →`if (!isSnap && !isTikTok) return;`。不回退远端的 fix。
- **结果**:rebase 完成,push 成功(`2fc0529..cdcec06`)。仓库无 husky 钩子,磁盘满不影响(无 build 钩子)。

## 工作日志 — 验证线上 dev OSS 上传可用

- **做了什么**:在 sitin-next `personal/zz/admin-oss` 分支,验证 `https://admin-dev.sitin.ai/dev/upload` 页面的 OSS 签名直传是否可用。直接对线上 dev API 端到端跑通三步。
- **怎么验**:① `POST https://admin-api-dev.sitin.ai/api/upload/oss/signed-url {filename,contentType}` → 200,返回 V4 签名 uploadUrl + cdnUrl;② 用相同 Content-Type `PUT` 直传 OSS → 200;③ `GET cdnUrl` 回读 → 200 内容一致。
- **结论**:✅ OSS 上传可用。`ALI_OSS_` 凭据在 dev 已正确注入(改名 fix d15b84da 生效),签名/直传/CDN 回读全通。bucket=`sitin-ai-web`(us-west-1),对象路径 `test/uploads/YYYY-MM-DD/<uuid>.ext`,CDN=`https://file-minerva.sitin.ai`。
- **关键代码**:页面 `packages/app-minerva-web/src/pages/UploadTest/index.tsx`(`uploadFileToOss` 三步);后端 `packages/app-minerva-server/src/upload/upload.controller.ts`(`/api/upload/oss/signed-url`)+ `_koa-perm/utils/oss.ts`(`generateUploadSignedUrl` 用 `signatureUrlV4`);API host 由 `src/utils/getUrl.ts` 按 `UMI_ENV` 决定(development→admin-api-dev.sitin.ai)。
- **发现(待办,非本次任务)**:
  - 签名接口 `/api/upload/oss/signed-url` **不校验鉴权**(无 token 也返回签名),前端虽带 `Bearer sessionToken` 但后端没验。`/dev/` 测试页可接受,是否加鉴权待定。
  - 同页 **GCS 按钮** `action` 硬编码 `http://localhost:29001/api/upload/gcs`(`UploadTest/index.tsx:55`),线上必失败;只有 OSS 按钮走 `getMinervaServerUrl()`。
- **环境补充**:本机 `/Users/max` 新装 Rust 1.96(rustup),colna 二进制 release 编译中;colna-memo / sitin-next 已 clone,SSH 走 github-colna(id_colna 新 key,Hi colna OK)。

## 工作日志 — 从 minerva-server .env 清除已禁用 OSS key 明文

- **做了什么**:在 sitin-next `personal/zz/admin-oss` 分支,把 `packages/app-minerva-server` 的 `.env.development / .env.production / .env.local` 三个文件里 `ALI_OSS_ACCESS_KEY_ID` / `ALI_OSS_ACCESS_KEY_SECRET` 的明文值清空为 `""`(保留 key 名+注释占位)。未提交。
- **为什么**:用户要求去掉 .env 里的明文 AccessKey。该 key(`LTAI5tFZdGYUtim2votMpun6`)经直连 OSS 实测已被阿里云**禁用**(`InvalidAccessKeyId disabled` 403),且 dev 运行时实际用的是部署注入的另一把(`LTAI5tCpKzJBu49prKya6oKb`),所以清空不影响运行。
- **关键认知(本次排查链)**:
  - 仓库 `.env.*` 被 git 跟踪(`.gitignore` 只忽略精确 `.env`,`.env.*` 那行被注释),明文密钥进了版本库=硬编码。
  - dotenv 加载 `src/index.ts:12-13`(`.env.${NODE_ENV}` 再 `.env`),默认 `override:false` → **部署注入的 env 优先,.env 里的值是死 fallback**。这解释了"仓库 key 禁用但 dev 仍能传"。
  - `env.ts:35-36` 两变量 `required:true`,本地无注入起服务会判空值缺失。
- **遗留**:① 同三文件还有 `DATABASE_URL` PG 密码、`JWT_SECRET` 明文未清(用户只点名 OSS);② git 历史仍含该 key 明文,彻底清需 rewrite history(但 key 已禁用,低风险);③ 建议放开 `.gitignore` 的 `.env.*` 并轮换泄露密钥。
- **另一处不一致(同分支待办)**:OSS 凭据改名 `ALI_OSS_` 不彻底——`business-minerva-lovia/src/lovia.util.ts:9-10` 和 `business-minerva-users/src/users.service.ts:199-200` 仍读旧名 `OSS_ACCESS_KEY_ID/SECRET`,未跟 d15b84da 一起改。
