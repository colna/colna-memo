---
title: 2026-08-17
date: 2026-08-17
tags: daily
---

# 2026-08-17

## 今日重点

- [ ]

## 工作日志

## 工作日志 — 15:09 sitin4.1 修聊天页误弹「Social Requests」交换弹窗

- **做了什么**:`sitin-next` `personal/zz/sitin4.1`(先 pull ff 16 提交对齐远端),改
  `packages/app-pwa/src/hooks/useInsTaskInit.ts` 的 `canShowInsModal()`:聊天页判断
  `pathname.includes("/chat/")` → `pathname.startsWith(CHAT_ROUTE)`,并新增
  `import { CHAT_ROUTE } from "@/constants/routes"`。
- **为什么**:真机截图发现「Social Requests」全局交换弹窗弹在了聊天页,与页内
  `InsExchangeBubble` 气泡重复。根因:聊天页统一路由是 `CHAT_ROUTE="/chat"`(无尾斜杠、
  无 `/chat/:id` 段,sitin4.0 已合并),而 gate 判的是带尾斜杠的 `/chat/`,永远失配 →
  `canShowInsModal` 在聊天页恒返回 true → 弹窗泄漏。全仓其它地方(GlobalAnomalyBanner
  `startsWith(CHAT_ROUTE)`、useOnlineStatusReport/useWorkStateMachine `=== "/chat"`)都判对,
  只此处写错。
- **结果**:1 file +3/-2。eslint 通过;tsc 我的改动 0 新增错误(既有 4 个 `cardType` proto
  基线错误与本次无关,已 stash 对比确认)。commit `0f068a251`,pre-commit lint 52/52 +
  circular 无违规、pre-push build 54/54,已 push `origin/personal/zz/sitin4.1`。
- **关键命令 / diff**:`git pull --ff-only`;`pnpm --filter @heyhru/app-pwa exec eslint/tsc`;
  `git add` 仅该文件(submodule `proto` M 状态未动);`git push origin personal/zz/sitin4.1`。
- **沉淀**:见 troubleshooting `sitin-pwa-chat-route-gate.md`。

### 附:本次会话其它排查(未改码)

- **CE 交换两个抽屉**:`showSocialAuthDrawer.tsx`(「Authorize accounts」多行)vs
  `SocialAuthTaskDrawer.tsx`(chat FE-2.2「Mike's request」CE 任务,commit `1b6e5adf5` 已实现)。
- **授权抽屉关闭时序疑点(待确认后再改)**:`useInsBridgeCallbacks.handleInsAuthResult` 成功回调里
  `setPlatformAuth(platform,true)` 在 `finishSocialBind` 内、`await` 于 `showSocialAuthDrawerFromStore()`
  **之后**执行 → 重弹时读到旧授权态,刚授权的平台被当 not_connected 重新加回(另一平台仍 expired 时抽屉
  又弹出)。修法:把置授权态提到重弹之前。
- **弹窗 socket 机制**:「Social Requests」触发靠腾讯云 IM(TIM,`@tencentcloud/chat` 长连接)推
  `ExchangeRequestMessage` → `checkPendingOrders()` 拉 HTTP `listUserInsExchangeOrder` 决定弹不弹;
  自建 `WebsocketManager` 只管心跳/在线状态,与交换弹窗无关。弹窗内容以 HTTP 为准,订单在别处处理不会
  经 socket 自动关闭弹窗(可能出现不同步)。

## 工作日志 — 16:xx sitin4.1 修「Authorize accounts」授权抽屉反复弹

- **做了什么**:改 `packages/app-pwa/src/components/showSocialAuthDrawer.tsx` +
  `hooks/useUserInit.ts` + `docs/ig状态一致性.md`。A 会话级抑制:模块级 `drawerDismissed`,
  用户「Maybe later」/点遮罩关闭即置真(open 时登记 `onClose:markDismissed`),
  `showSocialAuthDrawerFromStore` 开头非 `force` 且已关闭就 return;`dismissSocialAuthDrawer`
  (授权成功)close 后 `drawerDismissed=false`;`resetSocialAuthDrawerDismissed()` 在
  `useUserInit.cleanUser` 切/登出用户时重置。B 已打开去重:`showSocialAuthDrawer` 对已打开的
  `MODAL_ID` 直接 skip。设置页 `force:true` 不受抑制。
- **为什么**:真机反复弹。根因:`App.tsx:235` 的 effect 依赖 `[isReady,userState,userInfo?.userId,...]`
  → 每次变化重跑 `initUser`→`initInsTask`→`checkSocialProxyAbnormal`→`showSocialAuthDrawerFromStore()`;
  SC 一直 expired(authed&&abnormal,重连前不变),而抽屉**无「已打开去重」也无「用户已关闭」记忆**
  (新写的 `SocialAuthTaskDrawer` 有 `collapsedTaskId`,老抽屉没加)→ 关了又弹。
- **结果**:3 files +30。eslint 干净;tsc EXIT=0(上次 push 的 build 重建 proto gen 后,之前 4 个
  `cardType` 基线错已消失)。commit `2950e9e06`,pre-commit lint+circular 过、pre-push build 54/54,
  已 push `origin/personal/zz/sitin4.1`。
- **关键命令 / diff**:`git add` 仅 3 文件(submodule proto M 未动);`git push origin personal/zz/sitin4.1`。
- **沉淀**:见 troubleshooting `sitin-pwa-auth-drawer-repeat-popup.md`。

## 工作日志 — 17:xx sitin4.1 修 Social Connect 一次性任务卡「未登录+有订单不显示」

- **做了什么**:改 `hooks/useInsTaskInit.ts` `checkPendingOrders` + `docs/ig状态一致性.md`。
  汇总 `pendingByPlatform` 从 `cardTypeToPlatform(order.cardType)` 改为**严格判**:
  `ct===2→snapchat / ct===1→instagram / 其它(缺省/0/未知)`→ `continue` 跳过不猜。commit `0cc813786`。
- **为什么**:首页「Social Connect on IG/SC」一次性任务卡(`OnboardingTaskList` 的 `SocialConnectCard`,
  无 taskId,门槛 `amount>0 && !loggedIn`,amount=Σ`pwaFollowReward`)在「未登录对应社媒且有交换订单」时
  不显示。排查:显隐/渲染逻辑都对(独立于 isNewUser),问题在**金额记错平台**——`cardTypeToPlatform`
  对缺省 cardType **默认返回 instagram**,把无 cardType 的订单钱错算进 IG → SC 永远累计不到 → SC 卡不显示,
  且 IG 卡还显示了 SC 的钱。用户确认「金额 0 不展示任务」= 保留 `amount>0` 门槛,故只修归类不动门槛。
- **结果**:2 files +15/-2。eslint 干净、tsc EXIT=0(`CardType` 是 value enum 可作值导入)。
  commit 首行英文大写「Social」被 commitlint 拒(subject-case),改中文开头「社媒 Social Connect…」通过。
- **依赖后端(重要)**:SC 卡真正显示的前提是后端在 `ListUserInsExchangeOrderResponse.orders` 下发
  `card_type`(gen 已含字段 13,proto 源一度没有)。若列表接口没下发,SC 订单被我这版跳过 → SC 卡仍不显示,
  需后端补 `card_type`。真机 vConsole 看 `checkPendingOrders` log 的 `order.cardType` 可验证。
- **沉淀**:见 troubleshooting `sitin-pwa-social-connect-card.md`。

## 工作日志 — 16:xx PWA 分级机制前端技术方案(Minerva + PWA)

- **做了什么**:读需求真源飞书 wiki `AsNGwQa7`（「PWA 分级机制 & 对应权益」），产出一份前端技术方案，
  写入目标 wiki `Q1Dswuf8`（docx `HEPjdg9B4oPHWlxmRbQcfRHNndq`，append，revision 12）。方案区分两项目 × 前后端：
  - **Minerva（详细前后端）**：`app-minerva-web` + `minerva-server`(BFF) + `business-minerva-users`(领域服务/分级引擎)。
    Prisma 模型 `PwaPoolMembership/PwaGradeBatch/PwaGradeRule/PwaDirectedTrafficQuota`；定级跑批 Cron（周日24点美东、
    分布基线 P80、新人/活跃筛选、退池幂等）；11 个 BFF API；前端 7 路由 + `PoolMembersTable`(三池复用+动态指标列)/明细/导入/规则/跑批/流量看板。
  - **PWA（前端详细+后端仅列接口）**：`app-pwa` 4 页面（邀请卡/邀请页/常驻权益面板/每日考核）+ 组件状态 + 埋点坑；
    后端参考 5 接口 `/invitation`·accept·decline·`/benefit`·`/daily-assessment` 含字段。
- **为什么**:用户要一份可落地的前后端技术方案，Minerva 详、PWA 后端仅供参考。
- **结果/踩坑**:目标文档步川账号**无编辑权限** → 按工作区约定切**张峥**账号（device flow：`auth login --no-wait --json`
  拿 verification_url → `auth qrcode --output` 生成 PNG 发用户扫码 → `auth login --device-code` 完成）→ 张峥有权限，写入成功。
  注意 `auth qrcode --output` 只接受**当前目录相对路径**，需先 cd 到 OUTPUTS_DIR 再用 `./name.png`。
- **本地存档**:方案 md 在本会话 scratchpad（临时）。

## 工作日志 — 17:xx sitin-next PWA「Go Live → Go Online」文案

- **做了什么**:在 `sitin-next` 从 `feat/pwa-beauty` 新建分支 `personal/zz/live`,把 PWA 开播相关用户可见文案
  「Go Live」统一改成「Go Online」,共 6 处 / 3 文件:
  - `app-pwa/src/pages/Live/PreLiveView.tsx`:主标题 `Go Online`、提示气泡 `💡 Go ONLINE to receive video calls`
  - `app-pwa/src/components/GoLiveModal.tsx`:`Enable "go online" to auto-answer calls...`、按钮 `Enable "Go Online"`
  - `app-pwa/src/components/UndispatchModal.tsx`:`...exited Go Online mode.`、按钮 `Go Online`
  - 注释、组件名 `GoLiveModal`、埋点事件名、`console.log` 调试日志一律不动。
- **为什么**:产品把开播动作文案从 Go Live 统一为 Go Online。
- **结果**:tsc + eslint 过;commit `1b6200798`(`feat(app-pwa): 「Go Live」文案统一改为「Go Online」`),
  pre-commit 全量 lint(52 包)+ depcruise 循环检查过,pre-push build(55 任务)过;已 push origin,身份 colna。
- **踩坑(分支操作)**:persistent session 里我先建了 `personal/zz/live` 又被后续动作切回 `personal/zz/sitin4.1`,
  改动落错分支。用 `git stash push -- <指定文件>` 只隔离自己的 3 个文件(消息名 `goonline`),
  再把 sitin4.1 上**别人既有的**未提交 `socialAuthTask.ts` 也单独 stash(`sitin41-socialauth`),
  切到 live 后 `git stash apply stash@{N}` 落地。**教训**:多分支共享一个工作区,切分支前先确认
  `git branch --show-current`;挪改动用「按文件 stash」而非全量 stash,避免带走无关 WIP。
  ⚠️ sitin4.1 的 `socialAuthTask.ts` 现仍在 `stash@{0}: sitin41-socialauth`,切回 sitin4.1 需 `git stash pop` 恢复。

## 工作日志 — 18:xx Snapchat getChatMessages 新增 type:"call" 通话类型

- **做了什么**:在 `sitin-next2`(切到 `personal/zz/sp-snapchat` 分支,同 repo,干净工作树)给
  Snapchat 注入脚本 `getChatMessages` 加入通话消息类型 `call`,不区分音/视频。共 3 文件 +59 行:
  - `packages/app-ins-scripts/src/snapchat/actions/utils.js`:LABELS 新增 `callEvent` 关键词表
    (子串匹配,覆盖 简中`通话`/繁中`通話`/英`call`·`tried to call`/西`llamada`·`llamar`·`llamó`)。
  - `.../snapchat/methods/getChatMessages.js`:加 `isCallBubble()`(仅在「系统样式气泡」——无
    `span[dir="auto"]`/audio/img——上按关键词判定,避免正常文本含 call/通话 被误判);在
    `extractSingleBubble` 状态检测后插入「1.5 通话事件」分支,返回 `type:"call"`,发送方由气泡
    `border-color` 判定(红 `rgb(242,60,87)`=我,其余=对方),更新文件头类型清单 + 选择器策略注释。
  - `test/sc-label-match.unit.test.js`:加 2 组 callEvent 测试(zh简/繁·en·es 命中 + 普通消息/空串无假阳性)。
- **为什么**:样本聊天里「c bo 曾試著和你通话」这类通话事件此前掉进兜底逻辑被当成 `type:"text"`,
  需要独立 call 类型。参考 INS 版 `detectCallEvent`(L205+),但 INS 区分 audio_call/video_call,
  Snapchat 聊天里通话只以「曾试着通话」系统气泡出现、不带音视频区分,故简化为单一 `call`。
- **结果**:`node --test test/*.test.js` 全绿(7/7,新增 5→7)。未 commit/未 push(规范模式,等用户批准)。
- **假设/待确认**:①所有通话事件都上报(未做 INS 的「只报未接」过滤,因 Snapchat 聊天只出现未接尝试);
  ②西语文案为推测,待西语环境 DOM 校正(zh 由样本繁体确证、en 为标准文案)。
- **关联**:此前几轮在分析 SC-Scraper 运行日志(getStory/getProfile 顺序、clickMute 因服务端下发空
  `manSocialNickname` 4 连失败、回关后未 sendGreet 等),均为服务端编排/时序问题,已口头给出修复建议。

## 工作日志 — 17:xx sitin-next PWA 实现 Figma「earn-module-v2」卡片

- **做了什么**:按 Figma「直播需求」node `1-953`(earn-module-v2)在 `app-pwa` 实现等待期发礼引导卡:
  - 新建 `src/pages/Live/EarnModule.tsx`(纯展示组件,props `onGoLive/avgGift/topHosts`)。
  - 下载素材到 `src/assets/images/live/`:`earn_giftbox.png`(礼盒插画,imageRef)、`ic_earn_check.svg`(teal check)。
  - `src/pages/Live/PreLiveView.tsx`:import 并在底部 `bottom-[140px]` 挂载卡,红色 CTA 复用 `handleGoLive`;
    现有蓝色 Go Online 按钮暂保留(非破坏),是否替换待用户确认。
- **规格要点**:卡片深棕 radial+linear 渐变 + `#1C1C1F` 描边 + 大阴影 radius24;标题「Earn More」金渐变
  (`#FFE599→#F7A629` background-clip text)+「While You Wait」白;chip 底 `rgba(255,184,71,0.1)`/边 `rgba(255,199,102,0.28)`;
  金额值 `$0.99/$120` 用已注册字体 `RacingSansOne`;CTA `#FE2C55`+暗金 `$` 徽标;礼盒图右上顶部溢出 14px(overflow-visible)。
- **验证**:tsc + eslint 均过;用 headless Chrome(`--force-device-scale-factor=3`)渲染独立预览 HTML 截图与 Figma 逐项比对,
  还原度高(预览存 scratchpad + OUTPUTS_DIR `earn_module_preview.png`)。
- **踩坑/经验**:app-pwa 无 playwright/puppeteer,但本机有 Chrome.app → 可用 `Google Chrome --headless --screenshot`
  快速把独立 HTML 渲成 PNG 做设计还原自检(字体用 file:// @font-face、图片用 file:// 绝对路径)。省去起 dev server + 相机权限。
- **未提交**:规范模式,未 commit;新增 `EarnModule.tsx` + 2 素材 + 改 `PreLiveView.tsx`。

## 工作日志 — 17:xx(修正)earn-module-v2 卡片归属状态

- **修正**:上一条把卡片放到了 PreLive(开播前),**放错了**。用户澄清:该卡是**开播后 LIVE(Action)状态**才展示的
  (与「LIVE LOUNGE」等待卡同屏,在其下方)。已从 `PreLiveView.tsx` 移除、还原,改挂到 `LiveAction.tsx`
  的底部 `flex flex-col gap-3` 容器内、LIVE LOUNGE `<section>` 之后。
- **组件调整**:`EarnModule` prop `onGoLive` → 改名 `onCtaClick`(可选);LiveAction 内暂不传(红色
  「Go Live & Earn」CTA 目标=公开直播/发礼流程,产品/后端未定,留 follow-up)。
- **验证**:tsc + eslint 过;PreLiveView 净改动为 0(add 后 revert)。
- **经验**:PWA 直播页状态机在 `pages/Live/index.tsx` —— `liveState !== Action ? <PreLiveView/> : <LiveAction/>`;
  LIVE LOUNGE 毛玻璃卡本就在 `LiveAction.tsx`,新卡跟它同容器即对齐设计。动手前该先确认卡片属于哪个状态。
