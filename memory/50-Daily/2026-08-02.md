---
title: 2026-08-02
date: 2026-08-02
tags: daily
---

# 2026-08-02

> 时间口径为**北京时间**(UTC+8);机器时钟本会话早期一度显示 07-31(偏差),现已对齐 08-02。历史口径见 [CONVENTIONS 时间口径](../CONVENTIONS.md)。

## 今日重点

- [ ] 探针自拍超时:后端补 `taskType==PROBE` 分支(清 probe、不暂停绿灯收益)+ `handleOutboundMessage` 识别 `replyKind:probe` 走 `handleProbeComplete("completed")`——需给对应 owner 出改动说明(结转自 07-30 / 07-31)

## 工作日志 — 21:22 colna 检索优化 + 修全量重建致命 bug + 换 e5-base

- **做了什么**:按「检索优化方案」改 colna(`colna-memo/src`),四处:
  1. **修全量重建致命 bug(根因)**:`store.rs` 的 `insert_chunks`/`upsert_chunks` 一次性写入全部块,但 **zvec 单次写入上限 1024 条**,1367 块 → `Too many docs: 1367 exceeds max write batch size of 1024` → 索引写不成、空索引、检索 0 结果。**之前 N 次全量重建/`colna sync` 首次全量失败全是这个**(旧 2.4G 索引是增量一点点攒的才没触发)。修法:分批写入(每批 512)。
  2. **上下文增强切块**(精准度):`chunker.rs` 给 `Chunk` 加 `embed_text`,维护标题层级栈算「面包屑」,**嵌入文本 = 文档标题 · 日期 · 面包屑 + 正文**;展示/FTS/存储仍用原 `text`。块 ID 不变(`source_path+heading+序号`)。`main.rs` 两处嵌入改用 `embed_text`。
  3. **换模型 e5-small → e5-base**(精准度):`embedder.rs` `MultilingualE5Small→Base`、`DIM 384→768`。
  4. **嵌入分批 32**(效率/内存):`embedder.rs` `embed(..., Some(32))`,共享机器 swap 吃紧时避免大批次抖动拖慢。
- **效率项**:编译 release 版(`COLNA_PROFILE=release`,`./colna` wrapper 已支持),嵌入/检索快数倍。
- **结果**:全量重建成功「写入 1367 块」,**索引 2.4G → 13M**,检索恢复且结果带面包屑上下文;`colna sync` 收尾成功。重建用时 ~7.5 分钟(嵌入多核 844% CPU)。
- **踩坑(关键沉淀)**:① zvec 写入硬上限 1024,任何一次 insert/upsert 都要分批;② 全量重建卡死/爆内存的表象(另一 agent 报 29GB swap)其实是被 1024 报错前的嵌入 + 机器本身 swap 吃紧叠加误导,真凶是写入超限;③ 本环境后台任务(nohup/setsid 都试过)会在会话空档被连进程组杀,长任务要**前台阻塞**跑。
- **待办**:`src/{chunker,embedder,main,store}.rs` 4 个改动**未 commit**(规范模式等用户批准);尤其分批写入是真 bugfix,建议提交。

## 工作日志 — 21:35 整理 memo + 同步

- **做了什么**:执行「整理 + 同步」。因机器时钟本会话跳变(07-31→08-02)+ 并行 Codex agent 正在写 `07-31.md`,**不对 07-31.md 做破坏性搬移**(避免冲突),只做加法:① 结转未完成待办「探针自拍超时」到本日今日重点;② 核对可复用经验是否已沉淀。
- **关键发现**:Codex agent 已在 `troubleshooting/colna-sync.md` 沉淀「全量索引内存膨胀与假完成」——**同一个 colna 全量重建 bug 双方并行都定位到了**(zvec 1024 单批写入上限 + fastembed 大批次内存)。Codex 的修复(16 块串行 + state v2 完整性校验 + 删除幂等)在**其独立 clone**,未推 origin/main;origin/main 的 src 仍是旧版,我那 4 个改动是目前唯一作用于共享远端代码的改进(仍未提交)。→ troubleshooting 无需重复添加。
- **结果**:memo 侧仅追加待办 + 本条日志,`colna sync` 同步;src 代码改动仍挂起等批准。

## 工作日志 — sitin-next feature/pwa-ads:Partner 任务加埋点(点击 + 完成)

- **做了什么**:给 PWA「Become a partner」(Partnership Ads,task_id=2000)任务补两个 BytePlus 埋点。改 2 文件(`packages/app-pwa/`):
  1. `src/tracking/events.ts`:新增两个事件名 `pwa_earning_partner_task_click`、`pwa_earning_partner_task_complete`(命名对齐既有 INS 任务 `pwa_earning_ins_task_*`)。
  2. `src/hooks/usePartnershipAds.ts`:① `onGo` / `onReceivedClick` 各 `bpTrack(...task_click)`,带 `action: "go" | "received"` 区分两种点击;② `runCheck` 里 `ok===true`(唯一权威完成点)`bpTrack(...task_complete)`,带 `task_id` + `reward`。reward 用 `rewardRef`(仿现有 `awaitingRef`)读,避免把 `task` 塞进 `runCheck` deps 触发 visibility 订阅抖动/重复查询。
- **为什么这样放**:点击在 hook 的 onGo/onReceivedClick 而非卡片,保持 `PartnershipAdsCard` 为纯视图;完成放 `runCheck` 的 ok 分支,useLockFn 在途锁 + 完成后卡片自隐(`visible=false`)保证只报一次。注:`finishTask` 内部对所有 common task 已报通用 `pwa_task_complete`(带 task_id),这里另加 partner 专属事件供漏斗对照。
- **工作环境坑**:feature/pwa-ads 被另一会话的 git worktree(`scratchpad/wt-ads`)独占,主仓库无法再 checkout;直接在该 worktree 干活并 ff 到 origin(852a9de72)。worktree 无 node_modules,临时软链主仓库 `node_modules` 做校验、校验后删除软链。
- **校验**:eslint 0、`tsc -b`(用项目 pin 的 5.6.3,非 root 的 6.0.2——后者会误报 baseUrl 弃用)0。**规范模式未提交**。
- **事件名口径**:漏斗取数认内容/事件名,记牢 `pwa_earning_partner_task_click`(params.action=go/received)/ `pwa_earning_partner_task_complete`(params.task_id=2000, reward)。
- **提交 + PR**:经用户授权 `git push`,commit `5455b1483`(colna 身份)推到 `feature/pwa-ads`。该分支**已有 open PR #760**「feat(app-pwa): 新增 Partnership Ads 任务显示与点击跳转 IG」,base 是 **`feature/pwa`(不是 main)**,url https://github.com/presence-io/sitin-next/pull/760 —— 埋点 commit 已随 head 分支进入这条 PR,无需另开 PR。
