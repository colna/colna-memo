---
title: 2026-09-09
date: 2026-09-09
tags: daily
---

# 2026-09-09

## 今日重点

- [ ] koda buglist R00218/R00219/R00220（订阅 / 访客订阅 / like+访客数据）——已定位，等后端确认 koda 租户商品与数据
- [ ] koda 端到端 EAS 真机验：审核双号登录（`review@` / `metareview@kodasoc.com`，同密码）、恋爱铃三子系统（需后端推 S2C）、图片发送
- [ ] `apps/koda/tests/config.test.ts` pre-existing 失败（`appName` 实为 `koda_android`，断言写的 `koda`）+ BytePlus appId/appName 仍是 lumi 串包值
- [ ] Poo 注册流程待确认三项：身高字段去留 / Android 登录按钮几颗 / 开屏视频与开屏图先后

## 工作日志

## 工作日志 — 昨日整理

- **做了什么**：整理 2026-09-08 Daily。沉淀 2 条到 troubleshooting，搬 3 条未完成待办到今日重点，无需归档的垃圾内容。
- **结果**：
  - 新建 `30-Resources/troubleshooting/deliverable-consumer-first.md` —— 产出物有明确消费方时，先问「给谁用 / 什么工具吃」，排在拆分粒度和字数之前（Poo prompt 返工三次的根因）
  - `verification-discipline.md` 追加第 6 条 —— 只会说成功的同步脚本比没有同步脚本更危险（零命中被当成「无事可做」，必须 exit 1 + 反向验证）

## 工作日志 — 重置 feature/poo-ios，从 blueprint 重切

- **做了什么**：`sitin-rn` 删远端 + 本地 `feature/poo-ios`，从 `origin/feature/blueprint`（已更新到 `ecd39ded`）重建同名分支并 push。
- **为什么**：用户要在最新 blueprint 基线上重做 Poo，旧分支上只有 5 个 poo docs commit。
- **结果**：旧提交打了本地备份 tag `backup/poo-ios-20260909`（→ `03fc8838`，未推远端）。`apps/poo` 整个目录随之从工作树移除，PRD `apps/poo/docs/00-需求.md` 现在只在该 tag 里。
- **关键命令**：

  ```bash
  git tag backup/poo-ios-20260909 03fc8838
  git push origin --delete feature/poo-ios
  git checkout -B feature/poo-ios origin/feature/blueprint && git push -u origin feature/poo-ios
  ```

- **坑**：删远端分支时 pre-push hook 对 `sha..0000000` 算 changed files 会 `fatal: Invalid revision range`，然后 fallback 跑全量 lint/boundaries/typecheck——不是错误，别当失败处理。

## 工作日志 — 飞书 PRD 补「注册流程」小节

- **做了什么**：在「Poo 技术向PRD」（wiki `N848wkTFRij6ruko6tQck0gVnhe`）的「二、产品结构」下补 h2「（二）注册流程」，并给原有正文补 h2「（一）五个 Tab 与小崽的五处露出」。
- **为什么**：PRD 原先只有一句「注册引导流（性别／生日／身高／位置／头像分步）」，缺全链路口径。
- **结果**：写入九屏全链路（开屏 / 欢迎登录 / 登录中 / 五步问卷 / Ready）、五步字段表、路由守卫状态机、与审核 2.1+4.3 的关系、三项待确认。revision 44 → 46。
- **发现的口径冲突**（已在文中标出，待产品定）：PRD 旧写「性别／生日／身高／位置／头像」，但 `apps/blueprint/src/stores/onboarding.ts` 实现是 `name / age / gender / photoUri / location`——**多「名字」、少「身高」，年龄是直接选不是生日推算**；43 屏 prompt 也按实现口径出的图。
- **落位决策**：没有新增章号。PRD 内有 13 处「第 X 章」交叉引用，顺延编号会全部失效，改为在既有 h1 下补一层 h2。
- **关键命令**：

  ```bash
  lark-cli docs +update --doc <wiki-url> --command block_insert_after \
    --block-id doxcnUXEIDIFGk3YZxEplIgsHoe --as user --content "$(cat signup.xml)"
  ```

- **沉淀**：改飞书长文档补章节前，先 `docs +fetch --doc-format markdown` grep 一遍 `第[一二三...]章`——交叉引用数量直接决定「新增 h1 顺延」还是「挂 h2 不动编号」。
## 工作日志 — 远程排查另一台 Mac 的 brew install ccache 失败

- **做了什么**:远程指导排查另一台 Mac(用户 `colna`)`brew install ccache` 全量失败,最终装上 ccache 4.14。无代码改动,纯排错。
- **现象**:7 个包进度条走到 100% 后整齐报 `rb_sysopen - No such file or directory`,指向 `~/Library/Caches/Homebrew/downloads/` 下的 bottle。
- **根因**:`HOMEBREW_BOTTLE_DOMAIN` = 阿里云镜像,镜像缺 bottle → 回退官方域名下载,但缓存路径仍按镜像 URL basename 推导(单横杠 `name-version`),而实际保存是双横杠 `name--version` → 下载存 A、解压开 B。Homebrew 6.0.10 有此 bug(本机 6.0.22 无)。
- **定位关键**:比对 `downloads/` 文件名横杠数 + 时间戳(只有 manifest json 是新的,tar.gz 全是旧日期 → tar.gz 没落盘)。
- **结果**:`env -u HOMEBREW_BOTTLE_DOMAIN -u HOMEBREW_API_DOMAIN brew install ccache` 通过;中途遇 `Cannot link ca-certificates`,`brew unlink` 后继续。`ccache 4.14` 已就位。
- **踩的弯路**:第一轮误把日志里的 `/Users/colna/...` 当成本机环境变量污染(本机是 `max`,无 `colna` 用户),按"HOME 被 export 坏了"给了方案。实际是**另一台电脑**的日志。教训:用户贴日志时先确认是哪台机器,别拿本机 `whoami` / `dscl` 的结果去反推。
- **遗留**:那台机器 `brew` 仍是 6.0.10,建议 `brew update` 升到 6.0.22+;`df -h /` 显示容器剩余仅 3.8Gi/460Gi,需清理。
- **沉淀**:新建 `30-Resources/troubleshooting/homebrew-mirror-cache-mismatch.md`(镜像回退缓存名不匹配 / link 冲突 / `df -h /` 看的是只读系统卷)。

## 工作日志 — 01:19 搭 Figma 写入桥(figma-write-bridge)

- **需求**:用户问能否往 Figma 文件 `QBdMdjSPAFJQWndXHrnDFn`(ios 新包规范,画布 `291:902` "poo")写入。
- **结论(关键)**:**Figma REST API 没有创建/修改图层的接口**,跟 token 权限、文件编辑权限都无关。官方 write scope 只有 `file_comments:write` / `file_dev_resources:write` / `file_variables:write`(Enterprise) / `webhooks:write`。要写画布**只能靠 Figma Plugin**(插件在客户端里以用户身份跑,此时文件编辑权限才起作用)。
- **做了什么**:新建 `/Users/max/Dev2/zhangzheng/figma-write-bridge/`(零依赖,Node 24):
  - `bridge.mjs` —— 本地 HTTP 中转(127.0.0.1:3055)。`POST /exec` 由 CLI 投递命令并阻塞等结果;`GET /poll` 给插件长轮询(25s)取命令;`POST /result` 回传。
  - `plugin/manifest.json` + `ui.html` + `code.js` —— 插件 UI iframe 负责 fetch 长轮询(主线程沙箱没有网络),postMessage 转给主线程执行 figma API。
  - `figma.mjs` —— CLI:`node figma.mjs <cmd> '<argsJson>|@file.json'`。
  - 命令集:`ping` / `get_selection` / `get_page` / `create`(声明式 JSON spec 树,支持嵌套 + Auto Layout + 自动 `loadFontAsync`)/ `update` / `clone` / `move` / `delete`。
- **结果**:四个文件 `node --check` 全过,bridge 起来 `/health` 返回 `{"ok":true,"pluginConnected":false}`。**用户说「关上吧」,已 `pkill` 停掉 bridge,未在 Figma 里装插件、未做任何画布写入。代码保留待用。**
- **坑**:
  1. 插件主线程(QuickJS 沙箱)**没有 fetch/WebSocket**,网络必须走 `ui.html` iframe;manifest 要写 `networkAccess.allowedDomains` 含 `http://127.0.0.1:3055`。
  2. 导入本地插件**必须 Figma 桌面版**(Plugins → Development → Import plugin from manifest),网页版不行。
  3. 后台起服务用 Bash `&` 会被回收,要用 `run_in_background`。
  4. heredoc 用 `<<'EOF'`(引号)时 `$(date)` 不展开,别在里面塞命令替换。

## 工作日志 — 包名 Poo → Luka

- **做了什么**：包名（含宠物形象名）从 Poo 改为 Luka，同步三处产物。
- **结果**：
  - 飞书「技术向PRD」`N848wkTFRij6ruko6tQck0gVnhe`：19 处 Poo + 1 处 poo 已换，标题变「Luka  PRD」（rev 46 → 48）
  - 飞书「技术文档」`L4ERwvtqBiJXnRkNfWvch1PWnRd`：20 处 Poo + 5 处 poo 已换，标题变「Luka 技术文档」（rev 13 → 15）
  - 分支 `feature/poo-ios` → `feature/luka-ios`（本地 `git branch -m` + 删远端旧名 + push 新名）
- **未处理（已告知用户）**：PRD 首屏设计图的 `<img alt="...">` 里还有 5 处 Poo。那是飞书 AI 自动生成的图片描述、不在正文渲染，改它要 `block_replace` 整个 img block 并重建 `src` token，有丢图风险，判断不值得。
- **保留原名**：备份 tag `backup/poo-ios-20260909` 不改——它备份的就是 poo 时期的分支，改名反而失真。
- **关键命令**：

  ```bash
  # 大小写分两次，先 Poo 后 poo（替换后不会产生新的 poo，无顺序陷阱）
  lark-cli docs +update --doc <url> --command str_replace --pattern "Poo" --content "Luka" --as user
  lark-cli docs +update --doc <url> --command str_replace --pattern "poo" --content "luka" --as user
  ```

- **沉淀**：飞书 `docs +update --command str_replace` **只改文本内容，不碰标签属性**——img 的 `alt`、`name` 这类属性里的旧词会静默残留。批量改名后必须 `--doc-format markdown` 拉回来 grep 一遍旧词计数，不能只看命令返回的 `ok: true`。另：批量替换前先跑一次子串检查（`[A-Za-z]poo|poo[A-Za-z]`），确认要替换的词没有嵌在别的单词里。

## 工作日志 — 在 feature/luka-ios 上 new-app 派生 Luka 包

- **做了什么**：`pnpm new-app --name "Luka" --package com.lukasoc.luka`，从 `apps/blueprint` 派生出 `apps/luka`。
- **包名怎么定的**：仓库惯例是 `com.<slug>soc.<slug>`（irissoc / lumisoc / nayasoc / kodasoc，旧 poo 包也是 `com.poosoc.poo`），所以 Luka → `com.lukasoc.luka`。跑之前先 `--dry-run` 看过 token map。
- **结果**：复制并重写 119 个文件、改名 1 个，自动登记进 `apps.json` / 根 `package.json` / `.github/workflows/build.yml`。验收 `pnpm install && boundaries && --filter luka typecheck && --filter luka test` 全过（560 tests passed）。未 commit（规范模式）。
- **踩坑：new-app 的文本重写有两处漏网，派生完必查。**
  1. **token map 不含全大写。** `scripts/new-app.mjs` 的 `buildTokenMap` 只有 `Blueprint` / `blueprint` 两个键，`BLUEPRINT` 原样留下 → `tests/lib/review-mode.test.ts:21` 的 `METAREVIEW@BLUEPRINT.EXAMPLE.COM` 断言直接挂，是派生后唯一的红灯。
  2. **`TEXT_FILE_RE` 白名单漏了扩展名。** 正则是 `\.(ts|tsx|js|jsx|mjs|cjs|json|md|css|yml|yaml|plist|gradle|kt|swift|sh)$`，**不含 `.env.example`（无扩展名段）和 `.svg`**，这两类文件整份都没被重写 → `.env.example` 里的 `pnpm blueprint:start`、`BLUEPRINT_LOCAL_TIM_PUSH_ID`，`src/assets/icon.svg` 注释里的「Blueprint 的应用图标」全部残留。
- **修法**：派生后跑一次 `grep -rn "blueprint\|Blueprint\|BLUEPRINT" apps/<新包>/ | grep -v node_modules | grep -v template-rename.json` 兜底。**`.template-rename.json` 里的 blueprint 是刻意保留的**（派生映射真源，`template-migrate` 靠它把模板 diff 套到派生包上），千万别一起替换掉。
- **lint**：全仓 26 warnings，luka 占 8 条且与 blueprint 一一成对，是从模板继承的既有 `noUnusedVariables` / `noUnusedFunctionParameters`，不是派生引入。
- **脚本列出的待填项**（它无从知道的值）：后端域名与 appName（⚠️ 非 production 档现在连的是 Intro 测试后端，不换会和 Intro 数据混）、Sentry DSN、AppsFlyer、BytePlus、腾讯 IM 推送证书、App Store / Play 记录、App Group 注册、法务页面与客服邮箱、组件帧图。

## 工作日志 — 出 Luka 注册流程设计图 prompt（给 image2）

- **做了什么**：按 Luka 角色卡（用户新给的吉祥物角色卡图）＋ PRD 新补的注册流程九屏口径，写一份给 image2（GPT Image 2，走 super-image2 平台）的出图 prompt。
- **先确认消费方**（按 `deliverable-consumer-first.md` 的动作）：image2 是**出图模型**，不是 Figma Make → prompt 只写视觉（OUTPUT / 角色 / 色板 / 形状字体材质 / 逐屏 Layout / 禁止项），**不写 Interaction 和 Data 两段**——那两段是可交互原型工具才需要的。这次没返工。
- **结果**：`luka-signup-flow-prompt.md`，15687 字符（上限 32000），3×3 九宫格一张设计图，九屏＝Splash / Welcome / Signing in / Name / Age / Identity / Photo / Location / Ready。已发用户。
- **角色卡与既有 UI token 的对应**（角色卡五色 → PRD BASELINE，完全对得上，无需重定）：可可棕＝cocoa `#6B4A32`、奶油白＝surface `#FFFDF8` / page `#FAF4EA`、焦糖橙＝caramel `#D89A5C`、蜜桃粉＝peach `#F2B5BC`、浅咖色＝tertiary `#B39A80`。
- **写 prompt 时踩到的点**：
  - 角色卡是**参考图**，必须显式写「只取角色长相，不要复制它的排版 / 卡片 / 爱心 / 中文标签」，否则模型会连版式一起抄。
  - 尺寸全部写成**相对屏宽 `W` 的比例**而不是 pt——九宫格里每屏实际像素未知，写死 pt 模型会按整图尺度理解。
  - 九屏塞一张图，正文字号约 `0.035W`，在 image2 最大分辨率下**只能是装饰性文字、不保证可读**。设计图用途是「呈现布局与风格」（对齐 PRD 首屏那张 22 界面拼图），可接受；要production 级清晰得拆成 9 次单屏跑。

## 工作日志 — 注册流程 prompt 改成 Prompt Pack 格式（v2）

- **做了什么**：用户给了旧的 `Poo-UI-Prompts-EN(2).md`（37045 字符，30 屏）说「按这个风格」，把上一版九宫格 prompt 重写成同构的 Prompt Pack。产出 `Luka-Signup-Prompts-EN.md`，24008 字符（上限 32000）。旧的九宫格版已删。
- **这份参考文件的风格骨架**（以后再出 prompt 直接照抄）：
  1. 头部：版本行 + Device 规格行 + How to use（**每次渲染 = Master Prompt 逐字 + 一个 Screen Prompt**）+ What changed
  2. **Master Prompt** 单个 code block（STYLE BIBLE，~5400 字符）：DEVICE / MASCOT / EXPRESSION SET / PALETTE / VISUAL LANGUAGE / 共用 chrome / HARD RULES
  3. **Negative Prompt** 单个 code block + 逐条解释为什么有这条
  4. **Screen Prompts**：分组，每屏一个 code block，只写这屏独有的，700–1900 字符
  5. Production notes / State variants / Screen index 表
- **关键点**：单位用 pt（因为 Master 里钉死 1320×2868 @3x），code block 内纯英文、外面的说明也英文；每屏末尾写 `Do not draw ...` 排除项。
- **顺带解决了上一版的问题**：九屏拼一张图正文必糊 → 这个格式本来就是**单屏渲染 9 次**，字号可读性问题消失。
- **内容按 PRD 新口径写、没照抄旧 pack 的 A 组**：旧 A 组是 birthday / height / occupation + pet hatch + widget tutorial 那版旧字段，已作废；新的是 name / age / gender / photo / location 九屏。差异写进了文件的 What changed 段。
- **新增的 Negative Prompt 条目**（针对本 pack 特有的翻车方式）：`tab bar`（其余 29 屏都有，模型会按上下文自动加）、`mascot cropped by the screen edge`（旧 pack 的左下角裁切姿势在注册流里作废）、`photograph of a person`（本流程不出现真人）。

## 工作日志 — 01:54 sitin-rn 提交并推送 Luka app 首个 commit

- **做了什么**：把 `sitin-rn` `feature/luka-ios` 上未提交的 Luka app 全量提交并 push。commit `c2f1347b`「feat(luka): 基于 blueprint 模板新建 Luka app」，1001 个文件（`apps/luka/` 997 新增 + `apps.json` / `package.json` / `pnpm-lock.yaml` / `.github/workflows/build.yml`）。
- **为什么**：用户跑 `pnpm luka:ios` 报 `ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL`，怀疑分支没配 luka；排查发现脚本齐全，只是改动一直躺在工作区没提交。
- **根因不是配置缺失，是跑错目录**：本机有 `sitin-rn` 和 `sitin-rn2` 两份同 remote（`presence-io/sitin-rn`）的 clone——
  - `sitin-rn` = `feature/luka-ios`，`apps/` 有 blueprint / luka / poo，根 package.json 有 `luka:*`
  - `sitin-rn2` = `feature/koda-android`，`apps/` 只有 demo / intro / iris / koda / lumi / naya
  - pnpm 提示「Did you mean `lumi:ios`」就是在 rn2 里找不到 luka 时的近似匹配。**以后 pnpm 报 script not found 且提示了一个相似 app 名，先 `git branch --show-current` + `ls apps/` 确认在哪份 clone。**
- **验证**：`pnpm luka:typecheck` 通过；push 时 pre-push hook 跑了全 workspace typecheck（blueprint / intro / koda / lumi / luka / naya 全 Done）。
- **两点噪音，已确认可接受**：
  - `apps.json` 里其它 app 的 `eas` 字段被展开成多行——`new-app` 脚本 `JSON.stringify` 重写导致的格式差异，值没变。
  - `apps/luka/src/assets/pet/` 带进 ~7MB 视频（`source-color.mp4` / `source-matte.mp4` / `panda.mov` 等），与 `apps/blueprint` 同名同源，是模板自带资源，惯例一致没排除。

## 工作日志 — 修 luka iOS 模拟器构建卡在 code signing

- **做了什么**:排查另一台 Mac(`colna`)上 `pnpm luka:ios` 失败,定位到 expo 的 entitlements 签名判定,改 `apps/luka/app.config.ts` 临时关掉 `usesAppleSignIn`。
- **为什么**:`@expo/cli` `run/ios/XcodeBuild.js:279` 在 entitlements 命中 `com.apple.developer.applesignin` 或 `associated-domains` 时,**即使目标是模拟器也强制要求开发证书**;luka 的 `ios.usesAppleSignIn: true` 命中了,而那台机器没有 Apple Development 证书。
- **结果**:`apps/luka/app.config.ts` 注释掉 `usesAppleSignIn: true` 并写明恢复条件(填真实 Team ID + Xcode 登录 Apple ID)。`pnpm --filter luka typecheck` 通过。改动只在本机仓库,未提交未推送。
- **踩的弯路**:先猜 Xcode 环境(xcode-select 指向、模拟器 runtime 缺失、磁盘不足)三条全错 —— 那台机器 Xcode 26.6、11 个模拟器、磁盘 63Gi。**报错文案说 "physical iOS devices" 是 fallback 话术,会把人往环境方向带偏**,真因在 app.config。
- **发现的模板问题**:`apps/blueprint` 模板写死 `appleTeamId: "XXXXXXXXXX"` + `usesAppleSignIn: true`,从它派生的新 app(luka)全盘继承,第一次跑 iOS 必踩。对照 `lumi`/`koda` 是真实 `39CFYH6W55`,`intro`/`iris`/`naya` 走 `DEVELOPMENT_TEAM` 常量。
- **遗留**:luka 的真实 Team ID 待定(是否复用 `39CFYH6W55` 未确认);那台机器 Xcode 未登录 Apple ID。
- **沉淀**:新建 `30-Resources/troubleshooting/expo-run-ios-code-signing.md`。

## 工作日志 — 02:53 Luka 登录 + 注册流程按 Figma 落地(分支 personal/zz/luka-login)

- **做了什么**:`sitin-rn` 从 `feature/luka-ios` 切分支实现登录流程。40 个文件改动,未提交。
- **分支名踩坑**:先建的 `feature/luka-login` 违反仓库 `AGENTS.md` 规则 3(⛔ 不再有 `feature/<App名>` 常驻集成分支,只许 `personal/<人>/<事>` 或 `feat|fix/<事>`),已改名 **`personal/zz/luka-login`**。以后在 sitin-rn 建分支先看 AGENTS.md。
- **核心判断:视觉不是"改几屏",是整套 design token 换血**。`apps/luka` 从 blueprint 派生,theme 还是「白底 + 蓝前景 `#1D4ED8` + 圆角只有 3px + Inter」;Figma 是「奶油 `#FDF9EE` + 可可棕 `#3A2317`/`#684431` + 焦糖 `#D89A5C` + Nunito + 29px 胶囊」。
  - 做法:`design/theme.js` **同位替换**(键名一个没动)→ 298 处 `Colors.*` + 1197 个 className 自动跟着变,再 `pnpm luka:tokens` 重生成 `global.css` + `colors.ts`。
  - 新增 6 个 blueprint 没有槽位的暖色:`caramel` / `honey` / `honey-ink` / `cream` / `peach` / `warm-tertiary`。
  - 字体 Inter → Nunito(400/600/700/800),`@expo-google-fonts/nunito` 装上、`inter` 移除。
- **形状语言用白名单做渐进迁移(关键决策)**:blueprint 的 `tests/design/shape-language.test.ts` 强制「圆角只有 3px」。全库换 16px 会动到几百处未迁移的屏。改成**按目录守两套规则** —— `MIGRATED` 列表(`app/login.tsx`、`app/pet-hatch.tsx`、`app/onboarding/**`、`components/onboarding/**`、`components/login/**`)守「16px + 胶囊」,其余仍守 3px。**白名单就是迁移进度条**,一屏改完挪一个进去,不放宽全局规则。
- **屏幕**:登录页重做(挥手仓鼠 + Come say hi. + 三颗胶囊)、pet-hatch 换仓鼠、注册五屏 `total` 7→5 并全部加吉祥物旁白、`summary` 与 `notifications` **合并成一屏**「YOU'RE ALL SET / This is you」(删了 notifications.tsx)、age 竖向滚轮改**横向标尺**。
- **两处刻意偏离设计稿,已在代码头注释写清理由**:
  1. 登录页第二颗按钮 Figma 写死 `Sign in with Google`,iOS 上改成 `Sign in with Apple` —— App Store 指南 4.8 要求提供第三方登录就必须同时提供 Apple 登录。
  2. pet-hatch 底部 Figma 只有 `Name him`,命名页还没做,保留原来两颗(放桌面 / 继续)。
- **踩到的三个坑**:
  1. `pnpm boundaries` 报 identity leak —— 我在 `src/` 的**中文注释**里写了 "blueprint" 就算泄漏(它扫的是文本不是 import)。改成「模板」即过。
  2. `pet-flat.mp4` 把 `Colors.tagBg` **烤进了视频**(无 alpha),换主题色后 `tests/design/pet-flat-background.test.ts` 直接挂,修法是 `pnpm render-pet-flat luka`。这类「资产与主题隐式耦合」已写进重写后的 `design/README.md`。
  3. `design/README.md` 是 new-app 派生时没改的残留 —— 标题还写着「Intro 设计 Token」、命令写 `pnpm --filter intro tokens`、色板是一套 `#0A7CFF` 蓝。已重写。
- **验证**:`pnpm luka:typecheck` / `pnpm lint`(exit 0)/ `pnpm boundaries` / `pnpm luka:test` **574 passed(105 files)**,新增 `tests/design/onboarding-flow.test.ts` 守步数与路由链闭合。
- **遗留**:`app.config.ts` 的 splash / adaptiveIcon 底色仍是品牌蓝 `#0044A0`(那是 `icon.svg` 自己的底),启动瞬间蓝底、进 app 变奶油,会有跳变。要一起改得重画图标(`pnpm render-icon luka`),设计稿没给新图标,没动。

## 工作日志 — 03:20 拿到注册五屏 Figma 规格,回头 review 并修正上一版实现

- **做了什么**:用户补了 6 个 Figma node(`363:876/878/880/882/884/886` = Name / Age / Identify / Photo / Location / All set),对照 review 上一版实现,查出 **30+ 处偏差并全部修掉**。
- **最大的一条教训:上一版有一半的取值是我"自己发挥"的,不是从设计来的。** 上一版只有登录页(321:881)有精确规格,注册五屏我是照着一张位图截图推的,于是:
  - 卡面我定了 `#FFFDF8`(奶油白),**设计稿是纯 `#FFFFFF`**;
  - 焦糖我定了一个 `#D89A5C`,**设计稿有三个**:`#C8863C`(面/点)、`#C08A3E`(字/细线)、`#D99A3E`(选中描边),还有选中文字 `#8C5C29`;
  - 奶油淡面我定了一个 `#F5EADA`,**设计稿有五个**,各贴一个物件:`#F6F1E7` 照片区 / `#EFE7D8` 通知卡 / `#ECE3D5` 返回键 / `#E2DACE` 进度未完成 / `#E7DED0` 禁用 CTA;
  - 圆角我定了"只有 16px 一档",**设计稿是 16/18/20/22 四档**(越大的块圆角越大)。
  - → **收敛设计稿给的取值 = 丢信息**。设计师逐个指定 5 个几乎一样的奶油色,是因为每个都贴着一个具体物件;合并之后"按钮禁用"和"进度没走到"看起来一样重。以后拿到精确规格,先按规格落,想收敛也得等落完再说。
- **字重也错了一整类**:设计大量在小字号上用 **ExtraBold 800**(eyebrow / 城市名 / 人名 / 通知卡标题 / 输入值),我全写成了 700 或 400。补了 `font-sans-extrabold` utility。副标题设计是 **600**,我写成 400。
- **两个组件是整体重做**:
  1. `ZipCodeInput` 从模板继承的**五格验证码样式** → 设计稿是**单个 342×56 白框**(定位针 + 输入值 + 焦糖光标)。五格会让它看起来像验证码,而这一屏的主体其实是下面的城市列表。
  2. 位置结果从**卡片列表** → 设计稿是**无底色的行 + 1pt 分隔线**(城市名 16px/800,州 12px/600,右侧 chevron)。搜索框已经是这屏唯一的框,再给每条结果套框就有五个同样重的方块在竞争。
- **发现设计稿自身一处不自洽(已按更自洽的一侧实现并写进注释)**:年龄标尺的刻度线间距画的是 10.06pt(35 根均匀铺满),而标签 15/20/25/30/35/40 是按整宽五等分放的,间距 68.4pt(= 每岁 13.68)。两者对不上。**按标签走**(ITEM_STEP=13.68),否则会出现"指针停在 30 上、30 的标签却偏出去半格"。
- **另一个坑**:年龄指针要比最高刻度往上多露 8pt,但 viewport 是 `overflow: hidden`,给指针写 `top: -4` 会被裁掉 —— 改成给刻度条整体下移 8pt、指针 top 0。
- **验证**:`typecheck` / `lint`(exit 0)/ `boundaries` / `tokens:check` / `test` **574 passed** 全绿。
- 分支仍是 `personal/zz/luka-login`,**未提交**。

## 工作日志 — Tab 编排改按 intro 走

- **做了什么**：Luka 底栏 Tab 从「划卡 / Discover / Like / Chat / Me」改成 **intro 那套：Chats · Calls · People · Discover · Me**，同步改两篇飞书文档。
- **先读代码定口径**（`apps/intro`）：路由 `index` / `scene` / `into-you` / `discover` / `me`，标签在 `components/tabs/labels.ts`，图标在 `icons.tsx`，两套 tab bar 由 `tabs-navigator.tsx`（+ `.ios.tsx` 平台重载）分派——iOS 26+ 走系统 Liquid Glass 条，其余走 `pill-tabs`。Chats 是首页并挂 TIM 未读角标；Me 选中时图标换成用户头像。
- **关键发现：划卡在 intro 里不占 Tab 槽位**，它是 **Discover → Nearby → deck 视图**（与 list 共用切换按钮）；Discover 一级分段 Feed / Nearby，People 二级分段 Connections / Friends。
- **顺带消掉一条待确认**：原 PRD 有「无文字底栏下 Swipe / Discover 的可辨识度」——底栏不再同时出现「卡片堆」和「指南针」两个都表示「一叠可以看的东西」的图标，问题不存在了。
- **结果**：PRD rev 48 → 52（产品结构段重写 + 待确认条目改写），技术文档 rev 15 → 17（Tab 行标注 intro 分类，参照从泛指的 `app/(tabs)/_layout.tsx` 改成 `apps/intro` 的具体四个文件）。
- **踩坑**：PRD「九、待确认」里原本有**两条待确认被挤在同一个 `<li>` 里**（「五 Tab 的具体编排」和「第八章的创意储备采纳哪几条」），`block_replace` 整个 li 时把后半条一起冲掉了。**改 li 前先把它的纯文本打出来看一眼有没有夹带**，别只看开头那句加粗标题。已用 `block_insert_after` 把第二条拆成独立条目补回。
- **Prompt Pack 不受影响**：`Luka-Signup-Prompts-EN.md` 的 Master Prompt 写的是「NO TAB BAR ANYWHERE IN THIS PACK」，注册流程九屏本来就不画底栏。

## 工作日志 — 03:26 年龄标尺改用 ScrollView snap 机制(参考 koda 滚轮)+ 指针对齐抽库加测试

- **做了什么**:按用户要求,`apps/luka` 的 `AgeRuler` 从手写 `Gesture.Pan` + `withSpring` 换成 **`ScrollView` + `snapToInterval` + `decelerationRate: "fast"`**(与 `apps/koda/src/components/onboarding/age-wheel.tsx` 同一机制),样式仍按 Figma 的横向刻度尺。
- **为什么换**:Pan 方案要自己实现减速曲线、边界回弹、「甩一下停在整格上」,而这三样系统滚动视图已经调好了。手写版快速甩动会停在两格之间,指针与刻度对不上。
- **震动口径与 koda 不同(有意)**:koda 是滚轮大数字,`onMomentumScrollEnd` 停下后震一次;这里是刻度尺,改成 **`onScroll` 里每滑过一格震一次** —— 刻度尺模拟的是有棘齿的实物,手指还在动却没反馈,它就只是一张会动的图。`useSelectionHaptic` 自带按值去重,可以每帧调。
- **指针对齐的关键(易错点)**:左右 padding 必须是 `cardWidth / 2 − ITEM_STEP / 2`。**少减那半格**,每个 snap 落点整体偏半格 → 指针永远压在两根刻度中间,而且怎么滑都对不回来。这是**静默错误**:不崩、不报警、只是永远歪着。
  - 因此把这段几何抽到 `src/lib/age-ruler.ts`(`sidePadFor` / `indexToOffset` / `offsetToIndex` / `tickOffsetFromCentre`),配 `tests/lib/age-ruler.test.ts` **逐格**验证「每一根刻度都能滚到指针正下方」,覆盖 375/390/430 三种屏宽 × 81 个年龄。
- **步长从 13.68 取整到 14**:`snapToInterval` 与每根刻度位置乘同一步长,小数在第 81 格累积出可见错位;25 岁跨 350 vs 342 看不出来。
- **又踩一次 boundaries 身份泄漏**:注释里写「跟 Koda 的年龄滚轮同一个做法」→ `apps/luka ... mentions "koda"`。改成「本仓库另一个包」。**这是同一个坑第二次踩**(上次是 blueprint)—— 在 `src/` 里提任何别的 app 名都会被 `scripts/ci/boundaries.mjs` 拦下,包括中文注释。
- **验证**:`typecheck` / `lint` exit 0 / `boundaries` / `test` **580 passed(106 files)**。分支 `personal/zz/luka-login`,仍未提交。

## 工作日志 — 03:32 push `personal/zz/luka-login`(两个 commit)

- **做了什么**:把 Luka 登录 + 注册流程的全部改动分两个 commit 提交并 push 到 `origin/personal/zz/luka-login`(新分支)。
  - `c7259056` feat(luka): 整套设计 token 换成奶油/可可暖色 + Nunito(12 文件)
  - `50aca4be` feat(luka): 按 Figma 实现登录与注册流程(32 文件)
- **为什么拆两个**:token 换血本身是一个完整的逻辑变更(换值不换键 + 形状语言白名单 + 生成物 + 文档),屏幕实现是另一个。混在一起的话,`theme.js` 那 400 行 diff 会把屏幕改动淹掉。
- **拆分小技巧**:`photo-dropzone.tsx` 的 `git mv` 早先已进 index,所以第一个 commit 拿到的是 `R100`(只移动、内容未变),第二个 commit 才是内容重写 —— 移动的动机(进 shape-language 白名单)本来就属于 token 那一组,正好干净。
- **验证**:pre-commit(lint)+ pre-push(boundaries + 全 workspace typecheck)全过。PR 地址:`https://github.com/presence-io/sitin-rn/pull/new/personal/zz/luka-login`。
- **未做**:没开 PR(用户只说 push);没在模拟器跑起来验手感(年龄标尺的震动与吸附手感只能真机/模拟器确认)。
