---
title: 2026-08-21
date: 2026-08-21
tags: daily
---

# 2026-08-21

## 今日重点

- [ ]

## 工作日志

### 工作日志 — PWA 运营池后台:头像昵称对齐 databoard + 表格换 ProTable 内置 search

- **分支**:`personal/zz/admin-pwa-benefit-pool`(sitin-next3 checkout,app-minerva-web + business 后端)。
- **问题1 头像昵称空**(截图:灰底占位 + 只有 ID):根因在后端 `business-minerva-pwa-pool` 的用户信息组装和 pwa-databoard 不一致 ——
  - 头像:databoard 的 `custom_avatar` 是 JSON 串(`{"url","minUrl"}`),要 `JSON.parse` 取 url;pwa-pool 的 `resolveAvatar` 直接把**原始 JSON 串**塞给 `avatar` → 前端 `<Avatar src>` 加载不了 → 灰底。
  - 昵称:databoard 用 `username || nickname`;pwa-pool 只用 `nickname`(这些用户多半 nickname 为 null、名字在 username)→ 空。
  - **修**(对齐 databoard):`pwa-pool.sql.ts` `USERINFO_BY_IDS` 增查 `username`;`pwa-pool.model.ts` `UserinfoRow` 加 `username`;`pwa-pool.service.ts` 新增 `parseCustomAvatarUrl`(复刻 databoard util)+ `resolveAvatar` 改优先级「解析 custom_avatar URL > beautified > avatar_id」+ 新增 `resolveName=username||nickname`,用在 list/detail 两处。
- **问题2 表格用 ProTable**:PwaPool **本就用 ProTable**(精简版,`search=false`+`options=false`+手搓 toolBar)。用户要对齐 databoard 完整用法 → 换内置 search 表单 + 开 options。
  - `config.tsx`:加 `searchCols()`(用户 ID→`keyword`;入池时间 `valueType:'dateRange'`→`search.transform` 映射 `joinedStart/joinedEnd`,均 `hideInTable`),`columnsOf` 前置。
  - `index.tsx`:`search={{labelWidth:'auto'}}`、去 `options={false}`、toolBar 只留批量按钮、删本地 `keyword/range` state、`request` 从表单 `params` 取搜索值。
- **验证**:后端 eslint 0 error;前端 eslint 0 error(只剩既有 `max-lines` warning,组件本就 >80 行且此次减了行)。tsc 报错均为 workspace 未 build 的既有 dep/类型问题(`@heyhru/server-plugin-pg`、`tx:any`),非本次改动。
- **生效**:前端 Vercel(admin-dev)部署即生效;头像昵称修复在 business 后端 → **需重部署 minerva-server 到 admin-api-dev**。规范模式未自动 commit/push。
- **坑**:app-minerva-web 是 legacy 包,`max-lines`/`no-nested-conditional` 等降级为 warning;新引入的嵌套三元改回 if 风格避免加 warning。
- **提交/合并**:commit `44fe86491` → push `personal/zz/admin-pwa-benefit-pool`;合入 `release/test-admin`(merge `e2a89db89`)。合并遇 **add/add 冲突**(test-admin 已有 PwaPool 旧版基线,无共同祖先)→ 冲突块 HEAD=旧码、theirs=我的新码,`git checkout --theirs` 全取新版,`git diff 44fe86491` 验证 5 文件完全一致、合并净改动仅这 5 文件,无 submodule/意外文件。
- **真根因(部署后仍灰)commit `276ad342b`**:头像昵称空**不是**解析问题,是 **user_id 类型不匹配的 Map join bug**。dms 直查 dev 确认 userinfo 里数据齐全(2100064817=username"Cc"、custom_avatar JSON 都有);`information_schema` 查列类型:`pwa_tier_member_list_v.user_id` = **bigint**(node-pg 返字符串)、`userinfo.user_id` = **integer**(返数字)。service list 里 `infoMap.set(数字)` 后 `infoMap.get(字符串)` → JS Map `2100064817 !== "2100064817"` 永远 miss → nickname/avatar/country/videoLevel 全空。databoard 能显示是因其 userId 来源也是 integer、类型一致。**修**:list handler 统一 `Number(r.user_id)` 再 join(userIds/infoMap.set/get/levelMap.get)。→ **教训:sitin archat 库 pwa_tier_* 视图 user_id 是 bigint 会被 pg 返字符串,和 integer 的 userinfo join 前必须转 Number。**
- **去双请求 同 commit**:PwaPool 刷新触发两次 `members/list`(useRequest 拿 counts pageSize:1 + ProTable request pageSize:20)。list 响应本带 `counts` → 去掉单独 useRequest,counts 存 state 在 request 里 `setCounts(res.counts)` 复用,刷新只剩一次请求。
- **部署提醒**:头像昵称是纯后端修复(`business-minerva-pwa-pool`),前端 admin-dev Vercel 部署了也没用,**必须 minerva-server 部署到 admin-api-dev** 才生效。可用 dms(`POST admin-api-dev/api/aliyun-dms/run` + Bearer,body `{"sql":...}`,MINERVA_TOKEN 即可)直查 dev archat 库校验。
- **闭环验证**:后端部署后 dms + API 双验证 —— `members/list` 返回 nickname "chv" + custom_avatar 完整 URL;头像出来了(用户之前灰是浏览器缓存了部署前的空响应,硬刷新即好)。

### 工作日志 — 运营池详情抽屉:枚举中文化 + 状态 Tag + 时间格式(commit 779e11179)

- **纯前端**(`app-minerva-web/src/pages/PwaPool`),只需 Vercel 部署。
- **枚举中文 + Tag**:MemberDetailDrawer 入池信息(所在池/状态/来源)、最近邀请(类型/状态/决定)英文枚举 → 中文;状态类用 `<Tag>` 配色(生效中/系统邀请/已接受/接受 等)。config.tsx 新增 `POOL_TYPE_TEXT`/各 `*_META` 映射 + `metaTag` + `poolTypeText`/`memberSourceText`/`invitationTypeText`/`memberStatusTag` 等导出。
- **时间**:新增 `fmtDate`(取 ISO 前 10 位避免时区偏移);最近保底日期 `2026-08-21T00:00:00.000Z` → `2026-08-21`;过期时间从 fmtReview 改 fmtTime(年月日时分秒);补展示「邀请时间」(后端本就返 `latest_invitation_created_at`,types 补字段)。
- **顺带修**:列表小美池「入池来源」列原用 `SOURCE_TEXT`(只含 NEWCOMER/ACTIVE/MANUAL_IMPORT,漏 `SYSTEM_INVITATION`)→ 显示空白;改用 `memberSourceText`。删除旧 `SOURCE_TEXT` + 去掉未用的 `MemberSource` import。
- **「基础信息没展示全」结论**:dms 查 2100063961 的 `preferred_location`/nickname 本身为空 → 「国家/地区 -」「视频等级 -」是真实数据非 bug(其它用户有 country 正常显示)。基础信息可取字段都已展示。
- **验证**:eslint 0 error(MemberDetailDrawer 2 个既有 warning,改动后从 3 个减到 2 个)。
- **提醒**:`scroll={{ x: 'max-content' }}` 从最初版本就在 `index.tsx`,一直有,无需加。

### 工作日志 — PWA 邀请入口卡标题不换行自适应(nationwide + test-pwa)

- **背景**:承接权益池需求,设计要求两个邀请入口卡的大标题窄屏不换行、自适应缩放。
- **改动**:`packages/app-pwa/src/pages/Me/GradeInvitationCard.tsx` 两处标题加 `whitespace-nowrap` + `font-size: clamp(...)`:
  - 新人&活跃「Unlock Higher Earnings」→ `clamp(18px, 5.6vw, 22px)`
  - 小美「You're one of our top picks」→ `clamp(14px, 4.4vw, 17px)`
  - 原理:`nowrap` 保证一行,`clamp()` 随屏宽缩放,宽屏取上限、窄屏缩到下限,始终不换行不溢出。
- **提交/同步**:commit `8279bb73a`(`feature/pwa-nationwide`)→ push;同步合入 `release/test-pwa`(`5cb6bbc03`),两分支内容一致、test 落后 nationwide 0 commit。eslint 通过。

### 工作日志 — Snapchat DM 监听加去重键日志 + 上传 dev v111

- **背景**:排查一份 SC-Scraper 控制台日志(女号 `yy-668899` → 男号 `cc-bo88`,20 分钟)时,用户问「为什么最后一次没上报 NEW_MESSAGE」。
- **结论(机制,非 bug)**:`startDMListener.js` 的 NEW_MESSAGE 是**「会话未读边沿 + 跨 reload 去重」**触发,不是每条消息触发。
  - 未读判定按会话维度;`getChatMessages` 打开会话会把整会话标记已读 → 未读归 0。已未读、未读期间连发多条(日志见 `newMessages:3/4`)只算一次。
  - **跨 reload 去重**(`:290-345`):后端 `navigateToInbox` 整页刷新会清空内存快照,故把「已报未读标识」持久化到 `sessionStorage["__sc_dm_reported"]`,键 = `reportedKeyOf(t)= status + "|" + timestamp`;`reported[chatId] === key` 则跳过不报,专防「同一条挂着不清的 Snap 每次刷新被反复上报」。
  - **最后一次没报** = 该未读 `status|timestamp` 与上次已报相同(内容没变)→ 命中去重跳过(前 5 次同会话反复上报是因男号每次又发新消息、timestamp 变了 → 新键 → 视为新增)。**不是日志截断**:上报是同步的、会在「chatlist listener 已启动」之前打印。
  - **隐患**:去重键含 timestamp,`未接来电` 那条 timestamp 为空 `""` → 键退化成 `"未接来电|"`,同类型易撞键漏报。
- **改动**:`packages/app-ins-scripts/src/snapchat/actions/startDMListener.js` `diffAndReport()` 循环里,对每条未读打印 `未读 <nick> key=<status|timestamp> 已报=<旧key> → 跳过(去重命中)|新增`,让去重判断可观测(之前完全静音,库里存的键/命中都看不到)。
- **验证**:lint 改动零新增错误(HEAD 与现在都是 29 个既有 `no-undef`;**snapchat 脚本未进 eslint ignore**,`packages/app-ins-scripts/eslint.config.js` 只 ignore 了 `src/instagram/**`);`build.mjs` 出 `dist/snapchat/automation.js` 235kb。
- **提交/部署**:commit `0213caef4`(scope `app-ins-scripts`)→ push `personal/zz/sp-snapchat`;`node scripts/upload-all.mjs --platform snapchat --files startDMListener` 传 dev,**v111 已激活**(automation:16,15.2KB,已通知设备)。
- **坑复现**:upload 默认 `super_admin/admin123` 在 admin-api-dev 仍失效,需用户给 JWT(`MINERVA_TOKEN`),本次用户提供。
- **环境**:本次在 `sitin-next3` checkout 操作(此前该 checkout 一直做 PWA nationwide);切 `personal/zz/sp-snapchat` 时 proto submodule 需 `git fetch` 后 checkout `94ab20b9`,之前 PWA 的 `video_income_target_micros` `.proto` 便利改动已在 submodule 内 stash 保留(gen 真源已在主仓 nationwide)。

### 工作日志 — PWA 权益弹层兜底改 +50%/+50%(commit f5d885255)

- **背景**:用户贴 `getPwaTierMe` 响应发现 pending 邀请阶段**后端不返 `activeBenefits`**,而弹层显示 +50% Video/+100% Chat,问数字哪来的。
- **查证(feature/pwa-nationwide)**:数字是**前端写死基线** —— `gradeBenefits.ts` `deriveBenefitRows` 里 `benefits ? bpToPercent(...) : 50/100`,store `benefits = res.activeBenefits`(`pwaTierStore.ts:63`)缺失 → 命中 else 兜底。不是后端真实权益。
- **改动**:按用户要求把 chat 兜底 `100 → 50`,基线变 +50%/+50%(`gradeBenefits.ts:38`)。后端有 activeBenefits 仍优先真实值,只缺失才兜底。
- **提交/同步**:commit `f5d885255` → push `feature/pwa-nationwide`;合入 `release/test-pwa`(`11f94031d`,干净合并 1 文件)。两分支同步,已切回 nationwide。
- **遗留**:后端 pending 邀请阶段补发 `activeBenefits` 才能显示真实权益(旧 follow-up)。

### 工作日志 — PWA Welcome Checklist 闪切修复(sp-v2)

- **现象**:新用户 Task 页顶部 Welcome Checklist 先显示图一(对照组 4.0_original:Location/FaceVerify/Install/Instagram $3,共 $5.00),再切图二(onboarding_b:GoLive/Chat $0.10/Profile $0.50,共 $4.10)。
- **根因**:`CashoutTaskList` 的任务集来自 `useOnboardingExperiment()`,该 hook 在 `!isLoaded || segment!=='new'` 时返回对照组。两个登录期异步(`fetchUserSegment` void 非阻塞、`fetchAbConfig` 已 await 但排在重 await 后 resolve 晚)未就绪前 → 首帧渲染对照组(图一);segment resolve(new)+ AB(onboarding_b)到位后 displayTasks 依赖 experiment 重算 → 切图二。闪切由 **segment 的 isLoaded** 决定,不是 AB;abStore 无 loaded 标志、组件挂载即用默认值渲染,故未被挡。
- **修法(方案 B,gate 渲染)**:① `stores/abStore.ts` 加 `loaded`+`setLoaded`;② `http/abApi.ts` `fetchAbConfig` finally 里 `setLoaded(true)`(成功/兜底都置);③ `pages/Home/CashoutTaskList.tsx` 读 `userSegmentStore.isLoaded && abStore.loaded`,未就绪 return 骨架占位(h-220 pulse),就绪后一次性渲染正确分组。新用户不再闪图一→图二、老用户不再闪现 Welcome Checklist。
- **验证**:eslint 0 error。未 commit(规范模式)。
- **变体映射**:`constants/experimentConfig.ts` —— `4.0_original`=对照(现网 v4,含 Instagram),`onboarding_b`=教程组(GoLiveTutorial/DhTutorial=Chat $0.10/Profile Setup $0.50)。
- **上下文**:切到此分支前在 admin 分支 stash 了收益/时薪 WIP(`git stash`「pwa-pool 收益/时薪 WIP」),弄完 sp-v2 需切回 pop。

### 工作日志 — PWA 注册后首次 loading 卡死修复(sp-v2,commit e241e1ae0)

- **现象(真机 APK)**:注册后首次进入,① Welcome Checklist 顶部灰骨架卡死;② Win Free Cash「Checking your accounts…」卡死。
- **根因1 Checking**:`socialProxyChecking` 初始 true,唯一由 `checkSocialProxyAbnormal`(经 `runOpportunisticInit`→`initInsTask`)清。`runOpportunisticInit` 只在 initUser 内 `userState===FullRegister` 触发,而 `useUserInit` 注释说的「App.tsx 补触发 effect(新用户注册完成)」**根本没实现**(App 只解构 initUser/cleanUser)。主 init effect 有 `userId===prevUserId return` 守卫,注册中途 userState 从 SimpleRegister 升 FullRegister(userId 不变)不重跑 initUser → runOpportunisticInit 永不触发 → 永久 Checking。
- **根因2 骨架卡死(我上一条闪切修复引入的风险)**:骨架 gate 等 `segmentLoaded && abLoaded`,但 `services/userSegment.ts` `fetchUserSegment` **catch 里静默 return null、不置 isLoaded**(注释「on failure left as-is」)→ getUserBizGroup 失败(注册后首次后端未就绪常见)→ isLoaded 永远 false → 永久灰屏。
- **修法 A+B1+B2**:
  - A `App.tsx`:解构 `runOpportunisticInit` + 加 effect `if (isReady && userState===FullRegister) runOpportunisticInit()`(幂等)。
  - B1 `userSegment.ts`:catch 里 `setSegment("new")`(与成功 else 同假设)置 isLoaded=true,收敛。注意默认 segment 本就是 "new",故不改变分组值、只放开 isLoaded;canShowLegacyCashoutPopup 仍要 old,不误伤。
  - B2 `CashoutTaskList.tsx`:骨架 gate 加 2.5s `gateTimedOut` 超时兜底,`!groupReady && !gateTimedOut` 才显骨架,防任何未预期挂起永久灰屏。
- **验证**:eslint 0 error。commit `e241e1ae0` push sp-v2(前一条闪切修复 `55d2d31c9`;分支上另有 COLNA `9463aed07 fetch ab 前置`,互补不冲突)。
- **教训**:「加载 gate」类改动必须保证 loaded 信号一定会收敛(成功/失败/超时都置位),否则把闪切换成永久卡死。

### 工作日志 — PWA 闪切/骨架 追加修复 + 关键更正(sp-v2)

- **更正前一判断**:之前说「Welcome Checklist 走 taskRegistry 静态集、没接 experimentConfig」是**错的**。实际 `taskRegistry.ts:7` `import {onboardingTaskIds} from experimentConfig`,`onboardingTaskConfigs(true)=experimentConfig().onboarding` —— checklist **本就实验感知**。所以「checklist 出 Instagram」= experiment 回落对照组 4.0_original(含 Instagram),根因是 segment/AB 未就绪(即加载问题),非 merge 缺失。
- **真正未合并的两处(会咬人,待办)**:`ONBOARDING_GATING_TASK_IDS`(完成判据,registry v4 集,不随组变)+ `ONBOARDING_TASK_IDS`(去重,模块加载期 const 冻结在对照组)未实验感知 → onboarding_b 完成判据仍等 Instagram 有**锁死**风险。⚠️ 改判据=experiment.onboarding 前必须先确认 DhTutorial/GoLive/VideoCall 这类 custom 任务的 finish 是否写进 taskStore(DhTutorial 走独立 dhTutorialStore),否则换一种锁死。**此项未改,待验证后做。**
- **本轮实际改(commit 920195901)**:去掉 CashoutTaskList 骨架 gate 的 2.5s 超时兜底 —— 慢网下超时会在 segment/AB 未回时提前渲染对照组(图一)再切 图二,反成闪切元凶。改纯 gate 在 `groupReady`;依据:fetchAbConfig finally 必置 abLoaded(≤httpClient 45s)、userSegment 失败也收敛(B1),groupReady 一定收敛、骨架不会永久卡,短超时既有害又多余(任何 <45s 超时都会先出对照组=闪)。
- **提醒用户**:图一 出现对照组真实任务行(Authorize Instagram)说明测的很可能是未含 gate 的旧构建;需重新部署 sp-v2 再测。若部署后仍闪→experiment 真解析成对照组,查该用户 getUserBizGroup + ABTestConfigV2 返回。
- sp-v2 本轮提交链:`55d2d31c9`(骨架 gate)→`e241e1ae0`(注册后 loading:runOpportunisticInit 补触发 + segment 失败收敛 + 超时兜底)→`377014028`(复用原骨架)→`920195901`(删超时)。

### 工作日志 — PWA 运营池新人+活跃两池指标(commit 3c0da1796)

- **分支** `personal/zz/admin-pwa-benefit-pool`。运维给了 strategy_feature 库连接(envPrefix `STRATEGY_`,dev-pg-dora/db=strategy_feature)+ 新后台设计图(3 张,字段按图)。
- **地基**:`app-minerva-server` config.ts 加 `strategyDbUrl=STRATEGY_DATABASE_URL`、main.ts `createNamedPgDb("strategy",…)`、.env.development/.local 兜底(从 archat URL 派生换库名)。⚠️ 变量名按 `<PREFIX>_DATABASE_URL` 约定取,需与运维 k8s 注入名对齐。
- **后端**(`business-minerva-pwa-pool`):sql/model/service 加候选指标 —— 收益(pwa_user_balance_change_history)、有效通话/时薪(user_call_order VIDEO_CALL/PAID/NORMAL/>0)、被右滑率/划卡数(strategy `female_daily_features` features->'daily'->>'right_swipe_count'/'swipe_count',新人3天/活跃7天)、opener 回复率(strategy `female_user_features` features#>>'{breakthrough,...}')、颜值(userinfo.face_score)、新人前3天平均通话/通话数(剔<5s/>10000s)、注册天数、最近邀请(pwa_tier_invitation DISTINCT ON)、命中分支(新人:颜值≥70/右滑≥20%&曝光≥20/通话≥80s&数≥5;活跃:文字/视频/文字+视频,阈值右滑≥15%&划卡≥20、文字≥$6、视频≥$50)。service 重构成 loadMetricMaps/loadCandidateMaps/buildMemberRow/candidateFields/newcomerFields/xiaomeiTodayFields + faceHit/swipeHit/callHit,全过复杂度15门槛。detail 端点复用 loadMetricMaps 补单用户指标。
- **前端**(`app-minerva-web/PwaPool`):MemberRow/MemberDetail 字段对齐;新人+活跃列表列按新设计(单列口径、rateCount 组合、邀请状态Tag、颜值、通话 mm:ss、命中分支中文串);新增 `ScreenMetrics.tsx` 详情筛选指标区(新人三分支/活跃文字·视频分支,逐条 用户值/标准/命中✓·未命中✗)。新增 fmtDuration/rateCount。
- **口径**:用户定「nacos 为准」→ 小美 golive=90、quality_days=0、拒接/短通话≤0.5、短通话阈值15s(建小美时用);B/D 未配按 `*_enabled` 视为不参与判定,活跃命中分支近似(不含 opener/发起率)、抽屉标准列占位。
- **卡点(未做,等数据源)**:①视频主动发起率无源;②视频拒接率/短通话率依赖「挂断方」埋点未上;③小美周字段(monitor pwa_daily_data 周聚合+guarantee rollup)。→ 小美池整体卡在这些,后端排期后再接。
- **验证**:全量 eslint 0 error;strategy SQL 按指标文档口径写,dms 只连 archat 测不了 → 部署 dev 后实测表名/字段/user_id 类型。**生效需部署 minerva-server 到 admin-api-dev**。
