---
title: 2026-08-29
date: 2026-08-29
tags: [daily]
---

# 2026-08-29

## 今日重点(承接 08-28)

- [ ] **call-transcript 抽屉**:PR #1058(→feature/admin)+ CR 加固已进 release/test-admin,**待部署**(后端 minerva-server → admin-api-dev;前端 app-minerva-web 单独部署)+ 浏览器实测。
- [ ] 其余承接见 [[2026-08-28]] 今日重点(onboarding 生产恢复、social-auth 阻塞任务等)。

## 工作日志 — 23:16 prod call-transcript 查不到数据的排查(数据侧,非接口 bug)

- **现象**:prod 打 `/api/call-feed/call-transcript?maleUserId=8859706&femaleUserId=6575068&createTime=1787911401426&callDuration=69` 返回 `{found:false}`。
- **排查(prod DMS: POST admin-api-prod /api/aliyun-dms/run + Admin token)**:
  - 接口**已部署**(返回结构正常非 404),`found:false` 是真实「库里没有」。
  - 男 **8859706 全表 0 行**(from/to 都没有);女 6575068 有很多行但都配**别的**男,且在 createTime(北京 18:03)±189s 窗口内 **0 行**(最近的 17:41/18:11 都在窗外)。
  - → **这通电话根本没生成 transcript**。transcript 是**条件生成**(实时语音识别/审核触发才入库),**不是每通都有**;这个男用户所有通话都没转写。
- **互换 male/female 无意义**:接口 SQL 本就双向查 `(from=male∧to=female) OR (from=female∧to=male)`,互换展开后是同一组合,结果必然一致。
- **验证抽屉要用「库里确实有内容」的行**:prod 最近有内容示例 id=1357762(男 8654505/女 7338744,08-28 18:32,103 条 "Good morning, how are you…")、id=1357766(男 7240059/女 7109696,149 条);dev 示例 id=5572。
- **教训(排错口径)**:抽屉/接口显示「无对话」先分两类——① 该通**没转写入库**(数据侧,占多数,查 `count where from/to=id` 即知)② 接口取数 bug。用真实有内容的行反查订单来区分,别拿随手一通当基准。

> 口径=北京时间(机器 UTC-5,写入前 `TZ=Asia/Shanghai date`)。prod DMS 查询用的 Admin token 属敏感凭据,**不入 memo**。

## 工作日志 — HH:MM PWA 运营池「德州转全国」张峥 12 bug 修复(sitin-next3)

**背景**:张峥「PWA Bug List汇总」Base「德州转全国」视图(当前版本=德州转全国)下,指派张峥的 12 条未解决 bug。结合 dora `personal/smallnest/pwa-tier-dev5-test` 后端 + sitin-next3 `personal/zz/admin-pwa-benefit-pool`(@3a99ba009,PR #966)真实代码逐条判定后修可改项。

**踩坑(重要)**:sitin-next 与 sitin-next3 两个 clone 同名分支 head 不同——sitin-next 的 `be7db71` 是 origin `3a99ba00` 的**祖先(过时)**。第一次"回忆"用了 sitin-next 旧树,漏了 30 个提交,判定失真。**教训:改前先 `git rev-parse origin/<branch>` 对齐 head,认 sitin-next3(=origin=PR)为准。**

**做了什么**(6 文件,`pnpm --filter @heyhru/app-minerva-web` lint 0 error / tsc --noEmit 0 error):
- T10309:`business-minerva-pwa-pool/src/pwa-pool.sql.ts` `POOL_LIST_UNION` 成员分支 +`AND status='ACTIVE'`(与 POOL_COUNTS 对齐,去 EXITED/重复)。
- T10288/T10318:`MEMBER_LIST`/`_COUNT` 结束边界 `<= ?`→`< (?::date + 1)`(含当天)。model 传 end 两次,`?::date` 兼容。
- T10317:`config.tsx nextReview()` 改「下一个美东周一 00:00」(dayjs utc+timezone 插件),export.ts / MemberDetailDrawer.tsx 同步;新人池删复核列/抽屉行。
- T10323:`index.tsx` 切池 `reload()`→`reloadAndRest?.()`。
- T10314:`XiaomeiMetrics.tsx` 加留池健康度 Tag(留池两项 AND → 正常/需关注)。

**判为已实现免改**(建议回归):T10303/T10316(读侧已过滤 PENDING)、T10321(导入=发小美邀请)、T10308(活跃池 INCOME_7D 已取,注意近7日非自然周)。
**非本 repo**:T10313(app-pwa 端 me 页卡片)。**待澄清**:T10270(具体布局块)、T10314 的 view-weekly-progress 按钮归属。

**状态**:改动在 sitin-next3 分支上**未 commit / 未 push**(规范模式待张峥批准)。切了 sitin-next3 检出分支 feature/pwa-beauty-live→admin-pwa-benefit-pool(仅无关未跟踪 docs,安全可逆)。进度文档 `docs/pwa-benefit-pool/任务拆解与进度.md` 已追加修复记录。

**飞书**:两个群邀请 link_token 无法反解 chat_id;为读群把 app `cli_a95cafcf9cb9dcd4` 的 user token 从步川切成张峥(device-flow 扫码,张峥授权);但张峥 160 群里仍无法定位那两个具体群,待张峥给准确群名。
