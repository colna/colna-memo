---
title: 2026-08-20
date: 2026-08-20
tags: daily
---

# 2026-08-20

## 今日重点(结转 08-19)

**消息金(#959)**
- [ ] 真机/浏览器走一遍暴击 + 非暴击两态

**权益池(#962)**
- [ ] 埋点事件名待数据侧对齐(未接)
- [ ] 邀请弹层权益预览来源待后端确认(现回退 +50%/+100%)
- [ ] 小美每日考核设计出后重做视觉(逻辑已通)
- [ ] 联调/mock 后走浏览器/真机验证
- [ ] `Maybe later` 是否需显式 REJECT 待产品

**分支对齐残留**
- [ ] 复核 test-pwa 被 feature/pwa 覆盖的独有改动里,是否有非临时的真实修复被误伤(iOS 真人认证 web 兜底 / 键盘 viewport / IG 任务等)

**结转 08-18**
- [ ] test-pwa2 真机验证 sitin4.1 CE/社媒授权;依赖后端 21022 online + card_type;useInsBridgeCallbacks 时序

## 工作日志 — 09:50 release/test-pwa 对齐 feature/pwa(以 feature/pwa 为准)

- **背景**:`release/test-pwa` 与 `feature/pwa` 真分叉(共同祖先 `f6625ff5`@08-07;feature/pwa 独有 66、test-pwa 独有 132)。test-pwa 独有多为 app-pwa 测试环境热修(iOS FaceID/真人认证含 revert 与「临时走 web 路径」兜底、键盘 viewport、IG 任务/AB/onboarding);feature/pwa 独有含较新功能 + minerva/admin + `Merge release/online-*`。差异规模 323 文件。
- **用户定**:以 feature/pwa 为准。
- **做法**:切本地 `release/test-pwa` ← `origin/release/test-pwa`,`git merge origin/feature/pwa -X theirs --no-edit`(冲突全以 feature/pwa)。
- **踩坑**:`-X theirs` 只解冲突 hunk,不冲突的 test-pwa 补充会留下 → `app-pwa/src/types/task.ts` 里 test-pwa 补的枚举块把 `VideoCall2Times=200021` **重复定义**(feature/pwa 已有)= TS duplicate enum。→ `git checkout origin/feature/pwa -- task.ts` 取 feature/pwa 版,`commit --amend` folded 进合并提交。
- **验证 & 结果**:`git rev-parse HEAD^{tree}` == `feature/pwa^{tree}`(`49de15ef…`)—— **树哈希完全相同,内容 0 差异**;历史保留 test-pwa 两条线(合并提交 `35a9570f4`)。
- **push**:`origin/release/test-pwa` `37ac2a709→35a9570f4`(fast-forward,共享分支非破坏,`--no-verify`)。远端 test-pwa tree == feature/pwa tree,已核对。
- **提醒**:test-pwa 独有内容被 feature/pwa 覆盖(多为临时兜底/revert,合意;键盘 viewport 等若非临时且未进 feature/pwa 则一并没了,待复核——见今日重点)。

### PR #959 + #962 合并到 nationwide(已 merge)

**做了什么**:`gh pr merge --merge` 依次把 #959(消息金,message-gold)、#962(权益池,rights-pool)合入 `feature/pwa-nationwide`。顶端 `dd2e22454`(#962)/ `d292ef9b1`(#959)。

**遇到的问题 → 怎么解决**:
- 合 #959 后 #962 mergeable 短暂 UNKNOWN(GitHub 重算)→ sleep 几秒重查,恢复 CLEAN 再合。两 PR 触及不同区域(chat reward vs Me 页 grade),互不冲突。

**残留**:
- [ ] nationwide 汇齐两需求后,整体 `feature/pwa-nationwide → feature/pwa`(待用户发起)。
- [ ] 两需求各自的 follow-up(见今日重点:埋点、真机验证、权益预览来源等)。

## 工作日志 — nationwide 合入 release/test-pwa 并 push

- **背景**:两需求 PR(#959/#962)已合入 `feature/pwa-nationwide`(顶端 dd2e22454)。要把 nationwide 带进 test 环境。注意 nationwide 基于旧 feature/pwa(8eb80e94e)+ proto MsgReward + 两功能;test-pwa 此前已对齐到最新 feature/pwa(963b5dbc4)。
- **做法**:在 `release/test-pwa` 上 `git merge origin/feature/pwa-nationwide --no-edit` → **无冲突**自动合并(两边改动区域不重叠:test-pwa 侧是较新 feature/pwa 提交,nationwide 侧是 proto/chat reward/Me grade)。
- **验证(push 前必做)**:`git submodule update` 同步 proto 工作树到 3884fef0 → `pnpm --filter business-pwa-proto build` 重建 dist → app-pwa `tsc -b` **0 error**。合并树可编译。
- **push**:`origin/release/test-pwa` `35a9570f4→9fc7a3089`(fast-forward,`--no-verify`)。远端一致。
- **结果**:test 环境含 最新 feature/pwa + 消息金(MsgReward) + 权益池。合并链路:message-gold/rights-pool → nationwide → (test-pwa 已带) ;主线 feature/pwa 仍待用户发起 nationwide→feature/pwa。

## 工作日志 — 移除 nationwide 上权益池 mock

- **做了什么**:`feature/pwa-nationwide` 删 `mocks/pwaTierMock.ts` + 还原 `Me/index.tsx` 进页接线(去掉 `?gradeMock=` 动态 import,只走真实 getPwaTierMe)。commit `2f8fad4c6`,push。
- **踩坑**:`git add <Me> <mock>` 里 mock 已被 `git rm` 删除 → pathspec 报错致 **git add 整条中止**,Me 编辑没纳入;首个 commit 只删了 mock 文件、漏了接线还原,HEAD 仍 `import("@/mocks/pwaTierMock")` 会编译坏。→ `git show HEAD:Me/index.tsx | grep gradeMock` 发现残留 → `git add Me + commit --amend` 补回,核对 gradeMock=0、tsc 0 再 push。**教训**:`git rm` 后不要再 `git add` 同一路径;混合 add 一个不存在路径会静默漏掉同批其它文件,提交后用 `git show HEAD:<file>` 复核。
- **残留**:`release/test-pwa` 上仍有该 mock(此前先合 nationwide 后删),待用户定是否同步移除。

## 工作日志 — HH:xx snapchat 通话 orderKey 修复(fiber 每气泡直读)

- **问题**:call 类型消息落 `dom_N`、无 orderKey。连带:①无 orderKey → `filterByCursor` 的 `!m.orderKey`
  永远保留 → 通话每次重拉、`lastOrderKey=null` 游标不推进(「一直下发 getChatMessages」根因之一);
  ②端内容哈希去重把同天多次「曾试图与你通话」(text+date 相同)塌成一条。
- **诊断**(临时 CALL-FIBER v1→v3,bridge.log 上传 dev 跑):确认通话气泡**自身 fiber props 里就有完整
  message**:`orderKey="27"`、`descriptor.messageId="7"`、`metadata.createdAt`、`contentType=13`、senderId。
  只是不在 `group.messages` 里 → `fiberMessages.build` 的 group 位置配对配不上。
- **修**:`fiberMessages.build` 加第二遍兜底——未配对气泡用 `msgFromEl`(getFiber→findMsgObj 递归找
  descriptor+messageContent)直读自身 message,补 orderKey/timestamp/senderId/conversationId/contentType。
  hybrid(group 配对为主 + 每气泡兜底),不动已配上的,规避回归。删诊断。
- **验证**:node --check / build / test 7/7。上线 fiberMessages v105 + getChatMessages v118。
- **教训**:Snapchat 通话/系统事件在 fiber 里是**完整 message、有 orderKey**,只是不进 group.messages;
  按 group 位置配对不可靠(c bo 日志里很多 text/image 也 dom_N)→ 每气泡直读自身 fiber 更稳。

## 工作日志 — HH:xx snapchat DM 监听跨 reload 去重(止未读反复上报)

- **现象**:orderKey 修好后 getChatMessages 对建斌历正确返回 0(旧内容全过滤),但 NEW_MESSAGE 仍一直上报。
- **根因**:getChatMessages 后端派 navigateToInbox **整页 reload** → 监听重启「首次扫描」→ 内存快照丢 →
  同一条未读(没打开的「新的 Snap」一直不清)每次 reload 都被当"新"重报 → 服务端反复下发 getChatMessages 空转。
- **修**(startDMListener.js):用 **sessionStorage** 持久化「已报未读 chatId→status|timestamp」(同源刷新保留),
  diffAndReport 只报 `reported[chatId] != status|timestamp` 的未读,同一条不再重报,直到 timestamp/status 变。
  统一了首次扫描/后续扫描的上报门(都走持久化 gate)。上线 v110。
- 关联:与 fiber orderKey 修复(v105/v118)一起,基本闭环「一直下发 getChatMessages / 通话 churn」。
- 教训:注入脚本靠内存做跨请求去重会被整页 reload 清掉;需要跨 reload 的状态用 sessionStorage(同源保留)。

## 工作日志 — HH:xx snapchat getChatMessages 新增 type:snap(点开入站 Snap 抓媒体)

- **需求**:对方发的未打开 Snap(「点击可查看」)之前掉兜底成 type:text、text 垃圾串。改为点开抓媒体、type:"snap"、存 base64。
- **实现**(utils.js + getChatMessages.js):
  1. utils.LABELS 加 `tapToView`(点击可查看/Tap to view…);
  2. getChatMessages 加 `isTapToViewSnap` / `pointerClick` / `closeSnapViewer`;
  3. 新增 Step 3.5「Open incoming snaps」:循环找「点击可查看」气泡 → 先抓 fiber(orderKey)→ 点开 →
     **复用 `SP.getStory.extractCurrentMedia()`**(同 aria-label="媒体内容")抓 blob 图/视频 → `fetchMediaAsBase64`
     转 base64 → `closeSnapViewer`(点 [aria-label=close] 兜底 Escape)→ push {type:"snap",mediaType,base64,orderKey…};
  4. extractMessages 合并 ctx.snaps:按 orderKey 去重(DOM 残留以 snap 为准)+ 改按 orderKey(BigInt)排序;
  5. Step5 只转 image/audio → snap 的 base64 保留;端 uploadSnapMessages 按 base64 落盘走 OSS(type 透传)。
  - 防死循环:按 orderKey 记 seen,再选到同一个就 break;抓不到媒体 base64=null。
  - ⚠️ 点开 Snap = 标记已读(不可逆);增量下只点新的未打开 snap。
- **上线**:utils v111 + getChatMessages v119。node --check/build/test 7/7 过。
- **待验证**:真机拿一条对方未打开 Snap 跑,看日志「点开 Snap #0 orderKey=… → 抓取成功 → Snap 抓取完成 1 条」,
  结果里该条 type:"snap"、base64 非空、orderKey 有;关闭是否干净(别卡在查看器)。

## 工作日志 — 小美 3K Club 权益+每日考核卡

- **分支**:`personal/zz/xiaomei-card` ← nationwide(`2f8fad4c6`)。本地 commit,未 push。
- **做了什么**:设计稿出了小美卡(Figma node 7945:1236 未完成态 / 8131:3215 完成态 / 8125:3025 展开态)。
  - `GradeXiaomeiPanel.tsx`:整卡——3K Club 徽章 + MEMBER BENEFITS ACTIVE 药丸、"You're One of Our Top Picks"、三项权益(Video/Chat/Daily Guarantee)、3K CLUB GUARANTEE 每日考核(Go Live/Video Earn./Eligible 三环,SVG dasharray 画环,未完成显百分比、完成显 ✓,三环全达标药丸转 Achieved 绿态)。
  - proto:`PwaTierGuaranteeProgress` 加 `video_income_target_micros`(int64 field 8)——Video Earn. 环分母($80 视频收益门槛),proto 里原本没有,**前端先行加**(用户定「先用这个字段,后端改再改」);src/gen 手改 6 处 + 重建 dist,node 往返验证过。
  - `gradeBenefits.deriveDailyAssessment` 加 videoTarget/videoRatio/三环 done/allDone。
  - `GradeSection`:小美池→GradeXiaomeiPanel,其它池→GradeBenefitPanel;删占位 GradeDailyAssessment。
  - Figma 导出 medallion + video/chat/dollar 3 图标 svg。
- **$80 口径**:展开态设计确认 = 「视频收益门槛」("Earn $80–$99.99? We'll still top you up to $100.")→ Go Live 满 180min **且** 视频收益 ≥ $80 才够格拿 $100 保底。
- **验证**:tsc 0 / eslint 0 / proto 往返(video_income_target_micros)正确。
- **Follow-up**:① `video_income_target_micros` 待后端 proto 落地/确认命名;② 「View details →」展开明细(node 8125:3025,进度条+top-up note+Show less)未做;③ 无 mock,预览小美卡需后端 XIAOMEI 数据或临时 mock。
- **push/PR**:`personal/zz/xiaomei-card` push(gh 当时是 buchuan1023,`gh auth switch --user colna` 切回再提 PR)→ **PR #980** → `feature/pwa-nationwide`。⚠️ gh 身份坑复现:提 PR 前务必 `gh api user -q .login` 核对是 colna。

### PR #980 小美 3K Club 卡（已 merge）

**做了什么**:`GradeXiaomeiPanel`(3K Club 权益+每日考核整卡,两态三环)+ proto 先行加 `video_income_target_micros`。merge 进 `feature/pwa-nationwide`(merge 提交 `19d5d201d`)。nationwide 现汇齐 消息金+权益池+小美卡。

**遇到的问题 → 怎么解决**:无冲突直接合(小美卡集中在 Me/grade 区域,与 nationwide 其它改动不重叠)。

**残留**:
- [ ] 后端在 proto 落地 `video_income_target_micros` / 确认命名(前端先行字段)。
- [ ] 「View details →」展开明细(node 8125:3025)未做。
- [ ] nationwide → feature/pwa 上主线(待用户发起);同步到 test-pwa 时留意 proto 已更。

## 工作日志 — 16:xx 社媒授权任务弹窗手动关闭上报 reportTimeout(pwa-spv2)

- **需求**:聊天里的社媒授权任务弹窗(`SocialAuthTaskDrawer`,「探针提示·对方申请交换」抽屉,`convState.tasks` 里的 `TASK_TYPE_SOCIAL_AUTH=4`)——**用户手动关闭(X / 遮罩)时需调 `reportTimeout` 结束任务**,此前只本地收起不上报。
- **两套弹窗辨析**(用户问「是 ListConversationStates 驱动的吗」):① CE **交换请求**气泡(`InsExchangeBubble`)走 `listUserInsExchangeOrder`,**不在** convState.tasks;② **社媒授权任务抽屉**(本次)走 `ListConversationStates`(RPC 7008 `/anomaly/list`)→ `convState.tasks`。本次改的是 ②。
- **做法**(对齐 `ProbeTaskDrawers` 的 onDismiss 模式,抽屉保持展示层):
  - `SocialAuthTaskDrawer.tsx`:加 `onDismiss?` prop + `handleClose()`(收起 + 触发 onDismiss,`dismissedRef` 按 taskId 去重两条关闭路径);`ModalContainer.onClose` 与右上角 X 都改走 `handleClose`。**只有主动关闭**才上报,`expired`(倒计时归零)/`defer`(让位风险单)被动隐藏不走。
  - `Chat/index.tsx`:传 `onDismiss` → `reportTimeout(targetUserId, convState.status??2, TaskType.TASK_TYPE_SOCIAL_AUTH).then(()=>fetchConvState(conversationId))`,与 probe 的 onDismiss 只差 taskType。
- **验证 & 提交**:app-pwa eslint 0;pre-commit 全量 lint 过。commit `2bd844290`,push `origin/personal/zz/pwa-spv2`(`e60438431→2bd844290`)。
- **踩坑**:pre-push `--affected` build 挂在**无关坏包**(`business-minerva-payhub` build 失败 + `business-pwa-proto`/`minerva-outreach` 缺 node_modules)→ 按已记录 sitin-next 例外 `--no-verify`(我的改动只 app-pwa 两文件,lint 已单独过)。
- **合入 test2**:`spv2 → release/test-pwa2` 例行集成合并(test2 是共享集成分支,独有 8 笔全是历次合 spv2/sitin4.1 的 merge 提交)。spv2 vs test2 跟踪的 proto submodule 一致(`3329ee048`),工作区那个 `faee2b0ee` 只是本地 dirty、与合并无关未纳入。merge 无冲突,只带社媒授权那笔(app-pwa 两文件)。合并提交 `efd05bdb6`,push `origin/release/test-pwa2` `034d0ec2d→efd05bdb6`(`--no-verify` 同上)。
- **spv2 再拉 feature/pwa**:feature/pwa 仅 2 笔新提交不在 spv2(#967 发帖文案 JSON 兼容 CreatePost.tsx、#948 keyboardLift.ts)。`git merge origin/feature/pwa` 无冲突,合并提交 `871473f6c`。push `origin/personal/zz/pwa-spv2` `2bd844290→871473f6c`(`--no-verify`)。

## 排查(未改代码)— spv2 Welcome Checklist onboarding 任务「先老后新」闪现

- **现象**:spv2 合入 feature/pwa onboarding 后,Home 顶部 Welcome Checklist(`CashoutTaskList`,`{!onboardingDone && <CashoutTaskList/>}`)先显示一版老 onboarding 任务、再切新的。用户诉求:以 feature/pwa(用户真实实验组)的 onboarding 任务为准。
- **根因链**:`CashoutTaskList` welcome 分支 → `onboardingTaskConfigs(true)` → `taskRegistry.ts:7` import 的 `onboardingTaskIds()` 来自 **`experimentConfig.ts`(A/B 版,非 constants/onboardingTasks 静态别名)** → `= EXPERIMENT_CONFIG[getOnboardingExperiment()].onboarding`。`getOnboardingExperiment()`(useOnboardingExperiment.ts:74)在 **segment 未 loaded / AB 未拉到时回落对照组 `4.0_original`**(abStore 默认 configs.onboarding_experiment 也是 "4.0_original",无独立 loaded 标志 → 首屏必回落对照)。首屏渲染对照组 4 任务(Location/FaceVerify/Install/**IG**),AB 拉到真实分组后 `experiment` 翻转、`CashoutTaskList` 的 `displayTasks`(useMemo deps 含 experiment)重算 → 整套切成 A 组(5 项:…/CompleteProfile/DhTutorial)或 B 组(7 项)= 「先老后新」闪。
- **三组清单**(experimentConfig.ts:45-116):对照 `[Location,FaceVerify,Install,BindIns(IG)]`;A `[…,CompleteProfile,DhTutorial]`(IG/开播移一次性);B `[…,GoLive,VideoCall2,DhTutorial,CompleteProfile]`。
- **修法(待用户拍板,未动手)**:分组真正就绪前 Welcome Checklist 出 Skeleton(CashoutTaskList 已有 `displayTasks.length===0 → Skeleton`,就绪前让 welcome 分支返回空即可)。方案 A 治本(abStore 加 `loaded` 标志 + hook 暴露 ready);方案 B 最简(用 `userSegmentStore.isLoaded` 门控,风险:AB config 若晚于 segment 到仍可能残留极短闪)。
- **观测手段**(用户暂不加):最直观是 CashoutTaskList welcome 分支临时 `console.log('[welcome-flip]', experiment, ids)`,能抓到先后两条(时间差几百 ms、清单对照→实验);`sitin4.state()` 只能看 `segmentLoaded`、看不到分组/清单。
- 教训:spv2 的 `onboardingTaskConfigs(true)` 不是静态集,而是**随实验分组解析**;任何依赖它的 UI 都会在 AB 就绪瞬间重算,未就绪回落对照组是所有「onboarding UI 先闪一版对照」的统一根因。

## 工作日志 — nationwide 加分级卡片预览 mock(含小美两态)

- **需求**:用户要在 nationwide 上按接口格式写 mock,预览小美卡 + 权益卡样式。
- **做了什么**(`feature/pwa-nationwide`,commit `bea2d5e99`,已 push):
  - 重建 `mocks/pwaTierMock.ts`,数据对齐 gen 类型(basis points / micros / 枚举)。四态:`member`(权益卡 GradeBenefitPanel)、`xiaomei`(小美进行中:Go Live 180/180、视频 $35/$80=44%、eligibility false → In Progress 黄)、`xiaomei-done`(视频 $80/$80、eligibility true → 三环全 ✓、Achieved 绿)、`invitation`(邀请入口卡)。
  - `Me/index.tsx` 进页接线:带 `?gradeMock=<key>` 动态 import mock、否则真实 fetch。
  - 小美两态就是改 `guaranteeProgress.postBenefitVideoIncomeMicros` / `eligibilityReached`。
- **验证**:tsc 0 / eslint 0。
- **前置**:预览前须 `pnpm --filter @heyhru/business-pwa-proto build` 重建 dist(含 `videoIncomeTargetMicros`),否则 Video Earn. 环显 0%。
- **残留**:mock 在共享 nationwide 上(URL 门控、动态 import 不进主 bundle,无害),上 feature/pwa 主线前是否删由用户定(之前权益池 mock 就删过一次)。

## 工作日志 — 客服物料「AI 误判」内部思路修正(据代码核实)

- **背景**:重发 `gracechat-cs-supplement.md`(GraceChat 客服物料 v2,基于 `app-pwa` feature/pwa 代码核实)后,用户指出「累计违规是 本分钟无收益」,需订正原文的收益影响口径。
- **核实**(`sitin-next3/packages/app-pwa/src/hooks/useVoiceModeration.ts`):
  - 弹窗文案写死 `timesTip: "Violation of this minute"`,三档 `100%/50%/0$`(`generatePopupConfig:205-221`)。
  - 收益/计数是**按分钟**:`onMinuteEnd()`(`:548-570`)每分钟结束清空 `violationsRef` 并 `disableEarningRef=false`;`MAX_VIOLATION_COUNT=3`,第3次达上限置 `disableEarning=true` → **本分钟收益归0**,**下一分钟自动恢复**,非账号累计扣、非封号。
  - `sharedViolationCountRef` 语音+画面共享同一计数器。
  - **关键区分**:原文把「暂停3分钟」挂在 AI 误判下是错的。`FinalWarningModal.tsx:41` 文案 "This is your 3rd violation **(Decline or Short Call)**...pause your account for 3 mins" → 暂停3分钟属**拒接/短通话**线,与 AI 语音误判无关。
- **改动**:编辑 `zhangzheng/gracechat-cs-supplement.md`「6. AI 违规误判」条的「内部思路」,改为按分钟归零表述 + 拆开两套机制,行号引用更新为 `:205-221`、`:548-570`、`FinalWarningModal.tsx:41`。已重发用户。
- **沉淀**:AI 语音违规≠拒接/短通话违规,是两条独立线;客服口径「本分钟无收益、下一分钟恢复」而非「永久扣/封号」。

## 工作日志 — 消息金金额单位修正(1/100 美分 ÷100 转美分)

- **问题**:MsgReward 的 `base/bonus/totalCents` 命名带 Cents,但**真实单位是「1/100 美分」(万分之一美元,与旧 earnedCents 同单位)**;RewardPopup/VoiceCritRewardModal 要美分 → 之前直接传原值,金额**放大 100 倍**。
- **修**(`feature/pwa-nationwide` commit `e8784ba9a`,push):`ActiveChat.showRewardCoin` 加 `toCents = raw => raw/100`,base/bonus/total 换算后再进弹窗;`docs/voice-crit-bonus-calc.md` 单位口径更正为「1/100 美分」。
- **教训**:字段叫 `xxCents` 不代表就是美分——sitin 金额单位常是「1/100 美分 = 万分之一美元」(见 [[sitin-money-units]]);消费前确认口径。旧 earnedCents 链路本来就 `/100` 转美分,MsgReward 同单位却漏了换算。
- 验证:tsc 0 / eslint 0。

## 工作日志 — nationwide 最新同步进 release/test-pwa

- **做了什么**:`release/test-pwa`(起点 `179006624`,已被他人推过 cashoutFee 等)`git merge origin/feature/pwa-nationwide` → **无冲突**,带进:小美 3K Club 卡(GradeXiaomeiPanel + proto `video_income_target_micros`)、金额换算修复(1/100 美分 ÷100)、分级预览 mock。14 files。
- **验证(push 前)**:`git submodule update` → `pnpm --filter business-pwa-proto build`(gen 含 videoIncomeTargetMicros)→ app-pwa `tsc -b` 0。
- **push**:`origin/release/test-pwa` `179006624→d7f16a526`(`--no-verify`)。
- test 环境现含:小美卡、金额修复、mock(`?gradeMock=` 可在测试环境预览)。部署/本地跑前须重建 proto dist。
