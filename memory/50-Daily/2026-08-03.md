---
title: 2026-08-03
date: 2026-08-03
tags: daily
---

# 2026-08-03

> 时间口径北京时间(UTC+8);机器时钟本会话多次前跳(07-31→08-02→08-03),以 `TZ=Asia/Shanghai date` 为准。

## 工作日志 — 17:24 sitin-next AI Code Review workflow 换模型/provider(xingsuancode)

- **需求**:把 `.github/workflows/ai-code-review.yml` 的 CR 模型换成 `gpt-5.6-sol` + baseUrl `https://api.xingsuancode.ai/v1` + 新 key,切分支改。
- **切分支**:`personal/zz/cr-xingsuan`(off `origin/feature/pwa` @ 63e7feb2e)。
- **关键判断**:原脚本是 **Anthropic Messages API**(`/v1/messages`、`anthropic-version` 头、`thinking`、响应 `result.content[].text`);新 provider 是 **OpenAI 兼容**(`/v1` + `model_reasoning_effort`)。所以不是换 URL 那么简单,连请求体+响应解析一起改成 Chat Completions:endpoint→`/v1/chat/completions`、去 `thinking`/`anthropic-version`、body `{model, max_tokens, reasoning_effort, messages}`、响应→`result.choices[0].message.content`;summary 模型名同步改 `gpt-5.6-sol`。
- **API 验证(实测)**:`reasoning_effort: "ultra"` 被 API 拒(400「use "max"」)→ 改用 `max`(codex 层的 ultra 这个原生 API 不认,max 即最高)。用真实 review 式请求体测通:HTTP 200、`choices[0].message.content` 有实质内容、`finish_reason: stop`、`reasoning_tokens=62`。
- **key 处理(安全)**:**不硬编码进 yml**,继续用 `${{ secrets.AI_CR_API_KEY }}`;经 stdin `gh secret set AI_CR_API_KEY --repo presence-io/sitin-next` 更新 Secret 值(Updated 变今日 17:24,确认生效)。⚠️ gh active 账号是 buchuan1023(有 admin,能 list/set secret),非工作区默认 colna——secret 是仓库级、与 commit 身份无关。
- **提交 + PR(经批准)**:commit `feb4e922a`,scope `chore(ci)`。⚠️ commitlint `subject-case` 会拒绝英文开头的 subject(「AI Code Review …」被判 start-case),subject 改**中文开头**「切换 AI 代码审查到 …」才过。pre-commit(lint FULL TURBO + circular)过;pre-push 全量 test 又被无关 minerva 包挡(`business-minerva-annotation#build` 等)→ 授权例外 `--no-verify`。开 PR **#829**(`personal/zz/cr-xingsuan` → `feature/pwa`,gh 切 colna 后建),https://github.com/presence-io/sitin-next/pull/829
- **`reasoning_effort` 有效档(实测)**:minimal/low/medium/high + `max`;**`ultra` 被 API 拒**(400 `use "max"`),用 `max`(最高)。
- **AI_CR_API_KEY 改哪里(记牢)**:值是 GitHub **repo Secret**,不在 yml。① 网页 `Settings → Secrets and variables → Actions → Repository secrets`(https://github.com/presence-io/sitin-next/settings/secrets/actions);② CLI `printf '%s' "<key>" | gh secret set AI_CR_API_KEY --repo presence-io/sitin-next`;③ yml 第 27 行 `${{ secrets.AI_CR_API_KEY }}` 只是引用注入,不存值。base_url/model 才是写死在 yml 可直接改。
- key 明文不入 memo/文件。

## 工作日志 — 18:12 修 Snapchat「首次登录态检测必失败、再查才好」

- **现象**:GraceChat(com.gracechat.prod)首次检查 SC 登录态一定失败(上报 `notifyPageAbnormal type:1` 关会话),再查一次就好。
- **根因(结合 sitin-next `personal/zz/sp-snapchat` + GraceChat `p/ljb/snpachat`,两边检测逻辑一致)**:
  1. **检测窗口太短**:`checkLoginStatusIIFE` 只在挂载后 1s/2s/3s 各查一次(总 ~3s)。而 `/v2/welcome` 是 Snapchat Next.js SPA,日志里 `Abort fetching component for route "/v2/welcome"` 说明 ~2.3s 时页面还在重载 welcome 路由,登录卡片渲染在 3 次轮询**之后** → 全落空。
  2. **判定把「没渲染好」当「未登录」**:`checkLoginStatus` 只有登录态正向信号(welcome 卡片 / web.snapchat 好友列表),找不到就一律 `type=1`,与「真未登录」不可区分。
  3. **端超时兜底**:`PWAWebViewFragment.doCheckSocialProxyAbnormalWebview` 的 `DEFAULT_TIMEOUT_MS=30_000`;JS 不上报,端 30s 后以 `type:-1 error:timeout` 照样 `SocialProxyFloatingView.finish` 关会话。→ 所以「不上报」不安全,但 JS 有 ~27s 空间没用。
- **修法(B 方案,改 sitin-next 源两文件)**:
  - `checkLoginStatusIIFE.js`:窗口 3s→**25s**(< 端 30s)、间隔 1s→**500ms**;`type=0` 命中即返回;登出改**正向信号 + 连续稳定 3s(LOGGED_OUT_STABLE_MS)**才判 `type=1`(避开已登录用户 `/v2/login→/v2/welcome` 跳转途中短暂闪现登录表单被误判关会话);窗口耗尽才报 1。
  - `checkLoginStatus.js`:加 `checkLoggedOut()`(`input[type=password]` / `input[name=username]` / `[data-testid=loginForm]` / `form[action*=login]`),返回 `isLoggedOut`。
- **校验**:两文件 `node --check` 通过。
- **提交 + push(经批准)**:commit `fdba82329`(scope `fix(app-ins-scripts)`,2 文件)。pre-commit lint(FULL TURBO)+ circular 过;pre-push 全量 test 又被无关 `business-minerva-upgrade#build` 挡 → 授权例外 `--no-verify`,已推 `5812dca80..fdba82329`(origin/personal/zz/sp-snapchat)。
- **关键坑/残留**:
  - 线上跑的是 **GraceChat 镜像 assets**,不是 sitin-next 源 → 需把两文件同步到 GraceChat `haven/src/main/assets/scripts/snapchat/bootstrap/` 并发版才生效。
  - 登出选择器需**真机核对** Snapchat 实际 DOM;不匹配会退化到 25s 窗口才报(仍安全,只是慢)。
  - 检测 WebView 只注入 `JSBridge`(无 AndroidBridge),故 `bridge.eventTrack/log` 打不出(日志 `AndroidBridge.event 未注入`),`sp_check_login_status` 埋点在该 WebView 丢失。

## 工作日志 — 19:52 修 IG toProfessionalAccount 死循环(加载页↔编辑页反复刷)

- **现象(测试反馈 + IG-Scraper console 日志)**:`toProfessionalAccount`(转专业账号)在 `/accounts/convert_to_professional_account/` 与 `/accounts/edit/` 间无限来回、整页反复刷新;日志固定 `execute recovery: 恢复执行 toProfessionalAccount` → `开始执行` → `step 0/5: 导航到转换页` → 被 IG 弹回 edit → 重载,永远停在 step 0。
- **根因(`packages/app-ins-scripts/src/instagram/actions/executeAction.js` 的 `SP.pipeline`)**:step 前 `saveProgress(i+1)` 存断点到 sessionStorage(stateKey),但 `finally` 里 `sessionStorage.removeItem(stateKey)` **无条件执行**——step 0 导航返回 `pending` 字符串 → 函数 return → **finally 把刚存的 step=1 断点删了**。于是每次页面重载后 recovery 读不到断点 → `startStep=0` → 重跑 step 0 → 又导航 → IG 又把转换页弹回 edit(该账号已是专业账号/入口不可用)→ 死循环。而 step 1「选择账号类型」里的 `isAlreadyProfessional()` 短路(转换页被弹回 edit=已专业→上报成功)因永远到不了 step 1 而失效。
- **两层 pipeline**:外层 `executeAction`(单动作 recovery,PENDING_KEY)重新调 `toProfessionalAccount()`;内层 `SP.pipeline`(stateKey `__toProfessionalAccount_pipeline`)。bug 在内层。
- **修法(主修,不加尝试上限)**:`SP.pipeline` 加 `var suspended=false`;`if (typeof stepResult==="string") { if (isPendingResult(stepResult)) suspended=true; return stepResult; }`;`finally` 改 `if (!suspended) removeItem(stateKey)`。→ pending 时保留断点,重载后从下一步续跑;弹回场景推进到 step 1 命中 isAlreadyProfessional 上报成功,循环终结。正常账号不受影响。
- **校验**:`node --check` 通过。规范模式**未提交**。分支 `personal/zz/ig-to-professional-account` @ 798cb0620。
- **残留/提醒**:
  - 线上跑 **GraceChat 镜像 assets**,此 fix 需同步到 GraceChat `haven/.../instagram/actions/executeAction.js` 并发版才生效。
  - **设计风险(本次按用户要求未处理)**:isAlreadyProfessional 把「转换页被弹回 edit」一律当已专业。若 IG 下线了直连转换 URL(个人账号也被弹回)→ 会误报 converted 成功。后续可加尝试上限 + 账号状态核验区分「已专业」vs「入口不可用」。
  - 这个 finally 删断点是**通用 bug**:任何带导航 pending 步的 SP.pipeline 都靠「每步从页面态重新判定」硬扛,断点持久化其实一直没生效;本修复让断点真正可用。

## 今日重点

- [ ] 探针自拍超时:后端补 `taskType==PROBE` 分支 + `handleOutboundMessage` 识别 `replyKind:probe`(结转自 07-30/07-31/08-02)

## 工作日志 — 11:03 GraceChat 落地 snapchat 注入脚本(跨仓 · 按 build.mjs 分组)

- **做了什么**:把 `sitin-next` 分支 `personal/zz/sp-snapchat` 的 snapchat 注入脚本(`packages/app-ins-scripts/src/snapchat`),按 `scripts/build.mjs` 的 `SC_GROUPS` 分组,落地到 `GraceChat-Earn-Android` 的 `haven/src/main/assets/scripts/snapchat/{bootstrap,login,automation}/`。从 `p/ljb/snpachat`(注意拼写)切新分支 `p/zz/snapchat-scripts` 改,commit `d2b5c421` 已 push。
- **分组(build.mjs SC_GROUPS)**:bootstrap 4(utils/bridge/checkLoginStatus/checkLoginStatusIIFE)、login 6(+executeAction/watchLoginComplete/watchLoginCompleteIIFE)、automation 16(导航/输入/发送/取消息/加好友/getStory→getFriendStory/sendStory/clickMute/startDMListener/executeAction 末)。共 +26 .js,删 3 个占位 README。
- **关键约定(沉淀)**:
  - GraceChat 存的是**原始源码**(带注释 .js,非打包产物),与 instagram 目录同构;每组文件夹=该组 build.mjs 列表的个体文件平铺(actions/methods 不分子目录,共享的 utils/bridge 每组各一份)。加载顺序由 Android 端/热更管,GraceChat 侧不 build、不需 package.json。
  - GraceChat 用**受限 fetch refspec**(只跟踪 `p/ljb/sitin4`),`p/ljb/snpachat` 要 `git fetch origin p/ljb/snpachat` 后走 **FETCH_HEAD**,没有 `origin/p/ljb/snpachat` 跟踪 ref。
  - 源分支用 `git show <ref>:<path>` 抽取,**没 checkout** sitin-next(其工作树有未提交 `proto` submodule 改动,切分支会扰动)。
- **验证**:各组文件数精确对齐(4/6/16),无空文件,全部 `node --check` 语法通过。身份 colna,SSH `github-colna`。
- **remote 提示**:GraceChat push 有「Create a pull request」提示,新分支已建 `origin/p/zz/snapchat-scripts`。

### PR [#92](https://github.com/presence-io/GraceChat-Earn-Android/pull/92) 已 merge

- **做了什么**:`p/zz/snapchat-scripts` → 合入(snapchat 注入脚本落地 haven assets,26 .js / −3 README,按 build.mjs SC_GROUPS 分组)。
- **遇到的问题 → 怎么解决**:① GraceChat 受限 refspec 只跟踪 sitin4,目标分支要 `git fetch origin p/ljb/snpachat` 走 FETCH_HEAD → 用 FETCH_HEAD 切分支;② sitin-next 工作树有未提交 proto submodule 改动,切分支会扰动 → 不 checkout,改用 `git show <ref>:<path>` 抽取源文件。
- **残留**:
  - [ ] 端上核对 `AndroidBridge.notifyPageAbnormal` 是否暴露同名 `@JavascriptInterface`(SC 现有脚本此前只用过 uploadMessages/onResult/event,见 07-31 日志);方法名不同只改 `notifyAPK` 一处。
  - [ ] 确认 Android 端加载这 3 组脚本的**顺序来源**(GraceChat 侧无 manifest,靠端/热更;executeAction 必须最后)。
  - [ ] sitin-next 那个未提交的 `proto` submodule 改动仍在(与本任务无关,待其 owner 处理)。

## 工作日志 — 11:26 sitin-next feature/pwa-ads:Partner 任务加埋点(点击 + 完成)

- **做了什么**:给 PWA「Become a partner」(Partnership Ads,task_id=2000)任务补两个 BytePlus 埋点。改 2 文件(`packages/app-pwa/`):
  1. `src/tracking/events.ts`:新增两个事件名 `pwa_earning_partner_task_click`、`pwa_earning_partner_task_complete`(命名对齐既有 INS 任务 `pwa_earning_ins_task_*`)。
  2. `src/hooks/usePartnershipAds.ts`:① `onGo` / `onReceivedClick` 各 `bpTrack(...task_click)`,带 `action: "go" | "received"` 区分两种点击;② `runCheck` 里 `ok===true`(唯一权威完成点)`bpTrack(...task_complete)`,带 `task_id` + `reward`。reward 用 `rewardRef`(仿现有 `awaitingRef`)读,避免把 `task` 塞进 `runCheck` deps 触发 visibility 订阅抖动/重复查询。
- **为什么这样放**:点击在 hook 的 onGo/onReceivedClick 而非卡片,保持 `PartnershipAdsCard` 为纯视图;完成放 `runCheck` 的 ok 分支,useLockFn 在途锁 + 完成后卡片自隐(`visible=false`)保证只报一次。注:`finishTask` 内部对所有 common task 已报通用 `pwa_task_complete`(带 task_id),这里另加 partner 专属事件供漏斗对照。
- **工作环境坑**:feature/pwa-ads 被另一会话的 git worktree(`scratchpad/wt-ads`)独占,主仓库无法再 checkout;直接在该 worktree 干活并 ff 到 origin(852a9de72)。worktree 无 node_modules,临时软链主仓库 `node_modules` 做校验、校验后删除软链。
- **校验**:eslint 0、`tsc -b`(用项目 pin 的 5.6.3,非 root 的 6.0.2——后者会误报 baseUrl 弃用)0。
- **事件名口径**:漏斗取数认内容/事件名,记牢 `pwa_earning_partner_task_click`(params.action=go/received)/ `pwa_earning_partner_task_complete`(params.task_id=2000, reward)。
- **提交 + PR**:经用户授权 `git push`,commit `5455b1483`(colna 身份)推到 `feature/pwa-ads`。该分支**已有 open PR #760**「feat(app-pwa): 新增 Partnership Ads 任务显示与点击跳转 IG」,base 是 **`feature/pwa`(不是 main)**,url https://github.com/presence-io/sitin-next/pull/760 —— 埋点 commit 已随 head 分支进入这条 PR,无需另开 PR。

## 工作日志 — 13:54 sitin-next snapchat 分支:bridge.js 不再覆盖 window.JSBridge

- **做了什么**:sitin-next 分支 `personal/zz/sp-snapchat`,`packages/app-ins-scripts/src/snapchat/actions/bridge.js` 第 439–440 行「全局暴露」段,把
  `window.JSBridge = window.SocialProxyBridge;` + `window.JSBridge.response = _onResponse;` **两行注释掉**(英文注释说明原因)。
- **为什么**:`window.JSBridge` 端上/其它脚本另有单独定义,这里再赋值会覆盖掉它。改动对齐 GraceChat 侧同款(用户给的 diff 图 fc88d61c→ca9fe3f9 就是把这两行注释)。
- **约定沉淀**:sitin-next 该仓 `CLAUDE.md` 硬规则——① 代码/注释**只用英文**;② 改文件前先列改动待用户确认(bot 环境不阻塞,用户已明确指令即算确认);③ **禁自动 push**;④ commit 必带 scope。
- **校验**:`node --check` 通过(注入脚本纯 JS)。diff 只动 bridge.js;`proto` submodule 的 `M` 是无关既有 WIP,未碰。**规范模式未提交**。

## 工作日志 — 14:07 snapchat 上报通道核对(端 JSBridge)+ checkLoginStatusIIFE 改用 JSBridge

- **背景**:上一步注释掉 bridge.js 的 `window.JSBridge = SocialProxyBridge` 后,担心 `notifyPageAbnormal` 上报断掉,去 `GraceChat-Earn-Android` 分支 `p/ljb/snpachat`(仓库拼写 snpachat)核对端定义。
- **端架构结论(关键沉淀)**:
  - `notifyPageAbnormal` 原生实现 + 注入在 `PWAWebViewFragment.kt:1024-1047`——端为「social-proxy 异常检测 WebView」`addJavascriptInterface(object{@JavascriptInterface notifyPageAbnormal}, "JSBridge")`。**checkLoginStatusIIFE.js 就跑在这个检测 WebView 里**,所以 `window.JSBridge.notifyPageAbnormal` 是真·原生能力,端拿到后据 type 定向关会话。
  - SocialProxy 抓取 WebView(`WebViewManager.kt:59`)只注入 `"AndroidBridge"`,而 `AndroidBridge.kt` 的 @JavascriptInterface **没有 notifyPageAbnormal**(只有 onResult/scriptEvent/log/request/event/finish/changeStepText/requestHttp/getLastMessageId/uploadMessages/isFullSyncDone/setFullSyncDone)。
  - 所以端团队 GraceChat 最新(ca9fe3f9)两步配套:① bridge.js 注释掉 JSBridge 覆盖(否则覆盖端原生 JSBridge);② checkLoginStatusIIFE 改用 `window.JSBridge.notifyPageAbnormal`。GraceChat 那版头注释「只挂 AndroidBridge」是过时残留,与其代码矛盾。
- **改了什么**:sitin-next `snapchat/actions/checkLoginStatusIIFE.js`:notifyAPK 由 `window.AndroidBridge.notifyPageAbnormal` → `window.JSBridge.notifyPageAbnormal`(timestamp 也对齐 `new Date().getTime()`),并把过时头注释改对。→ 与 bridge.js 改动闭环,否则光去覆盖会让上报走进 `else` 打 warn、异常态永远漏报。
- **未动**:`watchLoginCompleteIIFE.js`(不直接调 notifyPageAbnormal,走 executeAction 调度)。
- **校验**:`node --check` 通过。本分支现共 2 个改动文件(bridge.js + checkLoginStatusIIFE.js)。
- **提交 + push(14:27)**:经用户授权提交 `5812dca80`(colna,scope `fix(app-ins-scripts)`,只含这 2 文件,proto 未 staged)。pre-commit(lint 51/51 + circular 无违规)全过。**pre-push 全量 test 失败但全在无关包**(`business-minerva-upgrade#build` 私有 registry `nexus.sitinai.com` 拉不到 → business-minerva-* / app-social-proxy-server / business-pwa-proto 连坐 ELIFECYCLE),我的 app-ins-scripts 不在失败列表 → 经用户逐次授权 `git push --no-verify` 跳过 pre-push,已推 `25388c814..5812dca80`。(此即 CLAUDE 规则里「sitin-next 被无关包全量 hook 拖累」的授权例外)

## 工作日志 — 11:51 整理 contact-exchange-service PR #104 为接口文档,追加到 SP V2 RFC

- **做了什么**:读 `contact-exchange-service` PR [#104](https://github.com/presence-io/contact-exchange-service/pull/104)(`personal/dzh/spv2 → release/test`,13 文件 +214/−48,CE 服务端 IG→IG+Snapchat 多平台)全 diff,整理成「第四部分 · 服务端 CE 接口变更」append 到飞书 wiki V33Zwbua(SP V2 RFC),revision 96。
- **接口变更要点(沉淀)**:
  - `checkFreeExchangeCondition` 响应 +`platform_online` map<平台,bool>(遍历 `ce.card.supported-types` 逐个查 `proxy:{platform}_online:$userId`);`is_ig_online` 保留兼容。**坑**:map key 来自 supported-types(IG/SNAPCHAT 大写),但服务端判 ig 用小写 `"ig"` → 前端读 platform_online 要大小写容错。
  - `checkBlurredCardCondition` 响应 +`card_type`(string,eligible 时给「最优社媒平台」);选平台 `selectBestPlatform`:在线>离线、同组 IG>其他。
  - `createBlurredCardOrder` 请求 +`card_type`(CardType 枚举,客户端传要交换平台;原写死 IG)。
  - 名片头像:IG 走 user-service GCS;非 IG 走新增 `AvatarStorageService`→阿里云 OSS(下载外链→传 OSS→回 URL,失败回退原始 URL)。
  - `CeExchangedEvent` 去掉 `userIgHandle/creatorIgHandle`,`platform` 取 `order.cardType`,social-proxy 自查 `user_contact_card`。
  - 埋点 +`platform` 维度;`saveInsChatHistory/getInsChatHistory` @Deprecated;新增 `ce.oss.*` 配置(仅 aliyun 构建)。
- **仍需确认**:`listUserInsExchangeOrder` 返回带 cardType(FE-1.3 金额分组 + FE-2.2 强制任务分派)本 PR 未涉及,订单表已有 cardType 字段,接口是否透出待核。
- **命令**:`gh`(全路径 `/opt/homebrew/bin/gh`,不在 PATH)`pr diff 104`;`lark-cli docs +update --command append`。

## 工作日志 — 12:06 sitin4.1 SP V2 Phase 0 基础层(FE-0.1~0.4)

- **做了什么**:在 sitin-next2 从 `origin/feature/pwa` 切分支 `personal/zz/sitin4.1`,实现 SP V2 前端 Phase 0(app-pwa,3 文件 +357/−152):
  - **FE-0.1 类型/配置表**(`utils/bridge.ts`):新增 `SocialPlatform`("instagram"|"snapchat")+ `CardType` 数值 enum(对齐 proto 1=IG/2=SNAPCHAT,proto 子模块本仓未生成故本地固化)+ `SOCIAL_CONFIG` 配置表(url/loginPath/loginPathPatterns/webViewConfig)+ helper(normalizeSocialPlatform / cardTypeToPlatform / platformToCardType)+ `PLATFORM_PRIORITY`/`DEFAULT_PLATFORM`。旧 `INS_*` 常量改为由 SOCIAL_CONFIG.instagram 派生(兼容)。
  - **FE-0.3 Bridge 透传**(`utils/bridge.ts`):openSocialProxyWebView / startSocialProxyRobotWebView / checkSocialProxyPageAbnormal 三个方法加**可选 platform 参数默认 instagram**,内部用 SOCIAL_CONFIG[platform]。InsRobotUserInfo.insId/insAvatar 字段名暂不改名(加注释说明语义已泛化),彻底改名留 follow-up。
  - **FE-0.2 insStore 多社媒化**(`stores/insStore.ts`):加 `authByPlatform`/`loggedInByPlatform`/`abnormalByPlatform` map + platform 感知 setter(setPlatformAuth/LoggedIn/Abnormal/resetPlatform)+ selector(selectAuthorizedPlatforms / selectPrimaryPlatform IG>SP)。旧 IG 字段(insState 等)与 map 的 instagram 项**始终同步**;persist 加 `merge` 迁移(旧版只存 insState 时回填到 map)。
  - **FE-0.4 授权回调**(`hooks/useInsBridgeCallbacks.ts`):finishPWAInsTask 加**可选 platform 第 4 参**(默认 instagram,兼容旧 APK),据此 setPlatformAuth/Abnormal 写回对应社媒;IG 成功后账号保存+小崽拉起抽成 finishInstagramBind(仅 IG,非 IG 名片/机器人留 FE-3.x);handleOpenSocialLogin 补 snapchat 分支(写回 SC 态,登录弹窗待 FE-1.2)。
- **设计原则**:**全程向后兼容**——旧常量/字段/方法签名全保留(可选参数 + 兼容字段),~10 个消费方(useInsTaskInit / AiGoldCashoutCard / showInsModal / InsExchangeBubble …)零改动;UI 迁移到 map/selector 留 Phase 1~3。
- **验证**:eslint 0 error(3 文件 + 全 app-pwa);tsc -b 仅 1 个**预存**错误(`chatApi.ts:269` proto 类型债,stash 后仍在,与本次无关),我的改动 0 新增。
- **状态**:分支 `personal/zz/sitin4.1`(sitin-next2,身份 colna),**未 commit/未 push**,等用户批准。工作在 sitin-next2 是因主树 sitin-next 脏(proto submodule)+ 有并行 agent worktree,避开。

- **push + PR**(用户批准):commit `39dbdb71f` push `origin/personal/zz/sitin4.1`,开 **PR [#824](https://github.com/presence-io/sitin-next/pull/824)**(base=`feature/pwa`)。**踩坑**:sitin-next2 有 husky commitlint,`subject-case` 规则禁止 subject 大写/首字母大写开头 → `feat(app-pwa): SP V2 Phase...` 被拦(commit-msg 失败即提交未成,但那步之后的 `git push -u` 先把空分支=feature/pwa 基线推了上去);subject 改小写开头 `sp v2 phase0 …` 通过。教训:sitin-next2 commit subject 必须小写开头。

## 工作日志 — 按 Native bridge 文档修正 PR #824(app-pwa bridge 配置)

- **依据**:飞书文档「Social Proxy Native Bridge Web 接入说明」 https://presence.feishu.cn/docx/V7b0dNyuCoO4rHxmUhEcyh7Nnmd(Native 端 `PwaNativeInterface` 的 web 接入契约)。
- **发现的3处 PR 与契约的偏差(都会让 Snapchat 实机跑不起来)**:
  1. `SocialConfig.webViewConfig` 只有 `{domStorage,thirdPartyCookies,loadsImages}` 三字段;契约白名单更大,且 **Snapchat 必须传桌面 Mac UA + viewport 1440×900**,否则页面按 Android 渲染、拿不到桌面 DOM。
  2. URL 模型是单一 `url`+`loginPath`;但 **Snapchat 每个动作 URL 不同**:运营=`https://www.snapchat.com/web`、检测=`https://accounts.snapchat.com/v2/welcome`、登录=`https://accounts.snapchat.com/v2/login`。单 url 表达不了。
  3. Snapchat `loginPathPatterns` 错(原 `["/accounts/login","/v2/welcome"]`),契约是 `["/v2/login"]`。
- **改动**(`packages/app-pwa/src/utils/bridge.ts`,仅 1 文件 +88/−25):
  - 新增 `SocialWebViewConfig` 白名单接口(userAgent/acceptLanguage/useWideViewPort/loadWithOverviewMode/domStorageEnabled/databaseEnabled/thirdPartyCookiesEnabled/loadsImagesAutomatically/mediaPlaybackRequiresUserGesture/viewportWidthPx/viewportHeightPx/headers,全可选,与契约「WebView 配置字段」逐字对齐)。
  - `SocialConfig` 把 `url`+`loginPath` 拆成 **`robotUrl`/`checkUrl`/`loginUrl` 三个按动作的完整 URL**;webViewConfig 类型换成 SocialWebViewConfig。
  - 抽 `INSTAGRAM_WEBVIEW_CONFIG`/`SNAPCHAT_WEBVIEW_CONFIG`(取自文档「推荐参数示例」),Snapchat 带桌面 UA+viewport。
  - `SOCIAL_CONFIG` 两平台按契约填三 URL / loginPathPatterns / webView。
  - 三方法改用对应 URL:openSocialProxyWebView→`cfg.loginUrl`、startSocialProxyRobotWebView→`cfg.robotUrl`、checkSocialProxyPageAbnormal→`cfg.checkUrl`。
  - `INS_URL` 别名由 `robotUrl` 派生(消费方只有 useInsTaskInit 用 INS_PLATFORM,其余别名无外部消费,安全重构)。
- **验证**:eslint 3 文件 0 error;tsc 我改的 bridge.ts 0 error(全库 232 错全是 proto submodule 未生成 `@heyhru/business-pwa-proto/gen`,本地环境问题,与本次无关)。
- **状态**:分支 `personal/zz/sitin4.1`(工作在主树 sitin-next),**未 commit/未 push**,等用户批准。

## 工作日志 — 16:19 sitin4.1 Phase1 起步:FE-1.2 登录弹窗平台化 + dev 弹窗预览页

- **做了什么**(sitin-next2 分支 `personal/zz/sitin4.1`,app-pwa):
  - **FE-1.2**:`components/showInsModal.tsx` 平台化——加 `PLATFORM_TEXT` 配置(IG/Snapchat 的 taskTitle/assistantLabel/primaryButton);`InsModalContent` 收 `platform`;`openSocialProxyWebView(platform)`;`showInsModal(source, opts)` 第 2 参改 opts 对象(`platform`/`onClose`/`force`),6 个调用点只传 source→零破坏;`force` 跳过非 APK 的下载框(dev 预览用)。SC 图暂复用 IG 素材占位(TODO)。
  - **新增 dev 页** `pages/dev/Spv2ModalsPreview/index.tsx` + 路由 `/dev/spv2`(照 `pages/dev/*` 先例 lazy import)。按钮:IG 登录弹窗 / Snapchat 登录弹窗(force:true 浏览器可弹)/ 交换抽屉(mock 一条)。
- **为什么**:用户要「开始实现 + dev 页弹这些弹窗」;登录弹窗默认只在 APK 弹,dev 页靠 `force` 在浏览器看真弹窗。
- **验证**:eslint 0(改动文件 + 全 app-pwa);tsc -b 仅预存 `chatApi.ts:269` proto 债(与本次无关)。
- **约定坑**:sitin-next2 UI 文案是产品英文(app-pwa 全英文),CLAUDE.md「UI 文案中文」不适用于面向英文用户的 app-pwa;沿用现有英文文案 + 中文注释。commit subject 须小写开头(commitlint)。
- **状态**:未 commit(规范模式等批准);在 PR #824 同分支上继续。

## 工作日志 — 16:41 sitin4.1 FE-1.2 登录弹窗严格对齐 Figma

- **背景**:用户指出 SC 登录弹窗样式不对(显示的是 IG logo + IG 手机图),要求严格照 Figma(节点 6453:2956 IG / 6453:3045 SC)。
- **做了什么**(sitin-next2 `personal/zz/sitin4.1`,commit `448e34c09` 已 push):
  - 从 Figma 下真 SC 素材到 `app-pwa/src/assets/images/social/`:`ic_logo_snapchat.svg`(46×46 黄底鬼)+ `bg_snapchat_login.png`(342×300 SC 登录 mockup,3x)。IG 复用现有 `icLogoInsRect`/`bgInsLogin`。
  - `showInsModal` 登录视图整体重写,按 get_figma_data 的精确值(颜色/字号/间距):头部 TASK+标题、In Progress 独立行(#2F80ED)、奖励卡 #E7F0FB(reward 按平台 $3.00/$0.50)、tabs 蓝下划线 #114CBF、mockup 按平台切换、revoke 说明、底部按钮 #114CBF 带 logo。修 Estiamted→Estimated。改 `PLATFORM_TEXT`→`PLATFORM_MODAL`(logo/mockup/title/reward/assistantLabel 配置驱动)。移除已不用的 icLogoIns/DragHandle import。
- **踩坑**:figma download_figma_images 的 image dir 是**工作区根** `/Users/max/Dev2/zhangzheng`,不是 sitin-next2 → 素材落到根的 stray `packages/`,得 mv 进 sitin-next2 再删 stray。
- **沉淀**:Figma 弹窗手机 mockup 是矢量拼的(非位图),但直接把 mockup frame 渲染成 PNG 当图用更省事;logo 是 SVG。get_figma_data 拿全部颜色/字号/坐标,照抄即像素级还原。
- **验证**:eslint 0、tsc -b 仅预存 chatApi proto 债。**未实机截图**,待用户 /dev/spv2 点验。

## 工作日志 — 16:55 sitin4.1 FE-1.3 授权抽屉(Phase1 进度 2/4)

- **做了什么**(sitin-next2 `personal/zz/sitin4.1`,commit `5e285ba05` push):新增 `components/showSocialAuthDrawer.tsx`——按 Figma 6453:2524 还原「Authorize accounts」多社媒授权抽屉。expired 态=红边卡(#F6C9C2)+ 过期徽章 + 红 Reconnect 钮(#E14B3B);not_connected 态=灰底(#F7F9FC)+ 灰点 + 蓝 Authorize›(#EAF1FF/#2F73F5)。Fraunces 标题、Maybe later。导出 `showSocialAuthDrawer(rows)`(显式态)+ `showSocialAuthDrawerFromStore()`(insStore 派生)。/dev/spv2 加三态按钮。
- **踩坑**:`EventName.xxx ?? yyy` 对 TS enum 不成立(不存在成员=编译错),要先 grep events.ts 确认名。图标(refresh/chevron/过期钟)用内联 SVG。
- **Phase1 进度**:FE-1.2 登录弹窗 ✅、FE-1.3 抽屉 ✅;剩 FE-1.1 授权卡 + FE-1.4 单登录态卡(都在 AiGoldCashoutCard.tsx)。
- **验证**:eslint 0、tsc 仅预存 proto 债。

## 工作日志 — 17:02 sitin4.1 Phase 1 授权登录 UI 全完成(FE-1.1/1.4)

- **做了什么**(sitin-next2 `personal/zz/sitin4.1`,commit `c3a964ba2` push):完成 Phase1 最后两个,`pages/Home/AiGoldCashoutCard.tsx`:
  - **FE-1.1**:`AuthorizeInsCard`→`AuthorizeSocialCard({platform})`,按 IG/Snapchat 动态文案/图标(SOCIAL_LOGO/SOCIAL_SHORT_NAME)/跳转(showInsModal(src,{platform}));链路 needsInsAuth→AuthorizeSocialCard。
  - **FE-1.4**(Figma 6453:1931):`PausedCard` 加 `authorizeOther?` 变体——单登录态右上半透明「Authorize {另一社媒}」胶囊(logo+chevron)+ 底部小字「Authorize more socials to unlock more social-exchange orders ~」;兜底 `<PausedCard authorizeOther={scAuthed?undefined:"snapchat"}/>`。
  - 导出两卡 + /dev/spv2 内联预览。
- **判据**:IG 授权态沿用现有 `insAuthorized`(BindInsAccount 任务 + userInfo.insId);SC 授权态用 `insStore.authByPlatform.snapchat`。单登录=IG 授权 + SC 未授权(SC-only 边界落回 needsInsAuth 授权 IG,可接受)。
- **Phase 1 收官**:FE-1.1 授权卡 ✅ / FE-1.2 登录弹窗 ✅ / FE-1.3 授权抽屉 ✅ / FE-1.4 单登录态卡 ✅。全部 eslint 0、tsc 仅预存 proto 债。dev 页 /dev/spv2 可点验全部。
- **PR #824** 现含:Phase0 基础层 + snapchat url 对齐(并行) + Phase1 全部 UI。

## 工作日志 — 17:07 sitin4.1 授权抽屉补奖励标注 + 只ins过期样式

- **做了什么**(commit `a0ba05366` push):对齐 Figma `6453:3088`,`showSocialAuthDrawer` 的 `SocialAuthRow` 加 `reward?` 字段——按钮下方金额:expired→绿「Claimable $X」(#00C950)、not_connected→灰「Rewards $X」(#8A94A6)。/dev/spv2 加带奖励三态 + 「只 IG 过期(单行 Claimable $6)」预览(用户指出缺这个样式)。
- **理解**:「只 ins 过期」= 单登录只授权过 IG 且过期 → 抽屉单行(RFC需求3「只展示丢失那个社媒」)。奖励标注是 RFC需求3① 均过期→Claimable 领取奖励 的可视化,之前 FE-1.3 初版漏了。
- 验证 eslint 0、tsc 仅预存 proto 债。Phase1 至此含奖励标注全套。

## 工作日志 — 修复 Snapchat「首次检测登录态必失败」

- **问题**:PWA 检测 Snapchat 登录态,第一次一定失败(误报未登录),再调一次就好。日志:`checkLoginStatus` 挂载后 IIFE 3 次短轮询(2s 窗口)全无资料卡片 → `notifyPageAbnormal type=1`;期间出现 `Abort fetching component for route: "/v2/welcome"`。
- **根因**:`accounts.snapchat.com/v2/welcome` 是纯 CSR 的 Next.js,已登录用户的资料卡片(`[data-testid="username"]`)要等 hydration 才渲染(≥2.5s),但检测脚本 IIFE 用固定「3 次×1s 短轮询,窗口耗尽即判 type=1」,把「还没渲染」误当未登录。IG 没事是因为 IG 页偏 SSR、DOM 快;两者复用了同一套等待参数。
- **对比分支**:`p/ljb/snpachat`(ljb 本人拼错 snapchat)只改了上报通道(撤 `bridge.js` 的 `window.JSBridge=SocialProxyBridge` 覆盖 + notifyAPK 走原生 JSBridge),**没动检测逻辑**,所以初次失败在 ljb 分支也没修。检测 invisible WebView 里原生 `notifyPageAbnormal` 以 "JSBridge" 名注入(`PWAWebViewFragment.kt:1047`),且收到 type≠0 会 `SocialProxyFloatingView.finish(platform)`(误报有实害);native 检测超时=前端传的 timeoutMs(默认 30s),超时兜底 type=-1。
- **改动 A — Android `GraceChat-Earn-Android` 分支 `p/zz/snapchat-scripts`**(3 文件,`haven/.../assets/scripts/snapchat/bootstrap/`):
  - `checkLoginStatus.js`:新增 `checkLoginPage()`(未登录正信号:path 含 `/accounts/login`|`/v2/login`,或有 `input[type=password]`);返回值加 `certain`(已登录恒 true;未登录仅命中登录页才 true,否则 false=可能还在 hydration)。
  - `checkLoginStatusIIFE.js`:`detect` 从「固定 3 次即判未登录」改为「按时间软超时轮询等确定态」(`SOFT_TIMEOUT_MS=20000` < native 30s,`POLL=500`):命中 type=0 或(type=1 && certain)立即上报,否则一直等,软超时才兜底 type=1;notifyAPK 改走原生 `window.JSBridge.notifyPageAbnormal`(合入 ljb)。
  - `bridge.js`:注释 `window.JSBridge = window.SocialProxyBridge` + `.response=_onResponse`(合入 ljb,不覆盖原生 JSBridge)。
- **改动 B — sitin-next 分支 `personal/zz/sp-snapchat`**(1 文件):`useInsTaskInit.ts` `checkInsAbnormal` 单发无重试 → 首检非正常时延迟 1.5s 重试一次,两次都异常才判异常(把手动第二次自动化;对 IG 冷启动误报也有价值)。**注**:该前端分支目前还没有 snapchat 检测入口(bridge.ts 仍旧单 IG 版、无 SOCIAL_CONFIG),日志里的 snapchat 检测是走 Android debug/手动触发,所以根治全在 Android 脚本,前端只是通用兜底。
- **验证**:Android 3 文件 `node --check` 通过;前端 eslint 0 error。**未 commit/未 push**(规范模式)。
- **坑**:`p/ljb/snpachat` 分支名是 ljb 把 snapchat 拼成 snpachat,本地要 `git fetch` 才有;`personal/zz/sp-snapchat`(前端)与 `personal/zz/sitin4.1`(PR #824,含 SOCIAL_CONFIG 多社媒重构)是两条不同的线,别混。

## 工作日志 — 18:30 sitin4.1 proto regen 解除 Phase3 阻塞 + FE-3.1 发卡 cardType

- **背景**:按 PR #104 做 Phase3,发现前端 `business-pwa-proto` gen 无 PR#104 字段(CreateBlurredCardOrderRequest.cardType / CheckBlurredCardConditionResponse.card_type / 条件响应 platform_online),proto submodule 未初始化,阻塞 FE-3.3/2.2。
- **解除阻塞(用户令)**:`git submodule update --init packages/business-pwa-proto/proto`(clone presence-io/proto)→ proto 切 `origin/release/test`(`19259794`,确认三字段都在)→ `PATH=/opt/homebrew/bin:$PATH bash scripts/generate.sh`(protoc 35.1 未进 PATH,手动加)→ `pnpm build`。regen 22 个 proto、948 映射,改 24 文件(proto 指针 + 23 gen)。**app-pwa tsc 现完全干净,连预存 chatApi.ts:269 proto 债一并消失**。
- **FE-3.1 完成**:`useInsTaskInit` 发卡 TIM payload `cardType` 从写死 1 → `platformToCardType(selectPrimaryPlatform())`(IG>SP,兜底 IG)。eslint/tsc 过。
- **关键沉淀**:proto regen 流程 = init submodule → `pnpm --filter @heyhru/business-pwa-proto proto:test`(fetch+checkout release/test+generate+build);protoc 在 /opt/homebrew/bin 需手动进 PATH;gen 是 **tracked**(入库),eslint 忽略 src/gen。TIM payload cardType 是客户端类型(非 proto),FE-3.1/3.2 不依赖 proto;FE-3.3/2.2 依赖(现已通)。
- **状态**:proto regen + FE-3.1 均**未 commit**,等用户定提交顺序。

## 工作日志 — 19:08 sitin4.1 FE-3.1/3.2/3.3 CE 多平台 cardType(proto 已解阻塞)

- **做了什么**(两个 commit push:proto `061e21289` + 业务 `51b42fb76`):
  - **proto regen**(见上一条):解除阻塞。
  - **FE-3.3**:`insApi.createBlurredCardOrder(maleUserId, cardType?)` 补 cardType 入参(cast 成 proto CardType)。
  - **FE-3.1**:发卡平台优先用服务端 `checkBlurredCardCondition.card_type`(最优,normalizeSocialPlatform 转),兜底 `selectPrimaryPlatform`;createBlurredCardOrder + TIM payload 同一 cardType。
  - **FE-3.2**:`InsExchangeBubble` 按 `message.payloadData.cardType` 渲染平台名(platformDisplayName → Instagram/Snapchat),替换硬编码。
- **踩坑**:**本地 CardType(bridge)与 proto CardType 是两个 nominal enum**(值同 IG=1/SNAP=2 但类型不同)→ createBlurredCardOrder 参数改收 `number`、body 内 `as CardType` cast。
- **进度**:Phase3 FE-3.1/3.2/3.3 ✅;剩 FE-2.1 一次性任务、FE-2.2 强制交换(已解阻塞)、FE-3.4 机器人调度按社媒。
- 验证 eslint 0、app-pwa tsc 完全干净。

## 工作日志 — 19:15 sitin4.1 FE-3.4 机器人调度按社媒(commit 8c7796d88)

- **做了什么**:`InsExchangePendingMessage` 加 cardType;交换气泡接受时透传 `message.payloadData.cardType`;`startRobot` 按 msg.cardType → cardTypeToPlatform 拉对应平台小崽(startSocialProxyRobotWebView 透传 platform),置 per-platform 登录/异常态(IG 经 Phase0 兼容层同步旧字段);`tryStartInsRobot(platform)`/`handleAcceptExchange` 按订单社媒判定授权+登录态。
- **收尾状态**:Phase0 ✅ / Phase1 ✅(FE-1.3 UI 完成待接生产) / Phase3 FE-3.1/3.2/3.3/3.4 ✅。剩:**FE-2.2**(强制交换卡,前端可做但需新增阻塞卡进 AiGoldCashoutCard 渲染链,较大)、**FE-2.1**(卡后端:Snapchat 绑定任务 id + Social Connect 取数)。
- 验证 eslint 0、app-pwa tsc 完全干净。

## 工作日志 — 19:40 sitin4.1 工作台授权卡显示未授权社媒(commit 796cc0bcf)

- **背景**:用户实机看到 Win Free Cash 卡只显示「Authorize IG」,问为什么没 Snapchat。根因:FE-1.1 的 AuthorizeSocialCard 之前渲染链写死传 instagram。
- **做了什么**:AuthorizeSocialCard 由单 `platform` 改收 `platforms: SocialPlatform[]`,渲染多按钮。链路 needsInsAuth 时按未授权计算:`scAuthed ? ["instagram"] : ["instagram","snapchat"]`(IG 必需;SC 未授权则一并出双按钮,对齐 Figma 6453:2899 空态)。/dev/spv2 预览同步更新为 platforms。
- **约定**:Welcome Checklist(OnboardingTaskList 的 BindInsAccount)不动;Snapchat 那条任务=FE-2.1 仍卡后端(需 Snapchat task id)。
- 验证 eslint 0、tsc 干净。

## 工作日志 — 19:52 sitin4.1 工作台授权态分流对齐设计(commit b855efd56)

- **需求(用户给图)**:Win Free Cash 卡 —— 两个都未授权=双授权按钮(右侧);只授权一个(单登录)=「AI earnings paused」暂停卡 + 授权另一社媒胶囊(左侧,即 FE-1.4 PausedCard 变体)。
- **做了什么**:AiGoldCashoutCard 链路分流:`needsInsAuth` 时——scAuthed(仅 SC 单登录)→ `PausedCard(authorizeOther="instagram")`;否则(都未授权)→ `AuthorizeSocialCard(["instagram","snapchat"])` 双按钮。兜底(IG 已授权)不变:!scAuthed → PausedCard(authorizeOther=snapchat)。
- **修的缺口**:上一版「只授权 SC、IG 未授权」错走 AuthorizeSocialCard(IG 按钮),应走左侧 PausedCard。
- **最终态机**:00→双按钮授权卡;01/10→单登录暂停卡+另一社媒胶囊;11→正常。eslint 0、tsc 干净。
