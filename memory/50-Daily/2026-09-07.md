---
title: 2026-09-07
date: 2026-09-07
tags: daily
---

# 2026-09-07

## 今日重点

- [ ] koda chat 列表官方消息绝对时间(R00265)修复出包实机验(纯 JS，随下个包)

## 工作日志

### 03:40 — koda 注册流程两权限现象排查：定位页不弹 + 通知启动就弹（结论=旧包，未改码）

- **问题**：用户报 Android 实机两现象——①注册到地理位置页，预期直接弹权限弹窗，实际没弹；②通知权限在刚打开 app 启动页就弹，预期第一次进 chat 列表才弹。用户确认平台 Android、包「旧包/不确定」。
- **排查结论：两个预期行为代码里都已实现，且都是近期才加的，Android 旧包不含**。
  - ①定位：`onboarding-location-picker.tsx` 有 mount `autoLocate`（`ensureLocationPermission` → `requestForegroundPermissionsAsync` 弹窗，store `location` 初值 null 会触发）。该逻辑是 **PR #376 `a49d92a9`（2026-09-04）** 才加的。9-4 前的包没有它 → 进页不弹、只能手输 ZIP。
  - ②通知：koda **不用** device-push（那是 iris），只用 TIMPush `registerTimOfflinePush`，仅 `(tabs)/chat.tsx` 的 `useFocusEffect`（status==="ready"）触发；默认落地 tab 是 `index` 不是 chat。原生侧 `rn-tim-push/plugins/timpush.js` 的 `wireTencentPushApplication` 往 MainApplication.kt 的 `attachBaseContext` 注入 `TIMPushConfig.getInstance().setAutoRegisterPush(false)` + `enableRequestPostNotificationPermission(false)`（挡 TIMPush ContentProvider 在 RN 前的 Android 13 自动请求）。JS 延迟逻辑 `efe96f56`「defer notification prompt to opening Chats」是 **2026-08-19** 加的。8-19 前的包是老逻辑（注册期就请求）。
- **一度以为的真 bug（已证伪）**：timpush.js 第 313 行 `if (businessIdAndroid) { withAndroidConfig(...) }`——Android 全部 disable 注入被 FCM businessId gate 住。但 koda `eas.json` 三个 profile 都配了 `EXPO_PUBLIC_TIM_PUSH_BUSINESS_ID_ANDROID=46740`，gate 通过、注入会执行。**证伪，配置无缺失。**（潜在加固点：若某 app 没配 Android FCM businessId，disable 注入会整段跳过 → 那个 app 启动会弹 Android 通知权限。koda 不受影响。）
- **给用户的路径**：基于最新 `feature/koda-android` 出 Android EAS 包（9-4 之后），两现象应消失；两者均原生层改动、**不能 OTA**。新包仍复现再要启动阶段 TIMPush 实机日志深挖。
- **未改代码**（规范模式；代码已是预期实现）。
- **可复用经验**：实机权限/推送类「现象与预期不符」，先查对应「预期修复」的 commit 进代码日期 vs 用户包构建时间——原生改动只能新包生效，`git log --format="%ci" -- <文件>` 拿日期锚点最快定「是不是旧包」。

### 02:55 — koda chat 列表官方消息时间「几分钟前」修复：R00265 被覆盖后重做（fix/koda-chat-official-time）

- **任务**：sitin-rn2 从 `feature/koda-android` 切分支，修「注册后 chat 列表官方消息时间应显示具体 HH:MM，不是几分钟前」。用户问「之前是不是改过」。
- **关键发现（之前改过，但改动丢了）**：这就是需求 **R00265**，PR **#374**（squash commit `206ef37f`，2026-09-03）改过——`buildChatListPresentation` 加 `official` 参数走绝对 `HH:MM`（隔日带 M/D），`ConversationRow` 传入。**但当前 `feature/koda-android` 上该逻辑不存在**：`apps/koda/src/lib/chat-list-presentation.ts` 官方消息仍走 `formatRelativeTime`。核查发现 `206ef37f` 之后的 commit **`9eb5b9a4 fix(koda): chat`（09-03 17:30）覆盖了这个文件**，把 `official`/`formatAbsoluteTime` 冲掉了（`git log -- <file>` 里已看不到 206ef37f）。残留状态：`conversation-row.tsx` 第 57 行 `isOfficialConversation` 还在算 `official` 布尔，但第 59 行调用没传给 `buildChatListPresentation`。
- **修法**：照 `206ef37f` 已过 review 的实现恢复两处——① `chat-list-presentation.ts` 加回 `formatAbsoluteTime` + `official=false` 参数 + `timeLabel` 三元切换；② `conversation-row.tsx` 第 59 行调用补 `official` 实参。测试补 2 个回归用例（当天 `/^\d{2}:\d{2}$/`、隔日 `/^\d{1,2}\/\d{1,2} \d{2}:\d{2}$/`，并对照 official=false 仍 "30m"）。
- **验证**：`pnpm --filter koda test chat-list-presentation` 14 passed；lint（`lint:fix` 修了 1 条行宽 format，剩 2 条 lumi 既有 warning）；koda typecheck 过；boundaries OK。
- **pre-existing 失败（与本次无关）**：完整 `koda test` 有 1 failed `config.test.ts > keeps ... scoped to Koda`——`config.extra.appName` 现为 `koda_android`、测试期望 `koda`。`git stash` 后在 base 上复现，确认是分支自带、非本次引入。
- **可复用经验**：长命集成分支 `feature/koda-android` 上，**已合并的 PR 改动可能被后续 commit 静默覆盖**。判「某需求是否还在代码里」不能只看 `git log --grep` 命中 squash commit，要看 `git log -- <具体文件>` 该文件的 blame 历史里还有没有那条 commit——命中 commit 但 blame 无它 = 被后续覆盖。
- **已 commit + push**（用户指示）：commit `9b8f450e`，分支 `fix/koda-chat-official-time` → origin，pre-push（boundaries + 全仓 typecheck）全过。PR 未开，待用户定合入目标（AGENTS.md：唯一合入 `main`，不合 `feature/koda-android`）。
