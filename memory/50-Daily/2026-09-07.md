---
title: 2026-09-07
date: 2026-09-07
tags: daily
---

# 2026-09-07

## 今日重点

- [ ] koda chat 列表官方消息绝对时间(R00265)修复出包实机验(纯 JS，随下个包)

## 工作日志

### 22:29 — PR #410 开白已 merge(ed897fbe)+ 对 iris 逐项审计无遗漏

- **做了什么**:按用户要求「参考 feature/iris-lumi-koda-android-apk-main 的 iris 开白,查 #410 有没有遗漏」→ 枚举 iris 全部接缝引用逐项对账,确认无功能性遗漏后 `gh pr merge 410 --squash` → `ed897fbe` 进 feature/koda-android。
- **审计结论(iris → koda 全覆盖)**:接缝2文件/身份/邮箱路由/deviceId钉死/Meta删号/session持久化/UTC+8守卫/6个行为点/两份守门测试 **全部对应**。两处「看似少了」实为等价:①iris `swipe-limit` 有两处 isMetaReview(`isLikeReady`+`isNopeActive` 双独立配额),koda 是**单函数 `canSwipe(direction)`**,一处 gate 覆盖双向(已验 canSwipe 是唯一闸门,(tabs)/index→deck/card 全走它);②iris 在 `chat-header` 隐藏视频按钮,koda **chat-header 无视频按钮**(只有返回/头像/更多),视频入口在 action-bar 的 `showVideo`,已 gate。③iris `video-match.ts` ×2 → koda 无该功能,合理跳过。④good-review 已验所有弹窗入口(showPop/checkSwipeCardPop/chatFiveRounds)都过 `shouldShowPop`,一处 gate 全覆盖。
- **唯一有意分叉**:注册流加长归属 —— iris 用 `use-onboarding-step`(`full=useIsMetaReview()`)让 **meta 号走长流程**;koda 是 #386 让 **store 号走 4 屏**(reviewFlow)。用户 Q2 已选「保持现状」。若要对齐 iris,只需 `sign-in.tsx` 的 `setReviewFlow(kind === "store")` 改成 `"meta"`。
- **残留**:`META_REVIEW_PASSWORD` 仍是占位 `REPLACE_WITH_META_REVIEW_PASSWORD`,待 koda 后端建 `metareview@kodasoc.com` + 给密码;端到端须 EAS 真机双号验;文档 §4 三个已知缺口(会话列表未过滤/主页真实订阅态/固定候选列表)koda 同样不做。
- **坑**:zsh 里 `git show "$REF:apps/..."` 的 `:a` 会被当成变量修饰符吃掉字符,必须写 `"${REF}:apps/..."`。

### 08:35 — koda 接入审核模式/Meta 开白全套(按 docs/review-mode.md,续 #386)

- **需求**:按 `docs/review-mode.md` 给 koda 接「商店审核(isStoreReview)/ Meta 开白(isMetaReview)」两类账号。#386 已做的审核账号登录 + 4 屏注册流(=onboarding)保留,续做接缝 + 行为点 + 地区守卫 + 测试。用户定:①meta 密码先占位符 ②store 号保持走 4 屏。
- **关键发现**:文档 + iris/lumi 参照实现**不在 koda 分支**(在 `feature/iris-lumi-koda-android-apk-main`,那条上 koda 是平行分叉);从那条读 iris 源、在当前 `feat/koda-review-mode`(从 feature/koda-android)实现。文档「koda 无实时通道跳过第1条」**已过时**(koda 刚加恋爱铃 alive)→ 做了第1条。
- **实现(13 文件,分支 feat/koda-review-mode)**:P1 接缝 `lib/review-mode.ts`(两邮箱/deviceId + reviewKindForEmail/reviewDeviceId/isLoginBlockedRegion 纯函数,拥有字面量)+ `services/review-mode.ts`(isStoreReview/isMetaReview/setReviewKind/isLoginBlockedByRegion,不 import store、由 session 反向推)+ `review-account.ts` 对账(字面量改 import,加 META_REVIEW_PASSWORD 占位 + reviewCredentialKind/reviewIdentity)。P2 `stores/session.ts`(StoredSession.reviewKind 全 persist 带上 + publishReviewKind→setReviewKind + useIsMetaReview)+ `use-fast-login.ts`(UTC+8 region guard 一处、审核号豁免 + reviewKind 透传 + meta 号先 deleteAccount(token) 再登)+ `sign-in.tsx`(reviewCredentialKind 路由,store→setReviewFlow(true) 走 4 屏、meta 不加屏)。P3 Meta 6 行为点:①_layout alive connect 加 `!isMetaReview()`(断恋爱铃/来电/匹配推送,TIM 登录另一 effect 不动)②swipe-limit.canSwipe ③good-review.shouldShowPop ④vibes 两墙 likedGated/visitorGated 加 `&& !metaReview`(只改锁判定不碰权益字段)⑤chat shouldBillText 加 `&& !metaReview`(单点覆盖 4 下游)⑥chat showVideo 加 `&& !metaReview`(接 action-bar 的 showVideo,非 chat-header,与文档差异已注)。第7条 video-match koda 无、跳过。P4 两份守门测试(review-mode.test 邮箱/deviceId/地区含 Perth+日韩边界;boundary test isStoreReview 零散引用 + 邮箱字面量只在接缝 + 互为子串精确分类)——已验证会响(catch 到 review-account 注释里的邮箱泄漏,去掉即过)。
- **验证**:biome/koda typecheck/boundaries/**353 test** 全过(唯一失败 config.test.ts appName 串包 pre-existing)。
- **后端硬前提**:`metareview@kodasoc.com`(deviceId `koda-meta-review-device-2026`)+ 密码须 koda 后端真实建号预置资料(store 号 #386 已用);META_REVIEW_PASSWORD 现为占位 `REPLACE_WITH_META_REVIEW_PASSWORD`,后端给密码后一行填。
- **坑/经验**:①boundary 守门测试扫 src 全文(含注释),邮箱字面量连注释都不能有,只能在 lib/review-mode.ts;②UTC+8 判据用标准时偏移(一月/七月取 max)非当前偏移,否则夏令时误挡;③两邮箱互为子串(metareview 含 review),判定必须精确相等,任何 includes/endsWith 都会把 meta 认成 store、拆墙全失效且本地看不出;④依赖方向反的:services/review-mode 不 import store,session 反向 setReviewKind 推入(否则把 SecureStore 原生依赖拖进每个测试)。

### 07:43 — koda 接入恋爱铃全套(三子系统)完成,开 PR

- **需求**:koda 完整接入 iris 的「假来电」增长功能(用户定范围=全做:恋爱铃 + 全屏来电邀请 + PWA 回播)。规范模式,Plan agent 出方案 → 逐 Phase 实现,详见项目笔记 `10-Projects/koda-love-ring.md`。
- **做了什么(分支 `feat/koda-love-ring`,20 文件)**:P1 基建(call-source 13 值枚举+投影、ab-test getAbBoolean+KODA_CONSUMED_AB_KEYS、report-user-events、call-record);P2 恋爱铃状态机 love-ring.ts(握手/门禁/120s 频控/结局回报/未接来电)+ call-session 各结局 gated isFakeCall 回报 + use-call 余额不足回报;P3 love-ring-banner(koda 暖色重画)+ fake-call-host + (tabs)/_layout 挂载 + logout resetLoveRing;P4 两个孪生(full-screen-invite + pwa-video-recall,service+组件)+ 三者互斥 couldShow + host 全挂。
- **koda 特有适配**:UI 全用 koda StyleSheet+暖色 token(不照搬 iris Uniwind);埋点走 LumiEvent(koda 前缀,共享 business-analytics 已有全部 FakeCall/NewUserVideoPenetration/PwaCallBack 事件,天然不串包);SecureStore 安全 key(去冒号);useSessionStore/useKodaChatStore 替 iris store;去 candidate-mapper 改内联投影 UserInfo;AliveConnectionState 从 @heyhru/rn-alive。
- **验证**:biome/koda typecheck/boundaries/**337 test 全过**(唯一失败 config.test.ts appName=koda_android 串包 pre-existing,与本次无关)。新增 6 个 test 文件(call-source/ab-test/report-user-events/call-record/love-ring/full-screen-invite/pwa-video-recall)。
- ****PR #409**(commit 81db36f3,base feature/koda-android)。待办**:端到端必须 EAS 真机 + koda 后端推 S2C_LoveRingPush / S2C_FullScreenVideoPush / S2C_PwaVideoRecall + SendMessageFromBot + AB 开关(后端前置依赖,见项目笔记第 3 节);孪生子系统稳定后可评估抽 headless 包。
- **坑(→ troubleshooting 可复用)**:①vitest 里被测模块新增了对拉入原生依赖(如 @/services/client→expo-modules-core)的 import,会导致整个 test suite「加载失败(Stripping types unsupported under node_modules)」——在 test 里 mock 掉那条 import 链的中转模块即可。②`vi.fn()` 的 `mock.calls[0]` 元组类型是 `[]`,解构断言要 `as unknown as [...]`。③循环 import(三子系统互相 import isXShowing)运行时安全(函数在调用时才求值,非 import 时)。

- **诊断包 `diag/koda-image-2400`(setLogLevel 0)取到真相**,三行关键日志:
  1. `[koda diag] upload picked: file:///...ImagePicker/....jpeg compressed: file:///...ImageManipulator/....jpg recompressed:true` → **uri 全程 file://,压缩正常** → uri 从来不是问题(legacy #406、时序 #408 全是红鲱鱼)。
  2. `UploadModule._getCosPreSigUrl ok errorCode:0 OK uploadUrl:https://file.im.qcloud.com/trans_upload?sdkappid=1600002475...` → **拿 COS 预签名成功,凭证/COS 正常**。
  3. `UploadModule._uploadWithPreSigUrl failed, error:{"message":"{}"} {"headers":{}}` → **真正失败:往 COS 上传的 HTTP 请求返回空响应/空 header = 网络层被拦**。
- **根因**:cache 路径包名 `com.local.test` = koda **development 档(dev-client)**。expo-dev-client 的 CDP 网络检查器会拷贝每个请求体,大请求体(图片上传/通话)会被搞坏——iOS 崩溃、**Android 表现为上传请求空响应 → 2400**(见 `apps/iris/docs/05 §1.1.c`)。**只影响 dev 包,release/preview 不含检查器**。现有插件 `@heyhru/rn-expo-plugins/dev-network-inspector` 只禁 iOS 侧(Podfile `EX_DEV_CLIENT_NETWORK_INSPECTOR=false`),**Android dev 档仍开着**;且 koda 的 plugins 里连这个插件都没加(iris 有)。
- **koda eas.json**:development=developmentClient:true(com.local.test);preview/nightly/production 无 dev-client。→ **用 preview 档出包图片即正常**。
- **交代**:#406(legacy)、#408(时序)都在追不存在的代码 bug。#406 已 merge(对齐 iris 无害);#408 未 merge,应关掉(改了图片 UX 却无收益)。
- **待用户**:出 koda **preview** 包验证(应能发图)。后续可选:把 `dev-network-inspector` 插件扩展到 Android(gradle 侧禁检查器)+ 给 koda 加上该插件,让 dev 档也能发图。
- **可复用经验(→ troubleshooting)**:RN dev-client(expo-dev-client)真机上**大请求体的网络请求**(图片/文件上传、TRTC 通话)可能被 **CDP 网络检查器**搞坏——iOS 崩溃(EXC_BREAKPOINT CFNetwork CustomProtocols)、Android 上传空响应。排查:先看是不是 **development/dev-client 档**,换 preview/release 档验证;根治用 `EX_DEV_CLIENT_NETWORK_INSPECTOR=false`(iOS Podfile)+ Android 侧 gradle 禁用。**别被上层错误码(如腾讯 IM 2400 文件上传失败)带偏去查业务代码。**

### 06:38 — koda 图片 2400 仍复现;定位错误码含义 + 上诊断包(停止猜测)

- **状态**:#408(gate-first 时序修复,未 merge)之外的包实测**仍 2400**。legacy(#406)、gate-first(#408)两次修改都没确认命中(且实测包大概率不含 #408)。停止继续猜。
- **关键定位**:在 `@tencentcloud/chat` 源码错误码表找到 **`MSG_F_UPLOAD_FAIL: 2400`** —— 是 SDK `_cosUploadMethod` 上传 COS 那步失败时抛的**通用码**(附近:`MSG_A_UPLOAD_FAIL:2300` 音频、`MSG_V_UPLOAD_FAIL:2350` 视频、`MSG_I_SIZE_LIMIT:2253` 图片)。真实失败原因(`t.files[0].error` / COS HTTP 返回)SDK 只用 **`M.w`(warn 级)** 打内部日志,而 koda rn-tim `getChat()` 里 `setLogLevel(3)`(只 error)**把 warn 吞了** → 只看到笼统 2400。
- **诊断分支 `diag/koda-image-2400`**(基于 feature/koda-android,不合并):`pickAndSendImage` 里 `getChat().setLogLevel(0)` + `console.log` 打印 picked/compressed uri。出包复现即可暴露:①上传的 uri 是 file:// 还是 content:// ②SDK 打印的真实 COS 错误。据此再精准修。
- **待用户**:出 `diag/koda-image-2400` 的 EAS 包复现,发新日志给我。
- **可复用经验**:腾讯 IM(`@tencentcloud/chat`)富媒体发送失败排查——错误码在 SDK 源码 `MSG_*_UPLOAD_FAIL` 码表(2300 音/2350 视/2400 文件),真实原因被降级到 warn 日志,**先 `setLogLevel(0)` 再复现**才看得到 COS 层真错误;别只盯着上层通用码猜。

### 06:32 — koda 图片 2400 真正根因=发送时序(建消息与 sendMessage 间夹扣费往返)→ PR #408

- **再纠错**:#406 legacy picker 合入后**新包实测仍 2400**,证明 legacy 不是根因(是红鲱鱼)。iris 能发图,逐项排除后定位真因。
- **逐项排除(均代码证据)**:①SDKAppID koda/iris/lumi 共用 `1600002475`、iris 能发 → COS 正常 ②tim-upload-plugin 注册在共享 `getChat()`(rn-tim:84)③`@tencentcloud/chat` 3.6.6 / `tim-upload-plugin` 1.4.3 koda=iris 完全一致 ④都装 `expo-image-manipulator`,压缩产出 `file://`(manipulator 能读 content://,所以 uri 早就是 file://、legacy 无关)⑤payload 逐字段同 iris。
- **真因=发送时序**:koda 为「所有类型先上屏」把 `prepareImage`(内部 `createImageMessage` 建图片消息)提到 `gate()` 扣费**之前** → 「建消息」与「`sendMessage` 真上传」之间夹了一次扣费网络往返(~500ms);`@tencentcloud/chat` 图片上传对此时序敏感,隔一个往返再发就 2400。文本/GIF 不走 COS 上传所以不中招。旗舰是「先扣费→建消息立即发」故无此问题。
- **修复(PR [#408](https://github.com/presence-io/sitin-rn/pull/408) `ee311ea4`,base feature/koda-android)**:`pickAndSendImage` 改 **gate-first**:compress → gate 扣费 → prepareImage(建消息+上屏)→ commitPrepared(立即 sendMessage),让 createImageMessage 与 sendMessage 紧挨着。`ImageSendHandlers` 用 `onBlocked(outcome)` 取代 failPrepared/onInsufficient。代价:图片气泡改「扣费通过后上屏」(本有压缩延迟、空钱包已在开相册前拦,影响小)。biome/typecheck/boundaries/test(303 passed)全过。**须 Android 实机验**。
- **踩坑**:注释里写了 "iris" 被 `pnpm boundaries` 判为 identity leak(§4.2 koda 源码不能提别的 app 名),改成"旗舰 app"才过。
- **可复用经验(→ troubleshooting)**:RN 腾讯 IM(`@tencentcloud/chat`)**图片上传 2400、文本正常**,若基建(SDKAppID/COS/插件/版本/uri)都和能发图的 app 一致,查**建消息与 sendMessage 之间的时序**——中间别夹网络往返(如扣费鉴权),`createImageMessage` 后要尽快 `sendMessage`;乐观 UI 把建消息提前到网络门控之前会触发。判"是不是 uri 问题"先想清楚:压缩(image-manipulator)会把 content:// 洗成 file://,uri 往往早就不是问题。

### 06:15 — #406 已 merge(2593eedd)进 feature/koda-android;待新 EAS 包实机验图片 2400

- **做了什么**:#406(legacy picker 修 2400)已 squash-merge `2593eedd` 进 `feature/koda-android`;确认 base 已含 `lib/image-picker.ts` + chat 2 处 `ANDROID_LEGACY_IMAGE_PICKER` 引用。
- **遇到什么**:用户实机 log 复现 —— `DeductCoinMsgRequest code:1 Success isFree:true`(扣费成功)但 `MessageModule._onSendMessageFailed type:TIMImageElem {"message":"文件上传失败","code":2400}` + `[koda chat] send failed`。前缀 `[koda chat]` 说明是含 #403 的较新包,但**不含 #406**(#406 当时还 OPEN)→ 2400 原样,证明修复没进那个包、非修复失效。
- **下一步(阻塞在用户)**:legacy 是**原生 picker 行为改动,不能 OTA**,必须从含 #406 的 `feature/koda-android` **重新出 Android EAS 包**(GitHub Actions,须用户触发,我无法跑)。出包后发相册图验证;若新包仍 2400 再挖 `tim-upload-plugin` 原生接线 / COS(但 iris 同 SDKAppID 能发图,证据强指向 legacy 已够)。
- **待确认 follow-up**:另 3 处单图选择(post/new、onboarding/photo、edit-profile/photo-profile-editor)是否一并加 legacy。

### 06:05 — koda 图片 2400 真因定位并修复:缺 legacy picker(≠COS,纠正 05:08 结论)→ PR #406

- **纠错**:05:08/05:31 把 koda 聊天发图 `文件上传失败 code 2400` 归因「共享 SDKAppID `1600002475` 的 COS 配置」——**错**。看 iris 图片发送(`sendImageImpl` + `pickAndSendImage`)后定位真因。
- **真因**:图片上传由纯 JS `tim-upload-plugin` 在 TIM `sendMessage` 内部传 COS(iris/koda 同机制,上传器由 `packages/rn-tim/src/index.ts:84` `chat.registerPlugin({"tim-upload-plugin":...})` 注册,koda 也有)。Android 上 koda 相册选图走**默认 Photo Picker / `ACTION_GET_CONTENT`** → 返回 **`content://` uri** → 纯 JS 插件读不到 content:// 文件 → 上传失败 2400。
- **关键机制**:仓库有全仓 patch `patches/expo-image-picker@57.0.5.patch`(pnpm `patchedDependencies`,koda 也吃),把 Android 单图选择改成 MediaStore **`ACTION_PICK`(Gallery)→ 可读 `file://` uri**,**但只在传 `legacy: true` 时生效**。iris 每处单图选择都传 `legacy: ANDROID_LEGACY_IMAGE_PICKER`,**koda chat 没传**——这是 iris 能发图、koda 2400 的唯一差异。扣费本身一直是成功的。
- **修复(PR [#406](https://github.com/presence-io/sitin-rn/pull/406) `d1ee8f59`,base feature/koda-android)**:新增 `apps/koda/src/lib/image-picker.ts`(`ANDROID_LEGACY_IMAGE_PICKER`,对齐 iris,纯 Platform 判断无身份);`chat/[id].tsx` 相册选图 `launchImageLibraryAsync` 传 `legacy: ANDROID_LEGACY_IMAGE_PICKER`(相机不受影响,直接 file://)。加 `shouldUseAndroidGalleryPicker` 2 用例。biome/typecheck/test 过。**须 Android 实机验(原生 picker 行为不能 OTA)**。
- **iris 图片发送时序(参考)**:permission → `launchImageLibraryAsync({legacy})` → **gate 扣费(先扣)** → compress → `sendImage`(createImageMessage → 乐观上屏 → sendMessage 内 tim-upload-plugin 传 COS → 替换/标红)。koda 是 compress→prepareImage 上屏→gate→commit(乐观,刻意不同)。
- **follow-up**:koda 另 3 处单图选择也没传 legacy(`post/new.tsx:73`、`onboarding/photo.tsx:25`、`edit-profile/photo-profile-editor.tsx:28`),若上传图片可能同源,PR body 已列、待用户确认是否一并修。
- **可复用经验(→ troubleshooting)**:RN/Expo Android **图片上传失败但选图成功** → 优先查是不是 `content://` uri(新版 expo-image-picker Photo Picker / ACTION_GET_CONTENT 的产物),很多纯 JS 上传库(如腾讯 `tim-upload-plugin`)读不了 content://,需 `legacy: true` 走 MediaStore ACTION_PICK 拿 `file://`。别一上来怪服务端/COS——先看 uri scheme。

### 05:51 — koda 聊天金币门控开 PR #405（05:31 + 05:47 两轮合并提交）

- **做了什么**:把 05:31(余额不足统一移除气泡+弹 Almost out of sparks)+ 05:47(Kira 式发送前空钱包预检)两轮**未提交**改动,从最新 `origin/feature/koda-android` 拉新分支 `feat/koda-chat-insufficient-coin-gate`,一起提交(`191a70e7`)、push、开 **PR [#405](https://github.com/presence-io/sitin-rn/pull/405)** → base `feature/koda-android`。
- **流程**:`git stash` → `checkout -b ... origin/feature/koda-android` → `stash pop`(干净无冲突,因为改动基础 228aa70d 已随 #403 squash 进 feature/koda-android)→ 新基线重跑 typecheck/test(301 passed,唯一失败仍是 config.test.ts `koda_android` 串包 pre-existing)→ commit(colna 身份)→ push(pre-push boundaries+typecheck 全绿)→ `gh pr create`。
- **涉及文件 5 个**:`chat.ts`(discardPrepared)、`chat/[id].tsx`(openRecharge/onInsufficient/blockIfEmptyWallet 四类接线、gateMedia 返回 outcome)、`chat-billing.ts`(canSendBillableMessage)、`chat-billing.test.ts`(+3)、`chat-image.test.ts`(+1)。
- **残留**:config.test.ts `appName=koda_android` 串包(pre-existing,PR body 已列 follow-up);BytePlus appId/appName 串包仍待 koda 真实值。
- **可复用经验**:把某功能分支(已被 squash-merge 的)上**未提交**的新改动迁到最新集成分支 → `stash` → 从 `origin/<集成分支>` `checkout -b` → `stash pop`。因原改动基础已随 squash 进集成分支,pop 通常干净;若冲突说明基础文件在集成分支被其它 PR 动过,需手动 merge。

### 05:47 — koda 聊天照 Kira 加「发送前本地余额粗预检」，消除大部分闪现（未提交）

- **背景**:承接 05:31,用户看了 Kira 做法后要求 koda 也照做。查 Kira-Android(Explore agent)结论:`CoinInfoManager.hasEnoughCoins() = getTotalCoin() > 5 || getRemainFreeTxt() > 0` —— **发送前用本地余额粗阈值(硬编码 5,非真单价)+ 免费次数**拦截,余额明显不足直接不上屏、弹充值;够则乐观上屏 + 后台扣费、失败不回滚(靠服务端对账)。礼物有真单价 `giftInfo.giftCoinValue` 走「余额≥单价」。余额读**内存缓存→本地持久化**,不实时拉。
- **koda 已具备 Kira 需要的本地数据**:`coinBalance`(entitlements)、`freeTextMessageCount`、礼物 `gift.price`(submitGift 早已本地预检)。缺的只是 text/gif/image 的发送前预检。
- **改动(未提交)**:
  - `chat-billing.ts`:加纯函数 `canSendBillableMessage(coinBalance, freeTextMessageCount) = coinBalance > 0 || freeTextMessageCount > 0`。**阈值取 `> 0` 而非 Kira 的 `> 5`**——koda 不知道文本真实单价,`> 0` **零误拦**(余额>0 的用户永不被本地错拦);余额恰好为 0 的绝对发不了 → 发送前拦、不上屏(消除闪现);「余额>0 但 <单价」的近空边界仍走乐观上屏 → `deduct` 兜底撤回。
  - `chat/[id].tsx`:拆 `openRecharge(source)`(弹 sheet+埋点),`onInsufficient`=`discardPrepared`+`openRecharge`(近空边界:上屏后撤回),新增 `blockIfEmptyWallet(source):boolean`(空钱包发送前拦)。四类接线:**text** prepareText 前拦;**gif** prepareGif 前拦;**image** 打开相册前拦(没钱连相册都不开);**gift** 真单价预检 `coinBalance < gift.price` **移到 prepareGift 之前**(Kira 式,不上屏),服务端 insufficient 仍 `onInsufficient` 兜底。
  - VIP/非计费(`!shouldBillText || isLumiPlus`)不预检。
- **效果**:真正没钱的用户「发送前拦下、直接弹 Almost out of sparks、零闪现」;只有近空边界(余额>0 但<服务端单价,如免费额度刚耗尽)才走一次乐观撤回。= 方案 B 的 Kira 优化版,接近方案 A 体验但保留正常发送即时上屏。
- **测试**:`chat-billing.test.ts` 加 3 个 `canSendBillableMessage` 边界用例(空钱包拦 / 有币放行含<单价 / 仅免费次数放行)。
- **验证**:biome / koda typecheck / boundaries / test 全过(**299 passed**,唯一 1 failed 仍是 `config.test.ts` `appName=koda_android` pre-existing 串包)。
- **可复用经验**:「乐观先上屏」与「不足不上屏」在无本地单价时看似互斥,但 Kira 用**本地余额粗阈值预检**破了这个二难——不需要真单价,只要拦掉「明显没钱」(余额≈0)就能消除绝大多数闪现,近空边界交给服务端兜底。koda 取 `>0` 比 Kira 的 `>5` 更保守(零误拦),代价是余额 1~单价-1 的极小区间仍闪一次。

### 05:31 — koda 聊天余额不足：统一移除气泡 + 弹 Almost out of sparks（未提交）

- **需求**：发消息时钱包金额 < 单价 → **不上屏**、直接弹 Almost out of sparks 充值窗。
- **关键约束**：koda 客户端**无本地单价**，`deduct`=扣费授权、不能空跑预检 → 严格「不足不上屏」只能 **deduct 先行**(对齐 iris `use-gated-send`),与 #403「乐观先上屏」互斥。用 `AskUserQuestion` 让用户选,**选了方案 B(保留乐观先上屏)**:正常秒上屏,不足时气泡先上屏再撤回 + 弹窗。
- **改动(未提交,规范模式)**:
  - `store chat.ts`:加 `discardPrepared`(filter 移除气泡 + 清 `pendingSends`),区别于 `failPrepared`(标红保留)。
  - `chat/[id].tsx`:加统一 `onInsufficient(peerId,messageId,source)`=`discardPrepared` + `setRechargeVisible(true)` + 埋点;**text/gif/image/gift 四类 insufficient 全走它**。`gateMedia` 改返回 `DeductOutcome`(UI 分派交调用方)。**删 `openChatPaywall`**(原 text/media 余额不足是 `router.push("/paywall")` 跳全屏,现改弹底部 `ChatRechargeSheet`,与 gift 统一、对齐 iris 的 in-chat recharge)。`giftRechargeVisible`→通用 `rechargeVisible`。`ImageSendHandlers.gate` 返回 outcome + 加 `onInsufficient`。
  - **区分**:仅 `insufficient` 移除 + 弹 sparks;`rate_limited`/`error`/上传失败仍 `failPrepared` 标红可重试,不弹充值。
  - **埋点口径统一**:gift 余额不足现在也发 `RechargeWindowInChat`(原只发 `RechargeSectionShow`,漏发),四类一致。
  - **一处轻退化**:image 限流(rate)不再显示 rate banner(gateMedia 副作用移出后 image 只标红;gif 仍 `setBillingBlock("rate")`)——媒体限流罕见,可接受。
- **测试**:`chat-image.test.ts` 加 `discardPrepared` 回归(移除气泡 + 不发送)。
- **验证**:biome / koda typecheck / boundaries / test 全过(**296 passed**,唯一 1 failed 仍是 `config.test.ts` `appName=koda_android` 串包遗留,pre-existing)。
- Almost out of sparks 弹窗 = `ChatRechargeSheet`(默认标题即 "Almost out of sparks")。
- **可复用经验**:koda/iris 聊天扣费是「服务端权威 + `deduct` 即授权」,客户端无本地单价 → 「发送前判断够不够」要么 deduct 先行(iris),要么做不到精确;「乐观先上屏」与「不足不上屏」在无本地单价时**互斥**,只能二选一(方案 B 折中=先上屏再撤回)。

### 05:08 — koda 所有消息类型「乐观先上屏」对齐 text（feat/koda-optimistic-send, 228aa70d）

- **任务**：用户报「消息发送后没第一时间上屏」「所有消息类型都要先上屏」「为什么还是 lumi」，在 `feat/koda-optimistic-send` 分支实机测。
- **改动（`228aa70d`，4 文件）**：`chat.ts` 把只服务 text 的乐观管线泛化——`pendingText`→`pendingSends`（text/image/gif/gift），新增 `prepareImage`/`prepareGif`/`prepareGift`（点击即插 `sending` 气泡、无 await），通用 `commitPrepared`（扣费过后原位替换 `sent`）/`failPrepared`（拒绝或失败标 `failed` 回滚），取代旧 eager 的 `sendImage`/`sendGifMessage`/`sendGiftMessage`；抽 `insertOptimistic` 去重四处 upsert；**`sendCallRecord` 未动**。`[id].tsx` 每个 handler 改「prepare 上屏+scroll → gate 扣费 → commit/fail」，埋点口径不变。
- **服务端扣费=发送授权未被绕过**：`commitPrepared` 仅在 `pendingSends` 命中且 `ensureReady` 后才真发，扣费失败前不 sendMessage。
- **一处取舍**：image 是**先本地压缩再 prepare**（保证预览气泡与已发气泡同一 message id，避免重影），故图片在「压缩后、网络往返前」上屏；text/gif/gift 真点击即上屏。
- **「为什么还是 lumi」根因=旧 bundle**：代码里已归零（`91d41c93` rename，`chat.ts` grep `lumi chat`=0，第 469 行已 `[koda chat] sendImage failed`）。用户看到 `[lumi chat]` 是 rename 前打的旧 bundle/APK，清 Metro（`--reset-cache`）或重 EAS build 即消。
- **图片 2400「文件上传失败」**：`DeductCoinMsgRequest` 成功、卡在 TIM→COS 上传，非客户端 bug，指向共享 SDKAppID `1600002475` 的 COS bucket 开通/配额，需腾讯 IM 控制台确认、代码不可修。改后即使 2400 也先看到图再变失败态。
- **验证**：biome / koda typecheck / boundaries 全过；`pnpm --filter koda test` 295 passed，唯一 1 failed 是 `config.test.ts` 的 `appName` `koda_android`（09-03 已记录的 pre-existing 串包遗留，本次未碰 config）；重写 `chat-image.test.ts` 覆盖新 API（即时预览、扣费拒绝回滚、上传失败标记）。
- **分支** `feat/koda-optimistic-send` 已 push `228aa70d`（用户在此分支实机测的功能分支）；PR #403 仍待 review/merge 进 `feature/koda-android`。
- **可复用经验**：RN「源码改了但设备上仍是旧字样/旧行为」优先怀疑 **bundle 缓存**——`git grep <字样> HEAD` 确认源码已改后，重点在 `--reset-cache`/重出包，而不是继续找代码；不重新 bundle，改动不会到设备。
- **[merge 确认]** PR **#403** 已 squash-merge（`1f70a3f2`）进 `feature/koda-android`。核实 squash 内容含 `228aa70d` 全部（`prepareImage/prepareGif/prepareGift`、`pendingSends`、`commitPrepared`）——**四类消息先上屏 + text 乐观 + lumi rename 三者一并落地**（`228aa70d` 05:06 早于 merge 10:13 六分钟，故被 squash 收入）。`git merge-base --is-ancestor` 对 squash PR 无意义（原 hash 不入历史），判是否合入要看 squash commit 的**内容**。功能分支 `feat/koda-optimistic-send` 使命完成、可删。**残留未修**：①图片 2400（共享 SDKAppID `1600002475` 的 COS 开通/配额，需腾讯 IM 控制台）②`config.test.ts` `appName=koda_android` + BytePlus `appId/appName` 串包（需 koda 真实值，代码内无法凭空补）。

### 03:40 — koda 注册流程两权限现象排查：定位页不弹 + 通知启动就弹（结论=旧包，未改码）

- **问题**：用户报 Android 实机两现象——①注册到地理位置页，预期直接弹权限弹窗，实际没弹；②通知权限在刚打开 app 启动页就弹，预期第一次进 chat 列表才弹。用户确认平台 Android、包「旧包/不确定」。
- **排查结论：两个预期行为代码里都已实现，且都是近期才加的，Android 旧包不含**。
  - ①定位：`onboarding-location-picker.tsx` 有 mount `autoLocate`（`ensureLocationPermission` → `requestForegroundPermissionsAsync` 弹窗，store `location` 初值 null 会触发）。该逻辑是 **PR #376 `a49d92a9`（2026-09-04）** 才加的。9-4 前的包没有它 → 进页不弹、只能手输 ZIP。
  - ②通知：koda **不用** device-push（那是 iris），只用 TIMPush `registerTimOfflinePush`，仅 `(tabs)/chat.tsx` 的 `useFocusEffect`（status==="ready"）触发；默认落地 tab 是 `index` 不是 chat。原生侧 `rn-tim-push/plugins/timpush.js` 的 `wireTencentPushApplication` 往 MainApplication.kt 的 `attachBaseContext` 注入 `TIMPushConfig.getInstance().setAutoRegisterPush(false)` + `enableRequestPostNotificationPermission(false)`（挡 TIMPush ContentProvider 在 RN 前的 Android 13 自动请求）。JS 延迟逻辑 `efe96f56`「defer notification prompt to opening Chats」是 **2026-08-19** 加的。8-19 前的包是老逻辑（注册期就请求）。
- **一度以为的真 bug（已证伪）**：timpush.js 第 313 行 `if (businessIdAndroid) { withAndroidConfig(...) }`——Android 全部 disable 注入被 FCM businessId gate 住。但 koda `eas.json` 三个 profile 都配了 `EXPO_PUBLIC_TIM_PUSH_BUSINESS_ID_ANDROID=46740`，gate 通过、注入会执行。**证伪，配置无缺失。**（潜在加固点：若某 app 没配 Android FCM businessId，disable 注入会整段跳过 → 那个 app 启动会弹 Android 通知权限。koda 不受影响。）
- **给用户的路径**：基于最新 `feature/koda-android` 出 Android EAS 包（9-4 之后），两现象应消失；两者均原生层改动、**不能 OTA**。新包仍复现再要启动阶段 TIMPush 实机日志深挖。
- **未改代码**（规范模式；代码已是预期实现）。
- **可复用经验**：实机权限/推送类「现象与预期不符」，先查对应「预期修复」的 commit 进代码日期 vs 用户包构建时间——原生改动只能新包生效，`git log --format="%ci" -- <文件>` 拿日期锚点最快定「是不是旧包」。

### 02:55 — koda chat 列表官方消息时间「几分钟前」修复：R00265 被覆盖后重做（fix/koda-chat-official-time）

- **任务**：sitin-rn2 从 `feature/koda-android` 切分支，修「注册后 chat 列表官方消息时间应显示具体 HH:MM，不是几分钟前」。用户问「之前是不是改过」。
- **关键发现（之前改过，但改动丢了）**：这就是需求 **R00265**，PR **#374**（squash commit `206ef37f`，2026-09-03）改过——`buildChatListPresentation` 加 `official` 参数走绝对 `HH:MM`（隔日带 M/D），`ConversationRow` 传入。**但当前 `feature/koda-android` 上该逻辑不存在**：`apps/koda/src/lib/chat-list-presentation.ts` 官方消息仍走 `formatRelativeTime`。核查发现 `206ef37f` 之后的 commit **`9eb5b9a4 fix(koda): chat`（09-03 17:30）覆盖了这个文件**，把 `official`/`formatAbsoluteTime` 冲掉了（`git log -- <file>` 里已看不到 206ef37f）。残留状态：`conversation-row.tsx` 第 57 行 `isOfficialConversation` 还在算 `official` 布尔，但第 59 行调用没传给 `buildChatListPresentation`。
- **修法**：照 `206ef37f` 已过 review 的实现恢复两处——① `chat-list-presentation.ts` 加回 `formatAbsoluteTime` + `official=false` 参数 + `timeLabel` 三元切换；② `conversation-row.tsx` 第 59 行调用补 `official` 实参。测试补 2 个回归用例（当天 `/^\d{2}:\d{2}$/`、隔日 `/^\d{1,2}\/\d{1,2} \d{2}:\d{2}$/`，并对照 official=false 仍 "30m"）。
- **验证**：`pnpm --filter koda test chat-list-presentation` 14 passed；lint（`lint:fix` 修了 1 条行宽 format，剩 2 条 lumi 既有 warning）；koda typecheck 过；boundaries OK。
- **pre-existing 失败（与本次无关）**：完整 `koda test` 有 1 failed `config.test.ts > keeps ... scoped to Koda`——`config.extra.appName` 现为 `koda_android`、测试期望 `koda`。`git stash` 后在 base 上复现，确认是分支自带、非本次引入。
- **可复用经验**：长命集成分支 `feature/koda-android` 上，**已合并的 PR 改动可能被后续 commit 静默覆盖**。判「某需求是否还在代码里」不能只看 `git log --grep` 命中 squash commit，要看 `git log -- <具体文件>` 该文件的 blame 历史里还有没有那条 commit——命中 commit 但 blame 无它 = 被后续覆盖。
- **PR [#402](https://github.com/presence-io/sitin-rn/pull/402) 已 merge**（merge commit `039cee86` → `feature/koda-android`，2026-09-07）。四要素：
  - **做了什么**：恢复 R00265 官方消息绝对时间（`chat-list-presentation.ts` 加回 `formatAbsoluteTime`+`official` 参数、`conversation-row.tsx` 补传 `official`、+2 回归测试）。
  - **遇到什么问题**：R00265 改动被 `9eb5b9a4` 覆盖丢失（commit 在分支但文件 blame 里没有它）。
  - **怎么解决**：照 #374 已过 review 的实现恢复两处；PR base 用 `feature/koda-android`（对 main 会带几百文件 diff）。
  - **残留**：纯 JS，需随下个 EAS 包实机验证；`config.test.ts`（`appName=koda_android` vs 期望 `koda`）为分支自带 pre-existing 失败，与本 PR 无关。
  - **收尾**：切回 `feature/koda-android` `pull --ff-only`（前进，引入 chat-rounds.ts 等新文件）→ `pnpm install` → 删本地+远端 `fix/koda-chat-official-time`。已验证 `official` 逻辑在 merge 后 base 里。
