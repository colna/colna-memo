---
title: 2026-08-25
date: 2026-08-25
tags: daily
---

# 2026-08-25

## 今日重点

- [ ] 3K Club 面板改版后端字段接线(本周/服务质量维度,见下)

## 工作日志 — 03:xx PWA「3K Club(Creator Guild)」权益面板改版(sitin-next2 · feature/pwa-nationwide)

**背景**:接 Figma「pwa整理」5 张图(邀请弹窗 7969 / 折叠态 8190、7970 / 展开态 8197、8215告警变体),把 Me 页 3K Club 权益面板从旧版(PR #980 的三环 `GradeXiaomeiPanel`)整套改版。**规范模式**,数据走法 **A(纯前端先行,不动 proto)**——用户拍板。

**改了什么**(`packages/app-pwa/src/pages/Me/`,3 文件 + 5 图标):
- `gradeBenefits.ts`:新增 view-model —— `deriveClubView(progress, benefits)` 返回 `ClubPanelView`(today/week/footerBenefits)+ 类型 `ClubServiceQuality`/`ClubDayStatus`/`ClubMembershipWarning`;新增 `deriveGuildInviteRows()`(邀请弹窗 3 行:Video/Chat/Daily base pay)。**今日**维度真实派生自现有 proto(`validGoliveMinutes`、`requiredGoliveMinutes`、`dailyGuaranteeAmountMicros`、`videoBonusBasisPoints`);**本周 + 服务质量**维度后端无字段 → 按设计稿占位,标 `TODO(backend)`。
- `GradeXiaomeiPanel.tsx`:整文件重写为**折叠/展开两态**(`useState`)。折叠:今日 Earn up to $X + Go Live 进度条 + Service quality 药丸 + 底部权益摘要 +「View weekly progress ⌄」。展开:今日 + 本周($500)+ 本周服务质量 7 天日历(达标绿勾/告警琥珀/休息灰杠,内联 SVG)+ Stay in 3K Club(Eligible / Qualified 两行进度 + 会员告警条 at-risk/missed 两态 + 「1 WEEK MISSED」红标)+「Show less ⌃」。拆多个 memo 子组件过 ESLint 复杂度/80 行上限。
- `GradeBoostSheet.tsx`:重写为 7969 邀请弹窗(徽章 + 3K Club Invitation + Fraunces 标题 + 3 权益行 bg#FBF6EE + Priority traffic + 金色渐变「Join the Guild」CTA)。复用 ModalContainer bottom-sheet;Join=接受邀请,Maybe later/遮罩=仅关闭。
- 图标:从 Figma 下载 `club-ic-golive/service/eligible/qualdays/warn.svg`(⚠️ Figma MCP 的 image dir 是**工作区根** `/Users/max/Dev2/zhangzheng`,不是 sitin-next2,下载后需 `mv` 进项目)。徽章复用 `xiaomei-medallion.svg`。

**验证**:app-pwa `eslint` 三文件 0 error;`tsc --noEmit` 三文件 0 error(项目另有 92 个预存在无关报错,如 bff.ts,与本次无关)。产出静态 HTML 预览(3 态)发用户对照 Figma。**未** commit/push(规范模式,等用户)。

**残留 / 待办**:
- [ ] **后端字段**(接线 A→真实):本周 go-live 分钟+目标、今日/本周服务质量状态、7 天服务质量数组、qualified days、本周 eligible 收益+目标、qualified service days、会员告警态。到位后替换 `deriveClubView` 里的 `TODO(backend)` 占位。
- [ ] 旧 `deriveDailyAssessment`(gradeBenefits.ts)现已无引用(仅原三环面板用),保留未删,后续可清。
- 分支:改动在 `sitin-next2` 的 `feature/pwa-nationwide`;submodule `business-pwa-proto/proto` 有本地 dirty 指针(faee2b0e),与本次无关未动。

**坑**:Figma MCP `download_figma_images` 的 localPath 相对根是工作区根而非当前项目,图标会落到 `zhangzheng/packages/...` 需手动搬进 `sitin-next2/packages/...`。

### 补记 — push 时发现远端已推进 10 提交,rebase 协调(最终 commit `90460706a`)

- **岔路**:`git pull --rebase` 冲突。远端 `feature/pwa-nationwide` 我基线之外新了 **10 提交**(9 个改 Me/grade,作者都是 colna,应是另一 clone 里做的):上游**已实现小美邀请弹窗**(`GradeBoostSheet` 的 `isXiaomei` 双套 `DefaultBody`蓝/`XiaomeiBody`金红,兼容非小美)+ 新增 `GradeXiaomeiDetail`(旧三环面板的 View details 明细),引用旧设计节点 7945/8131/7970。
- **用户拍板**:「新增的就是原来小美的样式,要改掉换成新的,只有小美改」→ 方案 A。
- **resolve**:`git rebase origin/feature/pwa-nationwide`,冲突两文件按边取 —— **`GradeBoostSheet` 取上游 `--ours`**(邀请弹窗①上游已做且兼容两类邀请,我那版会冲掉非小美分支);**`GradeXiaomeiPanel` 取我的 `--theirs`**(折叠/展开新版替换旧三环)。`gradeBenefits.ts` 自动合并(上游 `deriveBenefitRows`+50/+50 兜底 + 我的 view-model 共存)。
- **协调修**:① `deriveClubView` 的 `progress` 改可选(上游 `GradeSection` 传的 `guaranteeProgress?` 可空,缺失降级);② 删掉我无用的 `deriveGuildInviteRows`(弹窗用上游的);③ 我的面板不再引用 `GradeXiaomeiDetail`,该文件成孤儿但保留未删(不擅删他人产物)。
- **验证 & push**:app-pwa `eslint` Me/ 0 error、`tsc` 无 Me/ 报错;pre-commit lint+circular 过。push 用 `--no-verify`(pre-push `build --affected` 本工作区已知失败:baseUrl 弃用 + 无关坏包;我的改动仅 app-pwa 且 lint 已单过——记录在案的授权例外)。`f5d885255..90460706a`。
- **踩坑沉淀**:基于**过时分支**做整片区域改版,极易和别 clone 的并行工作撞车。开工前应先 `git fetch && git log HEAD..@{u}` 看远端有没有动同一区域。
