---
title: 2026-09-16
date: 2026-09-16
tags: daily
---

# 2026-09-16

## 今日重点

> 从 09-15 的 Daily 带过来。

- [ ] luka **真机业务验证**：真机链路已打通，业务主流程（登录 / 划卡 / 聊天）还没在真机上回归
- [ ] edit profile 剩余屏：`photos`（mockup `82`）
- [ ] Nearby **列表模式没有仓鼠**（w3 稿上有）—— 要加说一声
- [ ] luka 工作区 3 处未提交（规范模式，待发话）：`pet-perch-corner` 小崽放大右移 / Calls 副标题英文方向词 / gift-sheet 换通用抽屉
- [ ] 远端已合并分支清理（`feat/luka-edit-profile` 等）
- [ ] 未决设计点：photos 单图换头像 vs 多图相册；interests 平铺 vs 分组；AI tip 留不留
- [ ] **template-migrations 落后 26 条**（`appliedMigration` 停在 0009，blueprint 已到 0035；支付三件与 review-mode 两条动到实现）

## 工作日志

## 工作日志 — 10:46 前一天（09-15）整理

- **沉淀**：3 条新增 —— [opencode-desktop-image-extraction](../30-Resources/troubleshooting/opencode-desktop-image-extraction.md)（用户贴的参考图在 `opencode.db` 的 `part` 表里，base64 还原）、[metro-bundle-hot-and-file-write](../30-Resources/troubleshooting/metro-bundle-hot-and-file-write.md)（`hot=false` 必须 terminate+launch；`sed -i` 写文件 watchman 拾不到）、[expo-video-native-layer-overlap](../30-Resources/troubleshooting/expo-video-native-layer-overlap.md)（VideoView 是原生图层，z-index 压不住，裁自己的容器）。当天声称已沉淀的（figma-write-access 2026-09-15 节、image-gen-transparent-cutout 坑三、design-mock-measure-and-sample 两节、luka-chat-gift-video-fail、rn-prebuilt-core-dyld 等）复查都在，无遗漏。
- **带进今日**：7 条待办（真机业务验证 / photos 屏 / Nearby 仓鼠 / 3 处未提交 / 分支清理 / 三个设计未决点 / 26 条迁移），已写进上方「今日重点」；「heard.mov 没人引用」那条经 09-15 代码核对已被引用，划掉。
- **项目进展**：`luka-rn-app` 的已完成表补 09-15 批次（Chats / 聊天页 B 版 / Calls / 通知与访客付费 / Bond 三屏 / toast / 桌面组件 0.4.0 / eat.mov 入库），通话充值那行从「未提交」改为已入 `2fdbcc88`。
- **归档**：无 —— 09-15 的工作日志都是实质内容，原文保留。

## 工作日志 — 10:46 feature/pwa 拉最新 + native call 违规判罚埋点排查

用户：「进入 feature/pwa 拉取最新代码，看看 native call 违规判罚有没有埋点」。

- **拉取**：工作区原有 2 处未提交（`.serena/project.yml`、`business-pwa-proto` 子模块指针）不影响切分支；`git checkout feature/pwa` + `git pull --ff-only`，`c5d3d2f14 → 3a3cdcbe1`（28 提交，含 #1148 回拨来源在建单时落库 `toCallOrderSource → createCallOrder`）。
- **排查结论**（代码 `packages/app-pwa/src`）：
  - **有埋点**：证据链 `pwa_holding_state_change`（useNativeCall.tsx:444，陀螺仪翻转）、`pwa_face_rate_result`（useNativeCall.tsx:284，`source:"native"`、`total_detect_count:-1`）、图普三件 `pwa_screenshot_detect_start/step/violation_result`（useVideoScreenshotViolation，web/native 共用）；扣款 `pwa_call_real_time_deduct_Earnings`（useCallViolationDeduction.ts:52，native 接通时 `startMinuteCheck` 同挂）。
  - **无埋点（缺口）**：① native 实时违规弹窗走桥 `coiceViolationToast`（useNativeCall.tsx:116/122），**不报** `pwa_call_real_time_violation_pop_up_show`（该事件只在 web 的 ViolationLimitModal.tsx:109）；② 判罚动作本身（`!isHolding && faceRate<0.5 → changeDispatchStatus(false)`，useNativeCall.tsx:346）与 `processCallRecord` 违规记录/三次降级（callUtils.ts:236+，native 拒接 / 未接超时路径）全无 bpTrack；③ 语音违规 `recordViolation` 只 console.warn（useVoiceModeration.ts:314），选项 `nativeReportViolationEvent` 是死代码（定义+存 ref，全仓无人传、无人调）；④ web 弹窗里两个「继续/放弃」扣款埋点（ViolationLimitModal.tsx:188/210）native 无对应（原生弹窗选择 JS 无感知）。
  - 附身态（`isImpersonating()`）统一不上报；APK 原生侧是否另有 BytePlus 上报要查 GraceChat-Earn 端仓。
- **沉淀**：无（本轮排查未产生新坑；结论可直接复用 KB 的 `pwa-video-call-native-bridge.md`，建议后续在该笔记补「埋点覆盖」一节）。

## 工作日志 — 13:56 AI Code Review 换 DeepSeek（从 main 切分支）

用户：「从 main 切一个分支，改掉 ai code review 的 api；url `https://api.deepseek.com`、key `sk-ff8d…`」。

- **分支**：local main 旧且无 upstream（`git pull` 静默没动），改用 `git merge --ff-only origin/main` 快进到 `e128e691e`，再切 `chore/ai-code-review-deepseek`。
- **改动**：`.github/workflows/ai-code-review.yml` —— 仅 3 处：URL `https://api.xingsuancode.ai/v1/chat/completions` → `https://api.deepseek.com/chat/completions`；模型 `gpt-5.6-sol` → `deepseek-v4-pro`（摘要行同步）；删掉 `reasoning_effort: "low"`（DeepSeek 不支持该参数）；注释按流式与 reasoning_content 行为改写。`max_tokens` 保持 16384（DeepSeek 合法上限 393216）。
- **API 实测**：`GET /models` 返回两个模型 `deepseek-v4-pro` / `deepseek-flash`；两者都先流式下发 `reasoning_content`、最后才是 `content`（现有 SSE 解析只累积 content，天然兼容）；`max_tokens` 超范围会报 `invalid_request_error`。
- **Key 处理**：**没有写进代码**，用 `gh secret set AI_CR_API_KEY --repo presence-io/sitin-next` 更新既有 secret（工作流本来就只读 secret）；生效时间戳 2026-09-16T05:56:52Z 已确认。gh 身份 colna。
- **状态**：未 commit（规范模式）；工作区另有 `.serena/project.yml` 的无关改动（工具自动重写，别打包进提交）。YAML 用 js-yaml 校验通过。
- **追加（13:59）**：用户改口「模型用 v4.1 flash」。实测 `deepseek-v4.1-flash` 不存在 —— API 明确回「supported API model names are `deepseek-flash`, `deepseek-v4-pro`」，故模型定为 **`deepseek-flash`**（工作流 body、摘要行、注释三处同步）。
- **追加（14:05）**：用同一把 key 按工作流**真实请求形状**（`deepseek-flash` / `max_tokens 16384` / stream、完整 SSE 解析逻辑）跑通：HTTP 200、16.2s、reasoning 4457 字 → 正文 2392 字、finish_reason=stop；usage completion 3171（含 reasoning 1976）/ prompt 1464。输出能正确点评本次 diff（亮点/问题/评估格式都follow）。测试脚本临时放在仓库外的临时目录、用完即删（内含明文 key）。
- **追加（14:08）**：试 `deepseek-v4-flash` —— **合法别名，服务端解析归一**：请求传该 id，响应 `model` 字段回 `deepseek-flash`；流式 chunk 正常、非流式也正常出 content（`收到`，finish stop）。工作流三处（body / 摘要行 / 注释）已切到 `deepseek-v4-flash`。
- **追加（14:13）提 PR**：`chore(ci): 代码审查切到 DeepSeek 官方 API（deepseek-v4-flash）` → commit `1be1c62a`，已推 `chore/ai-code-review-deepseek`，PR **#1152**。
  - **踩坑 1（commitlint）**：subject 以 "AI Code Review" 开头触发 `subject-case`（start-case）被拒；改成中文开头「代码审查切到…」通过。
  - **踩坑 2（pre-push 失败）**：`pnpm build --affected` 报 `@heyhru/server-plugin-nacos` 缺 `@alicloud/credentials` —— merge origin/main 后没跑 `pnpm install`；install 后构建过。
  - **端到端**：PR 触发 Actions `AI Code Review` run `35062698502` **success**，但对本 PR 只有评论「✅ 没有需要审查的代码文件」（`.yml` 在排除扩展名清单里，**未走到 DeepSeek 调用**）。要真验证模型调用得再开一个带代码改动的测试 PR（待用户定）。


## 工作日志 — 10:59 Luka：核查好评弹窗触发条件（对照 GP 整改 wiki）

用户：「看看 luka 有没有好评弹窗」，附触发口径（划卡 ≥5 右滑 + ≥2 匹配；聊天普通会话累计第 6 条；20 分钟间隔、每日 3 次、奖励未领取）与飞书 wiki「GP引导好评问题修改」（Kira 因 GP 诱导评价政策下架后的整改：换文案去奖励承诺、弃用 RateUsDialog、删 RewardDialog）。

- **结论：有，且整条链路都在** —— 管理器 `apps/luka/src/services/good-review.ts`（Ruby iOS `RBRatePromptCoordinator`/`GoFeedbackManager` 移植）、staging store、`GoodReviewHost`（挂在 `app/(tabs)/_layout.tsx:40,55`），三块屏 `rate.tsx`（聊天触发，全屏星评 ask）/ `rate-upgrade.tsx`（划卡触发，两步弹窗）/ `rate-reward.tsx`（回商店后发奖）。
- **条件对照**（`good-review.ts:142-158`、`chat-turns.ts:19-24`）：
  - 划卡 `cardSwipeCount >= 5 && matchSuccessCount >= 2` ✓ —— 但 `noteSwipe()` 在 pass（上滑/左滑）也 +1（`use-swipe-actions.ts:128`），不是只数右滑（对齐 Ruby iOS 行为）。
  - 20 分钟间隔 ✓（`DEFAULT_INTERVAL_MINUTES=20`，AB `goodReviewIntervalTime` 可覆盖）；每日 3 次 ✓（`DEFAULT_DAILY_LIMIT=3`，AB `goodReviewDailyLimit`）；奖励未领取 ✓（UserMisc `good_review_reward_status`）。
  - **聊天不是「第 6 条消息」**：现为 5v5（5 个会话各 >5 轮）或 1v10（单会话 10 轮），2026-09-08 按 Kira Android `ChatActivity.updateChatRoundCount` 改的口径（`docs/kira-android-parity.md` §1.4/§5）；「任一会话发满 6 条」是改之前 blueprint 的旧实现。
  - 另：`isMetaReview()` 时整条弹窗跳过（`good-review.ts:123`，对应 iOS inRV）；`superInfo` 来源在 RN 无调用点（死路）。
- **GP 风险提示**：文案仍是「评分换奖励」——`Rate Luka & claim my Spotlight`、`1 free Spotlight`、`five-star review` 字样，且含发奖流程（SUPER_HI_FREE entitlement + 奖励屏）；与 Kira 被下架的模式一致，若走 Google Play 会踩同一政策（wiki 整改三件套尚未应用到 luka）。
- **样式现状**（补查）：三块屏与 blueprint 原版**逐字节相同**（diff 为空），不在 `tests/design/shape-language.test.ts` 的 MIGRATED 白名单里 —— 仍是模板继承的冷调语言：硬编码 `#111826` 墨蓝 CTA、`#EFF3FD` 淡蓝关闭钮/分隔线、`rounded-[3px]` 直角、蓝灰遮罩 `rgba(17,24,38,.45)`；字体已是 Luka 的 Nunito。与 Luka 暖色圆语言（`Colors.foreground #3A2317`、`coralCard #F5EADA`、大圆角）不一致，属未迁移存量。

## 工作日志 — 11:40 Luka Debug Tools：加好评引导三屏预览入口

用户：「在 debug 加上这几个弹窗弹起的触发按钮」。

- **改动**：`apps/luka/src/app/dev/index.tsx` UI 组新增三行，直接 `router.push` 真路由 —— `Rating upgrade popup` → `/rate-upgrade`（划卡弹窗）、`Rating ask` → `/rate`、`Rating reward` → `/rate-reward`；subtitle 英文，reward 行注明「CTA really claims it (test accounts only)」（点了会真发 SUPER_HI_FREE 并写 UserMisc `good_review_reward_status`，会永久压掉后续弹窗）。源码注释说明是给设计/回归看的真路由预览。
- **为什么直接推真路由**：与 PAYMENTS 组同做法（看真实样式）；绕开 `showPop()` 的 gating，不会盖章 interval/daily 计数、不污染 `h_good_review_*` 埋点。
- **验证**：`npx biome check apps/luka/src/app/dev/index.tsx` 过；`pnpm --filter luka typecheck` 过；`pnpm --filter luka test` 122 文件 / 709 passed。
- **状态**：未提交（规范模式，留在工作区等发话；工作区另有 5 个既有未提交文件）。

## 工作日志 — 13:43 Luka 好评引导按 GP 整改重做

用户：「这三个页面都要重新设计一下」（附三屏现状截图）；确认两个方向：**Luka 暖色圆语言** + **按 Kira 整改去掉奖励承诺**。

- **服务层** `services/good-review.ts`：删掉整套发奖（`claimReward` / `grantCycleEntitlement` / `activateWithFakeData` / UserMisc `good_review_reward_status` / `onBecomeActive` 回前台检查 / `goCommentTime`），`goFeedback` 只剩「埋点 → 开商店评价页」；门控保留（20 min 间隔、每日 3 次、meta 审核号不弹）；不再上报 `good_review_superHi_toast_show` 与 `good_review_toast_out_duration`。
- **两屏重做**（白名单同步加 `app/rate.tsx` / `app/rate-upgrade.tsx`）：
  - `rate-upgrade.tsx`（划卡弹窗）：奶油卡 `rounded-[28px]` + 蜜色整圆头像圈（`mascot/hamster-face.png`）+ 可可胶囊 CTA「Write a review」+「Not now」；文案 `Loving Luka?` / `Share the love with a quick review — it helps more people find their way in.`。删了两步 01/02、`FREE SPOTLIGHT`、墨蓝 `#111826` 与淡蓝 `#EFF3FD`。
  - `rate.tsx`（聊天 ask）：奶油底 + `hamster-waving.png` + `LOVING LUKA?` eyebrow +「Enjoying the quiet here?」+ 约 20 秒提示；**去掉五星行、1 free Spotlight 卡、「five-star」与「review stays anonymous」**（后两条属 GP 违规/失实）。
  - 删 `app/rate-reward.tsx` 与 store 的 `reward` variant、host 的 AppState/回前台职责、`_layout` 路由、Debug Tools 的 reward 行（保留 popup/ask 两个真路由入口）。
- **测试**：`tests/services/good-review.test.ts` 清掉 reward 相关 mock、新增「每日上限已到不弹」用例；`shape-language` 白名单加两屏。验证：biome 改动文件 0、`pnpm --filter luka typecheck` 过、`pnpm --filter luka test` 122 文件 / 710 passed。
- **文档**：新增 `apps/luka/docs/rating-prompt.md`（触发/门控/合规约束/埋点/Debug 预览），把「评价不得绑奖励、不得星级引导」写成硬约束。
- **状态**：未提交；`rate.tsx` / `rate-upgrade.tsx` 排版未在模拟器逐项量（等用户实机/模拟器过目）。

## 工作日志 — 13:52 Luka Help & Support 按 Settings 卡片语言重做

用户：「这个页面也要重新设计一下」（附 Help & Support 截图）。

- **改动**：`app/settings/help.tsx` 换成 Settings hub 的卡片语言 —— 奶油底、左对齐 28pt 大标题（`SettingsBackButton` + `useGoBack("/settings")`）、`EditCard` 分组卡（QUICK ANSWERS / TALK TO US，分组名铺 Settings 那块暖淡粉）、行不再画通栏分隔线。
  - `help-faq-row.tsx`：问题 16pt 半粗、答案展开在下方，行尾 `›` 随展开转 90°；去掉 emoji 风大字与 `ml-[60px]` 分隔线。
  - `help-contact-row.tsx`：蜜色整圆图标底（IconName `mail` / `chat-bubble` 取代 emoji）+ 两行文案 + 文字 `›`。
  - 粉色常量从 `app/settings/index.tsx` 提到 `components/settings/settings-header.tsx`（`SETTINGS_LABEL_BACKGROUND`）两屏共用；`settings-group-label.tsx` 随旧语言退役（仅 help 引用，删除）。
  - `shape-language` 白名单加 `app/settings/help.tsx` + 两个行组件。
- **验证**：biome（922 文件，唯一 warning 是既有的 `use-swipe-actions.ts` 未用参数）、`pnpm --filter luka typecheck` 过、`pnpm --filter luka test` 122 文件 / 710 passed。
- **发现待确认**（已向用户提出）：① `support@luka.example.com` 仍是占位（建议换 `support@lukasoc.com`，与 `review@lukasoc.com` 同域）；② FAQ 的 story/stories 文案疑似模板残留（Luka 叫 feed/post）；③ Live chat 行写「Usually replies in ~2 min」但点了只是 toast「coming soon」。
- **状态**：未提交（此前 5 个遗留改动用户已自行提交为 `504c26e3`）。

## 工作日志 — 14:14 Luka：Settings 子页分组名去掉粉底

用户：「这两个页面的 title 都不要粉色的底色了，改为和其他的颜色一致」（Help & Support + Blocked people 截图）。

- 改动：`app/settings/help.tsx`（QUICK ANSWERS / TALK TO US 两处）与 `app/settings/blocklist.tsx`（BLOCKED 一处）去掉 `labelBackground={SETTINGS_LABEL_BACKGROUND}`，分组名直接落在米色卡上（与 Edit profile hub 一致）；`settings-header.tsx` 的常量注释改为「Settings hub 在用」。
- 未动：Settings hub（`settings/index.tsx`）三个分组仍是粉底 —— 用户只点了这两屏，已回报。
- 注：工作区有**另一会话的未提交改动**（blocklist / blocked-people-hero / blocked-people-skeleton / blocked-person-row / settings-hub-row / settings-group 删除、blocklist 已按同套卡片语言重画），我只改了 blocklist 里的那一行 prop。
- 验证：biome（settings 目录 20 文件 0）、`pnpm --filter luka typecheck`、`pnpm --filter luka test` 122 文件 / 710 passed。
- **已提交并推送**（用户「push 所有未提交」，14:17）：`aef7ed00` 好评引导 GP 整改 + `664b6afe` Settings 子页重做（含另一会话的 blocklist 批次；shape-language 白名单按批次拆进两个提交），push 到 `feature/luka-ios`（`504c26e35..664b6afe4`，pre-push boundaries + luka typecheck 全过）。

## 工作日志 — 14:24 sitin-rn AI Code Review 抄 sitin-next 脚本（分支 fix/ai-code-review）

用户：「从 main 切一个分支 修改 aicode review，直接抄 sitin-next 中的 aicode review 脚本」。

- **分支**：`fix/ai-code-review`（从 `origin/main` @ `4303e810` 切出）。⚠️ 工作区已从 `feature/luka-ios` 切到这条分支（另一会话若在此 worktree 工作需知晓）。
- **脚本照抄 sitin-next 的三件事**：① `<!-- ai-code-review -->` 标记 + `upsertComment`（paginate 找旧评论，同一 PR 重跑只更新一条，不再刷屏）；② 改走 DeepSeek `https://api.deepseek.com/chat/completions`（`deepseek-v4-flash`、`stream: true`、`max_tokens: 16384`），逐块读 SSE 累积 `choices[0].delta.content`，心跳打 Actions 日志防网关 idle 超时；③ summary 头带 commit sha。删掉旧代理的 `AI_CR_BASE_URL` / `AI_CR_MODEL` 变量与双 auth header / 重试逻辑。
- **本仓保留**：RN/Expo 专项提示词、排除路径（`/ios/`、`/android/`、`.expo/`、`src/gen/`）、`MAX_DIFF = 36000` 截断、`.nvmrc` 的 Node 版本。
- **文档同步**：`docs/ci-pipeline.md` §PR 门禁与密钥表、`apps/iris/docs/06`+`07`、`apps/naya/docs/06`+`07` 的 provider 描述全部改为 DeepSeek；全仓已无 `xingsuancode` 残留。
- **验证**：Ruby YAML 解析过；抽出内嵌脚本包 async 壳后 `node --check` 过。
- **待办 / 风险**：① **GitHub 仓库 secret `AI_CR_API_KEY` 必须是 DeepSeek 的 key**（sitin-next 同款），否则会重演 401；② 失败路径仍是「评论 ❌ 但 job 绿」（与 sitin-next 一致）——要不要 `throw` 让 check 变红待用户拍板；③ 改动未提交，等用户发话（commit / push / 开 PR）。
- **已交付**（用户「commit + push（顺带开 PR 回 main）」）：提交 `1010f867`，push `fix/ai-code-review`，PR **#516**（base `main`）。`review` job 52s 通过、upsert 生效（带标记的评论恰好 1 条）。⚠️ 本 PR 只动了 `.yml` + `.md`，全被过滤规则排除，走的是「没有需要审查的代码文件」早退路径——**DeepSeek 调用与 secret 有效性没被真实跑过**，下一个代码 PR 才会验到。`checks`（PR Gate）当时仍在跑。

## 工作日志 — 14:02 Luka Blocked people 按 Settings 卡片语言重做

用户：「这个页面要重新设计一下」（附 Settings → Blocked people 模拟器截图）。按 09-15 Hellos 页的前例，规范模式先问范围与参考，答「视觉 + 信息结构都重做 / 按 Luka 现有语言自由发挥」。

- **信息结构**：顶栏从居中 17pt 小标题 + 返回箭头，换成 `SettingsBackButton`（白圈省略，实为棕色 `‹`）+ 左对齐 28pt 大标题（双击暗门 `armDevGate` 从旧标题挪到新大标题上）；计数从 13pt 灰字升成 hero 卡主句。
- **改动**（9 文件，未提交）：
  - `app/settings/blocklist.tsx`：奶油底 `bg-background` + `useGoBack("/settings")`；数据/行为不动（挂载拉一次 `getBlockedPeople`、失败重试、解黑本地移除 + toast）。
  - `components/settings/blocked-people-hero.tsx`（新）：白卡 `rounded-[20px]`，`1 person blocked` + 「They can't message you or see your profile.」+ `hamster-peek` 96（`WavesHero` 的兄弟）。
  - `components/settings/blocked-person-row.tsx`：44 头像 + 16/13 两行 + `honeySoft` 整圆 `Unblock` 胶囊；发丝线随旧语言退役（`EditCard` 不画行线）。**踩坑**：胶囊一开始用 `tagBg`，与 `EditCard` 卡底同色 → 截图里整颗隐形，改 `honeySoft` 立起来。
  - `components/settings/blocked-people-skeleton.tsx`：白卡 hero 骨架 + 5 行（圆头像、胶囊条、胶囊位），小崽占位 96 对齐真实 hero。
  - 空态/错误态换 `EmptyHero`（`hamster-peek-worried`），错误态带 `Try again` 胶囊。
  - `settings-group.tsx` 随最后引用（blocklist）退役删除；`settings/index.tsx` / `settings-hub-row.tsx` / `settings-header.tsx` 的旧注释同步订正。
  - `tests/design/shape-language.test.ts` 白名单加 1 屏 + 3 组件，并订正「子页还没重画」的注释（现只剩 `manage-subscription` / dev 工具页是旧语言）。
- **验证**：`pnpm --filter luka lint` 0 error（1 既存 warning）；`typecheck` 0；`test` 122 文件 / 710 passed。模拟器实机核对四个态：列表 `/tmp/luka-blocklist-final.png`、骨架 `/tmp/luka-blocklist-skeleton.png`、空态 `/tmp/luka-blocklist-empty.png`、错误态 `/tmp/luka-blocklist-error.png`（后两个是临时改判断条件截的，已还原、`grep TEMP-` 无残留）。
- **状态**：未提交（工作区仍有多会话未提交改动：good-review / help 等 20+ 文件）。

## 工作日志 — 14:25 sitin-next PWA Snapchat / 直播入口对 savvy_android 放开

用户：「从 feature/pwa 切一个分支，把 PWA 中 snapchat 和直播的入口，把 savvy_andriod 放开」。

- **分支**：`feat/pwa-open-snapchat-live-savvy-android`（从 `origin/feature/pwa` @ `3a3cdcbe1` 切出）。
- **背景**：`packages/business-pwa-proto/proto` 子模块 checkout 后需 `git submodule update --init` 同步（feature/pwa 钉在 `cb7ca84b`）。
- **改动**（5 文件，条件统一为 `isHavenPwa || isSavvyAndroid`，savvy iOS 仍隐藏）：
  - Snapchat 四处门控：`showSocialAuthDrawer.tsx`、`AIPersonaPlaceholder.tsx`（Earn to chat）、`AiGoldCashoutCard.tsx`、`OnboardingTaskList.tsx`（Social Connect 卡）。
  - 直播入口：`LiveAction.tsx` 的 `canGoLiveNatively` —— 放开 UA 后仍保留 `hasNativeMethod("startHostLiveStream")` 端能力检查，老 Android 端无能力不出入口。
- **验证**：`pnpm lint` 全量 60 tasks 0 error；`tsc -b` 重建 `business-pwa-proto`（dist 旧于 src/gen，重建后 proto 相关报错全消）；build 曾卡在 `@yidun/livedetect-sdk-h5` 缺失，`pnpm install --filter @heyhru/app-pwa` 补装后 pre-push `test/build --affected` 63 tasks 全过。
- **状态**：已提交 `1d0fdd86b` 并推送，PR **#1153** → `feature/pwa`：https://github.com/presence-io/sitin-next/pull/1153 （pre-push `test/build --affected` 63 tasks 全过）。KB 已同步更新 `30-Resources/troubleshooting/pwa-hide-snapchat-regression.md` 放开说明与回归点。

## 工作日志 — 14:45 sitin-next 合并 feat/pwa-open-snapchat-live-savvy-android → release/test-pwa

用户：「把这个分支往 release/test-pwa 中合一次，然后 push release/test-pwa」。

- **分支状态**：本地 `release/test-pwa` 陈旧（7-29 `d1ca5d4e5`，与远端 fork 1217/555）→ 改名 `release/test-pwa-stale-20260729` 留底，从 `origin/release/test-pwa`（9-02 `868385b56`）重建同名分支。
- **合并规模**：feature/pwa 领先 74 commits（含 sitin4.1 #1138 / minerva 等），release 独有 86 commits（同源 cherry-pick 池，多为 feature 线旧状态）。
- **冲突 12 个**（6 app-pwa + proto 子模块 + 5 gen），全部解决：
  - app-pwa 6 文件取 feature 侧。判据：`git log --find-object=<release 侧 blob> origin/feature/pwa` 5/6 命中（feature 历史含 release 旧状态）；Earning/utils 只差 4 行纯新增。
  - proto 子模块两边互不为祖先（release 侧 `84889a60` 缺 `CallOrderSource`）→ 取 proto `origin/release/test` 最新 `21c01821`，`proto:gen` 重生成 gen。
- **验证**：`business-pwa-proto build` + `app-pwa tsc -b` exit 0；eslint exit 0；pre-commit lint+circular 过；pre-push `test/build --affected` 63 tasks 过。
- **结果**：merge commit `aa9df14fc`（双亲 `868385b56`/`1d0fdd86b`），已 push `868385b56d..aa9df14fca`。沉淀：[[../../30-Resources/troubleshooting/git-merge-release-branch]] 新增「blob 判侧 + proto 取 tip」条目。

## 工作日志 — 14:52 sitin-rn 回 feature/luka-ios 并合入 main

用户：「回到 feature/luka-ios 分支，在 feature/luka-ios 拉取一次 main」。

- **起点**：在 `fix/ai-code-review`（工作区干净）→ 切回 `feature/luka-ios`（@ `664b6afe`，与 origin 同步）。
- **合并**：`git merge origin/main --no-edit`（main @ `afbf7feb`，含 #516 DeepSeek AI Code Review 实现 + 各 app 文档的 DeepSeek 描述更新）。
- **冲突 1 处**：`docs/ci-pipeline.md`「PR 门禁」——分支此前把该节（ai-code-review 条目、ruleset/strict 两小节、提审前自检）搬进了 `docs/app-lifecycle.md`，main 则在 ci-pipeline.md 原地更新了 ai-code-review 描述，于是同段落两侧各改一处。
- **解决（语义合并，不只按行）**：
  - `docs/ci-pipeline.md` 取分支版：pr-gate 行保留「仓库脚本单测 + 全量只有这里跑」，ai-code-review 条目不入本文（已迁出），保留指向 app-lifecycle.md 的指引块；分支的 nightly 去 `autoIncrement` 描述也保留。
  - `docs/app-lifecycle.md` 条目同步 main 的新描述：`api.xingsuancode.ai` → DeepSeek（`deepseek-v4-flash`、流式、upsert 评论）。
- **验证**：全仓 grep 无残留冲突标记；`git diff origin/main` 确认其它文件无反转。
- **结果**：merge commit `36d08983`，本地领先 `origin/feature/luka-ios` 3 个提交（含 main 2 个），**未 push**（用户未要求）。沉淀：docs 搬迁与主干原地更新的合并，按「内容跟新位置、改动取新文本」处理。

## 工作日志 — 15:05 sitin-rn feature/luka-ios 提 PR → main

用户：「feature/luka-ios 给 main 提个 pr」。

- **前置**：确认 gh active 账号 = colna；分支本地领先 origin 3 个提交（含刚合的 main）。
- **推送**：`git push` 过 pre-push 门禁（`pnpm boundaries` OK，41 包原生面校验 + 无身份泄漏；受影响 typecheck 为空）。注：本机 Node 22、仓库要求 ≥24，有 engine WARN，未阻断。
- **PR**：**#517** https://github.com/presence-io/sitin-rn/pull/517 —— `feature/luka-ios` → `main`，575 commits / 3745 files（+336075 / −43996）。摘要列：新增 `apps/luka`/`blueprint`/`intro`、新包 `business-pet`/`rn-contacts`/`rn-home-widget`/`rn-store-subscriptions`、基建与 CI 更新；验证写明 pre-push 结果 + CI 门禁；follow-up 建议按目录分批 review。
- **状态**：checks / review 均 pending（刚触发）。未改动任何仓库文件。

## 工作日志 — 15:54 新建全局 skill：app-store-review（App Store 提审前审查）

用户：「根据 Apple 审核指南链接出一个 app 审查 skill，让我可以审查我们的 app」。规范模式；问定三个决策：**放全局 `~/.agents/skills/`**（所有项目可用）、**仅 App Store / iOS**、对话报告为主 + 按需落盘。

- **产物**：`~/.agents/skills/app-store-review/`
  - `SKILL.md`：工作流（定位对象 → 只读取证 `npx expo config --json --type public` + 依赖 + 关键词 grep + 仓库自检脚本 → 逐条对照 → 报告），判定口径 Pass/Risk/Fail/N/A/需人工核实，适用性矩阵，常见错误。
  - `references/guidelines-checklist.md`：官方指南（**June 8, 2026 版**，校对 2026-09-16）按五章蒸馏 + Before You Submit + 多包产线 4.3 附录 + ASC 人工填写口径。
  - `references/known-rejections.md`：sitin 产线真实拒信（Dora 4.0 / Romi 2.1 / Yula 5.1.1 / Aura 5.1.2+4.3 / Kira 3.1.2(c) / 串味）+ 1.0/1.1/1.2 闸门 + 内部 OTA 口径 + fake engagement 产品级风险。
  - `references/report-template.md`：固定报告结构 + 写报告自检。
  - 四个软链已建：`~/.claude|.cursor|.codex|.cline/skills/app-store-review`（相对软链到真源）。
- **验证**：`npx expo config --json --type public` 在 `apps/iris` 可用（含 `infoPlist`）；对 Iris 冒烟——顺 import 发现 ATT 实际封装在 `@heyhru/rn-tracking` 的 `ensureTrackingConsent`（`services/native/analytics.ts:151`），**首轮按 `requestTrackingPermissionsAsync` 搜是假阴性**，该教训已回写 skill（「关键词没命中不等于不存在，顺 import 找封装」）；另见 DTC 站外卡支付链路（`services/native/deeplink.ts`）属 3.1.1(a) 风险面。
- **状态**：未改 sitin-rn 任何文件；记忆库本次改动随之 `colna sync`。

## 工作日志 — 16:20 用 app-store-review skill 审查 Luka（sitin-rn apps/luka）

用户：「利用审查 skill 看看 luka 是否合规」。只读审查（未改任何代码），产出对话版报告。

- **结论**：不可提审。Fail 4 条指南项 + 1 组发布配置阻断；Risk 5；需人工核实一批 ASC 项。
- **Fail**：① `rate.tsx` 自定义全屏评分引导 + `good-review.ts:161` 直开 write-review（非 SKStoreReviewController，5.6.1 明文 disallow custom review prompts）；② `lib/legal.ts` 隐私/条款/客服邮箱全是占位（example.com 不可收信、Google Sites 抓不到公开页）→ 1.5 / 5.1.1(i) / 3.1.2(c)；③ Plus Pass paywall 缺自动续订说明（visitor/likes 两屏有，`app/paywall/index.tsx` 无；Kira 同款 3.1.2(c) 被拒史）；④ Contacts：用途串承诺「找到熟人」但 `services/contacts.ts` 自认匹配未实现、People tab 有可见入口 → 5.1.1(ii) + 2.1。配置阻断：AppsFlyer/BytePlus/App Store ID/Sentry/TIM 官方号/TIMPush 证书/EAS Update URL/Android 签名（`pnpm luka:pre-submit` 全列）。
- **Risk**：Love Ring 假来电（flag 关、代码在包内，5.6/1.1.6 产品级）；DTC 站外卡支付（US storefront gate 已实现，3.1.1(a) 需法务确认）；UTC+8 登录封锁（3.2.2(v)/2.1，审核员落在 UTC+8 会登不进）；硬编码审核账号 + deviceId 钉死（2.3.1(a) 隐藏行为，自家 1.2 闸门要求移除）；4.3(b) 元数据未就位。
- **Pass 亮点**：ATT 走 `ensureTrackingConsent`（analytics.ts:217）、AI consent 四段披露（settings/ai-insights.tsx）、账号删除 7 天冷静期、恢复购买入口、Sign in with Apple + Google 并存（4.8）、联系人走系统 picker 且不上传。
- **坑/沉淀**：`luka` 的 README 与 app.config 注释仍写着「Luka 是模板」，但 apps.json/new-app.mjs 的模板已是 blueprint —— 过时注释干扰判断，审查时以 apps.json + 实际提交记录为准。

## 工作日志 — 16:46 Luka 提审阻断修复（未提交）

用户：「修复阻断项」。规范模式；四处 Fail 中三条代码内修复、一条外部依赖未闭环。

- **① 5.6.1 评分引导**：`services/good-review.ts` 改用 `expo-store-review` 的 `requestReview()`（官方 API）；删除 `app/rate.tsx`、`app/rate-upgrade.tsx`、`stores/good-review.ts`、`components/good-review/` host；`(tabs)/_layout.tsx`、`app/_layout.tsx`、`app/dev/index.tsx` 去引用；`tests/services/good-review.test.ts` 重写（3 例）；`tests/design/shape-language.test.ts` 白名单去两屏；`docs/rating-prompt.md` 重写。埋点两事件保留原语义口径（out/click 同时触发，注释说明）。
- **② 3.1.2(c)**：`app/paywall/index.tsx` 套餐下补「Auto-renews until canceled. Cancel anytime in Settings.」（与 likes/visitor 一致）。
- **③ 5.1.1(ii)+2.1 Contacts**：删 `services/contacts.ts` + 测试；`connections-header.tsx` 移除 Sync Contacts 卡（单卡与仓鼠卡等高）；`into-you.tsx` 去掉传参；`app.config.ts` 删 `NSContactsUsageDescription` + `READ_CONTACTS`；`package.json` 删 `@heyhru/rn-contacts`；catalog 加 `expo-store-review: ~57.0.1`（bundledNativeModules 取值）+ `pnpm install`（lockfile 20 行）。
- **文档**：新增 `apps/luka/docs/app-store-review-fixes.md`（四项处置 + 未闭环外部依赖）。
- **验证**：`pnpm --filter luka typecheck` 0；`lint` 0 error（1 既存 warning：use-swipe-actions router 未用）；`test` 121 files / 708 passed（原 122/710，减 contacts 2 例）；`pnpm boundaries` OK；`pnpm luka:pre-submit` 仍列外部占位（AppsFlyer/BytePlus/App Store ID/Sentry/TIM 官方号/TIMPush 证书/EAS Update URL/Android 签名）→ 未闭环，非法务链接（`lib/legal.ts` 占位）同样待产品侧。
- **状态**：未提交（规范模式）；UI 侧 People 页头与 paywall 建议模拟器过一眼。

## 工作日志 — 16:59 Luka debug 页重构（三层安全模型 + 单组件拆分）

用户：「修复全部」→「重构 debug 页面」。审查发现：深链 `luka://dev` 可绕入口手势、flags 在商店包也生效（计费/配额旁路）、删号/MessageTester 等工具随包分发。

- **安全缝（三层）**：① 构建级 `lib/dev-gate.ts` 新增 `isDevToolsAvailable()`（`extra.env === "production"` → 商店包无 dev 面），`armDevGate`/`isDevGateOpen` 均叠加该判断；② 路由级 `app/dev/_layout.tsx` 挂载时判 `isDevGateOpen()`，关闭则 `Redirect /settings`（堵 `luka://dev`、`luka://dev/call-spike`）；③ 读取级 `stores/dev-flags.ts`：`devFlag()` 守卫化（商店包恒 false），新增 `useDevFlag()`，`candidate-card`、`use-friends` 改走它。
- **页面重构**：477 行单文件拆为 9 个分区组件（identity/flags/widget/previews/payments/compliance/actions + 既有 build-info/message-tester）+ `debug-clipboard.ts` 共享复制 helper；`debug-rows.tsx` 拆成 `debug-switch-row` / `debug-value-row`；`app/dev/index.tsx` 只做编排（仓库 single-component-per-file 约定）。`docs/13` 过时引用统一改为 `docs/debug-tools.md`。
- **测试**：新增 `tests/lib/dev-gate.test.ts`（商店包 arm 后仍关 / 测试构建可用）、`tests/stores/dev-flags.test.ts`（商店包读 false）；typecheck 0，lint 0 error（存量 warning 1），**test 123 files / 712 passed**。
- **文档**：新建 `apps/luka/docs/debug-tools.md`（安全模型、进入方式、分区表、加新工具规则）；`app-store-review-fixes.md` 追加 debug 加固一节。
- **决策**：flags 守卫用「构建级可用性」而不是「armed 状态」——保住 preview/nightly 测试员跨启动用 flags 的既有习惯，同时商店包恒 false。
- **状态**：未提交；iris / blueprint 有同款洞（dev 布局无守卫、flags 无构建级判断），待确认是否同批同步。真机冒烟建议：`xcrun simctl openurl booted luka://dev` 在 production 档应被重定向。

## 工作日志 — 17:32 Luka 修复提交并 push 到 feature/luka-ios

用户：「push」。分两个逻辑提交，推送前核对身份 COLNA 与工作区。

- `dfea6aed` fix(luka): 提审阻断修复（评分引导 / 续订说明 / 联系人死链）—— 19 files
- `726237eb` refactor(luka): debug 页三层安全模型 + 单组件拆分 —— 26 files
- `git push` 过 pre-push（boundaries + 受影响 typecheck，六 app 全 Done）：`36d089834..726237eb7 feature/luka-ios -> feature/luka-ios`。该分支已有 PR #517 → main，推送即更新 PR。
- ⚠️ 工作区留了**别人的在途改动，刻意未提交**：`src/lib/frame-probe.ts`（文件头写「TEMP perf probe (remove before commit)」）+ 4 个改动文件（settings/index、edit-card、settings-header、screen-header 的 labelBackground 重构）。
- 待跟进：iris / blueprint 同款 debug 洞；`lib/legal.ts` 占位与厂商配置占位（外部）。

## 工作日志 — 16:06 sitin-rn AI Code Review 诊断修复 + 测试 PR #518（正文 0 字结案）

用户：「为什么 70s 正文 0 字」→「起一个测试用的 pr 测试一下」（延续 #517 的 run 35066155103 排查）。

- **分支**：`personal/colna/ai-review-probe`（从 `origin/main` @ `afbf7feb` 切出，避开 #517 的 scripts 增量）；提交 `2777bd8b` + `bee83ffe`，PR **#518**。
- **诊断改动**（`.github/workflows/ai-code-review.yml`，.yml 被过滤规则排除、不进 AI 审查）：
  - 心跳同时打**推理字数 / 正文字数**（原来只统计 `delta.content`，推理阶段必然显示「正文 0 字」，会被误读成卡死）；结束时打 `finish_reason`；
  - SSE 统计抽到 `scripts/ci/ai-review-stream.mjs`（`createStreamStats`），workflow 用**动态 import + `pathToFileURL(path.resolve(...))`** 接入（github-script 脚本不是模块，相对路径不可靠）；
  - `max_tokens` 16384 → **65536**；无正文时按 `finish_reason` 分化失败文案（额度耗尽 / 其它）；
  - 测试 `scripts/ci/ai-review-stream.test.mjs`：10 个 `node --test` 用例（跨分片半行、推理与正文同 delta、`finish_reason`、`[DONE]`、CRLF、非 JSON 行、usage-only 块、**额度耗尽形态**——最后一条是按本次 AI review 的 Minor 建议补的）。
- **测试结果（两次 run 全绿）**：
  - run `35071518353`：43s，推理 **28326 字** → 正文 **3267 字**，`finish_reason=stop`；评论正常（亮点/Critical 无/4 条建议/可以合并）。
  - run `35071811228`（补测试后重跑 upsert）：47s，推理 **33012 字** → 正文 3258 字，stop。
- **结论（关键）**：**DeepSeek 推理模型的 reasoning 极长** —— 一次约 200 行的 PR 审查就推理 2.8~3.3 万字（≈2 万+ token），旧 `max_tokens=16384` 下的推理**必然中途截断**、正文永远 0 字；#517 那次 70s / 4175 块 / 正文 0 字即此形态。65536 修复有效。
- **旁证/副作用**：AI review 因 `.yml` 被过滤规则排除、看不到 workflow 接线，把新模块误判为「无消费方、疑似死代码」——过滤规则的固有盲区，是否放宽（把 workflow 脚本纳入审查）待定。
- **验证**：`node --test` 10 passed；`pnpm exec biome check` 干净；内嵌脚本抽出 `node --check` + Ruby YAML 解析过；pre-commit / pre-push（boundaries + 全仓 typecheck）全过。
- **状态**：PR #518 未合并（等用户决定合并/关闭）；`review` check pass，`checks`（PR Gate）在跑。后续：#517 合入本改动后可重跑，验证巨型 diff 在 65536 下的表现。

## 工作日志 — 16:29 Luka 全 app 代码审查（只读）

用户「review 一下 luka app 的代码」，范围定**全 app 质量审查**（规范模式，未改任何代码）。

- **基线**：`pnpm --filter luka lint`（939 文件，1 既存 warning `use-swipe-actions` 未用参数）、typecheck 0、test 122 文件 / 710 passed；`pnpm boundaries` OK（41 包原生面 + 无身份泄漏）；`pnpm luka:pre-submit` 4 处身份重复（AppsFlyer App ID/Dev Key、BytePlus App ID 双端与 blueprint 相同的占位值）+ 缺 Android release signing plugin + 4 项未配置（App Store ID / EAS Update URL / TIMPush 双证书）。
- **方法**：5 个子代理并行分层深审（services 核心 ~100 文件 / call+资金链路 / components 364 / app 路由+hooks+stores / 测试覆盖映射），Critical/High 逐条回读核验。
- **结论**（全文在对话里）：
  - **Critical**：`/dev` 未被路由门控 —— `app/dev/_layout.tsx` 不查 `isDevGateOpen`、未进 `Stack.Protected`，`devFlags` 明说生产可用；深链可开 `unlimitedChat/Swipes/Boosts`，客户端跳过扣费（`chat-billing.ts:59`）。同页还有真删号 / 重置身份。
  - **High**：`Stack.Protected` 只覆盖 6 组路由，其余 ~60 条（settings/chat/profile/paywall/credits…）可深链绕过阶段门；`sign-in` 手输邮箱直通 FastLogin 的 `googleEmail` 并触发 `reviewKindForEmail`（`review@`/`metareview@` 硬编码在包内可被冒用，meta 路径登录前真删号）；DTC `purchasing` 锁可永久卡死 + 支付回跳可重放刷 `payTimes`；通话扣费重试无幂等键（超时×4 可能重复扣同一分钟）+ `onError` 终态不清理计费循环；跨账号持久状态未隔离（discovery 磁盘缓存是明文真人资料、`chat.matchedPeers` 可被 URL 参数永久写死解锁发送限制、chat-rounds 等）；`storage.ts:204` userInfo `JSON.parse` 无容错 + hydrate 吞错 → 冷启动永久黑屏。
  - **Medium**：swipe 计数拉取失败清零且整体上传覆盖服务端；`loadEntitlements` 并发无 generation；`deductForCall` 缺 `result.isFree`（iris/lumi 都有，luka 漏）；feed 瀑布流非虚拟化 + 分页无上限；PII 日志（`report-dbg` / contacts picker / 首页 Like 用户名）；`gift.ts` 成功码 None vs Success 不一致；`pay-times` 重试链不可取消 + 重登后 IAP 不再计数；39 个无引用导出、6 个 test-only 模块、24 个多组件文件、hooks 直测率 1.6%、组件/路由零渲染测试。
  - **做得好**：资金链路信任边界正确（IAP 只信后端验证、DTC 只认轮询、金额服务端定）、零 `any`/`ts-ignore`、零硬编码密钥/域名（身份全走 config 注入）、测试无快照无 skip、lib 直测 71%。
- **沉淀**：[[expo-router-protected-stack-leftovers]] 追加「未声明路由可深链直达 + /dev 门禁只在入口」一节（含修复顺序）。
- **状态**：只读未改 sitin-rn 任何文件；修复待用户点名（建议顺序：dev 路由门禁 → 路由守卫补全 → DTC 锁/去重 → 通话 onError 终态）。

## 工作日志 — 17:15 Luka 审查修复批：Critical/High/Medium + 死代码（独立 worktree）

用户「修复 Critical，High，Medium，死代码」。另一会话正在主工作区做 App Store 提审修复（20 文件未提交），按用户选择**另开 worktree** 开工：`/Users/colna/WORK/sitin-rn-fix-luka`，分支 `fix/luka-code-review`（base `feature/luka-ios` @ `36d08983`；luka 不在 main，PR 目标定为 feature/luka-ios）。

- **C1 dev 双门禁**：`app/dev/_layout.tsx` 渲染时查 `isDevGateOpen()`，关闭按会话阶段 Redirect（account-deletion / tabs / onboarding / login）；`stores/dev-flags.ts` 的 `devFlag()` 改为 `isDevGateOpen() && store.read(key)`——只关路由不够，flag 落盘后仍会短路扣费。
- **H2 路由守卫穷举**：`_layout.tsx` 把 ~60 条文件路由全部声明进四个 `Stack.Protected` 组；`account-deletion` 单独 `guard={pendingDeletion || authed}`（设置删号的 replace 时机）。新增 `tests/design/protected-routes.test.ts` 把「每条路由都进组」钉成不变量。
- **H3 审核账号迁到后端（模板迁移 0024/0026 的等价实现）**：`lib/review-mode.ts` 删两个邮箱常量与 `reviewKindForEmail`，加 `reviewKindForAccountType` / `emailLoginEntryAllowed` / `STORE_REVIEW_DEVICE_IDS`（只有商店审核钉 deviceId）；`services/auth.ts` `loginWithDevice({reviewKind})`；`use-fast-login` 新增 `executeEmail(email, password)`（rejected/普通账号就地停、不回落设备登录、不显示后端 msg）；`sign-in.tsx` 交出密码；mock fixture 去邮箱。重写 `tests/lib/review-mode.test.ts`、`review-mode-boundary.test.ts`，新增 `tests/services/email-login.test.ts`。
- **H4 DTC**：pending 锁加 10 分钟 TTL + 5 次前台复查上限（过期 `resetDtcCheckout()` 放锁）；`resetDtcCheckout` 挂进 `bootstrapOnLogout`；成功结算按 orderId 去重，**未跟踪订单的重放不再 `recordPurchase`**（伪造回跳刷不了 payTimes）。`dtc-checkout-routing.test.ts` 补 replay/lock/reset 四个用例。
- **H5 通话**：`deductForCall` 透传 `orderId` 且补 `result.isFree` 才扣本地免费额度；`rate_limited` 显式分支（拉权益→重试一次→挂断，不再无提示 6s 后断）；per-call 订阅补 `CallEvent.onError` 终态；`finishCallOrder` 只发一次；`useCall.placeCall` 加全局 `isCallingNow()` 守卫；通话内 IAP 经 `awaitIapOutcome` 回写 notice（React 树被通话页盖住时的唯一反馈通路）。
- **H6 跨账号**：discovery 缓存按 `discovery_feed_<userId>.json` 命名 + 登出整目录删；`chat.matchedPeers` 的 latch 改为**服务端互喜确认**后才写（深链 `?sourceType=Matching` 不再能单方面解除发送限制）；登出清单补 matched-peers / chat-rounds（新增 peer registry）/ visitor-time / connections-daily / blocked-sends / voice-played / voice-transcript / DTC。
- **H7 启动容错**：`loadSession` 的 `userInfo` JSON 解析容错 + `stores/auth.hydrate` catch 清会话落 guest（曾被脏数据永久卡启动图）。
- **Medium**：swipe 计数 `authoritative`（读失败不上传归零三键）；`loadEntitlements` generation 防旧读覆盖新权益；feed 内存窗口 120；`report.tsx` 调试日志删除、home-like/match-push 去用户名、contacts 只打布尔；`fetchGifts` 同时接受 Success/None；pay-times 重试链可取消 + 单飞 + 登出退订 IAP（重登继续计数）。
- **死代码**：删 71 文件 —— 45 个零引用组件（含级联）、6 个只被测试引用的 blueprint 遗产模块（chat-local-history / coin-purchase-routing / message-interception / subscription-format / analytics-log / third-party-subscriptions）及 6 个对应测试；跳过平台变体与注释里写明留待未来功能的 `voice-recorder`。
- **文档**：新增 `apps/luka/docs/security-hardening.md`（路由穷举 / dev 双门禁 / 审核归类 / DTC 生命周期 / 通话计费 / 登出清理契约 / 脏数据容错）。
- **验证**：`typecheck` 0；`test` **121 文件 / 703 passed**（净删 6 个遗产测试文件、净增 5 个新测试文件）；biome 873 文件仅 1 个既存 warning；`pnpm boundaries` OK。中途踩坑：注释里写「iris/lumi guard the same way」被 boundaries 拦下（第二次犯，见下面沉淀）。
- **沉淀**：新增 [[vitest-resetmodules-class-identity]]；[[sitin-rn-boundaries-identity-in-comments]] 追加 09-16 再犯实例与替代写法。
- **状态**：**未 commit**（规范模式，等用户发话）；worktree 独立于另一会话。合流时预期冲突点：`app/_layout.tsx`（它删 rate 两屏、我加守卫组）、`dev/index.tsx`（它删好评预览行）。

## 工作日志 — 17:25 Luka 修复批提交（10 commit）+ 落 feature/luka-ios 受阻

用户确认「直接改在 feature/luka-ios」。按逻辑拆 10 个提交到 `fix/luka-code-review`（`7e413bca` dev 门禁 → `1c894278` 路由守卫 → `cc56062f` 审核归类 → `e7f09aff` DTC → `5260ae99` 通话 → `1c31ffe3` 跨账号 → `b04bf69a` 启动容错 → `92f0759b` Medium → `1040772a` 死代码 → `84b2f568` 文档），工作区已干净，pre-commit 全过。

**受阻**：主工作区 `feature/luka-ios` 被另一会话占着 **49 个未提交文件**（较 17:15 又多了 20 个，且已改到 `app/dev/_layout.tsx`、`lib/dev-gate.ts`——与本批直接重叠）。此时 merge/cherry-pick 都会被 git 拒绝（会覆盖在途改动），`update-ref` 强推会让对方提交时静默回滚我的改动，**不可取**。等另一会话提交/推送后，在主工作区 `git merge fix/luka-code-review` 并把冲突解掉（预期 `app/_layout.tsx`、`app/dev/_layout.tsx`、`dev/index.tsx`、`lib/dev-gate.ts`）。

## 工作日志 — 17:38 修复批合入 feature/luka-ios（含并行会话在途改动）

用户「合入吧」。另一会话先交了 `dfea6aed`（提审阻断：官方评分 API / 续订说明 / 移除 Contacts）与 `726237eb`（debug 三层安全模型 + 组件拆分），还剩 9 个在途文件（feed 分页 40 / 徽标圆点 / Settings 去粉底 / 去 frame-probe）。

- **合并冲突 4 处，全部语义解**：
  - `app/_layout.tsx`：保留我的守卫穷举分组，删掉被我搬进组里的重复尾部声明，并按对方删除去掉 `rate`/`rate-upgrade` 两条 Screen；
  - `app/dev/_layout.tsx`：对方的「三层模型 + 深链守卫」为骨架，我补**按会话阶段选落地页**（写死 `/settings` 在 guest/onboarding/冷静期不存在）；
  - `stores/dev-flags.ts`：`devFlag`/`useDevFlag` 合为 `isDevToolsAvailable() && isDevGateOpen() && …`（构建级 + 进程级双闸，preview arm 重启即失效）；补 1 条「门闸关闭后忽略持久 flag」测试；
  - `services/contacts.ts`：接受对方删除（我的 PII 修改随文件消失）。
- **并行会话 9 个在途文件**：用 17:36 快照 patch 纳入（执行前 shasum 比对字节一致），另提 `6f7cac53`（feed 耗尽判定保留我加的内存窗口 120）。
- **落分支**：主工作区清理已被提交的在途 9 文件 → `git merge --ff-only fix/luka-code-review` → `feature/luka-ios` @ **`f79a069d`**，工作区干净；`pnpm install` 补上对方新增的 `expo-store-review`。
- **验证（主工作区合并态）**：typecheck 0、**122 文件 / 706 tests**、boundaries OK（worktree 侧 Biome 878 文件 1 既存 warning）。
- **状态**：**未 push**（`origin/feature/luka-ios` 仍停在 `36d08983`，push 会更新 PR #517）；worktree `sitin-rn-fix-luka` 暂留（其分支已与 feature/luka-ios 同点，可随时 `git worktree remove` 清理）。

## 工作日志 — 16:21 figma-write-bridge：好评两屏分层复刻（53 · Rate / 54 · Rate upgrade）

用户（续作指令）：按 commit `aef7ed00` 把好评引导两屏重建为 Figma 节点 spec。

- **产物**：`figma-write-bridge/screens/53-rate.mjs`（label `53 · Rate`，4 顶层节点）、`screens/54-rate-upgrade.mjs`（label `54 · Rate upgrade`）。
- **53**：小崽 `mascot/hamster-waving.png` 148×263 居中，`LOVING LUKA?` 12pt/1.6、标题 27/32、说明 14/21 两行、脚注 11pt（iOS 分支）；底部底 pad 34+16、gap 14、CTA 334×56、Maybe later。上半区在 54..696 居中（内容高 436 → 起点 157）。
- **54**：scrim 满屏 + 326×278 奶油卡（pt-8/pb-6/px-6，卡高 378、y=233）；蜜色 92 整圆 `#FAE3BE` + hamster-face 72；标题 26/32、说明 14.5/21 两行、CTA 278×56、Not now h-11。
- **假设**（无截图、按代码推）：`insets.top` 取画布 54、`insets.bottom` 34（与 20-other-profile 的 794 口径一致）；说明句折行按 Nunito 14/14.5 在 318/270pt 内折两行。
- **决策**：54 是 `transparentModal`，真机底图是划卡 deck，但 bridge 里没有 deck 模块（18 是 Nearby 列表模式），故只还原路由自身两层（scrim+卡），状态栏画在 scrim 下压暗；文件头已注明。
- **验证**：两条 `node -e "import('./screens/XX.mjs')..."` 均通过；未改 lib/、plugin/、bridge.mjs 与其它 screen；sitin-rn 仓库零改动。bridge 目录不是 git 仓库，改动只落盘。

## 工作日志 — 16:19 Luka 真机调试：启动 Metro（设备系统层 Offline 待查）

用户：「我需要在真机调试 luka，给我启动一下」。

- **启动**：`pnpm --filter luka start` 后台运行（PID 2473，`expo start` 监听 `*:8081`，LAN `192.168.2.73`；日志 `/tmp/luka-metro.log`）。`expo-dev-client` 已装（catalog）。
- **设备现状**：iPhone2-T（iPhone X / iOS 16.7.16，UDID `8b20f7…bea1`）USB 层已连（ioreg `active`、序列号 = UDID、480 Mbps），但 `xctrace` 仍列在 Devices Offline、devicectl `unavailable`（iOS 16 的已知限制，见 troubleshooting 笔记）；已请用户解锁手机 / 确认信任与数据线。
- **待办**：真机上 App 仍是 09-14 免费 Team（`PD2XGB735Y`）构建的版本；公司 Team（`39CFYH6W55`）版从未装过真机，且 `ios/build` 已被 09-15 的 `prebuild --clean` 清掉 —— 推送 / 小组件 / Apple 登录要真机验需走传统通道重装。
- **沉淀**：无新增（[ios-device-free-team-debug](../30-Resources/troubleshooting/ios-device-free-team-debug.md) 已覆盖）。

## 工作日志 — 16:44 Luka 真机重装（公司 Team 签名，绕开 Personal Team 占 ID）

用户删掉了手机上的 Luka，要求重装。重装过程中撞到新坑并解决：

- **构建失败根因**：公司 Team（`39CFYH6W55`）注册不了 `com.lukasoc.luka.dev` 等三个 ID，报 `cannot be registered ... not available` —— **同一 Apple ID（证书 UID `U5P7RUMJ28`）的免费 Personal Team（`PD2XGB735Y`）09-14 已注册**，App ID 跨 team 唯一；且免费 Personal Team 无网页管理入口（用户截图证实账号菜单只有 AI FANTASY），网上「切 Personal Team 删 ID」对免费账号无效。
- **解法（本地临时，不碰仓库）**：改 `apps/luka/ios/Luka.xcodeproj/project.pbxproj`（生成物、已 gitignore）三个 `PRODUCT_BUNDLE_IDENTIFIER` → `com.lukasoc.luka.devx` 系列（extension 前缀跟随 host），备份 `/tmp/luka-project.pbxproj.bak`；xcodebuild `-allowProvisioningUpdates -allowProvisioningDeviceRegistration DEVELOPMENT_TEAM=39CFYH6W55` 构建 → **BUILD SUCCEEDED**；`codesign -d --entitlements` 确认 `39CFYH6W55.com.lukasoc.luka.devx` + 推送/Apple 登录/App Group 齐全。
- **安装**：新装 `ios-deploy`（brew），`ios-deploy --bundle ... --id 8b20f7…bea1 --no-wifi` → `[100%] InstallComplete`（devicectl 不支持 iOS 16）。
- **沉淀**：新增 [ios-appid-personal-team-conflict](../30-Resources/troubleshooting/ios-appid-personal-team-conflict.md)（判据、改法、根治待验证：09-21 免费 profile 过期后试改回原 ID）。
- **待办**：手机上信任公司 Team 证书 + 打开 App 连 Metro；**手机 → Mac 的网络不通问题仍未解**（Metro 日志无请求，用户尚未做 Safari 测试）。

## 工作日志 — 17:03 Luka 真机：expo-store-review 原生模块缺失修复（catalog 钉回 57.0.1）

重装后 App 连上了 Metro（网络问题自然消失），但启动崩在 `Cannot find native module 'ExpoStoreReview'`（`good-review.ts:1`）。

- **根因 1**：store-review 是当天另一会话**未提交的**新依赖（`package.json` modified），09-15 生成的 ios/ 工程没跑过 pod install → 原生二进制里没有该模块（Metro 有 JS、App 没有原生）。补 `pod install` 接入。
- **根因 2**：pod install 后编译失败 `cannot find 'SceneGeometry' in scope` —— store-review **57.0.2 起**改用 `ExpoModulesCore.SceneGeometry`（changelog：foreground scene 修复），而本 SDK 的 core **57.0.6** 没有该 API（57.0.18 才有）；先试 `pnpm update expo-modules-core` 未生效（仍是 57.0.6，且全仓升 core 影响面大）。
- **修复（仓库改动）**：`pnpm-workspace.yaml` 的 catalog `expo-store-review: ~57.0.1` → **精确钉 `57.0.1`**（57.0.1 与 57.0.2+ 功能等价，都用「前景 scene」逻辑；附注释说明，先例同 `expo-video`）。`git checkout -- pnpm-lock.yaml` 后 `pnpm install` 重算 → lock diff 仅 17 行（纯 store-review 条目），core / expo 版本未动。
- **验证链**：`pod install`（ExpoStoreReview 57.0.1）→ xcodebuild **BUILD SUCCEEDED**（bundle id `com.lukasoc.luka.devx` 保持不变）→ `ios-deploy` 重装 `[100%] InstallComplete`。
- **教训**：① 未提交的原生依赖是构建地雷 —— Metro 能解析 JS，但没 pod install 的二进制必崩；② 新加 expo 包先核对与本 SDK 预编译 core 的兼容，`cannot find 'X' in scope` = 缺 API，查 npm tarball/changelog 后选「钉包版本」或「升 core」，不要盲目升 core。
- **状态**：`pnpm-workspace.yaml` 的钉版改动**未提交**（规范模式，待用户发话）；工作区另有另一会话的 store-review 工作（package.json / good-review.ts 等未提交）。

## 工作日志 — 16:24 figma-write-bridge：Settings 子页两屏分层复刻（55 · Help & Support / 56 · Blocked people）

用户（续作指令）：按 commit `664b6afe`「Settings 子页按卡片语言重做」把两屏重建为 Figma 节点 spec（无截图，全按代码）。

- **产物**：`figma-write-bridge/screens/55-help.mjs`（label `55 · Help & Support`，6 顶层节点）、`screens/56-blocked.mjs`（label `56 · Blocked people`，6 顶层节点）。
- **55**：`‹` 44pt 返回行（y=54）+ 28/34 标题（y=110）；两张无粉底 `EditCard`（tagBg / 圆角 20 / 卡头 36）：QUICK ANSWERS 五条收起态 FAQ（行 49 = py-3.5×2 + 16/21，行尾 24px `›`）与 TALK TO US 两行联系入口（行 65，蜜色 40 整圆图标底 + 布局）。
- **56**：白卡 hero 350×99（`3 people blocked` 20/25 + 两行说明 14/19 + hamster-peek 96 右缘居中）；`EditCard`「BLOCKED」三行：44 头像 + 名字/拉黑时间 + 蜜色 `Unblock` 整圆胶囊（宽 85.5 = Nunito Bold 14 实测 53.5 + px-4×2）。
- **验证**：两条 `node -e "import('./screens/XX.mjs')..."` 均通过（children 6/6）；未改 lib/、plugin/、bridge.mjs 与其它 screen；sitin-rn 零改动；bridge 目录不是 git 仓库。
- **关键命令**：CoreText + Nunito TTF 实测（临时 `measure.swift`，`~/.expo-google-fonts/nunito` 下的字体文件）—— hero 首行含尾随空格 227.26pt（226 宽折两行）、`Unblock` 53.49pt、FAQ 问句最长 220.4pt。

## 工作日志 — 16:40 新增 section 14：好评引导 + Settings 子页（53–56）

**需求**：继续把新增页面画进 Figma（用户再发一次「我们新增的页面开始画figma」）。

**这批新页面来自的提交**
- `aef7ed00` feat(luka): 好评引导按 GP 整改重做（去奖励承诺 + 暖色圆语言）—— `rate.tsx` / `rate-upgrade.tsx` 大改，**`rate-reward.tsx` 已删除**
- `664b6afe` feat(luka): Settings 子页按卡片语言重做（Help & Support / Blocked people）—— 新增 `components/settings/blocked-people-hero.tsx`，**`settings-group.tsx` / `settings-group-label.tsx` 已删除**
- `504c26e3` 聊天页礼物抽屉修复（纯 bugfix，未出屏）

**新增 4 屏 / 1 个 section**
- `· 14 好评引导 / Settings 子页`（53–56）：`app/rate.tsx`（hamster-waving 148×263 + 「Enjoying the quiet here?」+ Write a review）、`app/rate-upgrade.tsx`（scrim + 326×378 奶油卡，蜜色 92 圆 + hamster-face）、`app/settings/help.tsx`（QUICK ANSWERS 5 条 FAQ + TALK TO US 两行）、`app/settings/blocklist.tsx`（BLOCKED hero「3 people blocked」+ 3 行 Unblock 胶囊）。四屏都是新卡片语言（`EditCard` + 蜜色圆图标底）。

**结果**：`poo` 页现 **14 个「分层复刻」section、56 帧**，无游离节点。

**留存判断**（子代理标注）：54 的背景路由是 `transparentModal`，真机身后是划卡 deck，但 bridge 里没有 deck 模块（18 是列表模式），只还原了 scrim + 卡片两层；56 只画「已加载名单」态（另有 skeleton / error / empty 三态未出屏）。

## 工作日志 — 16:26 Luka 组件层只读审查（apps/luka/src/components，364 tsx / 24.7k 行）

用户要求：只读审查 Luka 组件层，查 AGENTS 约定（一组件一文件 / 英文文案 / strict / 无身份硬编码）+ 正确性 / 性能 / 可读性 / 架构 / 安全，最多 15 条按影响排序。未改任何仓库文件。

- **规模实测**：364 个 tsx（不是 375）、24760 行、34 个子目录；>250 行仅 8 个（最大 `discovery/card-stack.tsx` 430）、>150 行 27 个。`any` 0、`@ts-ignore` 0、`console.*` 1、无 `fetch`/WebView/硬编码域名 —— 底子干净。
- **主要发现**（按影响排序）：
  1. feed 瀑布流 `MasonryList` 是 ScrollView 全量渲染，而 `useSceneFeed` 分页无上限（`[...prev, ...page.posts]`）——长列表内存/滚动退化（High）。
  2. 「一组件一文件」违规 **24/364 文件**，最重 `chat-bubble.tsx` 6 个、`spark-pack-card.tsx` 6 个。
  3. **39 个导出无任何引用**（385 个导出里约 10%；node 脚本扫 `src`+`tests` 全量标识符，含 `BoostChip`/`BoostSheet`/`VoiceRecorderSheet`/`LegalLinks` 等）。
  4. `BubbleMedia(...)` 以普通函数调用而非 JSX（`chat-bubble.tsx:259,358`）——加 hook 即崩的隐患。
  5. `leave-link-dialog-host.tsx:35-41` `Promise.all(...).then().finally()` 无 `.catch` → 请求失败时 unhandled rejection 且弹窗静默空着。
  6. 文档化的 `time` prop 只在 call 气泡消费（iris/blueprint 同样，属继承行为）。
- **性能/动画**：3 处无限 `withRepeat` 无 cleanup（`hold-step` / `continue-story-watcher` / `boost-chip`），而同仓 ios 变体与 skeleton 都返回 `cancelAnimation` —— 属于本仓自身规范的漏网点。渲染期改 ref 出现在 4 个文件（bottom-sheet / leave-link / masonry-list / toast），而 `card-stack.tsx:312` 的注释恰好写明「渲染期改 ref 是副作用」，可作对照。
- **抽样验证**：`card-stack` / `card-scroll` / `bottom-sheet` / `location-picker` / `chat-input-bar` / `gift-sheet` / `voice-recorder` / `age-ruler` / `pet-chat-companion` / `hero-carousel` / `masonry-list` / `people-list` 深读，timer/监听/订阅清理都到位（AppState 四处均 `subscription.remove()`；`voice-recorder` 靠 expo-audio 的 `useReleasingSharedObject` 卸载释放，X 关闭不泄漏）。
- **结论**：不可当作「需要大修」，但约定违规与死代码是实打实的；正确性只有 1 个 unhandled rejection 与 1 个调用约定问题。报告已交付用户。

## 工作日志 — 16:49 sitin-next 排查:savvy_android 直播卡片不显示

用户问：`feat/pwa-open-snapchat-live-savvy-android` 分支里 savvy_android 的直播卡片(Go Live & Earn / EarnModule)不显示是为什么。只读排查,未改代码。

- **门控**(`packages/app-pwa/src/pages/Live/LiveAction.tsx:64-74`):① UA `isSavvyAndroid`;② `hasNativeMethod("startHostLiveStream")`;③ 后端 `canShowEarnCard()`(LiveCanGoLive)。
- **核对端侧**:shallow clone `presence-io/savvy-android`(master,103MB)——
  - UA 是写死的纯 Chrome UA(`app/src/main/java/com/savvy/app/web/PwaWebConfig.kt:18`),**从不追加 app 名**;端标识走 URL `?app_name=savvy`(BuildConfig.APP_NAME="savvy")+ 网络头。→ `isSavvyAndroid` 恒 false。`savvy_android` 这个 UA token 真机从无(8-24 的 getApkName 返空同源)。
  - `PwaNativeInterface.kt` 只有 `startLiveNative`(:533,LiveActivity)/`syncLiveState`(:518),**没有 `startHostLiveStream`** → 门 ② 也恒 false。
- **结论**:两道新增门在真实 savvy 端都过不了,卡片走「老端无开播能力 → 不展示」兜底,属预期而非 bug。若要放开:端追加 UA app 名或 PWA 改按 `app_name` 识别;端实现 `startHostLiveStream`(或 PWA 改判 `startLiveNative` 对齐契约)。
- **沉淀**:`30-Resources/troubleshooting/pwa-hide-snapchat-regression.md` 新增「八、复核」;教训——「UA 追加 app 名」只是 haven 端实现,别外推到同产线其它 App。
- **命令**:`gh repo list presence-io` 找仓库 → `git clone --depth 1 --filter=blob:none --sparse` 到 `$TMPDIR/opencode/savvy-android` → grep UA/PwaNativeInterface。

## 工作日志 — 16:55 sitin-rn-fix-luka 评审整改:feed 内存窗口 + 生产日志脱敏

在独立 worktree `/Users/colna/WORK/sitin-rn-fix-luka`(分支 `fix/luka-code-review`)按已确认的 code review 问题改两组,只动指定 5 个文件,按要求未 commit:

- **A feed 分页无上限**:`apps/luka/src/hooks/use-scene-feed.ts` 新增 `MAX_POSTS_IN_MEMORY = 120`(注释写明是内存护栏非业务规则;feed newest-first,尾部即最旧)并在 `loadMore` 的 append 处 `.slice(0, MAX_POSTS_IN_MEMORY)` —— 只在新页 append 时裁旧尾,`reload`/`prepend`/去重/曝光语义不变;`masonry-list.tsx` 未动。
- **B 生产日志去 PII**:`report.tsx` 删 `[report-dbg] params` 整段(含 userId/postId/authorId/name);`home-like.ts` 两处 `console.info` 去掉 `用户名=${person.name}`(保留 userId/曝光/triggerMatch);`match-push.ts` 推送日志去掉用户名字段(保留 userId/用户类型);`contacts.ts` picker 日志从整个对象改为 `selected != null`。`console.error` 均未动。
- **验证**:`pnpm --filter luka typecheck` 过;`pnpm --filter luka test` 122 files / 710 passed;`pnpm exec biome check` 5 个改动文件 0 告警。

## 工作日志 — 17:20 savvy_android 直播卡片排查续:savvy develop 已支持 + PWA 部署矩阵

用户让「去看 savvy 的 dev 分支」,复核后修正 16:49 的结论(当时只看 master)。

- **savvy develop(@`a89de1c2`)已具备两道门**:UA 追加 `savvy_android`(`3ed7cd5`,08-25);`PwaNativeInterface.kt:805 @JavascriptInterface startHostLiveStream` + `isSupportFeature("hostLive")`(`58bab0a`,09-15,随 beauty-live 从 GraceChat 迁移)。**master/release 2.2 两者都无**。
- **线上 bundle 实测**(curl 各域名 service-worker precache → 找含 startHostLiveStream 的 Live chunk → grep 门控):仅 `app-test.sitin.ai` 是 `(isHavenPwa||isSavvyAndroid)`(即已含分支,~14:57 部署);`app-test2` / `app-pre` / `app.sitin.ai` 仍是 `isHavenPwa && ...` 旧包。savvy debug 默认 app-test(GlobalConf)、release 默认 app.sitin.ai。
- **结论**:卡片不显示优先看测试环境 —— 除 app-test 外都没部署分支;savvy 还有 PWA 预热缓存,旧 bundle 可能被复用。app-test + develop 新包仍不出,再查后端 LiveCanGoLive.canShow 与 Live 页 Action 态。
- **沉淀**:更新 `30-Resources/troubleshooting/pwa-hide-snapchat-regression.md` 第九节(部署矩阵 + master/develop 区分)。
- **命令**:`curl <host>/service-worker.js` + 批量抓 `assets/js/index-*.js` grep。工具目录 `/tmp/pwa-chunks`、`/tmp/chunks-*`。

## 工作日志 — 17:25 savvy_android 直播卡片排查(app-test 环境复核)

用户补充「测得是 app-test.sitin.ai」。直接核线上 bundle(不用猜):

- `app-test.sitin.ai` 线上 **已含分支**:main-CQtZVLWJ → Live chunk index-Btt5Ky_m,门控 `(isHavenPwa||isSavvyAndroid) && hasNativeMethod("startHostLiveStream")`(压缩后 `(n||r)&&S(...)`);main 内 parseUA 含 `savvy_android`。部署时间 ~14:57/15:42(北京)。对照 test2/pre/prod 仍是旧门控。
- 同步核对 savvy-android develop:**UA token(`3ed7cd5`,8-25)+ `startHostLiveStream`/`isSupportFeature("hostLive")`(`58bab0a`,9-15)都有**;master/release 2.2/旧 `pwa` 分支都没有(旧 `pwa` 分支 tip 7-31)。
- 所以 app-test 上卡片不出的剩余可能:① 测试 APK 不是 develop 构建(或早于 8-25/9-15);② 浏览器里测(无 pwaBridge,hasNativeMethod 恒 false);③ 后端 `LiveCanGoLive.canShow=false`(主播资格);④ 不在 Action 态;⑤ 预热/SW 旧 bundle 缓存(部署时间就在今天下午)。
- **真机自检两条**:`navigator.userAgent` 含 `savvy_android`;`typeof window.pwaBridge.startHostLiveStream` 为 "function"。
- 命令:拉 `service-worker.js` precache → 取 `assets/js/index-*.js` 批量 curl → grep `startHostLiveStream` 定位 Live chunk → grep 门控;另 `gh repo list` 找仓库。沉淀见 troubleshooting 第十节。

## 工作日志 — 17:13 sitin-rn-fix-luka 死代码清理（组件导出 + 6 个 blueprint 遗产模块）

在独立 worktree `/Users/colna/WORK/sitin-rn-fix-luka`（分支 `fix/luka-code-review`）清理 `apps/luka` 死代码，只删确认零引用者，未 commit。

- **方法**：先按「标识符在 `apps/luka/src` + `apps/luka/tests` 只出现一次」找候选（得 40 个），再用导入感知脚本复核——按模块路径解析 import，排除同名不同模块、注释/字符串巧合、平台变体（`.ios.tsx` 经基名解析属在用的假阳性）。共删 **71 个文件**：59 个 `components/**` + 6 个 src 模块 + 6 个对应测试。
- **直接零引用（39 项导出）**：`TermsConsent`、`ChatPresenceStrip`、`ContactEditIcon`、`HowChemistryWorksCard`、`BondIcon`、`BondRecordIcon`、`DealCard`、`DistPinGlyph`、`NearbyPhotoCollage`、`NearbySayHiButton`、`AvatarNudgeCard`、`BoostChip`、`BoostSheet`、`CardMetaRow`、`DiscoveryHeader`、`DiscoveryTutorial`、`IntelSection`、`ProfileGuidanceSheet`、`SpotlightButton`、`AiPolishHint`、`SHEET_FIELD_STYLE`、`PhotosEditSheet`、`LocationEditSheet`、`LikerCard`、`ProfileGridCard`、`FeedPreview`、`LegalLinks`、`ProBadge`、`ProfilePostCard`、3 个 scene glyph、`SceneFeedRow/Separator`、`SettingsToggleRow`、`NativeTabsBar`、`TabScreenHeader`、`AnnotationLabel`、`PreviewPersonCard`。
- **复核新发现（超出上次候选）**：`WaveButton`、`me/MenuRow`（me-menu-card 有本地同名实现）、`SettingsListRow`、`onboarding/TypewriterText`（与 discovery 同名不同模块）、`BottomFade`、`ZoomableImage`、`HAS_PLUS_STEP_VIDEO`×2。
- **级联清理（删除后二次扫描确认）**：4 个 bond artwork、`SectionHeader`/`SkeletonBars`/discovery `TypewriterText`、`AndroidTextEditModal`、`EditSheet`（整文件，photos/picker 两个 sheet 删除后无消费者）、`ChevronGlyph`、`FeedSinglePostPreview`、`LegalRow`、`ScenePostCard`、`PostMediaCarousel`、`FeedLikeGlyph`、`LOCATION_FIELD_BLOCK_HEIGHT`（LocationPicker 本体仍被 `app/edit-profile/location.tsx` 使用，保留）。
- **任务 B**：`lib/chat-local-history`、`lib/coin-purchase-routing`、`lib/message-interception`、`lib/subscription-format`、`services/analytics-log`（纯 re-export）、`services/third-party-subscriptions` 仅测试引用，连测试一起删。
- **跳过**：`chat/voice-recorder.tsx`（chat/[id].tsx:1455 注释明写 kept for when the feature ships，属「给未来」）；内部使用的类型/常量（`WaveOrderRowProps`、`COMPOSE_PET_SIZE`、`PackLayout`、`feedCoverHeight`、`useSheetMaxContentHeight`、`ButtonVariant`）；平台变体 `.ios`（pet-animation / native-checkin-reminder-host / tabs-navigator / continue-story-watcher）；`ui/index.ts` + `ListRow`（barrel 非 .tsx、不属点名范围）。
- **验证**：`pnpm --filter luka typecheck` 过；`pnpm --filter luka test` 121 files / 703 tests passed（基线 127/733，减少的 6 文件 30 测试正是删掉的）；`pnpm exec biome check apps/luka/src/{components,lib,services}` 518 files 0 告警。

## 工作日志 — 17:17 Luka：Chats 头部未读数改红色圆形徽标

用户：「chat 右上角的 消息通知 数量 需要改为 红色 圆形」（处理中途中止了 discover 卡顿/分页任务，先做这条）。

- **改动**：`components/xuanyu/screen-header.tsx` 未读徽标 —— `rounded-[3px]` 方角 → `rounded-full`；`Colors.danger`（#C2410C 深橙）→ `Colors.badge`（#E0603A，与标签栏 / 会话行未读数同色，`design/theme.js` 里 badge 就是「压在奶油底上的未读数」）；撑法 `py-0.5` → `h-5 justify-center`，保留 `min-w-5`，个位数为正圆、多位数自动撑成胶囊（同 tab-bar 徽标）。
- **影响面**：Chats / Frames 两个 tab 头部 + `tab-loading-skeleton` 共用该组件，一并生效。
- **验证**：`biome check` 该文件 0；`pnpm --filter luka typecheck` 过；`pnpm --filter luka test tests/design/shape-language.test.ts` 6 passed。未在模拟器截图。
- **状态**：未提交（规范模式）；工作区仍有其它会话未提交改动。
- **未完成**：用户先前的 discover 卡顿（nearby→feed）与 feed 一次多加载几条的需求，只做了代码调研，未动代码。

## 工作日志 — 17:35 savvy_android 直播卡片排查第三轮(真机两门已过)

用户发了真机 DevTools 截图 + 控制台值:
- `navigator.userAgent` = `...Mobile Safari/537.36 savvy_android` ✓
- `typeof window.pwaBridge.startHostLiveStream` = `'function'` ✓
- 页面 `localhost:3000/live`(PWA host 切到本机),Live 页在 Action 态(LIVE LOUNGE 可见),但 EarnModule 卡片不在。

结论:端侧两道门都过 → `canGoLiveNatively=true`;**卡片只由第三道 `canShowEarnCard()`(后端 8000 `LiveCanGoLive` 的 `canShow`)决定**。
- 本地 dev 的 API 是 `api-dev.swapnumber.com`(`app-pwa/.env.development:1`),PWA 请求头 app_name 硬编码 `haven_pwa`(`httpClient.ts:146`);与 app-test 线上不是一套后端。
- 给了诊断:dev server 下控制台 `const {canShowEarnCard} = await import('/src/http/liveApi.ts'); await canShowEarnCard()` → false 就查 Network `canGoLive`(响应体有 `canShow`+`reason`,PWA 只取 canShow 丢 reason)。
- 顺带确认:线上 app-test 的 bundle 已是分支门控(`(isHavenPwa||isSavvyAndroid)&&hasNativeMethod`),排除环境问题。

## 工作日志 — 17:41 Luka 三处性能修复（discover 切页 / feed 分页 / post 详情）+ CI 救火

用户三连：① discover tab 切换卡（尤其 nearby→feed）② feed 一次多加载几条（瀑布流后加载太频繁）③ profile 页点 post 跳转卡。

- **① Discover 切页**：`feed-fab.tsx` 加 `visible` prop（opacity + `pointerEvents` + 读屏让开，`PetAnimation paused={!visible}`），`discover.tsx` 改为常驻挂载。FAB 上的仓鼠是 `point-down.mov` 原生播放器，卸载/重挂会重建播放器 —— 那一下正好落在 nearby→feed 的切换上；`pet-animation.ios.tsx` 的注释里本来就有这条规律（「两段都常驻挂载……避免切换时重新建播放器的那一下卡顿」）。常驻 + 暂停，切回来只是 play()。
- **② Feed 分页**：`use-scene-feed.ts` PAGE_SIZE 20 → 40；耗尽判定从「`posts.length < PAGE_SIZE`」改为「空页 or 游标不前进」—— 服务端若对 limit 有上限，旧判据会把第一页之后直接截断（40 请求返回 20 就误判到底）。
- **③ Profile→post**：`post/[id].tsx` 用 `useAfterInteractions()` 把内容整块 hold 到推入动画结束再挂。feed 引擎有内存缓存，详情通常只等一次评论请求（实测 dev API ~237-337ms，见 `.expo/dev/logs/start.log` 历史），正好压在转场上；动画期间只画 spinner。同 `chat/[id]` 的做法。下一步可选（若仍觉得慢）：引擎加 `getCachedPost` 做 cache-first 首帧渲染。
- **验证**：`pnpm --filter luka typecheck` 0；`lint` 944 文件 0 error（1 存量 warning）；`test` 123 files / 712 passed；biome 改动文件 0。
- **事故与修复**：并行会话 17:32 提交 `726237eb` 时把我 17:24 为测量放的临时探针（`_layout.tsx` 的 `import startFrameProbe`）一起带走，而 `lib/frame-probe.ts` 未入库 → PR #517 的 `checks` typecheck 挂 `TS2307 Cannot find module '@/lib/frame-probe'`。我在工作区清掉残留，随后并行会话 `6f7cac53` 把本轮三处修复一并提交并合入 `fix/luka-code-review` 的 13 个提交；用户批准后 `git push`（pre-push boundaries + luka typecheck 全过）→ `726237eb7..f79a069d6`。
- **未验证**：三处修复都没在真机/模拟器实测过 —— 模拟器 dev 包缺 `ExpoStoreReview` 原生模块（路由模块求值失败、每个路由报 missing default export；真机有模块所以正常），测量中途放弃。待用户真机验证；若 discover 仍卡，下一步是把两个 pager 页 memo 化（`setTab` 目前会整棵重渲两个列表）。

## 工作日志 — 17:50 Luka Discover 胶囊卡顿：分段动画改原生驱动

用户澄清「discover 还是卡」= **nearby→feed 时 tab 胶囊本身卡**，真机 iOS。上一轮的 FAB/仓鼠常驻修复没治到这个（那是切换时新建播放器，另一个点）。

- **根因**：`discover.tsx` 的 `onPageScroll` 是 `Animated.event(..., { useNativeDriver: false })`；`discover-feed-tabs.tsx` 里胶囊位移 + 两颗标签的 **`color` 插值**（`interpolate` 输出颜色字符串，原生驱动不支持）都挂在同一个值上 —— 整条动画只能在 JS 线程逐帧算。切 Feed 时 feed 页正好开始加载/布局，JS 一忙胶囊就掉帧（这解释了「尤其 nearby→feed」）。
- **改法**：
  - `discover.tsx`：`useNativeDriver: false` → **`true`**（PagerView 官方 hook `usePagerView.ts:97` 就是这个用法，v8 支持）。
  - `discover-feed-tabs.tsx`：`color` 插值 → 4 条 `opacity` 插值（位移也同值原生驱动）。
  - 新增 `components/discover-feed/discover-feed-tab-label.tsx`（一组件一文件）：每颗标签两层文字叠着交叉淡化 —— 底层选中色（白）撑盒子，上层未选中色（灰，绝对定位）—— `opacity` 原生驱动支持，观感与颜色插值等价。
- **验证**：biome 3 文件 0；`pnpm --filter luka typecheck` 0；`pnpm --filter luka test` 122 files / 706 passed（比昨天少 1 文件/6 测试是并行会话合并 `fix/luka-code-review` 删测试所致）。
- **未做（若胶囊顺了但切完还顿一下）**：`setTab` 仍会把两个 pager 页整棵重渲 —— 下一步把 NearbyView / Feed 页 memo 化。
- **状态**：3 个文件未提交（规范模式）；真机待用户验（Metro 会 Fast Refresh）。

## 工作日志 — 18:00 Luka Discover 胶囊改 Reanimated（原生驱动方案翻车并回退）

用户回传真机照片：「成这样了」—— 两张图里**胶囊和标签冻在初始值**（Nearby 页胶囊停在 Feed，Feed 页停在 Nearby），即上一版 `useNativeDriver: true` 在 PagerView（新架构 iOS）上**事件根本没送进原生动画节点**。

- **教训**：`react-native-pager-view` 的 `onPageScroll` 在 Fabric 下不能配 RN Animated 原生驱动。库自己的 `usePagerView.ts` 虽然写着 `useNativeDriver: true`，但这套栈上不成立。仓库里 `into-you.tsx` / `notifications.tsx` 的同类 pager 一直用 `useNativeDriver: false`，不是没原因的。
- **改法（对齐 Me 页已跑通的方案）**：整套换成 Reanimated。
  - `discover.tsx`：删 `Animated.Value` + `Animated.event`（原生/JS 驱动都不要）+ `Animated.createAnimatedComponent(PagerView)`；改 `useSharedValue(tabParam === "feed" ? 1 : 0)`，`onPageScroll` 回调里 `pagerProgress.value = position + offset`；`<PagerView>` 用普通组件。
  - `discover-feed-tabs.tsx`：`progress` 改收 `SharedValue<number>`；胶囊 `useAnimatedStyle` 平移、两颗标签各自 `useAnimatedStyle` + `interpolateColor`（颜色插值在 UI 线程，正是 RN 原生驱动做不到而 Reanimated 能做到的那件事）。做法与 `components/me/me-tabs.tsx` 完全一致。
  - 删掉上一步临时造的 `discover-feed-tab-label.tsx`（两层 opacity 交叉淡化不再需要）。
- **验证**：biome 2 文件 0；`pnpm --filter luka typecheck` 0；`pnpm --filter luka test` 122 files / 706 passed。真机待验（Metro 热刷；建议手动 Reload 一次避开上次的坏原生动画节点状态）。
- **状态**：2 个文件（`discover.tsx` / `discover-feed-tabs.tsx`）未提交。

## 工作日志 — 18:02 Luka Discover：分段长度固定 + Feed 页隐藏视图开关

用户：「不做可形变的 tab 栏了，tab 栏长度不变，在 feed 页时，把全屏和列表模式的开关隐藏吧」。

- **问题**：`discover.tsx` 里视图开关（`DiscoverViewToggle`，全屏/列表）是 `tab === "nearby" && toggleEnabled` 条件渲染，而分段控件是 `flex-1` —— 切到 Feed 时开关一卸载，分段就撑满整行，轨道宽度变化、胶囊要重新量。
- **改法**：开关外层包一个常挂载的 `View`，按 `tab` 切 `opacity`（Feed 时 0）+ `pointerEvents="none"` + 读屏让开；`toggleEnabled` 为 false（远端关掉整个功能）时仍不占位，让分段恢复整行。分段长度在两个 tab 上从此完全一致。
- **验证**：biome 该文件 0；`pnpm --filter luka typecheck` 0；`pnpm --filter luka test` 122 files / 706 passed。
- **状态**：与上一批 Reanimated 改动同在 `discover.tsx` / `discover-feed-tabs.tsx` 两个未提交文件里。

## 工作日志 — 18:06 Luka Discover：视图开关补滑动退场动画

用户：「再加上滑动动画」（承接上一条：开关在 Feed 页隐藏、分段长度固定）。

- **改法**：`discover.tsx` 的开关外层从「按 `tab` 切 opacity」改为 Reanimated `Animated.View`，样式由 `pagerProgress` 驱动：`opacity` 1→0、`translateX` 0→(自身宽度 + 12)（行内 `gap-3`）。滑出距离用 `onLayout` 量出的宽度算，不写死；`Extrapolation.CLAMP` 兜住 iOS 回弹时的越界值。跟手方向与胶囊一致（滑向 Feed 时向右滑出，滑回时滑回来）。
- **验证**：biome 0；`pnpm --filter luka typecheck` 0；`pnpm --filter luka test` 122 files / 706 passed。
- **状态**：仍在 `discover.tsx` 这一个未提交文件里。

## 工作日志 — 18:12 Luka Discover：胶囊/开关动画加 onPageSelected 兜底（怀疑 onPageScroll 不达）

用户：「为什么没有 tab 滑动的效果」。排查后判断：**`react-native-pager-view` 8.0.2 在 Fabric/iOS 上 `onPageScroll` 很可能没送进 JS** —— 胶囊与视图开关的动画一直只由它驱动，所以表现为「完全不动」（之前原生驱动那次的冻住照片是另一个原因，症状一样）。反证：`onPageSelected` 是好的（分段状态、开关显隐一直在跟着变）。

- **改法**（`discover.tsx`）：保留 `onPageScroll` 的跟手路径；在 `onPageSelected` 里加兜底 `pagerProgress.value = withTiming(position, { duration: 220 })`。事件送得到时这步等价空操作（值已是目标），送不到时胶囊与开关也会动画地滑到正确位置。
- **副作用**：若事件确实不达，用户看到的是「松手/点击后滑过去」而不是「跟手」，手指拖拽期间胶囊仍不动 —— 要恢复真跟手需要给库打 native patch（仓库有 `pnpm-workspace.yaml` → `patchedDependencies` 机制，先例是 expo-image-picker）。
- **验证**：biome 0；`pnpm --filter luka typecheck` 0；`pnpm --filter luka test` 122 files / 706 passed。
- **状态**：仍在未提交的 `discover.tsx`。

## 工作日志 — 19:19 Luka Discover：加临时诊断日志 + 兜底改弹簧

用户：「最开始 的 滑动特效是如何实现的，现在还是没有丝滑的动效」。

- **考证**：最初的实现是 commit `9f1d19d10`（08-25，intro 时期）—— RN `Animated.event(..., { useNativeDriver: false })` 挂在 PagerView `onPageScroll` 上，`position + offset` 合成进度，插值下划线位移与文字颜色；当时的栈与现在**完全相同**（Expo 57 / RN 0.86 / newArch / pager-view 8.0.2），所以「以前能跟手」是事实。
- **本轮**：
  - 兜底动画 `withTiming(220ms)` → `withSpring({damping 20, stiffness 320, mass 0.9})`（更像原始观感）。
  - 加两条**临时诊断日志**（验完删）：`onPageScroll` 里 `[discover-scroll] position offset`（限流 0.2）、`onPageSelected` 里 `[discover-selected] position`。目的是确认 `onPageScroll` 到底送不送得到 JS。
- **验证**：biome 0 / typecheck 0 / test 706 passed。
- **待用户**：重开 App → Discover → 慢拖一次 + 点一次标签，然后读 `/tmp/luka-metro.log`（或 `.expo/dev/logs/start.log`）判定；若事件不达，要真跟手只能给 `react-native-pager-view` 打 native patch（仓库有 `patchedDependencies` 机制）。

## 工作日志 — 19:25 Luka Discover：按用户要求还原到最初的 RN Animated 实现

用户贴出最初的 `discover-feed-tabs.tsx` 代码（`Animated.View` 胶囊 + `Animated.Text` 颜色插值）：「还原成这种」。

- **改法**：
  - `discover-feed-tabs.tsx`：`git checkout HEAD --` 直接还原（HEAD 就是最初版本，RN Animated + `ReturnType<typeof Animated.add>` 的 `progress`）。
  - `discover.tsx`：进度管线还原成 `Animated.Value` + `Animated.add` + `Animated.event(useNativeDriver: false)` + `AnimatedPagerView`；Reanimated 全部撤掉。视图开关的滑出改用同一套 RN Animated 插值（`pagerProgress.interpolate`，clamp），保留「常挂载 / 长度不变 / 隐藏 / 滑动」的行为。
  - 诊断日志保留（TEMP）：`Animated.event` 的 `listener` 里 `[discover-scroll]`、`onPageSelected` 里 `[discover-selected]` —— 用来最终确认 `onPageScroll` 是否送达。
  - 兜底弹簧保留但**自我禁用**：`onPageSelected` 里若 300ms 内有过滚动事件就跳过（避免和跟手路径打架），没有时才 `Animated.spring` 把 `pagerPosition`/`pagerOffset` 补到目标。
- **验证**：biome 0 / typecheck 0 / test 706 passed；`git diff HEAD` 只剩 `discover.tsx` 一个文件（tabs 零 diff）。
- **下一步**：等用户重开 App 后拖一次，读日志判定 `onPageScroll` 是否送达。

## 工作日志 — 19:32 Luka Discover 分段：砍掉事件依赖，重构为「tab 状态驱动弹簧」

用户：「重构这个 tab 吧，用最简单的方式实现切换动画效果」。

- **终版设计**：`discover.tsx` 里一颗 Reanimated 共享值 `tabProgress`（0/1），**只由 `tab` 驱动**（`useEffect` + `withSpring`）；`tab` 来自 `onPageSelected`（点击/落位都可靠）。胶囊位移、两颗标签的 `interpolateColor`、视图开关的滑出/淡出全部读它 —— 一朵弹簧同步三处，UI 线程。PagerView 恢复成普通组件，`onPageScroll`、`Animated.event`、RN Animated 全部删掉；诊断日志与兜底弹簧也删掉。
- **代价（已向用户说明）**：胶囊不再跟手（拖拽中不动，页面落位/点击才弹过去）—— 这台库的 onPageScroll 不送达，JS 侧做不到跟手；换到「一定动、一定顺」。
- **改动文件**：`components/discover-feed/discover-feed-tabs.tsx`（重写，收 `SharedValue<number>` + `onChange`）、`app/(tabs)/discover.tsx`。净减 50 行左右的管线代码。
- **验证**：biome 0 / typecheck 0 / test 122 files · 706 passed。
- **状态**：未提交；等用户重开 App 实测（上次真机 App 在 18:08 后已掉线，需手动重开）。

## 工作日志 — 19:35 Luka Discover 分段：点击即动 + 开关改为瞬时隐藏

用户反馈两点：① 点标签后有延迟才滑动；② 视图开关跟着滑出去，期望直接消失。

- **① 延迟根因**：`tab` 只在 `onPageSelected`（页面落位，约 300ms 后）才更新，弹簧等落位才起跑。改法：`goToTab` 里**立刻 `setTab(next)`** 再 `setPage(...)`，弹簧与页面动画同时起跑；落位事件报同一个值，等于空操作。`discover.tsx:92`
- **② 开关**：撤掉滑出/淡出动画（`useAnimatedStyle` + `viewToggleWidth` 全删），改成普通 `View` + 瞬时 `opacity`（Feed 页 0，仍占位，分段长度不变）—— 点标签时它随 `tab` 一起瞬间消失。`discover.tsx` 对 Reanimated 的依赖清空。
- **顺带**：弹簧搬回 `DiscoverFeedTabs` 内部（组件自己拿 `active` 驱动），`discover.tsx` 只传 `active`/`onChange`，动画接线不再外泄。
- **验证**：biome 0 / typecheck 0 / test 122 files · 706 passed。
- **状态**：未提交；等用户重开 App 实测。

## 工作日志 — 19:46 Luka feed / nearby 列表：无感无限滚动

用户需求（原话）：「无感知加载，除非是接口慢，或者接口没有返回了，才到底」。上一轮还要求给 nearby 列表模式和 feed 做预加载。

- **触发提前**（`components/masonry/masonry-list.tsx`）：`END_THRESHOLD` 420 → **1200**（约一屏半）。下一页在用户滑到底之前就开跑，滑到那里时内容已渲染好；底部「加载中」只有在接口慢到被追上时才看得见。
- **加载闩修复**（同文件）：原来只靠「内容高度变了」解闩 —— 空页、请求失败、nearby 滑窗 5 进 5 出（总高可能不变）都不解闩，挂一次就是永久，表现是「滑到底再也不加载」。新增 `loadingMore` prop + `useEffect`：请求回来（成功/失败）就解闩。
- **分页游标加固**：
  - `services/scene.ts`：`getPostFeed` 从直接 re-export 改成包一层 —— 后端没给 / 给 0 / 给不前进的值时，按 `offset + 本页条数` 猜下一页；真正到底时下一页返回空数组。
  - `hooks/use-scene-feed.ts`：加 `postsRef` 镜像（分页判重与裁剪都在镜像上做，避免被 `MAX_POSTS_IN_MEMORY` 裁剪切歪长度判断）；`loadMore` 里「本页一条新的都没有」也判到底，防止服务端重复发同一页导致「一直在加载」；`prepend` 同步镜像 + 裁剪。
- **预加载（上一轮诉求）**：提前一屏半触发 + 瀑布流非虚拟化（新页卡片一挂载就开始加载图片）＝ 到眼前时图已就绪，不再需要单独 prefetch。
- **顺带收尾**：上一轮没收完的接线补完 —— `DiscoverFeedTabs` 改为收 `progress: SharedValue<number>`，弹簧移到 `discover.tsx` 的 `goToTab` 里**同步起跑**（不等 setTab 后的重渲），修「点击后有延迟才滑动」。
- **验证**：biome 0 / typecheck 0 / test 122 files · 706 passed。
- **状态**：5 个文件未提交（discover.tsx / discover-feed-tabs.tsx / masonry-list.tsx / use-scene-feed.ts / services/scene.ts）。

## 工作日志 — 19:51 Luka Discover：胶囊改 350ms 时序曲线，与翻页动画同时长

用户：「tab 不卡了，但是下方的页面会比 tab 切换的慢」——弹簧（stiffness 320 那组）约 200ms 到位，PagerView 的原生翻页约 350ms，胶囊先停、页面后停。

- **改法**：`DISCOVER_TAB_SPRING` → `DISCOVER_TAB_TIMING = { duration: 350, easing: Easing.inOut(Easing.ease) }`，`withSpring` → `withTiming`（两处：`goToTab` 同步起跑 + `useEffect(tab)` 外部变化）。`Easing` 必须用 Reanimated 那份（worklet），RN 的 `Easing` 进不了 UI 线程。
- **调参口**：时长集中在 `DISCOVER_TAB_TIMING.duration` 一个常量；若页面仍先/后到，改这一个数即可。
- **验证**：biome 0 / typecheck 0 / test 122 files · 706 passed。
- **状态**：未提交。

## 工作日志 — 19:56 Luka 瀑布流：触发线改成「内容过半」

用户：「瀑布流我不想拉到底才加载，我想拉到一半就开始加载下一页」。

- **改法**（`components/masonry/masonry-list.tsx`）：触发条件从「距底部固定像素」改成**内容总高的比例**：`distanceToEnd < contentSize.height * loadMoreAtFraction`，默认 **0.5**（视口底越过内容中点就拉）。换批后内容变长、中点下移，不会连环拉。
- **nearby 列表模式**（`nearby-masonry.tsx`）传 `0.2`：它的 `onEndReached` 是**滑窗换人**（5 出 5 进、总高基本不变），用 0.5 会在滚动中连环换人。
- **参数口**：`loadMoreAtFraction` prop + `LOAD_MORE_AT_FRACTION` 默认值；feed 想更早/更晚就调这一个数。
- **代价**：拉得更早，预载的条目更多（内存由 `MAX_POSTS_IN_MEMORY = 120` 兜住）。
- **验证**：biome 0 / typecheck 0 / test 122 files · 706 passed。

## 工作日志 — 20:47 Luka Discover：胶囊改由 pager 滚动位置驱动，修「点击后先后到达」

用户：「解决一下 discover tab 和内容不同步的问题」。追问确认症状是**点击标签时，胶囊和页面先后到达**。

- **根因（两处叠加）**：
  1. `goToTab` 里同步起跑 `withTiming` 之后，`setTab` 触发整屏重渲，`useEffect([tab])` 又把**同一段动画重启一次** —— 胶囊实际耗时 = 重渲耗时 + 350ms，原生翻页只要 ~350ms，必然晚到；
  2. 更根本的是胶囊不读 pager 的真实位置，用「猜一个与原生翻页等长的时长」永远对不齐。
- **修正**（对齐 Me 页 `me.tsx` 的成熟写法）：`discover.tsx` 接回 `onPageScroll`，把 `position + offset` 直接写进 `tabProgress`（clamp 到 [0,1]）—— 胶囊与页面同读一条滚动位置，拖拽跟手、点击后的原生翻页动画期间也逐帧跟住，物理上不可能不同步。`withTiming` 降级为兜底：点击时同步起跑（`startTabAnimation` + `tabTargetRef` 去重，effect 不再二次起跑）；`onPageSelected` 距最近滚动事件超过 200ms 才补一段。
- **推翻旧结论**：18:00–19:32 期间判定的「onPageScroll 在 Fabric iOS 不送达」是热刷残留状态造成的误判 —— 库源码（`PagerScrollDelegate.swift` 每次 `scrollViewDidScroll` 都发事件）与 Me / Into-you 页同款写法均可佐证。
- **改动文件**：`app/(tabs)/discover.tsx`、`components/discover-feed/discover-feed-tabs.tsx`（注释同步新设计）。
- **验证**：biome 2 文件 0 / `pnpm --filter luka typecheck` 0 / `pnpm --filter luka test` 122 files · 706 passed。
- **状态**：规范模式，未提交；待用户重开 App 实测（建议手动 Reload，避开旧 bundle 的原生动画残留）。

## 工作日志 — 20:55 Luka Discover：撤掉第二条驱动，修「胶囊会闪」

用户实测 20:47 的版本：「tab 滑动样式会闪」。

- **根因**：点击时既起跑 `withTiming`（UI 线程平滑走）又用 `onPageScroll` 的真值写同一颗共享值 —— 两条进度不会逐帧相等；`setTab` 重渲卡住 JS、滚动事件成批迟到时，后到的真实位置把已经走远的动画值**往回拽**，胶囊连同文字颜色就闪一下。
- **改法**：彻底删掉第二条驱动（`DISCOVER_TAB_TIMING` / `startTabAnimation` / `tabTargetRef` / `lastScrollAtRef` / `SCROLL_SILENCE_MS`），与 Me 页完全一致：`tabProgress` 只由 `onPageScroll` 的 `position + offset` 写；`goToTab` 只 `setTab` + `setPage`，`onPageSelected` 只 `setTab`。
- **注释**：两处注释都写明「不要再叠加第二条驱动」的教训 —— 同步靠同读一个值，不靠对齐时长。
- **验证**：biome 0 / `pnpm --filter luka typecheck` 0 / `pnpm --filter luka test` 122 files · 706 passed。
- **状态**：未提交，待真机复测（全量 Reload）。

## 工作日志 — 20:57 Luka Discover：两页 memo 化，修「点标签延迟 / 滑动顿一下」

用户实测 20:55 版：「滑动内容区域 tab 会卡顿一下，点击 tab 区域会有延迟。期望无延迟无卡顿」。

- **根因**：胶囊已由 `onPageScroll` 驱动（自身不引入延迟），但 `tab` 一翻（点击立即 `setTab`、滑动中 `onPageSelected`）就把 `DiscoverScreen` 整屏重渲 —— 两个 pager 页（卡组 / 瀑布流几十张卡）全跟着重渲，JS 线程被卡住：点击时原生翻页刚起跑，滚动事件送不进 JS（延迟）；滑动中丢帧（顿一下）。
- **改法（纯 memo 化，不动动画管线）**：
  - 新增 `components/discover-feed/discover-feed-page.tsx`：Feed 页抽成 `memo` 组件；`FeedStatus` 拆到同目录 `feed-status.tsx`（一组件一文件）。
  - `components/discover-nearby/nearby-view.tsx`：`NearbyView` 包 `memo`。
  - `app/(tabs)/discover.tsx`：两页改用上述组件，传的 props 全部来自稳定 state / `useCallback` / `useMemo`（tab 变化不产生新值）；`FEED_GUTTER` 随 Feed 页迁走。
- **验证**：biome 5 文件 0 / `pnpm --filter luka typecheck` 0 / `pnpm --filter luka test` 122 files · 706 passed。
- **状态**：未提交，待真机复测（全量 Reload）。

## 工作日志 — 20:58 Luka Chats：白卡底部被标签栏遮挡

用户发实机截图（Chats 划到底）：「chat 列表划到底部会被遮挡，列表区域是不是应该再短一点」。

- **不是列表太长，是卡片底部让位算错了**。根因：`app/(tabs)/index.tsx` 白卡外边距写 `insets.bottom + 8`，注释称「iOS 26 上 bottom 已含系统标签栏」——对 Luka 不成立。Luka 标签栏在所有平台都是 JS 自绘 `TabBar`（`tabs-navigator.ios.tsx` → `PillTabs`，从未接 NativeTabs），绝对定位浮在场景上、高 63 + 安全区；裸 `insets.bottom` 只有 Home Indicator(34)，于是卡片底部 55pt 连同末行被栏盖住。2026-09-15 白卡改稿时引入，同仓 scene / into-you / me / discover 都用的 `useTabBarClearance()`，只有它例外。
- **修法**：改用 `useTabBarClearance()`（= bottom + 63 + 12），删 `useSafeAreaInsets` 引用，注释改成「标签栏是浮层，这一屏必须自己让位」。
- **改动文件**：`apps/luka/src/app/(tabs)/index.tsx`。
- **验证**：`pnpm luka:typecheck` 0 / `biome check` 该文件 0。没抢模拟器复验（并行会话正在用它测 Discover），待用户实机确认。
- **沉淀**：`30-Resources/troubleshooting/luka-tab-bar-clearance.md`（含三种 clearance 钩子的选用表 + 禁止 `insets.bottom + 常量`）。
- **状态**：规范模式，未提交。

## 工作日志 — 20:59 Luka Chats：爪印分割线改「铺到头像下 + 爪印居屏幕中」

用户：「每行中间的隔断的爪子期望居中于屏幕，隔断覆盖到头像下方」。

- **改动**：`components/chat/chat-row-divider.tsx` 的容器 `ml-[76px] mr-4` → `mx-4`。线从卡片内容区左沿（与头像左沿对齐）铺到右沿，左端伸到头像底下；容器左右对称 + 两段线各 `flex-1`，爪印落回整屏中线（上一版从文字左沿 76 起画，爪印偏右约 30pt）。
- **影响面**：`(tabs)/index.tsx` 的 `ItemSeparatorComponent` 与视频匹配行上方那条共用此组件，一并生效。`waves-you-sent.tsx` 有一条同款 76 缩进的**无爪**发丝线，用户没点，未动。
- **验证**：`biome check` 该文件 0 / `pnpm luka:typecheck` 0。规范模式，未提交。
