---
title: 2026-08-26
date: 2026-08-26
tags: daily
---

# 2026-08-26

## 今日重点

- [ ] 冷启定向邀请半屏:浏览器/真机 e2e 验证(在 `/` 弹、`/chat`·通话·提现弹窗时不弹、Join 切权益面板、关闭后 Me 入口仍在)
- [ ] 冷启邀请弹窗自动曝光/点击埋点(需求没提,待产品/数据侧给事件名)
- [ ] 直播 GoLive:`startHostLive` 补 try/catch + 失败 toast + 建房 loading(来自 live 会话 follow-up)
- [ ] nationwide 冷启弹窗已合入分支,联调后端 `getPwaTierInvitationPopup`(5068/5069)真实 should_show

## 工作日志

## 工作日志 — 02:43 sitin-next 直播 GoLive 建房从 WS 改为普通 HTTP

- **做了什么**:在 `sitin-next2` 的 `personal/zz/live` 分支(直播需求,规范模式,未 commit/push),把「Go Live & Earn」的建房接口 `requestGoLive` 从 WS 长连接改成普通 HTTP。
  - 新增 `packages/app-pwa/src/http/liveApi.ts` 的 `requestGoLive()`,走 `httpClient.requestPost2(LiveGoLiveRequest, {hostUserId,title?,dateModeOn?}, LiveGoLiveResponse)`(与 chatApi/liveApi 其它接口同构,proto id 8002/8003 走 protoMap)。
  - `packages/app-pwa/src/utils/startHostLive.ts`:import 从 `@/network/ws/liveGoLive` 改为 `@/http/liveApi`。函数签名/返回 `LiveGoLiveResponse` 不变,`!resp.roomId` 守卫照旧。
  - 删除 `packages/app-pwa/src/network/ws/liveGoLive.ts`(旧 WS 建房,一次性 listener + 10s 超时包 Promise)。
- **为什么**:用户指出这个接口本就是普通接口、不该走 WS。排查「App 里点 Go Live & Earn 没反应」时定位到旧 WS 版所有失败路径都是静默(WS 没连上 → sendMessage 返回 false → requestGoLive reject → 被 `void startHostLiveStream()` 吞掉)。
- **结果**:改动 3 文件(2 改 1 删)。`liveApi.ts` + `startHostLive.ts` eslint 0 error;tsc grep 这俩文件无报错(裸跑 tsc 有一堆 `bff.ts` unknown 报错是未构建 proto dist 的既有环境噪声,与本次无关)。改 HTTP 后建房失败会走 httpClient 的 Sentry `[API Error]` 上报,好排查。
- **关键命令 / diff**:`npx eslint src/http/liveApi.ts src/utils/startHostLive.ts`(exit 0)。
- **已 commit + push**(用户批准):`cbc32263a` refactor(app-pwa) 到 `personal/zz/live`(3 files +24/-63)。pre-commit 的 lint+circular+commitlint 全过(subject 不能 PascalCase 开头,「GoLive」被拒 → 改中文开头);**pre-push 用 `--no-verify`**:hook 跑全量 `turbo build` 被无关的 `app-cashier`(缺 node_modules,`@vercel/*`/styles.css 找不到)拖挂,连锁 SIGINT 掐了本来正常的 app-pwa build——记录在案的 sitin-next 例外,用户逐次授权。
- **沉淀**:`startHostLive` 仍缺 try/catch + 失败 toast + 建房中 loading,任意一步失败仍是「点了没反应」——已向用户提议补,待确认(follow-up)。GoLive 走 HTTP 而非 WS 的判断依据:该接口是一问一答的 RPC,不需要绑定在线长连接/服务端主动推(建房 8003 是同步响应)。

## 工作日志 — 03:05 直播建房传房间标题 + 合入 release/test-pwa3

- **做了什么**:
  1. `startHostLive.ts` 建房前用当前用户 `userInfo.username` 拼 `` `${username}'s Room` `` 作为 `title` 传给 `requestGoLive`;拿不到 username 则不传,由服务端走默认房名。(用户确认用 username 而非 nickname。全仓无 `roomName` 字段,`{username}'s Room` 原本是服务端默认房名,现改为客户端显式传。)
  2. commit `5f3735cae` feat(app-pwa) push 到 `personal/zz/live`。
  3. **合入 `release/test-pwa3`**:开独立 detached worktree(scratchpad,`origin/release/test-pwa3`,HEAD bbcff8df3)→ `merge --no-ff origin/personal/zz/live` 无冲突(仅 3 文件)→ push `HEAD:release/test-pwa3`(`bbcff8df3 → c9522e11e`)→ 删 worktree。
- **为什么**:直播间房名要按主播用户名显示,不依赖服务端默认。
- **结果**:eslint 0 error;pwa3 已含我 2 个 commit(refactor 改 HTTP + feat 房名)。
- **关键命令**:`git worktree add --detach <path> origin/release/test-pwa3` → `git merge --no-ff` → `git push --no-verify origin HEAD:release/test-pwa3` → `git worktree remove --force`(注意:worktree 属于 sitin-next2 的 git,删除要 cd 回 sitin-next2,不能在外层 zhangzheng 仓跑)。两处 push 均 `--no-verify`(pre-push 被无关 app-cashier 缺依赖拖挂,用户逐次授权)。
- **沉淀**:sitin-next 往 release/test-* 合并的安全套路 = 独立 detached worktree merge + push + 删,主 checkout 不受影响(已在 `troubleshooting` 有类似记录)。

## 工作日志 — 03:20 开播端能力增加 clickTime 时间戳字段

- **做了什么**:`startHostLive.ts` 的 `startHostLiveStream` 在函数**最开头**(建房 HTTP 请求之前)取 `Date.now()` 存 `clickTime`,加进传给端能力 `startHostLiveStream` 的 params。对应端侧新增 Kotlin 字段 `clickTime: Long`(Web 点击开播 epoch ms,作 prepare_duration 埋点起点,缺省 0)。
- **为什么**:端侧开播结果埋点要算 prepare_duration = 端能力收到时 - clickTime;必须在建房网络请求前取时间,否则把建房耗时也算进准备时长。
- **结果**:commit `ad0cc1894` feat(app-pwa) push 到 `personal/zz/live`;worktree 合入 `release/test-pwa3`(`d379df4c7 → 03443ed0a`,期间该分支被别人推进过一次,worktree 基于最新 origin 合的,无冲突,仅 1 文件)。eslint 0 error。
- **关键命令**:同上套路 worktree merge --no-ff + push --no-verify + remove。
- **沉淀**:端能力 params 目前字段 = roomId/liveRoomId/roomNo/title/dateModeOn/topics/beauty/**clickTime**(8 个)。

## 工作日志 — 04:30 消息金预上屏显示暴击 ×N 价(nationwide)+ 合入 release/test-pwa

- **需求**:聊天里「绿灯 ×5」消息下方金额会先闪基础价再跳 ×5,预期直接显示 ×5 后价。
- **根因**:该金额先由「预上屏」`PreviewMsgItem`(`ChatBody.tsx:185`)渲染,值来自 `getRewardCentsByType`(`useChatConv.ts`)只取基础 `cents`、**不乘暴击倍率**;后端结算的真实 `MessageItem` 才带 ×N → 闪现。crit 是 voice-only(proto `critHit`/`critHitMultiplier` 注释 text/image 恒 1),但绿灯下后端把该倍率应用到本次回复,与 `ChatFooter` 的 ×N badge 同源。
- **修法**(`feature/pwa-nationwide`,commit `fc8e155de`):`useChatConv.ts` 加 `getReplyRewardMultiplier(voice critHit ? critHitMultiplier : 1)`;`ActiveChat.tsx` 预上屏 `earnedCents *= rewardMultiplier`。**只改预览显示**,cloud/reportReply/埋点仍用基础价(后端自行按暴击结算,不重复乘)。eslint 0。
- **合入 `release/test-pwa`**(用户:nationwide 全部应在 release/test-pwa 上):push 前远端 nationwide 被推进,`-c rebase.autoStash` rebase(带 submodule 指针漂移)后 push(`fc8e155de`)。
- **⚠️ proto gen 合并踩坑(重要)**:merge 冲突在生成文件 `chat_match.ts`。两边分叉:nationwide 侧多 sitin4.1 免费消息新 message(冲突块,HEAD 侧空);release/test-pwa 侧某 message 有 `femaleUser` 字段。
  - **错法**:`git checkout --theirs 整份取 nationwide` → 把自动合并区的 `femaleUser` 也带走(误删)。
  - **对法**:**只删 2 处冲突标记、保留 theirs 新增**,其余自动合并区原样(femaleUser 在自动合并区,保留)→ 得并集。用 python 逐块「keep theirs/drop ours」。
  - 残留:3-way 自动合并本身把某 message 的 `femaleUser`(2 行:interface+createBase)按 nationwide 的 gen 去掉了(nationwide 该字段本就没,非我造成);app-pwa 无任何处读 `.femaleUser`,不编译报错。以 nationwide 状态为准放行。
- **结果**:merge commit `5c54c0498` push `release/test-pwa`(`485099769 → 5c54c0498`),6 个 nationwide commit + fix 全入;`rev-list --left-right release/test-pwa...nationwide = 164  0`(nationwide 完全并入)。两处 push `--no-verify`(pre-push app-cashier 缺依赖)。
- **沉淀**:生成文件(proto gen)merge 冲突,若两侧各有独有新增,**不能 `--theirs`/`--ours` 整取**(会丢对侧),要「只解冲突块取一侧、保留自动合并区」得并集;或按合并后 .proto 整包 regen(见本 daily 11:40 条的整包 regen 手法)。worktree scratchpad 中途会被环境清,改用 scratchpad 外 `/private/tmp/claude-wt-*`。

## 工作日志 — 04:45 定向邀请冷启弹窗 Me 页不弹(nationwide)+ 合入 release/test-pwa

- **需求**:`GetPwaTierInvitationPopupRequest` 触发的冷启定向邀请弹窗,不要在 me 页(`/me`)弹出。
- **做了什么**:`hooks/usePwaTierInvitationPopup.ts` 的 `blockedByLocalState()` 路径拦截加 `/me`(原只拦 `/chat`、`/live`)。命中即不调 `getPwaTierInvitationPopup`、不展示、后端不计次数。只拦**冷启自动弹窗**(仅 `tryShowInvitationPopup` 调 blockedByLocalState);Me 页主动点邀请卡片 `GradeInvitationCard` → `openInvitationPopup` 不受影响。
- **结果**:commit `e93b827bd` fix(app-pwa) push `feature/pwa-nationwide`;merge commit 合入 `release/test-pwa`(`5c54c0498 → 472dc4b03`,仅 1 文件,无冲突);`release/test-pwa...nationwide = 165  0`。eslint 0。push `--no-verify`(pre-push app-cashier 缺依赖)。

## 工作日志 — 05:20 消息金改 msgReward 同源 + 3K Club 面板接真接口去占位(nationwide→release/test-pwa)

- **① 消息金预上屏改用 `msgReward.totalCents`**(commit `00c1686ab`):之前(fc8e155de)用前端 `reward.cents × critHitMultiplier` 估算,与弹窗/最终消息可能不一致。改为:预上屏先不显金额(earnedCents 0),`reportReply` 返回后用 `msgReward.totalCents`(和金币弹窗同一个 reward,单位 1/100 美分)回填该组首条。三处同源。`previewMsgStore` 加 `setGroupReward`;`ActiveChat` send 回调回填;`useChatConv` 移除 `getReplyRewardMultiplier`(回退上一版估算)。
- **② 3K Club 权益面板全接真接口、去前端占位**(commit `93b48f0eb`):用户发现 Me 页「This week/Service quality this week/Stay」数字(420/600、$72/$100、2/3、4 qualified days)是 `gradeBenefits.deriveClubView` 里**写死的设计稿占位**(带 TODO(backend)),接口 `getPwaTierMe` 实际返回 0/无字段。改法:deriveClubView 全部真实派生,字段可选,无值不给 → 面板条件渲染。接线:今日 service quality 接 `serviceQualityPassed`;eligible video earnings 接 `requiredVideoIncomeMicros`(门槛)/`postBenefitVideoIncomeMicros`(收益),门槛=0 不展示;会员告警接 `consecutiveMissWeeks≥1→missed / exitWarning→at-risk`;底部权益接 `videoBonusBasisPoints`+`hasDailyVideoGuarantee`。**删除**本周 go-live、7 天服务质量日历、qualified service days 等后端无字段的占位块(整块不再展示)。
- **结果**:2 commit push `feature/pwa-nationwide`(`93b48f0eb`);merge 合入 `release/test-pwa`(`472dc4b03 → fc71c323f`,5 文件无冲突);`release/test-pwa...nationwide = 168  0`。eslint 0;gradeBenefits/GradeXiaomeiPanel 的 4 个 tsc 报错(PwaTierBenefitView/Progress 未导出)是**改前就有**的 proto 解析噪声(同命令对比 4=4),非本次引入。push `--no-verify`。
- **沉淀**:排查「页面数据真不真」= 对照接口返回 JSON 逐字段比 UI;不匹配的多半是前端写死占位(找 `deriveXxxView` 里的常量 + `TODO(backend)`)。裸 tsc 的 proto 报错要用「同 cwd 同命令 stash 前后对比」判断是否本次新增,绝对数无意义(memo 已多次记)。

## 工作日志 — 03:45 开播固定 Date 模式 + 点击开播静默挂断进行中的 mock

- **做了什么**(`startHostLive.ts`,commit `0fd79e8f9`):
  1. `requestGoLive` 传 `dateModeOn: true`——开公开直播固定走 Date 模式,服务端据此下发话题列表(8003 field7)。
  2. `startHostLiveStream` 开头调 `mockCallManager.cancelMockCall()`:有 mock 视频正在进行(Connecting/Connected)就**静默强挂**(复用真实来电抢占路径 `cancelMockCall()` 不传 reason → emit `MOCK_CALL_CANCELED` → `useMockCall.handleMockCallCanceled` 关弹窗/停响铃/停检测/结单/必要时导航,**无 toast/提示**),无活跃 mock 时 cancelMockCall 对 Idle/Ended/null 直接 return。然后照常建房开播。
- **为什么**:需求「点 goLive 后若 mock 视频在进行则强挂,挂断不要任何弹窗提示;没有则正常开播。只做这个拦截」。
- **踩坑/撤回**:先前按「加 isHostLiving store 标记 + scheduler 守卫屏蔽 mock 弹出」实现了 4 文件,用户否掉,要求改为「点击即静默强挂」的更简方案。已 `git checkout` 还原 liveStore/useLive/mockCallManager,`startHostLive.ts` 精准回退 isHostLiving(保留同文件未提交的 dateModeOn)。教训:mock 抑制不必造新状态,`cancelMockCall()` 就是现成的静默强挂入口。
- **结果**:eslint 0 error;commit `0fd79e8f9` push `personal/zz/live`;worktree 合入 `release/test-pwa3`(`f6dd9e0b7 → b1dca1b13`)。两次 push `--no-verify`(pre-push 被无关 app-cashier 缺依赖拖挂)。
## 工作日志 — 11:29 · sitin-next3 nationwide 加 getPwaTierInvitationPopup gen

**做了什么**
- 进 sitin-next3 `feature/pwa-nationwide`,给 `business-pwa-proto` 增量加 proto PR#453 的接口 `/getPwaTierInvitationPopup`(仅 gen)。
- 只改一个文件:`packages/business-pwa-proto/src/gen/archat_api/user_api.ts` 新增 `GetPwaTierInvitationPopupResponse`(interface+MessageFns)。submodule 指针不 bump、proto 源还原。
- build proto 包 + `app-pwa tsc -b` = 0 error(安全闸过)。

**为什么走 surgical 而非整包同步**
- 整文件同步 release/test 的 user_api.proto → `app-pwa tsc` 炸 7 处:release/test 把 app 在用字段改/删了(`benefitConfigVersion`/`termsVersion` 没了;`videoIncomeTargetMicros` 被后端改名 `requiredVideoIncomeMicros` 并加考核字段 service_quality_passed 等)。连带改 app 超出本任务范围,故只加目标 message。

**坑 & 手法(已另写 troubleshooting)**
- committed gen 的 `PwaTierGuaranteeProgress.videoIncomeTargetMicros` 是**前端先行手写占位**,不在任何 proto commit → 直接从某 commit regen 会丢它、diff 变脏。手法:以「记录指针 3884fef0 proto + 手补占位字段 + 追加目标 message」为底 regen,diff 才只剩目标 message。
- **sitin-next3 是并发会话共享 checkout**(daily 已警告被反复横跳分支)。本次全程锁定在 `feature/pwa-nationwide`;gen 改动未提交,已备份 patch 到 scratchpad 防横跳丢失。

**残留(需后端/后续)**
- 该接口**前端还调不通**:① 无 `GetPwaTierInvitationPopupRequest`(httpClient.requestPost2 靠 Request 的 $type 推 protoId);② ProtoConfig 未登记 protoId → protoIdMapping 无此接口。两者是后端侧,PR453 只加了 .proto message。
- 未提交(禁止自动 commit),等用户决定。提交时只 stage `user_api.ts`,勿带 submodule 指针漂移。

## 工作日志 — 11:40 · sitin-next3 nationwide 整包更新 gen 到 release/test + app 对齐字段

**做了什么(接上一条,用户改要求为「更新全部 gen,字段按 gen 来」)**
- submodule 整包切 `origin/release/test`(9994c8b6),全量 regen。用 `grep -vE '^[+-]//\s+protoc\s+v'` 过滤掉 15 个纯 protoc 版本噪声文件,只留 7 个真变化 gen(chat_match/chat_api/contact_exchange/live_api/messaging_api/payment_api/user_api)+ protoIdMapping(净 19 增 0 删,`S2C_MatchSuccessPush` 的 `-` 是尾逗号 reflow 假象)。
- **submodule 指针 bump** 3884fef0 → release/test HEAD 9994c8b6(整包更新,让指针与 gen 一致)。
- app-pwa 按 gen 对齐字段(release/test 的迁移):
  - `videoIncomeTargetMicros`(GuaranteeProgress)→ 迁到 `PwaTierBenefitView.requiredVideoIncomeMicros`。`gradeBenefits.ts:62` 改成 `benefits?.requiredVideoIncomeMicros`;mock 的 $80 门槛移到 `XIAOMEI_BENEFITS`。
  - 删除后端已移除字段:`benefitConfigVersion`(Benefit/Membership)、`termsVersion`(Invitation)—— 仅 mock 引用,直接删。
- 验证:`app-pwa tsc -b` = 0、`app-pwa lint` = 0、`web-util-http` build ok。仅 app-pwa/web-util-http 消费该 proto。

**最终改动**:app-pwa 2 文件(pwaTierMock.ts / gradeBenefits.ts)+ submodule 指针 + 7 gen + protoIdMapping。未提交(禁止自动 commit)。patch 备份 scratchpad。

**残留**:`getPwaTierInvitationPopup` 仍调不通 —— 无 Request message + ProtoConfig 未登记 protoId(mapping 里没有),后端侧待补。

## 工作日志 — 13:53 · sitin-next3 nationwide 冷启定向邀请半屏弹窗

**需求**:满足受邀条件用户,冷启动 App 时弹定向邀请半屏;每12h≤1次、累计≤5次;提现弹窗/通话中/接恋爱铃/Chat·Live 页时不弹;拦截不计次数;加入/过期/满5次停弹;关闭后 Me 页常驻入口仍可看;自动弹与常驻入口复用同一半屏。接口 `GetPwaTierInvitationPopup`。

**关键分工**:次数(12h/5次)+ 恋爱铃 + 邀请有效期 由**后端**接口判(should_show);前端只自查 a.提现弹窗 b.通话/呼叫中 c.Chat/Live 页,全过才调接口(拦截即不调 → 不计次数)。

**后端修复**:proto release/test 补了 `GetPwaTierInvitationPopupRequest`(protoId **5068**)+ Response(**5069**);gen 整包同步到 832e48c8(submodule 指针 bump,只 user_api.ts + protoIdMapping 变,app tsc 0)。

**前端实现(app-pwa,复用现成件)**:
- `http/pwaTierApi.ts`:加 `getPwaTierInvitationPopup()`(requestPost2 + clientContractVersion=1)。
- `stores/pwaTierStore.ts`:抽 `pickTierFields(res)`(fetch/弹窗共用);加 `invitationPopupOpen` + `openInvitationPopup(me)`(用 me 填字段并开)+ `closeInvitationPopup()`。
- `hooks/usePwaTierInvitationPopup.ts`(新):冷启触发,模块级 `attempted` 一次冷启只跑一次;`blockedByLocalState()` 查 cashout-modal(`useModal`)/`callStore.callState!==Idle`/`window.location.pathname∈{/chat,/live}`;全过调接口,should_show && me.pendingInvitation 才 `openInvitationPopup`。**自查只放调接口前**(计数=展示一致,不 await 后二次拦截)。
- `components/PwaTierInvitationPopup.tsx`(新):全局挂载,复用 Me 页 `GradeBoostSheet`;isXiaomei 按 `invitation.type===XIAOMEI`;onJoin→`respond(ACCEPT)`成功即关。
- `components/showCashoutModal.tsx`:导出 `CASHOUT_MODAL_ID` 供自查复用。
- `App.tsx`:`usePwaTierInvitationPopup(isReady && FullRegister && userId)` + 渲染 `<PwaTierInvitationPopup/>`(挨着 GlobalModal)。

**验证**:app-pwa `tsc -b`=0、`lint`=0。未跑浏览器 e2e(bot 环境)。未提交(待用户批准)。

**踩坑/决策**:
- app-pwa **无测试基建**(0 test 文件、无 vitest),按包既有约定不强加单测。
- 计数语义:后端 should_show=true 即计一次,故前端不能在 await 后二次拦截(否则计了没弹),自查只前置。
- 已知小 race:onboarding 完成用户冷启会 redirect /chat,若弹窗 effect 早于 redirect 在 "/" 命中则会盖在 /chat 上(概率低,决策点在 "/")。

## 工作日志 — 15:56 · 消 PR#960 submodule 指针冲突(独立 worktree)

**背景**:PR#960(`personal/zz/pwa-spv2` → `feature/pwa`)冲突,唯一冲突文件是 submodule 指针 `packages/business-pwa-proto/proto`(base `7e2982c9` vs head `3329ee04`)。用户要「去掉 proto 提交」消冲突。

**做法(关键:主 checkout 被另一会话占用+脏,绝不能碰)**:
- 主 checkout 当时在 `personal/zz/admin-pwa-benefit-pool`,有别的会话未提交的 minerva-pwa-pool 改动 → 全程用 **detached worktree**(`/Users/max/Dev2/zhangzheng/.sitin-wt-spv2`,基于 origin/personal/zz/pwa-spv2)操作,不碰主 checkout。
- 冲突消法:把 head 的 submodule gitlink 对齐到 base 的 `7e2982c9`(`git checkout origin/feature/pwa -- packages/business-pwa-proto/proto`),**只改 gitlink 不动 src/gen**(gen 是代码、按既有约定不随指针 bump)。3-way merge 两侧 gitlink 相同 → 冲突消。
- 单 commit `272c98613`,fast-forward 推 `HEAD:personal/zz/pwa-spv2`,worktree 用完即删。
- 结果:`gh pr view 960` → `mergeable: MERGEABLE`。

**沉淀**:共享 checkout 上给「非当前分支」做 git 手术,一律 detached worktree + 只推 `HEAD:<branch>`,主 checkout 零接触。submodule 指针冲突的通用消法 = 把一侧 gitlink checkout 成对侧值(不动 gen)。

## 工作日志 — 17:15 · 从 PR#1023 剔除 submodule 指针提交

**背景**:PR#1023(`personal/zz/update-proto2` → `feature/pwa`,单提交 `d7d1e5a20`)同时改了 submodule 指针(7e2982c9→87b11639)和 gen `messaging_api.ts`(+6)。用户要「不提交 packages/business-pwa-proto/proto」。
**做法**:detached worktree(`.sitin-wt-up2`,基于 origin/head)→ `git checkout origin/feature/pwa -- packages/business-pwa-proto/proto` 还原指针 → `commit --amend` → 提交变 `1f20afb45`,只剩 messaging_api.ts → `--force-with-lease` 推(单提交个人分支,lease 基准 d7d1e5a20 校验通过)→ 删 worktree。
**结果**:PR#1023「Files changed」只剩 `messaging_api.ts`。主 checkout(另一会话在 admin-pwa-benefit-pool)全程零接触。
**沉淀**:去掉某分支里 submodule 指针改动的两法——① 单提交 tip 用 amend + force-with-lease(最干净);② 中间/多提交则加一个把 gitlink 还原到 base 的跟进提交,净 diff 为 0。均在 detached worktree 做,`git checkout <base> -- <submodule路径>` 只改 gitlink 不动 gen。

## 工作日志 — 17:32 · 整理 8.17–8.21 工作并更新前端周会文档

- **做了什么**:读 50-Daily 8.17–8.21 五天日报,整理成 6 大类 + 25 子任务,替换「8月21日前端周会」文档里张峥的「本周工作」多维表格(app `E6uXbPyhna4SmusB6dhcwG0QnEb` / table `tblbHPtieRhwvxVH`)。删旧 13 条 → 建 6 父(顶层大类)+ 25 子(父记录挂靠)。
- **六大类**:①权益池 C 端(#962/#980/#981) ②权益池 Minerva 后台(#965/#966) ③消息金(#959) ④直播 Go Live & Earn ⑤多社媒 SP V2+CE(#960) ⑥Snapchat 脚本。用户要求去掉「冷启动/鉴权+加载优化」类。
- **身份**:用户明确「用步川」(该周会文档共享,步川有写权限),未切张峥。
- **坑**:① `base +record-batch-create --json {fields,rows}` 会**真创建**,但 `record-list --format json` 输出坏(显 0 条),要用默认 markdown 格式核对——我因此误判失败重跑一次 → 建了 6 组重复,删掉第二批(recvtosrP9*)才对。以后 batch-create 后一律用默认格式 record-list 核数。② 层级表:先建父拿 record_id,子记录 `父记录` 字段填 `[{"id":"rec..."}]`。

## 工作日志 — 06:09 · PR#1026 snapchat 图片 base64 压缩

- **做了什么**:sitin-next `app-ins-scripts` 的 snapchat 脚本,所有获取图片 base64 的路径统一压缩为 JPEG(长边≤1280、质量0.7)以减小回传体积。
- **实现**:新增共享函数 `utils.compressImageToDataURL(blob,{maxDim,quality})`——`createImageBitmap` 解码 + canvas 长边等比缩放(只缩不放)→ `toDataURL("image/jpeg",q)`;仅 `image/*`,`image/gif`/非图/缺 API/解码失败/跨域污染 canvas 一律返回 null → 调用方回退原图(绝不因压缩丢图)。接入 `utils.blobUrlToBase64`、`getChatMessages.fetchMediaAsBase64`(仅 image,audio 原样)、`getChatMessages.captureVideoPosterAsBase64`(视频封面帧加长边缩放+降质 0.8→0.7)、`getStory.blobUrlToBase64`(getFriendStory 复用 collectPages,自动生效)。`bridge.js` 的 `arrayBufferToBase64`(Android 协议二进制)不动。
- **改的文件**:`utils.js` / `getChatMessages.js` / `getStory.js`(3 文件)。`node scripts/build.mjs` ✅ 重建 dist。PR **#1026**(`personal/zz/sp-snapchat-img-compress` → `feature/sp`),head `99400fbe4`。**按用户要求不带测试文件**(原写过 sc-image-compress 单测,后 `git commit --amend` 移除 + `--force-with-lease` 重推)。
- **关键坑/做法**:用户当前工作分支 `personal/zz/sp-snapchat` 有 **1185 个 staged 未提交改动**(整坨 app-cashier 等),直接切分支或提交会把无关改动全带进去。→ 全程用**独立 git worktree**(`.wt-sp-imgcompress`,基于 sp-snapchat HEAD)+ **symlink 根 node_modules**(pnpm hoisted,build/test/pre-commit 的 pnpm lint 均可直接复用,无需重装),commit/push/PR 后 `git worktree remove --force`,主 checkout 全程零接触。
- **环境备忘**:`app-ins-scripts` **无 lint 脚本**(只有 build/test/upload);`dist` 被 gitignore **不入库**,只提交 src+test,但**改 src 后必须 `node scripts/build.mjs` 重建 dist**(线上用 esbuild bundle 的 dist)。build 是 iife bundle,`a:utils` 排最前 → methods 里 `var utils = SP.utils` 可直接复用共享压缩函数。pre-commit = `pnpm lint` + `pnpm circular` 全过。
- **PR base**:用户定为 **`feature/sp`**(分支基点 sp-snapchat HEAD 是 feature/sp 祖先,改 base 后 diff 仍只含这 1 个压缩 commit)。PNG 透明图会转 JPEG 变黑底(聊天/story 基本是照片,默认统一转 jpeg,如需保留透明可调)。

## 工作日志 — 18:00 · sitin-next 撤回 #960 并 reland(两个先后 PR)

- **做了什么**:用户要从 `feature/pwa` 撤回 `d2e5198`(#960 多社媒 SP V2 IG+Snapchat/CE),再重新提一个同内容 PR。确认为 **revert-and-reland 两 PR**、base 均 feature/pwa。产出:PR **#1027**(revert `personal/zz/revert-960`→feature/pwa)+ PR **#1028**(reland `personal/zz/reland-960`,先堆叠在 revert-960 上)。
- **关键判断:只 revert app-pwa,proto 全保留**。`d2e5198` = app-pwa 功能源码 + 重新生成的 proto;#960 之后 #1022/#1023 已两次重生 proto,直接 `git revert d2e5198` 在 `business-pwa-proto/src/gen/*`(user_api/messaging/contact_exchange/protoIdMapping + live_api modify/delete)全冲突,且会把 proto 倒退两版。→ 解决:`git checkout HEAD -- packages/business-pwa-proto/` + `git add` 全还原,`live_api.ts`(HEAD 已删)用 `git rm` 删掉残留。最终 revert 仅 **48 个 app-pwa 文件**,`git diff HEAD -- business-pwa-proto` 为空。
- **reland 做法**:`git revert` 那个 revert 提交(revert-the-revert)→ 精确加回 48 个 app-pwa 文件。校验 `git diff d2e5198 HEAD -- packages/app-pwa/` **为空**(与原提交 app-pwa 逐字节一致)。
- **stacked PR**:PR2 base 设 `personal/zz/revert-960`,diff 干净只含 reland;PR1 合并+删分支后 GitHub 自动把 PR2 base 切回 feature/pwa。用户流程即「先合 #1027,再走 #1028」。
- **环境**:主 sitin-next checkout 在 `personal/zz/sp-snapchat` 有大量未提交改动 → 全程用**独立 worktree**(scratchpad)操作,完事 `git worktree remove --force`,主 checkout 零接触。gh 身份已确认 colna。
- **未做**:隔离 worktree 无 node_modules,tsc/lint 依赖 CI(app-pwa 树等价 #960 前状态,本就编译通过;新 proto 为向后兼容增量,风险低)。

## 工作日志 — 19:20 · reland #1028 修复:保留 #935 实验感知 onboarding

- **背景**:排查「onboarding 任务完不成」确认根因是 #960(d2e5198)移植 SP V2 时,把 feature/pwa 的 **#935 A/B 实验 onboarding 子系统**整体换成 sitin4.1 固定集模型 → `onboarding_b` 完成判据里没 B 组任务(GoLive/Chat/Profile/VideoCall2Times),永远判不完成,且 B 组任务点击处理器也被删。我的 revert #1027 已合进 feature/pwa 把它修好;但 reland #1028(revert-the-revert)会**原样重放 d2e5198 → 再次打坏**。
- **深挖发现**:不是「3 个文件」,是**两套 onboarding 架构 + SP V2 交织**。归类 d2e5198 的 48 个 app-pwa 文件,6 个含 onboarding 回退、5 个混着 SP V2。且 App.tsx 的 SP-V2 是**聊天准入策略整体重写**(任一社媒授权解锁 vs #935 完成 onboarding 解锁,互斥);display/judge 必须同源否则照样完不成 → **onboarding 实验簇必须整体走好版**。
- **做法(用户定:onboarding 好版 + 加 SP V2,先推核心)**:worktree 隔离 + symlink node_modules。把 7 文件恢复 feature/pwa 好版(useOnboardingCompleted/useTask/useUserInit/constants/onboardingTasks/OnboardingTaskList/App/sitin4Debug)。唯一编译适配:OnboardingTaskList 的 IG 卡额外奖励 `insExchangeStore.pendingExchangeReward` → SP-V2 多平台化后的 `pendingByPlatform.instagram ?? 0`。SP-V2 纯功能文件全保留。
- **验证手法(重要经验)**:worktree symlink 主 clone node_modules 会让 `@heyhru/business-pwa-proto` 解析到主 clone 的别分支 proto → tsc 冒出一堆假 proto 债(chatApi 36 条等)。**绝对错误数无意义,要用增量对比**:`git stash` 我的改动跑 pristine tsc → `comm -13` 对比。结论:增量 0 个真实新错误。tsc 6.0.2 `-b` 撞 baseUrl 弃用报错,`--ignoreDeprecations` 不能配 `--build` → 改 `tsc -p tsconfig.json --ignoreDeprecations 6.0`(该配置本就 noEmit)。eslint 全量 exit 0。
- **产出**:commit `1b63321` push 到 `personal/zz/reland-960`(更新 PR #1028)+ PR 评论说明。
- **Follow-up(架构互斥,需产品决策,本 PR 不含)**:①App.tsx 多社媒聊天准入 ②首页 SocialConnectCard(可加性移植)③Snapchat onboarding 任务卡(依赖 registry 模型)。
- **仍待办**:生产 `release/online-pwa` + `release/test-pwa2` 的 onboarding 仍是坏版(固定集),需单独用好版恢复这几个文件、提 PR。

## 工作日志 — 20:10 · online-pwa-pre 对齐 feature/pwa(PR #1029)

- **背景**:想把 `release/online-pwa-pre` 重置成 `feature/pwa`,但该分支受保护、禁止 force-push,colna 非 admin/maintain → 强推被拒(`protected branch hook declined`)。
- **改法(重要经验)**:用 PR 达到「内容对齐/等效 reset」:`git commit-tree feature/pwa^{tree} -p release/online-pwa-pre -m ...` 造一个**以 pre 为父、tree 取自 feature/pwa** 的提交(纯 plumbing,无需 worktree),推成新分支 `personal/zz/align-pre-to-pwa`,提 PR 合入 pre。合并后 pre 文件内容与 feature/pwa 逐字节一致(历史保留,满足保护)。已校验 `commit^{tree} == feature/pwa^{tree}`。
- **产出**:PR **#1029**(base `release/online-pwa-pre`),`mergeable`、仅被保护规则 BLOCKED,356 文件 +67548/−55711。
- **坑**:sitin-next 的 pre-push husky 全量 build 钩子在无关包 `@heyhru/app-ins-scripts#build` 上失败 → 推分支用 `--no-verify`(既定例外)。
- **另**:排查确认 reland(#1028)已完整保留 #935 onboarding 核心(~40 文件与 feature/pwa 逐字节同);7 个文件 SP V2 覆盖了 #935 行,其中 auth 层属预期重设计,唯 `AIPersonaPlaceholder` 与「App.tsx 保留好版」不同源 → B 组未完成 onboarding 时聊天占位文案错(功能仍拦截),属 follow-up #1。

## 工作日志 — 20:45 · reland 按「好版为基」修 AIPersonaPlaceholder 聊天准入

- **原则(用户定)**:reland 以 feature/pwa 好版内容为基,多社媒(SP V2)需要时在其上加。用来定之前 A/B 的选择 → 选 B(好版为基)。
- **修的不一致**:App.tsx(好版)setAuthBlock 五态,但 reland 的 AIPersonaPlaceholder 是 SP-V2 版、删了 onboarding-incomplete/float-permission-missing 变体 → B 组未完成 onboarding 聊天占位文案错(功能仍拦截)。→ AIPersonaPlaceholder 恢复 #935 好版。
- **唯一 API 适配**:好版 ig-login-lost 用了 SP-V2 已废的 `showInsModal(...4参 forceReauth)`。SP-V2 语义:`authorizeOrLogin(platform,source)` 已授权→`openSocialProxyWebView`(重登)、未授权→弹 auth modal;正好等价 forceReauth。改成 `authorizeOrLogin("instagram","workspace")`。ig-unbound 的 `showInsModal("workspace")` 本就 2 参兼容。
- **留 SP-V2 的判断**:`AiGoldCashoutCard` 不消费 authBlock、是多社媒授权卡(AuthorizeSocialCard,dev 预览 /dev/spv2 也依赖),属「已加的多社媒」,保留 SP-V2(回好版会连带断 Spv2ModalsPreview,且需 showInsModal 适配)。作为待用户定的剩项。
- **产出**:commit `ace4a31` push `personal/zz/reland-960`(更新 #1028)。tsc 增量 0、eslint 0。
- **验证手法**:worktree symlink node_modules 后用 `tsc -p tsconfig.json --ignoreDeprecations 6.0` + `comm` 增量对比(pristine 12 → final 12,0 新增)。

## 工作日志 — 21:10 · 修正 #1028 base 到 feature/pwa 并重建分支

- **问题**:#1028 base 当初 stack 在 revert-960(建时 feature/pwa 还带 #960)。#1027 已合进 feature/pwa 后,应把 base 改回 feature/pwa。但 reland-960 是 `revert + revert-the-revert` 历史,合并基算到旧 feature/pwa(b08d6185,带 SP-V2)→ 光 retarget 会 CONFLICTING、合并时可能把 SP-V2 抵消。
- **修法(经验)**:两点 tree diff 确认 reland-960 的**树**是对的(feature/pwa vs reland = 42 个 app-pwa 文件 = SP-V2 重新加回 + 好 onboarding,proto 等一致)。用 `git commit-tree <reland树> -p feature/pwa` 重建成**单提交挂当前 feature/pwa**,`--force-with-lease` 推个人分支。合并基变 feature/pwa,#1028 → base=feature/pwa、MERGEABLE、42 文件干净 diff、可 fast-forward。
- **要点**:revert+revert-the-revert 的 stacked 分支要改基到「已独立 squash-revert 的目标」时,别只改 base(三点 diff 会抵消 + 冲突),直接用 commit-tree 把正确的树重挂到目标分支上。
- 新 reland-960 = `9318c03`(单提交)。

## 工作日志 — 21:40 · AIPersonaPlaceholder 授权空状态回 SP V2 多社媒样式

- **需求**:用户看截图发现聊天占位样式变了(我上一步回好版 → IG 单按钮「Earn While You Sleep」);要求这页回 SP-V2 多社媒样式。
- **做法(好版为基叠多社媒)**:不整文件换回 SP-V2(否则 onboarding-incomplete/float 又错渲)。保留好版 5 态状态机,仅把 ig-unbound / ig-login-lost 渲染换成 SP-V2 `EarnToChatState`(icEarnToChat 图 +「Earn to chat」+ IG/Snapchat 双按钮,useUA 非 haven 隐 Snapchat)。其余态(not-in-apk/onboarding-incomplete/float-permission-missing)保持好版文案。拆 EarnToChatState/ConfigPlaceholder 两子组件避免条件 hooks。
- **产出**:commit `1e8aba41` push `personal/zz/reland-960`(#1028)。tsc 增量 0、eslint 0。SP-V2 素材(ic_earn_to_chat/ic_logo_ins_rect/ic_logo_snapchat)+ useUA/SocialPlatform 在 reland 均在。
- **经验**:两版占位文案锚点——好版 ig-unbound=「Earn While You Sleep / Authorize IG」(IG 单按钮);SP-V2=EarnToChatState「Earn to chat / Login & Authorize Instagram+Snapchat」(双按钮)。

## 工作日志 — 22:00 · 修「Authorize Snapchat 一次性任务点击没反应」

- **根因**:reland 的 useTask 保留 #935 好版,好版没有 Snapchat 授权任务(TaskId.BindSnapchatAccount=137)的分支;点击落到 switch default → 无动作。(#960 的 SP-V2 useTask 本有此分支,但我为保 onboarding 好版整体用了好版 useTask,漏了这个多社媒点。)
- **修法(好版为基加多社媒)**:useTask 三处补 Snapchat——import authorizeOrLogin;getTaskTitle 137→兜底 "Authorize Snapchat";switch 137→`authorizeOrLogin("snapchat","media_task_from_tasklist")`(未授权弹 Snapchat 授权弹窗,已授权走 openSocialProxyWebView 重登)。
- **产出**:commit `bf91e4cc` push `personal/zz/reland-960`(#1028)。tsc 增量 0、eslint 0。
- **经验**:「onboarding 簇整体走好版」时,若某好版文件(如 useTask)同时是多社媒 SP-V2 的落点,会漏掉该文件里的多社媒分支;需按需把多社媒点逐个补回(BindSnapchatAccount、EarnToChatState 等),这些都是「好版为基 + 多社媒在其上加」的补丁。

## 工作日志 — 22:40 · 修 #1028 不能合并(feature/pwa 前进致冲突)

- **原因**:我把 reland 建到 c6dab3bb4 后,feature/pwa 又合了 #972(`ab86de84d` Hipay 手续费 + cashout_source 透传),和 reland 都改 `services/cashoutNew.ts` → PR 变 CONFLICTING/DIRTY。
- **修法**:worktree 里 `git merge origin/feature/pwa`,唯一冲突 cashoutNew.ts 的 IG 后置门授权调用。解为 **SP-V2 签名 `showInsModal("cashout",{onAuthorized})` + #972 的 `submitCashOut(amount,entry,cashoutSource)`**(两边意图都保留)。合并自动带入 #972 的新 proto(含 CashOutSource)。
- **验证坑**:tsc 冒出 8 个 `CashOutSource 未导出` 报错——是 worktree symlink 让 proto 解析到主 clone 旧 proto 的**假报**(合并后 worktree proto 有 CashOutSource=feature/pwa 一致=12,reland 旧 proto=0,主 clone=0)。排除后 =基线 12,解冲突零真实新错误。eslint 0。
- **产出**:merge commit `8b836403`。#1028 恢复 **MERGEABLE**,仅剩 `REVIEW_REQUIRED`(需 reviewer approve)。
- **经验**:reland 长期开着时 base 分支会前进,commit-tree 重建法会过期;后续冲突用 `git merge base 分支`解即可。symlink 跑 tsc 时新 proto 类型(CashOutSource 等)是假报,别当回归。
