---
title: 2026-09-08
date: 2026-09-08
tags: daily
---

# 2026-09-08

## 今日重点

- [ ] koda buglist R00218/R00219/R00220（订阅 / 访客订阅 / like+访客数据）——已定位，等后端确认 koda 租户商品与数据
- [ ] koda 端到端 EAS 真机验：审核双号登录（`review@` / `metareview@kodasoc.com`，同密码）、恋爱铃三子系统（需后端推 S2C）、图片发送
- [ ] `apps/koda/tests/config.test.ts` pre-existing 失败（`appName` 实为 `koda_android`，断言写的 `koda`）+ BytePlus appId/appName 仍是 lumi 串包值

## 工作日志

### 00:20 — 排查 koda buglist R00218/R00219/R00220（订阅 / 访客订阅 / like+访客）

- **做了什么**：从飞书多维表格「RN Android Koda bug list汇总」（wiki `AiWBwAFiwiZ3L8kYi1Rcdvklnld`，base `EDjmba79Ga41Cbs7lRUcrjUPnGf`，table `tblGoES4Sw6IgEmt`）取三条 P0，下载截图，逐条对代码。
- **结论**：客户端**全都接了真实 RPC**，三条同一病灶 —— 后端返回空 / 非 Success 业务码被吞。
  - 订阅两条走 `usePlanCatalog` → `GetSubscriptionsV3`（`normalSubscriptions` / `visitorSubscriptions`）；like+访客走 `@heyhru/business-vibes` → `GetMatchLikedList` / `GetVisitorList`。
  - 截图里**既无转圈也无 "Couldn't load plans."**，只有空的 CHOOSE YOUR PASS + 灰 CTA ⇒ `plans=[]` 且 `error=false`。
- **坑（值得记）**：`packages/business-payments/src/subscriptions.ts:100` 与 `packages/business-vibes/src/vibes.ts:45/65` 都是 `if (res.code !== Success) return []` —— **非 Success 返回空数组而不抛错**，UI 的 error 分支只有网络层异常才走。于是「后端报错」和「没东西卖 / 没人 like 你」在界面上完全一样。iris 用的是同一份共享引擎，同样中招，只是它后端有数据所以看不出来。
- **关键命令**：`lark-cli base +url-resolve --url <wiki url> --as user`；`+record-list --filter-json '{"logic":"or","conditions":[["BugID","==","218"],...]}'`（BugID 是 `auto_number`，**filter 只吃数字**，传 `"R00218"` 报 `800010509 Only number values are supported`）。
- **没验成**：想用 Grafana/Loki 查 `GetSubscriptionsV3` 日志坐实，`grafana-k8s.sitin.ai` 当时整站 502（`/api/health` 也 502）。

### 00:45 — 去掉 koda 的 `realPaymentsProvisioned` 开关

- **为什么**：这是 koda 有、iris 没有的一个硬门禁。`IAP_ENABLED = realPaymentsProvisioned && iapEnabled`、`DTC_ENABLED` 同理，而它在 `app.config.ts` 里写死 `false` ⇒ **koda 在任何 profile 下 IAP 与 DTC 都是关的**，只剩 dev 的 `Fake*`。用户要求先去掉。
- **改了哪些文件**（`sitin-rn` @ `feature/koda-android`，未 commit）：
  - `apps/koda/app.config.ts` 删 `features.realPaymentsProvisioned: false`
  - `apps/koda/src/services/payment-config.ts`：`IAP_ENABLED`/`DTC_ENABLED` 直读 `iapEnabled`/`dtcEnabled`，`BILLING_ACTIVE = iapEnabled || fakePaymentsEnabled`（与 iris 一致），重写头部注释取值表
  - `apps/koda/docs/05-chat-billing.md`、`apps/koda/README.md` 同步（AGENTS.md 规则 4）
- **效果范围（要点）**：**只影响 production**。preview / nightly 下 `iapEnabled = isProduction = false`，改前改后 `IAP_ENABLED` 都是 false、`BILLING_ACTIVE` 都是 true —— **测试包行为一字不变**，仍走 `Fake*`。想让测试包能验真实订阅，还得把 `dtcEnabled: isProduction` 改成 iris 的 `true`（iris 注释即写明「development QA / preview·nightly AB validation」都要）。
- **验证**：`pnpm lint` exit 0（仅 lumi 2 条 pre-existing warning）、`pnpm boundaries` ✓、`pnpm typecheck` 全 Done、`pnpm koda:test` **359 passed / 1 failed**（失败的是分支自带的 `config.test.ts` appName 断言，与本改动无关）。
- **注意**：`realPaymentsProvisioned` 在 **lumi 保留不动**（`docs/payments.md` §商店身份未落实那段仍只讲 lumi）。
