---
title: 2026-09-06
date: 2026-09-06
tags: daily
---

# 2026-09-06

## 今日重点

- [ ]

## 工作日志

## 工作日志 — 22:45 koda 聊天页消息列表抽成 memo 组件(对齐 iris 手感)

- **做了什么**:在 `feat/koda-ce`(PR #394)上,把 `apps/koda/src/app/chat/[id].tsx` 里内联的聊天消息 `FlatList` 抽成独立组件 `apps/koda/src/components/chat/chat-message-list.tsx`(`memo(ChatMessageList)`)。组件内部保留 inverted 布局、memoized `contentContainerStyle` 与 `renderItem`、greeting 面板作为 header;导出 `THREAD_BOTTOM_PADDING`。屏幕侧删掉 `invertedRows` / `listContentStyle` / `renderChatRow` 三个 memo,新增 `bottomPadding` useMemo,删除无用 `styles.list` / `styles.listFlex` 及 `Spacing` import。
- **为什么**:用户反馈「iris 手感明显比 koda 好」,键盘升起时仍有卡顿。根因是 koda 的 `ChatMessageRow` 虽已 memo,但列表内联在屏幕里,屏幕每次 re-render(输入、presence、抽屉开合、键盘)都会重算列表;iris 把列表隔离进独立 memo 组件做到了 render 隔离。这是两包最后残留的差异。
- **结果**:biome / `pnpm --filter koda typecheck` / `pnpm boundaries` 全过;`pnpm --filter koda test` 294/295 过(唯一失败 `tests/config.test.ts` 是既有问题:appName `koda_android` vs `koda`,与本次无关)。已 commit + push 到 `feat/koda-ce`(617e681e)。原生键盘/渲染行为需 EAS 真机验证。
- **关键命令 / diff**:`pnpm lint:fix` / `pnpm --filter koda typecheck` / `pnpm boundaries` / `git push`(pre-push 跑 boundaries+typecheck 均过)。
- **沉淀**:RN 列表卡顿排查顺序 —— ① 行组件 `React.memo` ② `renderItem` / callback props 稳定(useCallback + 避免内联箭头)③ `contentContainerStyle` memoized(内联数组会触发 FlatList 重新测量)④ **把列表整体抽成独立 memo 组件**做 render 隔离(前三步都做了仍卡时的最后一招)。

## 工作日志 — 23:05 koda 聊天输入框升降卡顿:memo 化 composer 子树

- **做了什么**:抽 list 后用户反馈「输入框上升下落时还是卡顿」。在 `chat/[id].tsx` 用 `useMemo` 把 `KeyboardStickyView` 里整块 composer 子树(billing banner + KodaExchangeChips + ChatInputBar + ChatActionBar)缓存成一个 element,**依赖数组刻意不含 `keyboardInset`**;JSX 改为 `<KeyboardStickyView>{composer}</KeyboardStickyView>`。commit `af3166c3`。
- **为什么**:对比 iris,iris 的 `KeyboardStickyView` **只包一个 `ChatInputBar`**(轻子树);koda 因产品要求把工具栏也塞进去随键盘 transform,子树大得多。而每次键盘开合触发 `setKeyboardInset` → 屏幕 re-render → 重建含 `TextInput` 的整块 composer,正好和 transform 动画抢帧 → 升降掉帧。两个 bar 都不是 `memo`(iris 的 ChatInputBar 也不是,但它靠子树小 + 重渲染便宜扛住)。memo 化 element 后,keyboardInset 变化时 React 拿到同一引用直接 bail out 整个子树,transform 独占动画。
- **结果**:biome / koda typecheck / boundaries 全过。已 push `feat/koda-ce`(PR #394)。**根因判断:消除「re-render × transform 抢帧」这条最可能路径;若真机仍卡,剩余嫌疑是 transform 大子树本身的合成开销(ChatActionBar 必须跟随、无法瘦身)。原生动画本地测不出,需 EAS 真机验证。**
- **沉淀**:RN「随键盘 transform 的容器卡顿」排查 —— 除上面列表四步外,还要看:⑤ **KeyboardStickyView/被 transform 的子树是否会因无关 state(如 keyboardInset)re-render**;若会,用 `useMemo` 缓存该子树 element、把无关 state 排除出依赖,让动画独占帧。⑥ 子树尽量小(iris 只 transform 输入框本身)。

## 工作日志 — 23:10 koda 聊天补齐与 iris 的三处交互差异

- **做了什么**:用户「把这些差异全部补齐」。在 `feat/koda-ce`(PR #394,commit `179f0e75`)补三块:
  1. **历史分页**:`stores/chat.ts` 加 `PageState`(nextReqMessageID/isCompleted/loadingMore)+ `page` state + `loadOlderMessages`(仿 iris `loadOlder`,本地 `prependOlder` 去重前置);`loadMessages` 记录 cursor;`teardown` 重置 `page`。`ChatMessageList` 加 `onEndReached`+`onEndReachedThreshold=0.2`(inverted 下 older 在列表末尾=顶部)+ 顶部 `ActivityIndicator`(hasOlder 时)。
  2. **typing 气泡**:新建 `components/chat/typing-bubble.tsx`(koda 视觉:StyleSheet+Warm token,32px 头像 + 收到方气泡皮肤 + 三点波浪 Animated,useNativeDriver),放进 `ChatMessageList` 的 ListHeaderComponent(inverted 下 header 在底部=贴 composer)。
  3. **跟随控制**:屏幕加 `atBottomRef`(`onScrollBeginDrag`→false、`onUserScrollSettle`→按 offset≤120 重判、`scrollToEnd` 兼作 pinToFoot→true);typing effect:仅当 `atBottomRef.current` 时才 `scrollToEnd`,读历史时不被 yank 回底。
- **为什么**:对比发现 iris 的 `atBottomRef` 唯一实际用途就是配合 typing 气泡(对方输入且在底部才滚动带出气泡),所以 typing 气泡 + 跟随控制是配套的;分页独立。
- **结果**:biome / koda typecheck / boundaries 全过;koda test 294/295(唯一失败 `config.test.ts` 既有问题)。原生滚动/分页/动画需 EAS 真机验证。
- **沉淀**:①iris 的 `prependOlderChatMessages`/`upsertChatMessage` 在共享包 `@heyhru/business-chat`,但 koda store 未依赖该包,逻辑极简(去重+前置)故本地实现 `prependOlder`,避免多引一层跨包依赖。②TIM `getMessageList({conversationID, nextReqMessageID})` 天然支持 cursor 分页,response 带 `nextReqMessageID`/`isCompleted`。③inverted 列表:older 历史在「末尾」→ 用 `onEndReached` 分页;typing/greeting 贴 composer → 放 `ListHeaderComponent`;loading-older spinner → `ListFooterComponent`。

## 工作日志 — 23:25 koda chat 基础设施对齐 iris(A 方案,用共享包)

- **背景**:用户要「用共享包、把 koda 整个 chat 除 UI 外逻辑做得和 iris 一模一样」。派两个调研 agent 深挖 store 层 + 屏幕层后确认:**iris/koda 是刻意的架构分歧,无法字面一模一样**——koda 有 iris 没有的整套业务(CE 交换 chips+Dialog+koda-exchange-ui store、social world、chemistry unlock、intro-notice、review 人物、koda 计费、通知权限条),iris 有 koda 没有的整套服务(relationship-events、chat-outgoing、buildEnvelope、hasMatched、source 限制、round-quota paywall、The Read、peer domain 缓存、useAfterInteractions)+ 不同登录态(useAuthStore vs useSessionStore+authenticateWithDevice)。用 AskUserQuestion 给三档,用户选 **A 基础设施对齐**。
- **做了什么**(commit `e403892e`):
  1. **复用共享包**:koda 加依赖 `@heyhru/business-chat`,删本地 `upsert`/`prependOlder`,改用 `upsertChatMessage`/`prependOlderChatMessages`(行为逐字等价)。这是用户「用共享包」的核心诉求。
  2. **监听生命周期**:store 存 handler 引用,teardown 真正 `chat.off()`(此前只置 flag→跨 re-login 泄漏监听、消息事件重复触发,真实缺陷)。
  3. **KICKED_OUT**:被踢/异地登录→`useSessionStore.logout()`(koda 原先完全缺)。
  4. **屏幕**:新消息到达(线程已打开、无导航)时 markRead(此前只 focus markRead);切会话时重置 `atBottomRef`。
  5. 更新两个 store 测试 mock(补 `off` + `TIM_EVENT.KICKED_OUT`)。
- **刻意不做(避免误伤/死代码)**:断线重连横幅 conn/ConnState(koda 无该 UI)、sendAudio(koda 无音频入口)、resendMessage(koda 无 retry 入口)、CE/peer-profile/badge 的 store 归属改造(koda 刻意抽在 service/hook,强行搬回会职责重复)。scrollToEnd 的 40ms 延迟保留(koda chips 展开可能依赖它,不冒险)。
- **结果**:biome/koda typecheck/boundaries 全过;koda test 294/295(唯一失败 `config.test.ts` 既有)。原生行为需 EAS 真机验证。
- **沉淀**:①`@heyhru/business-chat` 是**无头状态机**(createChatMachine + 纯函数),但**没有任何 app 用 createChatMachine**,iris/lumi/naya 都只借 3 个纯函数(prependOlderChatMessages/upsertChatMessage/toChatDomainEvent);"复用共享包"= 复用这几个纯函数,不是接状态机。②"和另一个 app 一模一样"类需求先查是否**刻意架构分歧**再动手——多 app monorepo 里 UI 刻意不同是过审硬要求,逻辑层也常各自演化,盲目对齐会误删业务+引入不存在的依赖。先调研+给分档让用户定边界。

## 工作日志 — 23:40 koda chat composer 升降两处微优化

- **做了什么**(commit `68f209d6`):① `KeyboardStickyView` 的 `offset` 用 `useMemo` 稳定(此前内联对象每次 render 新建,可能触发 worklet 重配;restingInset 会话内固定)。② composer 外层 `View` 加 `renderToHardwareTextureAndroid={IS_ANDROID}`——把子树栅格化成纹理,升降时只移纹理,避免 ChatActionBar 里 4 个 44×44 SVG 图标逐帧重绘。
- **为什么**:composer memo 隔离后,升降剩余成本主要是被 transform 的子树里 4 个 SVG(react-native-svg)逐帧重绘。纹理化对症;且纹理失效(内容变=打字)与升降动画在时间上不重叠(升降发生在键盘刚弹出、用户还没打字),所以对升降净正、对打字仅额外栅格开销。
- **结果**:biome/typecheck/boundaries 全过。**均需 EAS 真机验证**:renderToHardwareTextureAndroid 属实验性,要确认打字手感不受影响、升降确实更顺。
- **沉淀**:RN「随键盘 transform 的子树含 SVG/矢量图标」卡顿,memo 隔离后仍卡的最后一招是 `renderToHardwareTextureAndroid`(iOS 对应 `shouldRasterizeIOS`)——**前提是动画期与内容变更期不重叠**,否则频繁重栅格反伤。含 TextInput 的子树要特别评估:打字时会重栅格,但打字时键盘已稳定不在升降,故可接受。

## 工作日志 — 00:05 koda 聊天键盘改「一步抬升」不掉帧方案(放弃逐帧跟手)

- **背景**:真机上 koda 逐帧 transform(KeyboardStickyView)升降仍卡,即便加了 memo 隔离 + renderToHardwareTextureAndroid。用户看了 Kira-Android(原生,ADJUST_RESIZE 一步 resize、输入框先到位再键盘滑上来)问能否照用。**结论**:koda(Expo SDK 57 默认 edge-to-edge)用不了平台 adjustResize——koda docs/09-report.md 记录 R00272/R00278:edge-to-edge 下 Android adjustResize 不再压缩 RN 根视图、键盘避让全失效,才改用 keyboard-controller。Kira 能用是因它是原生非 edge-to-edge。用户确认要「不跟手但稳定不掉帧」。
- **做了什么**(commit `28a320df`,`feat/koda-ce`):在 keyboard-controller 体系内做「一步抬升」——① 去掉 `KeyboardStickyView`(逐帧 transform 是卡顿根源)② 用 `useKeyboardHandler({ onStart })`(键盘动画**开始**瞬间即带目标 height)+ `runOnJS(setKeyboardInset)` 一次性 setState ③ composer 外层包一个 `<View style={{ marginBottom: keyboardInset }}>`(Android),一次 layout 把 composer 顶到键盘上沿、列表 flex:1 随之收缩;iOS 不变(KAV padding,inset 恒 0)④ 列表 bottomPadding 去掉键盘项(列表真收缩了)⑤ 删 renderToHardwareTextureAndroid / stickyOffset / KEYBOARD_GAP import。
- **观感**:输入框在键盘动画开始时一步跳到位、键盘滑上来(≈Kira);因只一次 setState + 一次 layout、无逐帧 transform/重绘,**结构上不会掉帧**(代价:不跟手)。
- **结果**:biome/typecheck/boundaries 全过;koda test 294/295(唯一失败 config.test.ts 既有)。**必须 EAS 真机验证**升降是否真的顺、以及切 gift/more 面板与 exchange dialog 场景是否正常(键盘/面板互切)。
- **沉淀**:Expo SDK 57 edge-to-edge 下,RN 键盘避让**只能靠 keyboard-controller**,不能退回平台 adjustResize/softwareKeyboardLayoutMode:resize(Android 失效,记录见 koda docs/09-report R00272/R00278)。要「不跟手但不掉帧」:`useKeyboardHandler.onStart`(动画起点即给目标 height)+ 一次性 marginBottom,是逐帧 transform 卡顿的替代解;`onStart` 比 RN 原生 `keyboardDidShow`(动画结束才触发)更早,能得到「输入框先到位」的观感。

## 工作日志 — 00:20 koda 礼物弹窗加 loading 态(先弹后加载)

- **背景**:用户觉得礼物弹窗弹起卡顿,要求「先弹起 loading,不要等接口再弹」。核查后:GiftSheet **本就是先弹出、再异步 `fetchGifts`**(不等接口),`fetchGifts` 有模块级 `cachedGifts` 缓存,首屏用 `cachedGiftCatalog()` 初值;且 GiftSheet 是**常挂载**(visible 切换,非 lazy mount)。真正问题是:**首次打开无缓存时显示空态 "No keepsakes here yet",fetch 回来再跳成网格**,像"在等接口"。
- **做了什么**(commit `dbfaffc1`):`gift-sheet.tsx` 加 `loading` state——`visible` 变 true 时 `setLoading(cachedGiftCatalog() == null)`(只有从没缓存过才 true),fetch 完 `setLoading(false)`;ScrollView 内容改成 loading→`ActivityIndicator` / 空→文案 / 有→网格 三分支;加 `active` 标志防卸载后 setState。
- **效果**:首帧只画转圈(比首帧铺整个网格更轻,开屏也更顺);有缓存的后续打开仍瞬间出网格。
- **沉淀**:「弹窗弹起卡/像等接口」先分清两件事——①是否真的 await 接口后才弹(本例不是,已异步)②有没有 loading 占位(本例没有,空态被误读成等待)。加 loading 占位 + 首帧轻量化(不在开屏帧渲染重列表)通常就能解决「弹起卡顿」的观感。常挂载弹窗无 mount 成本,弹出动画归 heroui/gorhom。

## 工作日志 — 00:35 koda 金币图标换成 Figma 品牌金币(PNG)

- **做了什么**(commit `6221c09e`):按用户给的 Figma(node 2150-198)把 `CoinGlyph` 从原来单色可染色的 sparkle SVG,换成 Figma 那枚**镀金立体金币 + "K" 字**。经 figma MCP 导出 SVG 发现它含 `<filter>`(drop + 双 inner shadow,feColorMatrix/feComposite)+ 多层线性渐变——react-native-svg 15.15.4 对 filter(尤其内阴影)跨端渲染不保真,且 koda 无 svg-transformer(`<Image source={svg}>` 走资源管线也不保证)。故**导出 PNG @4x(56×58)** 放 `src/assets/chat/koda/coin.png`,`CoinGlyph` 改用 RN `<Image source resizeMode="contain">`(contain 吸收多出的投影高度不变形)。
- **API 变化**:金币现在是固定彩色品牌图标、**不可染色**,删掉 `color` prop;唯一传 color 的调用点(gift-sheet 发送按钮 `color={Warm.onDark}`)一并去掉——发送按钮现在也显示真金币。其余调用点只用 `size`。
- **结果**:biome/typecheck/boundaries 全过;koda test 294/295(既有失败)。
- **沉淀**:Figma 图标带 filter(内/外阴影)+ 渐变时,react-native-svg 手写不现实、SVG-as-Image 管线也不一定保真,**小尺寸彩色品牌图标直接导 PNG @4x + resizeMode contain 最稳**(scale 上限 4,想更高只能后期放大)。koda 图片资源惯例:静态 PNG 用 RN `<Image as RNImage>`(见 conversation-row 的 spark-heat-flame.png),SVG 也走 `<Image source={svg}>`(见 what-lumi-noticed-row 的 story-spark.svg)。

## 工作日志 — 00:55 koda 礼物气泡卡片改版(Figma 799:71)

- **做了什么**(commit `c0fc767e`):按 Figma「Keepsake Bubbles 799:71」重写 `chat-bubble.tsx` 的 `GiftCard`——① 礼物图 56×56 圆角 14(去掉原来金色 disc 底)② eyebrow 改 Title Case「You sent / {name} sent you」(Inter 12 `Warm.mocha500`)③ 礼物名衬线体带引号(`WarmFontFamily.serif` Crimson 17 `Warm.ink900`)④ **CTA 按方向分**:发送卡 = 「Send another ›」文字链(`Warm.terracotta800`);接收卡 = 实心赭色药丸「Gift back」(白字,高 26 圆角 13)⑤ 卡片皮肤跟气泡:发送 = 奶油 `Warm.sand` + 右上角小尾(圆角 18/6/18/18)、无边框;接收 = 白 `Colors.cardStrong` + `rgba(236,217,194,0.6)` 细边 + 左上角小尾(6/18/18/18)。
- **token 映射**:`#3A2A1E`→ink900、`#8A7364`→mocha500、`#C06A2E`→terracotta800(token 是 #C96A2E,1 位色差,取品牌一致)、`#F6EAD4`→sand(#F6EADA,最近)、边框 `rgba(236,217,194,0.6)` 无对应 token 故本地常量 `GIFT_CARD_BORDER`;两卡标题色 Figma 有 1 档差(#3A2A1E vs #6A3A2C)判为噪声,统一用 ink900。删除不再用的 `FontFamily` import。
- **结果**:biome/typecheck/boundaries 全过。需真机看真实礼物图(imageUrl)在 56×56 框里的表现 + 接收卡「Gift back」按钮观感。

## 工作日志 — 01:10 koda 送礼「没反馈」根因:toast 被礼物面板挡住

- **现象**:用户反映 koda 送礼后无任何反馈。**排查**:koda `submitGift` 其实和 iris 机制一致(成功→thread 插礼物卡 + scroll;error→`toast`;余额不足→充值面板),`sendGift` 也把 code=100 归为 `outcome:"error"`,ToastHost 也挂着。差异在于:**iris 的 `handleGiftSend` 一选中礼物就 `setGiftOpen(false)` 先关面板再异步发送**;koda 只在成功路径关面板,**error 分支不关**——当前后端返回 code=100「to user not found」时礼物面板一直开着,error toast 弹在面板后面被遮住 → 看起来"没反馈"。
- **做了什么**(commit `04fa341b`):`submitGift` 开头(guard 后)统一 `setGiftVisible(false)` 先关面板,删掉各分支里重复的关面板调用。这样成功卡片 / error toast / 充值面板每种结果都落在聊天页可见。
- **沉淀**:①「操作没反馈」先分清是**真没做反馈**还是**反馈被遮挡**(toast/提示弹在 modal/bottomsheet 之下是常见坑)——排查时先确认 toast host 挂载 + 结果分支是否关掉遮挡层。②iris 送礼反馈很朴素:thread 插礼物卡(成功)+ toast(失败/不足)+ 充值面板,无炫酷动画;范式是「选中即乐观关面板,再异步发送」。③koda 送礼当前在 dev 环境因对端 user 不存在(code=100)一直失败,这是后端/数据问题,与反馈修复独立。

## 工作日志 — 01:35 koda chat PR #394 自评 review + 修复

- **做了什么**:用 `/code-review` skill 对 PR #394(feat/koda-ce→feature/koda-android,+1020/-381,15 文件)做 review,回 6 条(无高危)。修了 5 条(commit `f577a178`):
  1. **[medium] kickedOut 监听泄漏**:`createKickedOutHandler` 原来只 set error+logout,不 unbind、listenersBound 仍 true——正是本 PR teardown 修复要防的泄漏。改为 `unbindListeners()` + reset idle,重登录能干净重绑。
  2. **[medium] inverted list onEndReached 急拉旧页**:开会话/内容不满屏时 onEndReached 立即触发,拉用户没滚的旧页。加 `hasDraggedRef`(onScrollBeginDrag 置 true),`onLoadOlder` 仅在拖动过后才分页。
  3. **[minor] near-bottom 新消息不滚入**:移除 onContentSizeChange 后,在底部(offset≤120 但非0)的新消息不完全可见。新增 effect:newestMessageId 变化时,若 atBottomRef 则 scrollToEnd,并把 incoming markRead 合并进去。
  4. **[cleanup] KEYBOARD_GAP dead export**:键盘改一步抬升后无人用,从 use-bottom-inset.ts 删除。
  5. **[cleanup] bottomPadding 恒常量 + 过时 JSDoc**:去掉 ChatMessageList 的 bottomPadding prop,内联 THREAD_BOTTOM_PADDING 到 styles.content;screen 删 const + import。
- **未修(留真机)**:[minor] iOS 键盘打开时 composer 仍留 ~34px safe-area 死区(review 假设 KAV padding 下 composer paddingBottom 冗余)——需真机确认是否既有行为/是否真浮空,不盲改 iOS。
- **结果**:biome/typecheck/boundaries 全过;koda test 294/295(既有 config.test.ts)。
- **沉淀**:①inverted FlatList 的 `onEndReached` 会在 mount/不满屏时触发,分页要用「用户拖动过」的 ref 门控,否则每次开会话都多拉页(iris 也有此瑕疵)。②store 里任何「异常登出」路径(kickedOut/被踢)都要走 unbindListeners 重置 listenersBound,否则重登录 bindListeners 早返回、新实例收不到消息——和 teardown 同源的坑。

## 工作日志 — 01:45 PR #394 已 merge(koda chat 大改)

- **做了什么**:squash-merge PR #394「fix(koda): chat keyboard fixes + transform lift + gift panel redesign」→ `feature/koda-android`,merge commit `f446cf94`,+1060/-384,17 文件。涵盖:键盘一步抬升(useKeyboardHandler.onStart + marginBottom)、聊天列表 inverted + ChatMessageList/composer memo 隔离、历史分页 + typing 气泡 + follow-the-foot、基础设施对齐 iris(复用 @heyhru/business-chat 纯函数、真正 chat.off 解绑、KICKED_OUT、incoming markRead)、礼物面板/礼物卡片(Figma 799)/金币图标(PNG)改版、gift-sheet loading 态、送礼反馈修复、code-review 6 条修 5 条。
- **遇到什么问题**:合并前确认 `mergeable=MERGEABLE state=CLEAN`、review 检查 pass;担心的 `config.test.ts`(appName koda_android vs koda)是既有失败,CI CLEAN 说明不阻塞。
- **怎么解决**:`gh pr merge 394 --squash --delete-branch`(gh 账号已确认 colna),远端分支自动删;本地切回 feature/koda-android,`feat/koda-ce` 本地分支已随合并清除;确认 local==origin 含 f446cf94。
- **残留(follow-up)**:①**整条 PR 的原生键盘/滚动/分页/动画本地测不了,必须 EAS 真机回归**——重点:键盘一步抬升观感、键盘↔gift/more 面板/exchange 弹窗互切、分页拉取、金币 PNG 在赭色发送按钮观感、礼物卡真实图。②code-review #3 未修:iOS 键盘打开时 composer 可能留 ~34px safe-area 死区,留真机确认后再定。③既有 `config.test.ts` 失败(appName)与本 PR 无关,待单独处理。④送礼在 dev 环境因对端 user 不存在(code=100)一直失败,后端/数据问题。

## 工作日志 — 02:10 koda 视频通话对齐 iris:阶段 A/1 免费通话秒数

- **背景**:调研确认 iris/koda 都有真实 TRTC 通话(腾讯 TUICallKit 原生 UI),koda 是 iris 严格子集,缺的是「精细计费/充值 + 增长漏斗」。用户选阶段 A(通话体验/变现),分步做。起分支 `feat/koda-video-call`(base feature/koda-android)。
- **做了什么**(commit `d376aec5`):免费通话秒数 freeCallSeconds——① koda entitlements store 加 `freeCallSeconds` + `consumeFreeCallMinute`,`load` 从**同一个 `newUser.value`**(共享 `business-payments` 引擎 fetchSnapshot,koda 后端本就带,和 freeTextMessageCount 同桶)读取。② use-call:`freeCallSeconds>0` 时免拨号金币门槛。③ call-billing:每分钟 tick 先花免费分钟再扣币。
- **关键验证**:freeCallSeconds **不 inert**——koda 早已 fetch newUser 快照并用了 freeTextMessageCount,同对象就有 freeCallSeconds(iris 也这么取),所以后端确定提供。proto 另有 `/userApi/checkUserFreeCallDuration`(id 4690)兜底。
- **踩坑**:`git checkout -b feat/koda-video-call` 后不知何故 HEAD 回到 `feature/koda-android`,Task1 提交误落集成分支。修复:`git branch feat/koda-video-call`(保住提交)→ 在 feature/koda-android 上 `git reset --hard origin/feature/koda-android` 还原(提交已在新分支、工作区干净、无丢失)→ 切回 feat/koda-video-call 推送。**教训:每次开新分支后、提交前先 `git branch --show-current` 确认,别默认还在新分支上。**
- **结果**:biome/typecheck/boundaries 全过;koda test 294/295(既有 config.test.ts)。原生计费需真机验证。
- **待续(阶段 A 剩余)**:②精细计费(iris call-billing 195 行:免费分钟/中途低余额预警/重试预算)+ call-session 接线;③低余额充值面板(stores/call-low-balance + components/call/call-low-balance-popup + 原生 CallRechargePresenter 接线,UI 按 koda 视觉重画);④Android CallKeepAlive 接线;⑤CallSource 补全(3→13,B 相关的按需)。

## 工作日志 — 02:35 koda 视频通话对齐 iris:阶段 A/2 精细计费

- **做了什么**(commit `dda80be6`,feat/koda-video-call):把 koda `call-billing.ts` 从基础版(60s+30s)换成 iris 精细模型——1s 主时钟三时刻:t=0 权益检查(有免费分钟→60s 倒计时 + 立即首扣消耗;无免费且余额不足→30s)、t=30/90 低余额检查点(付不起下一分钟→30s 预警)、t=60/120 扣费(每次扣费独立重试预算 3×1.5s);服务端确认 insufficient 而本地漏报时补开窗口再顺延;加 `resume()`(通话中充值成功重新武装预警);余额从注入 getter 读、不缓存。`call-session` 传 `hasFreeEntitlement`/`getCoinBalance`,`onLowBalance` 带倒计时秒数。`chat-billing.deductForCall` 加本地免费分钟同步(`ok && isFree` 时 `consumeFreeCallMinute`),同时废掉 Task1 的本地 consume 改走服务端 isFree。
- **关键前提**:koda `deductForCall` 早已返回 `DeductResult{costCoin,isFree}`(同 `DeductCoinMsgResponse` proto),所以精细计费能直接移植,不依赖新后端。
- **踩坑**:chat-billing 新 import entitlements store → 把 `business-payments→expo-modules-core` 拉进 `chat-billing.test.ts` 模块图,vitest strip-types 报错、整个测试文件加载失败。修:该测试 `vi.mock("@/stores/entitlements", ...)` 挡掉重依赖。**教训:低层 service 里 import 带原生依赖的 store,会连累它的单测加载,需在测试补 mock。**
- **结果**:biome/typecheck/boundaries 全过;koda test 294/295(既有 config.test.ts)。原生计费需真机。
- **待续 A/3**:低余额充值面板(stores/call-low-balance + components/call/call-low-balance-popup,按 koda 视觉)+ 原生 CallRechargePresenter 接线 + billing.resume 接到充值成功;onLowBalance 现在仍是 toast(占位)。A/4 CallKeepAlive;A/5 CallSource。

## 工作日志 — 03:05 koda 视频通话对齐 iris:阶段 A/3 通话低余额充值弹窗

- **Figma 结论**:node 0-1 整页搜遍——**没有通话专属的低余额倒计时充值弹窗**。`PAY2·聊天低额·充值弹层`(751:11)是占位(复用的模板 `EL-6a97ba4f` 同时被 `3E Discover 空状态` 用),`732:17 Urgency recharge` 是整屏无倒计时。故按用户指示「按 koda 风格设计一个和 iris 功能一致的」,**复用 koda 现成 `ChatRechargeSheet`**(充值弹窗:倒计时行+金币包+IAP+restore,本就 koda 视觉)。
- **做了什么**(commit `91f47e1a`):① 新建 `stores/call-low-balance.ts`(deadline + onRecharge/onCancel,计费层用 60/30s 窗口 open)。② `ChatRechargeSheet` 加可选 `countdownSeconds`/`title`/`subtitle`,传 countdownSeconds 时倒计时由父受控(避免双计时器)。③ 新建 `components/call/call-low-balance-popup.tsx`:读 store,渲染 ChatRechargeSheet + 通话文案(Almost out of coins / Top up within Ns…),倒计时归零只关不挂断(扣费才挂断),余额涨→resume+dismiss。④ `call-session` 的 `onLowBalance` 开 store + 接 `billing.resume`;use-call 去掉多余 toast。⑤ `_layout` 根部挂 `<CallLowBalancePopup/>`(挨着 ToastHost)。
- **范围**:**RN 路径**(前台/iOS)。Android 通话中 RN 宿主被冻结、无法在腾讯通话窗上显示 RN 弹窗——需原生 `CallRechargePresenter`(共享包已有,koda 接线),留 A/3-native follow-up。
- **结果**:biome/typecheck/boundaries 全过;koda test 294/295。原生通话需真机。
- **待续**:A/3-native(CallRechargePresenter 接线,Android 通话窗上覆盖充值面板)、A/4 CallKeepAlive、A/5 CallSource。

## 工作日志 — 03:35 koda 视频通话对齐 iris:阶段 A 收尾(A/3-native + A/4,A/5 判定不做)

- **做了什么**(commit `58c4abe1`,feat/koda-video-call):
  - **A/4 CallKeepAlive**:koda `call-state.ts` 的 `markRinging`/onCallReceived 调 `startCallKeepAlive()`、`resetCallState` 调 `stopCallKeepAlive()`(共享 rn-trtc)。不接的话 Android 通话中腾讯通话 Activity 抢前台冻结 RN 宿主,每分钟计费循环停摆。
  - **A/3-native**:`CallLowBalancePopup` 升级双路径——原生可用(`isCallRechargePresenterAvailable`)时用 `showCallRechargePresenter`/`update`/`dismiss` + `addCallRechargePresenterListener` 在腾讯通话窗上覆盖充值面板;否则回退 koda `ChatRechargeSheet`。koda `useCoinPurchase` 只有 `onStart`(auto 内部路由 DTC/IAP)/`onContinueDtc`、**无 `onContinueIap`**,故 presenter 的 continueDtc/continueIap 都映射到 `onStart()`,不给单独 IAP 按钮。
- **A/5 CallSource 判定不做**:iris 13 个值绝大多数是 B 类漏斗(FakeCall/VideoMatch/LoveRing/PwaVideoRecall)的来源,koda 没这些功能,补枚举 = 死代码。CallSource 会随各 B 功能落地时各自补来源。
- **结果**:biome/typecheck/boundaries 全过;koda test 294/295。**整条原生通话/计费/presenter/keepalive 本地测不了,必须 EAS 真机验证**;还需确认 koda EAS 构建已 link rn-trtc 原生模块(CallRechargePresenter/CallKeepAlive)+ TIM/TUICallKit SDKAppID + 离线推送证书。
- **阶段 A 完成**:A/1 免费秒数、A/2 精细计费、A/3 低余额充值弹窗(RN)、A/3-native 原生 presenter、A/4 keepalive。B 阶段(增长漏斗)需先确认 koda 后端 S2C 推送/接口,未开工。

## 工作日志 — 04:00 修 koda 通话内充值 Recharge 按钮"点不动"

- **现象**(真机图):通话中低余额原生充值面板已弹出(原生 CallRechargePresenter,盖在腾讯通话窗上,证明 A/3-native 生效),但 Recharge 按钮不可点。
- **根因**:原生按钮 `continueButton.isEnabled = !isLoading`(共享包 kt),`isLoading` = 我传的 `purchase.starting`。面板主按钮点击 emit `continueDtc`,但我错误映射成 `purchase.onStart()`(=`start("auto")`),这个 dev 包 auto 解析成 **IAP** 轨 → `runIap` 只在 Google Play 结果回调时才清 `starting`,而**通话中腾讯通话 Activity 占前台、Play 支付页弹不出来** → `starting` 永久卡 true → 按钮永久禁用。
- **对比 iris**:原生按钮禁用逻辑相同(同共享包);但 iris 主按钮走 **DTC**(`continueDtc`→`onContinueDtc`→`start("dtc")`),`runDtc` 跳支付页后**立即清 starting** → 按钮不卡;IAP(会卡到 Play 回调)在 iris 只是次要按钮 `continueIap`。**卡是我接线错,不是 iris 逻辑。**
- **修法**(commit `34902d63`):`continueDtc`→`purchase.onContinueDtc()`(koda 有,=start("dtc")),对齐 iris;`continueIap`→`onStart()`(koda 无 onContinueIap,auto 兜底)。
- **前提/残留**:需 koda 该构建 **DTC 已 provision**;若只有 IAP,则"通话中完成 IAP"本身受"Play 页弹不出通话前台"限制(iris 靠 DTC 规避)。真机验证。
- **沉淀**:通话中(Android 腾讯通话 Activity 前台)充值,**只能走不抢前台的支付轨(DTC/服务端下单)**;IAP 需要把 Play Activity 拉前台,在通话中拉不起来,会让"等结果"的 starting 永久卡住、连带原生按钮(isEnabled=!isLoading)死掉。
