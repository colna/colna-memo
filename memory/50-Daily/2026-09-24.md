---
title: 2026-09-24
date: 2026-09-24
tags: daily
---

# 2026-09-24

## 今日重点

- [ ] **T0150 / T0048 / T0124 / T0135**（likes & visitors 红点一族）：T0150 根因（`lastVisitTime` 毫秒 vs 基线秒）已修并 push `2b6a5988e`，T0135 兜底 `ea8651d72` —— 待测试同学在新包上验证「进 Visitors 后卡片红点与爪印归 0 / 停留期间新访客当场清 / 离开后新访客仍计 1」
- [ ] **真机验收一批**（Luka dev build 已装到 iPad Air 11-inch M3，见工作日志）：T0057 键盘退出 / T0054 进屏不弹键盘 / T0053 地址保存 / T0055 去动作栏 / T0056 全帖列表；Sent 卡 `a759a0692`；引导第 4 步 `35608d542`；通知分段徽标定位 `84a5b1107`；luka moments See all `a4d0f3f9a`；付费墙权益卡（`19861257a`，订阅态 + 短屏）
- [ ] **埋点 parity 分支 `feat/luka-analytics-parity` 未提交未推送**（20:26 全量逐事件修复 + `business-analytics` 0.5.0 + `rn-tim` sourceType）—— 等用户指示
- [ ] **T0052**（重登后资料丢）：等真机日志，`grep '\[profile\]'`（dev/preview 包 Export Log）→ 定后端修投影还是客户端合并读模型
- [ ] **T0061**（订阅赠币 +0）：给「SP后端内部群」的对账请求已拟好，等用户确认后发
- [ ] **skeleton shimmer 黑亮竖带**（21:07 定位为 expo-linear-gradient 伪影）：待用户确认真机屏上是否可见，再定是否按修法 1 改 `ShimmerOverlay`
- [ ] **iPad 模拟器走 onboarding**（23:14 被登录挡住）：等用户给测试账号或授权点击自动化
- [ ] 根目录未跟踪的 `-` 文件（devicectl UDID 误产物）删不删，待用户定夺

## 工作日志

### 10:03 · colna-memo：整理 2026-09-23 Daily（今日首次任务）

- 09-23 共 515 行、40 余条日志，全部为实质内容：**无归档**（无调试垃圾/闲聊，两处「未完成」均为等待态）；可复用经验当天已各自沉淀（`luka-likes-visitors-unread-semantics`、`luka-skeleton-shimmer-black-band`、`git-parallel-sessions`、`rn-prebuilt-core-debug-release-swap`），本次另外补了昨天跨夜任务的笔记（见下）。
- 复制 8 条未完成待办到今日「今日重点」，并把 T0135 一族与 T0150 的最新 commit 状态写全。

### 10:03 · sitin-rn：Luka 装到 iPad 真机并连 Metro 调试（跨 09-23 夜 ~ 09-24 早）

- **做了什么**：把当前分支（`feat/luka-analytics-parity`，含未提交改动）的 Development Build 装到插着的 iPad 真机（iPad Air 11-inch M3 / iPadOS 26.3.1 / UDID `00008122-000960643CB9801C`）并连 Metro 调试。链路 09-23 21:06 起跑，中途被签名与 Wi-Fi 慢挡住；09-24 早用户要「安装 luka」，完成重装 + 拉起，`curl localhost:8081/json/list` 出现 `com.presence.gracechat (iPad)`。
- **过程（四个坑）**：
  1. 先补 `apps/luka/.env`（gitignored）`EXPO_APPLE_TEAM_ID=39CFYH6W55`；`prebuild -p ios --clean` + pod install（`HEYHRU_BYTEPLUS_NATIVE=1 EXPO_USE_PRECOMPILED_MODULES=0`）成功。
  2. `expo run:ios --device` 签名失败：它**不带** `-allowProvisioningUpdates`，房号两个扩展（`.widget` / `.notiservice`）第一次上真机、没有描述文件 → 改走 xcodebuild 传统通道（`-destination 'generic/platform=iOS' -allowProvisioningUpdates -allowProvisioningDeviceRegistration -derivedDataPath build DEVELOPMENT_TEAM=39CFYH6W55 CODE_SIGN_STYLE=Automatic`）→ Xcode 自动注册两个扩展 App ID + 描述文件，BUILD SUCCEEDED。
  3. iPad 本地网络权限弹窗挡住 dev launcher（用户点了「允许」）。
  4. Wi-Fi 传 bundle 极慢（~2%/min，Mac→iPad socket sendq 积压 1.6 MB）→ 用 USB 隧道：Mac 侧 `fd79:1a6d:1adf::2`（`devicectl device info details` 的 Tunnel IP 是设备侧 `::1`），深链 `luka://expo-development-client/?url=http://[fd79:…::2]:8081` → 秒级完成。
- **结果**：Luka.app（房号 `com.presence.gracechat` 1.0.0）已装并运行在 iPad；Metro 用**用户原有的** 8081 实例（pid 54637）；确认 `com.presence.gracechat (iPad)` 调试目标在线。副作用：房号全产线共用，装 Luka 顶掉了同机的 Rocco（同 bundle id）。
- **关键命令**：`xcodebuild … -allowProvisioningUpdates …`；`xcrun devicectl device install app --device <UDID> …/Luka.app`；`xcrun devicectl device process launch --terminate-existing --device <UDID> --payload-url "luka://expo-development-client/?url=…" com.presence.gracechat`；`xcrun devicectl device capture screenshot --device <UDID> --destination /tmp/x.png`（看设备屏幕，排弹窗很快）。
- **沉淀**：新建 `30-Resources/troubleshooting/luka-house-id-device-build-usb-tunnel.md`（四坑 + 命令速查）。
- **未动**：仓库代码零改动（只有 gitignored 的 `apps/luka/.env` 新增）；根目录 `-` 与其他未提交 WIP 未碰。

### 10:35 · sitin-rn：Luka Profile 菜单卡去掉订阅管理两行（已 push）

- **来源**：用户贴 Profile 截图，圈出 `Manage payment account` / `Manage subscription` 两行，要求去掉。
- **改动（2 文件）**：`apps/luka/src/components/me/me-menu-card.tsx` 删两行及条件渲染（`config.extra.features.dtcEnabled`、`Platform.OS === "ios" && isPlus`）与随之不用的 imports，头注释同步；`get-more-tab.tsx` 删 `onManagePaymentAccount` / `onManage` 两个回调。路由与页面（`/manage-payment-account`、`/manage-subscription`）保留：`_layout` 仍注册、dev 调试区仍可进；Settings hub 本来就没有这两个入口。
- **验证**：biome 2 files 0；`pnpm --filter luka typecheck` 0；全量 luka **1311 passed / 4 failed** —— candidate-card-fields ×1 + shape-language ×3，来自并行会话 staged 的 discovery/chat 改动（本文件不涉圆角与候选卡）。
- **提交**：`9ebc5ce4f` → `origin/feature/luka-ios`（`1a2578612..9ebc5ce4f`，GitLab HTTPS），pre-push boundaries + typecheck apps/luka（6.1s）通过（按 luka 改完直推约定）。
- **插曲**：首次 `git commit` 把并行会话已 staged 的 5 个 discovery 文件一起带进提交 → 未推送前 `git reset --soft HEAD~1` + 只重提 2 个文件 + 把 5 个文件重新 `git add` 还原其 staged 状态。沉淀：`30-Resources/troubleshooting/git-parallel-sessions.md` 新增「commit 会捎上别人已 staged 的文件」一节。
- **未动**：并行会话 staged 的 5 个文件。

### 11:00 · sitin-rn：Luka 划卡页 iPad 适配（宽/高自适应 + 内容缩放）+ 真机验证（未提交）

- **来源**：用户贴 iPad 真机截图「这页 ipad 中划卡的样式有问题，兼容 ipad」。
- **实测定位**（真机 + 临时 debug 文本，用 `devicectl` 截图读数）：iPadOS 26 把 iPhone-only
  的包放进兼容窗口并整体放大 —— **逻辑窗口 375×669**（宽同 iPhone、矮 143pt），渲染放大
  ~3.4 倍；划卡页卡组可用高度只剩 **214pt**（iPhone 上约 350）→ 卡片成 335×214 的横条，
  固定 ~140pt 的信息带把照片挤成一条（只剩眼睛）。
- **改动（8 文件，未提交）**：
  - `lib/discovery-card-fit.ts`：新增 `discoveryDeckCardWidth`（竖框=可用宽度、与以前逐点一致；
    横框按 350×547 收窄、320 兜底、幂等）与 `discoveryCardContentScale`（卡高 <300 内容按比例
    缩、下限 0.62，手机恒 1）。
  - `nearby-view.tsx`：量本屏高度反推卡宽；仓鼠条与卡组同宽居中；量完高度再挂卡组（防先满宽闪一帧）。
  - `discovery-deck.tsx`：新增 `contentWidth`，卡组列收窄居中（`deckRef`/引导目标也移到该列）。
  - `candidate-card.tsx` / `candidate-card-info.tsx` / `tag-chip.tsx`：缩放贯通；标签行高度改用
    `tagChipMetrics` 单一来源（首版行高自算，把芯片裁掉半截 —— 已修）。
  - `deck-hero.tsx` 导出 `DECK_HERO_HEIGHT`；新增 `apps/luka/docs/discover-deck.md`；单测 +7。
- **验证**：`pnpm lint` 0；`pnpm --filter luka exec tsc --noEmit` 0；luka 全量 **1314 passed /
  4 failed**（与今日基线逐条一致：shape-language×3、candidate-card-fields×1）。iPad 真机 `/reload`
  后截图：照片露出整张脸、标签/名字/城市比例正常；同时 `lsof` 确认 App 已回到用户的 8081。
- **环境坑（沉淀）**：dev client 缓存了旧 bundle，换端口起第二个 Metro 也没用；
  `curl -X POST http://localhost:8081/reload` 广播 reload 才让它重拉。临时起的 8082 已停。
- **并行会话现场**：工作区被 `git pull --rebase --autostash` 动过 —— `nearby-view.tsx` 出现
  stash 冲突（上游 `openDeckProfile` 来源埋点 vs 我的改动），已手工合并保留双方并 `git add` 解冲突；
  **`stash@{0}`（对方 parity WIP）与 `stash@{1}`（autostash）都未动**，对方新增的
  `app.config.ts`/`package.json`/`pnpm-lock.yaml`/`pnpm-workspace.yaml` WIP 也未动。

### 10:51 · sitin-rn（luka）：Chats 小窝访客卡一页 8 → 6（已 push）

- **来源**：用户「chats 的头像一页最多展示 6 个吧」。
- **改动（5 文件）**：
  - `apps/luka/src/lib/chats-play.ts`：`NEST_VISITOR_SLOTS` 8 → 6（左右各 3，行 0-2）、
    `NEST_VISITOR_ROWS` 4 → 3；`NEST_PAGE_SIZE = slots.length` 自动变 6，分页/翻页/点击序号
    全随派生，无需另改。注释同步（8 per page、four/three rows）。
  - `nest-playfield.tsx`（props 注释）、`(tabs)/index.tsx`（videoMatch 说明里的 8 张 → 6 张）
    同步；`docs/chats-play.md` 五处（每页张数/行数/测试段）。
  - `tests/lib/chats-play.test.ts` 断言更新：选页 6、每页分组 6、三行布局。行距断言拆两档 ——
    600 高缩放到顶（1.0）时只保「行距 ≥ 卡高」，500 高才断言「卡高 + `NEST_VISITOR_ROW_GAP`」。
- **设计副作用**：3 行时 `nestVisitorRowLayout` 的缩放多数屏触顶 1（卡片回自然尺寸 82×105，
  比 4 行时缩到 ~0.8 更大）；行距按行数均分。若用户要「卡片大小不变只减数量」需另调。
- **验证**：`vitest run tests/lib/chats-play.test.ts` 14 passed；luka 全量 1314 passed / 4 failed
  —— 与今日基线一致（shape-language×3 + candidate-card-fields×1，并行会话 discovery 改动引起，
  stash 掉本改动后同样复现）；`pnpm luka:typecheck`、`pnpm boundaries`、biome 4 文件 0。
- **提交**：用户「push」→ `88f33d070 feat(luka): Chats 小窝访客卡每页 8 张改 6 张`（5 files，
  +27/−25）→ `origin/feature/luka-ios`（`9ebc5ce4f..88f33d070`，GitLab HTTPS）。用 pathspec
  commit 只提自己 5 个文件，并行会话 staged 的 5 个 discovery 文件原样保留；pre-commit biome
  4 文件、pre-push boundaries（41 包）+ typecheck apps/luka（6.5s）全过。

### 10:53 · sitin-rn（luka）：iPhone 18 Pro 模拟器装 Luka —— iOS 27 UIScene 强制 + pogo 修法移植

- **来源**：用户「给我的 iphone18 模拟器装上 luka，我要用模拟机调试」。
- **根因**：iPhone 18 机型只能用 iOS 27 运行时（与 26.5 建机报 403 Incompatible device）；
  iOS 27 强制 UIScene 生命周期（TN3187），而本分支 expo 57.0.7 还不支持 → 启动即
  SIGTRAP（栈顶 `UIApplicationEvaluateRuntimeIssueForNoSceneLifecycleAdoption`）。
  旧包在 iOS 26.5 / iPadOS 26.3 上一直可跑，所以这是「iOS 27 运行时 + Xcode 27 SDK」才触发。
- **改动（5 文件，未提交）**：
  - `pnpm-workspace.yaml`：catalog `expo: ~57.0.7 → ~57.0.23`（scene 运行时 backport）+ 新增
    `expo-build-properties: ~57.0.20`，两条都带注释；`pnpm-lock.yaml` 随之重解（expo 实装
    57.0.24、build-properties 57.0.21；expo-modules-core 57.0.18 override 保持）。
  - `apps/luka/package.json`：加 `expo-build-properties: "catalog:"`。
  - `apps/luka/app.config.ts`：plugins 顶部加 `["expo-build-properties", { ios: { enableSceneSupport: true } }]`
    （注释含 AppDelegate swizzle 回归警告）。
  - `docs/native-dev-onboarding.md` §八：加「UIScene life cycle is required」故障条目。
- **构建/安装**：`rm-ios-for-prebuild` → `expo prebuild -p ios --clean`（不带 `HEYHRU_BYTEPLUS_NATIVE`，
  模拟器约定）→ 确认 Info.plist 有 `UIApplicationSceneManifest` / AppDelegate 实现
  `ExpoReactNativeFactoryProvider` → `expo run:ios --no-bundler --device <iPhone 18 Pro UDID>`
  Build Succeeded → 写入 schemeapproval（`luka` + `exp+luka` 两个 key）并 kickstart SpringBoard
  → `luka://expo-development-client/?url=http://localhost:8081` 拉起。
- **验证**：Metro `/json/list` 出现 `com.presence.gracechat (iPhone 18 Pro)`；截图登录页 +
  Dev Tools 齿轮；config 测试 23 passed；typecheck 0；biome 0；luka 全量 1314 passed / 4 failed
  （与今日基线逐条一致，非本次引入）。
- **坑（沉淀 → troubleshooting）**：iOS 27 无头模拟器下 `simctl openurl` 弹「在 Luka 中打开?」
  没人能点 → 写 `com.apple.launchservices.schemeapproval.plist` + 重启 SpringBoard 绕过；
  重装 App 后 approval 会重置需重写；kickstart 期间 openurl 会超时，等 6–8 秒。
  新建 `30-Resources/troubleshooting/ios27-uiscene-lifecycle-required.md`。
- **未做**：未动并行会话 WIP（对方 10:51 条目里把本次 4 个文件记为「对方 WIP 未动」，同一份
  改动两方都小心避让了）；scene 后 TIM push / TRTC / AppsFlyer 的 AppDelegate swizzle 回归
  未在真机做；blueprint / iris 等包在新 SDK 下会踩同一坑，本次未跟进。
- **11:01 补记（用户说 push 后）**：`git commit --only` 显式路径，只带本次 5 个文件 →
  `d954f685c`；`git push` 成功（`88f33d070..d954f685c`，pre-push boundaries + 8 个受影响
  workspace typecheck 全过）。推送窗口里并行会话的 `88f33d070`（Chats 每页 8→6）与
  `78387e011`（划卡页 iPad 短窗口）也先后落同一分支 —— 非本次改动，两方互未触碰；
  推送后工作区干净。

- **已提交并 push（11:05，用户要求）**：`78387e011` `fix(luka): 划卡页适配 iPad 兼容短窗口（卡片按比例定宽，内容随卡高缩放）`（9 files，+424/−137，含新增 `docs/discover-deck.md`）→ `origin/feature/luka-ios`。只带本人 9 个文件（pathspec 提交）；并行会话的 `app.config.ts`/`package.json`/`pnpm-lock.yaml`/`pnpm-workspace.yaml`/`docs/native-dev-onboarding.md` WIP 未动，两个 stash 未动。push 与并行会话撞车（他们的 `d954f685c` 先一步把我的提交一并推上），fetch 核对远端已含 `78387e011`、local/origin 0/0。

### 11:11 · sitin-rn：拉取 feature/luka-ios 最新代码 + 出 luka test/preview IPA（含 UIScene / iPad 修复）

- **来源**：用户「拉一下 feature/luka-ios 最新代码 然后打一个 ipa 包」。
- **拉取**：`git fetch origin feature/luka-ios` → HEAD == origin == `78387e011`（behind 0，无需合并）；工作区干净，无并行 WIP（09-24 上午的 UIScene `d954f685c` 与划卡页 `78387e011` 已推送）。
- **复用工程判断**：与上次出包基线 `c4c07d87d` 之间原生面有变（expo 57.0.7→57.0.24 + expo-build-properties / UIScene、生产身份 env 注入），但今早 10:47 已 clean prebuild（`ios/` 全新：UIScene manifest 在、expo 57.0.24、Pods 工程含全部 3 个 usdz、Manifest.lock == Podfile.lock）；其后的提交都是 JS-only（划卡页 9 files、访客卡 8→6）。非生产档原生身份不随 development/preview 分支（`isProduction=false` 相同，`profile` 只影响 `extra.features.dtcEnabled`）→ 复用工程，不 clean。
- **命令**：`export PATH="$HOME/.nvm/versions/node/v24.21.0/bin:$PATH"`；`printf 'n\n' | IOS_TEAM_ID=39CFYH6W55 scripts/build-ipa.sh luka test preview "" 10001`（'n' 答 clean 询问；buildNumber 对 luka 仍无效，产物恒 10000）。
- **结果**：`apps/luka/ios/build/export/Luka.ipa`，85,822,102 bytes（~81.8 MB），`com.presence.gracechat` 1.0.0 (10000)，Ad Hoc（`iOS Team Ad Hoc Provisioning Profile: com.presence.gracechat`）；构建日志 `apps/luka/build-logs/build-20260924-110354.log`。
- **产物核验（三件套 + 扩展）**：`apiBaseUrl=https://api-dev.lukasap.com`、`appName=luka`、`dtcEnabled=true`（preview 内部档）✓；今日代码符号 `discoveryDeckCardWidth` / `discoveryCardContentScale` 各命中 1 处 ✓；built Info.plist 有 `UIApplicationSceneManifest`（`EXExpoAppSceneDelegate`）✓；6 个 usdz 全在包（含 `LukaSniff.usdz` 10,056,079 bytes）✓。
- **surface 提示（不拦）**：产物 `get-task-allow=true`、`NSLocalNetworkUsageDescription` 提到 Expo Dev Launcher —— Ad Hoc 内测包预期；正式包必须 false / 去掉该文案（提审清单已列）。
- **未上传蒲公英**（用户未要求）；无代码改动，未动工作区。

### 11:20 · sitin-rn：诊断「Luka 内测 IPA 装到 iPhone 报『无法验证其完整性』」

- **现象**：用户截图——iPhone 上安装刚出的 `Luka.ipa`，系统弹「无法安装"Luka"：无法安装此 App，因为无法验证其完整性。」（图标带下载云标，装机中弹窗）。
- **根因（已实锤）**：Ad Hoc 包只能装进描述文件登记过的设备。解出 IPA 内嵌的 `iOS Team Ad Hoc Provisioning Profile: com.presence.gracechat`（2026-09-21 生成，71 台设备）——**不含这台 iPhone（`象` / iPhone 17 Pro，UDID `00008150-000834890243C01C`）**；再扫本机 `~/Library/Developer/Xcode/UserData/Provisioning Profiles/` 全部 11 个缓存 profile，也都不含它 → 该机从未注册进公司 Team 39CFYH6W55。iPad（`00008122-000960643CB9801C`）在名单里，所以此前 iPad 构建/安装都正常。
- **判据命令**：`security cms -D -i Payload/Luka.app/embedded.mobileprovision` + plistlib 列 `ProvisionedDevices`；`xcrun devicectl list devices` 取设备 UDID（`象` 当前 unavailable）。
- **修法（待用户连手机后做）**：① 手机 USB 连 Mac + 信任 + 开发者模式；② `xcodebuild -workspace ios/Luka.xcworkspace -scheme Luka -configuration Debug -destination 'id=00008150-000834890243C01C' -allowProvisioningUpdates -allowProvisioningDeviceRegistration ... build` 注册设备；③ 重跑 `build-ipa.sh luka test preview`（复用工程）并在产物里核验新 profile 含该 UDID；若 Xcode 未自动刷新 Ad Hoc profile，在门户 revoke 旧 profile 触发重建。
- **沉淀**：新建 `30-Resources/troubleshooting/adhoc-ipa-device-not-in-profile.md`（现象文案 / 判据命令 / 修法 / 与 `ios-appid-personal-team-conflict.md` 09-18 那节的呼应：给测试同学装包前先注册 UDID）。

### 11:33 · sitin-rn（luka）：判读新的冷启闪退 .ips（142229）—— 仍是 JS fatal，取证要 crash.log

- **来源**：用户发来 `~/Downloads/Luka-2026-09-29-142229.ips`，bug 标题「【启动】冷启app后，闪退【偶现】」。
- **判读**：与 09-21 的 110853 / 135457 同形（`RCTExceptionsManager.reportFatal` → SIGABRT，faulting `com.meta.react.turbomodulemanager.queue`），但这次是**冷启 11 秒后**（14:22:17 → 14:22:28）；同批 112959 是 SVG mask 的 native 崩溃（已由 `2bf74b4ed` 修补）。
- **日志对不上**：手上两条 session 日志只到 06:03Z（设备 14:03），14:22Z 前后这次不在其中；日志里零 `[CrashLog]`。
- **排除**：Luka 未配 expo-updates（app.config 无 `updates` 段）→ 不是坏 OTA；`sentryDsn` 是占位值 → Sentry 取不到。
- **代码侧收窄**：release 的 `metroRequire` → `guardedLoadModule` → 模块顶层抛错 `ErrorUtils.reportFatalError`（`metro-runtime/src/polyfills/require.js:98`）；未捕获 promise rejection 是 `isFatal=false` 不 abort ⇒ 冷启 fatal 头号面 = 首次 require 的模块初始化同步抛错。
- **下一步（待用户/测试）**：从闪退那台机器的 Debug Tools → TOOLS → Export crash log 取 `[CrashLog]`（并 Export log 带上 14:22 那段）；若为空说明包早于 09-21 遗言提交，需换新包复现。
- **旁证（与崩溃无关，建议另开修）**：session.log 每次导航都刷 `No route named "contact/bond"` —— 根布局的 `<Stack.Screen name="contact/bond">` 声明位置不对，`formSheet` options 未生效。

### 11:33 · sitin-rn：luka 全屏充值页仓鼠从压标题改为标题之上（未提交）

- **来源**：用户截图 —— 视频通话余额不足跳转的 `/credits/recharge` 顶部文案（`Almost out of sparks` 大标题）被 `hamster-ask` 仓鼠遮挡，要求「老鼠挪到大 title 之上」。
- **根因**：仓鼠绝对定位挂在倒计时卡容器里（`right: -2, top: -104, 146×112`），上探 104pt 正好压住大标题与副标题；倒计时卡距副标题只有 `gap-5`（20pt），垂直空间不够它在文字上方另占一行。
- **改法**：`apps/luka/src/app/credits/recharge.tsx` —— 仓鼠移出倒计时卡（同时删掉只为它存在的 `relative` 包装），作为 ScrollView 内容顶部独立一行、`alignSelf: "flex-end"` 右对齐；盒子按素材比例取 146×94（原 146×112 里上下各有约 9pt 透明边），文字组整体下移约 114pt，前爪落在大标题上沿之上，不再遮任何文案。`hamster-ask.webp` 带 alpha、无白色卡沿，单独放置不会带出多余白块。
- **验证**：`npx biome check` 该文件 ✅；`pnpm --filter luka exec tsc --noEmit` 0；luka 全量 `1314 passed / 4 failed`，4 条（shape-language ×3 + candidate-card-fields ×1）stash 掉本次改动复跑逐条一致，为预存失败。**未上模拟器/真机肉眼复核**（改后需重出 dev/内测包看落点）。
- **待确认**：对齐方式选的是右对齐（保持原构图）；若要居中（同 `low-sparks-sheet` 的「小崽站标题组顶上」）改 `alignSelf` 一处即可。
- **11:41 追加（用户反馈「仓鼠顶到 header 上」）**：仓鼠盒子加 `marginTop: 16` —— 内容容器只有 `pt-1`（4pt），不给它的话仓鼠会贴着返回键/Restore 那行；现页头到仓鼠 20pt、仓鼠到 kicker 26pt（含素材底边透明边），两侧呼吸位对称。`biome check` + `tsc --noEmit` 复跑过。

### 11:48 · sitin-rn（luka）：注册结果页通知授权改「进屏自动弹」（未提交）

- **来源**：用户报「注册最后一屏点 `Turn on notifications` 不弹授权」（iOS 真机、首次装）；排查未发现代码/native 问题（`ios/Podfile.lock` 有 `ExpoNotifications 57.0.6`，权限状态已决定时 iOS 不会再弹）。用户随后定调需求：**进屏自动弹，不是手动点**；保留 `Maybe later`。
- **改动**：
  - `apps/luka/src/app/onboarding/summary.tsx`：进屏 `useEffect` 延迟 600ms（先渲染预授权卡片）自动 `ensurePushPermission()`；granted → 自动完成注册；拒绝 / 弹不出（已拒绝、模拟器）→ 停在本屏由 `Maybe later` 继续。`promptHandled` ref 防重，并拦住延迟窗口内抢先点的 `Maybe later`。删除 `Turn on notifications` 主按钮。
  - `apps/luka/src/services/native/analytics.ts`：删 `OnboardingNotificationAllowClick`（点击动作已不存在；Show / Allowed / Skip 保留）。
  - `apps/luka/docs/notifications.md`：更新权限时机口径。
  - `apps/luka/tests/services/push-permission-timing.test.ts`：注释改为「启动路径不得请求」的新口径 + 新增自动请求断言。
- **验证**：`pnpm --filter luka typecheck` ✅；`npx biome check` 三文件 ✅；`pnpm --filter luka test` = **1315 passed / 4 failed**，4 条（shape-language ×3 + candidate-card-fields ×1）与 stash 基线逐条一致，为分支既有失败。
- **提醒**：iOS 拒绝一次即永久不弹（本次需求也绕不开）；测试机若已拒绝需删 app 重装（重装重置权限）或换设备验证。纯 JS 改动，dev build 热更即可，无需重出原生包。
- **12:02 更新**：该页已整页替换为 `onboarding/notifications.tsx`（结构对齐 rocco、`Turn on notifications` 按钮恢复为手动重试），见下条。
- **未 commit**（规范模式，等用户指示）。

### 12:02 · sitin-rn（luka）：注册末屏改为独立通知授权页（结构对齐 rocco，未提交）

- **来源**：用户看完 `origin/rocco/develop` 的 `apps/rocco/src/app/onboarding/notifications.tsx`（独立一屏、按钮触发、跳过出口）后定调 —— 把 luka 的 summary 结果页**整页替换**成通知授权页，文案给定：标题 `Turn on notifications?`、正文 `Luka will let you know when it has something for you.`、按钮 `Turn on notifications` + `Maybe later`；并确认**保留进屏自动弹**。
- **改动**：
  - 新增 `apps/luka/src/app/onboarding/notifications.tsx`（删 `summary.tsx`）：去掉全部资料回显（ProfileSummaryCard / bullets / FALLBACK_AVATAR），改为 STAY IN THE LOOP + 标题 + 正文 + 居中仓鼠（`hamster-waving.webp` 180×320）；进屏 600ms 自动弹 + Turn on 手动重试 + Maybe later 跳过；granted 自动完成注册，拒绝/弹不出停留；落库后仍先 replace `/intro-video` 再翻 profileComplete。
  - `lib/onboarding-flow.ts`：`SUMMARY_ROUTE` → `NOTIFICATIONS_ROUTE = "/onboarding/notifications"`。
  - `services/native/analytics.ts`：恢复 `OnboardingNotificationAllowClick`（按钮点击）。
  - 删除不再使用的组件：`notification-teaser-card.tsx`、`components/onboarding/profile-summary-card.tsx`、`summary-bullet.tsx`。
  - 测试：`onboarding-flow`（常量/文件名）、`onboarding-notifications-copy`（原 summary-review-copy 改名 + 新文案）、`onboarding-fallback-avatar`（删注册段、prefetch 改路径）、`push-permission-timing`（路径 + 按钮/埋点断言）。
  - 文档/注释：`docs/notifications.md`、`docs/app-store-review-findings.md`、`src/assets/README.md`、`_layout` / `intro-video` / `complete-onboarding` / `push-registration` / `fallback-avatar` 注释。
- **验证**：`pnpm --filter luka typecheck` ✅；`npx biome check` 14 文件 ✅；`pnpm --filter luka test` = **1313 passed / 4 failed**（shape-language ×3 + candidate-card-fields ×1，与基线一致）；**未上真机 / 模拟器肉眼复核**。
- **未 commit**（规范模式，等用户指示）。

### 12:36 · sitin-rn：luka 三批改动推送完成（含多会话 git 并发事故）

- **用户「push吧」**。工作区三批未提交改动全部落库并 push 到 `feature/luka-ios`（GitLab）：
  - 通知授权页 + Name·Location 取消 autoFocus → 最终在 `b9ca12c8c`（另一会话提交，通知页被并了进去）；
  - 充值页仓鼠 → `fd190bad8`（我 cherry-pick 补上）；
  - push：`b9ca12c8c..fd190bad8`，pre-push boundaries + luka typecheck 通过。
- **事故经过（并行会话同一 worktree）**：它 12:35 的 `git commit` 把我已 staged 的通知页（21 个文件）一起提交，message 只写 autoFocus；我 `reset HEAD~1` 时它已又提交一笔 recharge，**HEAD 已前进**，我的 reset 实际撤销的是它那笔（内容退回工作区、目标 commit 还在）；随后它 force push 远端为 `b9ca12c8c`。
- **收尾**：`git branch backup/luka-split-20260924` 留备份 → 对齐远端 → cherry-pick 独有的 recharge → push。
- **验证**：push 前 `pnpm --filter luka typecheck` ✅、`pnpm --filter luka test` 1313 passed / 4 failed（与基线一致）。
- **沉淀**：`30-Resources/troubleshooting/git-parallel-sessions.md` 追加两节 ——「reset 前核对 HEAD 是否被并行推进」「远端被 force push 后对齐远端 + cherry-pick 独有提交」。
- **待办**：备份分支 `backup/luka-split-20260924` 待用户确认后删；`b9ca12c8c` 的 message 只写了 autoFocus（通知页说明缺失，历史已推送，不再 force）。
- **11:51 改版（用户截图圈红框：要把仓鼠放进页头）**：仓鼠从内容流（标题组上方独立一行）挪进**页头行**——绝对定位在 nav 行内、水平居中（`left: 50%` + `marginLeft: -73`），`bottom: -38` 让前爪落在内容上沿之上，可见头顶 ≈ 状态栏下沿（Dynamic Island 之下）；nav 行加 `zIndex: 10`（它是滚动内容的兄弟节点，不加会被滚动内容盖住前爪）。内容容器 `pt-1` → `pt-11`（44pt）给前爪留位，标题组因此从页头下 44pt 开始（比上一版回缩 90pt），滚动时标题从仓鼠下穿过。**未实机复核**；`bottom` 与 `pt` 是两个可调旋钮。
- **11:56 微调（用户「往右一点」）**：仓鼠落点相对屏幕中线右移 24pt（`marginLeft: -73 → -49`），仍让开 Restore（右缘 301pt vs Restore 左缘 ~335pt）。biome + tsc 过。

### 12:08 · sitin-rn（luka）：注册 Name 屏取消 autoFocus，修「小崽一直不显示」（未提交）

- **来源**：用户贴注册 Name 屏（01/05）截图问「这页小崽为什么没了」。核实：小崽没删（`name.tsx` 仍传 `hamsterCoach` + `Meet your personal coach.`），是 `OnboardingScaffold` 的紧凑态把仓鼠行与 eyebrow 一起藏了。根因链：该屏 `autoFocus` + `autoFocusInput` 默认 true → 首帧即紧凑（9/21 `616339e6b` 修「进屏闪两下」），要等「键盘真正弹起过且已收起」才展开；模拟器接硬件键盘时软键盘永不弹 → `keyboardHasAppeared` 永远 false → 永久紧凑。
- **改动（用户拍板「这页取消 autoFocus」）**：
  - `apps/luka/src/app/onboarding/name.tsx`：删 `autoFocus`、加 `autoFocusInput={false}`（进屏先完整展开，点输入框才弹键盘、折叠）；留注释说明为何别加回。
  - `apps/luka/src/components/onboarding/scaffold.tsx`：`autoFocusInput` 注释更新 —— 只剩注册 Location 屏仍为 true，并记录「键盘始终不弹会永久锁紧凑态」这个坑。
  - `apps/luka/docs/keyboard-and-insets.md` §5：注册 Name 屏取消 autoFocus、只剩 Location；补原因。
- **验证**：`pnpm --filter luka exec tsc --noEmit` ✅；biome 两文件 ✅；`pnpm --filter luka test` = **1313 passed / 4 failed**，4 条与 12:02 基线逐条一致（shape-language ×3 + candidate-card-fields ×1，branch 既有失败）。**未上模拟器肉眼看**。
- **遗留**：注册 Location 屏仍 `autoFocus`，同样有「硬件键盘下永久紧凑」边界，用户未要求改；注册第一步不弹键盘、第二步弹，交互上略不一致，后续可再议。

### 12:11 · sitin-rn（luka）：注册 Location 屏同样取消 autoFocus（未提交）

- **来源**：用户贴注册 Location 屏（05/05）截图「这页小崽也没了」—— 与 Name 屏同根因：`autoFocus` + `autoFocusInput` 默认 true，软键盘永不弹的环境（模拟器硬件键盘）永久停在紧凑态，仓鼠行与 eyebrow 不显示。
- **改动**：
  - `apps/luka/src/app/onboarding/location.tsx`：`<OnboardingLocationPicker>` 删 `autoFocus`；`OnboardingScaffold` 加 `autoFocusInput={false}`（注释同 Name 屏口径）。
  - `apps/luka/docs/keyboard-and-insets.md`：§5 改为「Name 与 Location 两屏先后取消、已没有屏传 true」；§6 补注「该屏进屏不再自动弹，键盘被点开时避让依旧」。
- **验证**：`pnpm --filter luka exec tsc --noEmit` ✅；biome ✅（location.tsx 的 JSX 按 formatter 收成一行）；`pnpm --filter luka test` = **1313 passed / 4 failed**（与基线一致）。**未上模拟器肉眼复核**。
- **遗留**：`OnboardingLocationPicker` / `ZipCodeInput` 的 `autoFocus` prop 与 scaffold 的 `autoFocusInput` 机制均已无调用方（是否清理待用户定）。

### 12:31 · sitin-rn（luka）：清理 autoFocus 死代码（未提交）

- **来源**：用户拍板「一并清理」。Name / Location 两屏取消 autoFocus 后，`autoFocusInput`（首帧紧凑）机制与 `autoFocus` 透传链全部无调用方。
- **改动**：
  - `apps/luka/src/components/onboarding/scaffold.tsx`：删 `autoFocusInput` prop、`keyboardHasAppeared` ref 与首帧紧凑逻辑，`compact = keyboardAdaptive && keyboardVisible`；`keyboardAdaptive` 注释重写（「别给这类屏加 autoFocus」+ 指向 docs §5）。
  - `apps/luka/src/app/onboarding/{name,location}.tsx`、`apps/luka/src/app/edit-profile/name.tsx`：删 `autoFocusInput={false}`（edit-profile/name 也传了，漏掉会 TS 报错）。
  - `apps/luka/src/components/onboarding/onboarding-location-picker.tsx`、`zip-code-input.tsx`：删 `autoFocus` prop 与透传。
  - `apps/luka/docs/keyboard-and-insets.md` §5：改为「判据只剩 `keyboardVisible`；autoFocusInput 机制随两屏取消 autoFocus 一并删除」。
- **验证**：`pnpm --filter luka exec tsc --noEmit` ✅；biome ✅；`pnpm --filter luka test` = **1313 passed / 4 failed**（与基线一致）。**未上模拟器肉眼复核**。
- **发现（未动）**：`components/location/location-picker.tsx` 与 `components/edit-profile/underlined-input.tsx` 的 `autoFocus` prop 同样已无调用方，且前者注释「Onboarding sets `autoFocus`」已 stale；待用户决定是否清。
- **11:59 提交并推送（用户要求）**：`aff65b1da` `fix(luka): 全屏充值页仓鼠挪到页头，不再遮挡大标题`（1 file，+24/−14）→ `origin/feature/luka-ios`（`78387e011..aff65b1da`）。pre-commit（biome 1 file）与 pre-push（boundaries + apps/luka typecheck 8.7s）全过；只 stage 这一个文件，工作区其余改动（并行会话 WIP）未动。

### 12:37 · sitin-rn（luka）：push 期间撞上第二个并发会话，事故与收尾记录

- **背景**：用户要「只推 autoFocus 修复」（9 文件）。当时工作区还有另一批未提交改动（通知授权页 + recharge，memo 记于 11:33 / 12:02）。
- **事故**：仓库里有**第二个会话并发操作**同一 checkout。我在 `git add`（9 文件）后第一次 `git commit`，暂存区里已有一个它 staged 的 rename，被连带提交（10 files）；`reset --soft` 撤回后重新 commit，**第二次 commit 又吞掉它刚 add 进共享 index 的 20 个文件，变成 29 files 混合 commit（`b9ca12c8c`，message 却只讲 autoFocus）**。它随后把 `b9ca12c8c` push 上远端，再 `reset` 到远端并 cherry-pick recharge 为 `fd190bad8`，选择接受历史不重写。
- **最终状态**：远端 = 本地 = `fd190bad8`（`origin/feature/luka-ios`）。远端历史：`78387e011` → `b9ca12c8c`（29 files 混合）→ `fd190bad8`（recharge）。**autoFocus 修复内容已在远端**（含在 `b9ca12c8c` 内）；通知页那批也随它上了远端。
- **教训（已写 troubleshooting 待落）**：同 repo 多会话并发时，`git commit`（无 pathspec）会吞下其他会话 staged 的内容 —— 应用 `git commit -- <paths>` 做 partial commit，或先协调；`git reflog` 是还原这类交错的关键证据；已推送的混合 commit 重写代价高（需 force push），优先接受 + cherry-pick 补齐。

### 12:50 · sitin-rn：给 luka 接 live-test 开关并出「prod 后端」内测 IPA（代码未提交）

- **来源**：用户「我们可以打 prod 接口环境的 ipa包 吗」→ 确认走 **live-test**（房号身份 + 生产后端；生产身份正式包只在发包机）→「给我打这个包」。
- **缺口与改动（3 文件，未提交）**：scripts/build-ipa.sh 早已支持 `<APP>_USE_PRODUCTION_BACKEND`，但 luka 没读它（全仓只有 intro 接了）。照 intro 先例在 `apps/luka/app.config.ts` 加 `usesProductionBackend = isProduction || process.env.LUKA_USE_PRODUCTION_BACKEND === "1"`，只翻四处：`extra.env`、`apiBaseUrl`、`alive.host`、`tim.officialAccountId`（prod 官号 9130276）；**支付档位不动**（fake payments 开 / IAP 关），包名 / 扩展 / App Group 等身份仍归 `isProduction`。契约测试 +1 条（断言「与 production 配置同后端、与房号同身份、支付不动」）；`apps/luka/README.md` 后端表加 live-test 说明。
- **验证**：契约测试 19 passed（含新条）；`pnpm luka:typecheck` 0；biome 干净（一处注释缩进自动修）。
- **构建**：`printf 'n\n' | IOS_TEAM_ID=39CFYH6W55 scripts/build-ipa.sh luka live-test preview "" 10001`（复用工程）。archive 成功（12:44），**export 首次报 `The network connection was lost`（Xcode 网络抖动）** → 对着同一 xcarchive 单独重跑 exportArchive 成功。
- **产物核验**：`apps/luka/ios/build/export/Luka.ipa`，85,819,591 bytes；`com.presence.gracechat` 1.0.0 (10000) Ad Hoc。EXConstants：`apiBaseUrl=https://api-prod.lukasap.com`、`alive.host=prod-tcp.lukasap.com`、`env=production`、`officialAccountId=9130276`、`dataSource=live`、`fakePaymentsEnabled=true / iapEnabled=false / dtcEnabled=true`；今日代码符号在包；UIScene manifest 在；6 个 usdz 全在。
- **沉淀**：`30-Resources/troubleshooting/sitin-rn-local-ipa-verification.md` 新增「坑 4：exportArchive 网络抖动 → 只重跑导出」。
- **注意**：这个包写的是**真实生产数据**，建议用测试账号；**未提交、未推送、未上传蒲公英**；「象」iPhone 仍不在 Ad Hoc 描述文件名单里，装之前要先注册设备（见 11:20 条目）。

### 14:10 · sitin-rn：调试入口改为「任何环境都生效」（去掉构建级剔除）+ 重出 live-test IPA

- **来源**：用户先问「debug 入口打 ipa 包会关闭吗」→ 确认 live-test 包会关（`env=production` 触发 `dev-gate` 的 STORE_BUILD）→ 用户要求「改一下 debug 入口，在任何环境下都要生效」。
- **改动（10 个代码/测试文件 + 6 个文档，均未提交）**：
  - `src/lib/dev-gate.ts`：删 `STORE_BUILD` / `isDevToolsAvailable()`；`armDevGate`/`isDevGateOpen` 在任何构建都生效（dev 直接开；其余走双击 Blocklist 标题 arm + 三击版本号；arm 仍进程级）。
  - `src/stores/dev-flags.ts`：`devFlag`/`useDevFlag` 只剩 `isDevGateOpen()` 一门（构建级双查移除）。
  - `src/app/_layout.tsx`：`LoveRingPreviewHost` 无条件挂载；`src/app/dev/_layout.tsx` 注释同步；`src/services/me.ts`：`[profile]` 诊断日志去掉 gate（所有构建都记，仍只记字段存在性）。
  - 测试：`dev-gate.test.ts` 改成「production/development 都打开、且不再暴露 availability 检查」；`dev-flags.test.ts` 删 store-build 用例；`me.test.ts` 删 dev-gate mock。
  - 文档：`debug-tools.md` 安全模型改两道门（路由守卫 + 进程级 arm）+ 写明已接受代价；`security-hardening.md` §2 同步；三份提审记录（`app-store-review-fixes.md` 加 2026-09-24 更新注、`app-store-review-findings.md` 对照表该行改「❌ 命中（有意接受，提审前需复核）」、`app-store-checklist-review.md` TC-56-005 标 ⚠️）。
- **验证**：focused 3 文件 8 passed；全量 luka **1314 passed / 4 failed**（与今日基线逐条一致：candidate-card-fields ×1 + shape-language ×3，非本次引入）；`pnpm luka:typecheck` 0；biome 8 文件干净。
- **重出 live-test 包**（用户要的包）：archive + export **一次过**（无网络抖动）；产物 `apps/luka/ios/build/export/Luka.ipa`，85,820,052 bytes，`com.presence.gracechat` 1.0.0 (10000) Ad Hoc；`apiBaseUrl=api-prod.lukasap.com` / `env=production` / 官号 9130276；包内 `isDevToolsAvailable` 符号 **0 处**（旧门禁已消失）、`armDevGate` / `isDevGateOpen` / `discoveryDeckCardWidth` 各 1 处。日志 `apps/luka/build-logs/build-20260924-140021.log`。
- **未提交 / 未推送 / 未上传蒲公英**；「象」iPhone 仍不在 Ad Hoc 描述文件名单，装包前要先注册设备。

### 14:15 · sitin-next：feature/pwa 合入 release/test-pwa2（savvy_android 放开 Snapchat 入口 #1153）

- **背景**：`release/test-pwa2` 本地落后远端 2 个提交，先 `pull --ff-only` 到 `f9af28235`；`feature/pwa` 相对 merge-base 只独有 1 个提交（PR #1153 的 merge `c1abc583c`），test2 侧独有 492 个 —— 本次 merge 实质 = test2 + #1153。
- **冲突 2 处**（根因：test2 的 4.2 chat 重构 `2b6536bf6` 删掉了整套 authBlock 机制）：
  - `App.tsx`：feature/pwa 侧加的是 `scUsable/scAuthed/setAuthBlock` + authBlock effect，全基于已删机制 → 取 ours。
  - `AIPersonaPlaceholder.tsx`：modify/delete → 保持删除（test2 侧已移除该占位页，全仓无引用）。
- **带入的 savvy 改动**（auto-merge 4 文件）：`showSocialAuthDrawer.tsx`、`AiGoldCashoutCard.tsx`、`OnboardingTaskList.tsx`、`LiveAction.tsx` 的 Snapchat 入口判定从 `isHavenPwa` 改为 `isHavenPwa || isSavvyAndroid`。
- **结果**：merge commit `cc644bd7f`；`eslint packages/app-pwa` exit 0；用户批准后已 push（`f9af28235..cc644bd7f`，pre-push 的 affected test + build 65 tasks 全过，未用 --no-verify）。
- **坑**：commitlint `body-max-line-length`（100 字符）—— 中文 body 一行写太长被拒，缩写后过。
- **遗留**：`packages/business-pwa-proto/proto` 工作区仍 dirty（submodule HEAD `9e4276bc9` vs 父仓 gitlink `8347028a4`），merge 前就有，未动。

### 16:05 · sitin-rn：push 今天两笔改动（live-test 开关 + 调试入口全构建生效）

- **来源**：用户「有什么是可以 push 的吗」→ 拆两笔 →「push」。
- **提交**（`feature/luka-ios`）：
  - `464c6ea69` `feat(luka): live-test 档接生产后端（LUKA_USE_PRODUCTION_BACKEND）`（3 files，+47/−8）；
  - `b125f955c` `feat(luka): 调试入口在任何构建都可用（去掉商店包构建级剔除）`（13 files，+93/−91）。
- **push**：`fd190bad8..b125f955c` → `origin/feature/luka-ios`。pre-commit biome（2 + 8 files）；pre-push boundaries（41 包）+ typecheck apps/luka（6.9s）全过。工作区现已干净。
- **插曲**：共享工作区里并行会话在我检查后提交并推送了 `b9ca12c8c`（Name/Location 屏取消 autoFocus）与 `fd190bad8`（全屏充值页仓鼠位置），我的两笔叠在其上，线性无冲突。
- **备注**：14:03 打好的 live-test IPA 就是从含这两处改动的 working tree 构建的（无需重打）；「调试入口」那笔会让商店包重新带 dev 面，提审文档已标注风险。

### 16:27 · sitin-rn：在 iPhone X 模拟器上启动 luka 调试（接上 Metro）

- **来源**：用户「启动 真机调试」。首轮误把目标当 iPad（iPad 在线但息屏锁定，`devicectl` 拉起连续被 `Locked` 拒）→ 用户纠正「没插 iPad，用的 iphoneX」= 名为 **iPhone X (11 Pro eq)** 的模拟器（iOS 26.5）。
- **做了什么（零代码改动）**：
  1. `simctl boot` + `bootstatus -b` 启动 iPhone X 模拟器（UDID `7DE771A0-…`，原 Shutdown）；
  2. `simctl install` 复用 10:53 给 iPhone 18 Pro 构建的 `Debug-iphonesimulator/Luka.app`（DerivedData `Luka-bimsvdnybbhpesdkqerkvmeohvok`）；
  3. `simctl openurl <udid> "luka://expo-development-client/?url=http://localhost:8081"` → 复用 12:24 起的 **luka Metro**（pid 13800，cwd `apps/luka`，分支 `feature/luka-ios`，工作区干净）。`/json/list` 出现 `com.presence.gracechat (iPhone X (11 Pro eq))`，截图确认登录页正常渲染；当前 iPhone 18 Pro 与 iPhone X 两个模拟器同连一个 Metro。
- **发现（本机环境）**：**Xcode 27 已没有 Simulator.app** —— `open -a Simulator` 报找不到，`Xcode.app/Contents/Developer/Applications/` 已空；替代品是 `Xcode.app/Contents/Applications/DeviceHub.app`，已 `open`（pid 37019）供用户看设备界面。DeviceHub 的交互能力（点按/键盘）尚未验证。
- **备注**：iOS 26.5 上跑 27 SDK + enableSceneSupport 的 dev 包正常；两个模拟器（26.5 / 27.0）并行连同一 Metro 无冲突。
- **关键命令**：`xcrun simctl boot/bootstatus`、`simctl install <udid> <app>`、`simctl openurl <udid> "luka://expo-development-client/?url=…"`、`simctl io <udid> screenshot`、`open /Applications/Xcode.app/Contents/Applications/DeviceHub.app`。

### 16:57 · sitin-rn（luka）：iOS TIMPush 证书填 44170（四个 profile，未提交）

- **来源**：用户给出控制台值 `EXPO_PUBLIC_TIM_PUSH_BUSINESS_ID_IOS 44170`，指定「luka、全部 iOS profile」。
- **改动（3 文件，未提交）**：
  - `apps/luka/eas.json`：四个 profile（development / preview / nightly / production）的 `env` 均加该值 —— 本机 `build-ipa.sh` / `archive-ios.sh` 会从 eas.json 读并 export，EAS 云端同理。
  - `apps/luka/docs/app-store-review-fixes.md`、`app-store-review-findings.md`：「未闭环外部依赖」标注 iOS 已配 `44170`（2026-09-24），Android 仍待配。
- **验证**：`pnpm luka:pre-submit` → `✓ TIMPush 证书 (iOS/APNs) 44170`；`node` 校验四档值一致；biome 对 eas.json 0。
- **pre-submit 仍报（需用户/后续确认）**：① **production 与 development 都是 44170**（「生产证书 = 开发证书」警告 —— 若 44170 是 .p8 密钥则沙盒+生产通吃、属预期；若是 .p12 沙盒证书，则生产包收不到推送，需给 production 换生产证书）；② TIMPush Android/FCM 仍空；③ AppsFlyer 占位值与 blueprint 撞车（既有）；④ luka 未注册 android-release-signing 插件（既有）。
- **备注**：本地 `luka:ios:device` 脚本未注入该 ID —— 真机 dev 包要验离线推送还得带 env（可参照 iris `ios:device` 注入 43542 的写法）。
- **17:0x 已提交并 push（用户「push」）**：`986b71fe3 chore(luka): 配置 iOS TIMPush 离线推送证书 44170（四个 profile）`（3 files，+13/−7）→ `origin/feature/luka-ios`（`b125f955c..986b71fe3`，GitLab HTTPS）。pre-commit biome 1 file、pre-push boundaries（41 包）+ typecheck apps/luka（6.4s）全过；pathspec 提交，未跟踪的 `apps/iris/luka.p8`（待用户处理，切勿入库）未动。

### 17:09 · sitin-rn：T0198（登出后跳登录页白屏）判读 session.log —— JS 链路完整，疑点落在原生栈重建

- **来源**：用户报 T0198「【退出登录】退出登录，跳转至注册首时，白屏【偶现】，需要重启才行，有点像网络波动导致，已上传日志」。从飞书 buglist（wiki `GufhwkKlDi5cCWkN1wvce6wFnSh` → base `MbjobIs4Qax91fsXfUWc9Z4WnKd`，表 `tblywHQVyR7KIGC9`）取记录 `recvw7SKyENh6M` 与附件 `session(1).log`（5,077,326 bytes；P0，赵延杰建，指派张峥）。
- **日志判读**（09:52–16:53 北京时间；测试包 = 09-24 11:11 `test preview` IPA `78387e011`，api-dev）：
  - 全日志 11 次完整登出：7 次登出后 3–5s 内测试同学点了 Apple 登录（正常）；**4 次登出后 3–16s 把 App 杀掉重开**（16:46 / 16:48 / 16:51 / 16:52）—— 对应「需要重启才行」。
  - 4 次异常里 JS 链路全部打完：`confirm: sign-out tapped → chat torn down → auth logged out → luka_login_page_show` 均在 ~1s 内 —— **登录页 JS 侧已挂载**，白屏在原生呈现层（或空白盖没被移走），不是卡在 await。
  - 网络确实差：Apple 授权 16:46 挂了 2.5 分钟、16:49 挂 19s 后 `RequestUnknownException` 回落设备登录；但不解释登出白屏（次次都在本地 ~1s 内完成）。
  - 测试包已含 T0144 终解全套（`b8e548a62` + `c4c07d87d` key + `67164e16e` session-cover），且这是该方案的首个测试包。
- **假设（待真机验证）**：退出路径三条原生操作挤在一起 —— `prepareSessionExit()` 空白盖 → `logout()` 翻守卫触发 `<Stack key>` 整棵原生栈重建 → ~10ms 后 `router.replace("/login")`；偶现时新栈没挂上 login 视图，留下奶油底（#FDF9EE，近白）空盖，重启（原生栈重建）才好。`Stack key` 是 T0144 中途尝试（真机复验证伪），终解是空白盖；盖已保证翻守卫时栈内无嵌套容器，key 可能已冗余，而全栈重建正是这类「白屏且重启才好」的温床。
- **下一步（等用户裁决）**：① 首选移除 `Stack key` + 同步不变量测试与 memo，出新内测包复验；② 让测试同学补白屏录屏/机型，确认是奶油底还是登录页本体；③ 本地模拟器临时脚本先跑复现。
- **代码未改**，仅分析（临时日志存 `/var/folders/.../T/opencode/t0198/session.log`）。

### 17:18 · sitin-rn（luka）：T0198 修复（去 Stack key）已推 + test/preview 内测 IPA 已出

- **用户裁决**：「直接改：去掉 Stack key」。
- **改动（2 文件，commit `896c8278e`）**：
  - `apps/luka/src/app/_layout.tsx`：根 `<Stack>` 去掉 `key={authed ? "authed" : "guest"}`；注释重写为「不许再加会话 key」+ 机理（T0144 真机复验证伪、终解是空白盖；整棵原生栈重建与退出路径的 `replace` 抢同一帧 → T0198 白屏）。
  - `apps/luka/tests/design/protected-routes.test.ts`：原「必须有 key」不变量换成「根 Stack 不许有 `<Stack key=` + 四条退出线（`settings/logout` / `settings/delete` / `components/dev/debug-actions-section`）都必须先调 `prepareSessionExit()`」。
- **验证**：biome 2 文件 0；`pnpm --filter luka exec tsc --noEmit` 0；`pnpm boundaries` OK；luka 全量 **1314 passed / 4 failed**（与今日基线一致：shape-language ×3 + candidate-card-fields ×1）；focused protected-routes 3 passed。pre-commit biome、pre-push（boundaries + typecheck apps/luka 6.4s）全过。
- **推送**：`986b71fe3..896c8278e` → `origin/feature/luka-ios`（GitLab）。
- **内测包**：`printf 'n\n' | IOS_TEAM_ID=39CFYH6W55 scripts/build-ipa.sh luka test preview "" 10001`（复用工程，JS-only）；产物 `apps/luka/ios/build/export/Luka.ipa` 85,820,131 bytes，1.0.0 (10000)，Ad Hoc 房号 `com.presence.gracechat`；日志 `build-20260924-171441.log`。含 16:57 那笔 TIMPush `44170`。**未上传蒲公英**。
- **待办**：测试同学按「登出 → 不重启应直接落登录页」复验（多试几次）；若仍白屏，下一手改 `session-cover` 为声明式跳转（去掉 imperative `replace`）。

### 17:35 · sitin-rn（luka）：T0199 左上角引导 Continue 改走 Apple/Google 身份登录（未提交）

- **来源**：T0199（P0，赵延杰 17:09 建，17:21 改描述 + 录屏）「【注册】左上角指引登录、开白、普通注册，注册成功后，是分别的账号，预期：应该是普通注册入口」。视频判读（下载附件抽帧）：登出 → 左上角组件引导 4 步 → 产品预览 Continue → 落回设备绑定的**开白账号**（与登出前同号）；随后 sign-in 邮箱登录也是同一账号。用户确认口径：要左上角这条路也走 Apple/Google 那条身份交接。
- **根因**：两条入口让后端认人的 key 不同 —— 引导页 `execute()` → `loginWithDevice()` 不带第三方身份，按**本机 deviceId** 认人（`packages/business-auth/src/auth.ts:110`）；欢迎页 Apple 走 `loginWithApple(appleUserId)`，把 **Apple opaque id 当 deviceId**（同文件 :167）。测试机 deviceId 被「sign-in 邮箱登录开白号」绑到开白账号（`meta` 不钉 deviceId，`review-mode.ts:74`），Apple ID 对应另一个（普通）账号 → 两路账号不一致。
- **改动（4 文件 +1 新增，未提交）**：
  - 新增 `apps/luka/src/lib/third-party-auth.ts`：Apple/Google 原生授权胶水从 `login.tsx` 抽出，欢迎页与引导页共用（防止账号归属再漂）。
  - `apps/luka/src/app/widget-guide.tsx`：`continueFromGuide` 由 `await execute()` 改为 `executeApple(authorizeWithApple)` / `executeGoogle(authorizeWithGoogle)`（按平台），注释写明「不要用纯设备 FastLogin（T0199）」；取消原地停、原生失败/后端拒绝回落设备登录的语义不变。
  - `apps/luka/src/app/login.tsx`：删本地两份 authorize，改用 `@/lib/third-party-auth`；头注释同步。
  - `apps/luka/docs/widget-guide.md`：流程图与「产品预览 Continue」口径改写（第三方身份 = 普通注册出口）。
  - `apps/luka/tests/design/guest-path.test.ts`：「Continue 走 FastLogin」用例换成「与欢迎页同一条第三方身份登录 + 两屏共用胶水」。
- **验证**：biome 4 文件 0、全仓 `pnpm lint` 0；`pnpm --filter luka exec tsc --noEmit` 0；`pnpm boundaries` 0；focused（guest-path + use-fast-login）38 passed；全量 luka **1314 passed / 4 failed**（与今日基线逐条一致：shape-language×3 + candidate-card-fields×1）。**未实机**。
- **待办**：出 test/preview 包后真机复验「登出 → 左上角引导 → Continue 拉起 Apple 面板 → 授权后进的是普通账号（与 sign-in 开白账号不同）；取消授权停在产品预览页」。
- **17:44 追加（用户要求）**：产品预览的 `Continue` 下面加一条 `Go back`，点击回 welcome 页。
  - 新增 `apps/luka/src/components/widget-guide/guide-text-button.tsx`：次级文字按钮（从 `guide-footer.tsx` 的 `Skip for now` 抽出，两处共用一份样式）；`guide-footer.tsx` 改用之。
  - `continue-preview-step.tsx`：Continue 下、假 TabBar 上插 `<GuideTextButton label="Go back" />`，新 prop `onLeaveGuide`；`widget-guide.tsx` 传 `onLeaveGuide={leaveGuide}`（与 `Skip for now` 同出口：`dismissTo("/login")`，不重复入栈）。`docs/widget-guide.md` + `guest-path.test.ts` 同步（新增「Go back 与 Skip 同一个出口 + 位置在 Continue 与 TabBar 之间」用例）。
  - 验证：biome 0、`tsc --noEmit` 0、guest-path **28 passed**、全量 luka **1315 passed / 4 failed**（与今日基线逐条一致）。**未实机**。
- **17:55 推送 + 出包（用户要求）**：
  - 提交 `c536d9241` `fix(luka): 左上角引导改走第三方身份登录并加 Go back（T0199）`（8 files，+126/−53，pathspec 提交）→ `origin/feature/luka-ios`（`896c8278e..c536d9241`，GitLab HTTPS）。pre-commit biome 7 files、pre-push boundaries（41 包）+ typecheck apps/luka（6.9s）全过。
  - **test/preview 内测 IPA**：`printf 'n\n' | IOS_TEAM_ID=39CFYH6W55 scripts/build-ipa.sh luka test preview "" 10001`（复用工程；PATH 指 node 24，17:46 起跑）。产物 `apps/luka/ios/build/export/Luka.ipa`，**85,824,851 bytes**，`com.presence.gracechat` 1.0.0 (10000)，Ad Hoc（`iOS Team Ad Hoc Provisioning Profile`，71 台，含 iPad `00008122-000960643CB9801C`，get-task-allow=false），Apple Distribution: AI FANTASY, INC (39CFYH6W55)；日志 `build-20260924-174618.log`；sha256 `1ae97c7d…`。
  - **产物核验**：EXConstants `env=development` / `apiBaseUrl=https://api-dev.lukasap.com` / `appName=luka` / `dataSource=live` / `dtcEnabled=true`（test+preview 内部档）；UIScene manifest 在、6 个 usdz 全在；JS bundle 命中今日符号 `Go back` / `authorizeWithGoogle` / `executeApple` 各 1 处。**未上传蒲公英**。
- **待办**：测试同学两题一起复验 —— T0198（登出→不重启落登录页）；T0199（登出→左上角引导→Continue→Apple 面板授权后进**普通账号**（不是开白）；Continue 下 `Go back` 回 welcome；取消授权停在产品预览页）。Base 里两条仍是「未开始」，由测试同学验完自行流转。

### 18:20 · sitin-rn：排查「luka 为什么没收到 push」—— 根因：17:50 的 IPA 里没有 timpush-configs.json（复用工程未 prebuild）

- **用户问**「为什么没有收到 push」。取证：解包 17:50 的 `Luka.ipa` → `Payload/Luka.app/` 里
  **没有** `timpush-configs.json`（find/cat 均无）；`embedded.mobileprovision` 的
  `aps-environment=production`（Ad Hoc）。
- **根因**：`EXPO_PUBLIC_TIM_PUSH_BUSINESS_ID_IOS` 只在 **prebuild 期**被
  `@heyhru/rn-tim-push/plugins/timpush` 消费（写 `ios/Luka/Resources/timpush-configs.json`）；
  17:18/17:50 两次出包都是**复用工程**（build-ipa.sh 非 clean 路径只回写 DEVELOPMENT_TEAM，
  不跑 prebuild）→ 44170 没进原生工程 → 包收不到离线推送，构建全程不报错。
- **修法**：clean prebuild 重出包（`printf 'y\n' | IOS_TEAM_ID=39CFYH6W55 scripts/build-ipa.sh luka …`）。
- **待查（会影响后续验收）**：① Push 服务（SDKAppID 1600002475）到期日 2026-08-06 是否已续；
  ② 44170 是 .p8 还是 .p12（Ad Hoc 走生产 APNs，沙盒证书必拒收）；③ Android businessID 仍空；
  ④ 测试姿势：前台不弹系统横幅（设计如此）、模拟器永远收不到离线推送。
- **沉淀**：`30-Resources/troubleshooting/sitin-rn-local-ipa-verification.md` 新增「坑 5」+
  「固定姿势」备注（原生依赖或 prebuild 期 env 变了才 clean prebuild）。
