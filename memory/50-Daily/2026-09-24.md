---
title: 2026-09-24
date: 2026-09-24
tags: daily
---

# 2026-09-24

## 今日重点

- [ ] **T0150 / T0048 / T0124 / T0135**（likes & visitors 红点一族）：T0150 根因（`lastVisitTime` 毫秒 vs 基线秒）已修并 push `2b6a5988e`，T0135 兜底 `ea8651d72` —— 待测试同学在新包上验证「进 Visitors 后卡片红点与爪印归 0 / 停留期间新访客当场清 / 离开后新访客仍计 1」
- [ ] **真机验收一批**（Luka dev build 已装到 iPad Air 11-inch M3，见工作日志）：T0057 键盘退出 / T0054 进屏不弹键盘 / T0053 地址保存 / T0055 去动作栏 / T0056 全帖列表；Sent 卡 `a759a0692`；引导第 4 步 `35608d542`；通知分段徽标定位 `84a5b1107`；luka moments See all `a4d0f3f9a`；付费墙权益卡（`19861257a`，订阅态 + 短屏）
- [ ] **埋点 parity 分支 `feat/luka-analytics-parity` 未提交未推送**（20:26 全量逐事件修复 + `business-analytics` 0.5.0 + `rn-tim` sourceType）—— 等用户指示
- [ ] **T0052**（重登后资料丢）：等真机日志，`grep '\[profile\]'`（dev/preview 包 Export Log）→ 定后端修投影还是客户端合并读模型
- [ ] **T0061**（订阅赠币 +0）：给「SP后端内部群」的对账请求已拟好，等用户确认后发
- [ ] **skeleton shimmer 黑亮竖带**（21:07 定位为 expo-linear-gradient 伪影）：待用户确认真机屏上是否可见，再定是否按修法 1 改 `ShimmerOverlay`
- [ ] **iPad 模拟器走 onboarding**（23:14 被登录挡住）：等用户给测试账号或授权点击自动化
- [ ] 根目录未跟踪的 `-` 文件（devicectl UDID 误产物）删不删，待用户定夺

## 工作日志

### 10:03 · colna-memo：整理 2026-09-23 Daily（今日首次任务）

- 09-23 共 515 行、40 余条日志，全部为实质内容：**无归档**（无调试垃圾/闲聊，两处「未完成」均为等待态）；可复用经验当天已各自沉淀（`luka-likes-visitors-unread-semantics`、`luka-skeleton-shimmer-black-band`、`git-parallel-sessions`、`rn-prebuilt-core-debug-release-swap`），本次另外补了昨天跨夜任务的笔记（见下）。
- 复制 8 条未完成待办到今日「今日重点」，并把 T0135 一族与 T0150 的最新 commit 状态写全。

### 10:03 · sitin-rn：Luka 装到 iPad 真机并连 Metro 调试（跨 09-23 夜 ~ 09-24 早）

- **做了什么**：把当前分支（`feat/luka-analytics-parity`，含未提交改动）的 Development Build 装到插着的 iPad 真机（iPad Air 11-inch M3 / iPadOS 26.3.1 / UDID `00008122-000960643CB9801C`）并连 Metro 调试。链路 09-23 21:06 起跑，中途被签名与 Wi-Fi 慢挡住；09-24 早用户要「安装 luka」，完成重装 + 拉起，`curl localhost:8081/json/list` 出现 `com.presence.gracechat (iPad)`。
- **过程（四个坑）**：
  1. 先补 `apps/luka/.env`（gitignored）`EXPO_APPLE_TEAM_ID=39CFYH6W55`；`prebuild -p ios --clean` + pod install（`HEYHRU_BYTEPLUS_NATIVE=1 EXPO_USE_PRECOMPILED_MODULES=0`）成功。
  2. `expo run:ios --device` 签名失败：它**不带** `-allowProvisioningUpdates`，房号两个扩展（`.widget` / `.notiservice`）第一次上真机、没有描述文件 → 改走 xcodebuild 传统通道（`-destination 'generic/platform=iOS' -allowProvisioningUpdates -allowProvisioningDeviceRegistration -derivedDataPath build DEVELOPMENT_TEAM=39CFYH6W55 CODE_SIGN_STYLE=Automatic`）→ Xcode 自动注册两个扩展 App ID + 描述文件，BUILD SUCCEEDED。
  3. iPad 本地网络权限弹窗挡住 dev launcher（用户点了「允许」）。
  4. Wi-Fi 传 bundle 极慢（~2%/min，Mac→iPad socket sendq 积压 1.6 MB）→ 用 USB 隧道：Mac 侧 `fd79:1a6d:1adf::2`（`devicectl device info details` 的 Tunnel IP 是设备侧 `::1`），深链 `luka://expo-development-client/?url=http://[fd79:…::2]:8081` → 秒级完成。
- **结果**：Luka.app（房号 `com.presence.gracechat` 1.0.0）已装并运行在 iPad；Metro 用**用户原有的** 8081 实例（pid 54637）；确认 `com.presence.gracechat (iPad)` 调试目标在线。副作用：房号全产线共用，装 Luka 顶掉了同机的 Rocco（同 bundle id）。
- **关键命令**：`xcodebuild … -allowProvisioningUpdates …`；`xcrun devicectl device install app --device <UDID> …/Luka.app`；`xcrun devicectl device process launch --terminate-existing --device <UDID> --payload-url "luka://expo-development-client/?url=…" com.presence.gracechat`；`xcrun devicectl device capture screenshot --device <UDID> --destination /tmp/x.png`（看设备屏幕，排弹窗很快）。
- **沉淀**：新建 `30-Resources/troubleshooting/luka-house-id-device-build-usb-tunnel.md`（四坑 + 命令速查）。
- **未动**：仓库代码零改动（只有 gitignored 的 `apps/luka/.env` 新增）；根目录 `-` 与其他未提交 WIP 未碰。

### 10:35 · sitin-rn：Luka Profile 菜单卡去掉订阅管理两行（已 push）

- **来源**：用户贴 Profile 截图，圈出 `Manage payment account` / `Manage subscription` 两行，要求去掉。
- **改动（2 文件）**：`apps/luka/src/components/me/me-menu-card.tsx` 删两行及条件渲染（`config.extra.features.dtcEnabled`、`Platform.OS === "ios" && isPlus`）与随之不用的 imports，头注释同步；`get-more-tab.tsx` 删 `onManagePaymentAccount` / `onManage` 两个回调。路由与页面（`/manage-payment-account`、`/manage-subscription`）保留：`_layout` 仍注册、dev 调试区仍可进；Settings hub 本来就没有这两个入口。
- **验证**：biome 2 files 0；`pnpm --filter luka typecheck` 0；全量 luka **1311 passed / 4 failed** —— candidate-card-fields ×1 + shape-language ×3，来自并行会话 staged 的 discovery/chat 改动（本文件不涉圆角与候选卡）。
- **提交**：`9ebc5ce4f` → `origin/feature/luka-ios`（`1a2578612..9ebc5ce4f`，GitLab HTTPS），pre-push boundaries + typecheck apps/luka（6.1s）通过（按 luka 改完直推约定）。
- **插曲**：首次 `git commit` 把并行会话已 staged 的 5 个 discovery 文件一起带进提交 → 未推送前 `git reset --soft HEAD~1` + 只重提 2 个文件 + 把 5 个文件重新 `git add` 还原其 staged 状态。沉淀：`30-Resources/troubleshooting/git-parallel-sessions.md` 新增「commit 会捎上别人已 staged 的文件」一节。
- **未动**：并行会话 staged 的 5 个文件。

### 11:00 · sitin-rn：Luka 划卡页 iPad 适配（宽/高自适应 + 内容缩放）+ 真机验证（未提交）

- **来源**：用户贴 iPad 真机截图「这页 ipad 中划卡的样式有问题，兼容 ipad」。
- **实测定位**（真机 + 临时 debug 文本，用 `devicectl` 截图读数）：iPadOS 26 把 iPhone-only
  的包放进兼容窗口并整体放大 —— **逻辑窗口 375×669**（宽同 iPhone、矮 143pt），渲染放大
  ~3.4 倍；划卡页卡组可用高度只剩 **214pt**（iPhone 上约 350）→ 卡片成 335×214 的横条，
  固定 ~140pt 的信息带把照片挤成一条（只剩眼睛）。
- **改动（8 文件，未提交）**：
  - `lib/discovery-card-fit.ts`：新增 `discoveryDeckCardWidth`（竖框=可用宽度、与以前逐点一致；
    横框按 350×547 收窄、320 兜底、幂等）与 `discoveryCardContentScale`（卡高 <300 内容按比例
    缩、下限 0.62，手机恒 1）。
  - `nearby-view.tsx`：量本屏高度反推卡宽；仓鼠条与卡组同宽居中；量完高度再挂卡组（防先满宽闪一帧）。
  - `discovery-deck.tsx`：新增 `contentWidth`，卡组列收窄居中（`deckRef`/引导目标也移到该列）。
  - `candidate-card.tsx` / `candidate-card-info.tsx` / `tag-chip.tsx`：缩放贯通；标签行高度改用
    `tagChipMetrics` 单一来源（首版行高自算，把芯片裁掉半截 —— 已修）。
  - `deck-hero.tsx` 导出 `DECK_HERO_HEIGHT`；新增 `apps/luka/docs/discover-deck.md`；单测 +7。
- **验证**：`pnpm lint` 0；`pnpm --filter luka exec tsc --noEmit` 0；luka 全量 **1314 passed /
  4 failed**（与今日基线逐条一致：shape-language×3、candidate-card-fields×1）。iPad 真机 `/reload`
  后截图：照片露出整张脸、标签/名字/城市比例正常；同时 `lsof` 确认 App 已回到用户的 8081。
- **环境坑（沉淀）**：dev client 缓存了旧 bundle，换端口起第二个 Metro 也没用；
  `curl -X POST http://localhost:8081/reload` 广播 reload 才让它重拉。临时起的 8082 已停。
- **并行会话现场**：工作区被 `git pull --rebase --autostash` 动过 —— `nearby-view.tsx` 出现
  stash 冲突（上游 `openDeckProfile` 来源埋点 vs 我的改动），已手工合并保留双方并 `git add` 解冲突；
  **`stash@{0}`（对方 parity WIP）与 `stash@{1}`（autostash）都未动**，对方新增的
  `app.config.ts`/`package.json`/`pnpm-lock.yaml`/`pnpm-workspace.yaml` WIP 也未动。
