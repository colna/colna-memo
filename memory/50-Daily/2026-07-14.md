---
title: 2026-07-14
date: 2026-07-14
tags: daily
---

# 2026-07-14

## 今日重点

### 从 07-13 结转(与当前 APK 键盘工作直接相关)

- [ ] **APK 键盘 overlay 方案真机验**:输入栏贴键盘、跟随、结尾不跳;掉帧是否可接受(结论:WebView 逐帧 transform 合成有天花板,彻底解需安卓 resize WebView 消费 ime insets)
- [x] 前端 PR [#608](https://github.com/presence-io/sitin-next/pull/608) 已解决冲突并 push(`MERGEABLE`);安卓 PR [#84](https://github.com/presence-io/GraceChat-Earn-Android/pull/84) **已 merge** 进 `p/ljb/sitin4`
- [ ] **⏳ 明天做**:APK「从后台切回来输入框变低」——纯前端 workaround 改 `sitin-next` `packages/app-pwa/src/utils/bridge.ts`(根因+方案+代码落点见下方 08:43 工作日志)
- [ ] 07-13 其余四大类 TODO(拍板 / 技术债 / 安全 / 真机验证)未闭环,清单见 `50-Daily/2026-07-13.md`,未重复搬运

## 工作日志 — 08:1x 安卓键盘 ime 高度修复提 PR #84

- **做了什么**:`GraceChat-Earn-Android` 给 APK 键盘上抬修复提 PR。分支 `p/colna/kb-ime-height`(base `p/ljb/sitin4`),PR [#84](https://github.com/presence-io/GraceChat-Earn-Android/pull/84)。
- **改动(1 文件)**:`common/.../KeyboardHeightProvider.kt`。`onKeyboardAnimProgress` / `onApplyWindowInsets`(mStandardHeight)原用 `ime − systemBars`(减掉了导航栏),而 `onKeyboardAnimEnd` 用完整 `ime`(getKeyboardHeight)→ 两者差一个导航栏 → 前端在动画结尾那一帧突然补上、输入栏跳。改成 progress/end 都用完整 `typesInset.bottom`(ime)。
- **提 PR 前清理**:分支上原有「加 ~30fps 节流」+「revert 节流」两个相互抵消的 commit(节流反而放大掉帧,已 revert)。`git reset --hard` 到高度修复那个 commit + `--force` push,PR 只含 1 个干净 commit。
- **gh 身份**:已确认 colna。

## 工作日志 — 08:16 sitin-next PR #608 解决 merge 冲突

- **做了什么**:`sitin-next / app-pwa` 解决 PR [#608](https://github.com/presence-io/sitin-next/pull/608)(`personal/zz/pwa-apk-keyboard-navbar` ← `feature/sitin4.0`)的 merge 冲突。merge commit `a5c8f2579`,**未 push**(待批准)。
- **冲突文件**:仅 `components/TabLayout.tsx`(UU);`TutorialChat/index.tsx` 等 auto-merge。
- **冲突根因**:base 那次 commit `491e83f21` 把键盘检测抽成了 `hooks/useKeyboardOpen.ts` + `components/KeyboardAwareTabBar.tsx`(TabLayout 和 `/tutorial-chat` shell 都改用它),和本分支在 TabLayout 内联的键盘逻辑 + `--nav-height` 发布撞车。
- **解决方案(采用 base 架构)**:
  1. `TabLayout.tsx` 取 base 版(去掉内联 `useKeyboardOpen` + navRef 块,渲染 `<KeyboardAwareTabBar />`)。
  2. 本分支的 `--nav-height` 发布(ResizeObserver 量 nav offsetHeight,供 Chat/TutorialChat 输入栏 `--kb-height − --nav-height` 上抬)**搬进 `KeyboardAwareTabBar`** —— 比原来只在 TabLayout 发布更完整(base 让 tutorial shell 也用了 KeyboardAwareTabBar,两处 shell 自动覆盖)。
- **关键判断**:base 的 `useKeyboardOpen` 未显式判 APK,但 APK WebView 的 `visualViewport` 从不 resize → 恒 `false`,天然等价于本分支原来 `isApp()→false` 的效果,且 base 版多了横竖屏 baseline 重置,更好 → 直接用 base hook,弃本分支内联版。
- **踩坑**:`business-pwa-proto/proto` submodule 指针在 merge/build 时反复漂移,`git checkout --` 后又漂;不 add 不进 commit 即可,merge commit 干净。
- **结果**:无冲突残留,`pnpm --filter @heyhru/app-pwa lint` 0、pre-commit(lint 51 tasks + circular no violations)全过。已 push(pre-push `--no-verify` 绕过既有 minerva/social-proxy CI 失败,昨日已授权),PR #608 状态 `MERGEABLE`。

## 工作日志 — 08:43 排查「APK 从后台切回来输入框变低」+ 定方案(⏳明天做,可能换设备)

> **跨设备接手点。相关代码:前端 `sitin-next`、安卓 `GraceChat-Earn-Android`,均在 `/Users/max/Dev2/zhangzheng/` 下(换设备后路径可能是 `/Users/<user>/Dev2/zhangzheng/`)。**

- **状态前提**:安卓 ime 高度修复 PR [#84](https://github.com/presence-io/GraceChat-Earn-Android/pull/84) **已 merge** 进 `p/ljb/sitin4`(commit `ad63f9d9`,本地 base 已 ff 同步);前端 PR [#608](https://github.com/presence-io/sitin-next/pull/608)(分支 `personal/zz/pwa-apk-keyboard-navbar`)冲突已解决并推送。

- **新问题**:APK 键盘弹起状态下切后台 → 切回前台,输入框位置**变低**(被键盘工具栏盖住一截),不再贴键盘上沿。截图在飞书对话。

- **根因(已读代码定位)**:输入框上抬量 = `--kb-height − --nav-height`;变低 = `--kb-height` 变小。而 `--kb-height` 100% 来自安卓 `notifyKeyboardChanged` 上报的 `height`。安卓**只在「键盘动画回调」里 push 高度给 web**(`PWAWebViewFragment.kt:160-198`):
  - `onKeyboardAnimProgress` 逐帧上报 `min(typesInset.bottom, mStandardHeight)`
  - `onKeyboardAnimEnd` 上报 `getKeyboardHeight()` = **完整高度**(仅这一帧给完整值)
  - `onApplyWindowInsets`(`KeyboardHeightProvider.kt:287`)**不通知 web**,只更 `mStandardHeight` 就 `return CONSUMED`
  从后台恢复时,输入框仍 focus、系统直接把 insets 设成键盘终态、**不播放完整 IME 动画** → progress/end 正常序列没走完 → web `--kb-height` 停在偏小中间值(或旧值)。唯一必触发的 `onApplyWindowInsets` 又不 push web → **恢复路径缺一次「把完整键盘高度重推给 web」的动作**。

- **决定方案:纯前端 workaround(用户拍板——不想再发安卓版)**。前端没有独立键盘高度源(APK 里 visualViewport 不 resize),只能用 ①缓存的完整高度 ②输入框 focus 作「键盘是否弹着」代理。落点全在 `packages/app-pwa/src/utils/bridge.ts` 的 `notifyKeyboardChanged` handler(约 397 行)附近:
  1. handler 里累积 `lastKbFullHeight = Math.max(lastKbFullHeight, params.height)`(仅 `show` 时;用 max 防被恢复时的偏小中间值污染)。
  2. 加 `document.addEventListener("visibilitychange", ...)`:`visibilityState==='visible'` 时,若 `document.activeElement` 是 INPUT/TEXTAREA/contentEditable(键盘应弹着)→ 把 `--kb-height` 重设为 `Math.max(0, lastKbFullHeight - KEYBOARD_TOP_GAP)`;否则设 0。重设包一层 `requestAnimationFrame`。
  3. 时机若抖动,把重设改挂到 input 的 `focus` 事件 / 延后一帧,真机看效果再定。
  - **权衡**:纯前端优点 = 只发 PWA、快;缺点 = focus 是代理非权威,极端场景(切回来 focus 在但键盘没弹)会误抬。要 100% 稳才需安卓 `onResume`/`onWindowFocusChanged(true)` 里 `isKeyboardShow()` 为真时补推 `getKeyboardHeight()`——本次不走这条。

- **明天 TODO**:在现分支 `personal/zz/pwa-apk-keyboard-navbar`(或从最新 `feature/sitin4.0` 切)改 `bridge.ts` 实现上述方案 → `tsc`/`lint` → 真机验(切后台→回来输入框是否复位贴键盘;有无误抬)。`KEYBOARD_TOP_GAP=15` 常量已在 `bridge.ts:17`。
