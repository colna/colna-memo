---
title: 2026-08-23
date: 2026-08-23
tags: daily
---

# 2026-08-23

## 今日重点

- [ ] snapchat getChatMessages 视频分支:补 jsdom 单测(extractSingleBubble 需暴露/引 jsdom)
- [ ] snapchat call 不回根因未修:通话态聊天页打不开 → send/getChat 30s 超时(见下方 08-23 首条)

## 工作日志

### HH:xx snapchat DM 监听去掉跨 reload 去重

- **背景**:分析一份 SC-Scraper 运行日志(`sitin-next` `personal/zz/sp-snapchat`,女号 yy-668899 ↔ 男号 c bo/cc-bo88)。发现 `call`(未接来电)那条回复("Oh you tried calling me…")后端已生成,但 `sendMessage` 30s `ELEMENT_TIMEOUT` 发不出去 —— 根因:带「呼叫中/未接来电」的会话打开后 Snapchat 把页面弹回收件箱(`url=/web, inChat=false`),`checkChatPageLoaded` 只轮询输入框 `div[role="textbox"][contenteditable="true"]`,死等超时。这与 8-20 修的「call 无 orderKey → 游标不推进」是**两个相邻但不同**的问题(orderKey 那个现在 OK,cursor 正常推进到 313)。
- **本次改动**(用户要求「先去掉去重」):`packages/app-ins-scripts/src/snapchat/actions/startDMListener.js` 删掉 8-20 加的跨 reload sessionStorage 去重(`REPORTED_STORE_KEY`/`reportedKeyOf`/`loadReported`/`saveReported` + `diffAndReport` 命中判断),改为每次扫描把当前所有未读全量上报。日志 `未读 X key=… 已报=… → 跳过/新增` 简化为 `未读 N 条: …`。
- **动机**:去重 key = `status|timestamp`,`未接来电` 的 timestamp 为空 → key 退化成 `"未接来电|"` 被永久跳过(日志结尾 40+ 行空转),挡住了合法重报。
- **代价**:退回 8-20 前行为 —— getChatMessages 后端 navigateToInbox 整页刷新 → 首扫重报,同一未读随刷新反复上报,后端需自己幂等/承压。
- **结果**:1 file +4/-49;node --check OK;`node --test` 7 passed;pre-commit(全量 lint 51 包 + depcruise circular)+ pre-push build(53 tasks)全过。commit `bd5ff1a4f`,已 push `origin/personal/zz/sp-snapchat`(`0213caef4..bd5ff1a4f`)。
- **部署 dev**:`node scripts/upload-all.mjs --platform snapchat --token <JWT> --files startDMListener` → **v112** snapchat/automation:16 已激活到 `admin-api-dev.sitin.ai`,已通知设备更新。坑:默认 `super_admin/admin123` 在 dev 已失效,必须用户给 JWT token(token 不入库)。upload 读 `src/snapchat/` 源码、per-script 上传、不走 build。
- **未闭环 follow-up**:真正的 call 不回根因(通话态聊天页打不开导致 send/getChat 30s 超时)**未修**;方向:`checkChatPageLoaded` 识别通话 UI / URL 被弹回 `/web` 时快速失败,或 `sendMessage`/`getChatMessages` 超时后自动重导航一次(通话结束后即可进)。

### HH:xx snapchat getChatMessages 新增 video 类型抓取

- **需求**:用户给了真实 DOM —— 视频气泡是 `<video src="blob:…">` 包在 `div.f8j5n[role="button"][style*="aspect-ratio"]` 里,**无 `<img>`**。问「会抓 video 吗」→ 不会。
- **现状 bug**:`extractSingleBubble` 分类顺序 call→snap→audio→image→**3b aspect-ratio 占位图兜底**→text。视频无 img,落到 3b `div[style*="aspect-ratio"]` 命中 → `querySelector("img")=null` → 返回 `type:"image"` 且 `imgUrl:""` 的**空图**(误判,不是漏)。
- **改动**(`packages/app-ins-scripts/src/snapchat/methods/getChatMessages.js`):① 新增 `type:"video"` 分支(`bubble.querySelectorAll("video")` → `videoUrl`+`resourceUrl`=src,支持多个),**放在 image 分支前**避免被空图兜底吞;② base64 白名单 `L1382` 加 `video`(复用 4 并发 + 8s 超时,大视频超时则 base64=null 只留 type+url);③ 文件头 type 注释补 video(Doc-first)。选 A 方案(和 image 一致转 base64)。
- **结果**:node --check OK;`node --test` 7 passed(现有,未新增)。
- **测试缺口**:`extractSingleBubble` 是 IIFE 闭包未导出,包里无 DOM 测试环境(没 jsdom),分类逻辑暂无自动化测试 → follow-up。
- **未部署**:仅改源码,未 commit/push/upload(等用户指示)。
