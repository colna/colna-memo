---
title: 2026-09-17
date: 2026-09-17
tags: daily
---

# 2026-09-17

## 今日重点

> 从 09-16 的 Daily 带过来（当天 50+ 条工作日志，全部实质内容，无归档）。

- [ ] luka **真机业务验证**：真机链路已打通，业务主流程（登录 / 划卡 / 聊天）还没在真机上回归
- [ ] **真机复测 09-16 晚的整批改动**：Discover 分页追加式 + 2/3 预取 + 菊花、性能收尾（写盘 debounce / prefetch 统一 / 阈值 O(1)）、首次切 Feed 窗口化、切回 Nearby 的 `rewindOnPause`、胶囊 tab 驱动；亲密度主页三段动作（播完回待机 / 连按重播 / Android 静态图换姿势）
- [ ] 聊天键盘跟随改动（`087d00e3`）真机看一眼上升/收起/连点
- [ ] Chats 两条待真机确认：白卡底部不再被标签栏遮（20:58）、爪印分割线居中（20:59）
- [ ] **工作区大量未提交等发话 push**：discover 全批（~20 文件）、pet 主页、Chats 两条、`pnpm-workspace.yaml` 钉版（store-review 57.0.1）、iris/blueprint debug 门禁改动等
- [ ] `fix/luka-code-review` 一批提交（P0 修复 / 死代码清理 / 评审整改）合回 `feature/luka-ios`：等主工作区在途改动落定 → merge → 解冲突（预期 `app/_layout.tsx`、`app/dev/_layout.tsx`、`dev/index.tsx`、`lib/dev-gate.ts`）
- [ ] Luka 提审阻断修复批（16:46，未提交）真机 + 产品侧：`lib/legal.ts` 客服邮箱占位（建议 `support@lukasoc.com`）、FAQ story/stories 模板残留、Live chat「~2 min」但只是 toast
- [ ] iris / blueprint 同款 debug 路由门禁洞（dev 布局无守卫、flags 无构建级判断）—— 待确认是否同批同步
- [ ] AI Code Review：过滤规则把 workflow 脚本排除导致模型看不到接线（误判死代码）—— 是否放宽待定；失败路径「评论 ❌ 但 job 绿」是否改红待拍板
- [ ] 性能审计 **P1/P2 待排**（BlurView 6/8 层减层需真机过目、每消息 3 RPC + SecureStore、chat 消息缓存无界、启动侧字重/Splash/串行 await、Android R8…），见项目笔记
- [ ] edit profile 剩余屏 `photos`（mockup `82`）；Nearby 列表模式仓鼠（稿上有，要加说一声）
- [ ] 未决设计点：photos 单图换头像 vs 多图相册；interests 平铺 vs 分组；AI tip 留不留
- [ ] `template-migrations` 落后 26 条（`appliedMigration` 停在 0009，blueprint 已到 0035）
- [ ] 远端已合并分支清理（`feat/luka-edit-profile` 等）
- [ ] review 账号（`review@lukasoc.com` / `metareview@lukasoc.com`）后端预置资料 —— 09-14 遗留，后端稍后就绪

## 工作日志

## 工作日志 — 10:40 前一天（09-16）整理

- **沉淀**：无新增遗漏 —— 09-16 各会话声称的沉淀复查都在（[[luka-tab-bar-clearance]]、[[rn-infinite-scroll-pagination]]「坑六」、[[muskapi-image2-relay]]、[[ios-appid-personal-team-conflict]]、[[ios-simulator-keyboard-and-taps]]）；当天新增的 3 份排查笔记（tab 栏让位 / 瀑布流分页 / 模拟器键盘与点击）内容完整。
- **带进今日**：16 条待办，已写进上方「今日重点」，重点是「真机复测 09-16 晚整批改动」与「工作区大量未提交待发话」。
- **项目进展**：`luka-rn-app` 已在 09-16 晚由各会话补到当天最新（瀑布流分页 / 性能收尾 / 首次切换卡 / 亲密度主页 / P0-P2 审计）；本次补一条「聊天键盘跟随 `087d00e3`」与 Chats 两条小修（白卡底部让位 / 爪印分割线）。
- **归档**：无 —— 09-16 的 50+ 条日志都是实质内容（跨 sitin-rn / sitin-next / figma-write-bridge / AI review 四线），原文保留。

## 工作日志 — 10:45 fix/luka-code-review 一批 Vercel RN 修复（4 项）

- **做了什么**（worktree `/Users/colna/WORK/sitin-rn-fix-luka`，分支 `fix/luka-code-review`，按要求未 commit）：
  - `list-performance-virtualize`：`apps/luka/src/app/credits/history.tsx` 从 `ScrollView` + 全量 `map` 改 `SectionList` 虚拟化 —— sections 由 `groupByDate`（`useMemo`）映射成 `{ date, data }`；分组头沿用 `DateHeading`，`stickySectionHeadersEnabled={false}` 保持原滚动行为；行间分割线与 `mt-5` 间距样式不变；`HistoryRow` 加 `memo`。
  - `react-state-minimize`：`apps/luka/src/stores/notifications.ts` 删掉派生字段 `totalBadge`（= scene + intoYou + messages），改导出纯 selector `selectTotalBadge(state)`；`hooks/use-app-badge.ts` 与 `services/widget-sync.ts` 改读 selector；新增 `tests/stores/notifications.test.ts`（3 例：partial setBadges / 求和 / clear）。
  - `react-state-minimize`：`apps/luka/src/stores/boost.ts` 删 `active`，等价语义由 `remainingTime > 0` 派生（原「先 active=true 后补时间」在本仓没有反例：三个 activate 调用点都保证 remaining > 0）；`services/boost.ts` 的 timer / recover / fake 三处读点同步。
  - `design-system-compound-components`：`components/ui/button.tsx` 删除 `children` / `renderContent` / `inkLabel` 死分支（全仓 0 处 `<Button>…</Button>`），只留 `label` 与 `loading` 路径；`components/ui/index.ts` 删除 `Button as PrimaryButton` 别名（0 importer）。
- **验证**：`pnpm --filter luka test` 126 文件 / 718 tests 全绿（基线 125/715 + 新增 3）；`pnpm exec biome check` 9 个改动文件 clean。
- **卡点（未越界修）**：`pnpm --filter luka typecheck` 只剩一条既有错误 `tests/stores/intimacy.test.ts(6,18) TS2352`，由本分支上一提交 `bff4c31a` 引入；清空本次改动后复跑同样报错，确认与本次无关。
- **坑**：该 worktree 同时有另一会话在改（`package.json` / `sign-in.tsx` / `manage-subscription.tsx` / `bond-*.tsx` 出现非本次改动）；我 `git stash push -u` 时把这些在途改动一起 stash 了，`pop` 已原样恢复。原因与规避见 [[git-parallel-sessions]]（stash 是全仓库共享栈），此后本会话不再使用裸 stash。
- **未做**：`components/ui/index.ts` barrel 全仓 0 importer，但按指示保留文件；`hooks/use-notifications.ts` 按要求未碰。

## 工作日志 — 10:46 fix/luka-code-review 另一批 Vercel RN 修复（7 项 / 17 文件）

- **做了什么**（同 worktree，按要求未 commit；与上一条是同一份「Vercel RN skill 审查整改」的另一半，两批文件不重叠）：
  - `js-hoist-intl`：`manage-subscription.tsx` 的 `Intl.DateTimeFormat` 提为模块级 `DATE_FORMAT`；`bond-rules-highlight/bond-progress-rail/bond-record-hero.tsx` 的 6 处 `.toLocaleString()` 改模块级 `NUMBER_FORMAT = new Intl.NumberFormat("en-US")` 的 `.format()`（值均为整数，输出一致；`bond-rules-highlight` 第三处 `value?.toLocaleString()` 改用已被 `isUnlock` 收窄的 `threshold`）。
  - `rendering-no-falsy-and`：`sign-in.tsx` 的 `{error && <Text/>}` 改 `error ? … : null`（`error: string | null`，空串会渲染空文本节点）。
  - `ui-pressable`：`photo-well.tsx` / `voice-recorder.tsx` 的 gorhom `TouchableOpacity` 换 RNGH `Pressable`。**坑**：Uniwind 只重写 RN 自家组件的 `className`（见 `packages/rn-image/README.md` §106），RNGH 组件上会被静默丢弃 → 原 className 落成等价内联 style；并用 `style={({pressed}) => …}` 复刻 TouchableOpacity 默认 0.2 按压变暗。
  - `ui-expo-image`：5 个文件的 RN `Image` 换 `@heyhru/rn-image`（photo-well / match-reveal / pwa-recall-invite 带 `blurRadius` / picture-tile / coin-emblem / sizes-step，共 6 文件）；`resizeMode` 原样保留（rn-image 兼容该 prop）。`sizes-step` 是普通 `Image` 不是 `Animated.Image`，故正常转换。
  - `ui-expo-image` 预取：`use-discovery-feed.ts` 的 `ReactNativeImage.prefetch` 换 `prefetchImages()`，本地 fixture 过滤保留，注释同步改写。
  - `ui-styling`：`candidate-card.tsx` / `gift-sheet.tsx` / `low-sparks-sheet.tsx` 的圆角补 `borderCurve: "continuous"`。
  - 依赖：`apps/luka/package.json` 补 `"expo-font": "catalog:"`（koda/lumi 已有，catalog 里是 `~57.0.1`）。
- **验证**：`pnpm --filter luka test` 126 文件 / 718 tests 全绿（基线 125/715，另有同 worktree 会话新增 `tests/stores/notifications.test.ts` 3 例）；`biome check` 17 个改动文件 clean；`pnpm boundaries` OK（41 surfaces，无身份泄漏）。
- **卡点（未越界修）**：`pnpm --filter luka typecheck` 剩 3 条错误、均非本批文件 —— ① `tests/stores/intimacy.test.ts(6,18) TS2352`（HEAD `bff4c31a` 既有：`0 as ChemistryBadge["plan"]`，而 `Plan = "a"`）；②③ `app/(tabs)/scene.tsx` 的 `onlineByPeer` / `online`（同 worktree 另一会话在途，属禁改清单）。
- **返工点**：`rounded-full` → 内联 style 时先写成 `borderRadius: 9999`，被 `tests/design/shape-language.test.ts` 拦下（迁移屏内联圆角白名单），改 `borderRadius: size / 2`（该规则明确放行的画圆表达式）后转绿。
- **边界**：worktree 有另一会话并发改动（[[git-parallel-sessions]] 已记 stash 事故）；本批只动点名文件，未 commit、未 push，也未碰对方在途文件。

## 工作日志 — 10:45 Luka Chats：会话行防抖（连点不再叠两层聊天页）

用户：「从 chat list 进入聊天页没有防抖，会出现点多次跳两次的情况」。

- **根因**：`app/(tabs)/index.tsx` 的会话行 `onPress` 直接 `router.push`；过渡约 350ms 内列表仍在屏上、行还能按 —— 连点两下就是两次 push，同一会话叠两层（返回要退两次，点击埋点也重复）。
- **改法**：`OPEN_CHAT_DEBOUNCE_MS = 600` + `lastOpenChatAt` ref + `allowChatNavigation()`；会话行 `onPress` 第一行过闸，覆盖两条入口（官方会话直推 `/team`、普通会话走 `openChat`）。提交 `342744cf`（path-scoped：只带这一个文件、且只含我的改动 —— 工作区里并行会话的 tab 间距改动按用户拍板保持未提交）。
- **验证**（模拟器 iPhone 17 / iOS 26.5）：双击 Chloe / Elena（间隔 ~130ms）→ 进入**单层**聊天页；返回一次回到列表（旧行为会停在第二层聊天页）；单点 Holly 正常导航（窗口外放行）。`pnpm luka:typecheck` 0 / biome 0 / 705 tests passed。
- **push 受阻**：pre-push typecheck 挂在并行会话**正在编辑**的 `app/pet.tsx` + `components/pet/pet-interaction-button.tsx`（10:44 刚落盘，pet.tsx 还没接 `enabled` prop）—— 不绕过 hook，等那边落定后 `git push`。
- **顺带**：为让缺 `ExpoStoreReview` 的旧 `.dev` 包能跑，临时改过一次 `services/good-review.ts`（requireOptionalNativeModule），验证后已还原、无残留。
- **踩坑**：模拟器窗口位置当天变过（(230,57)→(263,54)），点击前要重取窗口 `{position,size}` 并按「截窗口找设备屏边界」重标定；窗口不在前台时 CGEvent 点击不生效（先 `activate Simulator`）。详见 [[ios-simulator-keyboard-and-taps]]。

## 工作日志 — 10:52 Luka 亲密度主页：按钮去掉额度 + 摇一摇随机播动作

接昨晚那批（pet 页重做，未提交）。用户真机截图反馈两点：「点完还可以重复点击，不要点完就置灰」+「加入摇一摇，可以随机播放一个动作」。

- **按钮去额度**（用户拍板）：`stores/pet.ts` 的 `interact` 不再查 `petInteractionAvailability`，每次都记录、返回 `void`；`components/pet/pet-interaction-button.tsx` 删 `enabled` 与暗态；`app/pet.tsx` 去掉 `allowed` 三连。顺手删掉 store 里已无人用的 `usePetInteractionAllowed` / `usePetInteractionReason`（留着会让人以为还有门）。每日 3 次 / 60s 冷却规则仍留在 `@heyhru/business-pet`，blueprint 继续用。
- **摇一摇**：新增 `hooks/use-shake.ts`（合加速度 > 2.2G 触发、1.2s 冷却、只在页面聚焦时订阅、回调走 ref 不重订）+ `lib/pet-action.ts` 的 `pickPetAction`（**避开上一次那段**，`random` 可注入）+ 3 条单测；`app/pet.tsx` 摇到就调 `run()`，与按钮同一条路（记录 + 手感 + 动画）。
- **新原生依赖 `expo-sensors ~57.0.2`**（catalog + luka package.json；lock 解析到 `57.0.3`）。它在缺模块的旧基包里 **import 期就抛**，所以 `use-shake.ts` 走运行时 require + try/catch（Skia 教训）—— **旧基包上摇一摇静默失效、重出基包后才生效**（已写进 assets/pet/README.md）。
- **文档**：`assets/pet/README.md`「亲密度主页」补「按钮没有额度」「摇一摇」两条。
- **验证**：`pnpm --filter luka typecheck` 0 / biome 0 error / **122 文件 708 tests 全绿**（并行会话已还原 `good-review.ts` 的 TEMP）/ `pnpm boundaries` OK。
- **备注**：10:45 另一会话 push 曾被我这批未落定的 pet 文件卡住（pre-push typecheck），现已一致；工作区仍未提交（用户 09-16 晚选「先不提交」）。

## 工作日志 — 11:05 Luka 亲密度主页：接入 boring / jump / run 三段新动作（六颗按钮）

用户把三个素材直接放进了 `assets/pet/`（`boring.mov` / `jump.mov` / `run.mov`）并要求「把这几个动作也加入到 luka 页面中」，拍板：**两排按钮共六颗**、Boring 文案改 **Daydream**。

- **素材是 ProRes 4444 母版，不是产物**（604 MB / 104 MB / 207 MB，1440²/1080²）——按 `pet/README.md` 的流水线压成 320² HEVC-with-alpha：
  - `boring`（1440²/24fps/10.04s）：并集包围盒 1192²，**四角带 alpha ≤ 6 的软阴影 → 必须 `--alpha-floor 24`**（同 play-mac / fly 的坑）→ 1.4 MB。
  - `jump`（1080²/30fps/6.0s）→ 0.9 MB；`run`（1440²/24fps/8.04s）→ 1.2 MB。三段 alpha 都经 AVFoundation 复核。
  - **母版已移出仓库** → `/Users/colna/WORK/luka-pet-masters/{boring,jump,run}-master.mov`（与 heard/lie-down/play-mac 同处）。
- **代码**：`lib/pet-action.ts` 拆出 `PetInteractionKind`（feed/play/hug，写数据）与 `PetExtraKind`（daydream/jump/run，只播动画）+ `isPetInteraction`；按钮表改成 `PET_ACTION_ROWS`（两排三颗，上排互动、下排纯动画）；`pet-action-clip.ts` 加三段的视频映射与 Android 近似静图（curious / pointing / coach）；`assets/pet/index{,.ios}.ts` 加三个导出；`app/pet.tsx` 的 `run` 只对正式互动调 `interact()`，poke 拆成独立回调，按钮按行渲染。
- **摇一摇**：池子从 3 段变 6 段（`pickPetAction` 自动跳过上一次那段），逻辑不变。
- **文档**：`assets/pet/README.md` 的「亲密度主页」改写动作分组、三段的压缩命令与 Android 近似姿势说明。
- **验证**：biome 0 error / `pnpm --filter luka typecheck` 0 / **707 tests passed**（唯一挂的 `good-review.test.ts` 仍是并行会话刚重新加回的 TEMP `requireOptionalNativeModule("expo")`，与本批无关）/ `pnpm boundaries` OK。
- **待真机确认**：六颗按钮的版面（两排是否挤压小崽/按钮区）、换动作时小崽大小不跳。

## 工作日志 — 11:20 Luka 亲密度主页：七段素材「仓鼠大小」归一（脚钉同一条线）

用户真机反馈「每个动作的视频中的仓鼠大小不一致」，要求调成一样。

- **量法**：`ffmpeg` 解不出 HEVC 的 alpha（辅助层丢掉），新写 AVFoundation 探针
  `scripts/measure-pet-alpha.swift`（与 `verify-pet-alpha.swift` 同路）逐帧算 alpha ≥ 24 的
  包围盒。以待机的 √(逐帧面积中位数) 为 1：eat 0.944 / play-phone 0.952 / heard 0.992 /
  boring 0.999 / **jump 0.821** / run 0.934。jump 最小是结构性的：它的并集包围盒必须把
  腾空那一跃框进去，落地姿势就只好缩着。
- **为什么不在文件里补**：把 jump 的内容放大 1.22× 会超出画布 → 腾空那一帧会被裁头。
  所以修在舞台层：`PET_CLIP_LAYOUTS`（每段 `scale` + 内容底边 `bottom`），`pet-stage.ios.tsx`
  用 transform（`translateY` 写在 `scale` 左边 → 平移在缩放前坐标系里）把每段的脚**钉回待机
  那条线**（267.6pt / 270 方框），并允许溢出方框（`jump` 的腾空高度本来就超过它）。
  只缩放不钉脚的话，放大多少就陷进地毯多少。
- **数值校验**：脚落点七段全部 = 267.6；缩放后逐帧高度中位数从 0.894~0.972 收到 0.939~1.010
  （±3%），jump 的落地姿势 0.797→0.972 与待机一致。
- **换素材流程**：README「尺寸归一」一节写清量法/公式/填表；Android 静态图是紧裁图，
  `contain` 后高度天然一致，不需要这张表。
- **验证**：biome 0 error / `pnpm --filter luka typecheck` 0 / 707 tests passed / boundaries OK。

## 工作日志 — 12:05 Luka 亲密度主页：真机红屏根因 = 缺原生模块时 `try/catch` 是假兜底

用户连续两轮「一进 pet 页就报错」。先按「bundle 是否自洽」排查：从 Metro 拉整包做了静态链接检查
（自己写的小工具：逐模块比对「具名访问 vs 依赖导出表」），pet 页这条链**零缺失导出**；报错点名的
`petInteractionAvailability` 是 10:52 删掉的符号、且全包无裸引用 —— 一度判断是 Fast Refresh 残留。
用户后来按下 **Copy** 给出真栈，真因是这个：

```
Uncaught Error: Cannot find native module 'ExponentPedometer'
  <global> (expo-sensors/build/index.js:1)
  loadAccelerometer (use-shake.ts:58)      ← 明明包着 try/catch
```

- **根因**：Expo dev Metro 的 `guardedLoadModule`（`@expo/cli/build/metro-require/require.js`）
  对**首次**模块初始化异常是 `ErrorUtils.reportFatalError(e)`（红屏）**然后吞掉** ——
  调用方的 try/catch 只能拦「第二次 require」，红屏躲不掉。`expo-sensors` 的 barrel 第一行就
  `import ./Pedometer`，后者在模块作用域 `requireNativeModule('ExponentPedometer')`。
- **修法**：`use-shake.ts` 改成**先探测再 require** —— `requireOptionalNativeModule("ExponentAccelerometer")`
  （缺了返回 null 不抛）确认在，才 `require("expo-sensors")`。旧基包上摇一摇静默失效、页面照常。
- **顺带解释**：上一轮那条误导性的 `ReferenceError: Property 'petInteractionAvailability' doesn't exist`
  也出在同一机制上（模块 init 失败后 `exports = undefined`，后续依赖它的模块抛不相干的裸标识符错误）。
- **验证**：biome 0 error / typecheck 0 / **713 tests passed**。
- **沉淀**：[[expo-missing-native-module-guard]]（含正确姿势、模块名要查包源码、误导性红屏的识别）。
- **待用户**：Reload 后页面应正常；摇一摇要等重出基包（`pnpm luka:ios --device`），顺带补上
  `ExpoStoreReview`（另一会话还原临时改动后旧基包缺它）。
- **已提交**：用户说「push」，按 Luka 约定 path-scoped 提交并推送 —— `09b2f5bf`
  `feat(luka): 亲密度主页重做 —— 通栏房间场景 + 六段仓鼠动作 + 摇一摇`（33 文件 / +965 −209，
  含 `expo-sensors` 依赖与 catalog/lock、`scripts/measure-pet-alpha.swift`、煎蛋素材删除）；
  并行会话的 `nearby-list-pool.*` 与其余 20 个在途文件未动。pre-push（boundaries + 六个
  workspace typecheck）实跑全过。

## 工作日志 — 10:55 按 vercel-react-native-skills 修复全部（8 commit，worktree）

用户「修复全部」（针对上一轮该 skill 的审查清单）。继续在 `sitin-rn-fix-luka` worktree（`fix/luka-code-review`），主工作区另一会话 53 个在途文件全部避开。

- **提交**（8 个，逻辑分组）：
  - `d2d4c6b1` 列表行 memo/回调稳定/分隔线提级（notifications、my-feed、waves、activity、scene/calls、inbox、people；顺手抽出 `ThreadRow`、`VisitorRowItem`）
  - `db08adc2` RN Image ×6 → `@heyhru/rn-image`；卡组预取 `ReactNativeImage.prefetch` → `prefetchImages`（两条缓存不通、同一张图下载两次）；TouchableOpacity ×2 → RNGH Pressable；3 处补 `borderCurve: continuous`
  - `0f7afa6b` 键盘避让改 transform：scaffold/edit-profile×4 底部 CTA → `KeyboardStickyView`，scaffold eyebrow 收起改 scaleY+opacity+固定占位，sign-in 收起改 translateY；`ui/bottom-sheet` 的 iOS spacer 作为「measure 内容高度」例外保留并注明；顺带 sign-in `error &&` → 三元
  - `3409815e` `totalBadge`/`boost.active` 出 store 改派生（含 widget-sync 调用点与新测试）；Intl formatter 提模块级（manage-subscription + bond 三件）；Button 删 children 死分支与 0 importer 的 PrimaryButton 别名；`apps/luka` 补 `expo-font` 声明
  - `a6d241ce` credits/history 改 SectionList 虚拟化
  - `5a1fe5da` onboarding 草稿改 `draft ?? store`（identify/looking-for/drink/smoke）；age 步读 store（新增 `DEFAULT_ONBOARDING_AGE` 区分未选过）——修掉「返回年龄步跳回 28、继续则把 28 写进账号」
  - `bb36981a` 修正 intimacy 测试 Plan 字面量类型（我上一批引入的 TS2352，`0 as "a"` → `"a"`）
  - `21989085` `_layout` 删 3 个零引用字重；新增 `docs/performance-conventions.md`
- **验证**：typecheck 0、**126 文件 / 718 tests**、biome 884 文件 1 既存 warning、`pnpm boundaries` OK。
- **未做（及原因）**：feed/nearby/discover 的行 memo 与 renderItem 回调（主工作区在途文件，合并后补）；masonry 虚拟化（需支持 masonry 的虚拟化器 + 曝光语义回归）；JS 自绘 sheet → 原生 formSheet（gift-sheet 有已记录的 Portal 坑、需模拟器验证）；54 文件的 SafeAreaView sweep（视觉回归面）；列表缩略图（需后端字段/CDN 参数）；字体 config plugin（跨 app、不可 OTA、需单独评估）。
- **状态**：主工作区仍 53 dirty（另一会话在推 pet/键盘/防抖批次），未合入；预期合并冲突仅 `use-discovery-feed.ts`（我在途改了预取那两行）。

## 工作日志 — 10:58 对照飞书《iOS 常见拒审原因与规避手册》复核 Luka + 产出 findings 文档

用户给飞书手册链接，要求对照 Luka 再查一遍问题并出一个「问题 + 解决方案」的 md。

- **取数**：`lark-cli docs +fetch`（张峥 profile，token needs_refresh 但读取正常），手册九节全文拿到：iPad / 2.1 材料 / 5.1.1(ii) / SIWA / 订阅条款 / 5.6 / 5.1.2(i) / 2.5.4 + 4.3(b) / 统一检查表。
- **相对 09-16 审查的新发现**（已逐条代码核对）：
  - **P0**：手册第六节明文禁止「review 账号分类 / 固定设备 ID / 审核期专用逻辑 / 隐藏入口进生产包 / 绕内购」——Luka 命中前三条（`lib/review-mode.ts:18-41`、`use-fast-login.ts:65-74`），且 DTC 站外卡支付与手册「数字商品只走 IAP」冲突（`app.config.ts:369`）。三条都需产品决策。
  - **P1**：SIWA 是自绘胶囊（`login.tsx:28-29`）且失败静默（:57-61）；提审材料（账号/入口/英文备注/全流程录屏）未成文；隐私申报 Crash Data 填报口径。
  - **Pass 确认**：权限全部按需（通知在 onboarding 末步 `summary.tsx:98`）；取消订阅走系统入口（`manage-subscription.tsx:13`）；全仓无 `UIBackgroundModes`。
- **产出**：`apps/luka/docs/app-store-review-findings.md`（P0/P1/P2 摘要 + 手册九节逐项对照 + Luka 版提审检查表 + 外部依赖清单），未提交。
- **备注**：主工作区今天有并行会话在途（pet 摇一摇 / fix-luka 8 commit worktree），本文档独立新增，无冲突。

## 工作日志 — 11:33 sitin-next release/test-pwa 重置为 feature/pwa 并重合 snapchat 分支

用户：「release/test-pwa 重置为和 feature/pwa 一致，然后把 feat/pwa-open-snapchat-live-savvy-android 再合入 release/test-pwa，然后 push」。

- **备份**：重置前 `release/test-pwa`（aa9df14fc，比 feature/pwa 多 ~70 提交：beauty-live 一批 + 昨天 14:45 的旧合并）在 origin 无备份 → 建并推 `backup/test-pwa-before-reset-20260917`（沿用仓库 backup/test-pwa-before-reset-* 惯例）。
- **重置**：`git checkout release/test-pwa && git reset --hard origin/feature/pwa`（75f06f4b3，含 #1157 回拨弹窗批次）。
- **合并**：`git merge origin/feat/pwa-open-snapchat-live-savvy-android` 无冲突（5 文件，条件 `isHavenPwa || isSavvyAndroid` —— 与昨天 release/test-pwa 侧的同源改动互不重叠，feature 线历史已含它上次 merge 的痕迹）。merge commit `706b272a5`。
- **push 阻断与修复**：`git push --force-with-lease` 被 husky pre-push（`pnpm test/build --affected`）拦下 —— 失败是本地 node_modules 陈旧：`@heyhru/business-minerva-digital-human` 找不到 workspace 包 `@heyhru/server-ext-proto-gateway`（仓库里有、只是没 link），与本次改动无关。`pnpm install --frozen-lockfile` 后该包 test 通过，重跑 pre-push 65 tasks 全过。
- **结果**：`aa9df14fc...706b272a5 release/test-pwa -> release/test-pwa (forced update)`。`business-pwa-proto` 子模块指针仍脏（工作区在 21c01821，分支记 cb7ca84b）—— 与操作前一致，未动。

## 工作日志 — 12:15 Luka 会话页进场延迟（真机 iPhone X，dev 包）

用户报「点击会话行 1–2 秒才进聊天页」（真机、稳定复现；模拟器复现不出）。

**排查法**（写进了 [[rn-device-perf-measurement-via-metro-logs]]）：
- Metro 会把设备端 `console.log` 结构化写进 `apps/luka/.expo/dev/logs/start.log`（`metro:client_log` + `_t` 毫秒时间戳）——不插线就能读真机日志；
- 在 `onPress`/首帧/commit/数据落地/`transitionEnd` 五点打时间戳，把一次进场拆成毫秒级账单；
- 触摸事件 `e.nativeEvent.timestamp`（ms，boot 基准）与 `Date.now()` 的差值，跨多次点击取最小值为基线 → 量出「手指 → JS 处理」延迟（3–63ms，排除 JS 忙）。

**真机账单**：按下→首帧 29–139ms；→首屏 commit 87–338ms；→消息进 store 291–486ms（冷会话等 `getMessageList`）；→`settled`（transitionEnd）总 1.05–1.7s。模拟器同流程 ~0.7s —— 差距 = iPhone X + dev 包（React dev 模式 / 未压缩 / 每条网络日志走 WS 到 Metro）。

**改法**（提交 `b8d68623`，已 push 到 `feature/luka-ios`）：
1. **消息预取**：Chats 列表空闲时给前 10 个会话 `loadMessages`（新 `lib/chat-prefetch` 纯函数 + `hooks/use-chat-prefetch`，失焦即停、500/300ms 一笔），点进去内容已在本地 → commit→消息 291–486ms → **10–15ms**；
2. **本地快照直渲**：`loading` 不再只认本轮 `page[id]`，`messages[id]` 有快照即视为已就绪（`loadMessages` 照旧后台刷新）；
3. **首屏减负**：`initialNumToRender=6 / maxToRenderPerBatch=6 / windowSize=11`；悬浮小崽（两段 alpha-HEVC 播放器）挪到 `settled` 后再挂。

**试过但无效果/否决**：仓鼠视频 pause（A/B 无变化）、`animation: "none"`（页面瞬现但用户体感仍慢，用户要求还原）、时间戳基线法之外的「关动画」方案已回滚。

结果：整体 1.05–1.7s → ~0.8–1.0s；加了 6 条单测（`pnpm luka:test` 716 passed）。用户暂接受现状（余下是 iPhone X + dev 包开销），后续可发 EAS preview(release) 包对比。

**顺带踩坑**：① Biome 的 JSX 注释里出现反引号会 parse error（`expected ',' …`），中文注释别写 `` ` ``；② iPhone X(iOS 16) + Xcode 26 想用 Instruments：`xcdevice` 说 available 但 `xctrace` 栏里 offline（iOS 16 设备要解锁 + 直连 USB；无线配对不行），本次没采成。
