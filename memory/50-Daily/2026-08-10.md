---
title: 2026-08-10
date: 2026-08-10
tags: daily
---

# 2026-08-10

## 今日重点

- [x] 完成 colna-memo 同步并核对远端、工作树与本地索引状态。

## 工作日志

> 按 `HH:MM` 追加,最新在下。

### 11:04 完成 colna-memo 同步

- **做了什么**:初始化北京时间 2026-08-10 Daily,并执行 `colna sync`。
- **为什么**:按知识库每日规则补齐当天记录,同时把本地 Memo 与远端保持一致。
- **结果**:`colna sync` 成功完成本地提交、`git pull --rebase --autostash`、索引增量更新和 SSH push;本次重新嵌入 68 个内容块,`main` 与 `origin/main` 已对齐。源码未提交改动仍由 autostash 恢复在工作树中。
- **关键命令 / diff**:`HF_ENDPOINT=https://hf-mirror.com COLNA_PROFILE=release ./colna sync`;新增 `memory/50-Daily/2026-08-10.md`。
- **沉淀**:同步期间源码脏工作树可由 `--autostash` 安全保留;Memo 提交与源码改动保持隔离。

### 13:59 — social-post 修复「开始抓取」点击无反应

**做了什么**:`social-post/apps/web/src/pages/index.tsx` 抓取弹窗 `ModalForm` 加 `dateFormatter={false}`。commit `42ffdf7`,直推 main(colna 身份)。

**为什么 / 根因**:点「开始抓取」无反应、Network 无请求,控制台报 `TypeError: e.range[0].startOf is not a function`。根因是 `ProForm/ModalForm` 默认 `dateFormatter="string"`,提交时把 `ProFormDateRangePicker` 的 dayjs 值转成字符串 `'2026-08-03'`;`onFinish` 里对字符串调 `.startOf('day').unix()` 抛错,中断在 `api.crawl()` 之前 → 请求根本没发出。

**修法**:`dateFormatter={false}` 保留原始 dayjs 值,与 `onFinish` 里 `.startOf('day').unix()` 用法匹配。type-check 通过(tsc exit 0)。

**坑(可复用)**:antd ProComponents 的 `ProForm`/`ModalForm`/`QueryFilter` 默认 `dateFormatter="string"`,日期类字段(DatePicker/DateRangePicker/TimePicker)提交到 `onFinish` 时**已被转成字符串**,不是 dayjs。若 `onFinish` 里要调 dayjs 方法(`.startOf`/`.unix`/`.format`),要么设 `dateFormatter={false}` 保留 dayjs,要么用 `dayjs(v)` 重新包一层。

### 14:05 — social-post 启动「FB 端到端 + 表格筛选」增量(创造模式)

**背景/调研**:social-post 三端(crawler/server/web)架构已完全平台无关 —— crawler 是 registry 模式(`app/registry.py` 注册各平台 crawler)、server `/crawl/{platform}` 全透传无 IG 硬编码、`prisma/seed.ts` 里 `facebook` 已存在只是 `enabled:false`、web 菜单/表格/抓取弹窗全按 `platform` state 泛化。**所以真正工作量 = crawler 的 FB 抓取适配器**(核心难点)+ 表格筛选(独立)。

**FB 抓取方案(与用户确认)**:探测 mbasic / m.facebook / www 三入口**无 cookie 全 400/302 跳登录** → 必须带已登录 fb cookie(机制同 IG `IG_COOKIE`,将加 `FB_COOKIE` 不进仓库)。数据源定 **mbasic.facebook.com HTML + cookie**(复用 `fetcher.fetch_text` curl_cffi;parser 抠资料+帖子)。备选 Graph API(任意公开页拿不到帖子)/内部 GraphQL(fb_dtsg 最脆)均不采用。用户会提供一个可用 cookie 用于抓 fixture 写准 parser。

**做了什么(Phase A 表格筛选,已 merge 到 social-post main)**:
- 新增 `apps/web/src/utils/filter.ts` 纯函数 `filterAccounts`(按 handle 子串 / 展示名子串 / 最近抓取时间段筛选,忽略大小写、null 展示名不误命中、时间段排除从未抓取),配 7 条 Vitest 单测。
- `apps/web/src/pages/index.tsx`:`search={false}` → `search={{labelWidth:'auto'}}`,`lastCrawledAt` 列 `valueType:'dateRange'`+`search.transform`,follower/media 列 `hideInSearch`,request 内调 `filterAccounts` 前端过滤。IG/FB 共用同一张表,后端零改动。
- 新增 `docs/06-facebook-and-filter.md` 增量规划(PRD/技术/拆解/进度)。
- 全绿:tsc 0 / eslint 0 / 11 tests passed。commit `17d865d`(docs)+ `e69fefa`(filter),已 push。

**残留(Phase B/C 待 cookie)**:FB parser/crawler/注册、`config.fb_cookie`+`.env.example`、seed 启用 facebook、端到端 + 视觉走查。

**同今日另一坑**:MetaBot 长驻会话下多 bot 并发写 colna-memo,`colna sync` 会撞 add/add 冲突(今天 Daily 已撞一次),记 memo 前先 `git pull --rebase` 再写。

### 15:30 — social-post Facebook 端到端适配器(commit 026346c)

**做了什么**:给 social-post 加 Facebook 抓取(与 IG 一致展示),三端架构本就平台无关,只动 crawler + seed:
- `services/crawler/app/parsers/facebook.py`:解析 www 主页(资料 + `fb_dtsg/lsd/doc_id/userID/variables` 上下文 + 内嵌初始帖)与 `ProfileCometTimelineFeedQuery` 流式响应(carousel/video/image 三类,字段用递归按 key 搜索而非写死深路径)。
- `services/crawler/app/crawlers/facebook.py`:GET 主页 → 尽力 GraphQL cursor 翻页;FB 软封(`error 1357054`)时优雅降级为仅内嵌帖不报错;注册进 `registry.py`。
- `fetcher.fetch_post_text`:curl_cffi(`impersonate=chrome`)POST。`config.fb_cookie` + `.env.example` `FB_COOKIE`;`seed.ts` facebook `enabled:true`。
- 测试:crawler 32 passed(新增 parser 9 + API facebook 2)。fixture 用真实抓到的结构合成。

**FB 抓取硬核结论(踩坑,已抽到 troubleshooting)**:
1. mbasic 已死(带 cookie 也返回错误页)。
2. 桌面 www GET 稳但只内嵌约 1 帖;粉丝/帖子数懒加载不在初始页。
3. 多帖靠 GraphQL:从页面 `...RelayPreloader` 抠 `doc_id=27002668536074808` + variables(按 `userID` 翻页)。**关键**:urllib 发被拒(`1357054`,Python TLS 指纹),换 **curl/curl_cffi(impersonate chrome+HTTP2)** 成功拿 542KB/多帖/cursor;**但**连发 1~2 次即软封整个会话(全新 token 也 `1357054`)。稳定多帖需 会话/代理轮换 或 无头浏览器+拟人节流,本期不做。

**环境坑**:本机 homebrew Python 3.14.6 坏了(`pyexpat` 符号缺失,pip/ensurepip 全崩)→ 用 `uv venv --python 3.12` 建干净环境跑 pytest。以后本工作区跑 Python 测试都走 `uv`。

**残留**:
- [ ] FB live 稳定多帖(需代理/无头浏览器,已评估搁置)
- [ ] C.3 视觉走查(待用户本地 `prisma migrate/seed` + web 点 Facebook 验证)
- [ ] 前端「开始抓取」修复(`dateFormatter={false}`,commit 42ffdf7)+ 表格筛选(e69fefa)本次一并交付
