---
title: 2026-07-02
date: 2026-07-02
tags: daily
---

# 2026-07-02

## 今日重点

- [ ] shangtang-sdk-demo 项目:license 到期前(2026-07-26)找许佳续期
- [ ] shangtang-sdk-demo:验证 `npm install` + `npm run dev` 是否跑通,9 类 SDK 是否正常加载
- [ ] app-pwa 商汤美颜集成:等产品拉齐 P0 效果清单 + 商汤商务续期/OSS 商谈,再决定立项
- [ ] 【follow-up 2026-06-30】sitin-next 后端 `app-social-proxy-server` 里指向 src 不存在脚本的死映射清理(单独开 PR)

## 工作日志

### HH:MM — sitin-next 脚本错误日志:RangePicker 加快捷预设 + 允许清空

- **做了什么**:在分支 `personal/zz/script-errors-export` 改
  `packages/app-minerva-web/src/pages/SocialProxy/ScriptErrors/index.tsx`:
  1. 新增 `rangePresets`(`TimeRangePickerProps['presets']`):近一天 / 近7天 / 近30天 / 近3个月 / 近1年,value 用函数返回保证相对当前时间。
  2. RangePicker 加 `presets={rangePresets}`。
  3. `allowClear={false}` → `allowClear`;`dateRange` state 类型 `[Dayjs, Dayjs]` → `[Dayjs, Dayjs] | null`;`onChange` 清空时 set null。
- **为什么**:用户要时间筛选有快捷区间选项,且能叉掉不选(不选=查全部)。
- **结果**:`queryFilter` 早已用 `dateRange?.[0]?.toISOString()`,清空后 startDate/endDate=undefined=查全部,无需额外改。`npx eslint` 该文件 0 error(仅 2 个遗留 legacy 警告 max-lines/complexity,非本次引入)。未 commit/push(按项目规则等用户)。
- **沉淀**:
  - 有 RangePicker 的是 `personal/zz/script-errors-export` 分支;另一个 export 分支 `personal/zz/sp-script-error-csv-export` 里该文件还是迁移基线版(无 RangePicker)。
  - antd v5 RangePicker `presets` 用 `{ label, value: () => [dayjs...] }` 函数式,避免模块加载时固定时间。
- **提交/推送**:commit `e22a2723`,`git push --no-verify`(用户授权)推到 `origin/personal/zz/script-errors-export`。pre-push 全量 `pnpm test` 因私有 Nexus 连不上挂掉(与本改动无关),详见 [sitin-next-push-prepush](../30-Resources/troubleshooting/sitin-next-push-prepush.md)。
- **merge 到 test-admin**:`release/test-admin` 先快进到 origin,再 `merge --no-ff personal/zz/script-errors-export`,带过去 3 个 commit(presets/可清空、current-online→gateway、日期筛选+CSV 导出)。冲突仅 `ScriptErrors/index.tsx`(test-admin 已有导出旧版),取新版解决。merge commit `31a70cfc`,`--no-verify` 推送成功。
  - 坑:merge commit 的自定义 `-m` 消息会过 commitlint,`merge:` 不是合法 type → 用 `chore(scope): ...` 才过。默认 "Merge branch" 消息才被 commitlint 忽略。

### HH:MM — 上线申请规范落到项目 CLAUDE.md + 整理 2026-07-01

- **上线申请规范**:发现这次发飞书上线申请格式不对(用了 `--text` 且缺结构化字段、用错 app)。真源在 [feishu-release-request-format](../30-Resources/feishu-release-request-format.md);按用户要求把要点摘写进项目 `CLAUDE.md`「飞书侧使用规则 → 飞书上线申请规范」(必须 profile `colna-unrestricted`/app `cli_a948f5747e3b9ccc`、`--markdown`、open_id per-app、先给用户过目再发、字段模板、常用群 chat_id)。真源补了「SP后端内部群」`oc_713670054e6236e6074902693bec3595`。
- **整理 2026-07-01**(每日整理):昨天「今日重点」4 条已在今天(无需再搬);全是实质工作日志,无垃圾归档。沉淀 3 处:
  1. 新建 [pwa-mobile-gesture-media](../30-Resources/troubleshooting/pwa-mobile-gesture-media.md) —— 移动端长按手势 + 录音/媒体(async event 回收、setPointerCapture 竞态、AudioContext suspended、img 长按菜单、hook 卸载 cleanup 等 7+ 条)。
  2. 新建 [sitin-next-testing-and-merge](../30-Resources/troubleshooting/sitin-next-testing-and-merge.md) —— app-social-proxy-server 用 Jest 非 vitest;功能分支往落后 release 分支 merge 会带无关历史;拆/移 commit 的 rebase --onto 手法。
  3. 追加到 [sitin-online-stats-redis-online](../30-Resources/troubleshooting/sitin-online-stats-redis-online.md) —— prod「当前在线 0」的跨服务 Redis 不一致定性自测手法 + gateway/devices 产品级绕开修法 + admin 分支部署映射。

### HH:MM — 语音录制「录制中」态对齐 Figma(app-pwa)

- **背景**:分支 `personal/zz/pwa-chat-bottombar`,对 Figma 节点 `4139-13572`(S1·录制中)与 `ChatVoiceRecorder.tsx` 逐项比对。结论:结构/间距/尺寸/颜色基本一致(输入行 gap 10/pad 16、pill 44/圆角/pad 14/gap 8/#EBF5FF、mic 18、chevron 16、波形条宽 2、侧滑 target 40+图标 18+纵向 gap 12 全对齐)。
- **改动**(commit `3fdd68ec`,`style(app-pwa)`,只改 `ChatVoiceRecorder.tsx`):
  1. 蓝色 pill 阴影 `rgba(16,152,250,0.2)` → `0.04`(idle/recording/locked 三态统一)。
  2. 计时字号 `14px` → `15px`(recording + locked 两处;Figma 15/600)。
- **未改(之前用户明确要的有意偏离)**:波形条距 `gap-px`(Figma 2px)、侧滑无 `#F0F0F5@55%` 胶囊底。
- **结果**:eslint 0 error。`--no-verify` push(pre-push 全量 test 卡内网私有 registry,与本改动无关)。
- **figma token**:MCP 旧 token 过期(403),换用户给的新 token 写进 `~/.claude.json`;杀掉仍握旧 token 的 MCP 进程(旧 token 已从系统清除),本次比对用 curl 直连绕过 MCP;下次用 figma MCP 需 `/mcp` 重连。

### 05:20 — 「探真」PhotoTask 拍照验证抽屉(app-pwa,新分支 personal/zz/pwa-verify-drawer)

- **背景**:PR #499(bottombar)已合入。从最新 `personal/zz/sitin4`(@ `99cfad72`)切新分支 `personal/zz/pwa-verify-drawer` 实现「探真」任务底部抽屉,对 4 个 Figma 状态节点(`4139-11129/11645/11469/11294`)。
- **调研结论(app-pwa 底层已齐备)**:
  - 抽屉容器复用 `components/ModalContainer.tsx` 的 `variant="bottom-sheet"`(自带遮罩/下滑关闭/进出场动画),骨架抄 `ChatDetail/GiftList.tsx`。
  - OSS 直传:`utils/ossUpload.ts` `uploadToOss({file, fileType: FileType.IMAGE})` → `cred.cdnUrl`;审核:`http/chatModerationApi.ts` `auditImage({userId, imageUrl, targetId})` → `{passed, violationCategory}`,`ViolationCategory.NO_HUMAN_FACE=5 / NOT_FEMALE=6`。**注意**:该 proto(`archat_api/chat_api`)后端尚未落地,方法是「wired ahead」,真机接入要等 `chat_api.proto` + `pnpm proto:gen`。
  - 触发点:`MessageType.RequestSelfie` 已存在但 `MessageItem.tsx` 只渲染占位;`ChatDetail/index.tsx` 的 `handlePickImage` 还是 TODO。这些是下一轮接入点。
  - Tailwind v4,token 在 `styles/index.css @theme`;dev 预览页照 `ChatInputBarPreview` + router `/dev/*` 模式加。
  - 相机:全仓库无 `<input capture>`,要前置相机跳相册需新增 `capture="user"`。
- **改动(commit `3a116a6a`,`feat(app-pwa)`,`--no-verify` push 到新分支)**:
  1. 新建 `pages/ChatDetail/PhotoTaskDrawer.tsx` —— 状态机 `idle→ready→reviewing→failed`,4 态对齐 Figma;图标全内联 SVG(无新增二进制)。**上传+审核抽成注入函数 `onVerify(file)=>{passed,cdnUrl?,reasons?}`**,UI 与后端解耦。相机用 `<input accept="image/*" capture="user">`。
  2. 新建 `pages/PhotoTaskPreview/index.tsx` + router `/dev/photo-task` —— mock 审核可切通过/失败,点通全流程。
- **决策**:本轮范围=抽屉+预览页(用户选);② 待发送的绿色 chip 当**静态装饰**(本地拿不到性别,真实校验由服务端 auditImage 在点 Send 后兜);ChatDetail 实时接入延后(等任务数据源:倒计时到期时间/¢1.20 奖励/taskId 走哪个后端字段)。
- **结果**:ESLint 0 error(踩 `max-nested-callbacks`:`.then` 和 setInterval→setRemaining 各触发 4 层嵌套 → 改 await + 提 `decrement` useCallback)、tsc 干净(仅预存在 baseUrl 弃用警告)。未做真机/浏览器 visual 校验。

### 04:12 — ChatInputBar / 语音录制 API 与交互对齐(app-pwa,分支 personal/zz/pwa-chat-bottombar)

一次会话内做了 5 个 commit,全在 `personal/zz/pwa-chat-bottombar`,只碰 `ChatInputBar.tsx` / `ChatVoiceRecorder.tsx` 及两个调用页:

1. **`1e3b25c9` refactor**:发送回调统一成 `onSendText / onSendVoice / onSendImage / onSendGift`(原 `onPickImage`→`onSendImage`、`onGiftClick`→`onSendGift`)。同步改 `ChatDetail` / `ChatInputBarPreview` 两个调用方。
   - 设计讨论结论:发送类回调**按类型分开**而非收束成一个 `onSend(union)` —— 因为宿主本来就要按类型分叉,分开时「调哪个函数=已分类」,零 switch、签名最紧。收束只适合三类共享一条发送流水线的场景。
2. **`57bf535b` fix**(后被回滚):锁定手势阈值 `LOCK 56→88`、`CANCEL 112→144`。根因:zone 用「相对按下点的上移距离 dy」判定,而锁图标是绝对固定位置,56px 太小,拇指自然上移就误触 lock。
3. **`717e317e` refactor**:`freeVoiceCount` → `rewardMultiplier`(「×N」是**奖励金倍数**不是免费次数,也不是纯金额);同一 commit 把阈值**回滚**回 56/112(用户要先改回)。注意 bff/dashboard 里的 `rewardAmount`(真实金额)是另一码事,没动。
4. **`317d2d1d` fix**:too-short toast 对齐 Figma(节点 `4139-13798`)。原来 `left-4` 贴最左、无尖角/无阴影/padding 错。改成居中语音条上方 + 朝下小尖角(CSS border 三角 16×8)+ 阴影 `0 6px 16px rgba(0,0,0,.22)` + padding 14/8。关键:给录音根容器加 `relative`,让 toast / SlideChannel / LockedControls 三个 `absolute bottom-full left-1/2` 浮层从「锚整条 bar」改为「锚语音条居中」,与 Figma tail 对准 mic 一致。
5. **`79fc8e51` fix**:逻辑冲突——1800ms 未到时再次 hold,toast 不消失。在 `handlePointerDown` 开头 `clearTimeout + setShowTooShort(false)` 立即清掉。

- **推送**:每次都 `git push --no-verify`。pre-push 全量 `turbo run test` 卡在 `@heyhru/business-minerva-upgrade` 缺私有依赖 `@presence-io/datatester`(本地 `node_modules/@presence-io/` 不存在),与本改动无关,用户授权绕过。参见 [sitin-next-push-prepush](../30-Resources/troubleshooting/sitin-next-push-prepush.md)。
- **沉淀**:锁定手势的「阈值锚点漂移」问题(dy 锚在可变按下点、图标却是绝对位置)已记入本条;彻底修法是方案2(zone 用指针绝对 Y 对比图标真实屏幕区域),本次仅先回滚阈值待真机验证。

### 03:00 — archat.us 官网(Philomuse)按飞书文档改版,产出单文件 HTML

- **背景**:用户要按飞书 wiki `PiwFwHhcRi34Zpkr835cWz7jnlC` 改 `https://www.archat.us/` 官网,产出该网站的 HTML 文件。官网是一个自包含 `index.html`(暗色 + 橙色 #ff6b35,内联 CSS/JS,资源在 `./assets/`)。
- **改动**(在 scratchpad 改,非本工作区代码库):
  1. 左上角 logo `Philomuse - Presence` → `Philomuse`(删 Presence);`<title>` 同步。
  2. header 新增 `About ▾` 下拉(纯 CSS + 小段 JS toggle),含 About Us / Careers 两项,锚点跳转、点外部收起。
  3. 新增 `#about-us` 区块:公司介绍段 + "Our culture values Intellectual Honesty / Hiring the Best / Seeking Excellence";把原独立的 "What We Do"(三张卡片)整体移入 About Us。
  4. 新增 `#careers` 区块:Join Us + `IT Support Technician (Malaysia)` 折叠卡(accordion,+ 号旋转成 ×),展开 Job Description / Required Skills & Qualifications,结尾 recent graduates 一句。
  5. footer `@Philomuse Limited 2024-2025` → `2022-2026`。
- **图片内联**:用户追加"把图片也放进来" → python base64 把 6 个资源(favicon/presence-logo/bg1/1/2/3)全部内联成 data URI,替换所有 `./assets/xxx` 引用,产出完全独立的单文件(约 1.2MB),双击即显示,不再依赖 assets 目录。
- **验证**:puppeteer-core + 系统 Chrome 截图三处(header 下拉 / About Us / Careers 展开)+ 无 assets 目录环境验证内联图片正常。
- **结果**:两版都发到 OUTPUTS_DIR(最终为内联单文件 `index.html`)。未涉及任何仓库提交。
