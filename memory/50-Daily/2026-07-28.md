---
title: 2026-07-28
date: 2026-07-28
tags: [daily]
---

# 2026-07-28

## 今日重点
- face-to-face(FaceForge)创造模式构建 loop

## 工作日志 — 面对面换脸平台(FaceForge)Phase 0-2

**项目**:`/Users/max/Dev2/zhangzheng/face-to-face`(github colna/face-to-face),创造模式,「一直 loop 直到实现」。

**做了什么**
- **Phase 0 文档先行**:PRD / 技术架构 / 任务拆解 / 任务进度 + README + .gitignore。选型 FaceFusion 3.6(图/视频,hyperswap_1a_256+CodeFormer+遮挡蒙版)+ Deep-Live-Cam(实时);monorepo = apps/web + services/api + services/face-engine。
- **Phase 1 脚手架(T1.1-T1.5)**:pnpm+turbo 基座;Next.js15+React19+Tailwind+Vitest(web);FastAPI+/health(api);Python 包(face-engine);GPU Dockerfile+compose(🖥️只写文件)。
- **Phase 2 T2.1**:engine/schemas.py pydantic 模型(质量/人脸/任务状态)+ 8 条 TDD 单测。

**坑**
1. **NODE_ENV=production(用户全局 shell)** → React 走生产构建、`React.act` 缺失,vitest 报 `React.act is not a function`。修:web `test` 脚本前置 `NODE_ENV=test`。
2. **py3.9 + pydantic v2** 运行期无法 eval PEP604 `int | None` → 必须用 `typing.Optional`,ruff 忽略 FA100。

**约束**:沙箱无 GPU/docker → 换脸真实推理、GPU 镜像构建标 🖥️,只保证代码 + mock 单测,真机验证留证。

**每 Task 闭环**:先写测试 → pytest/ruff/mypy 或 vitest/tsc/build 全绿 → 更新 docs/任务进度.md → 单 commit + push。已推 7 个 commit。
