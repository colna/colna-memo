---
title: 2026-08-06
date: 2026-08-06
tags: daily
---

# 2026-08-06

## 今日重点

- [ ] 与端确认 Snapchat 的 `checkSocialProxyPageAbnormal` 端能力是否已上线(本次前端已改,依赖端返回有效 type 才生效)。
- [ ] toProfessionalAccount 完成阶段时序缝隙:点「继续」后 waitFor 只等按钮消失就返回、资料表单未渲染时多点一次「完成」;待用户确认第 2 个「完成」后走向再改为 `waitFor(reachedProfileForm())`。
- [ ] `#pepBio` 判据线上实测坐实(到达资料表单=账号已是专业号)。
- [ ] `sitin-next` 分支 `personal/zz/sp-snapchat` 的 stash@{0}(clickChatThread 统一扫描修 CLICK_FAILED)未提交,需要时切回 pop。

## 工作日志

### 00:20 sitin4.1 review PR#824 + 修 1 个业务遗漏(未提交)

- **review 范围**:PR#824(SP V2 多社媒,23 手写文件 +1602/-480)完单/授权/登录态/入口链路逐行审。
- **🔴 业务遗漏(已修)**:`handleAcceptExchange`(女被动接受)发给男方的回执 `sendPayload.accountID/avatarUrl` **硬编码 IG `insId/insAvatar`**,没按订单 `cardType` 取名片——而它 `:560` 已经拿到 cardType。后果:女方接受 **Snapchat** 订单时,发给男方的回执带的是女方 **IG 账号**(错的联系方式)。女主动 `initiateInsExchange` 早修过这点(注释「否则 snapchat 卡会带 IG 账号发出去」用 `getContactCard({cardType})`),女被动漏了。**修法**:对齐女主动,加 `const ownCard = await getContactCard({cardType})`,`accountID: ownCard?.accountId || insId`、`avatarUrl: ownCard?.avatarUrl || insAvatar`。
- **🟡 代码重复(已改)**:`handleAcceptExchange` 内联 `platformLoggedIn`(用 store0)与模块级 `isPlatformLoggedIn` 重复,删内联复用模块级。
- **观察(未改,产品确认)**:① `App.tsx` authBlock 逻辑已多社媒(`igUsable||scUsable`)但枚举名 `ig-unbound/ig-login-lost` 是 IG 命名遗留;② `AiGoldCashoutCard:66`/`Settings:31` 判绑定仍用 `insId`(IG),SC 有无对应管理入口待产品定。
- **已核对 OK**:`InsExchangeBubble` 手动接受传了 cardType(`:322`);完单按平台 gate、授权判据统一、两状态解耦、入口分流均对。
- **验证**:tsc/eslint 干净。未 commit。
- **未深审**:bridge 配置、`useInsBridgeCallbacks` 端回调、`showSocialAuthDrawer` 只扫未逐行。

### 23:45 sitin4.1 授权入口按状态分流:未授权→大抽屉/授权过→直接登录 webview(commit 1f173b776)

- **规格(用户定)**:小抽屉 `showSocialAuthDrawer` **只在系统检测到登录态丢失时弹**(`checkSocialProxyPageAbnormal` 冷启动探测 / `openSocialLogin` 端回调);用户主动点授权入口按钮时按状态分流——**未授权→大抽屉 `showInsModal`(区分社媒)**,**授权过(点按钮=登录态失效要重登)→直接调起 `openSocialProxyWebView` 登录 webview,不弹小抽屉**。
- **端能力确认**:`openSocialProxyWebView(platform)`(`bridge.ts`,`invokeBridge("openSocialProxyWebview",{platform,url:loginUrl,webView})`)已有,按平台加载登录页;IG 有 legacy `openInsWebView` 兜底,**SC 依赖端新方法上线**(同 checkSocialProxyPageAbnormal)。
- **改动**:`showInsModal.tsx` 新增 `authorizeOrLogin(platform, source)`——`authByPlatform[p]===true?openSocialProxyWebView(platform):showInsModal(source,{platform})`。三入口改调它:任务 `useTask:599/603`、workspace `AiGoldCashoutCard:249/503`、chat 拦截 `AIPersonaPlaceholder:97`。`tryStartInsRobot:496` 的 showInsModal 是"拉小崽发现未授权"自动分支(`!authed→大抽屉`),符合规格保留。小抽屉触发链未动(已符合)。
- **验证**:tsc/eslint 干净;pre-commit/pre-push(54/54)过。commit `1f173b776` 已推 origin。
- **胶囊已确认**:就是 `AiGoldCashoutCard.tsx` `PausedCard`(「AI earnings paused」收益暂停卡)右上角「Authorize IG ›」pill,授权点击 `:249` 正是已改的 `authorizeOrLogin`,四类入口(任务/workspace 卡片/胶囊/chat 拦截)全覆盖。**端能力 `openSocialProxyWebview` 用户已确认上线**。

### 23:10 sitin4.1 收口:未授权/登录态失效两状态完全解耦(commit 9f535cc09)

- **背景**:审视"未授权(not_connected)"与"登录态失效(expired)"是否解耦,发现字段层已分(authByPlatform/loggedInByPlatform/abnormalByPlatform 三独立 map),但读写侧 3 处没收干净。
- **关键纠错**:`resetInsState` 实际**只被 `useUserInit` 登出/切用户调**(不是 insStore 注释说的"授权关弹窗"),所以登出**就该清授权态、且全平台清**——我上个 commit(64b925f90)改成"不清 authByPlatform.instagram"方向反了,本次纠正。
- **收口 3 处**:
  - **A** `showInsModal.tsx`:IG 授权判据 `platform==="instagram"?insState:authByPlatform` → 统一 `authByPlatform[platform]===true`(去 `useIns`/`insState`,去 unused import)。
  - **B** `insStore.resetInsState`:改回**登出全清**——`authByPlatform/loggedInByPlatform/abnormalByPlatform` 三 map 全清 `{}` + 兼容字段;更新过期类型注释。冷启动由 hasContactCard(授权)+ checkInsAbnormal(登录/异常)按新用户重填。
  - **C** `checkInsAbnormal` SC 分支:补 `setPlatformLoggedIn("snapchat", scNormal)`(原只写 abnormal),和 IG 分支对齐,否则 SC 冷启动探测在线但 loggedIn 仍 false→完单 gate 误判未登录。
  - `docs/ig状态一致性.md`:补两状态解耦判据表 + resetInsState 登出全清说明。
- **验证**:tsc/eslint 干净;pre-commit(lint+circular)、pre-push(build/test 54/54)过。commit `9f535cc09` 已推 origin。
- **结果**:授权=authByPlatform(hasContactCard 回填/finishSocialBind 置/登出清)、登录=loggedInByPlatform(startRobot/探测/回调,IG+SC 都写)、异常=abnormalByPlatform,三者独立字段+独立数据源、互不污染;判"授权过"全局统一 authByPlatform,无 insState 漂移。两状态完全解耦。

### 22:40 sitin4.1 CE 完单/自动接受按订单平台判登录(方案 B,commit be1b79d8d)

- **背景**:接续授权判据统一,处理"自动完单只按 IG(isInsLoggedIn)、纯 SC 不触发"。发现障碍:订单 `InsExchangeOrderInfo` 前端 proto 版本(b23ea527)没 cardType,拿不到订单平台→`checkPendingOrders` msg 没填 cardType→`handleAcceptExchange` 的按平台判全默认 IG 失效;`handlePeerAccepted`(follow 单)完全没按平台判。**用户 push 了 gen 更新(`5b985ac5f`,InsExchangeOrderInfo 加 `card_type=13`)**,pull + `pnpm build` proto 后数据地基就绪。
- **交换弹窗确认**:`:268` else 弹的是 `InsExchangeModal.tsx`「Social Requests」(Figma 7268:969),已支持多社媒混合(IG Jennifer + SC Ashley 各 Accept)。故用户选**方案 B(保留弹窗)**。
- **改动**(`useInsTaskInit.ts` 6 处 + doc):
  - `checkPendingOrders`:msg 填 `cardType: item.order?.cardType`。
  - `handlePeerAccepted`:加 `cardType` 参数 + 关单前 `isPlatformLoggedIn(cardTypeToPlatform(cardType))` gate,未登录 `tryStartInsRobot(platform)` 拉小崽、**不关单**;startRobot 带 cardType。抽模块级 `isPlatformLoggedIn`(IG=isInsLoggedIn/其它=loggedInByPlatform)。
  - 实时 `ExchangeSendMessage`:去 `isInsLoggedIn` 一刀切,下放 `handlePeerAccepted` 内按订单平台判,传 `payloadData.cardType`。
  - `checkPendingOrders` 入口:任一涉及平台在线才自动 dispatch,否则弹 Social Requests(方案 B)。
  - 订阅:监听各平台 `loggedInByPlatform` false→true 重拉,不只 IG。
  - `docs/ig状态一致性.md` 补完单按平台判说明。
- **坑**:① commitlint `subject-case` 拦——subject 不能以大写英文词(CE)开头,改中文开头过。② 又遇 proto dist 过期,pull gen 后 `pnpm --filter @heyhru/business-pwa-proto build` 重建。
- **验证**:tsc/eslint 干净;pre-commit(lint+circular)、pre-push(build/test 54/54)过。commit `be1b79d8d` 已推 origin。
- **效果**:accept 单(handleAcceptExchange)+ follow 单(handlePeerAccepted)+ 三处入口全部按订单平台判登录再关单;纯 SC 在线也能自动接受;对应平台没起来则拉对应小崽/弹 Social Requests,不再空关单或被 IG 挡。至此多社媒自动完单链对齐完成。

### 21:30 sitin4.1 CE 授权判据统一到后端名片 hasContactCard(commit 64b925f90)

- **背景**:排查 CE 自动完单是否判社媒在线时,发现 IG/SC「是否授权过」口径不一致——IG 真源是后端 `insId`(`authByPlatform.instagram`=insState 会被 `resetInsState` 清、不可靠),SC 是前端 `authByPlatform.snapchat`。用户定:统一用 `hasContactCard`(有名片=授权过)判,两平台同口径。飞书文档《免费交换接口文档-端上对接》v3.1(wiki HFaxwtn8...)确认:`hasContactCard`(Proto 21008)`user_id` 传谁查谁、双端通用(文档写"男端"是业务描述,传自己 userId 即查自己);另有 `getContactCards`(21006 双端)。
- **改动**(分支 `personal/zz/sitin4.1`,app-pwa):
  - `http/userApi.ts`:新增 `hasContactCard(cardType, userId?)` 封装(HasContactCardRequest/Response,返回 `hasCard`)。
  - `hooks/useInsTaskInit.ts` `checkInsAbnormal`:冷启动并行 `hasContactCard(IG/SC, 自己userId)` → `setPlatformAuth(platform, hasCard)` 回填 `authByPlatform`(权威授权态);随后按回填后的 authByPlatform 决定探测。
  - `hooks/useInsTaskInit.ts` `tryStartInsRobot`:IG 判据 `!!insId||insState` → 统一 `authByPlatform[platform]===true`。
  - `stores/insStore.ts` `resetInsState`:**不再清 `authByPlatform.instagram`**(授权过以后端名片为准,不该被关弹窗清;insState 兼容态仍清)。
  - `docs/ig状态一致性.md`:补授权态数据源=hasContactCard 说明。
- **未改**:`finishSocialBind` 已 `setPlatformAuth(platform,true)`(授权成功即等价 hasCard),不额外调。
- **验证**:改动 3 文件 tsc/eslint 干净。**坑**:切分支后 `business-pwa-proto` dist 又过期(报 TaskType/ConversationState/HasContactCard 类错),`pnpm --filter @heyhru/business-pwa-proto build` 重建后消;剩 `web-util-media`(GiftBubble/ChatFooter)dist 过期属其他包环境问题,pre-push turbo 会先建。未 commit(规范模式,待确认)。
- **残留**:① `setInsState` 仍镜像 `authByPlatform.instagram`,但冷启动 hasContactCard 覆写纠正,可接受;② 入口 gate(`:198/:268/:428`)仍用 `isInsLoggedIn`(IG)当自动完单总闸——纯 SC 在线不触发,是另一处待改;③ 待与后端确认 `getContactCards`/`hasContactCard` 不传 userId 是否 Gateway 注入自己(本次显式传自己 userId 规避)。
- **已 push**:commit `64b925f90` → `origin/personal/zz/sitin4.1`,pre-commit(lint+circular 559)、pre-push(build/test 54/54)通过。

### 17:50 sitin4.1 FE-1.3 冷启动补 Snapchat 掉线探测 + 探测降级非 IG 隔离(commit 094b8382c)

- **背景**:排查授权抽屉两类弹窗触发链路时发现缺口——冷启动 `checkInsAbnormal` 只探 IG(`checkSocialProxyPageAbnormal("leohalm")` 固定 IG),SC 断连只能等运行时 `openSocialLogin` 回调才标记,进页那刻 SC 掉线不会显示 expired。知识库 08-03 FE-1.3 笔记印证这是当时有意只接 IG 的已知缺口。
- **目标链路(用户定)**:冷启后对每个已授权社媒各调一次 `checkSocialProxyPageAbnormal` 判在线;存在不在线则弹抽屉,不在线的+未授权的都展示,只有正常在线的不展示;运行时 `openSocialLogin` 回调不在线那个,弹抽屉规则同上。对照现状:展示逻辑(`showSocialAuthDrawerFromStore`)、运行时回调已符合,唯一 gap = 冷启动只探 IG。
- **改动**(`packages/app-pwa`,分支 `personal/zz/sitin4.1`):
  - `hooks/useInsTaskInit.ts` `checkInsAbnormal`:按已授权平台并行探 IG+SC(`Promise.all`),IG 走 `setInsPageAbnormal`(兼 `abnormalByPlatform.instagram`+`isInsLoggedIn`),SC 走 `setPlatformAbnormal("snapchat")`;探完统一弹一次;未授权平台不探;返回值沿用「IG 是否异常」语义。
  - `utils/bridge.ts` `checkSocialProxyPageAbnormal`:fallback 到 IG 专用 `checkInsPageAbnormal` 前加 `platform!=="instagram"` 隔离(非 IG 桥失败直接返回 `{code:-1,type:0}` 不判定),避免老 APK 拿 IG 态误判 SC。对齐同文件 `openSocialProxyWebView`/`startSocialProxyRobot` 的「非 IG 不降级」范式。
  - 同步 `docs/bridge-methods.md`、`docs/ig状态一致性.md`。
- **关键机制确认**:`isBridgeError` = code∈{-500,-501,-502,-503}(桥层保留错误码,方法不存在/桥不可用),code=0 是成功、业务态看 type → 能干净区分「老 APK 不认新方法」vs「新 APK 调成功但账号异常」,fallback 只在真桥失败触发。
- **坑**:① 本地单独 `tsc app-pwa` 报 4 处 `cardType` 错误(`useInsTaskInit:311`/`insApi:173`),非本次引入——是 `business-pwa-proto` 的 `dist` 编译产物过期(pull 带来新 `src/gen` 但 dist 没重建);`pnpm --filter @heyhru/business-pwa-proto build` 重建后 tsc 干净,且 pre-push 的 `turbo build --affected` 会先构建依赖故线上不挂。② 工作区 `business-pwa-proto/proto` submodule 长期停在旧指针(+1d23c),`git submodule update` 对齐到分支期望 `b23ea527`(commit 78c6de3b6)后 `proto M` 消失。
- **验证**:改动两文件 eslint 0、tsc 干净;pre-commit(lint+circular 559 modules)、pre-push(build/test --affected 54/54)通过。commit `094b8382c` 已推 `origin/personal/zz/sitin4.1`。
- **残留**:见今日重点——SC 端能力上线确认是运行前提(端没上线时最多 SC 不弹,不会误弹)。

### 18:40 sitin-next 新增 Snapchat getProfile 脚本(分支 personal/zz/sp-snapchat)

- **做了什么**:在 `packages/app-ins-scripts` 新增 SC 端 `getProfile` 方法,从 `accounts.snapchat.com/v2/welcome` 资料卡片提取 `avatar / username / display_name`。
  - 新增 `src/snapchat/methods/getProfile.js`:纯解析函数 `extractProfile(doc)` + 主方法 `SP.getProfile`;不在 welcome 页则 `location.href` 跳转并返回 `NAVIGATING`,在页则 `waitForAnyElement` 等卡片渲染后提取,返回 `utils.buildResult` JSON 串。挂 `SP.getProfile.extractProfile` 供单测。
  - `scripts/build.mjs`:`m:getProfile` 注册进 SC 的 `bootstrap` + `login` 两组(welcome 页实际加载的 bundle;login bundle 含 executeAction,可经 execute 调度)。
  - `scripts/upload-all.mjs`:`SC_SCRIPT_GROUP_MAP` 加 `getProfile`(bootstrap:3、login:4),IIFE 顺序相应后移。
  - `test/snapchat-getProfile.unit.test.js`:vm 加载 utils+getProfile,mock document/location,6 用例覆盖提取/缺字段/NAVIGATING/PROFILE_NOT_FOUND。
- **为什么**:用户需求——进 welcome 页拿当前登录账号头像/昵称/id。
- **关键定位依据**:welcome 页(样例 `dom/snapchat/welcome.html`)资料卡 class 是构建 hash,不可依赖;选择器复用 `checkLoginStatus` 已验证口径——`[data-testid="username"]`、`[class*="displayName"]`、`[data-testid="snapCodeImage"]`。**welcome 页全页只有 1 个 `<img>` 即 Snapcode(URL 带 `bitmoji=enable`),没有独立头像图,故 avatar 取该 Snapcode**。
- **结果**:`node scripts/build.mjs` 通过,getProfile 进入 `dist/snapchat/bootstrap.js`+`login.js`;`npm test` 8 passed。未 commit/push(规范模式)。
- **沉淀**:① `app-ins-scripts` 无 `lint` script → 不在 `turbo run lint` 门禁内,所有 snapchat IIFE 文件单独跑 eslint 都报 `window is not defined` 等同类错,属预期,不必强改。② 方法返回值经 `executeAction` 的 `reportToClient` 原样透传,故方法应返回 `buildResult/buildError` 的 **JSON 字符串**(getStory 经 pipeline 也是返回字符串)。③ 测试坑:vm 跨上下文对象原型 ≠ 宿主,`assert.deepEqual` 会因 prototype 判不等,比较前先 `JSON.parse(JSON.stringify())` 归一化;`buildError` 内部 `captureDomSnapshot` 会读 `document.body.innerHTML`/`document.title`,mock document 需补这些字段。

### 18:55 提交并推送 getProfile(commit b590a67e3)

- 承接上一条:`git add` 仅 4 个文件(getProfile.js/test/build.mjs/upload-all.mjs),排除切分支带来的无关 `business-pwa-proto/proto` submodule 指针。
- `git pull` → Already up to date;`pnpm lint` 51/51 过;commit(带 scope `feat(app-ins-scripts):`)→ pre-commit lint+circular 过;`git push origin personal/zz/sp-snapchat` → pre-push build/test --affected 53/53 过。
- 已推到 `origin/personal/zz/sp-snapchat`,commit `b590a67e3`。

### 19:15 getProfile 上报后跳回 web 主页(commit 19def2bb4)

- 需求:getProfile 上报完成后跳回 `https://www.snapchat.com/web`。
- 实现:成功分支 return 前 `setTimeout(()=>location.href=WEB_URL, 500)`。**关键坑**:不能在 return 前同步跳转——`executeAction` 是在方法 return 之后才 `reportToClient` 发结果给 native,同步整页刷新会销毁上下文、上报丢失;故延迟到上报发出后再跳。`returnToWeb:false` 可关(单测用)。仅成功跳,NAVIGATING/错误不跳。
- 验证:npm test 9 passed;pre-commit/pre-push 全过。commit `19def2bb4` 已推 `origin/personal/zz/sp-snapchat`。

### 19:45 getProfile 补进 automation 组 + 删测试(commit 476399b95)

- 现象:welcome 页 console `execute("getProfile",{})` 报 `UNKNOWN_ACTION`。根因两层:① 线上仍是旧部署 bundle(未 upload);② 更关键——getProfile 原来只加进 `bootstrap`/`login` 两组,**没加 `automation` 组**,若 app 注入的是 automation bundle 就找不到。
- 修法:`build.mjs` automation 组补 `m:getProfile`(executeAction 仍最后);`upload-all.mjs` SC map 的 getProfile 加 `{group:'automation',order:7}`、executeAction automation 顺延 8。三 bundle(bootstrap/login/automation)build 后均含 `SP.getProfile = async`。按用户要求删除 `test/snapchat-getProfile.unit.test.js`。
- 沉淀:welcome 页(accounts.snapchat.com/v2/welcome)能跑 `execute` 说明注入的是带 executeAction 的 bundle(login 或 automation),不是 bootstrap;新增可被 execute 调度的 SC 方法,**automation 组必须加**,只加 bootstrap/login 不够。线上生效需 `node scripts/upload-all.mjs --platform snapchat` 部署 + 刷新页面;即时验证可把方法 IIFE 粘进 console(复用页面已有 SP.utils/bridge)。
- commit `476399b95` 已推 `origin/personal/zz/sp-snapchat`。

### 20:30 social-post 修 Vercel web 构建失败 + seed 平台(commit 45a6fea)

- **现象**:线上 `social-post-web-two.vercel.app` 一直是旧样式(旧的 tailwind 卡片),不是最新 antd Pro(ProLayout/ProTable);`/api/platforms` 一度返回 `[]`。
- **根因链**:
  1. `[]` = 生产 Neon 库 `platform` 表没跑 seed(Vercel 部署只 `prisma generate`,不含 seed)。已从本地 clone `DATABASE_URL=<neon> pnpm --filter server prisma:seed` 幂等 upsert 写入 instagram/facebook,`/api/platforms` 恢复正常。
  2. 前端旧样式 = **Vercel web 构建一直失败**、持续服务上一版旧构建。报错 `Can't resolve '@ant-design/icons'`:antd Pro 的 `pro-descriptions` 直接 import 未声明的 `@ant-design/icons`,pnpm 严格 symlink 布局在 Vercel 全新安装解析不到(本地 node_modules 恰好可解析故本地 build 过)。仓库**没有 `.npmrc`**。
  3. 前端即使样式对,平台/账户仍空 = `UMI_APP_API_BASE` 没配,前端默认打自己域名 `/api`,又被 web `vercel.json` 全 rewrite 到 index.html → 拿到 HTML 而非 JSON。后端 CORS 已 `origin:true`,无需改。
- **修法**:新增根 `.npmrc` `shamefully-hoist=true`,把依赖提升到根 node_modules,Node/webpack 向上查找可解析 icons。`CI=true pnpm install` 重装(布局变更需清 node_modules,非交互加 CI=true)+ `pnpm --filter web build` 通过。commit `45a6fea` 已推 `colna/social-post` main。
- **残留(交用户在 Vercel 面板)**:① 确认 `social-post-web-two` 是否连了 main 自动部署(URL 带 -two,疑似重复项目);② 配 `UMI_APP_API_BASE=https://social-post-server.vercel.app/api` 重部署;③ seed 目前只有 instagram/facebook,无 snapchat。
- **沉淀**:pnpm + antd Pro Components 在 Vercel/CI 严格布局下,`pro-*` 子包会直接 import 未声明的 `@ant-design/icons` 等 → 加 `.npmrc` `shamefully-hoist=true`(或 `node-linker=hoisted`)。「本地 build 过≠CI 过」,严格隔离才暴露隐式 peer。

### 20:55 social-post 修前端 platform 前导斜杠 bug(commit cd39213)

- **现象**:新增账户/查账户列表报 `platform /instagram not available` 400。curl 复现:`{"platform":"/instagram"}`→400,`{"platform":"instagram"}`→201。
- **根因**:`apps/web/src/pages/index.tsx` ProLayout `menuItemRender` 回传的 `item.path` 带前导斜杠(`/instagram`),`onClick` 直接 `setPlatform(item.path)` 把 state 污染成 `/instagram`,再发给后端。首屏未点菜单时 state=first.key(干净)故初始可用,一点侧栏就坏。
- **修法(3 处)**:`setPlatform(item.path.replace(/^\//, ''))` 去斜杠让 state 恒为干净 key;`location.pathname` 与 `menuProps.selectedKeys` 改用 `'/'+platform` 保持侧栏高亮。`pnpm --filter web build` 通过。commit `cd39213` 推 `colna/social-post` main。
- **沉淀**:ProLayout 菜单 key 会被规范化加前导斜杠,别把 `item.path` 直接当业务 key 用;要 `.replace(/^\//,'')`。
- 备注:调试时已用正确参数 POST 真实创建了一条 instagram/colna_zheng 账户(201),UI 再加同名会撞唯一约束。

### 21:20 social-post crawl 链路排障:配置对、crawler 缺 curl_cffi(commit 31d2728)

- **现象**:`POST /api/accounts/:id/crawl` 返回 503 `crawler unreachable: Failed to parse URL from sp_7fK.../crawl/instagram`。
- **逐层定位**:
  1. `Failed to parse URL from sp_7fK...` = Vercel server 的 `CRAWLER_BASE_URL` 被误填成了 token(`sp_7fK...`),`fetch(token/crawl/instagram)` 非法 URL。用户改成 `https://social-post-nrry.onrender.com`(Render 上的 crawler)后此项修复。
  2. 核对 crawler(Render，Docker/Free)：`/health` 200;`POST /crawl/{platform}` 路由与 server 拼接一致;错 token→401、正确 token `sp_7fK...`→过鉴权,说明两边 token 一致。
  3. 正确 token 打 crawl → 502 `{"detail":"抓取上游失败: 抓取失败: No module named 'curl_cffi'"}`。根因:`app/fetcher.py` 默认走 `scrapling.fetchers.Fetcher.async_get(impersonate="chrome")`,底层依赖 `curl_cffi`;`requirements.txt` 只有 `scrapling>=0.2`,新版 scrapling 把 curl_cffi 拆成可选依赖未装。
- **修法**:`services/crawler/requirements.txt` 加 `curl_cffi>=0.7`。commit `31d2728` 推 main;Render 连 main 自动重建镜像。未能本地完整验证(需 build Docker+真抓 IG)。
- **沉淀**:社媒采集端到端排障顺序 = server 拼的 URL 是否合法(base url 别填成 token)→ crawler /health → 路由匹配 → token 边界(401 vs 通过)→ 抓取执行错误。scrapling 默认 Fetcher 需显式装 curl_cffi。Render 免费实例休眠+冷启动 30-60s,首请求易超时。
- 残留:下一坑可能是 IG_COOKIE 有效性 / Render Free 资源限制。CRAWLER_TOKEN(sp_7fK...)、Neon DATABASE_URL 已在会话明文,建议轮换。

### 22:10 social-post crawler 打通抓取:curl_cffi + user feed 接口(commit e7236fd)

- **接上条链**:token/URL/mode 都对后,crawl 仍 502 `No module named 'playwright'`。venv 实测(scrapling 0.2.99,卸掉 playwright)坐实:`from scrapling.fetchers import Fetcher` 在**导入阶段**就连锁加载 `scrapling.engines.camo → camoufox → playwright`,缺 playwright 直接 ModuleNotFoundError,**改 SCRAPLING_MODE=fetch 也没用**。
- **第二层坑**:用真实 cookie 抓 colna_zheng,`web_profile_info` 返回资料 OK(COLNA/粉丝4/media 3)但 `edge_owner_to_timeline_media.edges` 为空——该接口现在只给帖子 count、不给明细。改调 `/api/v1/feed/user/{user_id}/?count=N`(带 x-ig-app-id + cookie + curl_cffi impersonate=chrome)实测拿到全部 3 条(code/media_type/caption/like/cover)。
- **改动(4 文件,16 单测过)**:
  - `app/fetcher.py`：fetch 分支用 `curl_cffi.requests.AsyncSession(...).get(impersonate="chrome")` 直连,绕开 scrapling 导入;stealth 分支保留(仅真用时 lazy import)。
  - `app/crawlers/instagram.py`：web_profile_info 无 timeline 明细时回退调 user feed 接口(按 `account.external_id`)。回退式设计使旧 fixture 测试仍过。
  - `app/parsers/instagram.py`：新增 `parse_feed_items`(feed item 结构:media_type 1/2/8→image/video或reel/carousel、caption.text、like/comment、image_versions2 封面)。
  - `tests/test_parser_instagram.py`：加 feed 解析测试。
  - 依赖 `curl_cffi` 前一 commit 31d2728 已加。commit `e7236fd` 推 main。
- **沉淀(troubleshooting 备选)**：IG 采集端到端排障顺序 = server 拼 URL 合法性 → crawler /health → 路由 → token 边界 → 抓取执行；scrapling Fetcher 导入即拉 playwright,轻量场景直接用 curl_cffi;IG `web_profile_info` 只给资料+count,帖子明细要走 `feed/user/{id}`。
- **安全提醒(未落 cookie 明文)**：测试用的 IG 会话 cookie 已在会话明文出现(用户主号 77833594843),已提醒登出/改密;crawler IG_COOKIE 应用专用小号。CRAWLER_TOKEN(sp_7fK...)、Neon DATABASE_URL 亦建议轮换。

### 22:35 social-post crawler 抓取验证:代码 OK,porsche 是 IG 侧 400

- 部署后 crawl 依次报 401→400。排查:
  - 401 = Render `IG_COOKIE` 失效(带被拒 sessionid 反而 401);匿名反而 200(小号公开资料不需 cookie)。
  - 清 cookie 后抓 porsche 仍 400,本地也复现。抓原始 body:`{"message":"Asset asset://laser.provider/ig_business_category_subvertical has been deleted. You cannot use this schema","status":"fail"}` —— **IG 对 porsche 这个企业号自身返回 400(其 business category asset 被删),带不带 cookie 都 400**。nasa/instagram/colna_zheng 均 200。
  - 端到端验证:nasa(1.04 亿粉、media 4870)返回 posts 正常(carousel/reel/image + 赞 + caption)。crawler 代码完全正常。
- **结论/给用户**:crawler 已通;porsche 是 IG 侧坏号,换正常号即可;Render 需配**有效 IG_COOKIE**(大号需登录态,小号可匿名),建议专用小号非主号。
- 沉淀:IG `web_profile_info` 对个别企业号会返回 `laser.provider ... asset deleted` 的 400(IG bug,非爬虫问题);排查非 2xx 时应打印上游 body 才看得到真因(建议给 fetcher 的 FetchError 带上 body 片段——待用户确认再改)。本轮无代码改动。

### 22:55 social-post crawler 失败率诊断:IG web_profile_info 对商业号普遍 400

- 用户反馈失败率高。批量测(有效 cookie):zaijianmosaic/smartgroupuk/porsche/natgeo → 400 `asset://laser.provider/ig_business_category_subvertical has been deleted`;nike/nasa/instagram/colna_zheng → 200。**规律:商业/专业号(有 business category)必 400,个人号 OK。IG 接口自身 bug,非本项目问题。**
- 商业号替代路径(porsche 实测):① 主页 HTML 正则 `profilePage_(\d+)` 拿 user id 200 ✅ ② `feed/user/{id}/` 200 拿帖子 ✅ ③ mobile `users/{id}/info/` 返回 200 但 body `status:fail`(要 App 头,不可用);`i.instagram web_profile_info` 也 400。
- 资料字段缺口:商业号精确粉丝数拿不到。HTML `og:title`=显示名、`og:description`="33M 位粉丝、已关注 126 人、4,837 篇帖子"(粉丝缩写、关注/帖子精确、locale 依赖);嵌入 JSON 里 full_name/profile_pic 是**登录者(viewer)**的不是目标号。
- 待办(已向用户提方案待拍板):crawler 加回退——web_profile_info 400 时走 HTML(id)+og(基础资料)+feed(帖子),让商业号也能出帖子。本轮无代码改动。
- 沉淀候选(troubleshooting/instagram-web-scraping.md):web_profile_info 商业号 400 是 IG 侧 bug;帖子统一走 `feed/user/{id}`(id 从 HTML `profilePage_` 抠);mobile info 需 App UA。

### 23:20 social-post 实现商业号回退路径(commit bb22272)

- 承接上条诊断:web_profile_info 对商业号必 400。实现回退:
  - `app/fetcher.py`:抽 `_raw_get` 共用,新增 `fetch_text`(抓 HTML 文本);`fetch_json` 复用之。
  - `app/parsers/instagram.py`:新增 `parse_profile_html(html)` —— 正则 `profilePage_(\d+)` 抠 user id,og:title 取显示名/username,og:image 取头像,og:description 顺序取 followers/following/posts(`_abbrev_to_int` 处理 33M/4,837/1.2K 缩写)。
  - `app/crawlers/instagram.py`:web_profile_info 抛 FetchError(商业号 400)时 → `_fetch_via_html`(HTML 抠 id+资料)→ 再走 feed 拿帖子。个人号/大号仍走主路径。
  - `tests/test_parser_instagram.py`:加 parse_profile_html 测试。共 18 单测过。
- 实测(有效 cookie):smartgroupuk/porsche/natgeo(原全 400)现均返回 posts 3;nasa/colna_zheng 无回归。
- 局限:商业大号粉丝数近似(og 缩写 33M);显示名/关注/帖子数/帖子本身精确。
- commit `bb22272` 推 colna/social-post main。残留:Render 重部署 + 配有效 IG_COOKIE(专用小号)。

### 23:45 social-post feed 分页 + 侧栏样式修复(413ba09 / 24586c7)

- **分页(413ba09)**:用户反馈 smartgroupuk 共 761 帖只拉到 12。根因:IG feed 每页约 12 条、忽略更大 count,crawler 只拉一页。实测 `next_max_id`+`max_id` 可翻页(每页 more_available/next_max_id)。改 `app/crawlers/instagram.py` 加 `_fetch_feed_posts`:循环翻页直到 len>=max_posts 或 more_available=False/无 next_max_id;`FEED_PAGE_SIZE=12`。实测 maxPosts=30→30、=5→5(1请求)、colna_zheng(3帖)→3 不死循环。`tests/test_api.py` 加 2 个分页单测(共 20 过)。
- **样式(24586c7)**:`apps/web/src/pages/index.tsx` disabled 菜单项(Facebook)的「敬请期待」Tag 换行、与文字重叠。改成 flex 单行(justify space-between + whiteSpace nowrap + flexShrink0),Tag 做小(borderless/11px)。umi build 过。
- 均推 colna/social-post main。残留:Render 重部署 crawler、Vercel 重部署 web。
- 沉淀:IG feed 分页靠 `next_max_id`,单页固定约 12;抓 N 条需循环翻页。
