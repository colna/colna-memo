---
title: 2026-09-20
date: 2026-09-20
tags: daily
---

# 2026-09-20

## 今日重点

- [ ]

## 工作日志

### 工作日志 — 磁盘占用排查（系统数据 357G）

用户贴出「储存空间」截图（系统数据 357.59G，磁盘 404/460G 已用），问怎么清理。只做只读排查，未执行任何删除。

排查方法与结果（`du -sh` 逐层下钻，未用 sudo）：

- 用户 home 约 220G：`~/Library` 140G（Containers 34G / Developer 27G / Caches 24G / pnpm 23G / Application Support 23G）、`~/WORK` 39G、`~/TEST` 13G、`~/Pictures` 9.6G、`~/Downloads` 7.8G、`~/facefusion` 6.6G、`~/deep-live-cam` 3.9G。
- 系统级：`/Library/Developer/CoreSimulator` 19G（Volumes 16G 为 iOS 26.5 cryptex，不可删）、`/Applications` 约 30G。
- 高收益可清理项：`~/.cache/uv` 24G、微信容器 28G、Xcode DerivedData 11G、`/Library/Developer/CoreSimulator/Devices` 13G（3 台大设备）、`~/.android/avd` 11G（Pixel_9_Pro）、pnpm store v3 旧 major 8G、`~/.gradle/caches` 6.8G、`~/Library/Caches` 24G、`/Library/Developer/CoreSimulator/Caches` 3.0G、Android SDK system-images 3.9G。
- 注意点：`~/Library/Containers/com.docker.docker/Data/vms/0/data/Docker.raw` 逻辑 460G 但实际仅 15M（稀疏文件，没占空间）；无 Time Machine 本地快照（`tmutil listlocalsnapshots` 为空）；`~/Library/Application Support/MobileSync` 为空目录。
- 复用了 2026-09-09 的结论：看真实剩余空间要用 `df -h /System/Volumes/Data`（本次 404G used / 6.2G avail）。

结论已按「安全清理 / 需人工判断」两档整理给用户，等其选择后再执行。

### 工作日志 — 执行磁盘清理（可用空间 6.2G → 135G，腾出 ~129G）

用户批准执行「安全清理 + ~/Downloads + ~/facefusion + ~/deep-live-cam」，微信自行处理。

已执行：

- `uv cache clean` → 删 21.1GiB / 185 万个文件
- `rm -rf ~/Library/Developer/Xcode/DerivedData/*`（27G → 3.0G）
- `rm -rf ~/Library/pnpm/store/v3` + `pnpm store prune`（22G → 0B；pnpm 10 在 macOS 用 clonefile，prune 后 store 清空，但 node_modules 完好已验证）
- `rm -rf ~/.gradle/caches`（7.5G → 732M）
- `rm -rf ~/Library/Caches/*`（24G → 523M，残留 dotslash 521M 因权限没删掉）
- `xcrun simctl delete` 11 台模拟器，保留 `iPhone X (11 Pro eq)`（在用）
- 删 Android AVD `Pixel_9_Pro` + `~/Library/Android/sdk/system-images`
- 清空 `~/Downloads`（保留 `.env`、`.localized` 隐藏文件）
- 删 `~/facefusion`、`~/deep-live-cam`

权限不足跳过的（需 sudo，未强求）：`/Library/Developer/CoreSimulator/Caches`（~3G）、`/Library/Updates`（~2G）、`~/Library/Caches/dotslash`（521M）。

结果：`df -h /System/Volumes/Data` avail 6.2Gi → 135Gi；`~/Library` 140G → 50G。

### 工作日志 — 二次排查 + 删 openclaw 死日志（avail 135Gi → 184Gi）

用户问剩下 203G「系统数据」的构成。二次扫描发现最大单项：`~/.openclaw/logs/gateway.log` **31G**（最后写入 6 月 24 日，openclaw 进程已不在运行）。其余构成：`~/Library` 50G、`~/WORK` 39G、`/Library` 30G、`/Applications` 30G、`/private/var` 12G、`~/.local` 9.7G（opencode.db 8.8G）、`~/Pictures` 9.6G。

- 注意：Finder 的「已使用」比 `diskutil` 的 Volume Used 多约 58G，是 **purgeable（可清除空间）**，非真实占用；废纸篓同理显示滞后（`~/.Trash` 实际为空）。
- 用户批准后 `rm ~/.openclaw/logs/gateway.log`，avail 135Gi → 184Gi（释放 31G + 系统顺带回收 purgeable）。保留了 `gateway.err.log`（346M）。

### 工作日志 — 删除整个 ~/.openclaw（avail 184Gi → 185Gi）

用户随后要求整个删掉。删前确认目录剩 744M（含 workspace git 仓库、4 个 agent sqlite、feishu-pairing.json 凭据），无运行中进程，`rm -rf ~/.openclaw`。

结果：`~` 162G → 130G。

### 工作日志 — metabot 冗余副本清理（~1.8M）

用户问 metabot 有无缓存/无用文件。扫描结果：`~/WORK/metabot` 312M（node_modules 288M、.git 15M，源码有未提交改动不动）、`~/.metabot` 4.5M（运行数据库）、`~/metabot-workspace` 4.6M。

用户选择删除 `~/metabot-workspace` 下两个冗余副本：`.claude.zip`（`.claude` 的压缩备份，472K）和 `.claude 2`（macOS 复制残留，`diff -rq` 确认与 `.claude` 仅差 .DS_Store）。删后 metabot-workspace 4.6M → 2.8M。

### 工作日志 — pm2 启动 metabot

`cd ~/WORK/metabot && pm2 start ecosystem.config.cjs`（ecosystem 用 `node --import tsx` 跑 `src/index.ts`，NODE_ENV=production，日志到 `logs/{out,error}.log`）。

- pm2 版本 7.0.1（nvm node v22.22.0 下的全局安装，`~/.pm2` 之前只有残余数据）。
- 启动后稳定：online、0 restarts、内存 134.8M，error.log 为空；MetaMemory Web UI `http://localhost:8100`、API server `127.0.0.1:9100` 已起，ws client ready。
- 未做 `pm2 save`（开机自启），如需要再补。

### 工作日志 — 10:46 metabot 模型名报错排查 + effort 设置路径

- **做了什么**: 排查飞书里 `/model deepseek-v4.1-flash` 报的 `invalid_request_error`；确认 metabot 是否支持设置 reasoning effort。
- **为什么**: 用户问「可以设置 effort 吗」并贴出 Codex notification 报错。
- **结果**: ① 直接打网关 `/v1/models` 验证，`https://api.deepseek.com/v1` 只提供 `deepseek-flash`、`deepseek-v4-pro` 两个模型，报错是模型名不存在（`/model` 只透传 `codex exec -m <name>`），不是代码问题。② effort 目前没有聊天命令，真源是 `~/.codex/config.toml` 的 `model_reasoning_effort`（当前 `ultra`），合法值 minimal/low/medium/high/xhigh/max/ultra；要只影响 metabot 可在 `bots.json` 的 `codex.extraArgs` 加 `-c model_reasoning_effort="high"`。
- **关键命令 / diff**: `jq -r .OPENAI_API_KEY ~/.codex/auth.json` → `curl -H "Authorization: Bearer $KEY" https://api.deepseek.com/v1/models`；读 `~/WORK/metabot/logs/out.log` 定位会话模型覆盖记录；从 codex 二进制 strings 确认 effort 枚举。
- **沉淀**: [metabot-codex-model-and-effort.md](../../30-Resources/troubleshooting/metabot-codex-model-and-effort.md)；未改动任何代码/配置，等用户决定是否落 effort。

### 工作日志 — 11:06 Luka 划卡刮层：一开始预置 30-40% 固定形状清晰区（sitin-rn · apps/luka，未提交）

- **做了什么**: 用户要求在划卡模糊刮层上一开始露出 30-40% 清晰区，形状做成几个固定模板、每张卡随机选一个。改动：
  - 新建 `apps/luka/src/lib/frosted-scratch-shape.ts`：4 个归一化多边形模板，**只由直角矩形拼成且全部贴卡顶横向分层**（`bar` 顶部横长方形 / `ledge`、`ledge-right` 通栏四行 + 左下或右下垫一行 / `shelf` 通栏 + 中间垫一行），顶点全对齐 12×16 网格线；按 candidate id 的 31 进制 hash 稳定挑模板（同一张卡多次渲染形状恒定），**并按展示顺序整列递推、相邻两张必不同**（`frostedShapesFor`，卡组在 `discovery-deck.tsx` 算好整列传给 `CandidateCard` → `FrostedScratch` 的 `shape` prop）；另含射线法点在多边形内、格子覆盖、`frostedHole`（洞径系数 0.7，与手刮同一条公式）等纯函数。多边形只是骨架，实际露出面积按**圆洞并集**标定在 32.0-34.5%（用户要求的窗口是 30-35%）。
  - `frosted-scratch.tsx`：形状先摊到刮层格子，把这些格子直接摆进 `holes` —— 与手刮共用同一套带 wobble 的圆洞，边缘同样是圆弧拼出的波浪；格子同时标 `scratched=1`，`markParkScratch` 对它们返回 0，在清晰区划动不推进刮进度。手势分区未动（照片区仍只刮；未刮开只有信息带能上滑划卡）。
- **为什么**: 此前整卡都是模糊的，得先刮开才看清是谁；直接改手势分区会影响既有的「刮开解锁整卡可划」设计，故只改视觉层。
- **结果**: 新增 `apps/luka/tests/lib/frosted-scratch-shape.test.ts` 9 条全过（圆洞并集面积 30-35%、顶点不出卡、模板分布、格子不重复、预置/手刮同款洞）；`pnpm --filter luka typecheck` 与 Biome 过。`pnpm test` 全量另有 5 个失败（shape-language ×4、candidate-card-fields、good-review）—— `git stash -u` 后在 HEAD 上同样失败，其中「没有投影」是并行会话 11:10 新加的未跟踪 `components/page-guide/` 引起，均与本次无关。
- **关键命令 / diff**: `pnpm vitest run tests/lib/frosted-scratch-shape.test.ts`；预览图校验用 `esbuild` bundle lib → SVG → `magick` 渲染（临时目录）。
- **提交**: `593ac9b20`（新手浮层引导替换发现教程 —— 工作区里并行会话的 page-guide 工作，与本次同批提交）+ `63da0b185`（划卡预置清晰区 + 刮层 MaskedView 挖洞 + `@react-native-masked-view/masked-view` 0.3.2 精确锁版），已 push `76e5d74d0..63da0b185` 到 `feature/luka-ios`；pre-push 的 boundaries + 全 workspace typecheck 通过。注意并行会话把 `frosted-scratch.tsx` 的 SVG 清晰副本方案换成了 MaskedView 直接挖掉模糊层（免二次加载），`photo` prop 已删。
- **沉淀**: ① 刮层「预置透明区」必须排除出刮进度统计，否则大块预置区会让用户不刮模糊区就把整层蹭开（阈值只有 20%）。② 预置区第一版用多边形直画，和手刮的圆洞边缘对不上（直线 vs 波浪）—— 正确做法是**同一套圆洞**画法；圆洞并集比多边形骨架外扩半颗圆（+5~13 个百分点），模板参数要按并集反推。③ 模板形状只由直角矩形拼成（用户要求，不要圆形），顶点对齐 12×16 网格线：面积按格子数离散调节（一行/一列 ≈ 1-2 个百分点，直接按格数设计比画任意几何再凑面积省事）。④ 预置区不能出现左/右一条长竖边：第一版有左上竖长方形（column），实机截图里清晰区沿着脸竖切一半，看着像图片加载残废，用户明确不要；改成全部贴顶横向分层，竖边最多留一排洞高（≈6% 卡高）。⑤ 射线法对「点恰在边界上」是半开区间（左边界算内、右边界算外，下边界算外）；别把矩形边正好落在格子中心线上，否则包含与否靠巧合。⑥ 「相邻两张不重样」不能用单张纯函数避让：避让的是上一张**实际**用到的模板，而上一张自己可能也是避让才换的，拿它的 id 单独算会算错、漏出重复；只能顺着列表递推（`frostedShapesFor`）。函数返回模板池里的原对象，列表尾部追加时前面卡片形状引用不变，避免 `FrostedScratch` 依赖形状的 effect 重跑、把已刮开的洞重置。

### 工作日志 — 11:06 PWA 通话「语音转文字机器人没进房」链路排查（只读）

- **做了什么**: 用户问「pwa 语音转文字没有在房间里加上机器人，是为什么」。跨 sitin-next（app-pwa）与本地 Android 仓 `~/WORK/GraceChat-Earn-Android` 只读排查，结论：
  - 端上不会自己加机器人：PWA 不直连腾讯 TRTC API；接通时只发一次 `/dataBridgeApi/startAITranscription`（proto id 17014，body `{roomId, roomIdType:0}`）。实现 `hooks/useAITranscription.ts:55`，调用点 `useWebCall.tsx:509` / `useNativeCall.tsx:225`。机器人进房是后端调腾讯 `StartAITranscription` 的行为。
  - 转写文本回端：web 收 TRTC 自定义消息 `webCallManager.tsx:1191`；APK 由 Android `STTService.onRecvCustomCmdMsg`（只认 `type=10000`，payload text/end）→ bridge `videoConvertVoiceCallback` → H5 `NATIVE_VOICE_TRANSCRIPTION`。
  - 「没机器人」的端上可能：① 请求没发（roomId 空 early return `useAITranscription.ts:63`；voiceTaskIdRef 残留上一通 taskId 时 start 静默 return false `:57`，native 常驻 WebView 不卸载）；② 发了但后端 code≠1 只 warn 不重试 `:85`；③ 后端/腾讯侧失败（不在本仓）。
  - 另发现 Android `tuicallkit-kt/.../utils/AITranscriptionUtil.kt` 直连腾讯 API（硬编码 AK/SK、`test_tts_bot`）是**死代码**，无调用方；`RecordUtils` 走的是 `/dataBridgeApi/createCloudRecording`。
- **为什么**: 用户/测试反馈房间用户列表里没有转写机器人，需定位是端上没发请求、请求失败，还是后端口径。
- **结果**: 给出排查口径（console `[useAITranscription]` + proto 17014 响应是否有 `taskId`）；未改任何代码。
- **关键命令 / diff**: `rg` 跨仓检索 startAITranscription / STTService / AITranscriptionUtil；`read` 读 useAITranscription、webCallManager、nativeCallManager、STTService.kt、RecordUtils.kt。
- **沉淀**: 无新文件（结论已写在本条）。

### 工作日志 — 11:14 Luka 三页新手浮层引导（Chats / Friends / Discover，sitin-rn · apps/luka，未提交）

- **需求**：给 chat / Friends / discover 三页做新手引导，倾向页面浮层引导。用户拍板三点：每页独立浮层（不跨页串联）、蒙层盖住整屏含 tab bar、引导点先按代码拟一版。
- **做了什么**：
  - 方案与 step 清单落成文档 `apps/luka/docs/page-guide.md`（交互原则 / 触发与状态 / 三页 step 表 / 实现文件 / 边界 / 待办），后续改引导先改这里。
  - 新基建：`src/copy/page-guides.ts`（全部文案）、`src/lib/page-guide-steps.ts`（`PAGE_GUIDE_TARGET_IDS` + 步骤表 + 路由映射）、`src/services/page-guides.ts`（按 `userId + 页` 持久化 pending/done）、`src/stores/page-guide-targets.ts`（坐标表）、`src/hooks/use-guide-target-measure.ts`（元素 `measureInWindow` 上报，零布局侵入）、`src/components/page-guide/`（host + Modal／SVG Mask 挖洞／脉冲描边／气泡 Back-Next-Skip）。**替换并删除原 `discovery-tutorial` 三件套**（service / hook / test，从未接过 UI）。
  - 接线：`(tabs)/_layout.tsx` 挂 `PageGuideHost`；`complete-onboarding.ts` 注册完成时 queue 三页；9 个目标组件加可选 `guideTargetId`（ScreenHeader / NestWallFrame / NestDoor / PeopleTabs / ConnectionsHeader / PeopleRow / DiscoverFeedTabs / DiscoverViewToggle / DiscoveryDeck）；Debug Tools 加「Reset page guides」。
  - 测试 14 条：`tests/services/page-guides.test.ts`、`tests/lib/page-guide-steps.test.ts`、`tests/lib/page-guide-ui.test.ts`。
- **为什么**：目标是新注册用户首日三页各看一次，目标不在（空态 / 开关关）自动跳过该步，蒙层期间锁触摸只留翻页。
- **结果**：`pnpm luka:typecheck`、`pnpm --filter luka lint`、`pnpm boundaries` 全过；luka 测试 820 passed / 4 failed，4 条全是预存（good-review 的 expo `ImportMetaRegistry` 模块缺失、candidate-card-fields 引用的 nearby-list-row.tsx 已不在 HEAD、shape-language 余下 3 条为 chat/paywall 等未迁移文件）—— 与我改动无交集，另见 11:06 条目同一结论。实机验收未做。
- **关键命令 / diff**: `pnpm --filter luka test tests/services/page-guides.test.ts tests/lib/page-guide-steps.test.ts tests/lib/page-guide-ui.test.ts`；`pnpm --filter luka exec biome check --write <本次改动文件…>`。
- **沉淀**：[sitin-rn-luka-design-guard-tests.md](../../30-Resources/troubleshooting/sitin-rn-luka-design-guard-tests.md) —— `tests/design/shape-language.test.ts` 扫全 src：新 UI 用 Luka 新语言必须加进 `MIGRATED` 名单；`shadowRadius` 会被「内联圆角」正则命中、投影一律禁用（层级靠线与留白）。

### 工作日志 — 12:03 Luka 三页引导 · 实机验收后两处修复（sitin-rn · apps/luka，未提交）

用户发了 Chats 页引导的实机截图，据此修两处：

1. **快照竞态（步数丢步）**：截图显示 `2 of 2`，`chats.notifications` 没进快照 —— 原因是快照固定在进入页面 800ms 时一次性读坐标表，页面从骨架切真实内容时 `ScreenHeader` 重挂、重新测量，快照可能落在「旧坐标已清、新坐标回调未到」的窗口。改成：**最短 800ms + 坐标表稳定 200ms（上限 5s）** 再快照；Host 由 armed/session 两态重构成单一 `phase`（`steps: null` = 等快照）。
2. **Chats 第 1 步高亮目标换成示例卡**：实机看相框在照片房只是背景图上的透明点击区（无视觉实体、且与访客卡带重叠）。按用户「引导时可以用 mock 数据」的方向：新增 `lib/page-guide-mock.ts`（3 张示例访客卡，仅「引导排队/展示中 + 真空态」注入，id 前缀 `page-guide-mock-`、`openVisitor` 拦截点击）、`stores/page-guide-session.ts`（`activeKey` 广播给页面）、`NestPolaroid` 加可选 `guideTargetId`、`NestWallFrame` 移除该 prop、target id `chats.team` → `chats.cards`、文案改「Your chats」。示例卡在引导结束或真实会话出现时撤掉。

- **结果**: `pnpm luka:typecheck` / Biome / `pnpm boundaries` 过；新增 `tests/lib/page-guide-mock.test.ts` 3 条，四组引导测试 17 条全过；全量 827 中 4 条预存失败（同上一条，无变化）。待用户重新实机验收。
- **沉淀**: 引导类「展示前定步骤」的实现不要用固定延时读一次状态快照 —— 页面异步换内容时目标会重挂，正确做法是**等注册表稳定**（变化即重置计时）再定；空态页面可以用「仅引导期存在的示例数据」把交互演出来，示例 id 要有可拦截的前缀。

### 工作日志 — 11:26 Luka 划卡刮层：预置清晰区不再「先整卡糊、再跳清晰」

- **现象**（用户报）：卡面预置的随机清晰形状出现得慢 —— 先看到完整模糊蒙层，过一会儿才跳出随机刮开的那块。
- **根因**（两处叠加）：
  1. 预置区的洞放 `useEffect` 里 `setHoles(cells)`：effect 在首帧画完之后才跑，必然先渲染一帧「整卡糊」再跳成清晰区。
  2. 清晰区是靠 `<SvgImage>` 在 SVG 里**另画一份照片副本**：react-native-svg 的图片管线与卡片的 `expo-image` 不共享缓存，冷卡要等第二次下载/解码回来，清晰区才真的可见 —— 这才是「有点慢」的主因。
- **改法**（`apps/luka/src/components/discovery/frosted-scratch.tsx`）：反转图层 —— `BlurView` 整层铺满，用 `@react-native-masked-view/masked-view` 按洞的 alpha 把模糊挖掉，露出的就是卡片自己那张已经在渲染的照片（零额外加载、洞与手刮同源）。预置格子改为首次渲染同步 seed（`seeded` ref），`holes` 初值即 `frostedShapeCells(shape)`，删掉 effect。mask 用「白底 + 黑洞」经 SVG 亮度遮罩换算成「洞处 alpha=0」。组件不再需要 `photo` prop（`candidate-card.tsx` 同步去掉）。
- **依赖**：`@react-native-masked-view/masked-view@0.3.2` 直接写进 `apps/luka/package.json`（本就是 expo-router 的传递依赖，Podfile.lock 里已有 `RNCMaskedView`，**不需要重出 dev client**）；`pnpm install --lockfile-only` 只 +3 行。
- **结果**：iPhone X 模拟器冷启动录屏逐帧验证 —— 卡片出现时是占位灰（照片在加载），照片到位的**同一帧**预置清晰区一起出现，不再有「整张糊 → 后跳清晰区」的中间态；`Kat`/`Hunter` 截图上模糊/清晰边界正常，手刮洞与预置洞同一套圆。`luka:typecheck`、biome、`pnpm boundaries` 全过；`frosted-scratch-shape.test.ts` 的覆盖面用例此刻红（28.6% < 30%）—— 是并行会话正在调模板（`frosted-scratch-shape.ts` mtime 11:26 仍在变），与本改动无关。
- **关键命令 / diff**：`xcrun simctl io booted recordVideo` + `ffmpeg -vf fps=30` 抽帧；`pnpm --filter luka exec vitest run tests/lib/frosted-scratch-shape.test.ts`；`pnpm install --lockfile-only`。
- **沉淀**：SVG 图与 expo-image 不同缓存 → 任何「同一张图叠两层」的视觉都要避免用 react-native-svg 再画一份；要挖洞就用 `MaskedView` 把上层（模糊）按 alpha 挖掉，透出底层真图。Android 未验（本机 AVD 已删），MaskedView 走 DST_IN 位图，风险低。

### 工作日志 — 12:16 image2 出图：趴在「绿幕头像」上的仓鼠（物料任务，与 sitin-rn 无关）

- **需求**：用 image2 出「趴在头像上的仓鼠」图，头像区域用绿幕代替（用户给了 2 张参考：桌面会话里贴的圆形头像框截图 + `/Users/colna/Pictures/Luka- 物料/三视图.png` 角色卡）。
- **卡点（网关侧，约 1.5h）**：`api.muskapi.cc` 的 `/images/edits`（带参考图）全程 502 `system access forbidden` / 503 `No available compatible accounts`，三种 gpt-image 模型都不行；同一时间 `/images/generations`（纯文字）**间歇可用**（10 次里 7 次成功）。另：`gpt-image-2-4k` / `-adobe` 是网关 pricing 表缺档（1K/2K/4K 都报 missing），`chat/completions` 该 key 的 group 未开通（`Chat Completions API is not enabled for this group`），gemini 图像模型不是 404 就是账号耗尽。
- **结果**：走纯文字 prompt 出 3 版（1024x1024，方形构图：仓鼠趴在圆形头像盘顶沿、盘中纯绿 `#00FF00`±5、盘外近白 253、无文字），存 `~/Pictures/Luka- 物料/opencode-image2/20260920-hamster-on-avatar-green-textonly-v{1,2,3}.png`。参考图版（/images/edits）仍挂后台重试脚本 `/tmp/image2-refs/retry-loop.sh`，成功写 `retry-result.txt`。
- **关键命令**：`python3 ~/.agents/skills/image2-gen/scripts/gen_image.py --model gpt-image-2 --prompt-file … --size 1024x1024`；绿幕取样 `magick img.png -format '%[pixel:p{512,700}]' info:`。
- **沉淀**：[image2-gateway-edits-blocked.md](../../30-Resources/troubleshooting/image2-gateway-edits-blocked.md)（网关分通道故障判断 + 桌面版会话里用户贴图的提取方法）。

### 工作日志 — 14:30 Luka 模拟器构建链接失败：RN 预编译 core 被换成 Release 变体（sitin-rn · apps/luka，无仓库改动）

- **现象**：`pnpm luka:ios -- --device 'iPhone X (11 Pro eq)'`（模拟器 Debug）链接 `Luka.debug.dylib` 失败：`Undefined symbols` 报 `RCTPackagerConnection` / `RCTReconnectingWebSocket` / `facebook::react::Sealable::Sealable()` / `DebugStringConvertible::getDebug*` 等一批；前面夹着两条误导性的 `auto-linked framework 'UIUtilities' / 'CoreAudioTypes'` warning（是 warning，不是元凶）。
- **根因**：09-18 本地 `build-ipa`（`xcodebuild -configuration Release` archive）把共享的 `ios/Pods/React-Core-prebuilt/React.xcframework` 换成 **release** 变体并写 marker；今天 13:50 因新增 masked-view 触发的 `pod install` 把 marker 清掉，而 Debug 构建的 `[RNCore] Replace` 脚本见「无 marker」就按注释假设已是 Debug → 跳过替换 → release core 链 Debug 包，dev-only 符号全缺。
- **排查要点**：先分清 warning / error；`xcrun dyld_info -exports <framework> | grep -c RCTPackagerConnection` 可 30 秒区分 debug/release 变体；和 `Pods/ReactNativeCore-artifacts/reactnative-core-0.86.0-{debug,release}.tar.gz` 解出的同片 binary 比 shasum 复核。
- **修法**：`cd apps/luka/ios/Pods` → `printf 'Release' > React-Core-prebuilt/.last_build_configuration` → `node "$(node --print "require.resolve('react-native/scripts/replace-rncore-version.js')")" -c Debug -r 0.86.0 -p "$PWD"`。**别用 `pod install` 修**（会再抹掉 marker）。
- **结果**：同一条 xcodebuild 重新构建 `** BUILD SUCCEEDED **`，framework 换回 debug（shasum 与 debug tarball 一致）。只动了 gitignore 的生成目录，无仓库代码改动。
- **沉淀**：[rn-prebuilt-core-debug-release-swap.md](../../30-Resources/troubleshooting/rn-prebuilt-core-debug-release-swap.md)。

### 工作日志 — 14:13 Luka：划卡玩法反转（上滑=喜欢 / 右滑=不喜欢）+ 引导扩到 Quick Pick / AR（sitin-rn · apps/luka，未提交）

用户三件事：① 划卡页上划改为喜欢、不喜欢改为右滑；② 引导加入这个玩法；③ Friends 引导增加 Quick Pick / AR 等功能。

- **划卡反转**：
  - 新增纯逻辑 `src/lib/swipe-gesture.ts`（主轴判定 `swipeDragDirection` / 松手越线 `swipeReleaseDirection` / 进度归一 `swipeDragProgress`，worklet 可调用）+ `tests/lib/swipe-gesture.test.ts` 4 条。
  - `card-stack.tsx`：pan 从「只认向上」改成双轴（向上 12px 或向右 12px 激活；向左 16px `failOffsetX`；向下仍钳 0）；位移拆成每槽 X/Y 两个 shared value；`flyOut(direction)`；新增 `onLike`（上滑提交前**同步**问业务受不受理，不受理回弹、受理只飞卡且不报 pass）；右滑走 `shouldAllowSwipe("right")` → `onSwipeCard(index,"right")`；Skip 按钮与卡上 dismiss = `commitSwipe("right")`；`swipeToNext`（按钮 like 的程序化前进）仍向上飞。
  - `discovery-deck.tsx`：抽 `playSayHiFlight`；`likeFromSwipe` 接 CardStack 的 `onLike`（`onSayHi(person, { advance: false })` —— 手势路径卡片已当场飞出，业务不能再定时前进，否则多跳一张）；`onSayHi` 签名加 options。
  - `use-swipe-actions.ts`：`sayHi` 加 `{ advance?: boolean }`；注释改右滑 = pass。
  - `DeckSwipeHint`：从单一的向上双角标改成左右两组（↑ Say hi / Skip →），固定带高 27 与 `BELOW_CARD_HEIGHT` 的拆项对齐。
  - `nearby-ticket-playfield.tsx`（未挂载的备用玩法）同步新方向类型。
- **引导扩展**：Discover 第 2 步文案改「Like or skip / Swipe up to say hi, swipe right to skip」（这一步就是玩法说明）；Friends 从 3 步扩成 5 步（分段 / Find Nearby / Quick Pick / AR play / Say hi）—— `PeopleActionCard` 加 `guideTargetId`，`ConnectionsHeader` 三张入口卡分别注册（AR 卡不存在时该步自动跳过）。
- **文档**：`docs/discover-play.md`（现状段手势、功能对照表）、`docs/page-guide.md`（Friends / Discover 两张 step 表 + 目标组件清单）。
- **验证**：`pnpm luka:typecheck`、`pnpm lint`（仅余 `use-swipe-actions` 既有的 `router` 未使用 warning）、`pnpm boundaries` 全过；全量 831 passed / 4 failed（仍是那 4 条预存失败，无新增）。
- **注意**：右滑让卡片 pan 也开始识别横向 —— 与刮层的横向手势靠 `swipeBottomBand` 隔离（未刮开的照片区起手直接 fail、信息带没有刮层；揭开后刮层收手势）。实机要重点验：刮照片时卡片不跟手、右滑能跳过、上滑能喜欢（含额度用尽回弹）。
- **沉淀**：引导里讲手势时「文案 + 页内方向提示」要一起改（`DeckSwipeHint` 与 step 文案同源说一套），否则两边说法会漂。

### 工作日志 — 14:29 Luka 引导修复：Pressable 测量不可靠 + Chats「去认识人」改指 Discover tab（sitin-rn · apps/luka，未提交）

实机反馈两点：① Chats 引导第 2 步「Tap the door to find people nearby」与实机对不上 —— 门只在**房间真空**时才是 Discover 入口，收到 Team 欢迎线程后房间算「非空」（`shouldShowChatEmptyState` 的 `hasOfficialFallback`），门变成 video match 的 call 入口、没就绪就点不动；② Friends 三张入口卡（Find Nearby / Quick Pick / AR play）气泡出现了但**没有挖洞**（第 2/3/4 步，共 4 of 4，Say hi 步也没进快照）。

- **挖洞失败根因**：测量挂在 `Pressable` 上的目标不可靠 —— 页头爪印（Pressable）从未高亮、三张入口卡（Pressable）快照时有坐标、展示时已丢；挂普通 `View` 的目标（分段、卡片、列表行）一直正常。
- **修复**：
  - `ScreenHeader` / `PeopleActionCard` / `TabItem` 的测量改挂在**外层 View**（布局不变，`ml-auto`/`flex-1` 跟着挪）；这是「不挂 Pressable」的约定。
  - `useGuideTargetMeasure`：`onLayout` 后下一帧（rAF）再量一次；store 加 `measureToken`/`requestMeasure`，Host 排队时主动请求一次全场重测。
  - 等待窗口 `PAGE_GUIDE_PRESENT_DELAY_MS` 800 → 1200、稳定窗 200 → 300，让列表晚到的目标（Friends 第一行 Say hi）更容易赶上快照。
  - 用户原则「实际展示的要和引导对应得上」→ Chats 第 2 步从「门」改成**高亮底部标签栏 Discover**（`TabBar` 的 Discover 格加 target，`chats.door` → `chats.discover-tab`，文案 "Tap Discover to meet people nearby."）；`NestDoor` 的 target prop 移除。
- **文档**：`docs/page-guide.md`（Chats step 表 + 两条根因注记 + 目标清单 + 测量约定）。
- **验证**：`pnpm luka:typecheck`、`pnpm lint`（全仓干净）、18 条引导/手势测试过。
- **沉淀**：① UI 引导的坐标测量**统一挂 `View`，不挂 `Pressable`**（Fabric 下不可靠）；② 引导指向的入口必须是「所有账号状态下都存在且行为一致」的那个（tab bar 优先级高于房间场景里的热点）。

### 工作日志 — 14:35 Luka 引导：Chats 加 See all 一步（sitin-rn · apps/luka，未提交）

- **需求**：Chats 引导加上 See all。
- **实现**：`PAGE_GUIDE_TARGET_IDS.chatsSeeAll`（`NestSeeAllButton` 根 `Animated.View` 挂测量，由 `NestPlayfield` 传）；Chats 步骤变为 4 步：示例卡 → **See all** → Discover tab → 页头爪印；文案 "See all chats / Open the full list of everyone you talk with."（气泡 above）。See all 只在房间非空时渲染，真空态自动跳过。文档 `docs/page-guide.md` 同步。
- **验证**：`pnpm luka:typecheck`、Biome、14 条引导测试过。
- **备注**：本机 `/usr/bin/git` 因 Xcode license 失效（`xcode-select -p` 指向完整 Xcode），本次及下一步的 git 操作用 `/Library/Developer/CommandLineTools/usr/bin/git` 绕行；待用户 `sudo xcodebuild -license accept` 恢复。

### 工作日志 — 14:20 Luka：划卡 / Say hi 达到每日上限改跳会员页（sitin-rn · apps/luka，未提交）

- **需求**：划卡（nope）与 Say hi（like）达到每日额度时进会员页（原来只 toast）；埋点 source 按类型分开（用户确认：like+nope 都跳、不保留 toast、source 区分）。
- **改动**：
  - `apps/luka/src/services/swipe-limit.ts`：删 `SWIPE_LIMIT_MESSAGE`；新增 `SwipeLimitReason`（"like"|"nope"）与 `swipeLimitPaywallHref()`（like → `say_hi_limit`，nope → `swipe_limit`），source 映射单一来源；顺带修正「up-swipe」旧注释为右滑（跟随并行会话的玩法反转）。
  - `apps/luka/src/hooks/use-swipe-actions.ts`：`presentSwipeLimitReached(reason)` 改成 `router.push(swipeLimitPaywallHref(reason))`（router 参数终于用上，原 lint warning 一并消失）；`sayHi` 传 `"like"`；删 toast。
  - `apps/luka/src/hooks/use-nearby-say-hi.ts`：Nearby 列表 Say hi 超限改 `router.push(...)`；toast 只留「Already said hi」。
  - `apps/luka/src/components/discovery/discovery-deck.tsx`、`components/discover-play/nearby-ticket-playfield.tsx`：`presentSwipeLimitReached` prop 带 reason，右滑传 `"nope"`；注释同步「跳会员页」。
  - `apps/luka/tests/services/swipe-limit.test.ts`：加 1 条 source 映射用例。
- **结果 / 验证**：biome 6 文件过；`vitest run tests/services/swipe-limit.test.ts` 4 passed；luka 全量 832 passed / 4 failed（4 条为并行会话的 design/good-review 预存失败，与本改动无关）；`luka:typecheck` 被并行会话新引入的 `components/discover-feed/feed-masonry-card.tsx:179 ImageGallery` 缺 import 挡住（未触碰该文件）。
- **未覆盖**：profile 详情页的打招呼本来就不查 `isLikeReady`（无门控），未纳入本次；Connections / Quick Pick 走 `ignoreLimit`，按设计不跳。
- **沉淀**：额度门的「提示方式」和「埋点 source」都该收在 `swipe-limit.ts` 一处；两个入口各拼一次字符串，看板和文案就会漂。

### 工作日志 — 14:15 sitin-rn：luka Discover Feed 卡片接上帖子详情页

用户要求「Feed 里的帖子可以点进去看 feed 详情，有现成页面就接上，没有才设计」。

- **现状查明**：`apps/luka/src/app/post/[id].tsx` 已是完整详情页（作者行、PhotoGrid + 大图、正文、点赞、评论区 + 评论输入框、举报/拉黑/删除），入口已有 `my-feed` / `me` / 通知；唯独 Discover > Feed 的瀑布流卡片没接（点封面 = 有图开大图 / 无图进作者主页）。**有现成页面，未动用 image2-gen**。
- **改动 3 个文件**（其余 luka 改动属并行会话，未触碰）：
  - `src/components/discover-feed/feed-masonry-card.tsx`：加 `onOpen` prop，封面点击 → `onOpen(post)`；移除卡片内 `ImageGallery` 与 gallery state（看图走详情页）；顶部注释按新行为重写。
  - `src/components/discover-feed/discover-feed-page.tsx`：新增 `onOpenPost` prop 并透传。
  - `src/app/(tabs)/discover.tsx`：新增稳定 `openPost`（`router.push('/post/:id')`）`useCallback`，保证 memo 链不失效。
- 保留：作者行 → 作者主页、点赞、右上打招呼、长按 → 举报/屏蔽/删除。
- **验证**：`pnpm --filter luka typecheck` ✅、`lint` ✅（1042 files）、`pnpm boundaries` ✅；`pnpm --filter luka test` 832 passed / 4 failed（shape-language ×3、candidate-card-fields ×1、good-review 1 suite 报 expo `ImportMetaRegistry`，offenders 全是未改动的 chat/credits/paywall/discover-play 文件，预存失败，与本次无关）。
- **未覆盖**：未在模拟器/真机走查，建议 Discover → Feed → 点卡片 → 详情（看大图、评论、返回）。
- 并行会话此前在 `feed-masonry-card.tsx` 看到的 `ImageGallery` 缺 import 是本次编辑中间态，最终 typecheck 已过。

### 工作日志 — 14:17 Luka：Nearby 划卡右下角小崽收小（sitin-rn · apps/luka）

用户贴模拟器截图圈出卡角那只 `ParkPassHamster`，「这只仓鼠要小一点」；给了三档幅度，用户选 **-25%（画布 170 → 130）**。

- **改动 2 个源文件 + 1 文档**：
  - `src/components/discover-play/park-pass-hamster.tsx`：加可选 `height` prop，默认仍是 `PARK_HAMSTER_SIZE`（170），票卡玩法（未挂载）不受影响；宽度照旧按 320×436 比例算。
  - `src/components/discover-nearby/nearby-view.tsx`：新增 `DECK_HAMSTER_HEIGHT = 130` 传进去；右/下锚点（`cardInset + 2`、`tabBarClearance + BELOW_CARD_HEIGHT - 16`）未动。
  - `docs/discover-play.md`：现状段补一句「卡角这只按 `DECK_HAMSTER_HEIGHT` 画 130pt，比票卡玩法小一号」。
- **验证**：`pnpm luka:typecheck` ✅、`npx biome check` 两文件 ✅、`pnpm luka:test` 832 passed / 4 failed（与 14:13 那笔同样的预存失败，无新增）。
- **未覆盖**：未在模拟器截图复核。画布缩小后可见小崽会往下坐约 2pt（透明边同比缩小、画布底仍锚在原处），要「脚正好贴卡底」再微调那个 `- 16`。
- **提交**：用户让「push」→ 连同并行会话的三笔 luka 改动一起提交 `8cca6dd70`（`feat(luka): 划卡上滑=喜欢/右滑=跳过，Friends 引导逐卡，Feed 卡片进详情`，23 files）并 push 到 `feature/luka-ios`；pre-push 的 boundaries + luka typecheck 通过。commit 里带了 4 个话题（划卡反转/引导/Feed 详情/小崽收小），因为 `nearby-view.tsx` 等文件被多笔改动交织，没法干净拆。

### 工作日志 — 14:30 仓鼠头像物料第二轮（按用户反馈重出）

- **反馈**：第一版（12:16 出的 3 张）「生成的不对」，要求仓鼠**外观严格对齐 `三视图.png` 角色卡**、版式对齐用户给的圆形头像框截图，头像区仍是绿幕。
- **新发现**（都写进 [image2-gateway-edits-blocked.md](../../30-Resources/troubleshooting/image2-gateway-edits-blocked.md)）：
  1. 网关开始按**区域**拒绝（CN 出口 IP 403 `GROUP_NOT_ALLOWED`）；用户开 Clash Verge 后 `HTTPS_PROXY=http://127.0.0.1:7897` 绕通（`gen_image.py` 用 urllib，环境变量自动生效）。
  2. `/images/edits` 代理下仍 502 —— 是 **group 权限**（`/v1/responses` 直接回 `not enabled for this group`），当前 key 只有 `/images/generations` 可用；**图生图这条路要换 key 才能走**。
  3. `gen_image.py` 一律发 **multipart**，这类请求被分组拒；改 **JSON body** 打同端点就通。故本次出图用自写 `~/.cache/image2-refs/gen.sh`（curl + JSON + 重试）。
- **结果**：按角色卡细节重写 prompt（可可棕+奶油白、宽粉口鼻、大眼睛高光、小粉爪、plush 3D、盘内纯 `#00FF00`），出 4 版：`~/Pictures/Luka- 物料/opencode-image2/20260920-hamster-on-green-avatar-sheetstyle-v{1..4}.png`（绿幕实测 rgb(2~6,246~249,7~13)，盘外 254 白）。参考图版（真图生图）仍不可用。
- **附带**：`/tmp/image2-refs` 被系统清空过一次（参考图、重试脚本全没），改到 `~/.cache/image2-refs/` 常驻；用户贴图重新从 opencode.db 解码得到。

### 工作日志 — 14:35 Luka：sign-in 页仓鼠从底边挪到表单与 Login 之间（sitin-rn · apps/luka，未提交）

用户贴两张模拟器截图，第二张红框圈出「Forgot password?」与 Login 胶囊之间的空白，要求小崽挪到红框处。

- **红框量出来**：贴图用 `sqlite3` 从 `~/.local/share/opencode/opencode.db` 的 `part` 表解出（`data:image/png;base64`），`magick` 按 `#FA5151` 提连通域 → 红框 297×348px，换算 **163.5×191.6pt、水平居中，纵跨屏高 435.5–627.5pt**（iPhone X/11 Pro 812pt 坐标系，按屏内奶油区 681×1475px 定标尺 1.8165px/pt）。对照 `sign-in.tsx` 的算式（表单底 415 / 胶囊顶 675 / 空隙 260），红框正是这段 flex 空隙 —— 即「整只可见地站在空隙里」。
- **改了 3 个源文件 + 1 个测试**（未碰并行会话的改动）：
  - `src/components/login/login-peek-mascot.tsx`：去掉 `absolute inset-x-0` 与 `bottom: -88`，改成随流布局的 260×320 盒子；注释重写（旧注释写的是「趴在真机底边，这就是产品」，红框版改为站在空隙里）。文件/组件名保留 `Peek`（避免顺手改名）。
  - `src/app/sign-in.tsx`：占位 `<View className="flex-1" />` 换成 `flex-1 items-center justify-center` 包住小崽；键盘弹起时仍 `tucked` 淡出。
  - `src/components/login/welcome-legal-links.tsx`：加 `tone?: "overlay" | "onCream"`，奶油底用 `Colors.inkSoft`。原因：原来是 72% 白（为压整屏视频设计），sign-in 上只靠页底仓鼠当底衬；仓鼠挪走后白字在奶油底上隐形，用户确认一并改深色。
  - `tests/design/guest-path.test.ts`：加 1 条守门（奶油底必须 `tone="onCream"` + `Colors.inkSoft`）。
- **验证**：`pnpm luka:typecheck` ✅；`npx biome check` 4 文件 ✅；`pnpm luka:test` **833 passed / 4 failed**（shape-language ×3 + candidate-card-fields ×1，offenders 全在未改动的 chat / credits / paywall / discover-play，与 14:15 记录同一批预存失败）。
- **尺寸取舍**：保持原尺寸（可见内容约 176×201pt，比红框宽约 5%），居中间隙 → 落点比红框整体低 ~13pt；要严格贴框得再给一个数。
- **未覆盖**：没在模拟器复核截图（本机 `git` / 模拟器都因 Xcode 许可未同意跑不起来，`git status` 一律回 `xcodebuild -license`），改动**未提交**。
- **沉淀**：`login-peek-mascot.tsx` 的透明边占位是「可见仓鼠只有盒子 63% 高」的来源 —— 视觉定位要按素材 alpha 包围盒算，别按盒子算（本次素材 941×1672，内容 918×1049+0+300）。

### 工作日志 — 14:38 Luka 会员页大标题被仓鼠盖住（sitin-rn · apps/luka，未提交）

- **需求**（用户截图）：`Keep your story lit` 右半句被探上来的仓鼠头挡住，要求标题**盖在仓鼠之上、完整展示**。
- **改法**：`apps/luka/src/app/paywall/index.tsx` 标题整块（PLUS PASS / 大标题 / 副标题）加 `style={{ zIndex: 20 }}` —— 仓鼠挂在**后一个兄弟节点**里、负 `top: -86` 探进标题区，不给这层 zIndex 默认就画在下面；仓鼠自己的 `zIndex: 10` 只在它的父盒内生效，压不过兄弟层。`biome check` 过；模拟器无法复核（本机 Xcode 许可未同意，`xcrun simctl` 同样报错），未提交。
- **沉淀**：RN 里「绝对定位探出父盒的装饰」与相邻文字区抢层级时，zIndex 要加在**文字所在的那个兄弟层**上，加在装饰自身只能管父盒内部顺序。

### 工作日志 — 14:42 Luka：All chats 搜索框 placeholder 被裁（sitin-rn · apps/luka，未提交）

- **现象**（用户截图，红框圈出抽屉里的搜索框）：placeholder `Find a chat` 只露上半截，下半截被切；光标是完整行高（22pt），光标底 ≈ 裁剪线。
- **根因**：`apps/luka/src/components/chats-play/all-chats-drawer.tsx` 的 `TextInput` 是 luka 里唯一既没有 `height` 也没有 `leading-*` 的**单行**输入框（`className="ml-2 flex-1 font-sans text-[16px]"`）。luka 其余输入框全都钉了行盒（`sign-in-field` height 24 + padding 0、`delete-confirm-field` leading-[22px]、`chat-input-bar` height + lineHeight、`edit-profile/*` height，dev 屏直接 `h-[46px]`）；`components/ui/text.tsx` 与 `sign-in-field` 的注释都记过同一个坑。从截图逐像素反解：输入框内高 83px（≈46pt）、光标高 40px（≈22pt = Nunito 16 自然行高 1.364×16），而字形基线落在框底之下约 6pt，于是只剩顶部可见。
- **改法**：`className` 加 `h-full`（= 抽屉行的内容高 46pt）+ `leading-[21px]`（与同屏副标题同一套 16/21），并留一行中文注释说明为什么必须钉。备选是 `style={{ height: 24, padding: 0 }}`（`sign-in-field` 写法）或 `h-[46px]`（dev 屏写法），选 `h-full` 是为了行高将来变了不用再改。
- **验证**：`pnpm --filter luka typecheck` ✅、`npx biome check` 两文件 ✅、直接 `curl http://localhost:8081/.expo/.virtual-metro-entry.bundle?platform=ios` 返回 200（bundle 编译通过 ✅）。**没能在模拟器里肉眼复核**：本想临时把 `(tabs)/index.tsx` 的 `allChatsOpen` 初值改 true + 重启 App 截图，但 `simctl launch` 后 dev client 报 `Failed to load app from http://192.168.2.73:8081`（当时 Metro 正常、bundle 可编译，疑为重启瞬间的加载超时），随后模拟器整台关机（`No devices are booted`）。临时改动已还原（`git diff` 只剩本轮真改动）。
- **文档**：`apps/luka/docs/chats-play.md` 的 `All chats` 条目补了「搜索框自己钉行盒」一句。
- **沉淀**：见 [luka-text-line-height.md](../../30-Resources/troubleshooting/luka-text-line-height.md) 新增「单行 TextInput」一节。

### 工作日志 — 15:00 Luka 帖子详情页按小红书式布局重做（sitin-rn · apps/luka，未提交）

承接 14:15 的「feed 卡片接详情页」：用户接着要求详情页用 image2-gen 重设计。迭代三轮设计稿（A 白卡版 / B 拍立得版 / 小红书布局版）后确认方向：**小红书式布局 + luka 蜜色视觉、去掉 Save 收藏、底栏加仓鼠**（明确用 `luka.mov`，即 2026-09-14 已改名的 `point-down.mov`）。本轮实现代码。

- **设计稿**（已归档）：`~/Pictures/Luka- 物料/opencode-image2/20260920-luka-post-detail-xhs-layout-v2.png`（v1 同目录）；参考图（模拟器详情页截图 + 用户发的小红书截图）在 `/tmp/image2-refs/`。
- **新增**：`lib/post-caption.ts`（`body` 首行当标题的拆分，5 条单测）；`hooks/use-feed-buzz.ts`（Say hi 逻辑从 `discover.tsx` 抽出，详情页与 Feed 共用，行为未变）；`components/post/` 下 `post-detail-header`（返回/头像+名字/Say hi/…）、`post-photo-pager`（全宽无圆角 + 多图分页 + 轮播点）、`post-caption-block`、`post-replies-card`、`post-action-bar`（胶囊输入框 + 赞 + 评论）、`post-perch-hamster`（iOS 视频，`PET_POINT_DOWN_SOURCE`；`PET_RIGHT=84` 等定位参数要实机对）。
- **重写**：`app/post/[id].tsx`（顶栏作者栏 → 全宽图 → 标题正文 → 评论卡 → 常驻底栏）；**删除** `components/scene/photo-grid.tsx` 与 `components/post/post-comment-composer.tsx`（两文件此前只被详情页引用）；`post-comment-row.tsx` 加 `last` 去掉末行横线。
- **取舍**：分享图标未实现（luka 无分享能力，位置给了「…」更多菜单）；话题标签不做（`ScenePost` 无字段）；评论保留内嵌而非设计稿的弹层；预览/点赞/评论/举报拉黑删除/Say hi/大图全部保留。
- **验证**：`pnpm --filter luka typecheck` ✅、`biome check` 1049 files ✅、`pnpm boundaries` ✅、全量测试 **838 passed / 4 failed**（shape-language ×3 + candidate-card-fields ×1 + good-review suite 的 expo 解析问题，与 14:15 同一批预存失败；新增 5 条 post-caption 全过）。
- **未覆盖**：模拟器已被关掉（`No devices are booted`），未实机走查；建议 `pnpm luka:ios` 看底栏仓鼠落点（素材注释：只能在实机对）。
- **实机反馈（同日内）**：用户按实机截图两轮微调仓鼠落点 —— ①「趴在输入框上」（原落点在输入框与心形之间）→ `PET_RIGHT` 84 → 142、`PET_OVERHANG` 加入 `BAR_PADDING_TOP = 10`；②「再往上一点」→ `PET_OVERHANG` 改为 `PET_SIZE/2 + BAR_PADDING_TOP − 6`（爪子落在输入框上沿上方约 6pt，原 +2 会看起来陷进输入框）。`biome` + `typecheck` ✅。
- **沉淀**：见 [opencode-desktop-image-extraction.md](../../30-Resources/troubleshooting/opencode-desktop-image-extraction.md) 新增「多条 part 时的两个坑」（用户贴的图按时间窗口从 `opencode.db` 抽，别共用一个输出文件）。

### 工作日志 — 15:30 Luka 模拟器构建又挂：Xcode 升 27 后 SwiftUICore stub 过期（sitin-rn · apps/luka，无仓库改动）

承接 14:30 的 RN 预编译 core 修复，同一构建流程又在新的一步失败。

- **现象**：链接 `Luka.debug.dylib` 报 `Undefined symbols`：`method descriptor for static SwiftUI.PreferenceKey._preferenceValuesEqual(A.Value, A.Value) -> Swift.Bool`，`Referenced from: l_got.$s7SwiftUI13PreferenceKeyP22_preferenceValuesEqualySb5ValueQz_AFtFZTq in libExpoUI.a[13](BottomSheetView.o)`。
- **根因**：本机 Xcode 已升到 **27.0**（iPhoneSimulator27.0.sdk，SwiftUICore `current-version: 8.0.84`），而 `ios/SwiftUICoreStub/SwiftUICore.tbd` 是今天 13:50 在 Xcode 26 下生成的快照（`7.5.3`，无该符号）。ExpoUI 是源码 pod，被新编译器重编后引用了 iOS 27 SDK 新导出的符号，旧 stub 符号表缺项。rn-trtc 的 stub 只在 `post_install`（= `pod install`）时重写，而 `scripts/ensure-simulator-pods.sh` 只在 Podfile.lock 有 RangersAppLog 时才触发 `pod install`，Xcode 升级没有触发点。
- **修法**：`cd apps/luka/ios && env -u HEYHRU_BYTEPLUS_NATIVE pod install` 重放 post_install → stub 重写为新 SDK 全量符号表（`current-version: 8.0.84`、`preferenceValuesEqual` 0 → 6 处）；随后同参数 xcodebuild（iPhone X (11 Pro eq)，Debug）`** BUILD SUCCEEDED **`。
- **守则**：**升级 Xcode 后，先 `pod install`（或 `prebuild`）再构建。**
- **沉淀**：[xcode26-swiftuicore-sim-link.md](../../30-Resources/troubleshooting/xcode26-swiftuicore-sim-link.md) 追加「变体：Xcode 大版本升级后 stub 过期」一节（30 秒识别法：比 stub 与 SDK 真 tbd 的 `current-version`）；可选兜底（在下一次 Xcode 升级前可做）是在模拟器构建前置脚本里加这个比对。
- **备注**：本轮无仓库代码改动，只动 gitignore 的 `ios/` 生成目录；`apps/luka` 工作区仍有一批未提交业务改动。

### 工作日志 — 15:36 Xcode 27 装机失败：@expo/cli 把模拟器当物理设备（sitin-rn · 改 pnpm-lock.yaml + pnpm-workspace.yaml，未提交）

承接 15:30：构建通过后 `expo run:ios` 装不上模拟器，报 `CoreDeviceError 1001 The capability "Install Application" is not supported by this device`，栈指向 devicectl.js。

- **根因**：Xcode 27 的 `xcrun devicectl list devices` 开始把模拟器也列出来（`Reality = simulated`）。@expo/cli 57.0.9 的 `getConnectedDevicesAsync()` 把 devicectl 条目硬编码为 `deviceType: 'device'`，再与 simctl 列表按 UDID 去重且 devicectl 条目在前 → 模拟器被当成物理设备 → usbmuxd 失败回落 devicectl → Capability 不支持。模拟器本身没坏（手动 `simctl install` 同一 `.app` 成功）。上游 57.0.14 修了（过滤 `hardwareProperties.reality === 'simulated'`）。
- **修法**（用户选升级）：`pnpm-workspace.yaml` 的 `overrides` 钉 `'@expo/cli': 57.0.26` → `pnpm install` → `node_modules/@expo/cli` 57.0.9 → 57.0.26。lockfile 变化 627+/110-：@expo/cli 及其子树（@expo/config、metro-config 57.0.12、metro 0.84.5 等）+ 若干 peer 上下文哈希（sentry-router/uniwind），无其它直接依赖版本变化。
- **验证**：`resolveDeviceAsync('iPhone X (11 Pro eq)')` 返回 simctl 条目（`isSimulator: true`）；`pnpm luka:ios -- --device 'iPhone X (11 Pro eq)' --no-bundler` 走完 `Build Succeeded → Installing → Opening on iPhone X (11 Pro eq)`，exit 0。
- **绕过（不改仓库）**：先 `xcrun simctl boot <udid>`，再不带 `--device` 跑 `pnpm <app>:ios`（走 `getBestBootedSimulatorAsync` 纯 simctl 路径）。
- **沉淀**：[expo-cli-xcode27-devicectl-simulator.md](../../30-Resources/troubleshooting/expo-cli-xcode27-devicectl-simulator.md)。
- **状态**：pnpm-workspace.yaml + pnpm-lock.yaml 已改**未提交**（仓库有 luka 一批业务改动也一起挂着）；若要入库需按仓库规矩走分支/PR。

### 工作日志 — 16:0x Luka Quick Pick 打地鼠换「仓鼠画框」素材 + 头像落位（sitin-rn · apps/luka，未提交）

用户给了一张 image2 新图（仓鼠扒着一个绿幕圆，白底），口径：「打地鼠头像上的仓鼠图片改为这张，
把背景和绿幕抠了，头像放到绿幕的位置」。

- **素材**：源图 `~/Pictures/Luka- 物料/de4a2a7d-….png`（1024×1024，自带 alpha 白底）→ 绿幕色键
  （`min(G-R,G-B)` 软阈值，几何裁圆会切掉压在圆上的爪子，所以不能用裁圆）+ 透明区 RGB 外推 +
  预乘 LANCZOS 缩到 500 宽 → `apps/luka/src/assets/quick-pick/mole-frame.webp`（31.8KB，500×673）；
  **删** `mole-hamster.webp`（旧素材只被地鼠引用）。recipe 沉淀见
  [image-gen-transparent-cutout.md](../../30-Resources/troubleshooting/image-gen-transparent-cutout.md) 附二。
- **代码**：`mole-geometry.ts` 换成 `frameW/frameH/avatarLeft/avatarTop/avatar`（把旧素材的
  `hamsterW/hamsterH/hamsterOverlap/pawTop/pawHeight/ring` 全删）；`quick-pick-mole.tsx` 里
  旧的三层（仓鼠在下 + 头像盖住切边 + 爪子用裁切窗口再画一遍压前面）**塌成两层**：头像先画、
  画框压上去；印章层必须移到画框**之后**（画在之前会被白环吃掉）。地鼠内容高 150.3pt（原 154）、
  占位高 189.3pt（原 193），棋盘行高/缩放不受影响。
- **产品决策（已问用户）**：素材自带白环约 1.2pt，和 RN 那条「拍中变焦糖」的描边打架 —— 用户选
  **保留素材白环**，拍中反馈改成「小卡转蜜色 + 右上爪印印章」，白环不再变色。`quick-pick-impl.md`
  §2.6 与素材表已同步。
- **验证**：`biome check` ✅、`pnpm --filter luka typecheck` ✅、`pnpm --filter luka test` 838 passed /
  4 failed（shape-language ×3 + candidate-card-fields ×1 + good-review，均为工作区里其他未提交改动
  的预存失败，与本轮无关；quick-pick 两个测试文件全过）；另按代码里的精确 pt 值合成了一张
  「普通态 + 拍中态」对照图（含洞口），逐半径复核头圆与白环之间无缝隙、无绿边残留。
- **未覆盖**：没实机走查（模拟器状态未知）；若要入库需按仓库规矩走分支/PR。

### 工作日志 — 15:46 Luka：Chats 墙上恢复可见的 Luka Team 标牌（含未读角标）（sitin-rn · apps/luka，未提交）

用户贴两张模拟器截图（图一 All chats 抽屉里 Luka Team 行未读 1；图二 Chats 房间），
「luka team 的消息也要在图二这个状态下展示」。规范模式先确认形式，给了三个选项，用户选
**墙面可见 Team 标牌**（最小改动、不占 8 张访客卡位）。

- **背景**：2026-09-18 房间铺满整屏后，进 Team 只剩一块**隐形**的墙上热区
  （`NestWallFrame` 的 `photoRoom` 分支），Team 的未读在房间里完全看不见；official 会话被
  排除在卡片池外，也不参与房间的新消息信号。
- **代码**（4 个文件）：
  - `components/chats-play/nest-wall-frame.tsx`：删 `photoRoom` 隐形分支，改为常驻墙上的可见
    标牌 —— caramel 外框 + cream 卡 + Line 内框（沿用可见版墙框样式）、定宽 98、头像 44
    套 rounded-[12px] 裁切（同 Polaroid 语言）、未读角标右上（`Colors.badge` + 2px 底描边），
    位置 `left-[28%] top-[14%]`（避开悬浮页头），点击进 `/team`。
  - `components/chats-play/nest-playfield.tsx`：`NestTeam` 加 `unreadCount` / `lastMessageTime`；
    未读快照比对抽出 `observe()`，Team 以 `TEAM_SNAPSHOT_KEY` 进同一份快照 —— Team 新消息
    同样触发 `unreadPulseToken`（小崽播收消息短片 + See all 抖）。
  - `app/(tabs)/index.tsx`：新增 memo 的 `nestTeam`，未读/时间与 All chats 的 official 行同源
    （`officialFallback.unreadCount` 或 `officialConv.unreadCount / lastMessageTime`），替掉原来
    的内联 `team={{...}}`。
  - `docs/chats-play.md`：首段、页面结构注释、数据规则（official 只进墙框 → 只进标牌 +
    参与新消息信号）同步。
- **验证**：`biome check` ✅、`pnpm --filter luka typecheck` ✅、`vitest tests/lib/chats-play.test.ts`
  14 passed；整包 838 passed / 4 failed 全是工作区里其它未提交改动的预存失败（同上一条）。
  模拟器 `xcrun simctl openurl booted "luka://"` 回 Chats 截图确认：标牌在空墙上、未读角标 1
  与 All chats 行一致。
- **已知小瑕疵（未动）**：首次进入的「Tap someone to chat」气泡（一次性 3s）会短暂压住标牌
  上半部；若嫌挡可再调 `nest-playfield.tsx` 里 hint 的落点。
- **未覆盖**：点击进 Team、真机未走查；未提交（规范模式，等用户决定是否走分支/PR）。
- **→ 15:49 撤回**：用户「算了 撤回来吧」。四个文件全部还原 —— `nest-wall-frame.tsx`
  用 `git checkout` 回到 HEAD 版（含隐形热区），`index.tsx` / `nest-playfield.tsx` /
  `chats-play.md` 逐处手改撤销（后两个只剩工作区里他人未提交的改动）。`pnpm --filter luka
  typecheck` ✅，模拟器截图确认墙上标牌已消失、房间回到原状。

### 工作日志 — 15:57 Quick Pick 说明页：小崽 200 → 160 + 去掉 ScrollView（sitin-rn · apps/luka，未提交）

用户两条：「这个仓鼠需要再小一点」「这个页面不能出现滚动体哦」（截图是 iPhone X 上的
Quick Pick 开局说明页，`Start picking` 被挤出屏、页面可滚动）。

- `components/quick-pick/quick-pick-intro.tsx`：HERO 200 → 160（高 213）；外层 `ScrollView`
  换成固定 `View`（`flex-1`，整页不再滚动）；小崽容器 `height: HERO_HEIGHT, flexShrink: 1`
  + 图片 `maxHeight: "100%"` + `contentFit="contain"` —— 矮屏只把它收小，CTA 不会被裁；
  版式微调 `mt-6 → mt-5`、`pt-7 → pt-6` 留出底部余量。
- 文档：`docs/quick-pick.md` §3.2 补「固定一屏、不用 ScrollView、小崽是唯一可收缩元素」。
- 验证：biome ✅、`pnpm --filter luka typecheck` ✅、quick-pick 两个测试文件 18 passed；
  模拟器 `luka://quick-pick?source=debug` 截图确认小崽变小、清单与 CTA 全在一屏内。
- 未覆盖：没在更矮机型上实测（只靠 flexShrink 逻辑兜底）；未提交。
- **→ 16:02 追加**：用户「按钮要置底」。`Start picking` 改为固定贴底 —— 清单卡后加
  `flex-1` 空段吃掉余量，footer 用 `paddingBottom: 24 + insets.bottom` 让开 home indicator
  （与结算面板同法）。说明页此前已看过（本地标记），验证时临时把 `loadQuickPickIntroSeen`
  改成 `return false` 取景，截完已还原（`services/quick-pick.ts` 已无 diff）。量截图：按钮
  下沿距屏底正好 174px / 3 = 58pt = 24 + insets.bottom(34)，确认贴底成功。

### 工作日志 — 16:05 Luka 真机调试：Xcode 27 下走传统通道重装（sitin-rn · 无仓库改动）

用户「我要在真机上调试」。设备 iPhone2-T（iPhone X / iOS 16.7.16 / UDID `8b20f709…bea1`）USB 已连。

- **关键发现（Xcode 27 新变化）**：`xcodebuild -showdestinations` **不再列出 iOS 16 设备**，`-destination "id=<40hex>"` 与 CoreDevice UUID 两种写法都报 `Unable to find a device matching…`；devicectl 依旧不支持 iOS 16（pairing unsupported、capabilities 里无 Install Application）。**但传统通道仍可用**：`-destination 'generic/platform=iOS'` 可解析（`.devx` + `39CFYH6W55` + deployment 16.4 + iphoneos27.0），构建/签名/安装全部成功。
- **当前工程状态**：`apps/luka/ios` 是 09-18 prebuild 的 `.devx` + 公司 Team 版本，app.config.ts 此后无改动 → 不需要再 prebuild/patch；只补 `HEYHRU_BYTEPLUS_NATIVE=1 EXPO_USE_PRECOMPILED_MODULES=0 pod install`（RangersAppLog 等真机 pod 回归，25 处）。
- **构建**：`xcodebuild -workspace Luka.xcworkspace -scheme Luka -configuration Debug -destination 'generic/platform=iOS' -allowProvisioningUpdates -allowProvisioningDeviceRegistration -derivedDataPath build DEVELOPMENT_TEAM=39CFYH6W55 CODE_SIGN_STYLE=Automatic build` → **BUILD SUCCEEDED**（约 6 分钟，arm64 全量编 pod）。签名含 `application-identifier` / `aps-environment` / `get-task-allow`。
- **安装**：`ios-deploy --bundle ios/build/Build/Products/Debug-iphoneos/Luka.app --id 8b20f709… --no-wifi` → `[100%] InstallComplete`；`--exists` 返回 true。
- **描述文件核对**：公司 Team 的 Development（`com.lukasoc.luka.devx` 系，exp 2027-09-16）与 Ad Hoc（exp 2027-08-30）都含 iPhone X 的 UDID（各 9 台），不需要 `-allowProvisioningDeviceRegistration` 也能签。
- **注意**：以后**不要跑 `pnpm luka:ios:device`** —— `prebuild --clean` 会把 bundle ID 还原成 `.dev`（公司 Team 冲突）并重全量编译；模拟器侧下次跑 `pnpm luka:ios` 会自动 `ensure-simulator-pods.sh` 摘掉 RangersAppLog。
- **沉淀**：追加到 [ios-device-free-team-debug.md](../../30-Resources/troubleshooting/ios-device-free-team-debug.md)「2026-09-20 追加：Xcode 27」一节。
