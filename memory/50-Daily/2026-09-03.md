---
title: 2026-09-03
date: 2026-09-03
tags: daily
---

# 2026-09-03

## 今日重点

- [ ] koda 张峥名下剩余 bug 收尾（R00222 进度条、R00232 UI、R00240 文案、R00249 归后端）

## 工作日志

### 02:10 — koda R00252 注册填名页 Android 返回退桌面（PR #365）

- **做了什么**:修 R00252——注册流点「Start your story」进填名页后，Android 硬件返回键直接退回桌面，预期回启动页。
- **根因**:`login.tsx`/`sign-in.tsx` 经 `router.replace("/onboarding/name")` 进 onboarding，`name`（step 1，无返回按钮）成为导航栈根；硬件返回无处可退 → 退桌面。
- **修法(第一版，错误)**:`name.tsx` 用 `useFocusEffect` + `BackHandler` 拦截 `hardwareBackPress`，改为 `router.replace("/login")`。→ **不生效**:测试反馈「name 页无法返回启动页」。
- **修法(第二版，正确)**:根因更深——`app/_layout.tsx` 用 `Stack.Protected` 按登录态挂载路由：`login`/`sign-in` 仅 `status==="guest"` 挂载，`onboarding` 仅 `status==="authed" && !profileComplete` 挂载。`execute("welcome")`(设备快登)后 session 已是 **authed**，login 屏被卸载，`router.replace("/login")` 目标路由不存在 → 无效。改为 `hardwareBackPress` 时调 `useSessionStore.logout()` 登出临时 guest 会话 → status 回 `guest` → 守卫卸载 onboarding、挂回 login → **自动回启动页，无需手动导航**。
- **验证**:lint ✅ / typecheck ✅ / boundaries ✅ / `koda:test` 280 passed。
- **PR**:#365 已改为 R00252 + Edit Profile 合并版（把 #364 的 commit cherry-pick 进来，关闭 #364）。→ `feature/koda-android`（待 merge）。history 保持 2 条干净 commit（`git commit --fixup` + `GIT_SEQUENCE_EDITOR=: rebase -i --autosquash <merge-base>`，非交互 squash）。

### 02:50 — koda Edit Profile 保存 height 清空 location（并入 #365）

- **现象**:刚注册完 profile 有 location；改 height 保存回来后 location 没了。
- **根因**:#364 的 merge-preserve 覆盖了 displayName/age/gender/occupation/education/bio/height/interests，**漏了 location/locationName/zip**。改 height 走 `editProfile → refreshSelfProfile()`(preserveLocation=false)，`...mapped` 直接铺 `toProfile` 的 location；后端 `UserInfoRequest` 不回 `geoLocation` 时 `toProfile` → `location:null / locationName:"" / zip:""`，把本地 location 覆盖清空。（`preserveLocation` 段只在 `editLocation` 路径开，救不了 editProfile 路径。）
- **修法**:`user.ts` 把 location/locationName/zip 也加入 merge-preserve，以 `info.geoLocation` 是否返回为准：`location: info.geoLocation ? mapped.location : (current?.location ?? mapped.location)`（zip/locationName 同理）。尾部 `preserveLocation` 段保留（true 时覆盖）。加回归测试「改 height 且 refresh 省略 geoLocation → location 保留」。
- **验证**:lint ✅ / `koda:test` 281 passed。已 squash 进 #365 的 Edit Profile commit（fixup+autosquash），force-push。
- **补强(15:53 截图反馈「还是有问题」)**:原 `info.geoLocation` 真值判断只挡「省略」，挡不住后端返回**空 `geoLocation` 对象**（present-but-empty）。改为算 `refreshHasLocation = Boolean(geo?.city||province||zipCode||lat||lng)`，两种情况都保留本地 location。回归测试用 `it.each`（undefined + `{}`）覆盖。`koda:test` 282 passed。⚠️ 该修复仅在 PR #365 分支，**未合并、未出新包**——测试同学看到的仍是旧构建，需重新出包才能验证。

### 03:10 — #365 重开为 #366（基于最新 feature/koda-android）

- **为什么**:#365 分支 base 是 #360，落后 `feature/koda-android` 10+ 条（含大整合 merge + 新增 `@heyhru/rn-tracking` 包）。用户要求重起分支把改动搬过去。
- **做法**:`git checkout -b fix/koda-onboarding-editprofile origin/feature/koda-android` → cherry-pick #365 的两条 commit（R00252 + Edit Profile），无冲突 → **`pnpm install`**（新 base 引入 `@heyhru/rn-tracking`，不装 typecheck 报 TS2307 Cannot find module）→ lint/typecheck/boundaries/`koda:test` 284 passed → push → 开 #366 → 关闭 #365。
- **坑**:切到前进过的分支后 typecheck 报 `Cannot find module '@heyhru/rn-tracking'` = 新 base 加了 workspace 依赖但没重装。**换 base 后先 `pnpm install` 再 typecheck。**
- **PR**:#366 → `feature/koda-android`（含 R00252 + Edit Profile 全套修复，含 location 补强）。待 merge + 出新包验证。

### 坑（沉淀）

- **Expo Router `Stack.Protected` 登录态守卫下，跨守卫组的路由导航会被吞。** koda 根布局按 `status`(guest/authed) + `profileComplete` 分组挂载屏；`router.replace("/login")` 在 authed 态下无效（login 组已卸载）。**要回到 guest-only 的启动页，必须改状态（`logout()`）让守卫重挂，而不是导航到那条路由。** 判断：目标屏在哪个 `Stack.Protected` guard 下，当前 status 是否满足该 guard；不满足则导航不会到达。
- **判断某页是否导航栈根**：看进入它的是 `push` 还是 `replace`；栈根的 Android 硬件返回无处可退会退桌面。
- **非交互 autosquash**：`git commit --fixup=<sha>` 后 `GIT_SEQUENCE_EDITOR=: git rebase -i --autosquash <base>`（编辑器设 `:` 空操作），可在无 `-i` 交互环境下把 fixup squash 进目标 commit。base 用 merge-base 避免撞上目标分支的新提交。
