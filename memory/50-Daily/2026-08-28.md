---
title: 2026-08-28
date: 2026-08-28
tags: [daily]
---

# 2026-08-28

## 工作日志 — 10:16 整理 8.24–8.28 工作并写前端周报

- **做了什么**:把 8.24–8.28 张峥工作从 memo(50-Daily 08-24~27)汇总成 **5 大类 + 23 子任务**,填进「8月28日前端周会」文档张峥的「本周工作」多维表格(bitable `TkWfb8dAtaAjsTsP0E9cDkXUncd` / table `tblmRL8uLtH1kTxt`)。用户定「只要一到五」(去掉分支/proto 维护那类)。
- **五大类**:①多社媒 SP V2 reland(revert #1027 + reland #1028 + Social Connect #1040 + CE 按平台隔离 + online-pwa-pre #1029)②PWA 权益池 Minerva 后台(小美周字段/dms 多库/特征库口径/定向邀请待接收/三池列,PR#966)③直播 Go Live 开播(WS→HTTP/clickTime/须知抽屉,→test-pwa3)④消息金+定向邀请 nationwide(暴击价/msgReward/3K Club/冷启弹窗,→test-pwa)⑤Snapchat/IG 脚本(封面帧/回复取正文/commentOnPost/图片压缩 PR#1026)。
- **流程**:先删表里上周残留(建时已被并发清成 1 条空记录)→ batch-create 5 父(记 record_id)→ batch-create 23 子(父记录 `[{"id":"rec..."}]`)→ **默认 markdown 格式 record-list 核对**(JSON 格式坏,只出 1 条;核对总 28 条、层级对)。
- **坑**:①lark base flag 是 `--base-token` 不是 `--app-token`;②`record-list --format json` 输出坏(显 1 条),核对必须用默认 markdown 格式;③状态字段写字符串「已结束/进行中」即可(选项已存在);④表被并发会话清空过(周会 prep),写前先核对当前记录数。

### HH:MM — 新建 feature/pwa-beauty-live = feature/pwa + 美颜 + live
- 从 origin/feature/pwa(28c90ea72)切 feature/pwa-beauty-live。
- 合 feat/pwa-beauty(美颜,+25):无 proto/gen 冲突,只 2 app 冲突取并集(PreLiveView imports=GoOnlineTutorial+BeautyEntryButton;sitin4Debug=美颜块+IG助手)。164e00ca2。
- 合 personal/zz/live(+32):24 proto/gen + 1 app(PreLiveView)冲突。proto 用 live 的 7dd(含 LiveCanGoLive)regen 解 gen;PreLiveView 取 feature/pwa 版(goLiveNow 直开、去 3s 倒计时、Go Live)——live 旧 pre-live 倒计时被 feature/pwa 直开取代(feature/pwa 本就删了)。live 的 EarnModule+门控+GoLiveWarningSheet+startHostLive 全保留。app-pwa tsc PASS。7d102ed2e,push -u。
- 复用:gen 冲突不手工 merge —— 定 proto 指针(submodule checkout <sha> + update-index --cacheinfo)后 `pnpm proto:gen` 重生成再 git add;语义冲突(倒计时 vs 直开)按"哪方是较新重构、另一方引用已删符号会编译失败"判定取舍。

## 工作日志 — 01:06 release/test-pwa3 重置为 feature/pwa 再合入 beauty-live

- **背景**:beauty-live 直接合 test-pwa3 有 12 个 app 冲突;查证发现其中 **10 个核心冲突源自 feature/pwa↔test-pwa3 本身大幅分叉**(mock 通话/任务引导/AB/Live),不是 beauty/live 引入(beauty-live 只多带 BeautyPanel+sitin4Debug 2 个)。用 `git merge-tree --write-tree --name-only` 只读比对得出。
- **决策(用户定)**:放弃解冲突,直接把 **test-pwa3 重置成 feature/pwa** 再快进合 beauty-live。
- **做了什么**:
  1. `git push origin fb960fe08:refs/heads/backup/test-pwa3-before-reset-20260828` 备份旧 test-pwa3(领先 feature/pwa **178 提交**,cjkun 115/xueyangeng 31/colna 18/cjkunwang 11/Max 3)。
  2. `git push --force origin origin/feature/pwa:release/test-pwa3` → test-pwa3 = feature/pwa 28c90ea72(那 178 提交作废,已备份)。
  3. `git push origin origin/feature/pwa-beauty-live:release/test-pwa3` **快进合并零冲突** → test-pwa3 = beauty-live **7d102ed2e**(+美颜+直播+LiveCanGoLive 门控,proto 7dd93d5d)。
- **结果**:test-pwa3 远端 = 7d102ed2e。全程 `--no-verify`(pre-push turbo test 被无关包拖累,sitin-next 记录例外)。提交人 colna。
- **坑/注意**:①force push 前**先推 backup 分支**保命,可 `git push --force origin backup/...:release/test-pwa3` 恢复;②本地 release/test-pwa3 分支仍停旧 fb960fe08(共享 checkout,别的会话在用)——**绝不能 push 本地这个 stale 分支**,会把远端退回;③需通知 cjkun/xueyangeng:`git fetch && git reset --hard origin/release/test-pwa3`,否则他们 pull 会把 178 提交带回。

## 工作日志 — 16:00 修语音违规正则子串误报(PR #1053)

- **问题**:feature/pwa `useVoiceModeration` 违规检测正则对语音转写做 `pattern.test(text)` 子串匹配,单词关键词没加 `\b` 词边界 → AI_PATTERNS 的 `AI`(+/i)命中任意含「ai」的词(said/rain/train/wait/again/email/fair…)误报 NON_HUMAN_DISCLOSURE;MONEY_PATTERNS 的 `earn`→learn/yearn、`paid`→unpaid 误报 MONETIZATION_INTENT。误报会误禁收益、弹违规窗。
- **修法**:`AI`→`\bAI\b`、`earn`→`\bearn\b`、`paid`→`\bpaid\b`。node 运行时验证:误判词全不命中,真违规(AI texting/earn money/I get paid/scammer)仍命中。tsc 增量 0、eslint 0。
- **产出**:PR **#1053**(base feature/pwa,author colna)。app-pwa 无测试框架,未加单测(follow-up)。

## 工作日志 — 16:08 语音违规正则全量加词边界(#1053 补强)

- **续上条**:用户指出「所有单词都该加 \b,为什么只加了几个」——对。第一版只修报出来的 AI/earn/paid,审计发现 scam→scamper/scampi、**not me→not mean/not mention/cannot mention(高频)**、money→moneymaker、pay me→pay menu、robot→robotics 仍子串误报。
- **修法**:两条多关键词正则的所有单词/短语统一加 \b。AI: `/\bAI\b|\brobots?\b|\bscams?\b|\bscammers?\b|\bnot me\b|\bauto replies\b|\bwasn't me\b/i`;MONEY: `/\bmoney\b|\bearn\b|\bpaid\b|\bget paid\b|\bmake cash\b|\bpay me\b|\bworking here\b|\bpaypal\b/i`。robot/scam 用 s? 兼容复数、scammer 单列。commit dc651a9,#1053 更新。tsc 增量 0、eslint 0。
- **教训**:修正则子串误报时要**审所有 alternative**,不能只补报出来的那个;短语也会子串泄漏(not me→not mention、pay me→pay menu)。

## 工作日志 — 16:23 把语音违规正则修复 cherry-pick 到 release/test-pwa3

- 把 #1053(useVoiceModeration 正则全量加词边界)的两个提交(42855d2e0+dc651a96c)cherry-pick 到 `release/test-pwa3`。该文件在 test-pwa3 与 feature/pwa 基线逐字节一致 → 干净无冲突。test-pwa3 未受保护,--no-verify 直推。test-pwa3 → `547820d10`。

## 工作日志 — 17:00 通话历史「查看对话内容」抽屉(创造模式,PR #1058)

- **需求**:app-minerva-web 通话历史页每行加「查看对话内容」→ 右侧抽屉,数据源 video_call_transcripts。创造模式:4 份文档(app-minerva-web/docs/call-transcript-drawer/)→ Phase→Task 单 commit。
- **关联键(坑)**:CallDataInfo 无 messageId;video_call_transcripts.message_id 是 TIM 消息 id 对不上订单 → 用 (from,to)=(male,female) 双向 + start_time ∈ [createTime-60s, createTime+(callDuration+120)s]、ABS 取最近一条。content_json=[{userId,parts:[{text}]}] 无逐条时间戳。
- **后端**(Fastify+getPgDb 原始 SQL,非 Prisma):minerva-schemas 契约 + business-minerva-call-feed(SQL/model/parseTranscriptContent 纯函数/callTranscript handler)+ app-minerva-server 路由+权限。**vitest 8 passed**。
- **前端**(umi+antd,无测试框架):api + CallTranscriptDrawer(气泡女右男左+三态)+ 页面操作列接线。eslint 0、tsc 零新增(51=51 预存 zod z.infer 债)。
- **worktree symlink 坑(重要)**:worktree 的 node_modules symlink 主 clone → 跨包依赖(minerva-schemas/call-feed)tsc 解析到主 clone 旧 dist。**改包后要 build 并把 dist 覆盖到主 clone 解析处**才能让消费方 tsc 过;feature 做完从各自源重建主 clone dist 还原。umi web tsc 需先  生成 .umi/tsconfig(含 jsx/paths),否则裸 tsc 全是 JSX/别名假报。
- **产出**:PR **#1058**(base feature/admin,author colna),7 Task 全绿。建议合并前浏览器实测抽屉。

## 工作日志 — 17:09 通话对话内容抽屉 cherry-pick 到 release/test-admin

- test-admin 与 feature/admin 大幅分叉(独有 1708/213),合整条分支会带巨量差异+冲突 → **只 cherry-pick 我的 5 个功能提交**(docs+T1.1~T1.3+Phase2)。独立 worktree 里 cherry-pick 干净无冲突。call-feed vitest 8 passed、eslint 0。test-admin → `51710f937`。
- **待部署**:后端接口(callTranscript)要生效需**重部署 minerva-server 到 admin-api-dev**(同 08-25 admin 系列)。

## 工作日志 — 17:31 联调 video_call_transcripts + 修 content_json 双重编码 bug

- **数据核对**(admin-api-dev POST /api/aliyun-dms/run + MINERVA token,dev archat):video_call_transcripts 共 **4550 行、3202 有内容**,最新 2026-08-28(在写)。from_user_id=男、to_user_id=女,message_id 是 TIM 消息 id。
- **发现 bug**:content_json 是**「数组套 JSON 字符串」双重编码** —— `["{\"userId\":..,\"parts\":[{\"text\":..}]}\", ...]`,每个元素本身是 JSON 字符串(字段驼峰 userId)。我原 parseTranscriptContent 只解外层数组、拿到字符串取不到字段 → 解析空。**修**:元素为字符串时再 JSON.parse 一次(兼容对象)。补双重编码测试(9 passed)。用真实行验证通过。
- **产出**:fix commit 2fe931e38 push 功能分支(PR #1058 更新)+ cherry-pick 到 release/test-admin 0e5927477。
- **DMS 坑**:aliyun DMS profile 只连到 test 的 archat_fdw(无此表);dev archat(10.51.1.3 内网,含表)只能走 minerva /api/aliyun-dms/run + MINERVA token 查。

## 工作日志 — 18:02 按 CR 加固 call-transcript(参数校验/解析/测试)+ 部署确认

- **CR 评审 6 条**:#1 参数无约束、#2 ZodError→500、#3 双重编码 null 崩、#4 索引、#5 duration 无上限、#6 测试太弱。逐条核实后:
  - **#2 前提不成立**:项目已有全局 `setErrorHandler`(error-handler.ts:101 `ZodError→400`),现有 callData 也是裸 `.parse` 靠它兜 → 生产 bad params 已是 400 不是 500,**handler 内无需 try/catch**。
  - **#4 用真实数据 EXPLAIN**:查询走 `idx_video_call_transcripts_from_user_id`(BitmapOr 双向)+ 时间/to_user_id 过滤,**0.049ms / 5 buffer hits** → 索引已命中,复合索引记 follow-up 不改。
  - **改了 #1/#3/#5/#6**:请求参数加 `int().positive()/nonnegative()/max(24h)`(#1+#5 界定时间窗口);parseTranscriptContent 双重编码分支加**非空对象守卫**(元素为 "null"/"[]"/原始值/坏串 → 降级 {userId:0,text:""} 不抛 TypeError),parts[].text 非字符串忽略(#3);测试改 **mock findCallTranscript**,覆盖缺/非法参数→400、命中→解析+时间窗口断言、无匹配→found:false + 脏数据用例(**11 passed**)。
- **踩坑(测试 dual-zod)**:测试里 `err instanceof ZodError` 为 **false**(`err.name` 却是 ZodError)——worktree copy-dist 下 schema 的 zod 与 test 的 zod 非同一实例。生产 `_koa-perm` handler 本就用 `err.name==='ZodError'` 规避;测试 mini handler 也改按 name 判定。**教训**:跨包/copy-dist 场景 instanceof 不可靠,ZodError 判定用 name。
- **产出**:commit `2261b5487` push PR #1058;cherry-pick `1bf27a951` 到 release/test-admin。tsc/eslint 全过。改后重建主 clone minerva-schemas dist 还原、清 worktree。
- **部署结论(回答用户)**:光 cherry-pick 到 test-admin **不自动部署**——`merge-to-release-test.yml` 触发条件是 push `feature/admin*`(自动 merge test-admin + 触发 Jenkins 部署 minerva-server 到 dev)。要生效:① 后端 minerva-server → admin-api-dev(合 PR #1058 到 feature/admin 触发,或手动 Jenkins `deploy-servers/sitin-next/dev` BRANCH=release/test-admin);② 前端 app-minerva-web **不在 workflow 的 ALLOWED_APPS**(只后端),要单独部署(OSS bucket admin-dev-sitin-ai,build:dev+deploy:dev)。
- **联调数据**:最近有内容且可测的行 id=5572(男 2100065404/女 2100063060,08-28 06:20,4 条 Right./Right./Music./You.)。
