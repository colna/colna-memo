---
title: 2026-07-31
date: 2026-07-31
tags: daily
---

# 2026-07-31

> 本文件起时间口径为**北京时间**(UTC+8);历史(≤2026-07-30)为机器本地(美中)时间,详见 [CONVENTIONS 时间口径](../CONVENTIONS.md)。

## 今日重点

- [ ] 探针自拍超时:后端补 `taskType==PROBE` 分支(清 probe、不暂停绿灯收益)+ `handleOutboundMessage` 识别 `replyKind:probe` 走 `handleProbeComplete("completed")`——需给对应 owner 出改动说明(结转自 07-30)

## 工作日志

## 工作日志 — 15:23 memo 时间口径改北京时间(仅往后 + 历史加说明)

- **做了什么**:确立「往后所有 memo 一律记北京时间(UTC+8)」约定,历史原样不动。改两处:① `CONVENTIONS.md` 新增「时间口径」章节(含取数命令 `TZ=Asia/Shanghai date` + 历史 +13h/+14h 换算);② `.agent-memory/memory.md` 追加同款行为约定。本文件即首个北京口径 Daily。
- **为什么**:本机时钟是美国中部(CDT/CST),此前 memo 日期/时间都是美中时间;用户要求改北京时间,便于阅读对齐。
- **为什么历史不改**:摸查全库发现历史 memo 绝大多数只有日期、没有 `HH:MM`(带时间的不到 100 条,无时间的几百条),无法可靠判定 +13h 是否跨天来重定日期,机械改会破坏真源 → 选「往后北京 + 历史加换算说明」的零风险方案(用户拍板)。
- **命令**:`TZ=Asia/Shanghai date "+%Y-%m-%d %H:%M"` 取北京时间;校验:机器 02:23 CDT = 北京 15:23。
- **残留**:07-30 那条我写的 `21:xx` 占位、07-26 的 `HH:MM(占位)`/`现在` 属历史,按约定不回改。

## 工作日志 — 15:59 sitin-next2:新增 Snapchat checkLoginStatusIIFE bootstrap 登录检测

- **做了什么**:在 `sitin-next2`(分支 `personal/zz/sp-snapchat`)参照 IG 的 `instagram/actions/checkLoginStatusIIFE.js`,实现 SC 版 `snapchat/actions/checkLoginStatusIIFE.js`。逻辑:应用启动短轮询 `SocialProxy.checkLoginStatus()`,命中 `type=0`(已登录)立即上报,3 次仍 `type=1` 就报未登录。接线 `scripts/build.mjs`(新增 SC `bootstrap` group)+ `scripts/upload-all.mjs`(bootstrap 映射),补 `docs/snapchat-plan.md`。
- **为什么**:SC 脚本套件缺 bootstrap 登录检测,对齐 IG。
- **三处 SC 差异(关键)**:① 上报端能力同 IG 的 `notifyPageAbnormal`,但 SC 的 `bridge.js` 把 `window.JSBridge` 覆盖成 JS 包装层 `SocialProxyBridge`,原生方法只挂在 `window.AndroidBridge` 上 → IIFE 走 `window.AndroidBridge.notifyPageAbnormal`;② SC 的 checkLoginStatus 只返 0/1(无封禁态 type=2),去掉该分支;③ SC 无"保存登录信息"弹窗,删掉 IG 版的 `tryCloseDialog`(其依赖的 `utils.LABELS.close` 在 SC 字典里也不存在)。日志改用 `SocialProxyBridge.log` 而非裸 console。
- **结果/命令**:`node scripts/build.mjs` → `dist/snapchat/bootstrap.js` 4 脚本合并成功;新文件单独 eslint 干净(初版 `document` 未用报错,已从 `/* global */` 移除);`pnpm lint` 全量 51 task 全过 0 error。commit `f0d861ab8`,已 push `origin/personal/zz/sp-snapchat`(colna 身份,SSH `github-colna`)。
- **待确认**:`AndroidBridge.notifyPageAbnormal` 前提是 SC 的 Android WebView 暴露了同名 `@JavascriptInterface`(SC 现有脚本此前只用过 `uploadMessages`/`onResult`/`event`,没用过它),需端上核对;方法名不同只改 `notifyAPK` 一处。

## 工作日志 — 16:29 sitin-next2:新增 Snapchat watchLoginComplete + watchLoginCompleteIIFE

- **做了什么**:接着上一条,参照 IG 的 `watchLoginComplete.js` / `watchLoginCompleteIIFE.js`,实现 SC 版长时登录监听。`watchLoginComplete` 轮询 `checkLoginStatus`,**命中 welcome 页(`accounts.snapchat.com/v2/welcome`)且拿到 username → 成功**(返回 username/displayName/avatar/source);老用户直接落 chat 页(无资料卡、无 username)则等 5s 降级返回,避免无限等待。IIFE 走 `SocialProxy.execute("watchLoginComplete")` 调度,pending/orphan-state 判断交给 executeAction 的 recovery 做跨页面续传。接线 `build.mjs` SC `login` group(补 executeAction/watchLoginComplete/watchLoginCompleteIIFE)+ `upload-all.mjs` SC map,补 `snapchat-plan.md`。
- **关键判据(用户定)**:Snapchat 登录流程 = 输入密码 → 直接跳 `/v2/welcome`。welcome DOM 真源 `dom/snapchat/welcome.html`(用户本轮新增):`data-testid="username"` = `colna_zheng`(即 @handle/snapchatId)、`UserProfileCard_displayName` = `COLNA`、`data-testid="snapCodeImage"` src 带 `?username=colna_zheng`。页面无独立数字 snapchatId,username 即 Snapchat ID。现有 `checkWelcomePage` 已能抓 username,无需改 checkLoginStatus。
- **无需 DOM 的判断**:两个脚本都架在 `checkLoginStatus` 之上(它已封装 SC 登录 DOM 信号),watchLoginComplete 仅循环调它 + sessionStorage 续传;IIFE 纯编排(executeAction + sessionStorage),零 DOM。SC executeAction 用 `SP[action]` 派发、`storePending`/recovery 齐全(executeAction.js:300/367),`execute("watchLoginComplete")` 可正常派发到 `SP.watchLoginComplete`。
- **SC 差异**:无"保存登录信息"弹窗 → 删 IG 版 dismissDialog;SC 无 `userId`(无 cookie 用户 ID)→ 返回值去掉,改带 displayName;`window.jsBridge.onLoginDetected` 为可选回调,端无此方法则静默跳过(主通道 executeAction 的 `onResult` 一定生效)。
- **结果/命令**:`node scripts/build.mjs` → `dist/snapchat/login.js` 6 脚本合并成功;两个新文件 eslint 干净;`pnpm lint` 全量 51 task 全过 0 error。commit `38d791e8a`,已 push `origin/personal/zz/sp-snapchat`。

## 工作日志 — 17:08 sitin-next2:watchLoginComplete 简化为 welcome-only(删降级分支)

- **做了什么**:用户澄清「checkLoginStatus 只在 welcome 页跑、watchLoginComplete 最终也只停在 welcome 页」→ 确认 chat-page 降级分支是死代码。简化 `watchLoginComplete.js`:删掉 partial/degrade 全套(`PARTIAL_FAST_TRACK_MS`/`PARTIAL_POLL_INTERVAL`/`partialFound`/`partialFoundAt`/`shouldDegradeNow`/`resolveDegraded`),`tickOnce` 收敛成「拿到 username → 成功,否则按 1.5s 继续轮询等资料卡渲染」。state 从 `{startedAt,partialFound,partialFoundAt}` 精简为 `{startedAt}`,保留 login 页→welcome 页跳转的续传。同步改 `snapchat-plan.md`。
- **为什么死代码**:只在 welcome 跑时,`isLoggedIn=true` 必然伴随 username(资料卡),不存在「已登录但无 username」的 chat-page 场景 → 降级分支永不触发。
- **未动**:`checkLoginStatus.js` 的信号 B(`checkChatPage`,`web.snapchat.com` 好友列表)用户没提,保留(理论上也是死代码,如全局只在 welcome 跑可后续再删)。
- **结果**:`node scripts/build.mjs` → `SC:login.js` 6 脚本合并成功;`watchLoginComplete.js` eslint 干净。commit `25388c814`(-98/+14),已 push `origin/personal/zz/sp-snapchat`。
- **踩坑**:验证 eslint 时把输出 pipe 给 `grep -v`,`$?` 取到的是 grep 退出码(无输出=1)而非 eslint 的,误判有错;应 `eslint file 2>/dev/null; echo $?` 直接取 eslint 退出码。

## 工作日志 — 14:32 整理 2026-07-30 Daily

- **做了什么**:整理昨日 Daily;结转 1 条未完成待办,将 Partnership Ads 最新实现与任务状态语义同步到项目笔记,将 app-ins-scripts 上传白名单机制同步到脚本容器项目笔记,并补充 lark-cli 单 app 单 user token 的鉴权说明。
- **为什么**:执行工作区“每天第一次任务先整理前一天”的约定,避免昨日结论只留在 Daily。
- **结果**:沉淀 / 更新 3 处知识,复制 1 条待办,归档 0 段;昨日已闭环工作日志原文保留。
- **关键命令 / diff**:`rg` 检索既有笔记并去重;更新 `10-Projects/partnership-ads-pwa.md`、`10-Projects/social-proxy-scripts-container-app.md`、`30-Resources/troubleshooting/lark-cli-auth-and-perms.md`。
- **沉淀**:见上述项目与 troubleshooting 笔记。

## 工作日志 — 14:50 审查 metabot-workspace AGENTS.md 与实际环境一致性

- **做了什么**:逐条核对工作区路径、Agent 工具、skills、Git / gh 身份、lark-cli profile、知识库 CLI 与规则内部约束;未修改 `AGENTS.md`。
- **为什么**:用户要求确认规则是否存在与当前 Codex / MetaBot 运行环境不符之处。
- **结果**:确认多处高优先级不一致:长期记忆目录缺失、旧机器路径、Claude/Codex 工具与配置混用、Git includeIf / SSH alias 机制不存在、lark-cli 当前 app 与 user 身份不符、创造模式 commit/push 与全局禁令冲突、通用 pnpm/Vitest 流程不适配多语言项目。`colna sync` 首次全量索引运行约 13 分钟后占用约 29.5 GB physical footprint、28.6 GB swap 且索引文件无进展,已终止自启动进程;知识库改动未同步。
- **关键命令 / diff**:`rg` / `nl` / `git config` / `gh api user` / `lark-cli config show` / `lark-cli auth list` / `lark-cli doctor` / `ps` / `vmmap`;`cargo build`;`./colna sync`(异常后中断,exit 130)。
- **沉淀**:应先修 `AGENTS.md` 的运行时身份、路径、工具名和副作用边界;另需排查 colna-memo 全量索引内存膨胀。

## 工作日志 — 14:55 Git 改用本机 SSH 约定

- **做了什么**:将 `metabot-workspace/AGENTS.md` 的 Git 规则从旧 `includeIf + github-colna alias` 改为遵循仓库现有 remote、使用本机 SSH 配置与密钥;同步在 `colna-sync.md` 标注当前约定。
- **为什么**:用户明确指定 Git 使用本机 SSH,且实测本机 `github.com` 已通过 SSH config 使用 `ssh.github.com` / `~/.ssh/id_rsa`,旧机器路径与 alias 均不存在。
- **结果**:保留 colna 提交身份要求;不再强制改写 remote,不依赖工作区专用 `includeIf` 或 `github-colna`。
- **关键命令 / diff**:`ssh -G github.com`;`git config --global user.name`;更新 `AGENTS.md:174`。
- **沉淀**:Git 传输身份以本机 SSH 配置为准,提交作者以仓库实际生效的 `user.name` / `user.email` 为准,两者分开核验。

## 工作日志 — 15:25 修订 AGENTS.md 运行规则并发起本机飞书登录

- **做了什么**:删除 AGENTS.md 的旧 bot / Claude 叠加说明、`.agent-memory` 章节、旧 Agent 工具名和 TodoWrite 规则;把默认项目探索范围改为 `/Users/colna/WORK/`;Daily 日期与时间统一为北京时间;飞书身份改为使用本机当前 lark-cli profile。
- **为什么**:用户指定删除审查中的第 1、2、5 项旧规则,并要求修正 WORK 范围、Memo 时区及本机飞书登录方式。
- **结果**:规则修改完成并通过静态复核;北京时间命令实测有效。本机 active profile 为 `cli_a929bdd9c578dcba`,两次 device authorization 均未完成,最终 `auth list` 仍无登录用户,没有 token 被写入或覆盖。
- **关键命令 / diff**:`env TZ=Asia/Shanghai date`;`lark-cli profile list`;`lark-cli auth login --domain im --recommend --no-wait --json`;删除固定 app/profile/open_id。
- **沉淀**:飞书操作前按本机 active profile 核对用户身份;用户未授权走 auth login,应用后台未启用 scope 则先配置应用权限,两者不能混为一谈。

## 工作日志 — 15:46 完成本机 lark-cli 张峥授权

- **做了什么**:为 active profile `cli_a929bdd9c578dcba` 发起 IM 最小权限 device flow,用持久 `screen` 会话保持后台轮询;用户授权后核对日志、`auth list` 与 `auth status --verify`。
- **为什么**:普通后台子进程会被执行器回收,此前链接因此失效;本次需让轮询跨消息持续存活。
- **结果**:张峥登录成功,token `valid` 且服务端 `verified: true`。浏览器最终页虽报 20001,不影响已完成的 token 交换。当前缺少 `im:message.send_as_user`,上线申请暂不能按 user 身份发送。
- **关键命令 / diff**:`screen -dmS metabot-lark-auth ...`;`lark-cli auth list`;`lark-cli auth status --verify`;`lark-cli auth check --scope 'im:message.send_as_user'`。
- **沉淀**:浏览器最终页不是 device flow 成败真源;以 CLI token 日志和服务端 verify 为准。详见 `30-Resources/troubleshooting/lark-cli-auth-and-perms.md`。

## 工作日志 — 15:54 更新 lark-cli 与配套 Skills

- **做了什么**:将全局 `@larksuite/cli` 从 `1.0.28` 更新到 `1.0.80`,并从官方仓库重新安装 27 个 lark Skills。
- **为什么**:CLI 检测到新版本,用户明确要求更新;官方升级流程要求 CLI 与 Skills 同步更新。
- **结果**:`lark-cli --version` 与 npm 全局包均显示 `1.0.80`;Codex 使用的 `~/.agents/skills/lark-*` 已更新。升级后 active profile 仍为 `cli_a929bdd9c578dcba`,张峥 user token 与 bot 身份均为 `verified: true`,无需重新登录。
- **关键命令 / diff**:`npm update -g @larksuite/cli`;`npx skills add larksuite/cli -g -y`;`lark-cli profile list`;`lark-cli auth status --verify`。
- **备注**:Skills 安装器报告的 PromptScript 全局安装不受支持不影响 Codex;因 colna 全量索引存在已知高内存问题,本次未运行 `colna sync`。

## 工作日志 — 17:25 修复 colna-memo 索引内存与同步完整性

- **做了什么**:将 embedding / zvec 写入改为每批 16 chunks 串行;新增 state v2、doc_count / schema / vector completeness 门禁、zvec optimize、按 chunk 主键幂等删除;重排 sync 为 commit memory → pull --rebase --autostash → reindex → push,并在 pull 前后检查 Git 冲突态。
- **为什么**:旧版全量索引因 fastembed Rayon + ONNX 线程叠加占用约 29.5 GB,且部分 index / 旧 state 可能误报完成;pull 失败与 autostash 恢复冲突也可能被吞掉。
- **结果**:真实全库 108 files / 1355 chunks 在 107.35 秒完成,最大 RSS 2.04 GB、swap 0;中断后能自动全量恢复。临时库 add/change/remove 与混合搜索通过;fresh-context review 后补齐 `text` FTS / `embedding` HNSW schema 存在性校验,18 项测试、fmt、Clippy、release build 全过。源码保留为未提交改动,仅 memory/ 按授权走 `colna sync`。
- **关键命令 / diff**:`/usr/bin/time -l env COLNA_PROFILE=release ./colna index`;`cargo fmt --all -- --check`;`cargo clippy --all-targets -- -D warnings`;`cargo test --all-targets -- --test-threads=1`;核心改动 `src/embedder.rs`、`src/main.rs`、`src/store.rs`、`src/state.rs`、`src/gitsync.rs`。
- **沉淀**:见 `30-Resources/troubleshooting/colna-sync.md` 与 `10-Projects/colna-memo.md`。
