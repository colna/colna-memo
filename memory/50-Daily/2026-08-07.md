---
title: 2026-08-07
date: 2026-08-07
tags: daily
---

# 2026-08-07

> 本文件由 `2026-08-06.md` 拆出整理:以下条目按 **commit 北京时间**实际发生在 08-07(commit 京时 11:33~19:30,全 sitin4.1)。原因是 08-06 晚的会话跨午夜续写到了 06 号文件。条目标题里的 HH:MM 是当时机器本地(US Central)时间,非北京;对应 commit 北京时间见下表小节顺序(08:15=京19:30 … 01:40=京11:33)。

## 工作日志

### 08:15 sitin4.1 PR review + 设置页 Link Social Media(commit 77c2041f5)

- **review 全 PR 找不对齐点**(用户要求),结论:🔴 Settings 只有 Link Instagram 无 SC 入口、InsExchangeBubble Follow 未登录只 toast 不弹登录;🟡 两套「登录」判据并存(完单 gate=isPlatformLoggedIn loggedInByPlatform / 展示=selectPlatformUsable)、useIns.ts 死代码、InsExchangeBubble:356 注释说反;⚪ CE 任务跨页可见、抽屉 ¢/无倒计时(已 flag)。
- **修 #1(commit 77c2041f5)**:`Settings.tsx`「Link Instagram」→「Link Social Media」,状态读 authByPlatform(任一已授权=Linked,不再看 insId),点击 `showSocialAuthDrawerFromStore({force:true})` 弹小抽屉;移除 useTask/TaskId/useUserStore。`showSocialAuthDrawerFromStore` 加 `force`:默认仅 expired 才弹(不变),force 跳过门槛(有未连接即弹),全连接 toast「All social accounts are connected」。
- **follow-up 未做**:review #2(bubble 弹登录)、#3/#4/#5(口径统一/删死代码/改注释)、点击主动重探(checkSocialProxyPageAbnormal 需抽成独立可调用)。
- **验证**:tsc/eslint 干净;pre-commit/pre-push 过。77c2041f5 已推。


### 07:40 sitin4.1 Win Free Cash 卡按登录态分流 + 冷启动按平台拉小崽(commit 2c273a014 / c0367cb2a)

- **卡片分流(2c273a014)**:用户给两张图(PausedCard「AI earnings paused」=单登录态 / AuthorizeSocialCard「Earn While You Chat」两按钮)。需求:有一个社媒登录成功→PausedCard;都没登录成功(两个未登录/一个未授权一个未登录)→AuthorizeSocialCard;未登录(已授权掉线)文案=Reconnect,未授权=Authorize。改 `AiGoldCashoutCard`:判据从旧 IG 中心(needsInsAuth/insAuthorized/insPageAbnormal/hasBoundIG)换成 `selectPlatformUsable`(已授权且非异常,与 App.tsx 同口径);`anyOnline=igUsable||scUsable`;PausedCard pill 与 AuthorizeSocialCard 按钮各读 authByPlatform 决定 Reconnect/Authorize。删 useTask/useUser/TaskId 引用。
- **小崽对齐(c0367cb2a)**:用户问「SC 探测到已登录(checkSocialProxyPageAbnormal code0/type0)为什么小崽没开」。根因:`checkSocialProxyPageAbnormal` 只探测不开小崽;冷启动唯一自动拉小崽的 `initInsFollowedUsers` 是 **IG-only**——闸用 IG 异常态一刀切 return、`startRobot(EMPTY_ROBOT_MSG)` 无 cardType 默认 IG,没有拉 SC 分支。修:新版 APK 分支改按平台各拉——`selectPlatformUsable(s,"instagram")`→拉 IG,`selectPlatformUsable(s,"snapchat")`→`startRobot({...EMPTY, cardType: platformToCardType("snapchat")})`;insAbnormal 闸下放到仅旧 APK 路径(getPwaFollowedUser 是 IG 专属)。
- **口径备忘**:`checkSocialProxyPageAbnormal` 返回 `{code,type}`:code 0=探测成功,type 0=正常/已登录、1=未登录、2=被封、3=被禁言。探测 ≠ 拉小崽,小崽=startSocialProxyRobot 悬浮窗,按需拉(授权成功/待处理订单/冷启动 usable)。
- **验证**:tsc/eslint 干净;pre-commit/pre-push 过(commitlint 又拦 Win 大写开头,改中文)。两 commit 已推。


### 07:05 sitin4.1 社媒授权任务改为不阻塞输入(commit beeaedcd8)

- 用户「不用阻塞」——推翻之前需求里的「阻塞流程」。去掉 Chat/index 里抽屉打开时的输入阻塞:footer disabled / deferAutoSend 恢复原样、ProbeTaskDrawers defer 恢复 showRiskSheet、删 socialAuthDrawerOpen 状态。抽屉仍照常弹、可关闭,仅让位风险单。
- **CE 交换任务当前状态汇总**(commit 链 1b6e5adf5→db67e1080→6fe3b65fd→31288f24d→beeaedcd8):Figma 样式对齐;两种订单都可关闭;男主动倒计时(标题行右,到 0 消失);不阻塞输入;banner 排除;/debug 两场景。**仍缺**:「红黄绿跳转时可见」跨页 banner/Home 卡(follow-up,待设计)。
- **验证**:tsc/eslint 干净;pre-commit/pre-push 过。beeaedcd8 已推。


### 06:55 sitin4.1 CE 交换任务抽屉按最新需求修正(commit 31288f24d)

- **需求变更(用户给全量)**:① 男用户发起、女方未登录对应社媒 → 弹社媒登录(Accept→authorizeOrLogin 已覆盖);② 双登录态+单在线+另一社媒待完成男 CE 单 → 强制任务;③ 任务:优先级最高、**红黄绿跳转时可见**、阻塞流程、完成后解除、点接受→跳官网登录;④ **男主动:倒计时同男端、结束消失、可关闭**;系统代发:无倒计时、可关闭。
- **关键修正**:此前把男主动做成**不可关**是错的 —— 新需求两种都**可关闭**。改:关闭 X 恒显示、dismissible 恒真;倒计时从右上角挪到**标题行右侧**(右上角让给关闭 X);男主动 `expired=remaining<=0` 时隐藏(客户端先行,不等后端撤单)。
- **仍未做**:③ 的「红黄绿跳转时可见」= 跨页顶部 banner + Home「Win Free Cash」卡把 social-auth 纳入优先级/可见,follow-up(需设计稿);已问用户是否一并做。
- **验证**:tsc/eslint 干净;pre-commit/pre-push 过(commitlint 又拦 CE 大写开头,改中文)。commit 31288f24d 已推。


### 06:30 sitin4.1 社媒授权抽屉加回倒计时(commit 6fe3b65fd)

- **问题**:上一版对齐 Figma 时去掉了倒计时,用户问「为什么没显示倒计时」。根因:用户给的两张稿(6752-5444/6752-5371)是**可关·系统代发版、本身无 timer**,但男主动订单按需求要倒计时 —— 稿与需求对不上。
- **调和**:右上角槽位按订单类型二选一 —— `timeoutSeconds>0`(男主动)显示 mm:ss 倒计时(<30s 转红 #E5484D,否则 #1F3FD6)、不可关;`<=0`(系统代发)显示关闭 X、可关。倒计时锚 createdAt(useTaskRemaining + 按 taskId 固化 ref)。既不破坏稿(可关版右上角本就是 X),又满足需求。
- **验证**:tsc/eslint 干净;pre-commit/pre-push 过。commit 6fe3b65fd 已推。


### 06:10 sitin4.1 社媒授权任务抽屉对齐 Figma(commit db67e1080)

- **背景**:上一版 SocialAuthTaskDrawer 复用了 probe 的琥珀 SheetHead 样式,用户指出不对,给了 Figma(pwa整理 6752-5444 Snapchat / 6752-5371 IG,同一个抽屉两平台)。用 figma MCP get_figma_data 取设计。
- **重写**:底部白抽屉(圆角顶 30、上阴影 0 -10px 34px)、**头像骑顶沿**(84px 圆 + 6px 白边,右下角 28px 平台徽标)、标题「{名}'s {平台} request」(Fraunces SemiBold 18 #131B29)、奖励行(金币徽标 + 绿字 #00722A「Earn ¢X.XX when you authorize」)、**蓝色渐变主按钮** #2F6BED→#1F3FD6「Authorize {平台}」、底部灰免责说明。右上角关闭 X 仅可关(系统代发 timeoutSeconds<=0)时出现。
- **两点偏差(已告知用户)**:① 设计稿**无倒计时** → 去掉倒计时 UI,「可关/不可关」只体现为关闭 X 出不出;② 奖励用 **¢ 分**(跟稿 ¢3.00)不是 $。
- **复用**:ModalContainer bottom-sheet(外层不裁剪,头像可负 margin 溢出;遮罩 black/40≈稿 0.34)、SOCIAL_LOGO、usePeerUserInfo(取对端名/头像)、getAvatarUrl、authorizeOrLogin。
- **验证**:tsc/eslint 干净;pre-commit/pre-push 过。commit db67e1080 已推。


### 05:30 sitin4.1 FE-2.2 强制社媒授权(CE 交换)任务 · chat 页实现(未 commit)

- **数据契约**(用户给):`{ id, taskType: SOCIAL_ACCOUNT_AUTHORIZATION(=4), socialPlatform: "ig"|"snapchat"|"xxx", reward, timeoutSeconds }`,挂 convState.tasks(与 probe 同源)。解决了原方案开放问①:平台是**字符串短名**不是 CardType 数值。
- **proto 缺口**:b23ea527 的 TaskType 只有 UNKNOWN/PENDING/PROBE/VIDEO_TIPS,Task 无 socialPlatform。用**前端薄 shim**(`utils/socialAuthTask.ts`:常量 4 + 严格平台解析 + getSocialAuthTask/Platform),后端 proto 重生成 gen 后搜常量名替换。严格解析:未知平台(未来 "x")→null,不走 normalizeSocialPlatform 的默认 instagram、避免误当 IG。
- **管线**:摸清 anomaly 链 = chatConvStore.chatMap →(syncChatAlert→reduceHighestAlert)→ chatAlertStore →(useHighestAnomaly)→ AiGoldCashoutCard/GlobalAnomalyBanner/ProbeTaskDrawers。
- **实现(范围收敛到「chat 页」)**:① 新 `pages/Chat/SocialAuthTaskDrawer.tsx` 会话级阻塞抽屉,复用 probeDrawerParts(SheetShell/HeroRing/SheetCta 等)。timeoutSeconds>0(男主动)有倒计时不可关、<=0(系统代发)可关;倒计时锚 createdAt via useTaskRemaining;Accept→authorizeOrLogin(platform)(统一入口);任务被后端移除即消失。② Chat/index 挂载 + 优先级最高(probe defer 其后、footer 阻塞)。③ GlobalAnomalyBanner 排除 taskType=4(否则被 pickVariant 误当 ai-suspicion 弹横幅)。④ anomalyMock 加 social-auth-ig / social-auth-sc-closable 两个 /debug 场景(Debug 页自动遍历渲染按钮)。
- **坑**:app-pwa **无测试基建**(无 test 脚本/vitest config),故没补单测。GlobalAnomalyBanner.deriveBannerItems 遍历全部 task,新 taskType 必须显式排除。
- **follow-up**:后端 proto 补字段;跨页 Home 卡 + banner 的 social-auth 样式/文案(需设计稿);抽屉文案暂拟待产品。
- **验证**:tsc/eslint 全绿;pre-commit/pre-push 过。**commit 1b6e5adf5 已推 origin**。改动文件:utils/socialAuthTask.ts(新)、pages/Chat/SocialAuthTaskDrawer.tsx(新)、pages/Chat/index.tsx、components/GlobalAnomalyBanner.tsx、mocks/anomalyMock.ts。


### 04:40 sitin4.1 社媒授权卡 loading 提到卡片级(commit 93d263b05)

- **诉求**:`checkSocialProxyAbnormal` 是所有社媒一次性探测,原来是按钮级「Logging in IG…/Snapchat…」各转,改成**卡片级** loading——探测中整卡显示加载,完成后按状态渲染两种态(未授权卡 AuthorizeSocialCard / 单登录暂停卡 PausedCard)。
- **改动**(`AiGoldCashoutCard.tsx`):① 顶层读 `socialProxyChecking`,在 `!inApp` 后加早返回 `if (socialProxyChecking) return <SocialLoadingCard/>`。② 新增 `SocialLoadingCard`:复用 CardWrapper,标题/副标题骨架 + 白胶囊内菊花 spinner + 「Checking your accounts…」,状态中性不预设文案(避免探测完文案跳变)。③ `AuthorizeSocialCard` 删掉 checking/disabled/「Logging in…」分支,按钮回归纯 Authorize。SpokeSpinner 改由 loading 卡复用。
- **收尾可靠性**:`checkSocialProxyAbnormal` 在 `init`(TIM 登录后)跑,finally 必置 `socialProxyChecking=false`;非 APK 立即 false —— 与原按钮级同生命周期,不会卡死。
- **验证**:tsc/eslint 干净;pre-commit/pre-push 过。commit 93d263b05 已推 origin。


### 04:10 sitin4.1 Snapchat 进 Welcome Checklist 但不作 onboarding 门槛(commit 47689d634)

- **背景**:用户要把 taskId 137(BindSnapchatAccount,$0.5)加进 Welcome Checklist。发现坑:`ONBOARDING_TASK_IDS = onboardingTaskConfigs(true).map(t=>t.id)` 是**展示集和完成判据集共用**,只加 `v4OnboardingOrder` 会让 137 自动变成 onboarding 完成门槛 → 新用户不授权 SC 就完不成 onboarding、拿不到 Win Free Cash/提现。用户拍板「137 不能作为 onboarding 任务」。
- **B 方案(解耦)**:① 注册表加 `v4OnboardingGating?`(默认 true),SC 设 `v4OnboardingOrder:5 + v4OnboardingTitle + v4OnboardingGating:false`;新增 `onboardingGatingTaskIds()`。② `constants/onboardingTasks.ts` 保留 `ONBOARDING_TASK_IDS`=展示集(含 137,底部 Task 列表去重用),新增 `ONBOARDING_GATING_TASK_IDS`=判据集(不含 137)。③ gating 消费方(`useOnboardingCompleted`、`useTask` 完成副作用 :436、`sitin4Debug`)改读判据集;展示/去重方(`CashoutTaskList:62`、`useTask:273`、`OnboardingTaskList:131`)保持展示集。
- **效果**:SC 显示为 checklist 第 5 项(`Authorize Snapchat $0.50`,后端返回 137 时);核心 4 项做完即 onboarding 完成,Win Free Cash/提现照常;SC 授权在 Win Free Cash 卡继续;不重复进底部列表。
- **坑**:关键在 `ONBOARDING_TASK_IDS` 一个常量担了「展示去重」+「完成判据」两职,`v4OnboardingOrder` 是两者共用开关——加显示项前必须确认是否也想当完成门槛。commitlint 又拦大写英文开头(Snapchat),改中文开头过。
- **验证**:tsc/eslint 干净;pre-commit/pre-push 过。commit 47689d634 已推 origin。

另:今天早些还完成了 ① 授权口径 IG/SC 双端统一 + 登录中态(c36f7e2f7)② 登录成功收起小抽屉(f2e45cc08)③ 登录中 spinner 换菊花(c333ac74a),均已推。


### 03:20 sitin4.1 登录成功收起小抽屉 + 登录中 spinner 换菊花(commit f2e45cc08 / c333ac74a)

- **收起小抽屉(f2e45cc08)**:`finishPWAInsTask(state=true)` 成功分支清异常态后,`dismissSocialAuthDrawer()`(新增,close MODAL_ID「social-auth-drawer」)收起「Authorize accounts」小抽屉,再 `showSocialAuthDrawerFromStore()` 按最新 store 重派生——本平台异常已清不再入 rows;若还有别的社媒仍 expired 只重弹剩余行,全清则 early return 不再弹(多社媒友好)。放在 finishSocialBind 前,登录一成功抽屉即消失。
- **登录中 spinner 换菊花(c333ac74a)**:按用户图,去掉 logo 外圈旋转进度环 + 文字 animate-pulse,logo 常态显示;新增 `SpokeSpinner`(12 根渐隐辐条 opacity 1/12→12/12,整体 animate-spin 0.9s 的 iOS 风指示器),置于「Logging in {平台}…」文案末尾转。
- **验证**:tsc/eslint 干净;pre-commit/pre-push(54/54)。两 commit 已推 origin。


### 02:30 sitin4.1 授权口径 IG/SC 双端统一 + 登录中态收尾(commit c36f7e2f7)

- **原则(用户定)**:不为「接口没上线/失败」兜底(信任 hasContactCard 可用),只保留端能力(bridge)降级;IG/SC 授权判据双端统一,**不做 IG insId 兜底**(之前建议的撤销)。
- **改动**:① `selectPlatformUsable` IG 去掉 `isInsLoggedIn` 特判,统一 `authByPlatform[p] && !abnormalByPlatform[p]`;`App.tsx` authBlock 的 igUsable/igAuthed 走 authByPlatform,彻底不用 insId;InsExchangeBubble 共用一并统一。② 授权按钮「登录中」态改为**探测完成才可点**(`socialProxyChecking` 初始 true,checkSocialProxyAbnormal 完成才置 false;非 APK 提前放开)。③ loading 美化:社媒 logo 外套旋转进度环 + 「Logging in {平台}…」。
- **归属结论**:IG 若点 Authorize 还弹大抽屉 = `hasContactCard(IG)` 返回 false = 后端没给该 IG 授权存 contact card → **后端补齐双端一致**,前端不兜底。
- **验证**:tsc/eslint 干净;pre-commit/pre-push(54/54)。commit c36f7e2f7 已推。
- **未做**:`openInToolApp` 死等 5 秒(点 Open SITIN.ai 弹下载抽屉慢)的体验优化——已给方案(visibilitychange 事件 + 超时缩到 ~2s),待用户确认。


### 01:40 sitin4.1 检查登录态改名 + 授权按钮「登录中」态 + 回归排查(未提交)

- **回归**:用户实测 IG 任务已 Done(授权过、有 insId),但点 Win Free Cash 卡(`AuthorizeSocialCard`)的 Authorize IG 还弹大抽屉。根因:我把 IG 授权判据从 `!!insId||insState` 统一到 `hasContactCard`(commit 64b925f90),而 `authorizeOrLogin` 同步读 `authByPlatform.instagram`,它由冷启动 `checkSocialProxyAbnormal` 的 `hasContactCard` 回填、且 `.catch(()=>null)` 吞失败就不回填 → 老 IG 授权(有 insId 无 CE 名片)或 hasContactCard 请求失败时 `authByPlatform.instagram` 保持 undefined → 判未授权弹大抽屉。**建议修:IG 加 insId 兜底 `igCard||!!insId`(未做,待用户确认)**;并确认后端 21008 是否上线。
- **改名**:`checkInsAbnormal` → `checkSocialProxyAbnormal`(全 5 处引用:useInsTaskInit 定义/调用/deps + insStore 2 注释)。
- **授权按钮「登录中」态**:`insStore` 加 `socialProxyChecking`(运行时态,不 persist)+ setter;`checkSocialProxyAbnormal` 用 try/finally 包裹,进入置 true、探测结束置 false;`AuthorizeSocialCard` 双按钮读它——期间显示 spinner + 「Logging in…」+ disabled,探测返回真实登录态后恢复。顺带缓解上面弹错抽屉(状态未定时不可点)。
- **验证**:tsc/eslint 干净。含 review 修复(accountID 按平台/platformLoggedIn 复用)、IG 探测回填改用 platform setter,一并 **commit `3a73af891` 已推 origin**(pre-push 54/54)。
