---
title: 2026-09-21
date: 2026-09-21
tags: daily
---

# 2026-09-21

## 今日重点

- [ ] luka：注册 all set 页未传照片时兜底动漫头像；me 页头像出现慢（本次任务）
- [ ] 待用户定夺：仓库根未跟踪的 `-` 文件（devicectl UDID JSON 误产物）删不删
- [ ] 待定：`edit-profile/age` 的 `FALLBACK_AGE` 要不要与注册默认 30 对齐（T0022 只改了注册）
- [ ] 待定：Notifications Visitors 首跨页掉帧 → onPageScroll 升级 reanimated `useEvent`（需实机验证）

## 工作日志

### 10:57 · sitin-rn：luka 提审检查清单 review + 出报告文档

- 做了什么：读飞书 Base《提审检查清单》（92 条，全「未开始」），对照 `feature/luka-ios` 静态 review；拉取最新代码（`ec8955caf → c73a4ed94`，PR #525/#527）后复审，整理成 `apps/luka/docs/app-store-checklist-review.md`。
- 发现（代码实锤）：审核账号钉死 deviceId（`lib/review-mode.ts` 的 `STORE_REVIEW_DEVICE_IDS`）；DTC 站外卡支付 `dtcEnabled: isProduction` 且闸门阈值在后端（`third_party_payment_show`，`0` = 总是显示）；ATT 在登录后才弹（清单要求登录页）；`expo-audio` plugin 无条件写 `UIBackgroundModes: audio`；`supportsTablet: false` 与清单 10 条 iPad 用例冲突；LEGAL/Sentry DSN/appleTeamId"TODO" 等一批非空占位值（pre-submit-check 抓不到）；SIWA 授权失败静默回落设备登录（本次拉取新增）。
- 拉取后已修复：UTC+8 登录地区封锁整体移除；dev 包统一 GraceChat Ad Hoc 标识（生产 `com.lukasoc.luka` 不受影响）。
- 坑：① `pnpm pre-submit-check` 只查空值，非空占位（假 DSN、"TODO" Team id、carrd 占位 URL）全漏——提审清单必须人工盯；② expo plugin 之间会互相覆盖 Info.plist 键（infoPlist → expo-camera → expo-image-picker 三处相机文案，最后生效的是 image-picker 版）；③ gift 图片 URL 纯后端下发，仓库零硬编码，GIFT 系列只能在运营侧验。
- 产出：`apps/luka/docs/app-store-checklist-review.md`（未 commit，用户未要求）。

### 11:13 · sitin-rn：luka Plus Pass paywall 小字被仓鼠遮挡修复

- 做了什么：`apps/luka/src/app/paywall/index.tsx` 的自动续订说明（`Auto-renews until canceled…`，3.1.2(c) 审核修复文案）原在主 CTA 上方，被坐在按钮上沿的仓鼠（`sittingPet`，`hamster-sit.webp`，探出按钮约 76px、水平居中）整个盖住；挪到主 CTA 下方（三分支 `DtcPayArea` 之后，与非三分支按钮之后统一）。
- 为什么：仓鼠是稿子定的「坐在按钮上沿」姿态不能动，移动文案是唯一不破坏设计的解法。
- 结果：biome + `pnpm --filter luka exec tsc --noEmit` 均过；顺带更新 `apps/luka/docs/app-store-review-fixes.md` 那行（3.1.2(c) 位置说明）。用户要求 push 后提交 `21ba211ee`（`fix(luka): paywall 自动续订小字移到主 CTA 下方，避开坐沿仓鼠遮挡`），只 stage 了这两个文件，工作区其他未提交改动原样保留；pre-push（boundaries + apps/luka typecheck）通过，已推 `feature/luka-ios`。`likes.tsx` / `visitor.tsx` 的仓鼠不挨按钮、无此问题，未动。
- 备注：图片我这边模型读不了，凭「订阅按钮上方仓鼠遮挡小字」的文字描述定位——luka 三屏中只有 Plus Pass 的主 CTA（Light up Plus）带坐沿仓鼠，定位唯一。
### 11:17 · sitin-rn：luka Chats Team 入口挪到页头右上角（替换墙上相框）

- 做了什么：Chats 房间墙上的 Team 相框（`NestWallFrame`，`top: topInset+94`）与 8 张访客卡带重叠被挡，按用户要求把 Team 入口挪到页头右上角 notifications 爪印旁，只留官方头像 + 未读徽标（复用爪印徽标样式与 `-right-1 -top-1` 落位，99+ 封顶）。
- 改动：`components/xuanyu/screen-header.tsx` 加可选 `team`/`onTeamPress` props（不传零渲染，`ml-auto` 条件让给 team 入口；引导测量仍挂爪印 wrapper）；`nest-playfield.tsx` 删 `NestWallFrame` 渲染与 `NestTeam`/`team`/`onTeamPress`；删除 `nest-wall-frame.tsx`；`(tabs)/index.tsx` 的 team 数据改喂 ScreenHeader（`openTeam` 仍走 `markOfficialRead` + `/team`，All chats 抽屉入口不动）；同步 `docs/chats-play.md`、`docs/page-guide.md`、shape-language 测试注释。
- 结果：`pnpm --filter luka typecheck`、Biome、`pnpm boundaries` 过；chats-play / chat-list / notification-badges 测试全过；shape-language 3 条失败为预存（paywall / chat 气泡 / discover-play 未迁移文件），与本次无交集。未 commit（规范模式）。
- 坑：图片传不进来（当前模型不支持图片输入），凭文字 + 9-20 日志「相框只是透明点击区」定位。

## 工作日志 — 11:20

- **做什么**:sitin-rn(feature/luka-ios)修 luka 注册「未上传头像」的两个体验问题:① all set 页只剩首字母兜底;② Me 页动漫头像出现慢。
- **根因(模拟器 session.log 实证)**:FastLogin(进注册流程时)后端 `avatarUrl` 为空串——CAI 动漫头像要等 UploadUserInfo(点 Continue 落库)才按性别分配(如 `male_green_13.jpg`),所以 all set 页当时拿不到 URL;Me 页慢是 `me.tsx` 的 `initialLoading` 骨架把整屏含头像挡到 `GetMeOverview` RPC 返回 + 远程图首下。
- **改动(4 文件 + 2 素材 + 1 测试,未提交)**:
  - 素材:`src/assets/onboarding/cai-avatar-{male,female}.webp`(从 `file.archat.us/cai/avatar_image/` 下载 384² jpg,cwebp q85;male_green_13 / female_red_14),`FALLBACK_AVATAR` 按 `store.gender` 选,nonbinary 用 female 版。
  - `onboarding/summary.tsx`:photoUri 空时传 `fallbackSource`;落库 `setUserInfo(snapshot)` 后 fire-and-forget `prefetchImages([snapshot.avatarUrl])`(intro-video 播放的几秒预热缓存)。
  - `onboarding/profile-summary-card.tsx`:加 `fallbackSource?: ImageRequireSource`,`Avatar source={photoUri ? undefined : fallbackSource}`。
  - `(tabs)/me.tsx`:渲染分支 `me ? content : initialLoading ? skeleton : fallback`——snapshot 已有 displayName+avatarUrl 就直接渲染头部,RPC 失败但有 snapshot 不再弹错误页(列表走各自空态+下拉刷新)。
  - README(assets)目录表补 `onboarding/` 行;新测试 `tests/design/onboarding-fallback-avatar.test.ts` 5 条。
- **验证**:biome 4 files 0、`tsc --noEmit` 0、Metro bundle curl 200、全量 908 passed / 4 failed(good-review / candidate-card-fields / shape-language ×3,与 09-20 记录的既有红一致)。模拟器深链 `luka://onboarding/summary` 被登录守卫挡(App 停在未登录落地页),simctl 无法点屏走 8 步注册,**视觉未实机复核**——留用户/QA 真机走查。
- **已知边界**:本地兜底与后端分配头像同图集但非同一张(同性别多色),落库后 Me 页会换一次图;用户已确认接受。

- 提交 / 推送：`33b742ba1`（7 files, +65/-177），已 push `c73a4ed94..33b742ba1` 到 `feature/luka-ios`；pre-commit Biome、pre-push boundaries + luka typecheck 全过。只暂存了本次 7 个文件，工作区里并行会话的改动（me.tsx / onboarding / paywall / bootstrap 等）未动。

- **提交并推送(11:3x)**:`df9b16d6c` `fix(luka): 注册结果页未选照片时兜底 CAI 头像,Me 页头部不等网络` → `origin/feature/luka-ios`(7 files:+90/−8;工作区里并行会话的 app.config.ts / _layout.tsx / good-review / analytics / bootstrap 等未扫入,根目录 `-` 与 docs/app-store-checklist-review.md 未动)。pre-commit biome(4 files)、pre-push boundaries(41 包面)+ apps/luka typecheck 全过。
- **踩坑**:第一次 edit `profile-summary-card.tsx` 时用旧注释做 oldString,把并行会话刚改的示例注释(`open to new connections`)回退成了旧文案——提交前看 diff 发现并修回。教训:同文件有并行会话改动时,edit 前重读文件、提交前必看 diff。

### 13:42 · sitin-rn：定位 luka「订阅页未获取到任何套餐」

- 做了什么：从用户 bug 单「luka 订阅页未获取到任何套餐，无法订阅」出发，先查知识库（koda R00218/R00219 是同一病灶），再定位到模拟器里的 `session.log` 取原始 RPC 证据。
- **证据（实锤）**：`apps/luka` dev（`api-dev.lukasoc.com`）的 `GetSubscriptionsV3Request` 响应是
  `code=1 {"code":1,"message":"success","normalSubscriptions":[],"visitorSubscriptions":[]}`
  —— 客户端原样收到空目录，不是解析/映射问题。同一租户、同一 `app_name` 下 `GetCoinProductInfos`（`coins_tier8..11`）、`GetSupportedCardTypes`（`[1,2,3]`）、`SwipeCard` 都 `code=1` 且有数据 ⇒ 身份/鉴权正常，**只有订阅商品目录为空**。
- **结论**：客户端无可修 —— 症状是「后端该 app 租户没配会员/访客订阅商品」，需后端在 dev / prod 两套后端各配。界面之所以看不出病因：`packages/business-payments/src/subscriptions.ts:103` 把非 Success 吞成 `[]`，`useAsyncData` 的 `error` 只在真抛异常时置真，于是 `plans=[] & error=false` → `activeId=""` → `useSubscriptionPurchase.start()` 早退，CTA 点了没反应（既无转圈也无 "Couldn't load plans"）。
- 沉淀：新建 `30-Resources/troubleshooting/rn-empty-subscription-paywall-backend-catalog.md`（含取证据方法与「可选客户端改进」，后者未做、需单独 PR）。
- 关键命令：`grep "GetSubscriptionsV3\|GetCoinProductInfos\|GetSupportedCardTypes" <container>/Library/Caches/logs/session.log`；无 token 直接 curl `api-dev.lukasoc.com` 会 401（正常）。
- 未做（等用户定夺）：① 通知后端补订阅商品；② 是否让共享包把「空目录」与「加载失败」分开（影响所有消费方）。

### 14:20 · sitin-rn：后端补订阅商品后复验 —— 目录回来了，但 ID 与客户端关键字全对不上

- 做了什么：用户「后端加上了 你再试试」→ 用 esbuild 打包 `/tmp` 脚本，带鉴权直连 `api-dev.lukasoc.com`（`FastLogin` deviceId（从 session.log 取）+ `GetSubscriptionsV3`）。
- 结果：**目录已非空** —— 会员 `luka_vip_yearly/monthly/yweekly`、访客 `luka_guest_yearly/monthly/weekly`、币 `luka_bronze_1..7`（此前 `coins_tier8..11` 也换了名）。
- **新发现（更严重）**：ID 与包内配置/解析不一致 —— `iapSkus` 关键字还是 `luka_membership`/`luka_visitor`/`luka_credits`，`periodFromProductId` 只认 `_w`/`_week`/`_y`/`_year`。后果：全部 SKU 被 `iapKindForSku()` 判 `unknown` → `iap-verifier.ts:54` 直接 return false（真实 IAP 不发货）、购买结果监听被过滤；套餐卡标题全变 `1 month`；`entitlements.ts` 的 `visitorActive()` 因 `luka_guest_*` 不含 `luka_visitor` 而把已购访客订阅读成未解锁。`luka_vip_yweekly` 疑为拼写错误，先与后端确认。
- 手法沉淀：模拟器 keychain 读不到 SecureStore（加密），深链被 AppsFlyer 吞掉/被守卫挡 → 改用「FastLogin 拿 token + esbuild 打包共享包源码 + node 直连后端」最可靠。
- 待用户定夺：① 后端改 ID（App Store 商品 ID 不可改，成本高）还是 ② 客户端对齐关键字 + 解析器。已同步 troubleshooting 笔记。

### 14:35 · sitin-rn：客户端对齐后端商品 ID（用户选 ②）

- 改动（3 文件）：
  - `apps/luka/app.config.ts` `iapSkus` → `luka_vip` / `luka_guest` / `luka_bronze`，并加注释说明「关键字由后端目录决定、写错是静默失败」。
  - `packages/business-payments/src/subscriptions.ts` `periodFromProductId` → 加认无分隔符 `weekly$` / `yearly$`（`luka_vip_yweekly` 的 `y` 不是分隔符），保留 `_week`/`_w`/`_year`/`_y`；其余 app 的 ID 行为不变。
  - `packages/business-payments/tests/subscriptions.test.ts` 补 6 个真实 ID 的解析用例。
- 验证：用 esbuild 打包实际函数跑真实 ID，6 条的 period/label/kind 全对（`luka_vip_yearly`→12 months/membership 等）；biome 0、`pnpm --filter luka typecheck` 0、`pnpm --filter @heyhru/business-payments typecheck` 0、`pnpm boundaries` OK；`pnpm --filter luka test` 915 中 911 passed，4 红为既有（good-review + candidate-card-fields + shape-language ×3）。
- 未提交（规范模式；涉及共享包，等用户确认是否 commit/push）。

### 11:40 · sitin-rn：luka 注册 name 屏进入「闪两下」修复（已提交并 push）

- **做了什么**：修用户报的「进入输入 username 页时，会闪两下才进入」（注册第一步 `apps/luka/src/app/onboarding/name.tsx`）。
- **排查（模拟器录屏逐帧，非读代码猜）**：iPhone X (11 Pro eq) 临时把根布局 onboarding 组守卫放宽为 `true`，`luka://onboarding` 深链复现 → `simctl io recordVideo` 录屏 → ffmpeg 抽帧 → `magick compare -metric AE` 逐帧差。深链后：17.2–17.77s 转场滑入（250 万 px/帧）、17.8–18.3s 键盘渐弹、**18.8s 单帧 20.5 万 px 突变**（变化集中在 y 20%–60%，正是 eyebrow / 仓鼠行 / 输入区）。
- **两处根因**：
  1. `scaffold.tsx` 的 `compact = keyboardAdaptive && keyboardVisible`：name 屏 `autoFocus` 立刻弹键盘，但折叠态要等 `keyboardVisible` 翻转才生效——这个翻转由 JS 状态驱动、比 OS 键盘动画晚几百 ms 落地，转场刚稳时仓鼠行**整块卸载** + eyebrow 折叠 + CTA padding 突变，一帧内完成 = 最刺眼那一下。
  2. `onboarding/index.tsx` 的 `<Redirect>` **渲染 null**：登录成功守卫翻转挂载本屏、与 replace 到 name 之间会露 1–2 帧窗口底色（白/黑）。
- **修法（3 文件）**：
  - `scaffold.tsx`：加 `keyboardHasAppeared` ref，`compact = keyboardAdaptive && (keyboardVisible || !keyboardHasAppeared.current)`——`keyboardAdaptive` 屏**首帧即折叠态**，键盘弹起无跳变；键盘收起过之后照旧展开。eyebrow 初值 `useSharedValue(compact ? 0 : EYEBROW_HEIGHT)`、CTA padding 改跟 `compact`。查过 `keyboardAdaptive` 三个消费方（onboarding/name、onboarding/location、edit-profile/name）**全部带 autoFocus**，语义升级无副作用。
  - `onboarding/index.tsx`：`Redirect` 包一层 `flex-1 bg-background`（与 name 屏同奶油底）。
  - `_layout.tsx`：根 View 加 `bg-background`，兜住 auth 守卫换组时的容器交接空隙。
- **验证**：同一深链路径重录 → 20.5 万 px 单帧突变消失，最大帧差降到 1.6 万（仅键盘渐入/噪声）；顶部区域转场早期已是奶油底。`tsc --noEmit` 0、`biome check` 3 文件干净。
- **关键命令**：`xcrun simctl io <UDID> recordVideo --codec h264 --force`（**必须 `kill -INT` 优雅停**，强杀会丢 moov、且可能残留 host 录制占用——本次踩过，重启模拟器才清掉）；`ffmpeg -ss 16.5 -t 3 -vf fps=30`；`magick compare -metric AE a.png b.png null:`。
- **提交**：`616339e6b` `fix(luka): 注册 name 屏进入不再闪两下` → `origin/feature/luka-ios`；pre-commit biome、pre-push boundaries + luka typecheck 全过。`_layout.tsx` 里并行会话的 `ensureTrackingConsent`（ATT 清单 DATA-02）用 `git add -p` 只暂存我这一处、未夹带；根目录 `-` 与 `docs/app-store-checklist-review.md`、`assets/onboarding/` 等并行产物也没扫进来。
- **沉淀**：RN 键盘自适应屏若 `autoFocus`，**首帧就渲染折叠态**，别等 `keyboardVisible` 翻转（JS 状态滞后于键盘动画，转场期暴露成布局跳变）；`Redirect` 落地屏要垫同色背景，否则守卫换组时露窗口底色。

### 11:49 · sitin-rn：修 luka 提审 review 里可修的项（deviceId / iPad 除外）

- 范围：用户指定 deviceId 与 iPad 保持现状，修其余。实修 3 项：
  - **ATT 时机（DATA-02）**：`apps/luka/src/app/_layout.tsx` 根布局挂载 effect 里 fire-and-forget `ensureTrackingConsent()`，首屏（登录页）即弹；`services/native/bootstrap.ts`、`analytics.ts` 注释同步改为「initAnalytics 只读已决状态」。
  - **相机权限文案统一**：`app.config.ts` 的 `expo-image-picker.cameraPermission` 改成与 infoPlist / `expo-camera` 逐字一致的 "…and for video calls and photos."，三处都加「必须逐字一致」说明。
  - **`good-review.ts` 注释**：去掉已删除的 `rate.tsx` / `rate-upgrade.tsx` 路径引用。
- 不修：`appleTeamId: "TODO"`（Luka 发行主体自己的生产 Team 真值未知，不套用 Intro 的 `C9JTACH6WZ`）、LEGAL / Sentry DSN / appsflyer / byteplus / officialAccountId 占位（需外部真值）、DTC（产品+后端决策）。
- 验证：`pnpm luka:typecheck` ✅、`pnpm lint` ✅、`app-config-contract` + `analytics-facade` 测试 ✅。`pnpm luka:test` 4 条失败（shape-language ×3、candidate-card-fields ×1）经 `git stash` 对照确认是 `feature/luka-ios` 分支自带，与本次无关。
- 坑：相机用途串被三个 plugin（infoPlist → expo-camera → expo-image-picker）先后写 `NSCameraUsageDescription`，**后注册的覆盖前值**，写岔了最终 ship 的是最后一个——必须在三处保持一致。改它属原生 Info.plist，要重出基包才生效（ATT 改动可在 JS 层生效）。
- 产出：`apps/luka/docs/app-store-checklist-review.md` 更新（新增《六、修复记录》，DATA-02 标已修）。未 commit（用户未要求）。

### 13:45 · sitin-rn：luka 资料页打招呼漏掉每日额度门（超限仍可发）

- **做了什么**：修用户 bug「划卡打招呼次数到上限后，在 profile 页点打招呼按钮还能打招呼」。`apps/luka/src/app/profile/[id].tsx` 的 `onGreet` 直接调 `performHomeLike`，**唯独没走 `isLikeReady()` 那道门** —— 划卡（`useSwipeActions.sayHi`）和 Nearby（`useNearbySayHi`）都先查额度、超限跳 `/paywall?source=say_hi_limit`，资料页漏了，所以额度打满后这颗键照发。
- **改动（3 文件）**：
  - `src/lib/profile-cta.ts`：新增纯函数 `profileGreetNeedsQuota(source)` = `!usesManualLikePath(source) && greetingCountsTowardQuota(source)`。即走共享 Buzz 单元的入口要吃额度门；`likesMe` / `visitedMe` 走各自的 manual-like 接口（文档明说不计划卡限额）与 Connections（十个固定卡在额度体系外）都放行。
  - `src/app/profile/[id].tsx`：`onGreet` 在 `markSent` 前插入 `if (profileGreetNeedsQuota(source) && !isLikeReady()) { router.push(swipeLimitPaywallHref("like")); return; }`，与划卡/列表口径一致。
  - `docs/discover-play.md`：「Say hi」那行补一句资料页共用同一份额度、超限同样跳会员页（行为变更同步文档）。
- **验证**：`pnpm --filter luka exec vitest run tests/lib/profile-cta.test.ts` 12 passed（新增 3 条 quota gate 用例）；`pnpm --filter luka typecheck` 0；`biome check` 3 文件干净。全量 `pnpm --filter luka test` 4 failed / 911 passed —— 经 `git stash` 对照确认是分支自带的 good-review / candidate-card-fields / shape-language 预存红，与本次无交集。
- **未做**：未实机走查（改的是闸门分支，逻辑等价于划卡侧已验证路径）。根目录 `-` 未动。
- **提交 / 推送**：用户要求 push 后提交 `aadf15c31`（`fix(luka): 资料页打招呼补上每日额度门，超限跳会员页`，4 files +55/−4）→ `origin/feature/luka-ios`（`3828132a5..aadf15c31`）；pre-commit biome、pre-push boundaries + luka typecheck 全过。只 stage 这 4 个文件，根目录 `-` 未扫入。

### 14:40 · sitin-rn：luka 删号屏「按钮压在输入框上」修复（14:55 按用户反馈改终稿）

- **做了什么**：修用户录屏报的「Delete my account 按钮压在 DELETE 输入框上」（`apps/luka/src/app/settings/delete.tsx`，键盘弹起时）。
- **第一版（14:40，被用户否）**：栏加 `useKeyboardAwareBottomInset()`（resting 34 → 键盘态 8）+ 确认框 `onFocus` 后 300ms `scrollToEnd`。用户反馈「往上滚动的时候成两段滚动了，应该按钮和内容一起往上滚动」—— `KeyboardAvoidingView(behavior=padding)` 是**压矮容器**：按钮先贴到键盘上沿，滚动区随后才缩，观感就是两段。
- **终稿（14:55）**：改成聊天页同一套键盘路径 —— 外层 `<View className="flex-1 overflow-hidden">` 静止裁剪窗口 + 内部 `KeyboardStickyView style={{flex:1}}` 把「ScrollView + 操作栏」**整块 transform 平移**（`offset.opened = restingInset - KEYBOARD_GAP`），删掉 KAV；底栏 padding 回到非键盘感知的 `useBottomInset()`（键盘态变高度会在动画末帧重排内容，`chat-input-bar.tsx` 注释有记录）。`onFocus` 保留但去掉 300ms 延时：整块平移不改滚动区高度，`scrollToEnd` 目标与键盘无关，可直接滚（内容不超一屏时是空操作）。
- **根因**：① KAV padding 只压矮滚动区，不会把聚焦输入框滚进可视区（UIKit 只按键盘上沿、不认固定栏高度）；② 栏的 resting inset 在键盘态多留 26pt；③ 结构上「输入框在滚动区、按钮在滚动区外」注定了固定栏 + 框内滚动 = 两段观感，要一起走得用整块平移。
- **验证**：`pnpm lint` 0、`pnpm --filter luka typecheck` 0、`pnpm luka:test` 4 failed / 917 passed —— `git stash` 对照确认 red 是分支自带（good-review + candidate-card-fields + shape-language ×3）。
- **未提交**（规范模式；工作区里还有并行会话的改动）。**未实机走查**（本会话模型无视觉，看不了用户截图/录屏）。
- **沉淀**：[[30-Resources/troubleshooting/rn-keyboard-and-pinned-footer]]（三档键盘避让结构对照表 + 整块平移的四条要点）；与 2026-09-16 聊天页「消息区 + 输入条同一个 KeyboardStickyView」、koda R00272 举报页三轮迭代同源。

### 15:01 · sitin-rn：luka 页头铃铛数量口径改为「两段名单人数之和」

- **需求**：用户「luka【likes&visitors】实际收到的 likes&visitors，外部通知按钮展示的数量不一致」。
- **排查（口径对比）**：Chats 页头铃铛读 `likeUnread + visitorUnread`（**未读增量**：`GetMatchLikedList(limit:1).totalUnreadLikedUserCount` + `GetNewVisitorCount(baseline)`），而 `/notifications` 两段是**存量名单**（`GetMatchLikedList` 行数 + `GetVisitorList` 全量行数），两者本来不是一个数；另有 `filterOutMatched` 只把「已在会话里的人」从页面名单隐掉、不扣铃铛。
- **用户裁决**：不一致方向是「铃铛偏小」；期望口径 = **两段实际人数之和**（看完不清零）。
- **改动（共享包只增不破，范围只 Luka）**：
  - `packages/business-vibes/src/vibes.ts`：新增 `getLikedCounts()` —— 一次 `limit: 1` 的 `GetMatchLikedList` 探针同时带回 `totalLikedUserCount` + `totalUnreadLikedUserCount`（不新增请求数）；旧 `getLikeUnreadCount` 保留（blueprint/intro/koda 三个 store 仍在用）。
  - `apps/luka/src/services/intoyou.ts`：导出 `getLikedCounts`。
  - `apps/luka/src/stores/into-you-unread.ts`：新增 `likedTotal` 字段；`refresh()` 改用合并探针（失败保留旧值不闪 0）；**mirror 仍是未读和**。
  - `apps/luka/src/app/(tabs)/index.tsx`：铃铛改读 `likedTotal + visitorTotal`（局部变量 `notificationUnread` → `notificationCount`）。
  - `apps/luka/tests/stores/into-you-unread.test.ts`（新，6 用例）、`apps/luka/docs/notifications.md`《徽标》重写 + 状态头加 2026-09-21。
- **关键决策**：**mirror 不动**——tab 徽标与 OS 图标角标继续走未读口径。理由：OS 角标语义就是「多少没看」，喂存量会让用户永远消不掉那个数字（`use-app-badge` 会把 `notifications.totalBadge` 设成 `setBadgeCountAsync`）。`clearLike()` / `markVisitorsSeen()` 只清未读、不动 total，行内红点 latch 链路原样保留。
- **已知残差**：服务端 `totalLikedUserCount` 仍含「被 `filterOutMatched` 隐藏的人」，`likedDisplayCount` 只修了页内计数 → 铃铛可能略大于页面行数（方向与本次主诉相反，先不修；要修得把 conversations 快照进 store，成本高）。已写进文档。
- **验证**：新测试 6 passed；`pnpm boundaries` ✅；全量 `pnpm --filter luka test` 4 failed / 917 passed —— 4 条是工作区既存（`nearby-list-row.tsx` 已删但 `candidate-card-fields` 未更新 + `shape-language` 圆角迁移未完成），零交集。`pnpm typecheck` / `pnpm lint` 的红落在并行会话未提交的 `settings/delete.tsx`（缺 `KeyboardAvoidingView`/`Platform` import），非本次文件。
- **未提交**（规范模式；工作区混着并行会话的 delete.tsx 改动，不宜一起 commit）。

### 15:05 · sitin-rn：删号屏键盘修复推送

- 提交 `407f13e51`（`fix(luka): 删号屏键盘起时整块上移，操作栏不再压在输入框上`，2 files +56/−22）→ `origin/feature/luka-ios`（`b3e21a6b0..407f13e51`）。
- pre-commit biome 2 文件干净；pre-push `boundaries` OK + 受影响 workspace 只 luka（6.6s）全过，8.8s 收尾。
- 只 stage 我的 2 个文件；工作区里并行会话的改动（`(tabs)/index.tsx`、`intoyou.ts`、`into-you-unread.ts`、`business-vibes`、`pnpm-lock/workspace`、`patches/react-native-svg@15.15.4.patch`、根目录 `-`、`notifications.md`）未扫入。

### 16:30 · sitin-rn：按《赛跑包物料分配》把 Luka 域名整体切到 lukasap.com

- **做什么**：用户给飞书表格《赛跑包物料分配》（wiki `MsHDw25L...`；Luka 行今天 15:38 由用户更新），要求「按文档来，不一致的都替换」。文档取值：新域名 `lukasap.com`（旧 `lukasoc.com` 废弃）、后端 appName `lukasap`、反馈邮箱 `lukaiosfeedback@lukasap.com`、远端机 `EU745.macincloud.com`。
- **改动（apps/luka，未提交）**：`app.config.ts` —— `apiBaseUrl`/`fileBaseUrl`/`alive.host` → lukasap.com，`appName` → `lukasap`、`appNameByPlatform` → `lukasap`/`lukasap_android`，注释同步；`README.md` 域名表 + push/WebSocket 备查段；`src/lib/legal.ts` 的 `SUPPORT_EMAIL` 占位 → `lukaiosfeedback@lukasap.com`；`docs/app-store-checklist-review.md` 一处 host。
- **刻意没动**：bundle id / App Group仍是 `com.lukasoc.luka`（商店身份与后端域名解耦，参照 koda `com.kodasoc.koda`）；审核账号邮箱 `review@lukasoc.com` / `metareview@lukasoc.com` 是否随迁未定（文档只给反馈邮箱）；`.template-rename.json` 派生映射不动。
- **验证**：`pnpm luka:typecheck` 0；biome 0；config/settings/payment-environment 23 tests 全过；全量 920 passed / 4 既有红（good-review 文件级 + candidate-card-fields ×1 + shape-language ×3，与本次零交集）。`api-dev.lukasap.com` 服务在线（405/500），appName 是否被后端接受未能离线验证。
- **坑**：`api-dev.lukasap.com` 的 appName 以表格为准，真机验证要登录后看接口是否被拒；只改域名不改 appName 会静默失败。

### 16:35 · sitin-rn：Luka 划卡页「订阅返回后刮层闪退」根因定位 + 两处修复（未提交）

- **报障**：划卡页触发订阅（超限进 paywall）→ 返回划卡页 → 滑动刮开卡面磨砂层时闪退（偶现）。用户提供 session.log + 3 份 .ips（`~/TEST`）。
- **日志时间线**：03:29:47/50/51 三次 `luka_subscription_show`（swipe_limit ×2、say_hi_limit ×1，均为 back 回去后再次超限），03:29:51.79 后日志中断、5 秒后 `luka_app_launch` → native 闪退（JS 侧无任何 error）。
- **.ips 定位**（`Luka-2026-09-29-112959.ips`，与日志时间线吻合）：主线程 `EXC_BAD_ACCESS/SIGSEGV`、`KERN_INVALID_ADDRESS at 0x0`；栈 `CA::Transaction::commit → CA::Layer::display_ → -[UIView(CALayerDelegate) drawLayer:inContext:]` → Luka 7 帧无符号绘制帧（与 `RNSVGSvgView drawRect:`→`drawToContext:` 递归渲染吻合）。
- **根因**：react-native-svg 15.15.4 的 iOS mask 合成把离屏位图按 `rect × getScreenScale()` 分配；`UITraitCollection.currentTraitCollection.displayScale` 在无 trait 环境（离屏 / 转场重绘）返回 0、`rect` 也可能为 0 → `CGBitmapContextCreate` 返回 NULL → 后续 CGContext 调用空指针。触发窗：paywall dismiss 转场期间 react-native-screens detach/reattach 划卡屏，恰好某次重绘落在退化帧。
- **修复 1（native，pnpm patch）**：新增 `patches/react-native-svg@15.15.4.patch`（三处防护：scale 为 0 依次回落 `UIScreen.main.scale`/`1`；`UIGraphicsGetCurrentContext()` 为空返回 NULL；filter/mask 分支要求 `contentImage != NULL` 且位图宽高 > 0），`pnpm-workspace.yaml` 注册 `patchedDependencies`。**原生代码改动，必须重新出基包才生效，OTA 覆盖不到**。
- **修复 2（JS，去重）**：`apps/luka/src/hooks/use-swipe-actions.ts` 的 `presentSwipeLimitReached` 加「一次聚焦只跳一次」闩锁（`useFocusEffect` 重置）——push 转场那几百毫秒里卡面仍可收触摸，连划会把 paywall 叠栈、用户要多次 back。
- **回归测试**：新增 `apps/luka/tests/hooks/use-swipe-actions.test.ts`（3 条：同聚焦只 push 一次 / like 走 say_hi_limit / 重新聚焦后恢复）；已用 stash 对照验证能抓住原 bug（撤修复后 2 条失败）。
- **文档**：`docs/react-native-guide.md` 新增 svg patch 记录（症状 / 根因 / 修法 / 何时可删）。
- **验证**：node 24 下全量 `pnpm typecheck` ✅、`pnpm lint` ✅（5298 files）、`pnpm boundaries` ✅、新增测试 3/3 ✅；其余 4 红为既有（good-review 文件级 + candidate-card-fields ×1 + shape-language ×3）。lockfile 只留 patch 相关重链（install 顺带把 metro 0.84.4→0.84.5 的引用漂移已手工还原，`--frozen-lockfile` 验证自洽）。
- **提交并推送**：`2bf74b4ed` fix(repo): react-native-svg 打补丁（patch + pnpm-workspace/lock + docs）；`b969cf838` fix(luka): 超限跳会员页一次聚焦只跳一次（hook + 回归测试）→ `origin/feature/luka-ios`（pre-push 全量 boundaries + typecheck 过）。
- **push 清障（并行会话的工作区改动，未纳入我的提交）**：pre-push 被两处并行产物挡住 —— `apps/luka/src/services/profile.ts` 末尾多余空行（全量 lint）、`packages/rn-tim/src/envelope.ts` 注释里的 app 名 "Luka"（boundaries identity leak）；各就地改一处（删空行 / 注释改「wired app-side」），两处留在工作区。
- **遗留**：另两份 .ips（11:08:53、13:54:57）是 `com.meta.react.turbomodulemanager.queue` 上 `RCTExceptionsManager reportException → RCTFatal` 的 SIGABRT（JS fatal 路径），与本次 SVG 崩溃不是同一条，需真机复现单独排查。真机回验路径：额度用尽 → 进会员页（like/nope 各一次）→ back → 立刻刮层 + 划卡。
- **沉淀**：[react-native-svg-ios-mask-crash](../30-Resources/troubleshooting/react-native-svg-ios-mask-crash.md)（.ips 取证脚本 + 判读方法 + patch 细节）。

### 16:38 · sitin-rn：luka likes/visitors 资料页打招呼命中匹配不弹揭示页、会话不建立（未提交）

- **报障**：likes 页进对方资料页点 Say hi → 无匹配成功弹窗、对方从 likes 名单消失、chat 页也找不到；预期弹揭示页并建立会话。likes 命中率 100%（对方已喜欢我），visitors 有概率 —— 都该由 ManualLike 的 `triggerMatch` 决定。
- **根因（两处，均为 blueprint 派生时漏接）**：
  1. `apps/luka/src/app/profile/[id].tsx` 的 manual-like 分支把 `manualLike(uid)` 的 `triggerMatch` 当布尔吞掉；`manualRevealParams` / `stashRevealPeer` 从建包起就是死代码（`services/match-reveal.ts` 注释自己写着「no RN caller yet」）→ 命中匹配既不进 `/match-reveal`，也不走 `startMatchConversation`，只剩 `markSent` 翻「Sent」，TIM 里只是条 pending wave（Chats 里归到 Waves，不在房间卡片）。
  2. `apps/luka/src/app/notifications.tsx` 的 Visitors 段开资料页没带 `source` → 落 `other`，走共享 Buzz 路径，`visited_me` / `VisitorProfileLikeSuccess` 整条链永远不可达（`lib/profile-cta` 契约与测试都写着 visitedMe 该走 manual-like）。
- **参照实锤**：`apps/iris/src/app/profile/[id].tsx` 的 `useProfileRelationshipAction` 是完整接法（manualLike → triggerMatch → `stashRevealPeer` + push `/match-reveal?origin=...&threadSource=...`），Luka 直接对齐。
- **改动（4 文件 + 1 测试 + 2 文档，未提交）**：
  - `lib/profile-cta.ts`：`manualRevealParams` 从 `services/profile.ts` 迁入（纯逻辑、可测），新增 `profileRevealHref(person, source)`。
  - `app/profile/[id].tsx`：manual 分支命中即 `stashRevealPeer` + push 揭示页（likes_me/visited_me），由揭示页 Message X 真正开会话。
  - `app/notifications.tsx`：Visitors 点行进 `/profile/<id>?source=visitedMe`。
  - 注释同步 `services/match-reveal.ts`、`packages/rn-tim/src/envelope.ts`；文档 `docs/notifications.md`（两段 source 与 manual-like 口径）、`docs/discover-play.md`（资料页额度例外说明）。
  - `tests/lib/profile-cta.test.ts` +3 用例（两个入口的 origin/threadSource 映射 + 路由拼装）。
- **验证**：`pnpm --filter luka typecheck` ✅、biome 6 文件 ✅、`pnpm boundaries` ✅；全量 luka 938 tests → 934 passed / 4 既有红（good-review 文件级 + candidate-card-fields ×1 + shape-language ×3，零交集）。
- **未做**：未实机走查（本会话无视觉，需真机点 likes/visitors 资料页验证）；`apps/blueprint` / intro 同样带这个缺口（blueprint 修需按模板规则补 migration 记录再走 template-migrate，另开任务）。
- **提交 / 推送**：用户要求 push 后提交 `17d45cefd`（9 files，+82/−25）→ `origin/feature/luka-ios`（`414684b8c..17d45cefd`）；pre-commit biome 7 文件、pre-push boundaries + 9 个受影响 workspace typecheck 全过。混合文件 `notifications.tsx` / `docs/notifications.md` 用 `git apply --cached` 只暂存本 hunk（并行会话的 RefreshControl / 访客分批 / 翻页改动未扫入）。

### 16:35 · sitin-rn：luka 通知两段加下拉刷新；非会员下拉进付费墙；截屏 246 溯源

- **用户口径（本轮澄清）**：铃铛不封顶、显示总人数；Activity / Visitors 要有上拉翻页 + 下拉刷新；未订阅时下拉刷新进 "See Who Likes You"；已在聊的人**计入**铃铛。
- **截屏 246 溯源**：确认来自本 worktree（`feature/luka-ios`，Metro 正从本目录跑；截图页头有 Team 头像 = `33b742ba1` 的产物，而 `sitin-rn-fix-luka` 分支缺该提交、页头无 Team 头像）。**246 = likedTotal + visitorTotal**（本轮未提交改动）。排除旧公式的依据：HEAD 公式页头 = `likeUnread + visitorUnread` 与 Tab 徽标 `messages + intoYou` 共享同一 mirror 值 → 页头必然 ≤ Tab（截图 82），246 > 82 不可能。
- **本轮改动**：
  - `hooks/use-liked-list.ts`：`reload` 拆出 `loadFirstPage(showSpinner)`；新增 `refreshing` + `refresh()`（重拉第一页**不闪全屏 spinner**）；`reload` 类型改 `Promise<void>`。
  - `hooks/use-visitor-list.ts`：新增 `refreshing` + `refresh()`；`loading = loading && !refreshing`（下拉刷新不换屏）。
  - `app/notifications.tsx`：Activity `FlatList` 与 Visitors `MasonryList` 各挂 `RefreshControl`；非会员拉 Activity → `/paywall/likes?source=seeWhoLikesYou`。
- **发现（不改）**：`GetVisitorListRequest` **无游标/limit 字段**（一次返回全量 `visitorInfos`，另有 `visitorRecentCount` / `repeatVisitorCount` / `friendVisitorCount` 三个汇总数未用）→ Visitors 无分页可做，列表本就全量；Activity 非会员仍单页 10 条预览（iOS 口径）。
- **验证**：`tsc --noEmit` 干净（并行会话 delete.tsx 已自行提交）；`biome check` 7 文件干净；`pnpm --filter luka test` 4 既存失败不变（shape-language ×3 + candidate-card-fields ×1）。
- **文档**：`apps/luka/docs/notifications.md` 新增《列表手势（2026-09-21）》；《徽标》补「不按非会员预览封顶」。
- **未提交**（规范模式，等用户指令）。

### 16:35 · sitin-rn：Luka 商店身份与审核邮箱也随迁 lukasap

- **背景**：用户对上一条的「刻意没动」拍板「都改了吧」。
- **改动（接上一条，未提交）**：
  - `app.config.ts`：`PRODUCTION_IOS_ID` `com.lukasoc.luka` → `com.lukasap.luka`（Android package 同一常量）、App Group → `group.com.lukasap.luka`（HomeWidget / NSE / Widget 扩展 id 由 bundle id 派生，自动跟着变）+ 顶部注释。
  - 测试 4 个：`app-config-contract` / `payment-environment` / `iap-verify-platform` / `fake-payment-production-guard` 的期望值与 fixture 同步。
  - 文档：`app-store-checklist-review.md` 生产身份一处；`app-store-review-findings.md` 的 `review@lukasoc.com` → `review@lukasap.com`（4 处）。
  - native module：`modules/luka-call-recharge/android/build.gradle` 的 gradle `group` 同步。
  - 不动：`.template-rename.json`（派生时记录，非当前取值）。
- **验证**：biome 5 文件 0；`pnpm luka:typecheck` 0；全量 931 passed / 4 既有红（good-review、candidate-card-fields、shape-language，零交集）。
- **⚠️ Apple 侧待办**：新 bundle id 要在 Developer 后台建 App ID、注册新 App Group、重签描述文件；App Store Connect 的旧记录（`com.lukasoc.luka`）作废，按 `com.lukasap.luka` 新建。后端审核名单要建 `review@lukasap.com` / `metareview@lukasap.com`。

### 16:38 · sitin-rn：luka「退出登录闪退 → 账号消失」判读 .ips + 四处加固（未提交）

- **报障**：luka【setting】点退出登录后闪退（偶现），重启后「账号被删除」（偶现）。用户给 session.log + `~/TEST` 4 份 .ips。
- **.ips 与日志对上号（可复用手法）**：session.log 时间戳是 **UTC**、.ips 是 **+0800**（设备本地），差 8h；三份 .ips 与三次「日志中断 + 数秒后 app_launch」一一对应 —— `110853`=session 03:08（dev 页附近）、`112959`=03:29（订阅返回刮层，16:35 已定位 SVG patch）、**`135457`=05:54 登出闪退**。
- **崩溃形态**：`135457` 的 `last_exception_backtrace` = `objc_exception_throw ← RCTGetFatalHandler ← -[RCTExceptionsManager reportFatal:]`，跑在 `com.meta.react.turbomodulemanager.queue` 上 → **JS fatal → RCTFatal → abort**；`110853` 同队列、同 abort（`std::terminate` 形态），同源可能性高。session.log 里零 error —— fatal 不给日志落盘机会。
- **「账号被删」不是删除**：全日志零 DeleteUser 请求。链路 = 崩溃前 `logout()` 已清本地会话 → 重启落登录页 → 换身份重登 → 后端 `FastLogin` 纯按 deviceId 认人、认不出就开新号（`packages/business-auth/src/auth.ts`）。实证：03:08 崩后自动 quick_login（`luka.66bb4d3d…`+googleEmail）→ 新号 "Fh"；05:54 崩后 Apple 登录（Apple user id 当 deviceId）→ 新号 "Fy"；05:57 同 Apple ID 再登 → 回到 "Fy"（绑定稳定）。旧号仍在服务端，只是这台设备没入口。
- **本轮加固（5 文件 + 3 测试，未提交）**：
  - 新增 `services/native/logout-teardown.ts`：`runTeardownStep/Async` 逐条隔离 + 调用**前**面包屑；`bootstrapOnLogout` 全量套上（原先同步段裸调用，一步抛错全跳过、异常被 `.catch(() => {})` 吞）。
  - `stores/auth.ts` 的 `logout()` 顺序：先翻内存态（触发 teardown）→ **设备凭证最后删**；崩在中间磁盘会话仍在，重启恢复同账号（原顺序先 `clearSession()`，崩一步就把用户扔到登录页）。
  - 新增 `services/native/js-fatal-breadcrumb.ts`：包 `ErrorUtils` 全局 handler，fatal 先按 `[CrashLog]` **同步**写 crash.log（含 JS 栈）再交回原 handler —— 下次闪退能拿到真正的 JS 错误（本次 .ips 里没有 JS 栈）。
  - `app/settings/logout.tsx`：signOut 各步面包屑 + catch 记 warn。
  - 文档：security-hardening §6（隔离 + 凭证最后删契约）、debug-tools（crash.log 与遗言机制）。
- **验证**：新增 11 条单测（teardown 隔离 4 / auth 顺序与不丢号 3 / fatal 遗言 4）全过；`luka:typecheck` + biome 干净；全量 931 passed / 4 既有红（与基线一致，零新增）。
- **待定夺**：根因（哪个 JS 错误）等带遗言的包复现；iOS 登出后必然开新号（登录页只有 Apple 入口，device 号无入口回登）属产品/后端；测试包与 main 不一致（03:08 的自动 quick_login 带 googleEmail，main 的 iOS 登录页无 Google 入口）。
- **沉淀**：[luka-logout-fatal-account-stranded](../30-Resources/troubleshooting/luka-logout-fatal-account-stranded.md)

- **提交 / 推送（16:39）**：`414684b8c`（`fix(luka): 登出闪退不再丢号 —— teardown 逐步隔离、凭证最后清、fatal JS 留遗言`，10 files +417/−37）→ `origin/feature/luka-ios`（`b969cf838..414684b8c`，同批带上并行会话的 `0c18c24cc` 域名切换）。只提交本次 10 个文件（pathspec 限定；并行会话 staged 的域名改动已由作者会话先一步提交，未夹带）。pre-commit Biome 8 ts/tsx；pre-push boundaries（41 包）+ apps/luka typecheck（10.8s）全过。
- **提交/推送（16:4x）**：`0c18c24cc` `chore(luka): 域名与商店身份切到 lukasap.com`（10 files，+52/−36）→ `origin/feature/luka-ios`；pre-commit biome 6 文件、pre-push boundaries + 五个 workspace typecheck 全过。push 时撞并行会话的 ref 锁（远端当时已到 `b969cf838`），fetch 后确认远端已含本提交（并行会话的 `414684b8c` 叠在其上），本地与远端一致，无需 rebase。

### 16:44 · sitin-rn：luka 非会员预览封顶放开（Activity 可翻页 / Visitors 客户端分批）

- **用户裁决（第二轮澄清）**：① Activity 列表**继续隐藏**已在聊的人（`filterOutMatched` 保留；徽标含他们 → 列表行数永远少这几个人，作为已知残差）；② 非会员预览上限**放开**，可滚到全量（仍模糊 + 付费墙入口）。
- **改动**：
  - `lib/liked-list-logic.ts`：删 `firstPageLimit` / `LIKED_NON_PREMIUM_LIMIT`；`isExhausted` 去 `premium` 参数（改 3 参）。
  - `hooks/use-liked-list.ts`：去掉 `useEntitlementsStore` 依赖与 `premiumRef`；所有身份同一游标 20/页；`hasMore = !exhausted`；删「购买时 reload」（成员态只影响渲染，数据一致）。
  - `app/notifications.tsx`：删 `FREE_VISITOR_LIMIT = 15` 裁剪；Visitors 改**客户端分批 reveal**（`VISITOR_PAGE_SIZE = 20` + `onEndReached` 追加，接口无游标）；非会员 Activity 自然可翻页（模糊行）。
  - `tests/lib/liked-list-logic.test.ts` 更新；`docs/notifications.md`《列表手势》与 Activity 锁定态描述更新。
- **验证**：`tsc --noEmit` 干净、biome 干净、`pnpm --filter luka test` 4 既存失败不变（932 passed）。
- **未提交**（规范模式）。

### 16:55 · sitin-rn：luka 飞书 Buglist T0052/54/57/58/61 根因分析（只读，未改码）

- **来源**：飞书 wiki《IOS Luka Buglist 汇总》（base `MbjobIs4Qax91fsXfUWc9Z4WnKd` / table `tblywHQVyR7KIGC9`），5 条全「未开始」、指派张峥、v1.0.0；附件 2 png（T0052 资料页、T0061 Sparks history）+ 2 qt 录屏（T0054/T0057），已下载抽帧看过（ffmpeg fps=1 tile → 关键帧）。
- **T0057（P1，feed/nearby 半屏被遮）= 模板迁移 0032 没跟进**：键盘没收完就 `router.back()`（`app/compose.tsx:106-115` 无 `Keyboard.dismiss`）+ `insets.bottom` 在过渡几帧报键盘量级（250–350pt）→ `components/tabs/tab-bar.tsx:147,167` 直接吃值把底栏撑成半屏、`clearance*.ts`/`discovery-deck.tsx:86,166` 连带塌陷；blueprint 迁移 0032（`bottom-inset-guard` / `keyboard-exit` / `use-pager-relayout` 三件）与 intro `aa92f7808` 是现成蓝本，luka 的 `.template-rename.json` `appliedMigration` 停在 0009。四条 PagerView 页要一次全接（0032 原话「漏一个等于没做」）。
- **T0054（P2，进键盘页卡顿）= 编辑页自己的三叠**：`autoFocus` 与 push 同帧 + `keyboardVisible` 迟到翻转改真实 `paddingBottom`（occupation/education/bio 三页，scaffold 已在 09-21 `616339e6b` 修过、这三页没修）+ `EditorCard → PeekingHamster`（`tilt-head.mov` 1.5MB alpha-HEVC）进入帧新建播放器；fix/luka-code-review worktree 里有未合并的 `0f7afa6b5`（四页 + scaffold 改 KeyboardStickyView transform），可直接 cherry-pick。
- **T0052（P1，重登后资料丢）= 读模型 + 客户端两处**：Hub 值全部来自 `GetUserBasicInfosV2` self（`hooks/use-me.ts:33-42` 快照兜底只有昵称/年龄/geoLocation；`lib/edit-profile-hub.ts:69-80`）；Iris 有过修复（`patchMeOverviewProfile` + `preserveProfileFields`，`141e269c1`），**blueprint 与 luka 都没有**。但录屏截图里 Height 6'2"/Location 是留住的、丢的是 Occupation/Education/Interests/Bio → 「登出重登」路径下本地缓存会被清，客户端缓存救不了，需先用真机日志确认 `GetUserBasicInfosV2` 响应里这四个字段是否为空（`GetUserProfilePageInfo` 在 Luka 后端回 code=0 也值得查），再定客户端读别接口 or 后端修投影。
- **T0061（P0，订阅赠币 +0）**：流水金额完全来自后端 `GetUserCoinChangeHistory.coinValue`（`packages/business-payments/src/payments.ts:226-240`），客户端**没有任何字段能传赠币数**；唯一可控错配是 iOS `VerifyIOS` 只送周期码（100/101/102，由 SKU 猜）。同时 paywall 硬编码「750/1,500/3,500 Coins instantly added」（`app/paywall/index.tsx:35-42`）与后端 `subCoins` 脱钩 → 界面照喊、历史记 0。结论偏后端/Matrix 配置未配或 DTC 履约未发币，需后端对账（`GetUserPaymentOrderDetail` 的 `coinDelivered`）。
- **T0058（P1，去掉 feed Say hi + 评论）**：feed 卡片右上白色圆气泡就是 Say hi（`components/discover-feed/feed-masonry-card.tsx:113-127`，`chatbubble-ellipses-outline` + `accessibilityLabel="Say hi to ..."`），nearby 列表卡同款（`nearby-masonry-card.tsx:88-112`）；评论只在帖子详情 `/post/[id]`（`post-action-bar.tsx` 输入栏 + `post-replies-card.tsx`）与 My Feed 卡入口（`my-feed-post-card.tsx:90-100`）。评论能力在共享包 `business-feed`（7 个 app），只删 app 内 UI。**待产品拍板**：详情页 Say hi 胶囊是否同删、`/post/[id]` 是否保留、nearby 同款气泡是否也删。
- **产出**：本分析（未落文档）；下一步等用户定夺修哪些。

### 16:50 · sitin-rn：luka 通知两段各自未读徽标 + 按未读分布决定默认段

- **需求**：Activity / Visitors 两个 tab 各带未读徽标；只有 Visitors 有未读时进屏默认落 Visitors，其余（只有 likes / 都有 / 都没有）落 Activity。
- **改动**：
  - `lib/notification-badges.ts`：新增纯函数 `initialNotificationsSegment(likeUnread, visitorUnread)`（+ 单测 3 条）。
  - `components/notifications/tab-unread-badge.tsx`（新，遵守一组件一文件）：计数圆盘，`Colors.badge` + cream 描边 + 99+ 封顶，与底部 Tab 徽标同语言。
  - `components/notifications/notifications-tabs.tsx`：新增 `activityUnread` / `visitorUnread` props；徽标用 `absolute left-full ml-1` 挂在 label 右上，label 不位移。
  - `app/notifications.tsx`：`initialTab` 挂载时读一次 `into-you-unread` store；`PagerView initialPage` 与 `pagerProgress` 初值（visitors=1）对齐；徽标数据源 `likeUnread` / `visitorUnread`。
  - `docs/notifications.md`：新增《分段徽标与默认段》。
- **口径细节**：进段即清未读（Activity→`clearLike`，Visitors→`markVisitorsSeen`），所以默认落的那段徽标随即消失、另一段保留（与 tab badge/OS 角标同步下降）；默认段读的是 store 最近一次轮询值（app 级 30s），冷启动立刻进屏可能 0/0 → Activity（已写进文档）。
- **验证**：`tsc --noEmit` 干净、biome 5 文件干净、`pnpm --filter luka test` 4 既存失败不变（935 passed）。
- **未提交**。

### 16:56 · sitin-rn：luka 分段徽标可见性修复——未读清零时机从「进段」改到「离开该段」

- **用户反馈**：「tab上没出现 徽标」（截图 16:52，Activity / Visitors 都无徽标）。
- **根因**：上一版把清未读放在**进段**（默认落 Activity → `clearLike()` 进屏立刻清零），Activity 徽标只闪一帧就消失；Visitors 当时无未读 → 两个都看不到。功能实现了但不可见，是我上一版的设计问题。
- **修法**：`app/notifications.tsx` 清未读时机改为**离开该段**——切段时清上一段；`useEffect` cleanup 在离屏时清最后停留的那段。浏览期间两段未读数常驻可见，行内红点 / 新访客点也一并保留到离开；服务端未读（`MarkMatchLikedRead`）与访客 baseline（`saveVisitorTime`）同步延后。
- 默认段规则与挂载时读取不变；`docs/notifications.md` 三处「进段即清」措辞改为「离开该段时清」。
- **验证**：`tsc --noEmit` / biome 干净。
- **事实核对**：本机 `date` 16:54；Metro 自 09-20 17:24 起持续运行于本工作区，截图（16:52）即时反映工作区代码。另修正本文件两条误标时间戳（17:05→16:44、17:40→16:50）。
- **未提交**。

### 17:02 · sitin-rn：luka tab 徽标仍不显示——实为工作区 cherry-pick 冲突导致 bundle 编译失败（非代码问题）

- **用户反馈**：「还是 没有，但是 chat 页 notification 是有角标的」（爪印有徽标、tab 无）。
- **排查结论（关键）**：**不是徽标代码的问题**。证据链：
  1. `lsof` 显示 Luka App（PID 3786）正连着本机 Metro 8081；抓 Metro 当前 bundle（`lazy=false`）验证：16:59 那版含 `TabUnreadBadge` / `initialNotificationsSegment` / `likedTotal`，代码已到 Metro。
  2. 但 `.git/CHERRY_PICK_HEAD`（16:59:17 生成，并行会话在挑 T0054 键盘修复 `0f7afa6b5`）在 6 个文件留下冲突标记（`edit-profile/bio|education|location|occupation.tsx`、`sign-in.tsx`、`onboarding/scaffold.tsx`）→ Metro 现在直接 `TransformError: bio.tsx SyntaxError` → **App 从 16:59 起无法 reload / 热更任何新代码**，设备停在旧 bundle。这是 tab 徽标出不来的直接原因。
  3. 我改的文件 tsc 干净（报错全在冲突文件）；3 个测试文件 23 passed。**cherry-pick 未动**（并行会话属地）。
- **顺带改动**：`app/notifications.tsx` 屏内挂 `useIntoYouCounts()`（进屏立即刷新 + 10s 轮询），徽标不再等 app 级 30s 轮询；`docs/notifications.md` 加《计数新鲜度》并改写已知时序。
- **待用户验证**：冲突解决 → App reload → 若要看到 tab 徽标，需要**真有未读**（此前进过页面，服务端未读已被旧版进段清零清掉；造一条新 like / 访问再看）。徽标只在未读 > 0 时显示；若要「一直显示总人数」需另说。

### 17:07 · sitin-rn：luka 商店 Contact Email 换成 apple@lukasap.com

- **来源**：飞书 wiki《Luka-上架文案包（IOS）》（`PPotw6ZKoilT8pkXRm3cdas7nPg`，16:42 更新）「应用基本信息」表的 `Contact Email` = `apple@lukasap.com`；用户要求据此改。
- **改动**：`apps/luka/src/lib/legal.ts` 的 `SUPPORT_EMAIL` `lukaiosfeedback@lukasap.com` → `apple@lukasap.com`，注释改引上架文案包（覆盖同日《赛跑包物料分配》的「用户反馈邮箱」口径）。同一常量管三处：Help 屏「Email us」、sign-in 忘记密码的 mailto/toast、商店列表联系方式。
- **验证**：biome 干净；`pnpm --filter luka typecheck` 0（并行会话在途编辑一度红，恢复后重跑通过）；`review-mode-boundary` 2 passed（该测试扫描邮箱字面量，`apple@` 不触红线）。
- **提交 / 推送**：`d820db478` `chore(luka): 商店 Contact Email 换成 apple@lukasap.com` → `origin/feature/luka-ios`（`17d45cefd..d820db478`，pre-push boundaries + luka typecheck 过）。
- **坑**：首次 `git commit` 把并行会话已 `git rm` 暂存的 T0058 删除文件一起卷进来（9 files）—— 工作区多会话并行时必须用 `git commit --only <path>` 做单文件提交；已 `reset --soft` 重做，对方 index 未受影响。
- **17:11 追加**：用户「push」→ 把并行会话的 `e7aeff2fa feat(luka)!: Feed 下线打招呼与评论（T0058）` 也推上 `origin/feature/luka-ios`（`d820db478..e7aeff2fa`，pre-push boundaries + luka typecheck 过）。

- **17:04 补记**：cherry-pick 冲突已由并行会话收尾（`.git/CHERRY_PICK_HEAD` 消失、无 UU 文件），Metro bundle 恢复编译（29.8MB，含 `TabUnreadBadge`）。App reload 后即可验证 tab 徽标；用户确认徽标位置 = Activity / Visitors 文字右上角（截图红框处）。

### 17:12 · sitin-rn：luka 爪印徽标回到未读口径（撤回整套「总人数」链路）

- **用户裁决**：「爪印也要根据是否未读来」——爪印与两个 tab 徽标统一为未读语义：有未读才显示、显示未读数、该源清掉即消失；没有新东西不显示。
- **撤回的总数链路**（此前按「两段实际人数之和」实现，现已整体移除）：
  - `apps/luka/src/app/(tabs)/index.tsx`：爪印回 `likeUnread + visitorUnread`。
  - `stores/into-you-unread.ts`：删 `likedTotal` 字段与 set/clear；`refresh()` 回 `getLikeUnreadCount()`；头部注释改为「所有徽标统一未读」。
  - `packages/business-vibes/src/vibes.ts`：删 `getLikedCounts`（恢复只留 `getLikeUnreadCount`）；`services/intoyou.ts` 删导出。
  - `tests/stores/into-you-unread.test.ts`：重写 6 用例（去 total 断言）。
  - `docs/notifications.md`《徽标》重写：四个徽标统一未读；保留撤销过程说明；`visitorTotal` 仅 Me 页 tile 用，与徽标无关。
- **验证**：tsc 干净、biome 5 文件干净、`pnpm --filter luka test` 953 passed / 4 既存失败（candidate-card-fields ×1 + shape-language ×3）。另 `tests/services/me.test.ts`、`good-review.test.ts` 文件级失败为 expo 源码在 vitest 下的解析环境问题，且来自并行会话在飞的改动（依赖链不含本次模块）。
- **未提交**。

### 17:20 · sitin-rn：luka 飞书 Buglist 五条开工 T0052/54/57/58/61（5 笔提交已推）

- **背景**：上一条分析后用户拍板五条全修；T0058 口径定为「只删 feed：卡片气泡 + 详情页 Say hi + 评论 UI（/post/[id] 保留大图+点赞）」。
- **T0057（P1）** `c9cd2a1c2`：移植 blueprint 0032 三件套到 apps/luka —— 新增 `lib/bottom-inset-guard.ts`、`lib/keyboard-exit.ts`、`lib/pager-relayout.ts`、`hooks/use-pager-relayout.ts`；接线 `use-bottom-inset` / `clearance{,.ios}.ts` / `tab-bar` / `compose` 两条退出 / scaffold 返回键 / edit-profile 六页返回+Save / 四处 PagerView（Discover、Me、Into You、Notifications）；新增 3 个测试文件 20 条 + `docs/keyboard-and-insets.md`。
- **T0054（P2）** `7ec47e35a` + `b08f5a7a8`：从 fix worktree 的 `0f7afa6b5` 等价移植键盘避让改 `KeyboardStickyView`（四页 + scaffold；**sign-in 未应用**——修复分支那份缺本分支的 B 版稿重做，已在提交信息注明）；并把「进屏转场里新建仓鼠视频播放器」推迟到 `useAfterInteractions()` 之后（`PeekingHamster` 先用同一姿势静态图顶着、`ComposePet` 转场前不挂）。
- **T0058（P1）** `e7aeff2fa`：Feed 卡 Say hi 圆气泡、详情页 Say hi 胶囊、评论底栏（输入框/评论数/纸飞机/`PostRepliesCard`/`PostCommentRow`/`FeedCommentGlyph`/`PostPerchHamster`）、My feeds 评论入口全删；整文件删 `use-feed-buzz`/`services/feed-buzz`/`lib/scene-author`（含单测）与 `services/scene` 的评论函数；保留 Nearby 划卡 Say hi、详情页点赞、共享包与 mock 契约；`docs/discover-play.md` 加《Feed（2026-09-21 下线）》。
- **T0061（P0）** `2b7738e2d`：paywall 赠币行改读后端目录 `subCoins`（没配就不显示）、去掉已下线的「7 Feed Greetings」；`packages/business-payments` 新增 `periodFromPlan(name, productId)`（周期优先目录 name，ID 后缀兜底）+ 目录侧记住周期供 `VerifyIOS`/`FakeVerifyIOS` 使用 → 版本 0.7.1→0.7.2 + CHANGELOG（补记此前未记的 ID 后缀补漏）。60 tests 全过。**赠币发不发只能后端**，对账清单见 troubleshooting 笔记。
- **T0052（P1，先验证）** `0296c01d1`：在 `getMyProfile` 与 `getMyProfilePageAvatar` 加**只记字段存在性**的日志（`[profile] self …` / `[profile] page code=… …`，仅 `isDevToolsAvailable()`，无 PII）——`rn-net` 默认 400 字符会把 self 的 `profession/education/bio` 截掉，不能靠 `[接口]` 行判断「空」。等真机日志：两模型都空 → 后端；page 有值 → 客户端合并该读模型（注意它没有 education/height、`code=0` 不是失败）。
- **验证**：luka typecheck 干净；952 tests 中 948 passed（4 条既有红：good-review + candidate-card-fields + shape-language ×2，与本次零交集）；business-payments 60 passed；biome 干净；`git push` 走过 pre-push（boundaries + 8 个受影响 workspace typecheck，42.5s 全绿）。
- **并行会话注意**：推前 luka tsc 一度因并行会话在途的 `into-you-unread`/`intoyou` 报错；对方落定后 pre-push 全过。
- **沉淀**：[luka-profile-fields-lost-on-relogin](../30-Resources/troubleshooting/luka-profile-fields-lost-on-relogin.md)、[subscription-coins-zero](../30-Resources/troubleshooting/subscription-coins-zero.md)
- **待办**：T0052 等真机日志（dev/preview 包 Export Log grep `[profile]`）；T0061 后端对账；真机验收三条（键盘弹着点关闭 → Feed 完整一屏；进 edit 子页不顿；详情页只剩点赞）。

### 17:12 · sitin-rn：T0053 地址保存后被设备定位覆盖（luka，未提交）

- **Bug**：飞书《IOS Luka Buglist 汇总》T0053（P1，录屏 `飞书20260921-143819.qt`）。两次保存时编辑页都显示 `75231 / Dallas, Texas`：第一次 hub 变 `Dallas, TX`，未编辑直接保存后 hub 变 `San Jose, California`。
- **根因**：两个写地址的入口互相打架。`hooks/use-discovery-feed.ts` 的 `resolveDeckZip` 在 Discover 挂载时把设备反解析地址静默写回后端，覆盖用户刚存的 ZIP —— 它写的 province 是**全州名**（`resolvedToGeoLocation`），而 ZIP 表路径写州码，录屏里错值正是 `San Jose, California`，只能是这条路径。编辑页全程看不出来：只显示 ZIP（下面那行 `Dallas, Texas` 是 ZIP 表的搜索结果，不是已选值），且 seed 没等 `useMe().initialLoading`（Name/Age 已修过同类坑）。
- **修法**（用户选 A+C）：
  - A：新增 `planDeckLocation`（`lib/location.ts`）——资料里已有 `location/zip/geoLocation` 时不再回写后端，卡池也用保存的 ZIP；只有资料完全没有地址时才用设备定位补空并回写。
  - C：编辑页 seed 等 `initialLoading`；新增 `completeSelectionFromZip`——只有 ZIP 时用内嵌 ZIP 表补 city/state，city/state 与 ZIP 打架时以 ZIP 为准并丢掉异城坐标（所见即所存）。
  - 新增 `apps/luka/docs/location.md`：三个写入点、两条约定、后端 `geoLocation` 可能只回 ZIP 或与 ZIP 不一致。
- **验证**：`tests/lib/location.test.ts` 11 passed（新增 8 条覆盖 T0053）；`pnpm luka:typecheck` 0；biome 干净。全量里 `candidate-card-fields` / `shape-language` / `me.test.ts` 失败与本次无关——用 `git stash push -- <本次 4 文件>` 回退后 `me.test.ts` 同样失败，确认为并行会话在飞改动/环境问题。
- **提交 / 推送**：用户「push」→ `f2c6bb0fd` `fix(luka): 保存过的地址不再被 Discover 设备定位覆盖，编辑页所见即所存（T0053）` → `origin/feature/luka-ios`（`7fd855557..f2c6bb0fd`，pre-push boundaries + luka typecheck 过）。首次 push 被并行会话在飞的 `profile/[id].tsx`（`onSeeAll` 半成品）拦下，未绕 hook，等对方补齐后重推通过。
- **顺带发现**：`apps/iris`、`apps/blueprint` 的 `use-discovery-feed` 有同样的 GPS 覆盖形状，本次未动。

### 17:20 · sitin-rn：luka 通知徽标工作提交并 push（7fd855557）

- **用户要求 push**。远端 `origin/feature/luka-ios` 与本地一致（fetch 后 `rev-list 0 0`），fast-forward。
- **途中发现**：并行会话此前的提交（`c9cd2a1c2` 等）用 `git add` 把我**部分未提交改动**扫进了它们的提交（`notifications.tsx` 的徽标/清零/reveal/pull-to-refresh 已在其提交里），但配套的 `lib/notification-badges.ts` helper 与 `tab-unread-badge.tsx` 还没提交 → HEAD 当时**编译不完整**（import 了不存在的模块/导出）。本次提交补齐，HEAD 恢复完整。
- **提交**（只 stage 我的 13 个文件，并行会话在飞的 `location.tsx` / `use-discovery-feed.ts` / `keyboard-and-insets.md` 等一律未动）：
  - `7fd855557` `feat(luka): 通知页分段未读徽标与默认段，两段可翻页/下拉刷新`（13 files, +347/−77；含 2 个新文件：`tab-unread-badge.tsx`、`tests/stores/into-you-unread.test.ts`）
- **门禁**：pre-commit biome 12 files 干净；pre-push `boundaries OK` + `typecheck apps/luka` 通过；push `0296c01d1..7fd855557`。
- **验证提醒**：tab 徽标与爪印均为未读口径（0 不显示），需造新 like/visit 才能在设备上看到；reload App 即取到本提交。

### 17:36 · sitin-rn：luka T0055 + T0056 —— feed 入口资料页去动作栏 / See all 进全帖列表（未提交）

- **需求**：飞书 Buglist T0055（P1，feed 头像进的 profile 页不应有打招呼按钮）用户拍板「整颗按钮删掉」；T0056（P1，See all 弹出相册而非全部帖子）用户拍板「新开全帖列表页」。录屏 `飞书20260921-150844.qt` 抽帧确认现状：See all 弹全屏相册 `1/14`，底部是 Say hi。
- **实现**：
  - T0055：`lib/profile-cta.ts` 新增纯函数 `profileShowsActionBar(source)`（仅 `feed` 为 false）；`app/profile/[id].tsx` 按它整块撤掉 `ProfileActionBar` + `BottomBlurFade`，滚动内容底部留白 96 → 24。其它入口（Nearby / People / Quick Pick / 喜欢我 / 访客 / chat / friends）不变。
  - T0056：新增 `app/profile/[id]/posts.tsx`（单列全帖列表，`useProfilePosts` 分页 + 下拉刷新 + 空/错态）与 `components/profile/profile-post-card.tsx`（作者行 / 正文 / 照片 / 点赞 / 时间，复用 `DiscoverFeedPhoto`、`HeartGlyph`；点赞走共享 `usePostLike`，与详情页同一份状态）；`ProfileMoments` 加 `onSeeAll`，See all 改 push `/profile/<id>/posts?name=`，点照片仍开画廊；`_layout.tsx` 注册 `profile/[id]/posts`；`usePostLike` 入参放宽为 `Pick<ScenePost, …>` 以吃 `ProfilePost`。`docs/discover-play.md` Feed 章节补两条，功能对照表同步。
- **验证**：biome 8 files clean；`pnpm luka:typecheck` 0；`tests/lib/profile-cta.test.ts` 17 passed（新增「feed 去栏 / 其余保留」2 条）；全量 959 中 955 passed，4 条红（good-review / me / candidate-card-fields / shape-language）与本次零交集、offender 列表无新文件。Metro bundle curl 200 且含 `ProfilePostCard` / `ProfilePostsScreen` / `profile/[id]/posts` —— 路由与编译已验证；模拟器仍停在 onboarding 登录守卫（未登录），**视觉未实机复核**，留真机走查。
- **坑 / 沉淀**：[expo-router：叶子路由与同名目录能共存；不起 Metro 的静态验证法](../30-Resources/troubleshooting/expo-router-leaf-branch-route-static-check.md)。
- **并行会话注意**：17:12 日志提到本工作区有并行会话的 `profile/[id].tsx` `onSeeAll` 半成品；我动手前该文件已回到干净态，本次改的 `discover-play.md` / `_layout.tsx` / `profile/[id].tsx` / `profile-moments.tsx` / `use-post-like.ts` / `profile-cta.ts` 及两个新文件**全部未提交**，请勿被其它会话的 `git add` 扫入。
- **未提交**：按规范模式留在工作区（未 commit / push）。

### 17:54 · sitin-rn：拉取 luka 最新代码 + 本地出 test/preview IPA（70 MB）

- **做了什么**：`feature/luka-ios` fetch 后 ff `f2c6bb0fd → 288fb2d91`（Merge PR #531，含 `9dad78adc` 通话计费 / 金币发放 / 通话中充值 UX 修复；catalog 顺带升 `expo-constants ~57.0.19`、`expo-linking ~57.0.10`、`expo-router ~57.0.22`，加 `@expo/metro-runtime 57.0.16` override）。`pnpm install`（+23 −121 包）后本地打 IPA：`test` 环境 + `preview`（Ad Hoc）+ **clean prebuild**（catalog 升了 expo 原生包，必须重生成原生工程）+ team `39CFYH6W55` + buildNumber 10000。
- **为什么**：用户要求「打一个 luka 的 ipa 包」；worktree 里 T0055/T0056 未提交改动按用户预期一并打进包。
- **结果**：`apps/luka/ios/build/export/Luka.ipa`，70 MB，`com.presence.gracechat` 1.0.0 (10000)，Ad Hoc 描述文件含 71 台设备；日志 `apps/luka/build-logs/build-20260921-174106.log`。**未上传蒲公英**（用户未要求，产物本地自取）。
- **关键命令**：`export PATH="$HOME/.nvm/versions/node/v24.21.0/bin:$PATH"; IOS_TEAM_ID=39CFYH6W55 scripts/build-ipa.sh luka test preview clean 10000`（本机默认 Node 22，仓库要求 24）。
- **拉取时操作**：`discover-play.md` 本地改动与 incoming 撞上 → `git stash push -m "..." -- apps/luka/docs/discover-play.md` → `git pull --ff-only` → `git stash pop`（两边改动落在不同段落，auto-merge 无冲突）。
- **注意**：`ios/Pods/React-Core-prebuilt/.last_build_configuration` = `Release`，是本地 Release 构建的预期结果（marker 在 → 后续 Debug 构建会自动换回 debug，不会重演 09-20 的 core 变体链接失败）。

### 18:01 · sitin-rn：重启 luka 真机调试 Metro（Node 22 → 24）

- **做了什么**：旧 `expo start`（PID 97036）已挂 1 天多，`kill` 掉它 + 两层 pnpm 包装进程（97003、96982），端口 8081 释放后重启：`PATH=~/.nvm/versions/node/v24.21.0/bin:$PATH` + `pnpm --filter luka start`（detached，`nohup`）。验证：8081 LISTEN、`/status` → `packager-status:running`。
- **为什么**：用户要求「重启一下 真机调试」。旧进程跑在 nvm 默认 **Node 22** 上，与 `.nvmrc` / `engines: >=24` 不符，本次按仓库约定换 Node 24.21.0（同 17:54 打包那次的做法）。
- **真机连接**：dev client URL `luka://expo-development-client/?url=http%3A%2F%2F192.168.2.242%3A8081`（scheme `luka`）。
- **日志**：`apps/luka/build-logs/metro-20260921-180049.log`（gitignored）。

### 18:20 · colna-memo：修坏掉的本地索引 + 补推积压的 sync 提交

- **起因**：本次记工作日志后 `./colna sync` 卡住 → 查明是**并行会话的 sync 正在全量重建索引**（持 `.colna/index.zvec` 锁，新进程静默等待，不报错不输出）；该重建最终在 flush 阶段失败：`zvec error InternalError: Invalid: File is too small: 0`（`.colna/index.zvec/0/scalar.0.ipc` 为 0 字节），且 `state.json` 未落盘 → 索引不可用。
- **修复**：`.colna` 是 gitignored 缓存 → 备份并移走坏的 `index.zvec`（`.colna/index.zvec.broken-20260921-181723`），后台单进程 `./colna index` 全量重建（202 批 × 16 块，约 25 分钟），日志 `/tmp/colna-index-fix.log`；完成后链式 `./colna sync` 补同步本文这条。
- **补推**：仓库此前积压 **7 个未推的 `colna sync` 提交**（17:11 ~ 18:01）。原因是 sync 顺序为 `add+commit → pull → reindex → push`，历次都在最长的 reindex 阶段被 agent 工具超时 kill，push 从未执行。已在 KB 根手动 `git push origin main` 补推（`8bf6ce0..25c7216`）。
- **沉淀**：`30-Resources/troubleshooting/colna-sync.md` 增补两条 ——「并发 sync 持锁时新进程静默等待（判死活看 batch 速率，别急着 kill / 重跑）」与「ahead 一堆 `colna sync` 提交 = 历次死在 reindex，手动 push 即可补」。

### 19:43 · sitin-rn：luka 协议 appName 回改为 `luka`（表格 `lukasap` 已过时）

- **背景**：用户问「请求头里有没有 appname」→ 查明协议 `app_name` 在 `params` 头（`packages/rn-net` 的 `getParams`）+ `User-Agent` 里带显示名；luka 的值是今日 16:37 随域名切换一起改成 `lukasap` / `lukasap_android`（`0c18c24cc`）。
- **冲突核对**：先读飞书《赛跑包物料分配》K6（`docs +fetch` 报 sheet 不支持 → `wiki +node-get` 拿 obj_token `UR0vsXwf9hioUWtBTlWcrbItnqg` → `sheets +csv-get`），表格仍写 `appName: lukasap`；仓库注释也警告「appName 与域名必须配套」。用户确认表格已过时、后端在 `lukasap.com` 上注册的仍是 `luka` → 回改。
- **改动**：`apps/luka/app.config.ts` —— `appName` / `appNameByPlatform` `lukasap[_android]` → `luka[_android]`，注释改为「appName 是后端租户注册名，不随域名变」；`apps/luka/README.md` 域名表同步。bundle id / App Group / 邮箱仍是 `lukasap`（身份，不受影响）。
- **验证**：`pnpm luka:typecheck` 0；biome clean；`tests/config + settings` 23 passed。全量 1009 中 7 红（good-review / me 文件级 + candidate-card-fields ×1 + shape-language ×3 + use-swipe-actions ×3）均为工作区并行改动既有红，与本次零交集。
- **坑 / 沉淀**：飞书「表格」类文档不能走 `docs +fetch`（报 `Unsupported document type 'sheet'`），正确路径是 `wiki +node-get` 取 `obj_token` 后切 `lark-sheets`。
- **真机仍显示 `lukasap` 的原因（19:46 处置）**：`readAppConfig()` = `Constants.expoConfig`，`extra` 随 **Metro 启动时算出的 manifest** 下发，不是从 JS 源码现读 —— 旧 Metro 是 18:01 启的（改动 19:41 才落），故仍是旧值。已重启 Metro（19:46，Node 24，日志 `apps/luka/build-logs/metro-20260921-1946.log`），`curl -H 'expo-platform: ios' -H 'accept: application/expo+json' http://127.0.0.1:8081/` 验证 `extra.expoClient.extra.appName = luka`。真机需**冷启/重开 dev client** 拉新 manifest。
- **注意**：真机日志里 `package_name: com.lukasoc.luka.devx` 是 `Application.applicationId`（原生二进制 id），来自已装的旧 dev client，与 app.config 无关；要重装包才会变。
- **提交**：用户「把未提交的都提交了」→ `464491d64` `fix(luka): 协议 appName 回改为 luka —— 后端租户注册名不随域名变`（2 files，+9/−8；未 push）。仓库根未跟踪的 `-`（devicectl UDID 误产物）没提交，仍待用户定夺删不删。

### 18:35 · sitin-rn：luka T0054 改为「进屏不自动弹键盘」（fbbc49d22，已推）

- **用户口径**：T0054 换修法 —— 进入页面时不要自动弹起键盘（原先是靠 transform 把卡顿压小）。
- **改动（9 文件）**：
  - 删 `autoFocus`：`edit-profile/{name,occupation,education,bio,location}` + `app/compose.tsx`；要输入时点输入区。
  - `components/onboarding/scaffold.tsx` 加 `autoFocusInput`（默认 true＝注册五步不变）；编辑资料 Name 传 `false` → 紧凑态只跟 `keyboardVisible` 走（没有会迟到的键盘，就不需要「首帧即紧凑」的规避）。
  - `LocationPicker` 编辑资料侧不再自动聚焦（`OnboardingLocationPicker` 不变）；`docs/keyboard-and-insets.md` 加 §5 记录约定。
  - 前两笔 T0054 性能改动（transform、转场推迟仓鼠播放器）保留：手动点开键盘时仍受益。
- **验证**：luka typecheck + biome 干净；全量 7 条红全与本次无关（`use-swipe-actions` ×3 = 并行会话在途的 swipe-limit mock 缺导出、`me.test` = node_modules 转译环境错、既有 3 条）。push 走 pre-push（boundaries + luka typecheck，7.5s）。

### 19:56 · sitin-rn：luka 划卡「小卡切回大卡停在已打招呼的卡上」（未提交）

- **报障**：【划卡】切到小卡（列表）把人全打招呼后，切回大卡（卡组）—— 展示的是刚在列表打过招呼的人，打不了招呼、也划不动（照片区只刮不划），只能点 Skip。预期落回未打招呼的人。
- **根因（两层，都在 `apps/luka`）**：
  1. 卡组入口 `firstUnsentNearbyIndex`（`lib/nearby-list-pool.ts`）在「游标之后整段都发过招呼」时**回落到游标本身**（Sent 卡），而入口索引 `initialIndex` 只在卡组挂载那一帧生效：补批的新人是挂载**之后**才到的，没有任何东西把卡组往前推 → 永远停在 Sent 卡。
  2. Sent 卡的划卡门（`DiscoveryDeck` 的 `swipeBand`）仍按「未刮开＝只有信息带能划」处理，照片区划不走；Sent 卡又没有再打招呼的意义 → 用户视角就是「划不动，只能 Skip」。
- **修法**：
  - `lib/nearby-list-pool.ts`：`firstUnsentNearbyIndex` 返回值改 `number | null`（`null` = 没有能停的卡，调用方去补批）；新增纯函数 `deckSkipTargetAfterAppend`（停在 Sent 卡 + 补批 → 第一个未打招呼的人，否则 `null`）。
  - `components/discover-nearby/nearby-view.tsx`：deck 模式下 `firstUnsent === null` 时停在游标 + `prefetch()` 补批，补到有人可停或 `exhausted` 为止。
  - `components/discovery/discovery-deck.tsx`：补批 effect 里若还停在 Sent 卡（且该卡的打招呼飞行动画没在等定时前进，避免多跳一张）→ `cardScroll.skipTo(next)`；Sent 卡 `swipeBand` 直接给 `FULL_CARD_SWIPE_BAND`（整卡可划，不用先刮开）。
  - `components/discovery/card-stack.tsx` + `card-scroll.tsx`：`CardScrollHandle` 新增 `skipTo(index)`（吸附、无动画、不上报）；`docs/discover-play.md` 同步该段行为。
- **验证**：`nearby-list-pool.test.ts` 11 passed（新增/改写 5 条：null 回落、补批吸附、不动未发卡、越界）；`pnpm luka:typecheck` 0；biome 6 文件干净；Metro bundle 200（29.8MB，含 `deckSkipTargetAfterAppend`）；全量 1007 passed / 7 条既有红（与 18:35 那条同一批，`git stash` 对照确认非本次）。**未实机走查**（本会话无视觉）：真机验收路径 = 列表把人打完 → 切回大卡 → 应短暂停在游标后自动跳到未打招呼的人；服务端打尽时应能整卡划走。
- **未提交**（规范模式；工作区有并行会话改动，未混入）。

### 20:12 · sitin-rn：luka 资料页打招呼返回后卡组卡片不划走（并入上一条，未提交）

- **报障**：【nearby】在 profile 点打招呼，返回 Nearby 时这张卡应划走；实际卡死在当前页，除非点 Skip。用户问「和上一条是不是同一个问题」→ 答：同族不同触发（上一条是列表入口/补批；这条是卡组内当前卡被别处标记 `Sent`）。
- **根因**：卡组不会对 `sentIds` 变化做反应 —— 当前卡在资料页被 `markSent` 后，`candidates` 没变、卡组没换人；`swipeBand` 仍是「未刮开只有信息带能划」（照片区划不走）。
- **修法**：`DiscoveryDeck` 加聚焦 effect（`useFocusEffect` + 稳定回调 + 每次渲染刷新的 ref 拿最新闭包）——回到 Nearby 时若当前卡已 `Sent`：先放开整卡可划（`FULL_CARD_SWIPE_BAND`），再 `deckSkipTargetFromCurrent` 找下一张未打招呼的人；下一张就能停走 `swipeToNext()` 飞出动画，中间夹着 Sent 就 `skipTo` 吸附，后面暂时没人则等补批 effect 吸附。**刻意不监听 `sentIds`**：卡组自己的 Say hi/上滑也会翻它，那一刻卡片正等自己的前进动画，盯着集合会多跳一张（`pendingSayHi` 闩只覆盖按钮路径）。
- **lib 更名**：`deckSkipTargetAfterAppend` → `deckSkipTargetFromCurrent`（两个触发共用同一决策），测试标题同步；`docs/discover-play.md` 补这段行为。
- **验证**：`nearby-list-pool.test.ts` 11 passed；`pnpm luka:typecheck` 0；biome 6 文件干净；Metro bundle 200 且含 `deckSkipTargetFromCurrent` / `advanceOnReturn`；全量 1007 passed / 同 7 条既有红。**未实机走查**：验收 = 卡组里点开某人资料 → 打招呼 → back，卡片应飞出到下一个未打招呼的人；末尾时先整卡可划、补批后自动吸附。真机要 reload bundle 或重打包（17:54 那版 IPA 不含本次改动）。
- **提交 / 推送（用户要求 push）**：`a759a0692` `fix(luka): 划卡列表切回与资料页打招呼返回后不再停在 Sent 卡上`（7 files，+209/−16）→ `origin/feature/luka-ios`（`fbbc49d22..a759a0692`）。pre-commit biome 6 文件、pre-push boundaries（41 包）+ luka typecheck（7.9s）全过。`discover-play.md` 是混合文件：用 `git apply --cached` 只暂存我的 hunk，并行会话的 T0055/T0056 两处（Say hi 表行 + Feed 章节）仍在工作区未扫入。

### 20:25 · sitin-rn：重新出 luka test/preview IPA（含 Sent 卡修复 + T0055/T0056 未提交改动）

- **做了什么**：用户「拉取 feature/luka-ios 最新代码然后重新打 ipa 包」→ `git fetch origin feature/luka-ios` 确认 `HEAD = origin/feature/luka-ios = a759a0692` 无新提交；复用 `ios/`（上周五仓 catalog 升 expo 原生包后已 clean prebuild 过；本轮原生依赖未变）打 test + preview（Ad Hoc）：`export PATH="$HOME/.nvm/versions/node/v24.21.0/bin:$PATH"; printf 'n\n' | IOS_TEAM_ID=39CFYH6W55 scripts/build-ipa.sh luka test preview "" 10001`。
- **背景坑（20:09 首打的包是废的）**：首打时并行会话正在 `git stash` 验证（`nearby-list-pool.ts` / `discovery-deck.tsx` mtime 20:10–20:11），JS bundle 生成于 20:10:54 → 解包 grep `deckSkipTargetFromCurrent` / `FULL_CARD_SWIPE_BAND` 均为 0，判定没打进 19:56–20:12 的 Sent 卡修复，作废重打。
- **结果**：`apps/luka/ios/build/export/Luka.ipa` 70 MB（73,112,xxx bytes），`com.presence.gracechat` 1.0.0 (10000)，Ad Hoc（描述文件 `iOS Team Ad Hoc Provisioning Profile: com.presence.gracechat`）；产物核验：`deckSkipTargetFromCurrent` ✓、`Like post` ✓（未提交 T0055/T0056 改动已打入）、`firstUnsentNearbyIndex` ✓；`extra.apiBaseUrl=https://api-dev.lukasap.com`、`extra.appName=luka`。日志 `apps/luka/build-logs/build-20260921-201948.log`。**未上传蒲公英**（用户未要求）。
- **注意**：用户要在 10001，但 luka 的构建号是 `app.config.ts:15` 的常量 `BUILD_NUMBER = 10000`（prebuild 烤进 Info.plist），脚本第 5 个参数只影响 xcodebuild 的 `CURRENT_PROJECT_VERSION`，对本包**无效**，产物仍是 10000。按仓库约定该常量只在提审前 +1，不为此改。
- **沉淀**：坑 + 核验方法见 `30-Resources/troubleshooting/sitin-rn-local-ipa-verification.md`。

### 20:34 · sitin-rn：T0055 / T0056 提交并 push（衔接 17:36 与 20:25）

- **用户要求 push**：17:36 那两个改动此前一直留在工作区（当时标了「未提交」）；本次只 stage 这两个改动的 9 个文件（`lib/profile-cta.ts` + 单测 / `app/profile/[id].tsx` / `profile/[id]/posts.tsx` / `profile-moments.tsx` / `profile-post-card.tsx` / `use-post-like.ts` / `_layout.tsx` / `docs/discover-play.md`），并行会话在飞的 `apps/luka/README.md` 与 `app.config.ts`（appName 回 `luka`）未混入。
- **提交 / 推送**：`1ef4c4e4e` `fix(luka): feed 入口资料页去掉动作栏、See all 改进全帖列表（T0055 / T0056）`（9 files，+247/−21）→ `origin/feature/luka-ios`（`a759a0692..1ef4c4e4e`）。pre-commit biome 8 文件、pre-push boundaries（41 包）+ luka typecheck（6.0s）全过。
- **与 20:25 IPA 的关系**：那版 test/preview IPA（`build-20260921-201948.log`，`Like post` ✓）打的就是这两个改动的工作区态，内容 = `1ef4c4e4e`，无需为本次提交重打；尚未上传蒲公英。

### 20:47 · sitin-rn：luka 新手引导 Chats 第 4 步（页头爪印）整层偏移一个状态栏

- **报障**：用户贴实机录屏截图 —— Chats 引导 `4 of 4`「Stay in the loop」的气泡/箭头/高亮都贴在页头顶上，没对上爪印。
- **诊断（截图像素取证，无设备可用）**：截图 740×1614 → 反推视频区 x0≈15.5 / y0≈76、1.891 px/pt、机型 375×812。气泡白块 `598×317+93+172`：左边被右边缘夹住（left=39pt = 320 宽 + 16 右留白）→ 水平没偏；但气泡上沿在 59pt。按 `placement=below`（`top = rect.bottom + 14`）反推 **rect.bottom=45pt → rect.y≈5–7pt** = 页头还没让开状态栏（44pt）时爪印在页头里的位置（(54−40)/2=7）。旁证两条：团队头像未读徽标实测 `x 565..603 / y 172..210` 与「页头在 44pt 处」的几何完全吻合（页面渲染没问题，是引导缓存的坐标旧了）；QuickTime 半透明控制条下透出的亮块右沿 ≈702px = 洞的右沿（363pt）同样吻合。
- **根因**：`useGuideTargetMeasure` 只在目标自己的 `onLayout`（+ 下一帧）测量。`SafeAreaView` 的原生 padding 比目标首次布局晚一拍生效，页头整体下移 44pt 时目标**相对父容器的位置没变** → 不再有 onLayout → 坐标永久停在 y≈7；排队时的 `requestMeasure` 又早于这一刻（今天 11:20 把 Team 入口挪进页头，页头结构变动让这条旧路径第一次被看到）。
- **修法**：`hooks/use-guide-target-measure.ts` 在 onLayout 后加一拍补量（`GUIDE_TARGET_SETTLE_MS = 300`，每次 onLayout 重置、卸载清定时器）。补量会让坐标表再动一次 → `PageGuideHost` 的「稳定 300ms 才快照」自动顺延到补量之后，所以目标晚挂载也能收住。`docs/page-guide.md` 同步（测量三条保险 + 实现文件表）。
- **验证**：`pnpm --filter luka typecheck` 0；biome 干净；四组引导测试 17 passed。**未实机走查**：验收 = 新账号注册完进 Chats 走到第 4 步，气泡应在爪印下方 14pt、箭头指爪印、洞套住爪印。
- **提交 / 推送（用户要求 push）**：`35608d542` `fix(luka): 新手引导目标在安全区生效后补量坐标，爪印步不再整体偏移`（2 files，+28/−4）→ `origin/feature/luka-ios`（`1ef4c4e4e..35608d542`，**顺带带上并行会话未推的 `464491d64`（协议 appName 回改 luka）**）。pre-commit biome、pre-push boundaries（41 包）+ luka typecheck（7.0s）全过。
- **沉淀**：`30-Resources/troubleshooting/react-native-stale-measure.md`。

### 20:48 · sitin-rn：luka 分段徽标「压在文字上」= dev 端 Uniwind CSS 表旧了，非代码问题

- **报障**：用户贴实机截图（20:13）—— 通知页 Activity / Visitors 的未读徽标压在 "Visitors" 文字左上（红点盖住 "Vi"），应贴 label 右上。
- **结论：代码与编译产物都正确，是 app 进程里的 Uniwind CSS 表停在旧版本**（本仓库已知坑，见 `30-Resources/troubleshooting/uniwind-dev-stale-css-new-class.md`）。证据链：
  1. 抓 Metro 当前 bundle（`apps/luka/.expo/.virtual-metro-entry.bundle`，29.8 MB）：表里有 `"left-full"`: `left: "100%"`；徽标其余类（`absolute` / `-top-1` / `ml-1` / `h-[18px]`）也都在，且都是旧类 —— 只有 `left-full` 是本文件独有的新类（全仓仅此一处）。
  2. 把 bundle 里的表抠出来喂 Uniwind 运行时（Node + `--import` loader stub 掉 `react-native`）：`"absolute -top-1 left-full ml-1"` → `{position:"absolute",top:-4,left:"100%",marginLeft:4}` ✓。
  3. Yoga 复刻同构树（临时目录装 `yoga-layout`）：absolute 子节点 `left:100% + ml 4` → `left = 文本宽 + 4` ✓；RN Fabric `conversions.h` 把 `"100%"` 转 `StyleLength::percent(1)` ✓。
  4. Metro（PID 81565）是 19:48 才重启的，app 进程早于它、只接过 fast refresh → JS 新、表旧，旧类照常命中，新类 `left-full` 静默丢失，徽标落回静态位（左上）。
- **处置**：不改代码，让用户整包 reload（Dev Menu → Reload / 杀掉重开）后复核；若仍重叠再改成 onLayout 量 label 宽度 + px 定位（备用方案）。
- **核对手法沉淀**：已追加到上面那篇 troubleshooting（含抠表 + stub 运行时 + Yoga 三步复现法）。
- **未改任何代码**；若 reload 后正常，本条即为闭环。

### 20:55 · sitin-rn：T0061 订阅赠币 +0 复核（未改代码）

- **来源**：用户贴 T0061（【订阅】订阅成功后，赠送金币数为 0，P0，未开始，指派人张峥）。从飞书 base 拉到行（`recvvPZFKFmGHz`）与截图 `T0061.png`：Sparks history 9/21，15:36 `Subscription coins +0`，余额 5 = 15:28 `+3` + 11:37 `+2` 两次签到 —— 订阅生效、只差发币。
- **复核（客户端）**：`mapCoinHistory` 的 `amount = |coinValue|` 全来自后端 `GetUserCoinChangeHistory`；两条下单/验单链（DTC `CreatePayment(productId/provider/bizType)`、iOS `VerifyIOS(transactionId/bundleId/appAppleId/subscriptionId)`）都没有赠币字段；proto 里也没有 claim subscription coins 类 RPC。客户端能做的两件（周期码优先取目录 `name`、paywall 赠币行读 `subCoins`）今天 17:08 已随 `2b7738e2d` 落地。
- **新证据**：`~/Downloads/session.log` / `session(1).log`（旧域名 `api-dev.lukasoc.com` 的设备日志，账号 2100067621）里 `GetSubscriptionsV3` 三次都返回 `normalSubscriptions: []` —— 该租户当时订阅目录为空，一并交给后端。
- **下一步**：已拟好给「SP后端内部群」的对账请求（subCoins 配置 / Matrix 权益 `%d` 条目 / 订单 `GetUserPaymentOrderDetail` 的 coinExpected·coinDelivered / DTC 激活与 IAP 验单两条路径分别对单），等用户确认后发。未改代码。
- **沉淀**：`30-Resources/troubleshooting/subscription-coins-zero.md` 追加「复核补充」一小节。

### 21:00 · sitin-rn：真机日志为什么还是旧域名（api-dev.lukasoc.com）

- **现象**：用户问「真机日志里为什么已经换了域名」——`~/Downloads/session.log` / `session(1).log` 里 API 请求 **全部**打 `https://api-dev.lukasoc.com`（2961 / 3457 次）、文件域 `file.archat.us`，没有一条 lukasap。
- **时间线核对**（日志里没有任何版本号，用时间对）：日志前缀时间戳是 **UTC**（`SocketHandler._connect` 那行 TIM SDK 自己打的本地时间是 10:32:33，前缀是 02:32:33，差 8h）→ 该会话是**今天 10:32–11:30 北京时间**，导出时间 13:36 / 14:06；而域名切换的提交 `0c18c24cc` 是**今天 16:37**。三处都早于切换。
- **原因**：`extra.apiBaseUrl` 是**构建期**固化进包里的（`app.config.ts` → prebuild / bundle），装上去之后不会自己变；旧包会一直打旧域名。源码里没有残留旧域名 URL（`rg lukasoc` 只剩注释与文档）。
- **对 T0061 的意义**：QA 15:36 那笔订阅也在切换前 → 同样打在旧租户上；这批日志里 `GetSubscriptionsV3` 的 30 条响应**全是空目录**（`normalSubscriptions:[]`），本机账号 `GetUserSubscription` 是 `subscriptionId:1`（无会员）—— 旧租户当时目录就没配全。要判新租户发不发币，必须用 16:37 之后出的包重测（20:19 那版 IPA 的 `extra.apiBaseUrl` 已是 `https://api-dev.lukasap.com`）。
- **未改代码**；结论已口头回报用户。

### 20:57 · sitin-rn：luka 编辑资料 Location 页两处间距（仓鼠压标题 / 提示贴 Save）

- **做什么**：`apps/luka/src/app/edit-profile/location.tsx`
  1. picker + 仓鼠的容器加 `mt-16`（64pt）：仓鼠是 112pt 的 `hamster-lean.webp`，`contentFit=contain` + `contentPosition=bottom` 在 393pt 宽的机器上可见部分伸到卡片上沿之上约 72pt，而标题「Where are you based?」下沿到容器顶只有 `pb-5` 的 20pt，所以它的身体把标题结尾整块盖住；让出 64pt 后可见顶落在标题行框下沿以下约 12pt。用 **margin 不用 padding**（同 `height.tsx` 的 `mt-16` 注释）：仓鼠绝对定位锚在容器 padding box 顶上，padding 只会推走卡片、把它留在原地。
  2. `AiTip` 加 `className="pb-4"`：它是 Save 上方最后一件东西，组件默认只有 `pt-4`、下边距为 0，原来那行小字直接贴着胶囊按钮。
- **为什么**：用户贴实机截图（05:43）报「Save 与上方提示过近；仓鼠和输入区域挡住标题」。
- **结果**：`pnpm --filter luka exec biome check` 该文件 0、`pnpm --filter luka typecheck` 0；`pnpm --filter luka test` 1007 passed / 7 failed，失败集（candidate-card-fields / shape-language ×3 / use-swipe-actions ×3 / good-review / me）与本次改动无交集，为分支既有红。规范模式未 commit，未实机复核（登录守卫挡深链，视觉留给用户走查）。
- **备注**：仅动 location.tsx；bio.tsx 的仓鼠与标题也是同一落位（标题同 `pb-5`），用户没提，未顺手改。

### 20:57 · sitin-rn：luka 分段徽标定位改「量宽 + 点值」——IPA 也错位，放弃 `left-full`

- **用户追加**：「打完的 ipa 包 也有这个问题」（Ad Hoc IPA 是整包构建、CSS 表在构建期生成，`left-full` 必在表里；顺带解包 `Payload/Luka.app/main.jsbundle` 确认徽标 className / `TabUnreadBadge` / stylesheet 元数据都在）→ 推翻上一条「只 reload 就行」的结论：**百分比 `left` 在这个定位上不可靠**（dev 与 IPA 都落到静态位 = label 左缘）。
- **改动**（`apps/luka/src/components/notifications/notifications-tabs.tsx`）：
  - 两个 label 各挂 `onLayout` 记宽度（`activityWidth` / `visitorsWidth`）；
  - 徽标容器 `absolute -top-1 left-full ml-1` → `absolute -top-1` + inline `style={{ left: width + 4 }}`；
  - 宽度未量到前不渲染徽标（避免先闪在错误位置）；label 仍绝对定位挂徽标、不位移。
  - `apps/luka/docs/notifications.md`《分段徽标与默认段》同步：写明不要退回 `left-full`。
- **验证**：`pnpm --filter luka typecheck` 0；biome 干净；`tests/lib/notification-badges.test.ts` 4 passed。**未实机走查**（需要用户装新包：dev client 可 fast refresh 立刻看，IPA 需重打）。
- **未提交**（工作区另有并行会话在飞的 `edit-profile/location.tsx`、`credits/balance-card.tsx`；本次只改 2 个文件）。
- **沉淀**：`30-Resources/troubleshooting/uniwind-dev-stale-css-new-class.md` 的 2026-09-21 段已改写为该修正结论 + IPA 侧字符串核对手法。

### 21:02 · sitin-rn：luka Get sparks 金币页——仓鼠把余额文案压住了

- **报障**：用户贴实机截图（05:47，模拟器 iPhone X / 375pt）——「322 sparks」下面那句 `Top up below to keep the sp…` 右半截被 `hamster-happy` 整块盖住。
- **诊断**：仓鼠是 `components/credits/pack-grid.tsx` 里的绝对定位装饰（156×126，`top: -118`，底边压档位格上沿 8pt）。2026-09-14 曾特意把它从 128×104 放大上提过（头顶接近标题一带），所以它头上那 118pt 必然落在余额卡的 tagline / `OR PICK A PACK` 那一条带里。`CURRENT BALANCE`、`322 sparks` 都短，伸不到它左缘（375pt 上 ≈199pt），唯一全宽的就是 tagline，所以让文案让位、仓鼠不动。
- **改动**：`apps/luka/src/components/credits/balance-card.tsx` 的 tagline 加 `paddingRight: HAMSTER_LANE = 168`（= 仓鼠 156 宽 + 12 间隙），提前折成两行（`Top up below to keep` / `the sparks flowing.`），文字右缘 187pt < 仓鼠左缘 199pt。用右内边距不用 `max-w`：它随内容宽自适应，小屏（375）和大屏（430）都留出同一条仓鼠通道。**写成 inline style 而不是 `pr-[168px]`**：新加的任意值类在 dev fast refresh 下会被 Uniwind 旧 CSS 表静默丢掉（同一晚通知徽标踩过这个坑，见 `30-Resources/troubleshooting/uniwind-dev-stale-css-new-class.md`），inline 值不受表新旧影响，用户 fast refresh 就能看见；生产整包构建两者等价。
- **没走的备选**：① 仓鼠整体下移 —— 会盖住 500/1000 档位数字（小卡只有 ~82pt 高）；② 在档位格上方留 118pt 空白带 —— 白占一屏、档位被推下去；③ 缩短文案 —— 「Top up to keep sparks flowing.」量下来仍伸到 ~220pt，不够。
- **验证**：`npx biome check` 该文件干净；`pnpm luka:typecheck` 0；`pnpm luka:test` 1007 passed / 7 failed —— 失败集（candidate-card-fields / shape-language ×3 / use-swipe-actions ×3 / good-review / me）与本条无交集，与并行会话记录的既有红一致。**未实机复核**（credits 在登录守卫后，`luka://credits` 深链进不去；模拟器停在 onboarding 第一步），视觉留给用户走查。规范模式未 commit。
- **备注**：`recharge.tsx` 里 `hamster-ask`（146×112，`top: -104`）压在倒计时条上沿，上方那段 `Top up now to keep talking with {peer}.` 同样是全宽文本、peer 名长了也会被压 —— 用户没提，未顺手改。

- **20:59 提交 / 推送（用户要求 push）**：`84a5b1107` `fix(luka): 通知分段徽标改用量宽定位，不再依赖 left-full`（2 files，+26/−9）→ `origin/feature/luka-ios`（`35608d542..84a5b1107`）。`git commit --only` 只带本次 2 个文件（并行会话的 `location.tsx` / `balance-card.tsx` 未混入）。pre-commit biome、pre-push boundaries（41 包）+ luka typecheck（5.4s）全过。**需重打 IPA 才能验证**（dev client 若在可 fast refresh）。
- **提交/推送**：用户要求 push → `94984567b`（1 file，+8/−2）已推 `84a5b1107..94984567b` 到 `feature/luka-ios`；pre-commit Biome、pre-push boundaries（41 包面）+ apps/luka typecheck 全过。只暂存 location.tsx，工作区里并行会话的 `components/credits/balance-card.tsx` 未动。

### 21:10 · sitin-rn：T0048（likes&visitors 与通知按钮数量不一致）视频取证与初判

- **来源**：飞书 Buglist《IOS Luka Buglist 汇总》base `MbjobIs4Qax91fsXfUWc9Z4WnKd` / table `tblywHQVyR7KIGC9`，T0048（P1，指派人张峥）。历史：14:03 建单 → 14:14 附视频 → 17:31 张峥标「已解决待验证」→ **20:41 赵延杰打回「未开始」+ 20:58 新视频**。描述：「【likes&visitors】实际收到的likes&visitors，外部通知按钮展示的数量不一致」。
- **取证**：下载 record 附件（15.5s `飞书20260921-205827.qt`）+ 用户给的 9s 剪辑（`~/Downloads/飞书20260921-210413.qt`，纯静音），ffmpeg 抽帧逐帧核对。视频里（05:57–05:59）：
  - Chats 小窝：头像徽标 1（Team）、爪印 2→3（录制中到了一条）、Sofia 2 / Sky 3 / Alex 3、See all 9、底栏 Chats 12；
  - Notifications（Activity 段）：Visitors 分段徽标 **1**、Activity 分段徽标**无**；Activity 列表 **1 条未读 like（带红点）**；
  - 切到 Visitors：徽标消失，列表 **2 张访客卡都带未读红点**。
  - 另一段 15.5s 视频：主屏 Luka 图标角标 10、底栏 Chats 7（两处不是同一时刻）。
- **初判（未定论）**：真正对不上的是「**分段 tab 徽标** vs **列表里的未读点**」（Visitors 1 vs 2，Activity 0 vs 1；爪印 3 / 底栏 12 是按服务端计数 sum 出来的，与列表未读点总和 3 恰好一致）。根因是两套来源：徽标 = 服务端计数（`GetMatchLikedList.totalUnreadLikedUserCount` / `GetNewVisitorCount(startTime=visitorTime)`，10s/30s 轮询）；列表点 = 本地（likes 是进段 latch 的 `isUnread`，visitors 是 `lastVisitTime > visitorTime` 实时算）→ 服务端口径/时序与本地不一致就会差。另：若「外部通知按钮」指**桌面 App 图标角标**，则是另一条链（`totalBadge = messages + Focus + Frames`，且 push 的 badge 会覆盖）。
- **待用户确认**：赵延杰指的「外部通知按钮」是桌面图标角标 / Chats 页头爪印 / 分段 tab 徽标？期望哪个数等于哪个数？确认后再定改法（A：徽标改成与列表同一来源；B：改 OS 角标聚合/推送口径）。
- **附带**：视频里分段徽标还压在 "V" 上 —— 是今天 21:00 `84a5b1107` 修掉的那个定位问题，说明该视频/包早于那次修复。

- **21:30 澄清 + 细化**：用户确认「外部通知按钮」= **Chats 页头爪印**（`ScreenHeader` 的 notificationCount = `likeUnread + visitorUnread`，与分段徽标同一 store；同一时刻两者必然一致）。重新逐帧放大读数：爪印静帧全程 = **3**（之前 4fps 缩略图看错成 2→3）；小窝头像（Team）1、See all 9、底栏 12；通知页 Activity **无徽标**、列表 1 条未读 like，Visitors 徽标 **1**、列表 2 张未读访客卡。→ 爪印 3 与分段徽标 0+1=1 在同一时刻对不上，说明**中间发生了一次 store 刷新把数改了**（`useIntoYouCounts()` 进屏立刻 refresh；服务端返回 0/1 覆盖了此前 3）→ 最可能是**爪印陈旧**（app 级 30s 轮询，回前台/收推送不会立刻刷；`useInterval` 重新 enable 不立即触发，`use-into-you-badge` 也只在登录状态变化时刷一次）。次要可能：分段徽标/列表红点与「服务端计数」口径不同（visitor 徽标= `GetNewVisitorCount` 服务端计数 vs 卡片红点= `lastVisitTime > visitorTime` 本地算；like 红点= 进段 latch）。
- **下一步**：给用户两条候选修法（A 新鲜度：回前台/进 Chats 立即 refresh；B 口径统一：visitor 徽标改从 `GetVisitorList` 本地数、like 红点去 latch），等他用延杰的实际数字确认是哪一条再动手。
