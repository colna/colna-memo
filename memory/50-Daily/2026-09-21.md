---
title: 2026-09-21
date: 2026-09-21
tags: daily
---

# 2026-09-21

## 今日重点

- [ ] luka：注册 all set 页未传照片时兜底动漫头像；me 页头像出现慢（本次任务）
- [ ] 待用户定夺：仓库根未跟踪的 `-` 文件（devicectl UDID JSON 误产物）删不删
- [ ] 待定：`edit-profile/age` 的 `FALLBACK_AGE` 要不要与注册默认 30 对齐（T0022 只改了注册）
- [ ] 待定：Notifications Visitors 首跨页掉帧 → onPageScroll 升级 reanimated `useEvent`（需实机验证）

## 工作日志

### 10:57 · sitin-rn：luka 提审检查清单 review + 出报告文档

- 做了什么：读飞书 Base《提审检查清单》（92 条，全「未开始」），对照 `feature/luka-ios` 静态 review；拉取最新代码（`ec8955caf → c73a4ed94`，PR #525/#527）后复审，整理成 `apps/luka/docs/app-store-checklist-review.md`。
- 发现（代码实锤）：审核账号钉死 deviceId（`lib/review-mode.ts` 的 `STORE_REVIEW_DEVICE_IDS`）；DTC 站外卡支付 `dtcEnabled: isProduction` 且闸门阈值在后端（`third_party_payment_show`，`0` = 总是显示）；ATT 在登录后才弹（清单要求登录页）；`expo-audio` plugin 无条件写 `UIBackgroundModes: audio`；`supportsTablet: false` 与清单 10 条 iPad 用例冲突；LEGAL/Sentry DSN/appleTeamId"TODO" 等一批非空占位值（pre-submit-check 抓不到）；SIWA 授权失败静默回落设备登录（本次拉取新增）。
- 拉取后已修复：UTC+8 登录地区封锁整体移除；dev 包统一 GraceChat Ad Hoc 标识（生产 `com.lukasoc.luka` 不受影响）。
- 坑：① `pnpm pre-submit-check` 只查空值，非空占位（假 DSN、"TODO" Team id、carrd 占位 URL）全漏——提审清单必须人工盯；② expo plugin 之间会互相覆盖 Info.plist 键（infoPlist → expo-camera → expo-image-picker 三处相机文案，最后生效的是 image-picker 版）；③ gift 图片 URL 纯后端下发，仓库零硬编码，GIFT 系列只能在运营侧验。
- 产出：`apps/luka/docs/app-store-checklist-review.md`（未 commit，用户未要求）。

### 11:13 · sitin-rn：luka Plus Pass paywall 小字被仓鼠遮挡修复

- 做了什么：`apps/luka/src/app/paywall/index.tsx` 的自动续订说明（`Auto-renews until canceled…`，3.1.2(c) 审核修复文案）原在主 CTA 上方，被坐在按钮上沿的仓鼠（`sittingPet`，`hamster-sit.webp`，探出按钮约 76px、水平居中）整个盖住；挪到主 CTA 下方（三分支 `DtcPayArea` 之后，与非三分支按钮之后统一）。
- 为什么：仓鼠是稿子定的「坐在按钮上沿」姿态不能动，移动文案是唯一不破坏设计的解法。
- 结果：单文件改动，biome + `pnpm --filter luka exec tsc --noEmit` 均过；未 commit（工作区 `feature/luka-ios` 上还有他人未提交改动）。`likes.tsx` / `visitor.tsx` 的仓鼠不挨按钮、无此问题，未动。
- 备注：图片我这边模型读不了，凭「订阅按钮上方仓鼠遮挡小字」的文字描述定位——luka 三屏中只有 Plus Pass 的主 CTA（Light up Plus）带坐沿仓鼠，定位唯一。
### 11:17 · sitin-rn：luka Chats Team 入口挪到页头右上角（替换墙上相框）

- 做了什么：Chats 房间墙上的 Team 相框（`NestWallFrame`，`top: topInset+94`）与 8 张访客卡带重叠被挡，按用户要求把 Team 入口挪到页头右上角 notifications 爪印旁，只留官方头像 + 未读徽标（复用爪印徽标样式与 `-right-1 -top-1` 落位，99+ 封顶）。
- 改动：`components/xuanyu/screen-header.tsx` 加可选 `team`/`onTeamPress` props（不传零渲染，`ml-auto` 条件让给 team 入口；引导测量仍挂爪印 wrapper）；`nest-playfield.tsx` 删 `NestWallFrame` 渲染与 `NestTeam`/`team`/`onTeamPress`；删除 `nest-wall-frame.tsx`；`(tabs)/index.tsx` 的 team 数据改喂 ScreenHeader（`openTeam` 仍走 `markOfficialRead` + `/team`，All chats 抽屉入口不动）；同步 `docs/chats-play.md`、`docs/page-guide.md`、shape-language 测试注释。
- 结果：`pnpm --filter luka typecheck`、Biome、`pnpm boundaries` 过；chats-play / chat-list / notification-badges 测试全过；shape-language 3 条失败为预存（paywall / chat 气泡 / discover-play 未迁移文件），与本次无交集。未 commit（规范模式）。
- 坑：图片传不进来（当前模型不支持图片输入），凭文字 + 9-20 日志「相框只是透明点击区」定位。

## 工作日志 — 11:20

- **做什么**:sitin-rn(feature/luka-ios)修 luka 注册「未上传头像」的两个体验问题:① all set 页只剩首字母兜底;② Me 页动漫头像出现慢。
- **根因(模拟器 session.log 实证)**:FastLogin(进注册流程时)后端 `avatarUrl` 为空串——CAI 动漫头像要等 UploadUserInfo(点 Continue 落库)才按性别分配(如 `male_green_13.jpg`),所以 all set 页当时拿不到 URL;Me 页慢是 `me.tsx` 的 `initialLoading` 骨架把整屏含头像挡到 `GetMeOverview` RPC 返回 + 远程图首下。
- **改动(4 文件 + 2 素材 + 1 测试,未提交)**:
  - 素材:`src/assets/onboarding/cai-avatar-{male,female}.webp`(从 `file.archat.us/cai/avatar_image/` 下载 384² jpg,cwebp q85;male_green_13 / female_red_14),`FALLBACK_AVATAR` 按 `store.gender` 选,nonbinary 用 female 版。
  - `onboarding/summary.tsx`:photoUri 空时传 `fallbackSource`;落库 `setUserInfo(snapshot)` 后 fire-and-forget `prefetchImages([snapshot.avatarUrl])`(intro-video 播放的几秒预热缓存)。
  - `onboarding/profile-summary-card.tsx`:加 `fallbackSource?: ImageRequireSource`,`Avatar source={photoUri ? undefined : fallbackSource}`。
  - `(tabs)/me.tsx`:渲染分支 `me ? content : initialLoading ? skeleton : fallback`——snapshot 已有 displayName+avatarUrl 就直接渲染头部,RPC 失败但有 snapshot 不再弹错误页(列表走各自空态+下拉刷新)。
  - README(assets)目录表补 `onboarding/` 行;新测试 `tests/design/onboarding-fallback-avatar.test.ts` 5 条。
- **验证**:biome 4 files 0、`tsc --noEmit` 0、Metro bundle curl 200、全量 908 passed / 4 failed(good-review / candidate-card-fields / shape-language ×3,与 09-20 记录的既有红一致)。模拟器深链 `luka://onboarding/summary` 被登录守卫挡(App 停在未登录落地页),simctl 无法点屏走 8 步注册,**视觉未实机复核**——留用户/QA 真机走查。
- **已知边界**:本地兜底与后端分配头像同图集但非同一张(同性别多色),落库后 Me 页会换一次图;用户已确认接受。

- 提交 / 推送：`33b742ba1`（7 files, +65/-177），已 push `c73a4ed94..33b742ba1` 到 `feature/luka-ios`；pre-commit Biome、pre-push boundaries + luka typecheck 全过。只暂存了本次 7 个文件，工作区里并行会话的改动（me.tsx / onboarding / paywall / bootstrap 等）未动。
