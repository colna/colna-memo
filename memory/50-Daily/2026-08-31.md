
## 工作日志 — HH:MM T10331 活跃池 Onboarding 恒显「未完成」修复 + 上线 test-admin

**根因**:`business-minerva-pwa-pool` 后端从不算 `onboardingDone`,字段恒 undefined → 前端 `onboardingDone ? '已完成':'未完成'` 恒显「未完成」(所有活跃池用户,非 2100047176 个例)。
**权威口径**(dora `PwaTierBasicMetricQueries.ONBOARDING`,注释「Onboarding uses user_common_task FINISHED」):`user_common_task` 有 `task_kind='onboarding' 且 status='FINISHED'` 行即完成;表在 archat MAIN,Minerva 直读。
**改法**(3 文件):`pwa-pool.sql.ts` 加 `ONBOARDING_FINISHED_BY_IDS`;`model.onboardingFinishedByIds`;`service` MetricMaps 加 `onboarding:Set<number>`,活跃池 `loadCandidateMaps` 装配,`candidateFields` 输出 `onboardingDone`。新人池不取(基础条件=注册≤3天)。前端 types/列已有,无需改。lint 过。
**上线**:2 commit(fix 49c0c2782 + docs 5656e6564)→ push origin @5656e65 → 合 release/test-admin @1954447e(--no-verify 绕 payhub)。Base 状态**未改**(张峥只说 commit/push/合,没让改状态)。

**复用经验**:minerva 运营池「列表有列、后端没接数据」→ 字段 undefined、前端恒显默认值。排查此类"字段恒为某值"先确认后端 service 是否真的 set 了该字段;onboarding 权威口径认 `user_common_task`(task_kind='onboarding',status='FINISHED'),不是 user_onboarding_task_v2。

## 工作日志 — HH:MM T10339 过期邀请挡手动导入小美「已有待处理邀请」修复 + 上线 test-admin

**根因**:女用户(2100065541)新人池邀请过 expires_at,但 dora 过期 job(约每日 01:07 ET)未 flip,DB 仍 status='PENDING'。手动导入小美 INSERT 新 XIAOMEI PENDING 邀请撞唯一索引 `uq_pwa_tier_invitation_pending`(每人≤1 条 PENDING)→ inviteErrMsg 返回「已有待处理邀请」失败。T10303 只改列表隐藏(读侧),写侧仍被过期 PENDING 卡。
**修法**:`pwa-pool.sql.ts` 加 `EXPIRE_STALE_PENDING_INVITATIONS`(UPDATE ... status='EXPIRED' WHERE user_id=? AND status='PENDING' AND expires_at<=NOW());`moveToXiaomei`/`importToXiaomei`/`inviteUsers` 三处 INSERT 前同事务先跑,自愈 flip 延迟(仅清已过期 PENDING,有效不动)。lint 过。
**上线**:2 commit(fix 729a01c4c + docs f31c28cff)→ push origin @f31c28c → 合 release/test-admin @640f9659(--no-verify 绕 payhub)。Base 状态未改(张峥只说 commit/push/合)。

**复用经验(收敛)**:pwa_tier 邀请是「定时 job flip 状态」模型,过期有延迟窗口 → 读侧展示要 `expires_at>NOW()`(T10303),写侧 INSERT 前要先清过期 PENDING(T10339),两头都得处理,否则「UI 说没邀请、库里还占着」自相矛盾。
