---
title: 2026-08-14
date: 2026-08-14
tags: [daily]
---

# 2026-08-14

## 工作日志 — 20:28 GraceChat 客服语料 T11–T28 + sitin4.1 CE/授权 一串排查

**A. GraceChat 业务语料补全(交付)**
- 需求:据 `feature/pwa`(创作者/主播侧 PWA)全功能,续写飞书表《需求-Case》T10 之后的主题,给**客服回用户邮件**用。
- 做法:派 4 个 Explore agent 分片扫(收益/提现/任务、聊天/AI/社媒/CE/礼物、通话/直播/美颜、资料/认证/亲密度/邀请),汇总去重 → 站在**创作者视角**、白话话术版(用户会怎么问 / 回复话术+思路 / 客服要知道的要点 / 别对用户说)。
- 结论:删「美颜(前端无,在原生壳层)」「邀请好友 BFF(已下线)」;**T15/16/17 放开公开讲 AI**(三色带 AI 标识本就对用户可见;AI 代回/AI 打理社媒/系统代发交换都可明说),仅 **T21 引导来电=系统考核 保密**。
- 已写入飞书表 `K7bNs5WNphvYPFtiBsRcbqpMnQc` sheet `cMVIPQ` 的 **A15:F32**(T11–T28,6 列)。md 稿在 outputs 目录。

**B. sitin4.1 一串排查(未改码,仅结论)**
- 「点 authorize snapchat 任务未拉起登录抽屉」:`authorizeOrLogin` 分流——**已授权→openSocialProxyWebView(webview,不弹抽屉)**;未授权才 showInsModal。(用户让忽略)
- 「authorize snapchat 完成后未展示加钱弹窗」:加钱弹窗**唯一触发点**是 `InsModalContent.handleComplete → finishTask(taskId)`;`finishPWAInsTask` 回调本身不调 finishTask。所以只要不走弹窗完成(webview 重登/后台完成),就没加钱窗。图里 $0.20 应是别的任务。
- 「Win Free Cash 里 authorize snapchat 会有加钱弹窗吗」:走同一 authorizeOrLogin → **首次授权(未授权+任务未完成)会弹**,掉线重连不弹。
- 「chat 没弹 CE 抽屉」:`canShowInsModal()` 对 `/chat/` **返回 false(聊天页不弹窗,设计如此)**;有平台在线时 CE 订单走**自动派发**(日志证:自动接受 + FinishAndFollow + 发 INS_FOLLOW_REWARD)。设计里聊天页 CE 就是「内嵌卡 InsExchangeBubble + 底部 SocialAuthTaskDrawer」,不弹大抽屉。

**C. ⚠️ 待修 bug:SocialAuthTaskDrawer 不弹 = 前后端字段口径不一致**
- 后端 `GetConversationState` 返回的 task 是:`{id, reasons:["{\"kind\":\"social_account_authorization\",\"platform\":\"ig\"}"], createdAt, updatedAt}` —— **没有 taskType、没有 socialPlatform**,信息全在 `reasons[]` JSON 字符串里。
- 前端 `utils/socialAuthTask.ts` 的 `isSocialAuthTask` 却判 `task.taskType===4` + 读 `task.socialPlatform` → 两者皆空 → `getSocialAuthTask` 返回 undefined → 抽屉 `open=false` **不弹**。
- 修法:前端改成解析 `reasons[]`(找 `kind==="social_account_authorization"` 读其 `platform`)。**待与后端对齐最终契约(reasons vs taskType/socialPlatform)再改。** 用户在拿完整 JSON。

## 工作日志 — 10:51 sitin4.1 新增 Social Connect 积压订单任务卡(无 taskId)

- **需求**:社媒有积压 CE 交换订单 **且** 该平台未登录成功 → 首页 Task 栏出现「Social Connect on Instagram/Snapchat」卡,金额=该平台积压订单总金额;点卡直接调起社媒登录 webview;完成不加钱、任务消失即可。**不要 taskId、不进 taskRegistry、无 finishTask/奖励弹窗**。
- **数据源选型**:用户定「复用 `checkPendingOrders` 的 fetch,不新增请求」(方案 A)。纠正其一处逻辑误解——不能只在「小崽登录成功」那次写 store(那是卡该消失的时刻),必须**每次 fetch 都写**(启动/新 CE 请求/登录成功),否则离线积压时卡冒不出来。
- **改动 3 文件(commit `586fd50a4` push)**:
  - `stores/insExchangeStore.ts`:新增 `pendingByPlatform: Partial<Record<SocialPlatform,number>>` + `setPendingByPlatform`。
  - `hooks/useInsTaskInit.ts` `checkPendingOrders`:拉单后按 `order.cardType`(`cardTypeToPlatform`)汇总 `pwaFollowReward` 写 store,**空订单写 `{}` 兜底清零**(放在 `if(!orders.length) return` 之前)。
  - `pages/Home/OnboardingTaskList.tsx`:新增 `SocialConnectCard`(复用彩色卡样式,IG 粉/SC 黄,金额框+标题+箭头,点击 `openSocialProxyWebView(platform)`);列表按 `pendingByPlatform[p]>0 && loggedInByPlatform[p]!==true` 置顶渲染;放宽 `return null` 为「无 onboarding 任务 **且** 无 social 卡」才隐藏。
- **消失机制**:平台登录成功 → `insStore.loggedInByPlatform` 翻 true → 卡**立即消失**(显示条件带 `!loggedIn`,不依赖金额刷新);登录那次 fetch 再清零兜底。双保险。
- **只在 App 内**:`checkPendingOrders` 有 `isApp()` 门槛,web 端 `pendingByPlatform` 恒 `{}`,卡不显示(点击也需 App bridge)。
- **验证**:proto 先 `pnpm --filter @heyhru/business-pwa-proto build`,tsc 全包只剩 2 存量错(GiftBubble/ChatFooter,非我文件),我的 3 文件 0 错;eslint 全过。
- **已知小边界**:平台「先在线(卡隐藏)→ 积压清空 → 又离线」时 `pendingByPlatform` 不主动刷新,可能残留旧值短暂误现;下次新请求/重进 App 自动纠正,点击也只拉登录 webview 无副作用。用户暂未要求补「掉线时 refetch」。

## 工作日志 — 11:49 social-post 帖子加「转发数」字段(贯通 crawler/server/web,未提交)

- **需求**:帖子抓取表格在 点赞/评论 之外新增「转发数」列,金额同其它计数列口径(拿不到显示 -)。
- **数据来源**:IG `reshare_count`。两条路径都取:`web_profile_info` node 与 `feed/user` item;抽 `_extract_share_count`(优先 `reshare_count`、退 `share_count`,`_to_int` 兜底)。**注意 IG 多数场景不返回转发数** → 字段 None → 前端 `formatCount` 显示 `-`,属数据可得性问题非 bug。
- **改动 13 文件(3 层,照抄 likeCount 链)**:
  - crawler:`app/models.py`(PostItem +`share_count`)、`app/parsers/instagram.py`(+helper、`_parse_post`/`_parse_feed_item` 各填一处)、测试 `tests/test_parser_instagram.py`+fixture `ig_web_profile_info.json`(补 `reshare_count` 断言,含 None 用例)。
  - server:`prisma/schema.prisma`(Post +`shareCount Int?`)、新 migration `0002_add_post_share_count/migration.sql`(`ALTER TABLE "Post" ADD COLUMN "shareCount" INTEGER;`)、`crawler/crawler.types.ts`、`post/post.service.ts`、`ingest/ingest.service.ts`、`ingest/dto/ingest-facebook.dto.ts`;`post.service.spec.ts` 加断言(create 入参 p1 shareCount=7、p2=null)。`listByAccount` 返回整行,加 schema 字段即自动带到 web。
  - web:`services/types.ts`(Post +`shareCount`)、`pages/index.tsx`(评论列后加「转发」列)。**不改 `.umi/` `.umi-production/` 生成缓存**。
- **验证**:crawler pytest 32 passed(uv 建临时环境,本机无 venv);server tsc/eslint 干净、jest 13 passed;web tsc/eslint 干净、vitest 11 passed、umi build 成功。我新增行 0 lint 违规(crawler 文件既有 UP017`timezone.utc` 是存量,不属本次范围未动)。
- **坑**:本机 python3.14 `ensurepip` 建 venv 失败 → 改用 `uv venv --python 3.14` + `uv pip install -r requirements-dev.txt` 跑 pytest;`uvx ruff` 会拉最新版带新规则(误报存量 UP017),核对时用既有行归属判断。
- **提交/推送**:commit `aeb1462` 已 push 到 `colna/social-post` main(`6c12aeb..aeb1462`)。
- **上线关键**:server Vercel `buildCommand="prisma generate && nest build"` **不含 migrate deploy** → 新 server 上线前必须先在 Neon 跑 `prisma migrate deploy`(0002 加 `shareCount` 列),否则帖子列表接口 500(Prisma 查全字段)。加的是 nullable 列,向后兼容;顺序:①Neon 迁移 → ②redeploy server → ③redeploy web/crawler(顺序不敏感)。
- **Neon 迁移已代跑**(我这边用仓库锁定 prisma 6.19.3 + 直连非 pooler URL:`prisma migrate status` 确认 0001 已应用/0002 pending → `migrate deploy` → `Database schema is up to date`)。用户本机 `npx prisma` 误拉 prisma@7 + npm 缓存 EEXIST/EACCES 才失败;正解是 `pnpm exec prisma` 用锁定版、且迁移用直连(去掉 host 的 `-pooler`)。
- **残留**:①redeploy server/web/crawler(已连仓库则 push 自动);②IG 转发数真实可得性需真机抓取验证(cookie)。

## 工作日志 — 01:58 social-post FB「转发数 + 精确时间抓取」(创造模式 Phase E)

**背景/复盘**:用户要「继续 social-post facebook」,列 5 项需求。先查知识库(facebook-scraping 排错笔记 + docs/06)发现 FB 端 08-10 已基本做完,4/5 需求早已实现。唯一真缺口:**「转发/分享数」全链路字段齐(schema/types/UI「转发」列都在),但 FB 两条解析路径都没填 → shareCount 恒 None**。次要:抓取时间选择是「按天」粒度。

**用户拍板**:①抓取时间精确到时分 ②走浏览器脚本路径(server 直抓会被 FB 软封)补转发数。

**做了什么**(social-post,commit 798a0ad + f76f7d8,已 push main):
- `services/crawler/app/parsers/facebook.py`:新增 `_post_share_count`,FB Comet feedback 的 `share_count.count`(退回 `reshare_count`)按 reaction_count 同款递归 best-effort;`_parse_node` 填 `share_count=`。fixture `fb_timeline.txt` post 111 加 share_count、222 验 None,test 补断言。
- `scripts/fb-collector.user.js`:新增 `pickShareCount`,parseNode payload 带 shareCount(server ingest 早已接收 `item.shareCount ?? null`)。
- `apps/web/src/pages/index.tsx`:抓取弹窗 `ProFormDateRangePicker` → `ProFormDateTimeRangePicker`,since/until 用确切时间点(去 startOf/endOf day 取整)。

**结果**:crawler pytest 32 passed、web vitest 11 passed、tsc/lint 全过。

**关键坑/沉淀**:
- 本机 homebrew py3.14 坏,crawler 测试必须 `uv venv --python 3.12`(已印证)。
- FB 分享数字段:feedback 里 `share_count:{count:N}` / `i18n_share_count:"N"`,退回 `reshare_count`;脚本 `intCounts` 已支持 {count}/number/string 三态。

**残留(E.5,待用户)**:装/更新 Tampermonkey 脚本 → 打开 facebook.com/uksmartgroup 点采集 → web 表格确认「转发」列有值、发布时间到时分、账号筛选可用。

## 工作日志 — 02:05 social-post 自建 Chrome MV3 扩展(替代 Tampermonkey,Phase F)

**背景**:用户不想依赖 Tampermonkey,问「能不能自己写浏览器插件」。定:建自己的 MV3 扩展,采集逻辑复用。

**做了什么**(social-post `extension/`,commit 0f654d5,已 push main):
- `manifest.json`(MV3):content script 注入 `www.facebook.com/*`;`permissions:["storage"]`;`host_permissions:["http://localhost/*","http://127.0.0.1/*"]`。
- `content.js`:搬 `fb-collector.user.js` 全部采集/解析逻辑,GM_* → chrome.storage.local(配置)+ chrome.runtime.sendMessage(POST 转发)。
- `background.js`:service worker 收 `{type:'ingest'}` 做跨域 fetch。
- `popup.html/js`:配置 server API 基址 + ingest token,存 chrome.storage.local。
- docs/06 补 Phase F + 安装步骤。旧 userscript 保留作备选。

**结果**:node --check 三个 JS + manifest JSON 全过。server 零改动(DTO 已收 shareCount/externalId;whitelist 只剥离不报错)。

**关键机制/沉淀**:
- **content script 与页面同源**,同源 `fetch('/api/graphql/',{credentials:'include'})` 浏览器自动带 httpOnly cookie(httpOnly 只挡 JS 读 document.cookie,不挡网络附带)→ GraphQL 采集可在 content script 跑,不需注入页面。
- **唯一跨域** = 打本地 server:content script 直打会被 CORS 拦 → 交 background service worker,有 host_permissions 的扩展请求**绕过 CORS**,所以 server 无需开 CORS。
- 部署到远端 server 时需在 manifest `host_permissions` 加对应 host。
- 装法:chrome://extensions 开开发者模式 → 加载已解压 → 选 extension/ 目录。

**残留(E.5/F.4)**:装扩展 + server 运行,打开 facebook.com/uksmartgroup 点采集,验证「转发」列有值。

## 工作日志 — 02:15 social-post 扩展支持批量主页 + 自动跳转采集(Phase G)

**需求**:扩展 popup 填多个主页链接 → 依次进入每个主页采集 → 每个采完上报 → 自动跳下一个 → 表格可见。

**做了什么**(social-post `extension/`,commit 6a9e662,已 push main):
- `content.js`:① `collect` 重构为 `collectAndReport(setStatus)` 返回 {ok,handle,added,total,error,note}(不 alert),手动/批量共用;② 加 `waitContext` 就绪重试(SPA 首屏可能还没塞 timeline 上下文,重试抠 fb_dtsg/doc_id/userID);③ 批量状态机 `batchTick`:读 `chrome.storage.local.batch`,匹配当前 handle 则采集→上报→index++→节流 3s→`location.assign` 下一个,末尾 `finishBatch`;④ 右下角面板显示进度。
- `popup.js/html`:文本域多行链接/handle → `parseTargets`(支持完整 URL/裸 handle,去重,不支持 profile.php 数字 id)→ 存 batch + `chrome.tabs.update` 导航第一个;每 1.5s 刷新 results 摘要;停止批量按钮。
- `manifest.json`:加 `tabs` 权限。

**状态机**:`batch={running,items:[{handle,url}],index,results:[{handle,ok,added,total,error}]}`,存 storage 跨页面导航存活。

**关键坑/沉淀**:
- **跨导航状态必须存 chrome.storage**,不能存内存(每次导航 content script 重新加载)。
- **重入保护**:`_batchRan` 每页只跑一次;popup 兜底发 `batch-start` 消息仅在 `!_batchRan` 时补触发(应对同 URL 未 reload),避免与页面加载的 batchTick 并发双跑。
- SPA 首屏未就绪 → `waitContext` 重试(12 次 ×1.5s)再采,否则抠不到 token。

**结果**:node --check 三 JS + manifest JSON 全过。
**残留(G.4)**:待用户填多个真实主页链接跑一遍,验证依次采集 + 表格可见。

## 工作日志 — 02:35 social-post 扩展加外显日志 + 诊断只采到 1 帖

**排查(用户反馈 +0/没采到)**:直接 curl Vercel API 确认——server 正常(POST 201)、DB 连着、token 默认值 change-me-ingest-token 通(Vercel 未设 INGEST_TOKEN)。查 `/api/accounts?platform=facebook` + `/accounts/:id/posts`:uksmartgroup **已入库 1 条真实帖**(shortcode 1675192414613855),displayName/externalId 都更新对 → **全链路正常**。`+0` = 这次无新增(那 1 条上次已入)。**根因**:只有页面内嵌的 1 条;脚本自发 GraphQL 翻页被 FB 软封(1357054),翻不动。给全量 HTML 也没用(HTML 里就内嵌 1 条)。正解方向=自动滚动 + hook FB 自己的 fetch 截 timeline 响应(待用户拍板)。

**做了什么**(social-post `extension/`,commit 9b1e2b2,已 push,版本→1.1.0):
- `content.js`:加外显日志面板(右下角,深色 pre 区)+ 复制/清空按钮;`logLine` 存 `chrome.storage.local.sp_log`(防抖 400ms),批量跳转后 mount 回填不丢;埋点采集全过程:waitContext 就绪重试、内嵌帖数、每页「解析N/新增/累计/hasNext/cursor」、限流 1357054 响应片段、上报 added/total。批量 hop 也记。

**关键坑/沉淀**:
- **诊断先打 API 别猜**:curl accounts/posts 端点一下就区分「客户端没采」vs「server 没入」vs「重复」。
- `[platformKey,shortcode]` 是**全局唯一**(非 per-account),跨账户同帖会被判重复 added=0。
- FB 主页 www 页只内嵌 ~1 帖,多帖必须翻页/滚动,自发 graphql 必被软封——下一步走「劫持 FB 自身请求」。
- Vercel 远端 server 要在 manifest `host_permissions` 放行,且扩展详情页里那行 host 开关要**手动打开**(新增 host 不自动授权)→ 否则 background fetch `0: Failed to fetch`。

## 工作日志 — 03:25 social-post 扩展改「滚动采集」拿全量帖(劫持 FB 自身请求)

**背景**:uksmartgroup 只入库 1 帖——www 页只内嵌 1 条,自发 GraphQL 翻页 1~2 次即被 FB 软封 1357054。用户提「注入脚本操作 DOM 获取」。定更稳方案:不自己发请求,劫持 FB 自己发的 timeline 响应。

**做了什么**(social-post `extension/`,commit 5da3046,已 push,版本→1.2.0):
- 新增 `inject.js`(MAIN world,document_start):content script 隔离世界拿不到页面 window.fetch → 在 MAIN 世界 patch;命中 /api/graphql/ 且请求体含 Timeline 的响应 `response.clone().text()` 读出 `postMessage({source:'sp-fb-cap'})` 交 content。
- `content.js`:message 监听 → parseTimeline → Story 按 shortcode 累积 _cap Map;`collectByScroll` seed 内嵌帖 → 自动滚到底触发 FB 懒加载 → 每 2.5s 查增长,连续 4 轮无新增判定到底/或 120 轮上限 → 上报。主按钮改「滚动采集本页」+ 运行中「停止采集」(_scrollAbort);批量也切 collectByScroll。删旧自发 graphql 路径(collectAndReport/gqlPage/cookie/jazoest/waitContext),放宽就绪(不再强求 fb_dtsg/doc_id,displayName 尽力取或 document.title)。
- manifest 加 MAIN world 注入;pack.sh 白名单加 inject.js。

**关键坑/沉淀**:
- **MV3 隔离世界拿不到页面 window.fetch** → 要 hook 页面自身请求,必须 `content_scripts` 里加 `"world":"MAIN"` 的注入脚本(Chrome 111+),document_start 抢在 FB 用 fetch 前 patch,再 postMessage 回隔离世界。
- **反软封本质**:让 FB 用它自己会话/指纹发 timeline 请求(滚动即触发),我们只旁路读响应;自己构造 graphql 才会被 1357054。
- 滚动采集不需要 fb_dtsg/doc_id(那是自发请求才要的)。
- 复用现成 parseTimeline 解析捕获的响应 → 拿结构化 JSON(精确点赞/评论/转发/时间),比抠混淆 DOM 稳得多。

**结果**:node --check 四 JS + manifest 全过。
**残留(H.4)**:待用户重载扩展(1.2.0)在 uksmartgroup 点「滚动采集本页」,看日志滚动增长 + 表格帖数。

## 工作日志 — 03:45 social-post 扩展加日期限制(默认最近7天) + DOM 分析

**DOM 分析(用户存了 doms/main.html 5MB)**:结构正常(有 Story 节点 + timeline_list_feed_units),但**实际只有 1 个唯一 post_id**(就是库里那条)——FB 主页首屏只渲染 ~1 条,更多帖必须滚动实时加载,静态 DOM 看不出滚动行为。结论:要么跑 1.2.1 诊断版看日志,要么滚完再存 DOM。用户转而要日期限制功能。

**做了什么**(social-post `extension/`,commit 6af42d7,版本→1.3.0,已 push):
- popup 加「采集最近 N 天」输入(默认 7,0/空=不限),存 sp_days。
- content `collectByScroll`:since=now-N天;消息监听捕获 + 内嵌 seed 都过滤,只留 takenAt>=since(parseNode 的 takenAt 是 unix 秒;无时间不误杀)。
- 时间线倒序 → 遇到 ≥3 条早于窗口的帖即判"翻过窗口"停滚(容忍置顶旧帖,省时)。结尾判定 <=1 改为 ===0 才失败(窗口内可能就 1 条)。

**关键沉淀**:
- FB 主页首屏只内嵌 ~1 帖;`walkStories` 认 `__typename==="Story"`,DOM 里确实有(3 处但同一帖)。之前统计 0 是因为这份 DOM 的 JSON 带空格缩进(`"__typename": "Story"`),正则漏了空格。
- 日期窗口用"遇到 N 条更早帖即停"利用时间线倒序,避免无谓滚到底;置顶旧帖用阈值 3 容忍。
- 上轮 1.2.1 诊断版(hook fetch+XHR、inject loaded/gql/timeline 计数)用户还没跑,根因(hook 是否命中滚动加载)仍待其日志确认。

## 工作日志 — 04:00 social-post 滚动采集跑通(558帖)+ 修 413

**重大进展**:用户跑 1.2.1,滚动采集**捕获 558 帖成功**(见 graphql 197、timeline 响应 185)——「劫持 FB 自身 timeline 响应」方案完全跑通,FB 全程不软封。inject hook 生效(loaded 信号因 document_start 发/document_idle 监听而错过,是误报;captures 一路正常)。

**卡点**:558 帖一次性 POST → `413 request entity too large`(NestJS 默认 body 仅 100kb)。

**做了什么**(social-post,commit 059cfd6,已 push;扩展版本→1.3.1):
- 扩展 `collectByScroll`:分批上报,`uploadChunk=100`/批,累加 added;任一批失败即中止并报"已入 N"。
- server `bootstrap.ts`+`main.ts`:`NestFactory.create(..,{bodyParser:false})` + `express.json({limit:'5mb'})`(两处入口都改;serverless 用 server.use,本地用 app.use)。ingest 测试仍过。
- 修 inject 误报:收到 sp-fb-cap 即置 _injectLoaded=true。

**关键沉淀**:
- **滚动采集方案确认可行且高效**:~150 轮/8min 拿 558 帖,+3/响应稳定,FB 用自己会话加载不软封。
- **NestJS body 默认 100kb** → 批量入库必须放大(bodyParser:false + express.json limit)或客户端分批;Vercel serverless 另有 ~4.5MB 平台硬限,故客户端分批(100/批)最稳,双保险。
- MAIN world inject 的 `loaded` 一次性信号会被 document_idle 才挂的监听错过 → 别用它判定 hook 是否生效,用"是否收到 capture"判。

**待验(下一步)**:用户刷到 1.3.1 + server 重新部署后再跑,确认 558 帖分批全部入库、表格可见;日期限制(默认7天)生效后帖数应大减。server 改动需 Vercel 重新部署才在线上生效。

## 工作日志 — 04:20 social-post 采集账户资料(粉丝/关注/简介)+ 平台自动启用

**需求**:账户详情里 粉丝/关注/帖子数/简介 都是 "-",要一并抓。

**排查**:doms/main.html 里**没有** follower_count/biography 现成字段(follower 只有导航 URI)。FB 主页粉丝/关注是**渲染文本**("4.7K followers"),简介在 `og:description` meta。

**做了什么**(social-post,commit ab0706f + 795fe96,已 push;扩展→1.4.0):
- 扩展 `extractProfile()`:读 `document.body.innerText` 正则提粉丝/关注(支持 4.7K/1.2M/4.7万,中英文 followers/粉丝/关注者),`parseCount` 归一;简介取 `meta[property="og:description"]`。随 account 上报(每批都带,upsert 幂等)。
- ingest DTO 加 followerCount/followingCount/mediaCount/bio;service account upsert 一并写(?? undefined 不覆盖已有)。
- 另修:ingest platform upsert `update:{enabled:true}` → 采集即激活 facebook 平台(线上 08-06 seed 为 false,前端菜单置灰)。

**关键沉淀**:
- FB 主页粉丝/关注**没有结构化字段**,读渲染可见文本最稳(抗类名混淆),但依赖 UI 语言 → 中英文都写了正则。简介用 og:description 最稳。
- 帖子总数(mediaCount)FB 主页不好拿,暂留 null(server 爬虫路径能拿但已弃用)。
- 平台 enabled:线上 DB 未重 seed → 靠 ingest update 翻;需 Vercel 重新部署。

**待部署**:server 三处改动(413 body 5mb、平台 enabled、账户字段)全部需 Vercel 重新部署才在线上生效。

## 工作日志 — 04:53(sitin-next / IG 脚本:发送前检查空结果不再上报 CHAT_HISTORY)

**背景/排错**:多份 IG-Scraper console 日志显示「准备发链接却刷新/发送被打断」。逐层追:
- 脚本 `getChatMessages` 扫到 0 条会 `emitScriptEvent` 一条空 `CHAT_HISTORY`(`data:[]`,`SE-` 前缀 SCRIPT_EVENT)。此行为由 commit `48d121a30`(2026-06-11)引入——把「空则脚本自动回收件箱」改成「上报空 CHAT_HISTORY,由服务端决定」。
- 服务端(`app-social-proxy-server`)`ig-websocket.adapter` 对 `nonMe==0` 打 `signalError:empty_chat_history` → `CloseChatDef`(always close)→ `apk-script-map` 里 `CLOSE_CHAT = navigateToInbox` → 下发跳回收件箱。
- `sendMessage` 的「发送前检查新消息」也会调 `getChatMessages`,于是发送中途触发空上报 → 服务端下发 navigateToInbox → 打断发送(尤其链接消息因预览渲染慢,窗口更长)。

**改动**(分支 `feature/sp-presend-skip-empty-chat-history`,从 origin/feature/sp 切):
- `packages/app-ins-scripts/src/instagram/methods/getChatMessages.js`:空结果分支加 `ctx.options.fromSendMessage` 判断——发送前检查场景**不上报**空 CHAT_HISTORY(有无新消息由 sendMessage 本地 `getNewThemMessagesAfter()` 判断);非空仍 `uploadMessages`;非发送前检查仍上报空 CHAT_HISTORY(PULL_NEW_MESSAGE 关页流程不能动)。
- `sendMessage.js`:发送前检查那次调用加 `fromSendMessage: true`(post-send 采集那次**不加**)。
- 文档 `docs/script-result-design.md` 新增「九、getChatMessages 空结果上报与 fromSendMessage」。
- 测试 `test/getChatMessages-presend.unit.test.js`:vm 桩掉 `SP.pipeline` 直接驱动 `result(ctx)`,验证 3 种情况,`node --test` 全过(含既有用例 5/5)。

**关键点/坑**:
- 客户端 `uploadMessages`(真实消息)最终也走 **SCRIPT_EVENT** 帧,但 request_id 前缀是 `upload_<uuid>`;脚本空信号是 `SE-`。服务端**只能靠前缀/`data` 是否空区分**,frame type 都是 SCRIPT_EVENT。
- `src/instagram/**` 不参与 lint(包 eslint.config 显式 ignore),该包也无 lint script → 本次脚本改动不受 lint 门禁。
- feature/sp 用 `manIGId/womanIGId` 命名;sp-snapchat 系分支已重命名为 `manSocialId/womanSocialId`(纯改名,逻辑一致)。线上设备跑哪个分支可由日志字段名反推。

**状态**:未 commit / 未 push(等用户确认)。排错细节另见 troubleshooting。
