---
title: 2026-08-11
date: 2026-08-11
tags: daily
---

# 2026-08-11

## 今日重点

- [ ] （承接 08-10）social-post FB 采集脚本端到端实测（待用户浏览器验证)

## 工作日志

> 按 `HH:MM` 追加,最新在下。

### 13:59 — sitin-next2 · getStory「没发 story 不再报错」

**做了什么**：`sitin-next2` 分支 `personal/zz/sp-snapchat`,改 `packages/app-ins-scripts/src/snapchat/methods/getStory.js` Step 1「Click View My Story」(+28 −5)。未 commit/push。

**为什么**：用户要求「账号没发 story 时不应上报错误,他只是没有」。原逻辑:找不到 View My Story 入口 → 一律 `buildError(VIEW_MY_STORY_NOT_FOUND)`,且注释自承「分不清『真没 story』和『菜单没展开(真故障)』」。

**怎么改**：用头像 toggle 按钮的 `aria-expanded` 区分(DOM 快照 `header.html` 确认展开时为 `"true"`):
- 菜单已展开(`aria-expanded==="true"`)但无该入口 → 视为**没发 story**,返回**成功空结果** `buildResult(ACTION,{username:"",totalPages:0,pages:[],noStory:true})`,并 eventTrack `noStory:true`。
- 菜单未展开 → 仍 `buildError(VIEW_MY_STORY_NOT_FOUND)`(消息改为 "Profile menu did not open",selector 改 `button[aria-haspopup="listbox"]`)——真故障不吞。
- 调用方判据:`success:true && data.noStory===true`(或 `totalPages===0`)= 没 story。

**踩坑/校验**：
- 该包 `@heyhru/app-ins-scripts` **无 `lint` 脚本 → 不进 `pnpm lint` 门禁**;从根目录直接 `npx eslint` 会用 node 环境根配置误报一堆浏览器全局 `no-undef`。这些 32 个 error 全是文件既有问题(浏览器全局、410 `clickOnce` 重复、442 主函数 125 行超 80),**没有一条落在我新增行**。
- sitin-next2 CLAUDE.md 硬规则:**英文注释/日志**(该文件历史是中文,但按目录级规则我的新代码写英文)、doc-first(查过 `docs/*.md` 无 getStory 契约引用,无需改文档)、无交互提问、不自动 commit/push。
- sitin-next2 当前分支原为 `personal/zz/sitin4.1`;切到 `sp-snapchat` 时本地落后 origin 6 提交,已 `pull --ff-only` 到最新(head `96897f881`)再改。脏 submodule `business-pwa-proto/proto` 是工作区常态,未动。

**已提交**：commit `3da5b9705` `fix(app-ins-scripts): ...`,已 push `origin/personal/zz/sp-snapchat`(`96897f881..3da5b9705`)。pre-commit(`pnpm lint` 51/51 + `pnpm circular` 无违规)+ pre-push 门禁均过。只提了 getStory.js,脏 submodule 未带。

**残留**：
- [ ] 生效需 `scripts/build.mjs` 重打包 dist,且部署侧(容器 App 的 automation.js 拷贝)要更新
- [ ] i18n 风险不变:`LABELS.viewMyStory` 仅确证英文,别的语言若文案没匹配上,即便有 story 也会被判「没 story」

### 14:30 — SC followUserById 报 `Missing snapchatId` 诊断 + execute 加 params 日志

**诊断**:用户线上日志 `followUserById` 报 `INVALID_PARAMS: Missing snapchatId`。查当前源码(sp-snapchat 最新)——followUserById 校验的是 **`params.manSocialId`**、报 `Missing manSocialId`,全仓无 `snapchatId` 残留。字段改名在 `4a3ddc094`/PR #899 `social-field-rename`(snapchatId→manSocialId,man/woman 统一)。**结论:线上跑的是改名前旧 bundle → 版本错配**。真因是 followUserById 缺 id 参数(校验本身没错);修法=重打包+重部署让线上与源码一致,并确认调用方传 `manSocialId`。

**改动**:`executeAction.js` SP.execute 入口(line 291 后)加一条 `execute: params=<JSON.stringify(params)>` 日志(安全 try/catch),便于看调用方实际入参、定位字段错配。英文注释。未 commit。

**重打包**:`packages/app-ins-scripts` 跑 `node scripts/build.mjs` 成功;验证 `dist/snapchat/automation.js`(186kb)已含 `execute: params=` / `noStory` / `Missing manSocialId`,旧 `Missing snapchatId` = 0。dist 是 gitignored。

**残留**:
- [x] `executeAction.js` params 日志已 commit `d684de71b` + push(`3da5b9705..d684de71b`,pre-commit lint+circular 过)
- [ ] 部署:两处改动(getStory noStory + execute params)源码/dist 已就绪但**未上线**;用户本次只 push 不部署。上线走 `scripts/upload-all.mjs` 或同步容器 App 的 automation.js 拷贝(待确认通道)

### 15:10 — getProfile 跨源闭环(跳 welcome 取数再上报,不再返回 NAVIGATING)

**需求**:getProfile 一次调用闭环:跳 welcome 页 → 取数据 → 上报最终结果(原逻辑不在 welcome 页就 `buildError(NAVIGATING)` 让调用方手动重调)。

**难点**:资料卡只在 `accounts.snapchat.com/v2/welcome`,与 `www.snapchat.com` 跨源,跳转销毁上下文 + sessionStorage 不跨源 → 同源 recovery 用不了。

**机制**:`executeAction.js:318-321` 对 pending 结果(`success:true && data.pending===true`)不上报、挂起;方法只收 `params` 拿不到 requestId(`fn(params)`)。

**改法(2 文件)**:① `executeAction.js` 调方法前 `SP.__requestId=requestId` 暴露给方法;② `getProfile.js`:不在 welcome 页 → 跳 `WELCOME_URL#__sp_getprofile=<rid>`(URL hash 跨源带 requestId)+ 返回 `buildResult({pending:true,navigating:true})`;尾部自启动 IIFE `continuePendingGetProfile` 在 welcome 页读 hash → 清 hash → 等 `SP.execute` → `execute("getProfile",{},rid)` 重触发 → 取资料 `buildResult(profile)` 用原 rid 上报。header 文档同步为英文闭环说明。

**验证**:build 无错;包测试 2 passed;bundle(automation/bootstrap/login 都含)含新逻辑,旧 `NAVIGATING`=0。

**已提交**:commit `996913079` `feat(app-ins-scripts): make getProfile a cross-origin closed loop`,push `d684de71b..996913079`(pre-commit lint+circular、pre-push 均过)。

**残留**:
- [ ] 部署上线(dist 未上传;三处 SC 改动 getStory/params 日志/getProfile 闭环都未上线)
- [ ] 后端确认:pending 期间挂 requestId 不快速超时/不判失败;welcome 页会自动注入 bootstrap/login bundle

### 15:21 — getProfile 闭环双执行 bug 修复

**实测反馈**:闭环跑通(www→welcome→取到 colna_zheng/COLNA→success 上报),但 **getProfile 执行了两次**(eventTrack/profile/桥接结果各 2 次),跳回 web 也乱(没生效/弹回 welcome)。

**根因**:续传 IIFE 调 `execute("getProfile")` → executeAction `storePending` 存任务;getProfile 等 welcome DOM(慢),页面加载 2s 后 `executeAction` 的 **recovery(`:362-398`)把同一 pending 任务又 resume 一遍** → 双执行、双上报、双跳回打架。

**修法(executeAction.js + getProfile.js)**:
- executeAction 暴露 `SP.__clearPending = clearPending`。
- getProfile 续传 IIFE 改为**直接 `SP.getProfile({})` + `bridge.onResult(rid, result)` 自己上报**,不走 execute()/不 storePending → recovery 无任务可 resume,双执行消除。
- getProfile www 侧跳转后 `SP.__clearPending(rid)` 清遗留 pending,防下次访问 www 被 recovery 误触发跳 welcome。
- 验证:build 无错,包测试 2 passed,bundle 含 `__clearPending` + 续传 onResult。未 commit。

**残留/观察**:
- [ ] 跳回 www/web 若仍弹回 welcome,可能是 Snapchat 按账号状态自身重定向(非脚本),单执行后再观察
- [ ] 副作用:welcome 页 startDMListener 自动启动会 NO_CHAT_LIST(welcome 无聊天列表)——既有自动行为,噪音,非本次问题
- [x] 已 commit `709043255` + push(`996913079..709043255`,门禁过)
- [ ] 重部署 dist 再实测双执行是否消失、跳回是否正常

### 16:25 — clickMute 改为按 manSocialNickname 找会话(不再要 chatId)

**需求**:clickMute 静音时还没有 chatId,应像 sendGreet 那样按好友**显示名 manSocialNickname** 搜索开会话。sendGreet 早已是这套(搜好友→开会话→从 URL 抓 chatId),**无需改**;clickMute 原走 `chatId||snapId` → `navigateToChat`,本次改它。

**改动(仅 `clickMute.js`)**:
- 入参:`chatId/snapId` → 要 `manSocialNickname`(缺则 INVALID_PARAMS)。
- Step0「Navigate to chat」→「Open chat by manSocialNickname」:`input[role="searchbox"]` 输名字 → 精确匹配 `[role="listitem"]` 叶子文本 → 点 `[tabindex="0"]` 开会话(复用 sendGreet 逻辑,helper 本地复制:norm/typeIntoSearch/findFriendRowByName/waitForFriendRowByName/getChatIdFromUrl)。
- Step1「Wait for chat page」后 `ctx.chatId = getChatIdFromUrl()`(`/web/{chatId}`),仅上报用。
- 「Open settings panel」`findChatHeader(ctx.task.snapId)` → `findChatHeader(ctx.manSocialNickname)`(匹配不到 aria-label 走「输入框上方顶部短文本」兜底,已确认不依赖 identity 也能定位)。
- ctx `{task}`→`{manSocialNickname,chatId}`;result/eventTrack 加 manSocialNickname、chatId 改用 ctx.chatId。
- doc-first:头部参数文档同步(英文)。

**取舍**:搜索 helper 在 clickMute 内**本地复制**(不动正在工作的 sendGreet、无加载顺序耦合),已在两处加注释「改一处两处都要改」。后续可抽公共。

**验证**:build 无错,包测试 2 passed,bundle 含 `Open chat by manSocialNickname`/`Missing manSocialNickname`/`findFriendRowByName`。仅 clickMute.js 变更。未 commit。

**残留**:
- [x] 已 commit `cc58665ff` + push(`709043255..cc58665ff`,门禁过)
- [ ] 重部署实测(搜索匹配、开会话、静音、chatId 抓取)
- [ ] DOM 选择器 clickMute/sendGreet 两处重复,后续抽公共 helper

### 16:48 — SC-DM-Listener 加忽略名单(Team Snapchat 这类不上报)

**需求**:DM 监听器要过滤掉不需要上报的账户(如官方 `Team Snapchat`)。用户说「白名单」但描述是「过滤哪些账户不用上报」→ 实为**忽略/排除名单**,按此做。

**改动(仅 `actions/startDMListener.js`)**:
- 模块常量 `DEFAULT_IGNORE_NAMES = ["Team Snapchat"]` + `normName`(trim+lower)+ `isIgnored`(大小写不敏感、精确整名匹配)。
- `startDMListener(params)`:`params.ignoreNames`(数组)与默认合并存 `state.ignoreNames`(自动启动用默认,调用方可扩展;运行中再调也会更新名单)。
- `scanChatItems` 拿到 manSocialNickname 后 `if (isIgnored(...)) continue`——被忽略者不进快照/不判未读/不上报(唯一取数入口,一处搞定)。
- doc-first:头部文档 + 参数说明同步(英文)。

**取舍**:精确整名匹配(不做子串,避免误伤名字含子串的真人);多语言官方账号别名(如中文「Snapchat 团队」)需要时往 ignoreNames 加或后续补 i18n。

**验证**:build 无错,包测试 2 passed,bundle 含 DEFAULT_IGNORE_NAMES/isIgnored/ignoreNames。仅 startDMListener.js 变更。未 commit。

**残留**:
- [x] 已 commit `6b7e4a36f` + push(`cc58665ff..6b7e4a36f`,门禁过)
- [ ] 重部署实测(Team Snapchat 不再触发 NEW_MESSAGE)

## 工作日志 — 05:01

**做了什么**:用飞书文档「从 Aliyun CLI 到 SLS 根因定位」的方法(SLS `GetLogsV2`),定位并修复 admin one-click 下发 snapchat CE signal「设备收不到」问题;修复落 `personal/dzh/spv2_test` 并合入 `release/test-sp`。

**根因(SLS 证据链,creator=2100064074)**:
- `[DeviceRegistry] [Online] ... active on platform=snapchat` → 设备 snapchat 在线态正确、`activePlatforms` 已含 snapchat(排除在线检测/客户端/WS)。
- `[TodoService][Todo:new] FOLLOW_BACK ... handle=- status=PENDING` 之后**无任何派发日志** → todo 建了但从未派给设备。
- 对比分支代码:one-click CE 的 snapchat 修复只在 `snapchat-action-console`/`release/test-admin`,**`spv2_test`/`release/test-sp` 没有**。而 admin-api-dev 部署的是 test-sp → 旧代码把 CE 硬编码成 `platform=instagram` + 不传 manSocialId → FOLLOW_BACK todo 进 **instagram** ready 队列,但设备是 **snapchat** 在线 → 调度器 tick 只配对 `creator×snapchat` → 永远取不到 → 不派发。

**改了什么(3 处,均 app-social-proxy-server)**:
- `v3/dashboard/admin.controller.ts` one-click CE 分支:`platform: "IG"` → `body.platform==="snapchat"?"SNAPCHAT":"IG"`,并透传 `manSocialId: payload.manSocialId`。
- `v3/types/ce-event.ts` `CEExchangedEvent` 加可选 `manSocialId?: string`。
- `v3/signal/adapters/ce-kafka.adapter.ts` `onCEEvent`:`manSocialId = event.manSocialId || resolveHandleByUserId(...) || ""`(显式优先、反查兜底)。

**影响面**:真实 Kafka CE→todo→派发生产流程**零行为变化**(Kafka 事件不带 manSocialId,仍走反查);仅 admin one-click 测试链行为改变。

**验证**:改动 3 文件 tsc 0 error、eslint 0 error;ce-kafka spec 的失败为既有(mock 未注入 identity,干净 HEAD 同样失败)。pre-commit lint+circular 过;pre-push 因全仓无关包测试失败,授权 `--no-verify`。

**commit/push**:`spv2_test 6d0421c34` → push;merge 到 `release/test-sp 5d3538676` → push。

**残留**:
- [ ] admin-api-dev 重新部署 test-sp 后,重打 CE one-click(platform=snapchat + payload.manSocialId)实测设备能收到 FOLLOW_BACK 并 handle 非空。

## 工作日志 — 05:07(build 修复补记)

**问题**:上条 CE 修复 deploy 时 nest build 报 `TS2339: Property 'platform' does not exist on type 'OneClickBody'`。one-click CE 分支用了 `body.platform`,但 `OneClickBody` 接口没声明该字段。
**根因(自身失误)**:本地验证用 `tsc --noEmit | tail -15`,把顶部 admin.controller 的报错截掉了,只看到 spec 报错就误判通过。
**修复**:`OneClickBody` 加 `platform?: string`。改用 `pnpm build`(nest build)完整验证,exit=0。
**教训**:验证 build 别用 `tail` 截输出;要么 `grep -E "error TS|Found .* error"`,要么直接看 exit code。已沉淀到 troubleshooting。
**commit/push**:`spv2_test f781dfb3b` → push;merge `release/test-sp d923e9666` → push。

### 18:42 — SC 加「服务条款更新」弹窗自动关闭(加载后查一次,不轮询)

**需求**:Snapchat 偶尔弹「我们更新了服务条款」拦截弹窗,需自动点「同意并继续」关掉。用户要求**只在页面加载完成后检查一次**,不要轮询。

**改动(3 文件)**:
- 新增 `actions/dismissTermsPopup.js`:注入后 `window load`(或已 complete)+2.5s 延迟**查一次**;找文案匹配「同意并继续」的 `<button>` 且同页存在「稍后提醒我」按钮(双按钮共存=确是该弹窗,防误点)→ clickOnce 关闭。暴露 `SP.dismissTermsPopup()` 手动一次性。class 名是构建 hash 不用,纯按文案(LABELS+matchLabel)。
- `utils.js` LABELS 加 `agreeAndContinue`/`remindLater`(zh 由 DOM 确证,en/es 推测)。
- `scripts/build.mjs`:`a:dismissTermsPopup` 注册进 SC `automation` bundle(utils/bridge 之后、executeAction 之前)。

**范围**:先只进 automation(主 web app www.snapchat.com/web)。若 welcome 页也弹再加 bootstrap/login。
**验证**:build 无错(SC automation 18→19 脚本),包测试 2 passed,bundle 含 dismissTermsPopup/agreeAndContinue/remindLater。未 commit。

**残留**:
- [x] 已 commit `b2dba8c27` + push(`6b7e4a36f..b2dba8c27`,门禁过)
- [x] 已 commit `b2dba8c27` + push(`6b7e4a36f..b2dba8c27`,门禁过)
- [x] **补 upload 注册**:新脚本要进 `upload-all.mjs` 的 `SC_SCRIPT_GROUP_MAP`(否则 per-script 上传漏掉);dismissTermsPopup 加 automation order 17、executeAction 顺延 18(commit `535f572d5`)。改老脚本内容不用动 map。
- [ ] 重部署实测(弹窗出现时是否被自动点掉)
- [ ] en/es 按钮文案未实测,拿到实际 DOM 校准 LABELS
- [ ] 「加载后查一次」:若弹窗在 load 后 >2.5s 才渲染可能漏(按用户要求不轮询)

### 19:24 — dismissTermsPopup 扩展:也关「欢迎使用网页版 Snapchat」引导

**背景**:实测 dismissTermsPopup 挂载了、加载后查了一次,但**没找到条款弹窗**(只在真点时打日志,故无输出)——因为当时挡着的是**另一个**弹窗:「欢迎使用网页版 Snapchat!」首次进入引导(按钮「下一步」),把界面盖住导致 followUserById 报 Add Friends not found。用户发来该遮罩 DOM,要求「关掉」。

**改动(dismissTermsPopup.js + utils.js,2 文件)**:
- 把 dismissTermsPopup 泛化为「加载后查一次、关掉已知拦截弹窗」:① terms(同意并继续,gated by 稍后提醒我)② web-welcome(欢迎使用网页版 Snapchat,点「下一步」)。
- web-welcome:标题用 **contains 匹配**(`hasLeafTextContaining`,标题带「！」exact 会漏)确认身份,然后点「下一步」;**带上限的连点**(≤6 次、700ms/次,标题消失即停)以关掉可能的多步引导——仍是「查一次」内完成,非常驻轮询。
- 加调试日志:没找到任何已知弹窗时打 `checked, no known blocking popup`(回应用户「执行了吗」的困惑)。
- LABELS 加 `webWelcomeTitle`(contains)、`nextStep`(注意与 story 的 `next`「下一个/下一页」不同)。eventTrack 统一 `sc_dismiss_popup {popup}`。

**验证**:build 无错,包测试 2 passed,bundle 含 dismissWebWelcome/webWelcomeTitle/nextStep/「checked, no known」。upload map 不用改(脚本已注册)。未 commit。

**残留**:
- [x] 已 commit `ca8e29f6c` + push(`535f572d5..ca8e29f6c`,门禁过)
- [ ] 重部署实测(欢迎引导被点掉;若多步且标题跨步变化,可能只点一步,再看 DOM 调)
- [ ] en/es 文案未实测校准


### 19:36 — 引导弹窗改多步:按帮助中心页脚 gate、逐步点「开始/下一步/跳过」

**实测**:web-welcome 引导是**多步且每步标题变**:step1「欢迎使用网页版 Snapchat」→下一步→ step2「添加你的挚友 3/6」→需点「开始」。上版按标题(webWelcomeTitle)gate,点完 step1 标题变了就停,卡在 step2。

**改法(dismissTermsPopup.js + utils.js)**:
- gate 从「标题」换成**每步都带的帮助中心页脚**(`如果你遇到任何问题…帮助中心`,contains 匹配)—— 只要页脚在就说明仍在引导遮罩内。
- `dismissWebWelcome`→`dismissOnboarding`:一次检查内逐步点**优先级** finish(`开始/完成`)→ next(`下一步`)→ skip(`跳过`),直到页脚消失。step1 无 finish 按钮→点下一步;step2 有「开始」→点开始→引导结束。上限 10 步、每步 1200ms(等下一步渲染再 re-check)。
- 加好友/复制/已添加等按钮无匹配文案,永不误点(findButtonByLabel 只精确匹配 button 文本)。
- LABELS:删 `webWelcomeTitle`,加 `onboardingFooter`(contains gate)、`finishOnboarding`(开始/完成/Start/Done…)、`onboardingSkip`(跳过);`nextStep` 保留。

**验证**:build 无错,包测试 2 passed,bundle 含 onboardingFooter/finishOnboarding/onboardingSkip,webWelcomeTitle=0。未 commit。

**残留**:
- [x] 已 commit `081684c7c` + push(`ca8e29f6c..081684c7c`,门禁过)
- [ ] 重部署实测(多步引导一路点到进主界面)
- [ ] 若某步无 开始/下一步/跳过 而是别的按钮,会 break 卡住——再看 DOM 补 LABELS
- [ ] en/es 文案未实测校准


### 19:50 — 弹窗处理改「限时轮询」:3s×15(~45s)、正常页面出现即停

**实测**:日志 `[SC-Terms] checked, no known blocking popup` 在 2.5s 就打了(那时欢迎弹窗还没渲染),DM listener 15s 后才看到 `下一步`——**「只查一次」查早了、漏了后渲染的弹窗**(截图确认欢迎弹窗在)。

**改法(用户拍板,dismissTermsPopup.js + utils.js)**:
- 加载后开**限时轮询**:每 `WATCH_INTERVAL_MS=3000` 查一次、最多 `WATCH_MAX_TICKS=15`(~45s),**正常页面出现就提前停**(可不到 45s)。事件驱动 vs 定时的折中,**有时限、非常驻**。
- `isNormalPageReady()`:无 onboardingFooter + 无 terms「同意并继续」按钮 + 有主界面标记(`div[role="list"]` 聊天列表 或 `input[role="searchbox"]`)→ 视为正常页,停止轮询。
- 每 tick 调 `dismissKnownPopups`(terms + 多步 onboarding);去掉「没找到就打日志」的负日志(轮询会刷屏),改为停止时打 `normal page ready, stopping...` / `reached max ticks`。
- 每次 dismiss 是 await 完再排下一 tick(不重叠;onboarding 多步点击期间不会并发)。
- onboardingFooter 放宽:加「如果您遇到任何问题」容忍 你/您。

**验证**:build 无错,包测试 2 passed,bundle 含 startWatch/isNormalPageReady/WATCH_MAX_TICKS/您变体,CHECK_DELAY_MS=0。未 commit。

**残留**:
- [x] 已 commit `321e546c5` + push(`081684c7c..321e546c5`,门禁过)
- [ ] 重部署实测(弹窗延迟渲染也能被抓到并点过、进主界面即停)


### 19:59 — 修引导判据 bug:footer span 含 <strong> 被「只看叶子」跳过

**实测**:限时轮询跑满 15 次 `popup watch reached max ticks, stopping` 却没点掉「下一步」,也没有 `web onboarding detected` 日志。

**根因**:页脚 DOM 是 `<span>如果你遇到任何问题，请访问我们的<strong><a>帮助中心</a></strong></span>`——这个 span **有子元素(strong)**,而我原来的 `hasLeafTextContaining` 只在 `el.children.length===0`(叶子)时才匹配 → 含 strong 的 footer span 被跳过 → `onboardingFooter` gate 永远 false → dismissOnboarding 从不触发,轮询空转到 max ticks。**按钮匹配没问题,是判据挂了。**

**修法(仅 dismissTermsPopup.js)**:`hasLeafTextContaining` → `pageTextContains`:直接查 `document.body.textContent.toLowerCase().indexOf(label)`,不再限叶子节点 → 文本跨 `<strong>` 嵌套也能匹配。3 处调用(dismissOnboarding gate ×2、isNormalPageReady ×1)一并切换。

**教训**:contains 判据别限「叶子节点」——目标文本常跨子元素(链接/加粗)拆开。gate 用整页/容器 textContent 更稳。

**验证**:build 无错,包测试 2 passed,bundle 含 pageTextContains ×4、旧名 0。仅 dismissTermsPopup.js 变更。未 commit。

**残留**:
- [x] 已 commit `8d3563661`(含判据修复 + 每 tick 日志)+ push(`321e546c5..8d3563661`,门禁过)
- [ ] 重部署实测(这次应真能点过引导;每 tick 有 `popup watch tick N/15` 日志)


### 20:20 — clickMute/sendGreet 搜索选择器修:主侧边栏结果卡是 div[tabindex=0] 不是 role=listitem

**实测**:clickMute 报 `FRIEND_NOT_FOUND`(候选 [role=listitem]=0),搜「建斌 历」侧边栏只出「询问 My AI」。

**根因**:主侧边栏搜索结果卡片是 `<div class="_AmUf" tabindex="0">`(内含 显示名 span + 用户名 span),**不是 `[role="listitem"]`**(那是「添加挚友」引导用的结构)。findFriendRowByName 只在 `[role="listitem"]` 里找 → 主搜索永远找不到。clickMute + sendGreet 两处同款 helper 都有此 bug。

**修法(clickMute.js + sendGreet.js)**:
- findFriendRowByName 改**结构无关**:遍历**文本等于名字的 `<span>` 叶子**(只认 span,避开「询问 My AI」的 `<p>` 查询文本),上溯最近可点击祖先(`[role="listitem"]/[tabindex="0"]/[role="button"]`)→ 点它开会话。名字比较**去空格**(容忍「建斌 历/建斌历」空格差异,可能正是「建斌 历」搜不到的原因)。
- clickMute 的 findChatHeader:优先找**顶栏 downshift toggle**——`[aria-haspopup="listbox"]` 且文本含目标名字(避开自己头像那个无名字的 toggle),点它出静音;`button[aria-label]` + 位置兜底保留。顶栏 DOM 由用户确证:`<div id="downshift-325-toggle-button" aria-haspopup="listbox" tabindex="0">…<span>川 步</span></div>`。

**教训**:同一「好友卡片」在不同 UI 上下文结构不同(引导 besties=role=listitem,主搜索=div[tabindex=0])。选择器别绑单一 role,按「名字叶子→可点击祖先」更稳。

**验证**:build 无错,包测试 2 passed,搜索选择器不再限 role=listitem。clickMute.js + sendGreet.js 变更。未 commit。

**残留**:
- [x] 已 commit `ea5fcaced` + push(`8d3563661..ea5fcaced`,门禁过)
- [ ] 重部署实测(搜到好友→开会话→点顶栏→静音 全链路)
- [ ] 顶栏/搜索卡选择器仍是 hash-class 无关的语义匹配,发版稳但需回归


### 20:30 — sendGreet Step0 点击方式修:simulateClick→全指针序列(否则搜到卡片也开不了会话)

**实测**:上一条搜索选择器修复后 clickMute **成功**(搜「建斌 历」→开会话拿 chatId `c4ab4929…`→静音 verified:true)。但 sendGreet 同样搜到卡片(`Opening chat for: 建斌 历`)后 `checkChatPageLoaded` 30s 超时、`inChat=false`,截图主区还停在相机首页 → **点了卡片没开会话**。

**根因**:clickMute Step0 用 clickMute 自己的 `click()`=**全指针序列**(pointerdown→mousedown→pointerup→mouseup→click)→ 卡片能开;sendGreet Step0 用 `utils.simulateClick(row)`。SC 搜索卡片靠 **pointerdown** 触发,simulateClick(click+el.click+React onClick,还 2-3 连发)点不动。正是 memo 早记的坑「clickMute/followUserById/getStory 已弃 simulateClick 改单次全序列,唯 sendGreet 没改」。

**修法(仅 sendGreet.js)**:加本地 `clickCard(el)`(单次全指针序列,同 clickMute），Step0 `utils.simulateClick(row)` → `clickCard(row)`。

**验证**:build 无错,包测试 2 passed,sendGreet Step0 不再 simulateClick。仅 sendGreet.js。未 commit。

**教训**:SC 上所有「开会话/点卡片/点菜单项」都必须用全指针序列单次 dispatch;utils.simulateClick 一律别用于这类元素。

**残留**:
- [x] 已 commit `6023757ef` + push(`ea5fcaced..6023757ef`,门禁过)
- [ ] 重部署实测(sendGreet 全链路:搜→开会话→发消息)


### 20:39 — sendGreet 开会话改「点头像」(卡片本身点不开)

**实测**:sendGreet 换全指针序列后仍超时、`inChat=false` 会话没开。分析:clickMute 与 sendGreet 现在**同元素同点击方式**,却 clickMute 能开、sendGreet 不能 → 结论:**点 `_AmUf` 卡片本身打不开会话**;之前 clickMute「成功」是因为那会话已经开着(前面开过)。对上用户提示「需要搜索出来这个人的**头像**」——点名字区=看资料预览,点头像=进会话。

**修法(仅 sendGreet.js,假设验证)**:`clickCard(row)` 点击目标 `row` → `row.querySelector("img")||svg||row`(卡片内头像),全指针序列点。clickMute 先不动(用户确认可用),验证 sendGreet 成功后再对齐 clickMute。

**验证**:build 无错,包测试 2 passed。仅 sendGreet.js。未 commit。

**残留**:
- [ ] 待实测:点头像能否真正开会话
- [ ] 若成功,clickMute 也改点头像(它之前大概率靠会话已开)


### 20:44 — 搜索前先清空+停顿(clickMute 后搜索框残留导致 sendGreet 抓旧卡片)

**用户诊断(对)**:clickMute 结束后搜索框没清空(留着「建斌 历」)。sendGreet 再跑时 typeIntoSearch 是「清空→立刻设回同样值」中间无停顿,React 可能不识别为真变化→结果列表不刷新→抓到上一次搜索的**旧卡片(已失效)**,点了没反应(会话不开、checkChatPageLoaded 超时)。用户 DOM 确认好友卡确实是 `role="listitem" > div._AmUf[tabindex=0]`(建斌 历/v_ilittle7)。

**修法(clickMute.js + sendGreet.js)**:
- Step0 搜索前先 `clearSearch(searchInput)`(native value setter 置空+input 事件)→ `randomDelay(300,600)` 停顿 → 再 `typeIntoSearch`。保证一次全新搜索、拿到新 DOM 卡片。两方法都加。
- sendGreet 的 `clickCard` **还原为点卡片本身**(上次改点头像是猜错;clickMute 点卡片能开会话已证明卡片可点)。

**验证**:build 无错,包测试 2 passed,bundle 含 clearSearch。clickMute.js + sendGreet.js。未 commit。

**教训**:受控 input 清空后立刻设回同值,React 可能不 re-render;要「清空→停顿→再输入」。搜索类操作前务必先清空+停顿,别复用上次残留结果。

**残留**:
- [x] 已 commit `2eeddb1a0` + push(`6023757ef..2eeddb1a0`,门禁过)
- [ ] 重部署实测(sendGreet 搜→开会话→发消息全链路)
